---
phase: 28-latin-export-and-milestone-evidence
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/multilang/services/latin_export.py
  - src/multilang/cli.py
  - tests/services/test_latin_export.py
  - tests/cli/test_generate_latin_mvp_command.py
autonomous: true
requirements: [EXP-01, EXP-03]
must_haves:
  truths:
    - "User can export the approved Classical Latin 50-card deck to APKG, CSV, and TSV."
    - "APKG output packages both word and sentence WAV media for every Latin card."
    - "CSV/TSV output uses Anki import headers and the same stable Latin field order as the APKG note type."
  artifacts:
    - path: "src/multilang/services/latin_export.py"
      provides: "Latin APKG/CSV/TSV writer functions and result contracts"
      exports: ["export_latin_mvp_bundle"]
    - path: "src/multilang/cli.py"
      provides: "export-latin-mvp command"
    - path: "tests/services/test_latin_export.py"
      provides: "APKG and tabular export regression tests"
    - path: "tests/cli/test_generate_latin_mvp_command.py"
      provides: "CLI export command smoke tests"
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/latin_export.py"
      via: "export-latin-mvp command calls export_latin_mvp_bundle"
      pattern: "export_latin_mvp_bundle"
    - from: "src/multilang/services/latin_export.py"
      to: "data/latin_mvp/audio/latin-mvp-50-v1/*.wav"
      via: "media_index passed to genanki Package"
      pattern: "media_index"
---

<objective>
Wire the Latin export rows into APKG, CSV, TSV, and CLI output.

Purpose: Approved Phase 28 rows must become user-importable Anki artifacts with packaged media and scanner-friendly command output.
Output: A dedicated `export-latin-mvp` command and tested APKG/CSV/TSV artifact writer.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-06-SUMMARY.md
@src/multilang/services/latin_export.py
@src/multilang/services/export_anki_package.py
@src/multilang/services/export_tabular_bundle.py
@src/multilang/cli.py

<interfaces>
Existing export patterns to follow without mutating existing note models:

From `src/multilang/services/export_anki_package.py`:
```python
class MultilangNote(genanki.Note):
    @property
    def guid(self) -> str: ...
def export_anki_package(*, rows: list[ExportCardRow], media_index: dict[str, Path], output_path: Path, deck_name: str) -> ExportAnkiPackageResult
```

From `src/multilang/services/export_tabular_bundle.py`:
```python
def write_export_tabular_bundle(*, rows: list[ExportCardRow], export_format: ExportArtifactFormat, output_dir: Path, deck_name: str, note_type_name: str) -> ExportTabularBundleResult
```

From Plan 28-01 output in `src/multilang/services/latin_export.py`:
```python
LATIN_EXPORT_FIELD_NAMES: tuple[str, ...]
LATIN_NOTE_TYPE_NAME: str
LATIN_DECK_NAME: str
class LatinExportBundle: rows: list[LatinExportRow]; media_index: dict[str, Path]
def build_latin_export_rows(...) -> LatinExportBundle
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Latin APKG and tabular artifact writers</name>
  <files>src/multilang/services/latin_export.py, tests/services/test_latin_export.py</files>
  <behavior>
    - Test 1: exporting APKG writes a package with 50 notes, the Latin note type name, stable Latin fields, and 100 packaged WAV media entries.
    - Test 2: exporting CSV and TSV writes `#separator`, `#html:true`, `#notetype:Multilang::Classical Latin MVP`, `#deck:Multilang::Classical Latin::MVP 50`, and `#columns:` matching `LATIN_EXPORT_FIELD_NAMES`.
    - Test 3: APKG note fields contain `Latin Word`, `Latin Sentence`, Portuguese translations, `Gramatica`, source, word_audio, sentence_audio, blank Image, and no `Classe` field.
  </behavior>
  <action>Extend `latin_export.py` with `LatinExportArtifactResult`, `build_latin_anki_model()`, `export_latin_mvp_apkg(...)`, `write_latin_tabular_export(...)`, and `export_latin_mvp_bundle(...)`. Reuse genanki and the `MultilangNote` GUID pattern, but define a dedicated Latin model ID and note type locally so existing frequency/manual/highlight model IDs and fields remain unchanged. For tabular outputs, serialize rows using `LATIN_EXPORT_FIELD_NAMES` and Anki import headers.</action>
  <verify>
    <automated>uv run pytest tests/services/test_latin_export.py tests/services/test_export_anki_package.py tests/services/test_export_tabular_bundle.py -q</automated>
  </verify>
  <done>APKG, CSV, and TSV writers produce importable Latin artifacts from the approved 50-row bundle while existing export tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add export-latin-mvp CLI command</name>
  <files>src/multilang/cli.py, tests/cli/test_generate_latin_mvp_command.py</files>
  <behavior>
    - Test 1: `export-latin-mvp --format apkg --output-dir &lt;tmp&gt;` prints `artifact_path=...`, `card_count=50`, `media_count=100`, `note_type=Multilang::Classical Latin MVP`, and `export_status=completed`.
    - Test 2: CSV and TSV CLI invocations produce corresponding files and print stable public key=value lines without absolute audio paths or provider secrets.
    - Test 3: invalid format or blocked readiness exits nonzero and prints the fail-closed validator message.
  </behavior>
  <action>Add a dedicated Typer command `export-latin-mvp` with `--format {apkg,csv,tsv}`, `--output-dir`, and optional `--deck-name`. It must call `export_latin_mvp_bundle(...)` and print only public aggregate result lines. Do not route Latin through the existing persisted-job `export` command and do not add Latin to the modern-language `generate` command.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_generate_latin_mvp_command.py tests/services/test_latin_export.py -q</automated>
  </verify>
  <done>User can create APKG, CSV, and TSV Latin exports from the CLI with scanner-readable output and no existing command contract drift.</done>
</task>

</tasks>

<source_audit>
GOAL: Covered by writing APKG, CSV, and TSV artifacts with packaged media.
REQ: EXP-01 covered by dedicated note model; EXP-03 covered by APKG/CSV/TSV writers and CLI.
RESEARCH: Uses existing Python/genanki/csv stack already present in `pyproject.toml`; no new dependency.
CONTEXT: Honors Latin isolation, approved audio policy, blank Image, and privacy-safe aggregate CLI output decisions from Phases 22-27.
Deferred exclusions: Existing persisted-job export routing is not extended to Latin; Latin scale beyond 50 and automatic image sourcing remain excluded.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input -> filesystem output | User-provided output directory and format control generated artifacts. |
| committed media -> APKG package | WAV files are copied into a distributable deck archive. |
| export result -> terminal | CLI output can expose paths or sensitive provider detail if not constrained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | T | `export_latin_mvp_bundle` | mitigate | Reuse Plan 28-01 fail-closed asset/readiness validation before writing any artifact. |
| T-28-02-02 | I | `export-latin-mvp` CLI | mitigate | Print artifact path and aggregate counts only; do not print media storage paths, provider raw data, or secrets. |
| T-28-02-03 | D | APKG media packaging | mitigate | Deduplicate sound references and require each referenced media file through the validated media index before package write. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_latin_export.py tests/cli/test_generate_latin_mvp_command.py tests/services/test_export_anki_package.py tests/services/test_export_tabular_bundle.py -q`.
</verification>

<success_criteria>
Plan succeeds when all three Latin export formats are produced from committed approved assets, contain 50 cards and 100 media references where applicable, and CLI output remains public and scanner-readable.
</success_criteria>

<output>
After completion, create `.planning/phases/28-latin-export-and-milestone-evidence/28-02-SUMMARY.md`
</output>
