---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "23"
type: execute
wave: 23
runtime: opencode
assurance: self_checked
depends_on: [31-22]
files-modified:
  - src/multilang/domain/korean.py
  - src/multilang/services/korean_curriculum.py
  - src/multilang/services/korean_foundation_review.py
  - src/multilang/services/korean_foundation_media.py
  - tests/domain/test_korean.py
  - tests/services/test_korean_curriculum.py
  - tests/services/test_korean_foundation_review.py
  - tests/services/test_korean_foundation_media.py
autonomous: true
tdd: true
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals: [Do not migrate evidence snapshot export CLI integration, modify assets/requests/evidence, activate, export, or call providers.]
hard_boundaries:
  - Production-default source resolution reads one validated current-candidate bundle; explicit v1 history remains immutable and opt-in.
  - Review/media state remains needs_review and production readiness remains false.
escalation_triggers: [Stop on mixed bundle identities, schema widening, curriculum invariant drift, accidental v1 default, or readiness becoming true.]
approval_gates: [None; migration grants no review or production authority.]
anti_regression_targets: [Strict i+1 ordering, NFC, exact 92/47 records and 509 slots, v1 history, pending gates, no-provider behavior.]
known_unknowns: [Evidence/snapshot/export consumers migrate only in Plan 31-24.]
ui_proof_slots: []
no_ui_proof_rationale: Domain and service source resolution only.
high_leverage_surfaces: [src/multilang/services/korean_curriculum.py, src/multilang/services/korean_foundation_review.py, src/multilang/services/korean_foundation_media.py]
second_pass_required: true
high_leverage_second_pass: Trace default and v1-history source identities through curriculum, curation gates, media slots, and readiness refusal.
closure_claim_limit: Claim first-group v2 consumer migration with pending production only.
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
leverage: {lost: Explicit version-aware resolver seam, kept: Existing service schemas and v1 history, gained: One exact candidate bundle across curriculum review and media}
must_haves:
  truths:
    - Default curriculum/review/media consumers resolve exact v2 from one atomic candidate pointer.
    - Explicit v1 history remains loadable without changing production defaults.
    - All curation and media gates remain pending and readiness is false.
  artifacts:
    - {path: src/multilang/services/korean_curriculum.py, provides: Exact v2 strict-i+1 default curriculum}
    - {path: src/multilang/services/korean_foundation_review.py, provides: Exact v2 review projection with pending gates}
    - {path: src/multilang/services/korean_foundation_media.py, provides: Exact v2 media projection with pending gates}
  key_links:
    - {from: current-candidate.json, to: curriculum review media loaders, via: one fixed validated bundle resolver}
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 23

## Objective
Migrate curriculum, review, and media consumers to exact atomic v2 defaults while preserving explicit immutable v1 history and pending production gates.

## Context
- `31-APPROACH.md`
- `31-PATTERNS.md`
- `31-22-SUMMARY.md`
- `31-10-SUMMARY.md`

## Pattern Compliance
- **31-23-01/02:** self-extend current loaders and projections with one version-aware candidate resolver; preserve schemas and validation order.

## Requirements Covered
- **KHAN-01/02:** exact 92-item NFC strict-i+1 Hangul curriculum and review/media projection.
- **KPRO-01/02:** exact 47-item P0-P13 strict-i+1 pronunciation curriculum, nine fields, and review/media projection.

## Must-Haves
1. Named failing default-v2 tests before code.
2. One bundle identity across all first-group consumers.
3. Explicit v1 history and false production readiness.

## Anti-Goals
- No evidence/snapshot/export/CLI migration or canonical write.

## Hard Boundaries
- No per-file latest-version lookup; the pointer chooses a complete tuple.

## Evidence Contract
- RED/GREEN tests assert exact versions/hashes/counts, curriculum dependency closure, v1 history, pending gates, and no provider construction.

## Common Pitfalls
- Reading four files independently, translating draft status into approval, or deleting v1 compatibility required for historical snapshots.

## Stop-And-Challenge
- Stop on any consumer that cannot bind one exact bundle root.

## Approval Gates
- None.

<checks><plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check></checks>

## Tasks
<task id="31-23-01" type="auto">
  <name>Migrate domain and curriculum resolution to exact v2 test-first</name>
  <files>
    - MODIFY: tests/domain/test_korean.py
    - MODIFY: tests/services/test_korean_curriculum.py
    - MODIFY: src/multilang/domain/korean.py
    - MODIFY: src/multilang/services/korean_curriculum.py
  </files>
  <action>Add named failing tests that production defaults resolve one exact candidate bundle and explicit history resolves v1. Update minimal loader/resolver seams; preserve NFC, exact concept identities, H0-H10/P0-P13 ordering, strict-i+1 dependency closure, fields, source ranks, and no-provider behavior.</action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_curriculum.py::test_curriculum_defaults_to_atomic_v2_candidate_bundle -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/domain/test_korean.py tests/services/test_korean_curriculum.py -q</automated>
  </verify>
  <done>Domain/curriculum defaults bind exact v2 and explicit v1 history remains valid.</done>
</task>
<task id="31-23-02" type="auto">
  <name>Migrate review and media projections without widening authority</name>
  <files>
    - MODIFY: tests/services/test_korean_foundation_review.py
    - MODIFY: tests/services/test_korean_foundation_media.py
    - MODIFY: src/multilang/services/korean_foundation_review.py
    - MODIFY: src/multilang/services/korean_foundation_media.py
  </files>
  <action>Add the named failing `test_review_and_media_default_to_exact_v2_bundle_with_all_gates_pending` assertion before implementation, covering v2 default, one bundle root, 92/47 family split, 139 curation records, 509 media slots, exact request hashes, and every gate pending. Then resolve both services through the same bundle root and update fixed manifest/version bindings only where required. Preserve explicit v1 history, production false, deterministic validation, and zero asset/evidence mutation.</action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_review.py::test_review_and_media_default_to_exact_v2_bundle_with_all_gates_pending -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_review.py::test_review_and_media_default_to_exact_v2_bundle_with_all_gates_pending -q &amp;&amp; UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_review.py tests/services/test_korean_foundation_media.py -q</automated>
  </verify>
  <done>Review/media consumers bind exact v2 while all qualified-human/rights/playback gates remain pending.</done>
</task>

## Verification
- Run both first-group suites together and compare candidate/request/evidence/production prestates.

## Success Criteria
- Curriculum, review, and media agree on one exact v2 bundle with no authority widening.

## High-Leverage Review
- Trace one Hangul and one P13 item through curriculum, review, and every required media slot.

## Leverage Review
- Lost: Version resolver seam.
- Kept: Current service contracts.
- Gained: Atomic v2 identity propagation.

## Notes
- Historical v1 loading is concrete compatibility for immutable snapshots, not a second production default.

<output>After completion, create `31-23-SUMMARY.md` with bundle/member hashes, exact counts, curriculum invariants, v1-history proof, pending/readiness proof, test results, and zero-write diff.</output>
