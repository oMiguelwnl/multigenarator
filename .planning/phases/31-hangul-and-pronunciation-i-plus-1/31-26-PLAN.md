---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "26"
status: superseded
superseded_by: [31-29, 31-30, 31-31, 31-32]
type: execute
wave: 26
runtime: opencode
assurance: self_checked
depends_on: [31-25]
files-modified:
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/evidence-confirmation.json
autonomous: false
tdd: false
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals:
  - Do not invent/edit/repair/fetch/synthesize evidence, let AI fill reviewer roles, write a receipt/snapshot, activate, export, or claim Anki acceptance.
hard_boundaries:
  - The fixed inbox is one indivisible all-or-nothing bundle consumed by `inspect-inbox`; no arbitrary path/import/archive/URL option exists.
  - Evidence must bind exact v2 candidates/request hashes and exact licensed bytes; v1/draft bindings are stale.
escalation_triggers:
  - Stop with a named blocker if any reviewer qualification/role separation, Portuguese policy, P11-P13 acceptance, rights disposition, exact byte, PCM WAV playback, or independently confirmed index hash is unavailable.
approval_gates:
  - `checkpoint:user` requires direct placement of genuine evidence plus exact independent confirmation of the evidence-index SHA-256.
anti_regression_targets:
  - Candidates, drafts, requests, source code, receipt/snapshots/pointer/exports, and `.venv` remain unchanged; only the fixed inbox and evidence handoff may be new.
known_unknowns:
  - Genuine external evidence may be unavailable; the correct plan result is a zero-canonical-mutation blocker.
ui_proof_slots: []
no_ui_proof_rationale: Human evidence intake and hash confirmation only.
high_leverage_surfaces:
  - evidence-inbox/
second_pass_required: true
high_leverage_second_pass: Reconcile every v2/request/reviewer/policy/rights/media/playback/index hash and role-separation rule before recording confirmation.
closure_claim_limit: Claim only complete genuine v2-bound inbox validation and confirmed index handoff, or explicit blockers; no durable receipt or production state.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: External human/legal/media work remains nonautomatable.
  kept: Independent qualified authority and exact-byte evidence.
  gained: Reviewers assess complete selected candidate copy rather than placeholders.
must_haves:
  truths:
    - The fixed inbox is complete only when every required reviewer, policy, rights, media, and playback category binds exact v2 bytes.
    - Category-level read-only inspection passes before any confirmation handoff.
    - The user independently confirms the exact index hash; AI output and fixtures cannot substitute.
  artifacts:
    - path: evidence-inbox/
      provides: One fixed all-or-nothing genuine evidence bundle.
    - path: execution-handoffs/evidence-confirmation.json
      provides: Validated exact index confirmation for Plan 31-27.
  key_links:
    - from: v2 candidates and request projections
      to: fixed evidence bundle
      via: exact source/request/reviewer/rights/media/playback hashes
    - from: evidence-ready signal
      to: evidence-confirmation.json
      via: fixed handoff writer bound to current evidence-index bytes
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 26

> Historical plan: superseded on 2026-08-27 by the global AI linguistic review
> policy and replacement Plans 31-29 through 31-32. Its blocked summary remains an
> accurate record of the former human-evidence path.

## Objective

Collect one genuine fixed v2 evidence bundle, validate every category read-only, and persist only the independently confirmed index-hash handoff.

## Context

- `31-APPROACH.md`
- `31-RESEARCH.md` resolved evidence decisions
- `31-PATTERNS.md`
- `31-25-SUMMARY.md`
- `evidence-inbox/README.md`

## Pattern Compliance

- **31-26-01:** consume the existing fixed inbox/index/evidence contracts as one atomic bundle; no new importer or schema.
- **31-26-02:** reuse the mapped fixed handoff hash and validation-before-atomic-write pattern.

## Requirements Covered

- **KHAN-01/02:** qualified orthography/curriculum review and exact approved Hangul media.
- **KPRO-01/02:** qualified phonetics/Portuguese/P0-P13 review and exact approved pronunciation media/playback.

## Must-Haves

1. Genuine complete fixed inbox or explicit zero-write blocker.
2. Independent index confirmation after category-level readiness.
3. No receipt, snapshot, pointer, or export mutation.

## Anti-Goals

- No evidence fabrication/repair, role collapse, provider call, durable authority write, activation, export, or visual claim.

## Hard Boundaries

- The atomic bundle contains exactly the index, five proposal/review/rights JSON records, four reviewer records, and exactly indexed media members; README/receipt are excluded from its hash.
- Production audio for this phase is reviewed PCM WAV; images/GIFs use only formats declared by the fixed request. Other audio formats require stop/replan, not silent transcoding.

## Evidence Contract

- `inspect-inbox` reports category readiness for source/request, curriculum, reviewer qualifications/separation, Portuguese policy, P11-P13, rights, media integrity, and playback before returning success.
- Handoff helper verifies the supplied hash equals current `evidence-index.json` and writes outside the evidence bundle.

## Common Pitfalls

- Reusing v1/draft hashes, letting one person satisfy distinct playback roles, accepting unknown redistribution, or echoing inspector output without independent hashing.

## Stop-And-Challenge

- Any missing/ambiguous/unsafe/stale/rejected evidence stops this plan without receipt/snapshot mutation.

## Approval Gates

- Direct placement and `evidence-ready` authorize only Plan 31-27 receipt/snapshot preparation.

<checks>
<plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check>
</checks>

## Tasks

<task id="31-26-01" type="checkpoint:user" gate="blocking">
  <name>Place and independently confirm the indivisible genuine v2 evidence bundle</name>
  <files>
    - CREATE EXACT FIXED MEMBERS UNDER: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/
  </files>
  <action>
    Coordinate qualified reviewers/rightsholders from the exact v2 request documents. Directly place `evidence-index.json`, `proposed-curation.json`, `proposed-media.json`, `curriculum-review.json`, `audio-playback-review.json`, `rights.json`, four fixed reviewer records, and every exactly indexed media member. A qualified Korean phonetics specialist must accept the exact P11-P13 inventory/analysis or trigger replan; a qualified Portuguese reviewer records the editorial policy under canonical code `pt`; rights authority records source/attribution/license/reuse/redistribution; required audio is exact reviewed PCM WAV; phonetics-specialist and independent-native playback roles remain distinct. Run category-level `inspect-inbox`, independently hash/inspect the index, and never edit evidence to make validation pass.
  </action>
  <verify>
    <automated>UV_OFFLINE=1 uv run multilang korean-foundations inspect-inbox</automated>
  </verify>
  <resume-signal>Type exactly `evidence-ready {64-lowercase-hex-evidence-index-sha256}` after independent confirmation, or `blocked: {missing or unresolved evidence}`.</resume-signal>
  <done>One complete genuine v2 bundle passes every category and receives exact independent index confirmation, or execution stops with named blockers.</done>
</task>

<task id="31-26-02" type="auto">
  <name>Record and verify the exact evidence-index confirmation handoff</name>
  <files>
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/evidence-confirmation.json
  </files>
  <action>
    Export the exact resume-signal hash as `CONFIRMED_INDEX_SHA256`; reject missing, malformed, or current-index-mismatched values. Use only the fixed handoff helper to record and read back the confirmation. Re-run `inspect-inbox` before and after; do not write receipt/snapshot state.
  </action>
  <verify>
    <automated>: "${CONFIRMED_INDEX_SHA256:?missing evidence-ready handoff}" &amp;&amp; case "$CONFIRMED_INDEX_SHA256" in *[!0-9a-f]*|'') exit 1 ;; esac &amp;&amp; test "${#CONFIRMED_INDEX_SHA256}" -eq 64 &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations inspect-inbox &amp;&amp; UV_OFFLINE=1 uv run python scripts/phase31_handoff.py record-evidence --sha256 "$CONFIRMED_INDEX_SHA256" &amp;&amp; test "$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-evidence)" = "$CONFIRMED_INDEX_SHA256" &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations inspect-inbox</automated>
  </verify>
  <done>One validated machine-readable evidence handoff binds the exact current index; canonical receipt/snapshot/pointer/export state remains unchanged.</done>
</task>

## Verification

- Require category-complete inspection, exact handoff round-trip, and only inbox/handoff mutations.

## Success Criteria

- Genuine evidence is ready for durable validation, or unavailability is recorded without canonical write.

## High-Leverage Review

- Trace one Hangul and one pronunciation record plus all role/policy/rights/media aggregate hashes through index and handoff.

## Leverage Review

- Lost: Human evidence cannot be automated.
- Kept: Exact independent trust boundaries.
- Gained: Machine-readable validated input for the sole receipt writer.

## Notes

- Treating the fixed bundle as one path is an explicit scope exception: `inspect-inbox` consumes it atomically and no task may mutate individual members after confirmation.

<output>
After completion, create `31-26-SUMMARY.md` with category readiness, exact index/handoff hashes, reviewer/policy/rights/media/playback classifications, allowed writes, and zero receipt/snapshot/pointer/export mutation; if blocked, record exact categories.
</output>
