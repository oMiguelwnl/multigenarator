---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "25"
type: execute
wave: 25
runtime: opencode
assurance: self_checked
depends_on: [31-24]
files-modified:
  - scripts/verify_phase31_runtime_isolation.py
  - tests/services/test_phase31_runtime_isolation.py
  - tests/cli/test_korean_foundation_commands.py
  - tests/integration/test_korean_foundations_flow.py
autonomous: true
tdd: true
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals:
  - Do not create evidence/receipt/snapshot/pointer/exports, alter candidates/services/public CLI, call providers, or mutate `.venv`.
hard_boundaries:
  - Public `korean-foundations` commands remain unchanged and production defaults resolve v2.
  - Runtime isolation helper accepts only fixed `/tmp/multilang-phase31-py312` directly below sticky `/tmp` and repository `.venv` hash operations.
escalation_triggers:
  - Stop on any CLI signature drift, unexpected production readiness, unsafe temp component, shared-environment mutation, or focused regression.
approval_gates:
  - None; this plan proves blocked migration and safe later regression mechanics.
anti_regression_targets:
  - Complete Phase 30 Korean morphology/language/three-mode/persistence/privacy plus normal/manual/highlight, kana, phoneme, Latin, Mandarin, templates, CLI, integration, GUIDs, and no-provider behavior remain green.
known_unknowns:
  - Genuine evidence remains the next blocking dependency.
ui_proof_slots: []
no_ui_proof_rationale: CLI/integration regression and runtime-isolation tooling only.
high_leverage_surfaces:
  - tests/integration/test_korean_foundations_flow.py
  - scripts/verify_phase31_runtime_isolation.py
second_pass_required: true
high_leverage_second_pass: Recheck exact refusal outputs, v2 fixture/GUID bindings, temp ownership/mode/link rules, and deterministic `.venv` hash behavior.
closure_claim_limit: Claim v2 migration regression safety and blocked production only; no genuine evidence, activation, export, or final Python 3.12 closure.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Adds one narrow local runtime-safety helper.
  kept: Public CLI and shared `.venv` invariance.
  gained: Repeatable safe Python 3.12 closure path plus broad v2 regression proof.
must_haves:
  truths:
    - CLI/integration fixtures and GUID expectations bind exact v2 while public commands stay unchanged.
    - Production inbox/readiness/export paths refuse exactly and write nothing before genuine evidence.
    - Fixed temp preparation rejects links, foreign ownership, unsafe modes, and shared `.venv` mutation.
  artifacts:
    - path: scripts/verify_phase31_runtime_isolation.py
      provides: Fixed current-user 0700 environment directly below sticky `/tmp` and deterministic repository `.venv` tree hash.
    - path: tests/integration/test_korean_foundations_flow.py
      provides: v2 private-flow and existing-mode integration regressions.
  key_links:
    - from: v2 service defaults
      to: unchanged public CLI
      via: migrated CLI/integration fixtures and refusal assertions
    - from: fixed isolation helper
      to: Plan 31-28 Python 3.12 suite
      via: safe prepare plus pre/post `.venv` hash
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 25

## Objective

Migrate high-level v2 tests, prove exact blocked-production behavior, and build the safe fixed runtime-isolation helper test-first.

## Context

- `31-APPROACH.md`
- `31-PATTERNS.md`
- `31-24-SUMMARY.md`
- `31-10-SUMMARY.md`

## Pattern Compliance

- **31-25-01:** adapt the mapped snapshot lstat/write-poison and canonical-state digest patterns to the fixed temp/runtime boundary.
- **31-25-02/03:** self-extend the current exact CLI allowlist, private integration flow, refusal, and protected-prestate patterns.

## Requirements Covered

- **KHAN-01/02, KPRO-01/02:** preserve complete foundation behavior and existing modes across the v2 migration.

## Must-Haves

1. Safe fixed runtime helper with deterministic `.venv` hashing.
2. Updated v2 CLI/integration/GUID expectations without public command drift.
3. Exact pending-state refusal and focused regressions.

## Anti-Goals

- No canonical state write, public option, live provider, or full-suite closure claim.

## Hard Boundaries

- `/tmp` must be root-owned sticky `1777`; helper lstats `/tmp` and the one fixed direct child, rejects links/non-directories/foreign ownership or modes other than child `0700`, and securely creates only the absent direct child as current-user `0700`. It never depends on or traverses `/tmp/opencode`.

## Evidence Contract

- Runtime helper gets witnessed RED/GREEN tests; high-level suites prove v2 bindings and exact refusal output.

## Common Pitfalls

- Following symlinks, nesting beneath a group-writable intermediate directory, hashing only `.venv` metadata, or allowing refusal commands to create output paths.

## Stop-And-Challenge

- Stop rather than repair unsafe temp state or source code during regression verification.

## Approval Gates

- None; human evidence intake is Plan 31-26.

<checks>
<plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check>
</checks>

## Tasks

<task id="31-25-01" type="auto">
  <name>Implement fixed runtime-isolation checks test-first</name>
  <files>
    - CREATE: tests/services/test_phase31_runtime_isolation.py
    - CREATE: scripts/verify_phase31_runtime_isolation.py
  </files>
  <action>
    Add failing tests first for fixed `prepare` and `hash-venv` operations, absent `.venv` marker, recursive deterministic relative-path/content/mode hash, link/reparse rejection, root-owned sticky `/tmp`, direct-child containment, current-user ownership, exact child mode 0700, secure absent-child creation, safe owned-child recreation, and no arbitrary path. Implement minimally for `/tmp/multilang-phase31-py312`, with content-free errors and no cleanup/repair of unsafe existing state.
  </action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_phase31_runtime_isolation.py::test_runtime_isolation_contract_is_not_implemented -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_phase31_runtime_isolation.py -q</automated>
  </verify>
  <done>The helper safely prepares only the fixed isolated environment and deterministically proves repository `.venv` invariance.</done>
</task>

<task id="31-25-02" type="auto">
  <name>Migrate CLI and integration regression fixtures to v2</name>
  <files>
    - MODIFY: tests/cli/test_korean_foundation_commands.py
    - MODIFY: tests/integration/test_korean_foundations_flow.py
  </files>
  <action>
    Update test-only candidate/request hashes, source-pack versions, snapshot members, and pre-first-activation GUID expectations to exact v2. Preserve the public command signature and private temporary-root seams. Add exact assertions for production `inbox_incomplete`/`production_not_active`, zero output creation, no provider construction, and explicit v1-history loading.
  </action>
  <verify>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/cli/test_korean_foundation_commands.py tests/integration/test_korean_foundations_flow.py -q</automated>
  </verify>
  <done>High-level tests bind v2 exactly while the public CLI, private test seam, v1 history, and blocked production behavior remain intact.</done>
</task>

<task id="31-25-03" type="auto">
  <name>Prove exact refusal and focused existing-mode regressions</name>
  <files>
    - READ ONLY: data/korean_foundations/
    - READ ONLY: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/
    - READ ONLY: data/korean_foundations/snapshots/
    - READ ONLY: data/korean_foundations/active-foundations.json
    - READ ONLY: .multilang/exports/korean-foundations/
  </files>
  <action>
    Run exact refusal commands through a shell assertion function that requires exit 1 and exact scanner-safe output. Prove no requested export file exists afterward. Run the exact complete Phase 30 focused matrix and exact named existing-mode matrix from `30-08-PLAN.md`, followed by separate foundation and kana/phoneme/template groups. This covers Korean morphology, language support, three-mode modern flow, lexical persistence, privacy-safe highlights, text lifecycle, normal frequency, manual word-list, highlight generation/audio, Japanese, Mandarin, Latin, and Russian behavior before evidence. Compare `.venv`, v1/v2, candidate pointer/bundle, requests, inbox, receipt/snapshot/active pointer/exports, and unrelated worktree prestates; verification writes nothing.
  </action>
  <verify>
    <automated>assert_refusal() { EXPECTED="$1"; shift; set +e; OUTPUT="$("$@" 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; test "$OUTPUT" = "$EXPECTED"; }; assert_refusal "korean_foundations_error=inbox_incomplete" env UV_OFFLINE=1 uv run multilang korean-foundations inspect-inbox &amp;&amp; assert_refusal "korean_foundations_error=production_not_active" env UV_OFFLINE=1 uv run multilang korean-foundations check --family hangul &amp;&amp; assert_refusal "korean_foundations_error=production_not_active" env UV_OFFLINE=1 uv run multilang korean-foundations check --family pronunciation &amp;&amp; assert_refusal "korean_foundations_error=production_not_active" env UV_OFFLINE=1 uv run multilang korean-foundations export --family hangul --format apkg --output /tmp/multilang-phase31-refusal-hangul.apkg &amp;&amp; test ! -e /tmp/multilang-phase31-refusal-hangul.apkg</automated>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/domain/test_jobs.py tests/test_settings.py tests/services/test_frequency_decks.py tests/services/test_audio_voice_registry.py tests/services/test_tatoeba_sentence_source.py tests/domain/test_korean.py tests/services/test_korean_morphology.py tests/services/test_korean_language_support.py tests/domain/test_lexicon.py tests/services/test_lexical_lookup.py tests/services/test_word_list_parser.py tests/services/test_input_fingerprint.py tests/repositories/test_lexical_repository.py tests/test_migration_schema_parity.py tests/services/test_lexical_grounding.py tests/services/test_highlight_candidate_extraction.py tests/services/test_highlight_import_preview.py tests/services/test_highlight_ingest_lexical_items.py tests/services/test_text_generation.py tests/services/test_provider_text_adapters.py tests/services/test_text_validation.py tests/services/test_generate_text_items.py tests/services/test_regenerate_text_item.py tests/test_runtime.py tests/cli/test_kindle_highlight_preview_command.py tests/cli/test_webdav_highlight_commands.py tests/integration/test_korean_modern_flow.py -q</automated>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/integration/test_frequency_e2e_export_flow.py tests/integration/test_custom_word_list_e2e_export_flow.py tests/integration/test_highlight_generation_audio_flow.py tests/integration/test_v13_existing_modes_regression_evidence.py tests/services/test_japanese_furigana.py tests/services/test_assemble_export_cards.py::test_assemble_export_cards_builds_japanese_row_without_ipa tests/integration/test_mandarin_modern_flow.py tests/integration/test_v21_latin_google_tts_final_audio.py tests/services/test_russian_phoneme_deck.py -q</automated>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_phase31_runtime_isolation.py tests/services/test_korean_curriculum.py tests/services/test_korean_foundation_review.py tests/services/test_korean_foundation_media.py tests/services/test_korean_foundation_evidence.py tests/services/test_korean_foundation_snapshot.py tests/services/test_korean_foundation_export.py -q</automated>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_japanese_kana_deck.py tests/services/test_japanese_kana_generated_deck.py tests/services/test_phoneme_deck.py tests/services/test_russian_phoneme_deck.py tests/services/test_card_template_loader.py tests/integration/test_russian_phoneme_template_refresh_flow.py -q</automated>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_latin_source_pack.py tests/services/test_latin_export.py tests/services/test_latin_audio.py tests/integration/test_v20_latin_mode_isolation_evidence.py tests/integration/test_v20_latin_export_evidence.py tests/integration/test_v21_latin_google_tts_final_audio.py -q</automated>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_mandarin_language_support.py tests/services/test_mandarin_orthography.py tests/integration/test_mandarin_modern_flow.py -q</automated>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/cli/test_korean_foundation_commands.py tests/integration/test_korean_foundations_flow.py -q</automated>
  </verify>
  <done>Production refuses exactly with zero write, v2 high-level wiring passes, and existing modes plus shared `.venv` remain unchanged.</done>
</task>

## Verification

- Require exact refusal output, focused suite green, no-provider construction, and all protected prestate hashes unchanged.

## Success Criteria

- v2 migration is regression-safe and production remains correctly blocked before genuine evidence.

## High-Leverage Review

- Recheck CLI signature/output, private integration provenance, helper path safety, and protected-state hashes.

## Leverage Review

- Lost: One extra safe-runtime utility.
- Kept: Existing public and environment contracts.
- Gained: Reliable final Python 3.12 path and migration closure.

## Notes

- The full isolated Python 3.12 suite is intentionally deferred to Plan 31-28 after authorized activation; all named cross-mode focused regressions run here before evidence intake.

<output>
After completion, create `31-25-SUMMARY.md` with helper RED/GREEN evidence, exact refusal outputs, focused counts, CLI/v2/GUID bindings, no-provider proof, and protected-state hashes.
</output>
