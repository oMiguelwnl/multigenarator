---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "31"
type: execute
wave: 30
runtime: opencode
assurance: self_checked
depends_on: [31-29]
files-modified:
  - src/multilang/services/korean_foundation_media.py
  - src/multilang/services/ai_acoustic_review.py
  - scripts/build_korean_foundation_media.py
  - scripts/phase31_handoff.py
  - tests/services/test_korean_foundation_media.py
  - tests/services/test_ai_acoustic_review.py
  - tests/services/test_korean_foundation_media_build.py
  - tests/services/test_phase31_handoff.py
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/media/
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/media-rights.json
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/acoustic-review.json
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/media-authority.json
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/media-lane.json
autonomous: false
requirements: [KHAN-01, KPRO-01]
non_goals:
  - Do not perform linguistic text review, invent rights, use third-party media without authority, publish, activate, export, or claim human listening.
hard_boundaries:
  - Prefer project-generated visuals and exact-context audio; any network/paid call requires bounded project-owner authority.
  - Every byte must bind source/rights, expected text/context, format, integrity, and acoustic evidence.
escalation_triggers:
  - Stop the media lane on absent rights/provider authority, unsupported audio review capability, byte drift, decoder/acoustic failure, or budget drift.
approval_gates:
  - Project owner must return `authorize-media {64-lowercase-hex-authority-sha256}` for the exact rights/provider/item/budget document before any live/paid provider call; local deterministic visual generation needs no checkpoint.
anti_regression_targets:
  - No raw-glyph TTS, placeholder/silence/dummy production media, fallback provider, candidate mutation, active pointer, exports, or unrelated language changes.
known_unknowns:
  - Exact provider terms, credentials, budget, and AI audio-input capability must be established before their consuming operations.
no_ui_proof_rationale: Media bytes and acoustic/integrity evidence only; instrumented Anki observation remains Phase 34.
high_leverage_surfaces:
  - evidence-inbox/media/
  - evidence-inbox/media-rights.json
  - evidence-inbox/acoustic-review.json
second_pass_required: true
closure_claim_limit: Claim exact rights/integrity/AI acoustic evidence only; never claim human-heard playback, publication, device behavior, or linguistic text approval.
parallelism_budget:
  max_concurrent_plans: 2
  safe_parallelism: [31-30]
leverage:
  lost: Convenient third-party assets and implied provider approval.
  kept: Exact-byte fail-closed media contracts and Azure direction.
  gained: Independent media lane that no longer waits for linguistic review.
must_haves:
  truths:
    - Required media use project-owned/generated or explicitly authorized sources with exact rights metadata.
    - Every audio byte passes exact-text/context, decoder, duration, silence/clipping, hash, and applicable AI acoustic review.
    - No AI record claims human listening or legal authority.
  artifacts:
    - path: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/media-rights.json
      provides: Exact source/use rights and provider-processing authority by media member.
    - path: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/acoustic-review.json
      provides: Deterministic and AI acoustic evidence over exact audio bytes.
    - path: .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/media-authority.json
      provides: Project-owner-confirmed exact rights/provider/item/budget authority.
    - path: .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/media-lane.json
      provides: Common-baseline-bound media lane patch and evidence handoff for the join.
  key_links:
    - from: exact media request slots
      to: generated/authorized bytes
      via: expected text/context and rights-bound deterministic filenames
    - from: media bytes
      to: acoustic-review.json
      via: exact hashes, decoders/acoustic validators, and AI audio review where supported
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 31

## Objective

Create the exact rights-cleared foundation media and validate every byte without requiring human linguistic or playback review.

## Context

- `.planning/AI-LINGUISTIC-REVIEW-POLICY.md`
- `31-APPROACH.md`
- `31-AUDIO-PLAYBACK-REVIEW.md` as a historical request projection only
- exact v2 media request in the current candidate bundle

## Requirements Covered

- KHAN-01, KPRO-01

## Must-Haves

1. Exact rights/provider-processing authority for every production byte.
2. Complete required media with no placeholder/silence/raw-glyph synthesis.
3. Deterministic byte integrity plus policy-valid AI acoustic evidence where applicable.

## Anti-Goals

- No text/curriculum review, activation, export, publication, or human-listening claim.

## Hard Boundaries

- Provider authority grants only the exact route, item set, and budget named by the checkpoint.

## Evidence Contract

- Rights, media, decoder, acoustic, and hash validators pass over 100% of required slots.

## Common Pitfalls

- Treating provider success as approval, synthesizing raw glyphs, reusing stale bytes, or recording AI analysis as heard playback.

## Stop-And-Challenge

- Stop only this lane when rights/provider/audio capability is unavailable; Plan 31-29 and Phase 32 offline lanes continue.

## Approval Gates

- Explicit project-owner authority before live/paid provider use; no authority is required for local project-authored deterministic visuals.

<checks>
<plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: Media ownership, provider authority, no-human-claim boundary, and parallel ownership were checked against the global policy.
</plan_check>
</checks>

## Tasks

<task id="31-31-01" type="checkpoint:user" gate="blocking">
  <name>Prepare media plan and authorize only required provider effects</name>
  <files>
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/media-rights.json
    - MODIFY: scripts/phase31_handoff.py
    - CREATE: scripts/build_korean_foundation_media.py
    - MODIFY: tests/services/test_phase31_handoff.py
    - CREATE: tests/services/test_korean_foundation_media_build.py
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/media-authority.json
  </files>
  <action>
    Implement lane-owned planning and authority operations needed to resolve every slot and write `media-rights.json`; do not implement the acoustic schema in this checkpoint task. Define `media-authority.json` with `actor_type="project_owner"`, `agent_authored=false`, confirmation method, exact supplied response/hash, orchestration timestamp, rights-document hash, route, exact item-set hash/count, locked voice/profile ID and version, provider-attempt ceiling, exact budget/currency ceiling, credential boundary, single-use operation ID, consumed status, and replay constraints. Verify every authority scope field against the same authorized rights/request document. Accept only `authorize-media HASH` or `decline: reason`; agent-authored confirmation or stale/replayed authority fails.
  </action>
  <resume-signal>Type exactly `authorize-media {64-lowercase-hex-media-rights-sha256}` or `decline: {reason}`.</resume-signal>
  <verify>
    <automated>: "${PHASE31_BASELINE_SHA256:?set by 31-29 launch}" &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-worktree-runtime --lane media --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$PHASE31_BASELINE_SHA256" &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python scripts/build_korean_foundation_media.py validate-rights &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_handoff.py verify-media-authority --require-project-owner --require-unconsumed --require-voice-profile --require-provider-attempt-ceiling &amp;&amp; test "$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_handoff.py get-media-authority)" = "$(/tmp/multilang-phase31-py312/bin/python -c 'import hashlib,pathlib; print(hashlib.sha256(pathlib.Path(".planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/media-rights.json").read_bytes()).hexdigest())')"</automated>
  </verify>
  <done>Every required slot has a valid rights path and any provider effect has exact bounded authority, or this lane stops with explicit unresolved slots.</done>
</task>

<task id="31-31-02" type="auto">
  <name>Create exact media and run deterministic plus AI acoustic review</name>
  <files>
    - MODIFY: src/multilang/services/korean_foundation_media.py
    - CREATE: src/multilang/services/ai_acoustic_review.py
    - MODIFY: tests/services/test_korean_foundation_media.py
    - CREATE: tests/services/test_ai_acoustic_review.py
    - MODIFY: tests/services/test_korean_foundation_media_build.py
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/media/
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/acoustic-review.json
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/media-lane.json
  </files>
  <action>
    Implement the self-contained frozen `ai_acoustic_review.py` schema and its rejection tests here without importing concurrent Plan 31-30 code. Every relevant record requires the common policy ID/version/hash; `actor_type="ai_model"` and `is_human=false`; route, provider/API, model, prompt/template, and output-schema identities/versions/hashes; distinct exact source/candidate/analyzer/curriculum/media hashes; validator ID/version/result for every deterministic gate; atomic gate verdict; final consensus status; reason and uncertainty codes; confidence; pass/independence identity; orchestration timestamps; exact-byte subject identity; and canonical content hash. Every audio artifact additionally binds NFC-normalized display text, NFC-normalized spoken text, exact text hash, synthesis-request hash, synthesis provider ID/version, locked voice/profile ID/version, locale `ko-KR`, SSML hash and prosody hash or canonical no-SSML/no-prosody sentinels, output format, measured duration, repository-relative path, artifact SHA-256, reviewed-artifact SHA-256, and actual-byte SHA-256, with all three artifact hashes equal. Add rejection tests for drift in each field and continuity across request, bytes, media manifest, and acoustic review. Revalidate every authority scope field and consume the single-use media authority immediately before effects, then run `generate-authorized`. Require exactly 233 current v2 audio subjects and classify all as critical because every foundation audio item teaches jamo, contrast, or a phonological/curriculum rule. With batches of at most 20, assert `ceil(233/20)=12`, `12*3=36` required invocations, and `36*2=72` maximum attempts with one schema-repair retry per invocation. Dispatch at most four concurrent fresh-context tool-less reviewers with 20,000 input tokens, 8,000 output tokens, 600 seconds, 72 total attempts, and exact attachments. Reviewers return closed-schema atomic claims; `ingest-acoustic-pass` validates every mandatory provenance/validator/verdict/consensus field and records orchestration timestamps. `aggregate-acoustic` requires unanimous three-pass verdicts for spoken-text match, clipping/silence/artifact quality, pronunciation/context, and pedagogical usability. Cap exhaustion, disagreement, uncertainty, missing pass, unsupported capability, or deterministic failure is non-passing. Call `record-lane` with explicit external baseline path/hash, allowlisted diff/evidence roots, provider totals, and protected reads.
  </action>
  <verify>
    <automated>: "${PHASE31_BASELINE_SHA256:?set by 31-29 launch}" &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-worktree-runtime --lane media --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$PHASE31_BASELINE_SHA256" &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python -m pytest tests/services/test_korean_foundation_media.py tests/services/test_ai_acoustic_review.py tests/services/test_korean_foundation_media_build.py tests/services/test_phase31_handoff.py -q</automated>
    <automated>PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python scripts/build_korean_foundation_media.py generate-authorized &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python scripts/build_korean_foundation_media.py project-acoustic &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python scripts/build_korean_foundation_media.py acoustic-status &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python scripts/build_korean_foundation_media.py aggregate-acoustic &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python scripts/build_korean_foundation_media.py verify-evidence &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/build_korean_foundation_media.py record-lane --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$PHASE31_BASELINE_SHA256" &amp;&amp; test "$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-lane --lane media --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$PHASE31_BASELINE_SHA256")" = "parallel_lane_status=verified"</automated>
  </verify>
  <done>Every required slot has current rights-bound exact media and passing integrity/acoustic evidence, or an explicit exact-slot blocker.</done>
</task>

## Verification

- Verify exact required-slot coverage, rights scope, provider attempt/budget limits, byte hashes, and no protected output mutation.

## Success Criteria

- The media lane produces a complete exact root consumable by the Phase 31 join without human-review claims.

## High-Leverage Review

- Recompute every rights/request/artifact/acoustic hash and compare against the current v2 media projection.

## Leverage Review

- Lost: Human-heard terminology and convenient unlicensed assets.
- Kept: Exact media integrity and bounded side effects.
- Gained: Parallel rights/media production with automated and AI acoustic evidence.
