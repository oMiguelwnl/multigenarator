---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "11"
type: execute
wave: 11
runtime: opencode
assurance: self_checked
depends_on: [31-10]
files-modified:
  - src/multilang/services/korean_foundation_ai_curation.py
  - scripts/build_korean_foundation_candidates.py
  - tests/services/test_korean_foundation_ai_curation.py
autonomous: true
tdd: true
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals:
  - Do not author draft content, modify v1 assets, create v2 assets, write evidence, call a provider, or grant approval.
hard_boundaries:
  - Draft tooling uses fixed repository roots and enum-only operations; it accepts no arbitrary source/output root, URL, archive, provider, or approval bypass.
  - Draft schemas cannot represent reviewer authority, rights, playback, media bytes, production voice approval, or structural curriculum changes.
escalation_triggers:
  - Stop if complete drafts require changing item identity, sequence, stages, concepts, prerequisites, active rules, media-slot identity, or immutable v1 bytes.
approval_gates:
  - None; this plan creates validation machinery only and cannot select or promote content.
anti_regression_targets:
  - Canonical data, evidence inbox, receipt/snapshot/pointer/export state, and existing foundation services remain byte-identical.
known_unknowns:
  - Linguistic values remain for Plans 31-12 through 31-19; qualified review remains for Plan 31-26.
ui_proof_slots: []
no_ui_proof_rationale: Backend draft contracts and fixed local tooling only.
high_leverage_surfaces:
  - src/multilang/services/korean_foundation_ai_curation.py
  - scripts/build_korean_foundation_candidates.py
second_pass_required: true
high_leverage_second_pass: Recheck every forbidden authority/structural field, fixed-root boundary, canonical hash rule, and no-write validator path.
closure_claim_limit: Claim only tested nonauthoritative draft contracts and fixed tooling; no content completeness, selection, promotion, review, or readiness.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Adds an intermediate draft schema and validation step.
  kept: Immutable candidates, fixed pathless production commands, and independent evidence authority.
  gained: Deterministic reviewable AI-assisted proposals can be produced without canonical writes.
must_haves:
  truths:
    - Draft contracts are hash-bound, patch-only, bounded, NFC-safe, and structurally incapable of expressing approval authority.
    - Fixed tooling emits bounded stage projections, validates batches and assembled drafts read-only, and writes only under the fixed curation-drafts root.
  artifacts:
    - path: src/multilang/services/korean_foundation_ai_curation.py
      provides: Frozen draft schemas, canonical hashing, validation, and fixed-root invariants.
    - path: scripts/build_korean_foundation_candidates.py
      provides: Enum-only compact projection, batch/family assembly, and validation surface.
  key_links:
    - from: immutable v1 entry hashes
      to: draft patch records
      via: exact source file, pack, entry, and content hash bindings
    - from: fixed script operations
      to: curation-drafts/
      via: bounded stage projection, validation-before-write, and no arbitrary path inputs
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 11

## Objective

Create the fail-closed schemas and fixed tooling used by later assisted-curation plans, following witnessed RED-GREEN-REFACTOR cycles.

<assurance_check>
source_artifact: 31-10-SUMMARY.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: unreviewed
status: pending
warning: This rewritten plan sequence requires a fresh-context checker before execution.
</assurance_check>

## Context

- `31-APPROACH.md`
- `31-PATTERNS.md`
- `31-10-SUMMARY.md`
- `data/korean_foundations/{hangul-v1,pronunciation-i-plus-1-v1}.json`

## Pattern Compliance

- **31-11-01:** use the mapped `_FrozenManifest`/`extra="forbid"` and `korean_canonical_json_sha256` analogs; do not create a parallel hash/model convention.
- **31-11-02:** use the mapped thin standalone parser plus service-owned logic, fixed choices, and validation-before-atomic-write analogs.

## Requirements Covered

- **KHAN-01/02, KPRO-01/02:** establish bounded proposal contracts without weakening the implemented curriculum, review, media, or export gates.

## Must-Haves

1. Authority and structural spoofing are unrepresentable or rejected.
2. Every proposal binds exact immutable source hashes and permits only family-specific learner-copy fields.
3. Compact stage projections are fixed-root, deterministic, bounded to 120 KiB each, and omit unrelated source records.

## Anti-Goals

- No content curation, provider integration, canonical mutation, reviewer evidence, or production command change.

## Hard Boundaries

- `draft_only=true`, `review_status=needs_review`, and `promotion_authority=false` are invariant.
- Raw chain-of-thought is never stored; only concise grounding, values, and uncertainty codes are allowed.

## Evidence Contract

- Each behavior-changing task records a representative assertion failure before implementation, then the focused GREEN suite.
- Tests cover traversal, extra fields, unsafe text, stale hashes, authority spoofing, structural diffs, and write attempts.

## Common Pitfalls

- Treating complete-looking copy as approved evidence.
- Letting script convenience options widen fixed roots.
- Storing source text or model reasoning in diagnostics.

## Stop-And-Challenge

- Stop on any requirement for a provider call, arbitrary path, source mutation, or approval-bearing draft field.

## Approval Gates

- None; exact draft selection occurs only after Plans 31-12 through 31-19 complete.

<checks>
<plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check>
</checks>

## Tasks

<task id="31-11-01" type="auto">
  <name>Define frozen hash-bound draft contracts test-first</name>
  <files>
    - CREATE: tests/services/test_korean_foundation_ai_curation.py
    - CREATE: src/multilang/services/korean_foundation_ai_curation.py
  </files>
  <action>
    Write tests first for frozen Pydantic v2 stage, family, source-reference, uncertainty, and manifest models. Witness the representative assertion fail because the module/contract is absent, then implement the minimum contract. Require exact v1 file/pack/entry hashes, canonical JSON SHA-256, exact family/stage/item coverage, NFC Korean, bounded plain text, field allowlists, and the three draft-authority invariants. Reject approval/reviewer/qualification/timestamp fields, rights/redistribution dispositions, media/artifact hashes, playback, production voice data, structural graph fields, null mandatory proposals, literal placeholders, unsafe markup, extras, and stale source hashes. Add one behavior at a time test-first.
  </action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py::test_ai_curation_contract_module_exists -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py -q</automated>
  </verify>
  <done>Draft models accept only exact bounded nonauthoritative learner-copy patches and fail closed on structural, stale, unsafe, or authority-bearing input.</done>
</task>

<task id="31-11-02" type="auto">
  <name>Implement fixed compact projection, batch, and assembly tooling test-first</name>
  <files>
    - MODIFY: tests/services/test_korean_foundation_ai_curation.py
    - MODIFY: src/multilang/services/korean_foundation_ai_curation.py
    - CREATE: scripts/build_korean_foundation_candidates.py
  </files>
  <action>
    Add failing tests first for fixed enum operations `project-batch`, `validate-batch`, `assemble-family`, `assemble`, and `validate-drafts`, then implement the minimum script/service wiring. Permit only six named stage groups (`hangul-h0-h3`, `hangul-h4-h7`, `hangul-h8-h10`, `pronunciation-p0-p4`, `pronunciation-p5-p9`, `pronunciation-p10-p13`), two families, fixed immutable v1 inputs, and fixed `curation-drafts/inputs/` plus `curation-drafts/` outputs. Each projection contains only fields needed to curate its records, exact structural/source hashes, concise grounding references, at most the named stages, and at most 120 KiB canonical JSON; it excludes all unrelated records so later agents never load the 395-468 KiB full source packs. `validate-*` remains strictly read-only; projection/assembly validates before atomic writes. Reserve no promotion behavior and expose no arbitrary root, URL, archive, provider, force, approval, or repair option. Keep errors aggregate and content-free.
  </action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py::test_fixed_curation_script_surface_exists -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py -q</automated>
  </verify>
  <done>The fixed script creates context-safe exact stage projections and validates/assembles only bounded drafts, with no promotion or authority path.</done>
</task>

## Verification

- Run the focused suite offline and compare canonical/evidence/export prestates.
- Inspect `--help` and tests to prove enum/fixed-root-only inputs.

## Success Criteria

- Later plans can author and validate drafts without changing canonical state or representing human/legal authority.

## High-Leverage Review

- Reconcile draft allowlists, exact hash bindings, fixed roots, and atomic/no-write operations in a mandatory second pass.

## Leverage Review

- Lost: One additional typed layer.
- Kept: Existing immutable and fail-closed production architecture.
- Gained: Safe deterministic assisted-curation preparation.

## Notes

- Framework research is skipped because this uses existing Pydantic v2, canonical hashing, atomic-write, and pytest patterns mapped in `31-PATTERNS.md`.

<output>
After completion, create `31-11-SUMMARY.md` with RED/GREEN evidence, exact command surface, forbidden-field coverage, pre/post state hashes, and no-content/no-authority claim limits.
</output>
