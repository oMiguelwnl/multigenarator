---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "32"
type: execute
wave: 31
runtime: opencode
assurance: self_checked
depends_on: [31-30, 31-31]
files-modified:
  - src/multilang/cli.py
  - src/multilang/services/korean_foundation_evidence.py
  - src/multilang/services/korean_foundation_snapshot.py
  - src/multilang/services/korean_foundation_export.py
  - tests/services/test_korean_foundation_evidence.py
  - tests/services/test_korean_foundation_snapshot.py
  - tests/services/test_korean_foundation_export.py
  - tests/cli/test_korean_foundation_commands.py
  - tests/integration/test_korean_foundations_flow.py
  - .planning/.local/phase31-staged-closure/
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/validation-receipt.json
  - data/korean_foundations/snapshots/
  - data/korean_foundations/active-foundations.json
  - .multilang/exports/korean-foundations/
autonomous: true
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals:
  - Do not repair lane evidence, call providers, publish/upload/distribute, claim human review, or claim unobserved Anki behavior.
hard_boundaries:
  - Revalidate both parallel lane roots under one lock before receipt, snapshot, activation, or export writes.
  - Local activation is deterministic; external production/publication authority is not granted.
escalation_triggers:
  - Stop without partial output on lane drift, non-passing record/slot, rights mismatch, snapshot conflict, export inspection failure, provider construction, or regression.
approval_gates:
  - None for local activation/export after exact policy evidence; external publication remains a separate project-owner decision.
anti_regression_targets:
  - Preserve v1 history, current v2 candidates, model/deck IDs, GUIDs, fields, Phase 30, existing languages, shared `.venv`, and no-provider closure.
known_unknowns:
  - Instrumented native Anki acceptance remains Phase 34.
no_ui_proof_rationale: Local activation and archive/table/media inspection only; Phase 34 owns instrumented Anki proof.
high_leverage_surfaces:
  - validation-receipt.json
  - data/korean_foundations/active-foundations.json
  - .multilang/exports/korean-foundations/
second_pass_required: true
closure_claim_limit: Claim exact local AI-reviewed activation and structurally/media-valid outputs only; no human review, publication, or unobserved Anki claim.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Independent lane progress cannot bypass the final exact join.
  kept: Immutable receipt/snapshot, atomic pointer, and deterministic exports.
  gained: Automatic local closure after parallel evidence rather than another review checkpoint.
must_haves:
  truths:
    - The join accepts only current passing AI linguistic and rights/media/acoustic roots.
    - One canonical receipt and immutable snapshot bind every exact input and local activation is atomic/idempotent.
    - Six outputs pass deep APKG/table/media inspection and focused/full offline regressions.
  artifacts:
    - path: evidence-inbox/validation-receipt.json
      provides: Canonical joined AI/media evidence receipt.
    - path: data/korean_foundations/active-foundations.json
      provides: Atomic local pointer to the exact immutable snapshot.
    - path: .multilang/exports/korean-foundations/
      provides: Six exact local APKG/CSV/TSV outputs.
    - path: src/multilang/services/korean_foundation_evidence.py
      provides: Sole joined AI/media evidence consumer and receipt writer.
  key_links:
    - from: 31-30 and 31-31 aggregate roots
      to: validation-receipt.json
      via: lock-scoped full revalidation and canonical hashing
    - from: active-foundations.json
      to: six outputs
      via: fixed active resolution and deep inspection
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 32

## Objective

Join the parallel AI and media lanes, activate the exact reviewed v2 snapshot locally, and create six deeply inspected outputs without another human checkpoint.

## Context

- `.planning/AI-LINGUISTIC-REVIEW-POLICY.md`
- `31-30-PLAN.md`
- `31-31-PLAN.md`
- existing evidence/snapshot/export services

## Requirements Covered

- KHAN-01, KHAN-02, KPRO-01, KPRO-02

## Must-Haves

1. Exact current two-lane join and canonical receipt.
2. Immutable snapshot and atomic/idempotent local activation.
3. Six exact outputs and complete isolated regression proof.

## Anti-Goals

- No evidence repair, provider call, external release, human-review claim, or inferred device acceptance.

## Hard Boundaries

- Complete validation precedes every write and any failure leaves no partial output.

## Evidence Contract

- Canonical receipt/snapshot/pointer hashes, six-output inspector results, no-provider proof, and focused/full offline tests.

## Common Pitfalls

- Joining stale lane roots, treating one lane as sufficient, reusing historical human receipt fields, or describing archive checks as native Anki proof.

## Stop-And-Challenge

- Stop on any mismatch; rerun the producing lane rather than repairing evidence in the join.

## Approval Gates

- Local activation/export is automatic after exact evidence. Publication/upload remains separately unauthorized.

<checks>
<plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: Dependency join, no-provider boundary, atomic local activation, export inspection, and closure claim limit were checked.
</plan_check>
</checks>

## Tasks

<task id="31-32-01" type="auto">
  <name>Seal final lane heads and merge only the verified commits</name>
  <files>
    - CREATE OR REPLACE LOCAL: /tmp/multilang-phase31-parallel/lane-heads.json
    - CREATE OR REPLACE LOCAL: /tmp/multilang-phase31-parallel/lane-heads.json.sha256
  </files>
  <action>
    Before any integration edit, derive the final committed heads from the fixed AI and media worktrees and derive the trusted baseline digest from both exact committed handoff blobs, never from `/tmp`. Require the destination to be the expected integration worktree, with empty status and HEAD/tree exactly equal to the recorded common baseline. Record each lane through `record-lane-head`, requiring it to be a direct descendant of the common baseline and to reproduce the handoff's allowlisted patch and aggregate/evidence roots. Seal both records, run clean `verify-join`, and invoke `merge-lanes` for only those exact commits. After merge, require `verify-merged-lanes` to prove the resulting ancestry/diff is exactly the union of the two sealed heads and no unrelated commit/path. Reject dirty lane or destination worktrees, uncommitted results, changed handoffs, different ancestry, overlapping paths, unrelated commits, or merge conflicts. This task performs no integration source edit.
  </action>
  <verify>
    <automated>AI_LANE_HEAD="$(git -C /tmp/multilang-phase31-ai rev-parse HEAD)" &amp;&amp; MEDIA_LANE_HEAD="$(git -C /tmp/multilang-phase31-media rev-parse HEAD)" &amp;&amp; BASELINE_SHA256="$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py trusted-baseline-sha256 --ai-head "$AI_LANE_HEAD" --media-head "$MEDIA_LANE_HEAD")" &amp;&amp; test "$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-integration-base --worktree "$PWD" --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256")" = "parallel_integration_base_status=verified" &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py record-lane-head --lane ai --head "$AI_LANE_HEAD" --worktree /tmp/multilang-phase31-ai --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256" &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py record-lane-head --lane media --head "$MEDIA_LANE_HEAD" --worktree /tmp/multilang-phase31-media --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256" &amp;&amp; test "$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-join --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256")" = "parallel_join_status=verified" &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py merge-lanes --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256" &amp;&amp; test "$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-merged-lanes --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256")" = "parallel_merged_status=verified"</automated>
  </verify>
  <done>Both final lane commits are sealed against one baseline and merged exactly, with no integration edits or unrelated paths.</done>
</task>

<task id="31-32-02" type="auto">
  <name>Integrate final consumers and stage the joined closure</name>
  <files>
    - MODIFY: src/multilang/cli.py
    - MODIFY: src/multilang/services/korean_foundation_evidence.py
    - MODIFY: src/multilang/services/korean_foundation_snapshot.py
    - MODIFY: src/multilang/services/korean_foundation_export.py
    - MODIFY: tests/services/test_korean_foundation_evidence.py
    - MODIFY: tests/services/test_korean_foundation_snapshot.py
    - MODIFY: tests/services/test_korean_foundation_export.py
    - MODIFY: tests/cli/test_korean_foundation_commands.py
    - MODIFY: tests/integration/test_korean_foundations_flow.py
    - CREATE: .planning/.local/phase31-staged-closure/
  </files>
  <action>
    Consume the successful sealed `verify-merged-lanes` result from Task 31-32-01 as this task's precondition; do not rerun a clean-merge check after authorized integration edits. Complete all shared evidence, snapshot, exporter, deep-inspector, and CLI implementation before staging. Replace the historical human-only selector with strict AI policy, rights, media, and acoustic inputs while retaining explicit history loading only. Require both lanes to bind the same global policy ID/version/hash and compatible canonicalization; validate every mandatory actor/is-human, route/provider/API/model, prompt/template, output-schema, distinct source/candidate/analyzer/curriculum/media hash, validator ID/version/result, atomic verdict, final consensus status, reason/uncertainty, confidence, pass/independence, timestamp, subject, and canonical-content field. For every audio item, also require end-to-end equality of NFC display/spoken text and text hash, synthesis-request hash, provider/version, voice/profile, locale, SSML/prosody hashes or canonical sentinels, format, measured duration, relative path, artifact/reviewed/actual-byte hashes, and exact rights/authority scope. Reject any missing field, continuity break, or cross-lane policy/canonicalization mismatch. Wire fixed operations `stage-ai-media-closure`, `verify-staged-closure`, `inspect-staged-closure`, `install-staged-closure`, `verify-active`, and `inspect-exports`. The staged manifest binds the final tracked source-tree hash, tool/module hashes, policy/lane roots, receipt, snapshot, pointer, and six outputs. Under the evidence lock write only beneath `.planning/.local/phase31-staged-closure/`; reject historical human authority, stale/incomplete lanes, rights widening, tracked source drift, or any canonical write during staging.
  </action>
  <verify>
    <automated>AI_LANE_HEAD="$(git -C /tmp/multilang-phase31-ai rev-parse HEAD)" &amp;&amp; MEDIA_LANE_HEAD="$(git -C /tmp/multilang-phase31-media rev-parse HEAD)" &amp;&amp; BASELINE_SHA256="$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py trusted-baseline-sha256 --ai-head "$AI_LANE_HEAD" --media-head "$MEDIA_LANE_HEAD")" &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python -m pytest tests/services/test_korean_foundation_evidence.py tests/services/test_korean_foundation_snapshot.py tests/services/test_korean_foundation_export.py tests/cli/test_korean_foundation_commands.py tests/integration/test_korean_foundations_flow.py -q &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/multilang korean-foundations stage-ai-media-closure &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/multilang korean-foundations verify-staged-closure --require-source-unchanged &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-protected-state --allow staged-closure --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256"</automated>
  </verify>
  <done>Both exact lane commits are verified/merged and one complete staged receipt/snapshot/pointer/six-output closure exists with canonical state unchanged.</done>
</task>

<task id="31-32-03" type="auto">
  <name>Activate locally, export six outputs, and run isolated closure</name>
  <files>
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/validation-receipt.json
    - CREATE: data/korean_foundations/snapshots/
    - CREATE OR REPLACE: data/korean_foundations/active-foundations.json
    - CREATE OR REPLACE: .multilang/exports/korean-foundations/
  </files>
  <action>
    Make no tracked source edits in this task. Deep-inspect the staged archive/SQLite/table/media/field/ID/GUID/hash identity. Run focused Phase 31, Phase 30, kana, phoneme, template, Latin, Mandarin, and existing-mode regressions, then the complete frozen offline Python 3.12 suite in the pre-created isolated environment with credentials absent and provider/network constructors forbidden. Re-run `verify-staged-closure --require-source-unchanged` after tests so the final tracked source/tool hash must equal the hash captured before staging. Compare the common pre-lane `.venv` fingerprint and canonical protected-state hashes. Only then call the lock-scoped all-or-nothing installer to place receipt, snapshot, active pointer, and six outputs. Use a recovery journal and rollback-on-failure so injected failures at every replacement boundary restore exact prestates; replay is no-write. Verify active provenance, exports, and final allowed-path diff.
  </action>
  <verify>
    <automated>PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/multilang korean-foundations inspect-staged-closure</automated>
    <automated>PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python -m pytest tests/services/test_ai_linguistic_review.py tests/services/test_korean_curriculum.py tests/services/test_korean_foundation_review.py tests/services/test_korean_foundation_media.py tests/services/test_korean_foundation_evidence.py tests/services/test_korean_foundation_snapshot.py tests/services/test_korean_foundation_export.py tests/cli/test_korean_foundation_commands.py tests/integration/test_korean_foundations_flow.py -q</automated>
    <automated>PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/python -m pytest tests/domain/test_korean.py tests/services/test_japanese_kana_deck.py tests/services/test_japanese_kana_generated_deck.py tests/services/test_phoneme_deck.py tests/services/test_russian_phoneme_deck.py tests/services/test_card_template_loader.py tests/services/test_latin_source_pack.py tests/services/test_latin_export.py tests/services/test_latin_audio.py tests/services/test_mandarin_language_support.py tests/services/test_mandarin_orthography.py tests/integration/test_mandarin_modern_flow.py tests/integration/test_v13_existing_modes_regression_evidence.py -q</automated>
    <automated>AI_LANE_HEAD="$(git -C /tmp/multilang-phase31-ai rev-parse HEAD)" &amp;&amp; MEDIA_LANE_HEAD="$(git -C /tmp/multilang-phase31-media rev-parse HEAD)" &amp;&amp; BASELINE_SHA256="$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py trusted-baseline-sha256 --ai-head "$AI_LANE_HEAD" --media-head "$MEDIA_LANE_HEAD")" &amp;&amp; BASELINE_VENV_FINGERPRINT="$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py get --field venv_fingerprint --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256")" &amp;&amp; test "$(PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python -c 'import sys; print(f"{sys.version_info.major}.{sys.version_info.minor}")')" = "3.12" &amp;&amp; timeout 25m env -u AZURE_SPEECH_KEY -u OPENAI_API_KEY -u DEEPL_API_KEY PYTHONPATH="$PWD/src" UV_OFFLINE=1 MULTILANG_FORBID_NETWORK=1 MULTILANG_FORBID_PROVIDERS=1 /tmp/multilang-phase31-py312/bin/python -m pytest -q &amp;&amp; test "$BASELINE_VENV_FINGERPRINT" = "$(python3 scripts/verify_phase31_runtime_isolation.py fingerprint-venv)" &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/multilang korean-foundations verify-staged-closure --require-source-unchanged &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-protected-state --allow staged-closure --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256" &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/multilang korean-foundations install-staged-closure &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/multilang korean-foundations verify-active &amp;&amp; PYTHONPATH="$PWD/src" UV_OFFLINE=1 /tmp/multilang-phase31-py312/bin/multilang korean-foundations inspect-exports &amp;&amp; PYTHONPATH="$PWD/src" /tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-protected-state --allow receipt,snapshot,pointer,exports --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$BASELINE_SHA256"</automated>
  </verify>
  <done>The exact AI-reviewed foundations are locally active and six outputs pass deep inspection and offline regression evidence.</done>
</task>

## Verification

- Require protected-state diffs limited to receipt, one snapshot, pointer, and six local outputs.

## Success Criteria

- Phase 31 closes locally under transparent AI authority, exact rights/media evidence, and honest Phase 34 device-observation limits.

## High-Leverage Review

- Recompute both lane roots through receipt, snapshot, pointer, and one note/media identity per family/format.

## Leverage Review

- Lost: No partial-lane activation.
- Kept: Immutable authority and deterministic exports.
- Gained: Automatic exact join after parallel execution.
