---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "24"
type: execute
wave: 24
runtime: opencode
assurance: self_checked
depends_on: [31-23]
files-modified:
  - src/multilang/services/korean_foundation_evidence.py
  - src/multilang/services/korean_foundation_snapshot.py
  - src/multilang/services/korean_foundation_export.py
  - tests/services/test_korean_foundation_evidence.py
  - tests/services/test_korean_foundation_snapshot.py
  - tests/services/test_korean_foundation_export.py
autonomous: true
tdd: true
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals: [Do not migrate CLI/integration, create evidence receipt snapshot pointer exports, modify assets/requests, activate, or call providers.]
hard_boundaries:
  - Evidence snapshot and export defaults bind exact v2 and requests but remain fail-closed until genuine evidence and explicit activation.
  - Existing v1 historical receipt/snapshot validation remains explicit and immutable.
escalation_triggers: [Stop on mixed versions, relaxed evidence bindings, write during verification, readiness true, GUID/field/media contract drift, or v1-history failure.]
approval_gates: [None; this is blocked-path migration only.]
anti_regression_targets: [Evidence all-or-nothing contract, snapshot immutability, stable IDs/GUIDs, nine fields, six-output schema, zero providers/writes.]
known_unknowns: [CLI/integration and full cross-mode matrix remain Plan 31-25.]
ui_proof_slots: []
no_ui_proof_rationale: Backend migration and refusal tests only.
high_leverage_surfaces: [src/multilang/services/korean_foundation_evidence.py, src/multilang/services/korean_foundation_snapshot.py, src/multilang/services/korean_foundation_export.py]
second_pass_required: true
high_leverage_second_pass: Trace v2/request hashes into every evidence selector and blocked snapshot/export provenance path; recheck v1 history.
closure_claim_limit: Claim second-group v2 migration and exact blocked state only.
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
leverage: {lost: Version-aware authority bindings, kept: Existing evidence/snapshot/export schemas, gained: End-to-end v2 provenance before canonical writes}
must_haves:
  truths:
    - Evidence validation expects exact v2 candidate/request hashes and rejects v1/draft/mixed bindings.
    - Snapshot/export defaults carry exact v2 provenance but write nothing while evidence/activation is absent.
    - Explicit historical v1 verification remains valid.
  artifacts:
    - {path: src/multilang/services/korean_foundation_evidence.py, provides: Exact v2 fail-closed evidence contract}
    - {path: src/multilang/services/korean_foundation_snapshot.py, provides: Exact v2 blocked snapshot provenance}
    - {path: src/multilang/services/korean_foundation_export.py, provides: Exact v2 blocked export provenance and stable schema}
  key_links:
    - {from: current candidate and v2 requests, to: evidence snapshot export, via: exact immutable provenance validation}
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 24

## Objective
Migrate evidence, snapshot, and export consumers to exact v2 provenance while proving all canonical production paths remain blocked and write-free.

## Context
- `31-APPROACH.md`
- `31-PATTERNS.md`
- `31-23-SUMMARY.md`
- `31-10-SUMMARY.md`

## Pattern Compliance
- **31-24-01/02:** preserve the sole evidence writer, immutable snapshot, active-pointer, stable-ID, and deep export contracts; update source provenance only.

## Requirements Covered
- **KHAN-01/02, KPRO-01/02:** carry exact v2 reviewed-input expectations through fail-closed production boundaries.

## Must-Haves
1. Named RED for exact v2 evidence binding.
2. No receipt/snapshot/pointer/export write.
3. Stable schema/IDs and explicit v1 history.

## Anti-Goals
- No evidence intake, activation, actual export, CLI change, or full regression claim.

## Hard Boundaries
- Migration must not introduce fallback from v2 to v1 or from missing evidence to candidate-only content.

## Evidence Contract
- Tests prove stale/mixed/wrong request hashes reject before writes, verification is read-only, production is inactive, and no providers construct.

## Common Pitfalls
- Treating candidate pointer as active pointer or rewriting historical receipt expectations.

## Stop-And-Challenge
- Stop if a production path can proceed without exact genuine v2 evidence and activation.

## Approval Gates
- None.

<checks><plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check></checks>

## Tasks
<task id="31-24-01" type="auto">
  <name>Migrate exact evidence bindings to v2 test-first</name>
  <files>
    - MODIFY: tests/services/test_korean_foundation_evidence.py
    - MODIFY: src/multilang/services/korean_foundation_evidence.py
  </files>
  <action>Add a named failing test that the fixed inbox contract expects exact v2 bundle/member/request identities and rejects v1, draft, stale, mixed, and incomplete evidence before lock/write. Implement minimal provenance updates while preserving fixed roots, role separation, rights/media/playback checks, scanner-safe errors, and the sole combined receipt writer.</action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_evidence.py::test_evidence_contract_binds_exact_v2_candidate_and_request_hashes -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_evidence.py -q</automated>
  </verify>
  <done>Evidence validation is exact-v2 and remains all-or-nothing, fixed-root, and write-free without genuine input.</done>
</task>
<task id="31-24-02" type="auto">
  <name>Migrate blocked snapshot and export provenance to v2</name>
  <files>
    - MODIFY: tests/services/test_korean_foundation_snapshot.py
    - MODIFY: tests/services/test_korean_foundation_export.py
    - MODIFY: src/multilang/services/korean_foundation_snapshot.py
    - MODIFY: src/multilang/services/korean_foundation_export.py
  </files>
  <action>Add the named failing `test_snapshot_and_export_bind_exact_v2_and_refuse_before_activation` assertion before implementation for exact v2 source/request identities, missing-receipt/missing-active refusal, and zero writes. Then add explicit immutable v1 historical validation, stable model/deck IDs and pre-first-activation GUIDs, exact nine fields, media bindings, and no-provider assertions. Implement only provenance/default resolution changes; do not invoke writers.</action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_snapshot.py::test_snapshot_and_export_bind_exact_v2_and_refuse_before_activation -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_snapshot.py::test_snapshot_and_export_bind_exact_v2_and_refuse_before_activation -q &amp;&amp; UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_snapshot.py tests/services/test_korean_foundation_export.py -q</automated>
  </verify>
  <done>Snapshot/export consumers carry exact v2 provenance while all production outputs remain absent and blocked.</done>
</task>
<task id="31-24-03" type="auto">
  <name>Run the complete migrated service boundary</name>
  <files>
    - READ ONLY: data/korean_foundations/
    - READ ONLY: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/
  </files>
  <action>Run curriculum/review/media/evidence/snapshot/export suites together. Require one v2 identity, v1-history tests, exact pending/refusal results, and byte-identical candidate/request/evidence/receipt/snapshot/pointer/export prestates.</action>
  <verify><automated>UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_curriculum.py tests/services/test_korean_foundation_review.py tests/services/test_korean_foundation_media.py tests/services/test_korean_foundation_evidence.py tests/services/test_korean_foundation_snapshot.py tests/services/test_korean_foundation_export.py -q</automated></verify>
  <done>The complete service layer is internally consistent on v2 and remains safely blocked before evidence.</done>
</task>

## Verification
- Require all six service suites green and no protected state diff.

## Success Criteria
- Plan 31-25 can test unchanged public CLI/integration behavior against a coherent blocked v2 service layer.

## High-Leverage Review
- Trace one exact v2 item through expected evidence, inactive snapshot metadata, export row/GUID/media, and every refusal.

## Leverage Review
- Lost: Separate migration plan.
- Kept: Authority boundaries and stable exports.
- Gained: Coherent v2 provenance across production consumers.

## Notes
- Candidate selection never substitutes for active snapshot authority.

<output>After completion, create `31-24-SUMMARY.md` with exact provenance, RED/GREEN evidence, refusal/write-poison results, v1-history proof, stable IDs/GUIDs/fields, service matrix, and protected-state hashes.</output>
