---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "27"
status: superseded
superseded_by: [31-32]
type: execute
wave: 27
runtime: opencode
assurance: self_checked
depends_on: [31-26]
files-modified:
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/validation-receipt.json
  - data/korean_foundations/snapshots/
autonomous: true
tdd: false
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals:
  - Do not modify evidence/candidates/requests, activate a pointer, export, call providers, or claim observed Anki behavior.
hard_boundaries:
  - The sole pathless lock-scoped receipt writer consumes only the fixed evidence handoff hash and revalidates every input before atomic write.
  - Snapshot preparation creates one exact inactive immutable hash-named tree and never changes the active pointer.
escalation_triggers:
  - Stop without cleanup/repair/write on handoff/index/evidence/source/request/active-prestate drift or existing nonidentical receipt/snapshot state.
approval_gates:
  - No new gate; this plan consumes only the exact Plan 31-26 evidence confirmation.
anti_regression_targets:
  - v1/v2/drafts/requests/evidence/pointer/exports/`.venv` remain byte-identical; only one receipt and one inactive snapshot may be new.
known_unknowns:
  - The prepared snapshot remains unauthorized for activation until Plan 31-28.
ui_proof_slots: []
no_ui_proof_rationale: Durable backend receipt and inactive snapshot preparation only.
high_leverage_surfaces:
  - evidence-inbox/validation-receipt.json
  - data/korean_foundations/snapshots/
second_pass_required: true
high_leverage_second_pass: Reconcile exact index/source/request/reviewer/rights/media/active-prestate hashes through receipt, members, manifest, root, and read-only prepared tuple.
closure_claim_limit: Claim one exact genuine-evidence receipt and one verified inactive immutable snapshot only; no activation/export/readiness acceptance.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Any drift requires a new evidence confirmation cycle.
  kept: Validation-before-write, immutable snapshots, and separate activation authority.
  gained: Durable exact authority ready for explicit activation review.
must_haves:
  truths:
    - Receipt creation consumes the machine-readable confirmed index and revalidates under the fixed lock.
    - Snapshot preparation is complete, immutable, inactive, and verified strictly read-only.
    - Failure leaves no temp/staging/recovery/pointer/export mutation.
  artifacts:
    - path: evidence-inbox/validation-receipt.json
      provides: Exact v2 evidence receipt.
    - path: data/korean_foundations/snapshots/<bundle-sha256>/snapshot-manifest.json
      provides: One complete inactive immutable v2 snapshot.
  key_links:
    - from: execution-handoffs/evidence-confirmation.json
      to: validation-receipt.json
      via: fixed getter and sole validate-and-write operation
    - from: validation-receipt.json
      to: immutable snapshot
      via: expected receipt file SHA-256 and validation-before-write preparation
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 27

> Historical plan: superseded on 2026-08-27 by Plan 31-32, which joins the new
> AI linguistic and media/acoustic evidence lanes.

## Objective

Convert exact confirmed genuine evidence into one canonical receipt and one strictly verified inactive immutable snapshot.

## Context

- `31-APPROACH.md`
- `31-PATTERNS.md`
- `31-26-SUMMARY.md`
- `execution-handoffs/evidence-confirmation.json`
- `src/multilang/services/korean_foundation_{evidence,snapshot}.py`

## Pattern Compliance

- **31-27-01:** invoke the existing sole lock-scoped validation-before-receipt writer and read-only continuity pattern unchanged.
- **31-27-02:** invoke the existing hash-named staged snapshot and write-poisoned prepared-verifier pattern unchanged.

## Requirements Covered

- **KHAN-01/02, KPRO-01/02:** bind complete approved v2 curriculum/content/media evidence into immutable production inputs.

## Must-Haves

1. Sole lock-scoped receipt write from exact confirmed index.
2. One complete inactive snapshot.
3. Strict no-write prepared verification and unchanged pointer/exports.

## Anti-Goals

- No evidence repair, activation, export, provider, publication, or visual acceptance.

## Hard Boundaries

- Receipt SHA is derived from the fixed canonical receipt by `get-receipt`, never copied from prose.
- Prepared verification may not create lock/temp/recovery/timestamp state.

## Evidence Contract

- Commands consume only machine-readable fixed handoff/canonical file hashes and require scanner-safe exact status.
- Pre/post hashing permits only receipt plus one snapshot tree.

## Common Pitfalls

- Reusing a stale confirmation, parsing summary prose, preparing and activating together, or cleaning partial state after drift.

## Stop-And-Challenge

- Any continuity or prestate mismatch stops without mutation and requires a new evidence cycle.

## Approval Gates

- None; activation review remains Plan 31-28.

<checks>
<plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check>
</checks>

## Tasks

<task id="31-27-01" type="auto">
  <name>Write and verify the exact canonical evidence receipt</name>
  <files>
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/validation-receipt.json
  </files>
  <action>
    Capture protected prestates. Get `CONFIRMED_INDEX_SHA256` only from the fixed evidence handoff, inspect the inbox, invoke the sole combined lock-scoped receipt writer once, derive `RECEIPT_SHA256` from the fixed canonical receipt through the helper, and run read-only continuity. Require identical idempotency and no pointer/snapshot/export write.
  </action>
  <verify>
    <automated>CONFIRMED_INDEX_SHA256="$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-evidence)" &amp;&amp; test "${#CONFIRMED_INDEX_SHA256}" -eq 64 &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations inspect-inbox &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations validate-and-write-receipt --confirmed-index-sha256 "$CONFIRMED_INDEX_SHA256" &amp;&amp; RECEIPT_SHA256="$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-receipt)" &amp;&amp; test "${#RECEIPT_SHA256}" -eq 64 &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations check-receipt --expected-receipt-sha256 "$RECEIPT_SHA256"</automated>
  </verify>
  <done>One exact canonical receipt binds all genuine v2 evidence and current active prestate with continuity verified.</done>
</task>

<task id="31-27-02" type="auto">
  <name>Prepare one inactive immutable snapshot and verify it read-only</name>
  <files>
    - CREATE ONE EXACT HASH-NAMED TREE UNDER: data/korean_foundations/snapshots/
  </files>
  <action>
    Derive the receipt hash from the fixed receipt, run validation-before-write snapshot preparation, then strict `verify-prepared`. Require exact receipt, bundle, manifest-file, snapshot-root, active-prestate, and authorization hashes plus `snapshot_status=prepared_inactive` and `prepared_status=verified`. Compare prestates: only the receipt and exact inactive tree may differ.
  </action>
  <verify>
    <automated>RECEIPT_SHA256="$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-receipt)" &amp;&amp; test "${#RECEIPT_SHA256}" -eq 64 &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations prepare-snapshot --expected-receipt-sha256 "$RECEIPT_SHA256" &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations verify-prepared --expected-receipt-sha256 "$RECEIPT_SHA256"</automated>
  </verify>
  <done>One complete immutable v2 snapshot is prepared inactive and its authorization tuple is verified without pointer/export writes.</done>
</task>

## Verification

- Require exact machine-readable hashes, allowed-write diff, no temp/staging leftovers, and unchanged active pointer/exports.

## Success Criteria

- Exact genuine evidence is durably bound and ready for separate activation authorization.

## High-Leverage Review

- Trace all authority groups through receipt and immutable snapshot tuple in a mandatory second pass.

## Leverage Review

- Lost: Drift invalidates preparation.
- Kept: Separate activation and immutable authority.
- Gained: Exact production-ready-but-inactive snapshot.

## Notes

- `verify-prepared` output is displayed in Plan 31-28; no prose summary is used as a hash source.

<output>
After completion, create `31-27-SUMMARY.md` with exact index/receipt/bundle/manifest/root/prestate/authorization hashes, prepared status, allowed writes, and unchanged pointer/export proof.
</output>
