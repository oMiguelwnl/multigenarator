---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "28"
status: superseded
superseded_by: [31-32]
type: execute
wave: 28
runtime: opencode
assurance: self_checked
depends_on: [31-27]
files-modified:
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/activation.json
  - data/korean_foundations/active-foundations.json
  - .multilang/exports/korean-foundations/hangul.apkg
  - .multilang/exports/korean-foundations/hangul-csv/
  - .multilang/exports/korean-foundations/hangul-tsv/
  - .multilang/exports/korean-foundations/pronunciation-i-plus-1.apkg
  - .multilang/exports/korean-foundations/pronunciation-i-plus-1-csv/
  - .multilang/exports/korean-foundations/pronunciation-i-plus-1-tsv/
autonomous: false
tdd: false
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals:
  - Do not alter drafts/candidates/evidence/receipt/snapshot/templates/IDs/tests, call providers, publish/upload/distribute/import, or claim observed Anki acceptance.
hard_boundaries:
  - Read-only prepared verification precedes exact authorization; activation revalidates under lock and atomically changes only the fixed pointer.
  - Six fixed local outputs must bind one active immutable v2 snapshot and pass deep archive/table/media inspection.
  - Full Python 3.12 proof uses only the fixed safe temp helper/environment and leaves repository `.venv` hash unchanged.
escalation_triggers:
  - Stop on decline, malformed authorization, tuple/prestate drift, non-atomic activation, inspection failure, provider construction, Python mismatch, regression, or `.venv` mutation.
approval_gates:
  - `checkpoint:review` authorizes one exact pointer transition and deterministic replacement of the six named local outputs only.
anti_regression_targets:
  - Phase 30 Korean, kana/phoneme, Latin, Mandarin, templates, CLI/integration, IDs/GUIDs, and complete offline Python 3.12 behavior remain green.
known_unknowns:
  - Observed Anki Desktop/mobile import/render/playback remains Phase 34 and cannot be inferred here.
ui_proof_slots: []
no_ui_proof_rationale: Local activation plus static/archive/table evidence; observed Anki acceptance is Phase 34.
high_leverage_surfaces:
  - data/korean_foundations/active-foundations.json
  - .multilang/exports/korean-foundations/
  - complete offline Python 3.12 suite
second_pass_required: true
high_leverage_second_pass: Recompute prepared tuple, authorization, active provenance, all six output identities/media, and isolated full-suite invariance.
closure_claim_limit: Claim exact authorized local activation and structurally/media-valid local outputs only; no publication or observed Anki import/render/playback acceptance.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Activation remains deliberately manual and exact-tuple bound.
  kept: Immutable evidence, atomic pointer semantics, and Phase 34 observed-acceptance boundary.
  gained: Reproducible active v2 foundations and six deeply inspected local outputs.
must_haves:
  truths:
    - The user sees and authorizes the exact current prepared tuple before any pointer/export write.
    - Activation is exact, atomic/idempotent, and provenance-verified.
    - Six fixed outputs pass deep inspection against one active v2 snapshot.
    - Focused and complete offline Python 3.12 suites pass with unchanged `.venv` and no providers.
  artifacts:
    - path: data/korean_foundations/active-foundations.json
      provides: Atomic pointer to the exact authorized v2 snapshot.
    - path: .multilang/exports/korean-foundations/
      provides: Six exact local APKG/CSV/TSV outputs.
  key_links:
    - from: verify-prepared authorization hash
      to: execution-handoffs/activation.json
      via: exact activate-and-export signal and fixed handoff validation
    - from: activation handoff and canonical receipt
      to: active-foundations.json
      via: lock-scoped full revalidation and atomic replacement
    - from: active-foundations.json
      to: six outputs
      via: fixed active resolution and deep inspection
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 28

> Historical plan: superseded on 2026-08-27 by Plan 31-32. Local activation no
> longer requires a linguistic-human checkpoint after exact AI/media evidence;
> publication authority remains separate.

## Objective

Review and authorize the exact prepared tuple, activate it atomically, create six fixed local outputs, and close with isolated Python 3.12 regression evidence.

## Context

- `31-APPROACH.md`
- `31-PATTERNS.md`
- `31-27-SUMMARY.md`
- `src/multilang/services/korean_foundation_{evidence,snapshot,export}.py`

## Pattern Compliance

- **31-28-01/02:** invoke the existing read-only prepared tuple, exact authorization hash, and atomic active-pointer patterns unchanged.
- **31-28-03:** invoke the existing six-output staged writers/deep inspectors plus the mapped fixed runtime-isolation pattern.

## Requirements Covered

- **KHAN-01/02:** activate/export reviewed Hangul with exact strict curriculum/media evidence.
- **KPRO-01/02:** activate/export reviewed P0-P13 with exact nine fields and media in all formats.

## Must-Haves

1. Exact separate authorization before writes.
2. Verified atomic activation provenance.
3. Six exact deeply inspected local outputs.
4. Focused/full offline Python 3.12 proof without `.venv` mutation.

## Anti-Goals

- No evidence repair, provider, publication, distribution, Anki import, or observed visual/playback claim.

## Hard Boundaries

- Receipt hash is derived from the fixed canonical file; authorization comes only from the resume signal and fixed handoff.
- Any tuple/prestate drift requires a new review; no prepare/repair occurs at the checkpoint.

## Evidence Contract

- `verify-prepared` is read-only; `verify-active` proves pointer provenance; `inspect-exports` opens APKG ZIP/media/SQLite and validates tabular fields/media/checksums.
- Runtime helper validates fixed temp containment and compares deterministic `.venv` tree hashes around the full suite.

## Common Pitfalls

- Treating receipt existence as activation authority, parsing hashes from prose, reusing authorization after drift, or calling static inspection observed Anki acceptance.

## Stop-And-Challenge

- Stop without repair on any authorization/provenance/output/runtime/regression mismatch.

## Approval Gates

- The checkpoint grants local activation and six outputs only; external delivery and Phase 34 acceptance remain unauthorized.

<checks>
<plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check>
</checks>

## Tasks

<task id="31-28-01" type="checkpoint:review" gate="blocking">
  <name>Review and authorize the exact prepared transition and six local outputs</name>
  <files>
    - READ ONLY: .planning/phases/31-hangul-and-pronunciation-i-plus-1/evidence-inbox/validation-receipt.json
    - READ ONLY: data/korean_foundations/snapshots/
  </files>
  <action>
    Derive the receipt hash only through fixed `get-receipt`, invoke strictly read-only `verify-prepared`, and display its exact receipt, bundle, manifest-file, snapshot-root, active-prestate, and authorization hashes plus six fixed destinations. Confirm inactive/immutable state and bounded authority. Any write/recovery/cleanup/timestamp/directory mutation during review is failure.
  </action>
  <verify>
    <automated>RECEIPT_SHA256="$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-receipt)" &amp;&amp; test "${#RECEIPT_SHA256}" -eq 64 &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations verify-prepared --expected-receipt-sha256 "$RECEIPT_SHA256"</automated>
  </verify>
  <resume-signal>Type exactly `activate-and-export {64-lowercase-hex-authorization-sha256}` or `decline: {reason}`.</resume-signal>
  <done>The user authorizes the exact current local transition/outputs or production remains unchanged.</done>
</task>

<task id="31-28-02" type="auto">
  <name>Record authorization, activate atomically, and verify provenance</name>
  <files>
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/activation.json
    - CREATE OR REPLACE: data/korean_foundations/active-foundations.json
  </files>
  <action>
    Export the exact resume-signal hash as `AUTHORIZATION_SHA256`; reject missing/malformed values. Derive the receipt hash from the canonical file, record an authorization handoff bound to that receipt/current tuple, read it back, then run `activate` and `verify-active`. Accept `already_active` only for exact complete provenance and no rewrite; drift requires a new preparation/review cycle.
  </action>
  <verify>
    <automated>: "${AUTHORIZATION_SHA256:?missing activate-and-export handoff}" &amp;&amp; case "$AUTHORIZATION_SHA256" in *[!0-9a-f]*|'') exit 1 ;; esac &amp;&amp; test "${#AUTHORIZATION_SHA256}" -eq 64 &amp;&amp; RECEIPT_SHA256="$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-receipt)" &amp;&amp; UV_OFFLINE=1 uv run python scripts/phase31_handoff.py record-authorization --sha256 "$AUTHORIZATION_SHA256" --expected-receipt-sha256 "$RECEIPT_SHA256" &amp;&amp; test "$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-authorization)" = "$AUTHORIZATION_SHA256" &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations activate --expected-receipt-sha256 "$RECEIPT_SHA256" --authorization-sha256 "$AUTHORIZATION_SHA256" &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations verify-active --expected-receipt-sha256 "$RECEIPT_SHA256"</automated>
  </verify>
  <done>The pointer selects exactly the authorized immutable v2 snapshot with atomic/idempotent verified provenance.</done>
</task>

<task id="31-28-03" type="auto">
  <name>Export and inspect six outputs, then run isolated Python 3.12 closure</name>
  <files>
    - CREATE OR REPLACE: .multilang/exports/korean-foundations/hangul.apkg
    - CREATE OR REPLACE: .multilang/exports/korean-foundations/hangul-csv/
    - CREATE OR REPLACE: .multilang/exports/korean-foundations/hangul-tsv/
    - CREATE OR REPLACE: .multilang/exports/korean-foundations/pronunciation-i-plus-1.apkg
    - CREATE OR REPLACE: .multilang/exports/korean-foundations/pronunciation-i-plus-1-csv/
    - CREATE OR REPLACE: .multilang/exports/korean-foundations/pronunciation-i-plus-1-tsv/
  </files>
  <action>
    Capture protected prestates and the helper's deterministic repository `.venv` hash. Run both readiness checks, write six fixed outputs, and deep-inspect exact active receipt/snapshot/IDs/fields/GUIDs/media/checksums. Run focused tests. Use the fixed helper to safely prepare only current-user mode-0700 `/tmp/multilang-phase31-py312` directly beneath root-owned sticky `/tmp`, frozen offline-sync there, prove Python 3.12, run the complete offline suite, then require the identical `.venv` hash. Do not call providers, upload, publish, distribute, or import outputs.
  </action>
  <verify>
    <automated>UV_OFFLINE=1 uv run multilang korean-foundations check --family hangul &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations check --family pronunciation &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations export --family hangul --format apkg --output .multilang/exports/korean-foundations/hangul.apkg &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations export --family hangul --format csv --output .multilang/exports/korean-foundations/hangul-csv &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations export --family hangul --format tsv --output .multilang/exports/korean-foundations/hangul-tsv &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations export --family pronunciation --format apkg --output .multilang/exports/korean-foundations/pronunciation-i-plus-1.apkg &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations export --family pronunciation --format csv --output .multilang/exports/korean-foundations/pronunciation-i-plus-1-csv &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations export --family pronunciation --format tsv --output .multilang/exports/korean-foundations/pronunciation-i-plus-1-tsv &amp;&amp; UV_OFFLINE=1 uv run multilang korean-foundations inspect-exports</automated>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/domain/test_korean.py tests/services/test_korean_curriculum.py tests/services/test_korean_foundation_review.py tests/services/test_korean_foundation_media.py tests/services/test_korean_foundation_evidence.py tests/services/test_korean_foundation_snapshot.py tests/services/test_korean_foundation_export.py tests/services/test_korean_foundation_ai_curation.py tests/services/test_phase31_handoff.py tests/services/test_phase31_runtime_isolation.py tests/services/test_phoneme_deck.py tests/services/test_russian_phoneme_deck.py tests/services/test_card_template_loader.py tests/cli/test_korean_foundation_commands.py tests/integration/test_korean_foundations_flow.py -q</automated>
    <automated>VENV_HASH_BEFORE="$(python3 scripts/verify_phase31_runtime_isolation.py hash-venv)" &amp;&amp; python3 scripts/verify_phase31_runtime_isolation.py prepare &amp;&amp; UV_PROJECT_ENVIRONMENT="/tmp/multilang-phase31-py312" UV_OFFLINE=1 uv sync --frozen --python 3.12 --extra dev &amp;&amp; test "$(UV_PROJECT_ENVIRONMENT="/tmp/multilang-phase31-py312" UV_OFFLINE=1 uv run --frozen --python 3.12 python -c 'import sys; print(f"{sys.version_info.major}.{sys.version_info.minor}")')" = "3.12" &amp;&amp; UV_PROJECT_ENVIRONMENT="/tmp/multilang-phase31-py312" UV_OFFLINE=1 uv run --frozen --python 3.12 --extra dev pytest -q &amp;&amp; VENV_HASH_AFTER="$(python3 scripts/verify_phase31_runtime_isolation.py hash-venv)" &amp;&amp; test "$VENV_HASH_BEFORE" = "$VENV_HASH_AFTER"</automated>
  </verify>
  <done>Both families are active-ready; six outputs pass deep inspection; focused/full Python 3.12 suites pass with unchanged `.venv` and no provider/publication/import action.</done>
</task>

## Verification

- Require exact authorization handoff, active provenance, six-output inspection, no-provider proof, and protected-state diff limited to pointer/handoff/outputs.

## Success Criteria

- Exact reviewed v2 foundations are locally active/exported with complete automated evidence and honest Phase 34 acceptance boundary.

## High-Leverage Review

- Trace the prepared tuple through authorization/pointer and one record per family through every format and full regression.

## Leverage Review

- Lost: Manual exact activation gate.
- Kept: Evidence authority and Phase 34 observed acceptance.
- Gained: Reproducible locally active reviewed foundation decks.

## Notes

- Static/archive/table checks do not prove Anki Desktop/mobile import, rendering, fonts, responsive behavior, or heard playback; Phase 34 owns those observations.

<output>
After completion, create `31-28-SUMMARY.md` with exact checkpoint/handoff/activation hashes, six output paths/hashes/counts/IDs, deep inspector results, focused/full Python 3.12 results, `.venv` invariance, no-provider/no-publication proof, and Phase 34 claim limit.
</output>
