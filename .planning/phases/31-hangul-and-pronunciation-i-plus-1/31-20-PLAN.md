---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "20"
type: execute
wave: 20
runtime: opencode
assurance: self_checked
depends_on: [31-19]
files-modified:
  - scripts/phase31_handoff.py
  - tests/services/test_phase31_handoff.py
  - .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/curation-selection.json
  - src/multilang/services/korean_foundation_ai_curation.py
  - scripts/build_korean_foundation_candidates.py
  - tests/services/test_korean_foundation_ai_curation.py
autonomous: false
tdd: true
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals:
  - Do not create v2 assets, alter v1, write evidence, approve linguistic content, activate, export, or call providers.
hard_boundaries:
  - The first task is an exact read-only draft-selection checkpoint; selection grants candidate-promotion authority only.
  - Handoff files are fixed-root, typed, hash-only, atomic, idempotent for identical content, and refuse nonidentical overwrite.
escalation_triggers:
  - Stop if draft hashes drift, validation fails, selection is malformed/declined, or promotion would require structural mutation.
approval_gates:
  - `checkpoint:review` accepts only `select-curation {64-lowercase-hex}` after read-only validation.
anti_regression_targets:
  - Drafts, v1/canonical/evidence/pointer/export bytes remain unchanged; no v2 file exists after this plan.
known_unknowns:
  - Selected copy remains unqualified until genuine evidence in Plan 31-26.
ui_proof_slots: []
no_ui_proof_rationale: Hash selection and backend promotion contracts only.
high_leverage_surfaces:
  - scripts/phase31_handoff.py
  - src/multilang/services/korean_foundation_ai_curation.py
second_pass_required: true
high_leverage_second_pass: Trace the selected manifest hash through fixed handoff validation and structural-diff promotion contracts without writes.
closure_claim_limit: Claim only exact draft selection plus tested promotion primitives; no v2 creation, review, evidence, activation, or readiness.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Execution must preserve explicit checkpoint state across plans.
  kept: Selection is separate from human approval and canonical mutation.
  gained: Machine-readable hash handoffs remove prose placeholders and stale-value ambiguity.
must_haves:
  truths:
    - The user reviews and selects one exact valid two-family draft manifest before any promotion-capable work proceeds.
    - A fixed machine-readable handoff binds the exact signal and current manifest hash.
    - Promotion primitives enforce selected hash, immutable v1, allowed-field-only diffs, the four exact v2 member names, immutable hash-named bundle staging, and one atomic candidate pointer publication.
  artifacts:
    - path: execution-handoffs/curation-selection.json
      provides: Validated exact selection handoff for Plan 31-21.
    - path: src/multilang/services/korean_foundation_ai_curation.py
      provides: Selected-hash, structural-diff, hash-named bundle, and atomic candidate-pointer primitives without execution side effects.
  key_links:
    - from: select-curation signal
      to: curation-selection.json
      via: fixed hash-only handoff writer after read-only manifest validation
    - from: curation-selection.json
      to: promotion primitive
      via: fixed getter and exact manifest/hash revalidation
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 20

## Objective

Obtain exact user selection, persist it through a validated fixed handoff, and implement promotion primitives test-first without creating v2 data.

## Context

- `31-APPROACH.md`
- `31-PATTERNS.md`
- `31-19-SUMMARY.md`
- `curation-drafts/draft-manifest.json`

## Pattern Compliance

- **31-20-01:** reuse the mapped strictly read-only validation pattern.
- **31-20-02:** adapt the mapped fixed evidence roots, strict hash contract, lstat-safe atomic writer, and identical-idempotency pattern.
- **31-20-03:** reuse the mapped curation structural-diff and validation-before-write pattern.

## Requirements Covered

- **KHAN-01/02, KPRO-01/02:** authorize only bounded candidate preparation for the exact complete draft set.

## Must-Haves

1. Selection checkpoint is first and strictly read-only.
2. Handoff is exact, machine-readable, validated, and non-authoritative beyond promotion.
3. Promotion behavior is test-first and defines one hash-named four-member bundle plus a single atomic visibility pointer.

## Anti-Goals

- No actual promotion, evidence, human-role substitution, activation, export, or release claim.

## Hard Boundaries

- The executor exports the exact resume-signal hash as `SELECTED_DRAFT_MANIFEST_SHA256`; missing/malformed values fail before handoff write.
- Handoff helper exposes only fixed kinds/paths and hash arguments, never arbitrary files or roots.

## Evidence Contract

- Read-only validation reproduces the report/summary hashes.
- Representative handoff and promotion tests witness RED assertions before GREEN suites.

## Common Pitfalls

- Treating selection as Portuguese/Korean approval, copying a hash from prose, or allowing handoff replacement after drift.

## Stop-And-Challenge

- Stop on decline, malformed signal, stale manifest, nonidentical handoff, structural diff, unsafe bundle state, or conflicting candidate pointer.

## Approval Gates

- The checkpoint authorizes only deterministic candidate-bundle promotion in Plan 31-21.

<checks>
<plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check>
</checks>

## Tasks

<task id="31-20-01" type="checkpoint:review" gate="blocking">
  <name>Review and select the exact two-family draft manifest</name>
  <files>
    - READ ONLY: .planning/phases/31-hangul-and-pronunciation-i-plus-1/31-19-SUMMARY.md
    - READ ONLY: .planning/phases/31-hangul-and-pronunciation-i-plus-1/31-AI-CURATION-REPORT.md
    - READ ONLY: .planning/phases/31-hangul-and-pronunciation-i-plus-1/curation-drafts/draft-manifest.json
  </files>
  <action>
    Run only fixed read-only draft validation. Compare exact manifest/family hashes, 139-record coverage, grounding/uncertainty counts, disagreements, and canonical-invariance proof with the report and summary. Display the only later side effect: the selected patches may become new immutable v2 candidate packs with all review/media gates pending. Explicitly deny linguistic, Portuguese, rights, playback, evidence, activation, export, release, and Anki acceptance authority.
  </action>
  <verify>
    <automated>UV_OFFLINE=1 uv run python scripts/build_korean_foundation_candidates.py validate-drafts</automated>
  </verify>
  <resume-signal>Type exactly `select-curation {64-lowercase-hex-draft-manifest-sha256}` or `decline-curation: {reason}`.</resume-signal>
  <done>The user selects one exact valid manifest for candidate promotion or execution stops unchanged.</done>
</task>

<task id="31-20-02" type="auto">
  <name>Implement fixed machine-readable checkpoint handoffs test-first</name>
  <files>
    - CREATE: tests/services/test_phase31_handoff.py
    - CREATE: scripts/phase31_handoff.py
    - CREATE: .planning/phases/31-hangul-and-pronunciation-i-plus-1/execution-handoffs/curation-selection.json
  </files>
  <action>
    Write failing tests first for fixed `record-selection/get-selection`, `record-evidence/get-evidence`, `get-receipt`, and `record-authorization/get-authorization` operations. Implement a fixed-root JSON helper with lowercase-64-hex validation, typed kind/version, exact relevant current-artifact bindings, canonical self-hash, lstat/link refusal, validation-before-atomic-write, identical idempotency, nonidentical-overwrite refusal, content-free errors, and no arbitrary path/root. After GREEN, export the exact checkpoint signal as `SELECTED_DRAFT_MANIFEST_SHA256`, validate it, record selection once, and read it back exactly.
  </action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_phase31_handoff.py::test_fixed_handoff_contract_is_not_implemented -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_phase31_handoff.py -q</automated>
    <automated>: "${SELECTED_DRAFT_MANIFEST_SHA256:?missing select-curation handoff}" &amp;&amp; case "$SELECTED_DRAFT_MANIFEST_SHA256" in *[!0-9a-f]*|'') exit 1 ;; esac &amp;&amp; test "${#SELECTED_DRAFT_MANIFEST_SHA256}" -eq 64 &amp;&amp; UV_OFFLINE=1 uv run python scripts/phase31_handoff.py record-selection --sha256 "$SELECTED_DRAFT_MANIFEST_SHA256" &amp;&amp; test "$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-selection)" = "$SELECTED_DRAFT_MANIFEST_SHA256"</automated>
  </verify>
  <done>One exact fixed selection handoff exists, round-trips the reviewed hash, and future evidence/authorization handoff operations are tested but unused.</done>
</task>

<task id="31-20-03" type="auto">
  <name>Implement selected-hash and structural promotion primitives test-first</name>
  <files>
    - MODIFY: tests/services/test_korean_foundation_ai_curation.py
    - MODIFY: src/multilang/services/korean_foundation_ai_curation.py
    - MODIFY: scripts/build_korean_foundation_candidates.py
  </files>
  <action>
    Add failing tests before code for fixed selection-handoff consumption, manifest/base hash drift, immutable-v1 prestate, family field allowlists, complete deterministic structural comparisons, canonical bundle-manifest/root derivation, exact members `hangul-v2.json`, `pronunciation-i-plus-1-v2.json`, `korean-foundations-v2-curation.json`, and `korean-foundations-v2-media.json`, lstat-safe same-filesystem staging, one atomic `current-candidate.json` pointer publication, crash before pointer publication, concurrent readers seeing either no/old or complete new bundle, orphan-identical retry, and conflicting bundle/pointer refusal. Implement only reusable validation/staging/pointer primitives plus fixed `check-selection`; do not invoke a writer or create v2 files in this plan.
  </action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py::test_promotion_rejects_structural_diff_before_write -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py tests/services/test_phase31_handoff.py -q</automated>
    <automated>UV_OFFLINE=1 uv run python scripts/build_korean_foundation_candidates.py check-selection</automated>
  </verify>
  <done>Promotion primitives are selected-hash/fixed-root/structural-diff constrained and define crash-safe four-member visibility through one pointer, while v2/canonical state remains absent or unchanged.</done>
</task>

## Verification

- Require one exact selection handoff, no v2 files, and zero canonical mutation.

## Success Criteria

- Plan 31-21 can consume a validated machine-readable selection without interpreting prose or weakening authority.

## High-Leverage Review

- Trace signal to handoff to promotion validation and recheck all no-write paths.

## Leverage Review

- Lost: Additional handoff artifact.
- Kept: Exact authority separation and immutable v1.
- Gained: Runnable cross-plan hash contract.

## Notes

- The handoff is execution coordination, not reviewer evidence; it stays outside `evidence-inbox/` and canonical data.

<output>
After completion, create `31-20-SUMMARY.md` with exact selection/handoff hashes, RED/GREEN results, promotion-contract coverage, no-v2 proof, and authority claim limits.
</output>
