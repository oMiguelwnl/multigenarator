---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "21"
type: execute
wave: 21
runtime: opencode
assurance: self_checked
depends_on: [31-20]
files-modified:
  - src/multilang/services/korean_foundation_ai_curation.py
  - scripts/build_korean_foundation_candidates.py
  - tests/services/test_korean_foundation_ai_curation.py
  - data/korean_foundations/candidate-bundles/
  - data/korean_foundations/current-candidate.json
autonomous: true
tdd: true
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals: [Do not regenerate requests, migrate consumers, create evidence, activate production, export, approve content, or modify v1.]
hard_boundaries:
  - Four v2 members become reader-visible only through one validated atomic `current-candidate.json` pointer to one immutable hash-named bundle.
  - The pointer contains only `schema_version`, `bundle_sha256`, `bundle_relpath`, and `bundle_manifest_sha256`; the manifest declares only the four exact v2 member names and hashes.
  - Unreferenced identical bundle state after a crash is retryable; conflicting/unsafe state is never cleaned or overwritten.
escalation_triggers: [Stop on stale selection/v1, structural diff, unsafe path/link, conflicting bundle/pointer, or non-atomic visibility.]
approval_gates: [No new gate; consumes only Plan 31-20 exact promotion authority.]
anti_regression_targets: [v1 bytes unchanged, four v2 members pending-only, readers observe no/old or complete new tuple, no receipt/pointer/export state.]
known_unknowns: [Qualified evidence and consumer migration remain later plans.]
ui_proof_slots: []
no_ui_proof_rationale: Immutable candidate bundle publication only.
high_leverage_surfaces: [data/korean_foundations/candidate-bundles/, data/korean_foundations/current-candidate.json]
second_pass_required: true
high_leverage_second_pass: Recheck bundle/member hashes, fsync/rename ordering, crash points, concurrent visibility, v1 invariance, and pending gates.
closure_claim_limit: Claim one selected immutable v2 candidate bundle and pointer only.
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
leverage: {lost: Candidate readers add one pointer resolution, kept: Immutable v1 and exact authority, gained: Crash-safe all-or-none four-member publication}
must_haves:
  truths:
    - Readers see all four exact v2 members together or no newly selected candidate.
    - Promotion is deterministic/idempotent for identical state and refuses conflicting state.
    - Every curation/media gate remains candidate_only and needs_review.
  artifacts:
    - {path: data/korean_foundations/candidate-bundles/<bundle-sha256>/, provides: Immutable manifest plus exact hangul-v2 pronunciation-i-plus-1-v2 curation-v2 and media-v2 members}
    - {path: data/korean_foundations/current-candidate.json, provides: Atomic exact candidate visibility pointer}
  key_links:
    - {from: curation selection handoff, to: hash-named bundle, via: fixed structural promotion}
    - {from: complete validated bundle, to: current-candidate.json, via: final single atomic replace}
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 21

## Objective
Publish the exact selected four-member v2 candidate as one immutable bundle with a single crash-safe visibility pointer.

## Context
- `31-APPROACH.md`
- `31-PATTERNS.md`
- `31-20-SUMMARY.md`

## Pattern Compliance
- **31-21-01/02:** adapt the mapped hash-named snapshot directory, fsync, atomic pointer, concurrent-reader, and validation-before-write patterns.

## Requirements Covered
- **KHAN-01/02, KPRO-01/02:** materialize complete selected candidate data without partial visibility or authority widening.

## Must-Haves
1. Test-first crash/concurrency publication protocol.
2. Four exact immutable pending members plus one canonical bundle manifest.
3. One atomic pointer and unchanged v1.

## Anti-Goals
- No request regeneration, consumer migration, evidence, activation, export, or review.

## Hard Boundaries
- Bundle path derives from canonical manifest/member root hash; caller chooses no path or filename. Members are exactly `hangul-v2.json`, `pronunciation-i-plus-1-v2.json`, `korean-foundations-v2-curation.json`, and `korean-foundations-v2-media.json`.

## Evidence Contract
- Named RED proves writer absent; GREEN covers injected failures before/after bundle rename, before pointer replace, concurrent readers, exact retry, and conflicts.

## Common Pitfalls
- Calling four sibling replaces atomic, exposing an incomplete directory, or using unreferenced bundle presence as authority.

## Stop-And-Challenge
- Stop on any topology that cannot prove old-or-complete-new reader visibility.

## Approval Gates
- None beyond Plan 31-20 selection.

<checks><plan_check>
checker: independent fresh-context
checker_runtime: opencode
status: passed
blocking: false
notes: Cycle 1 passed all planning dimensions with no issues on 2026-08-24.
</plan_check></checks>

## Tasks
<task id="31-21-01" type="auto">
  <name>Implement candidate bundle publication test-first</name>
  <files>
    - MODIFY: tests/services/test_korean_foundation_ai_curation.py
    - MODIFY: src/multilang/services/korean_foundation_ai_curation.py
    - MODIFY: scripts/build_korean_foundation_candidates.py
  </files>
  <action>Add named failing tests for the four exact v2 names, canonical manifest with exact member hashes and no extras, immutable hash-named bundle stage/rename/fsync, a pointer containing only the four fixed fields, no/old-or-complete-new readers, injected crashes, identical orphan retry, exact idempotency, conflicting state refusal, and zero v1 mutation. Implement minimal fixed `promote` and read-only `verify-promoted`; expose no arbitrary roots or repair flag.</action>
  <verify>
    <automated stage="red">set +e; OUTPUT="$(UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py::test_candidate_bundle_publication_is_atomic_for_readers -q 2&gt;&amp;1)"; STATUS=$?; set -e; test "$STATUS" -eq 1 &amp;&amp; case "$OUTPUT" in *AssertionError*|*assert*) ;; *) exit 1 ;; esac</automated>
    <automated stage="green">UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py::test_candidate_bundle_publication_is_atomic_for_readers -q &amp;&amp; UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_korean_foundation_ai_curation.py -q</automated>
  </verify>
  <done>Promotion has a tested crash-safe single-pointer publication topology.</done>
</task>
<task id="31-21-02" type="auto">
  <name>Publish and verify the exact selected v2 bundle</name>
  <files>
    - CREATE ONE HASH-NAMED TREE UNDER: data/korean_foundations/candidate-bundles/
    - CREATE OR REPLACE: data/korean_foundations/current-candidate.json
  </files>
  <action>Get selection only from the fixed handoff. Run `check-selection`, promote once, then verify the exact four-field pointer, canonical bundle manifest/root, the four named member/version/content hashes, complete learner fields, no placeholders/authority fields, exactly 139 curation records split as 92 Hangul and 47 pronunciation records, 509 pending media slots, and unchanged v1/canonical evidence/export state.</action>
  <verify><automated>SELECTED_DRAFT_MANIFEST_SHA256="$(UV_OFFLINE=1 uv run python scripts/phase31_handoff.py get-selection)" &amp;&amp; UV_OFFLINE=1 uv run python scripts/build_korean_foundation_candidates.py check-selection &amp;&amp; UV_OFFLINE=1 uv run python scripts/build_korean_foundation_candidates.py promote --expected-draft-manifest-sha256 "$SELECTED_DRAFT_MANIFEST_SHA256" &amp;&amp; UV_OFFLINE=1 uv run python scripts/build_korean_foundation_candidates.py verify-promoted --expected-draft-manifest-sha256 "$SELECTED_DRAFT_MANIFEST_SHA256"</automated></verify>
  <done>One atomic pointer exposes one complete immutable v2 candidate bundle and nothing is approved.</done>
</task>

## Verification
- Require exact old-or-complete-new visibility, v1 invariance, all pending state, and no canonical production writes.

## Success Criteria
- Exact v2 candidates are safely visible for request regeneration and consumer migration.

## High-Leverage Review
- Trace selection to four members to pointer under every injected crash boundary.

## Leverage Review
- Lost: Pointer resolution.
- Kept: Immutable authority.
- Gained: Real atomic visibility.

## Notes
- Candidate pointer is not the production active snapshot pointer.

<output>After completion, create `31-21-SUMMARY.md` with selection/bundle/pointer/member hashes, crash/concurrency evidence, counts, v1 invariance, and pending-only claims.</output>
