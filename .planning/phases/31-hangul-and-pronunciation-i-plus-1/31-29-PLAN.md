---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "29"
type: execute
wave: 29
runtime: opencode
assurance: self_checked
depends_on: [31-25]
files-modified:
  - scripts/verify_phase31_runtime_isolation.py
  - scripts/phase31_parallel_launch.py
  - tests/services/test_phase31_runtime_isolation.py
  - tests/services/test_phase31_parallel_launch.py
  - /tmp/multilang-phase31-parallel/baseline.json
  - /tmp/multilang-phase31-parallel/baseline.json.sha256
autonomous: true
requirements: [KHAN-01, KHAN-02, KPRO-01, KPRO-02]
non_goals:
  - Do not review content, create media, call providers, mutate candidates/evidence/snapshots/pointers/exports, or start a lane from a dirty/uncommitted baseline.
hard_boundaries:
  - Prepare one fixed isolated Python 3.12 environment and one immutable clean integration baseline before launching either lane.
  - Never repair, follow unsafe external links from, or mutate repository `.venv`.
escalation_triggers:
  - Stop on dirty baseline, unsafe temp root, non-Python-3.12 runtime, dependency drift, failed no-follow fingerprint, or overlapping lane allowlists.
approval_gates:
  - None; local runtime/baseline preparation only.
anti_regression_targets:
  - Repository `.venv`, candidates, evidence, media, pointer, exports, lockfile, and dependency metadata remain unchanged.
known_unknowns:
  - Lane execution requires isolated worktree support and exact commits derived from the recorded baseline.
no_ui_proof_rationale: Parallel runtime and git-baseline orchestration only.
high_leverage_surfaces:
  - scripts/phase31_parallel_launch.py
  - /tmp/multilang-phase31-parallel/baseline.json
second_pass_required: true
closure_claim_limit: Claim only a clean common baseline, safe isolated Python environment, disjoint lane allowlists, and unchanged shared `.venv` fingerprint.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: A small serial launch preflight remains because both lanes need one trustworthy base.
  kept: Dirty-worktree safety, frozen dependencies, and exact protected-state accounting.
  gained: True concurrent lanes with machine-verifiable join provenance.
must_haves:
  truths:
    - Both parallel lanes start from one clean exact commit/tree and one frozen Python 3.12 environment.
    - AI and media lane write allowlists are disjoint and protected read surfaces are hash-bound.
    - The known-unsafe repository `.venv` has a deterministic no-follow fingerprint before any lane command.
  artifacts:
    - path: scripts/phase31_parallel_launch.py
      provides: Fixed baseline, lane registration, join verification, and protected-state operations.
    - path: /tmp/multilang-phase31-parallel/baseline.json
      provides: External read-only exact baseline/runtime/protected-state contract shared by both worktrees.
  key_links:
    - from: clean integration HEAD and protected paths
      to: parallel-baseline.json
      via: canonical commit/tree/path/fingerprint capture
    - from: parallel-baseline.json
      to: 31-30 and 31-31 isolated worktrees
      via: exact baseline and disjoint lane allowlists
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 29

## Objective

Create the one real shared prerequisite for safe parallelism: a clean exact baseline, frozen isolated Python 3.12 runtime, no-follow `.venv` fingerprint, and disjoint lane ownership.

## Context

- `.planning/AI-LINGUISTIC-REVIEW-POLICY.md`
- `31-25-SUMMARY.md`
- `scripts/verify_phase31_runtime_isolation.py`

## Requirements Covered

- KHAN-01, KHAN-02, KPRO-01, KPRO-02

## Must-Haves

1. Fixed safe Python 3.12 runtime prepared once before parallel work.
2. One clean integration commit/tree and complete protected-state fingerprint.
3. Disjoint AI/media lane allowlists and exact join verification contract.

## Anti-Goals

- No content/media/evidence/provider/activation/export work.

## Hard Boundaries

- `.planning/.local/phase31-parallel-baseline.json` is local orchestration state, not approval or production evidence.

## Evidence Contract

- Tests cover unsafe symlinks, dirty baselines, overlap, wrong lane head/base, unrelated edits, stale protected reads, replay, and join refusal.

## Common Pitfalls

- Starting worktrees from different heads, sharing one writable environment during sync, or fingerprinting unsafe `.venv` by following links.

## Stop-And-Challenge

- No parallel lane starts unless every preflight field passes.

## Approval Gates

- None.

<checks>
<plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: The only serial step is a real common-baseline/runtime prerequisite; subsequent linguistic and media plans are disjoint and parallel.
</plan_check>
</checks>

## Tasks

<task id="31-29-01" type="auto">
  <name>Implement no-follow runtime fingerprint and parallel launch verifier</name>
  <files>
    - MODIFY: scripts/verify_phase31_runtime_isolation.py
    - CREATE: scripts/phase31_parallel_launch.py
    - MODIFY: tests/services/test_phase31_runtime_isolation.py
    - CREATE: tests/services/test_phase31_parallel_launch.py
  </files>
  <action>
    Add `fingerprint-venv`, which hashes path/type/mode/link-target metadata and regular-file bytes without following links, reports unsafe state honestly, and never repairs it. Add fixed launch operations `prepare-baseline`, `get`, `verify-baseline`, `verify-worktree-runtime`, `record-lane`, `trusted-baseline-sha256`, `verify-integration-base`, `record-lane-head`, `verify-lane`, `verify-join`, `verify-merged-lanes`, `merge-lanes`, and `verify-protected-state`. The baseline lives only at fixed safe `/tmp/multilang-phase31-parallel/baseline.json` with parent mode 0700, file mode 0444, and a diagnostic SHA-256 sidecar. `prepare-baseline` returns the digest once to the executor as `PHASE31_BASELINE_SHA256`; every lane command requires that independently carried value and commits it into its handoff instead of trusting the adjacent sidecar. At join, `trusted-baseline-sha256` reads the digest independently from both exact committed handoff blobs and requires equality before checking `/tmp`. Post-commit lane heads are stored separately in mode-0600 `lane-heads.json`; its sidecar is diagnostic, while verification is anchored by the exact committed handoffs and heads. `verify-worktree-runtime` requires `PYTHONPATH=$PWD/src`, proves `multilang.__file__` is beneath the current worktree, and binds checkout/base to the external baseline. Tests replace baseline JSON+sidecar together and lane-head JSON+sidecar together after lane creation and require every recording, sealing, join, and merge operation to reject them.
  </action>
  <verify>
    <automated>python3 scripts/verify_phase31_runtime_isolation.py prepare &amp;&amp; UV_PROJECT_ENVIRONMENT=/tmp/multilang-phase31-py312 UV_OFFLINE=1 uv sync --frozen --python 3.12 --extra dev &amp;&amp; /tmp/multilang-phase31-py312/bin/python -m pytest tests/services/test_phase31_runtime_isolation.py tests/services/test_phase31_parallel_launch.py -q</automated>
  </verify>
  <done>The helper can safely fingerprint the existing unsafe `.venv` and reject any untrusted or overlapping parallel lane.</done>
</task>

<task id="31-29-02" type="auto">
  <name>Prepare frozen runtime and record the clean common baseline</name>
  <files>
    - CREATE: /tmp/multilang-phase31-parallel/baseline.json
    - CREATE: /tmp/multilang-phase31-parallel/baseline.json.sha256
  </files>
  <action>
    After Task 01 is committed, require a clean worktree. Prepare `/tmp/multilang-phase31-py312` and `/tmp/multilang-phase31-parallel` once, sync frozen Python 3.12 dependencies only into the fixed environment, and prove Python 3.12. Capture current HEAD/tree, lock/project hashes, no-follow repository `.venv` fingerprint, protected candidates/evidence/media/pointer/export hashes, and exact disjoint allowlists into the external read-only baseline plus sidecar. Capture the digest returned directly by `prepare-baseline` once as executor state `PHASE31_BASELINE_SHA256`; never repopulate it from either `/tmp` file. Create exact worktrees `/tmp/multilang-phase31-ai` and `/tmp/multilang-phase31-media` from that HEAD and inject the fixed variable into every lane until both handoffs commit it. Every command uses `/tmp/multilang-phase31-py312/bin/python` with `PYTHONPATH="$PWD/src"` and never invokes shared-project `uv run`.
  </action>
  <verify>
    <automated>python3 scripts/verify_phase31_runtime_isolation.py prepare &amp;&amp; UV_PROJECT_ENVIRONMENT=/tmp/multilang-phase31-py312 UV_OFFLINE=1 uv sync --frozen --python 3.12 --extra dev &amp;&amp; test "$(/tmp/multilang-phase31-py312/bin/python -c 'import sys; print(f"{sys.version_info.major}.{sys.version_info.minor}")')" = "3.12" &amp;&amp; PHASE31_BASELINE_SHA256="$(/tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py prepare-baseline --output /tmp/multilang-phase31-parallel/baseline.json --print-sha256)" &amp;&amp; test "$(/tmp/multilang-phase31-py312/bin/python scripts/phase31_parallel_launch.py verify-baseline --baseline /tmp/multilang-phase31-parallel/baseline.json --baseline-sha256 "$PHASE31_BASELINE_SHA256")" = "parallel_baseline_status=verified"</automated>
  </verify>
  <done>Both parallel lanes have one exact clean baseline, disjoint ownership, protected-state hashes, and a pre-lane `.venv` fingerprint.</done>
</task>

## Verification

- Re-run baseline verification read-only and prove candidates/evidence/media/pointer/exports are unchanged.

## Success Criteria

- Plans 31-30 and 31-31 can start concurrently without shared writes or ambiguous ancestry.

## High-Leverage Review

- Recompute commit/tree/runtime/allowlist/protected-state fields in a second pass.

## Leverage Review

- Lost: One minimal serial preflight.
- Kept: Exact provenance and no shared-environment mutation.
- Gained: Safe machine-verifiable parallel execution.
