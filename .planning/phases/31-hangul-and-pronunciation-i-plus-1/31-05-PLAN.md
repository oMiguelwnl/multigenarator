---
phase: 31-hangul-and-pronunciation-i-plus-1
plan: "05"
type: execute
wave: 5
runtime: opencode
assurance: self_checked
depends_on: [31-04]
files_modified:
  - src/multilang/services/phoneme_deck.py
  - src/multilang/services/russian_phoneme_deck.py
  - src/multilang/templates/korean_hangul_card.md
  - tests/services/test_phoneme_deck.py
  - tests/services/test_russian_phoneme_deck.py
  - tests/services/test_card_template_loader.py
files-modified:
  - src/multilang/services/phoneme_deck.py
  - src/multilang/services/russian_phoneme_deck.py
  - src/multilang/templates/korean_hangul_card.md
  - tests/services/test_phoneme_deck.py
  - tests/services/test_russian_phoneme_deck.py
  - tests/services/test_card_template_loader.py
autonomous: true
requirements: [KHAN-01, KPRO-01]
non_goals:
  - Do not change existing Russian, Polish, or Greek inventories, voices, audio synthesis behavior, commands, IDs, GUID inputs, field values, template bytes, or exported artifacts.
  - Do not parameterize, import, or modify the Japanese kana template/source as the Korean implementation path.
  - Do not implement Korean source joins, media readiness, export writers, or visual acceptance in this plan.
hard_boundaries:
  - The language-neutral phoneme layer owns only the exact nine-field tuple, shared template loading, ordered mapping, model construction, note construction, and GUID injection.
  - Existing public symbols in `russian_phoneme_deck.py` remain importable as wrappers or aliases with byte-equivalent rendered behavior.
  - The Korean Hangul template renders frozen values only and never calculates pronunciation, concepts, review, or media readiness.
escalation_triggers:
  - Stop if extraction changes any existing field order, template/CSS byte sequence, stable ID, note/deck name, GUID input, CLI surface, or current APKG behavior.
  - Stop if Korean template work requires touching Japanese source/template files.
approval_gates:
  - Static template checks do not approve appearance, readability, responsiveness, playback, or Anki import; Phase 34 owns observed acceptance.
anti_regression_targets:
  - Russian model/deck IDs remain 1_602_300_601/602, Polish 603/604, and Greek 605/606.
  - Russian/Polish/Greek front, back, CSS, field mappings, public imports, CLI exports, and audio behavior remain byte-equivalent where currently asserted.
  - Japanese kana source, template, IDs, fonts, and tests remain untouched.
known_unknowns:
  - Final Korean font rendering and responsive behavior cannot be established without observed Anki Desktop/mobile evidence.
ui_proof_slots: []
no_ui_proof_rationale: This plan claims only static template/model contracts; rendered Anki visual and playback evidence is explicitly Phase 34 scope.
high_leverage_surfaces:
  - src/multilang/services/phoneme_deck.py
  - src/multilang/services/russian_phoneme_deck.py
  - src/multilang/templates/korean_hangul_card.md
second_pass_required: true
high_leverage_second_pass: Compare pre/post Russian, Polish, and Greek model fields/templates/CSS/GUIDs and inspect every Korean template reference, class, label, font, and conditional-media block.
closure_claim_limit: This plan proves shared mechanics and static Korean-owned template structure only; it cannot claim content completeness, media survival, export readiness, or visual quality.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Generic mechanics become an explicit module instead of private Russian helpers.
  kept: Every shipped phoneme identity, public API, template byte, and legacy audio behavior.
  gained: Korean can reuse the exact nine-field visual contract without copying unsafe raw-letter synthesis or coupling to Russian semantics.
must_haves:
  truths:
    - A language-neutral module builds the exact nine-field phoneme model/note contract and allows only additive Korean font CSS.
    - Every existing Russian, Polish, and Greek public import and rendered artifact contract remains unchanged after extraction.
    - The Korean Hangul template has exactly 15 locked fields, Korean-owned labels/classes/fonts, hidden curriculum evidence, conditional media, and no Japanese semantic/font leakage.
    - Static checks preserve the kana layout mechanics without modifying or importing the Japanese implementation.
  artifacts:
    - path: src/multilang/services/phoneme_deck.py
      provides: Language-neutral nine-field model, note, template, ordered-field, and GUID mechanics.
    - path: src/multilang/services/russian_phoneme_deck.py
      provides: Compatibility wrappers preserving every existing public phoneme deck behavior.
    - path: src/multilang/templates/korean_hangul_card.md
      provides: Korean-owned Hangul front/back/CSS and exact field declaration.
    - path: tests/services/test_phoneme_deck.py
      provides: Neutral and Korean-additive mechanics contract tests.
  key_links:
    - from: src/multilang/services/russian_phoneme_deck.py
      to: src/multilang/services/phoneme_deck.py
      via: Existing builders delegate only model/note/field mechanics while retaining language-specific exports/audio.
    - from: src/multilang/templates/korean_hangul_card.md
      to: Hangul field order from 31-APPROACH.md
      via: Template references are a strict subset of the exact 15 declared fields.
---

# Phase 31: Hangul and Pronunciation i+1 - Plan 05

## Objective

<objective>
Extract language-neutral phoneme rendering mechanics without regression and create the isolated Korean Hangul template from approved kana layout behavior.

Purpose: Korean needs independent note identity and semantics while reusing only proven visual mechanics; unsafe legacy synthesis and existing language identities must remain isolated.

Output: Shared nine-field model/note helpers, compatibility-preserved existing phoneme module, Korean-owned Hangul template, and exact regression/static tests.
</objective>

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>

## Context

<context>
@AGENTS.md
@.planning/SPEC.md
@.planning/ROADMAP.md
@KOREAN-STRUCTURE.md
@.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-APPROACH.md
@.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-RESEARCH.md
@.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-PATTERNS.md
@.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-01-SUMMARY.md
@src/multilang/services/russian_phoneme_deck.py
@src/multilang/templates/russian_phoneme_card.md
@src/multilang/templates/japanese_kana_card.md
@tests/services/test_russian_phoneme_deck.py
@tests/services/test_card_template_loader.py

<interfaces>
Existing public contract that must remain available:
```python
PHONEME_FIELD_NAMES = (
    "Spellings", "Sound", "letter_audio", "Example Word", "word_audio",
    "Word Translation", "Example Sentence", "sentence_audio", "Sentence Translation",
)

class RussianPhonemeCard: ...
def build_russian_phoneme_model() -> genanki.Model: ...
def build_polish_phoneme_model() -> genanki.Model: ...
def build_greek_phoneme_model() -> genanki.Model: ...
```

New neutral interface consumed by 31-06:
```python
class PhonemeCard: ...
class PhonemeNote(genanki.Note): ...
def build_phoneme_model(*, model_id: int, note_type_name: str, additional_css: str = "") -> genanki.Model: ...
def build_phoneme_note(card: PhonemeCard, *, model: genanki.Model, guid: str | None = None) -> genanki.Note: ...
def phoneme_card_fields(card: PhonemeCard) -> list[str]: ...
```
</interfaces>
</context>

## Requirements Covered

- **KHAN-01:** Korean-owned kana-derived Hangul layout with exact fields/fonts and no Japanese leakage.
- **KPRO-01:** exact shared nine-field HTML/CSS/model contract exposed for Korean-specific identity.

## Must-Haves

1. Shared mechanics are truly language-neutral and narrow.
2. Existing phoneme decks are byte/identity compatible.
3. Korean Hangul static structure is isolated and exact.

## Anti-Goals

- No content, media, source joins, export writer, voice, provider, or visual acceptance work.

## Hard Boundaries

- Keep legacy synthesis and broad exceptions in the existing module; Korean never imports or calls them.
- Copy layout behavior, not Japanese field names/classes/fonts/source coupling.
- Evidence fields are stored but never rendered as learner copy.

## Evidence Contract

- Capture exact existing fields/templates/CSS/IDs/GUIDs before refactoring and assert them after delegation.
- Scan Korean template source case-insensitively for forbidden Japanese/Kana/Romaji/Hiragana/Katakana classes, labels, and font tokens.
- Validate every template reference against the exact Hangul field tuple.

## Common Pitfalls

- Renaming a public Russian class rather than retaining an alias/wrapper.
- Appending Korean CSS to existing language models.
- Importing the Japanese template at runtime.
- Reporting static CSS checks as visual acceptance.

## Stop-And-Challenge

- Stop on any existing artifact byte/ID/GUID drift or Japanese-file modification requirement.

## Approval Gates

- No human visual or playback approval is requested or inferred here.

<checks>
<plan_check>
checker: fresh-context same-runtime
checker_runtime: opencode
status: passed
blocking: false
cycle_count: 1
notes: Final user-authorized fresh-context same-runtime recheck passed with no issues; all prior blockers and warnings are closed.
</plan_check>
</checks>

## Tasks

<tasks>

<task id="31-05-01" type="auto" tdd="true">
  <name>Extract exact language-neutral nine-field phoneme mechanics</name>
  <files>
    - CREATE: tests/services/test_phoneme_deck.py
    - CREATE: src/multilang/services/phoneme_deck.py
  </files>
  <behavior>
    - RED: The neutral field tuple equals the existing nine names and ordered mapping exactly.
    - RED: Neutral model construction loads the shared front/back/CSS and appends only caller-supplied additive CSS after the unchanged base CSS.
    - RED: Neutral note construction injects the supplied stable GUID and maps all audio/Portuguese/sentence values to exact positions.
    - RED: Unknown template references and non-additive CSS replacement attempts fail closed.
  </behavior>
  <action>
    Write a new test against the desired public neutral API, run RED, then move only `PHONEME_FIELD_NAMES`, shared template parsing, the generic `PhonemeCard`/`PhonemeNote` shape, ordered field mapping, model construction, and note/GUID injection into `phoneme_deck.py`. Keep the template path `russian_phoneme_card.md` because it is already the approved shared visual contract, but expose no Russian inventory, voice, settings, synthesis, output, or CLI logic. Additional CSS must be append-only so Korean can add a font stack without changing shared bytes.
  </action>
  <verify>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_phoneme_deck.py -q</automated>
  </verify>
  <done>The neutral module owns exactly the reusable nine-field mechanics and rejects schema/template drift.</done>
</task>

<task id="31-05-02" type="auto" tdd="true">
  <name>Delegate existing phoneme builders through compatibility wrappers without drift</name>
  <files>
    - MODIFY: tests/services/test_russian_phoneme_deck.py
    - MODIFY: src/multilang/services/russian_phoneme_deck.py
  </files>
  <behavior>
    - RED: All existing constants, classes, builders, exporters, inventories, voices, and `__all__` symbols remain importable.
    - RED: Russian/Polish/Greek model fields, front/back, CSS, note values, IDs, deck/note names, and GUIDs equal the pre-extraction contract.
    - REGRESSION: Existing APKG tests still construct the same legacy audio path and archive shape.
  </behavior>
  <action>
    Strengthen compatibility tests before modifying the module. Re-export `PHONEME_FIELD_NAMES` and preserve `RussianPhonemeCard`, `RussianPhonemeNote`, private helper behavior where tests rely on it, and every public function. Delegate model/note/ordered-field mechanics to `phoneme_deck.py`; leave inventories, fixed IDs, voices/locales, Settings/Azure construction, raw-letter synthesis, swallowed legacy errors, deck assembly, filenames, and command behavior in `russian_phoneme_deck.py` unchanged. Do not clean up adjacent legacy behavior because Korean will use the dedicated reviewed exporter instead.
  </action>
  <verify>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_phoneme_deck.py tests/services/test_russian_phoneme_deck.py -q</automated>
  </verify>
  <done>Existing phoneme public APIs/artifacts remain compatible while their language-neutral mechanics delegate to the new module.</done>
</task>

<task id="31-05-03" type="auto" tdd="true">
  <name>Create and statically validate the Korean-owned Hangul template</name>
  <files>
    - CREATE: src/multilang/templates/korean_hangul_card.md
    - MODIFY: tests/services/test_card_template_loader.py
  </files>
  <behavior>
    - RED: The declared field list is exactly the locked 15-field Hangul order and every Mustache reference is allowlisted.
    - RED: Front/back use Korean-owned class names and show category/glyph, then conditionally reveal GIF, reading/name, sound/audio, picture, strokes, and mnemonic in the approved sequence.
    - RED: Target/prerequisite/observed/unknown/policy evidence is declared but not rendered.
    - RED: Korean font stack, dark canvas, conditional media, bounded image rules, and replay-button reset are present; forbidden Japanese tokens/fonts/classes are absent case-insensitively.
  </behavior>
  <action>
    Add static failing tests, then create a standalone template file by reproducing only the kana layout order and safe media mechanics. Use `hangulCard`, `hangulCategory`, `hangulGlyph`, and `koFont`-style Korean-owned classes; Portuguese learner hints/labels; and an explicit stack containing `Noto Sans KR`, `Apple SD Gothic Neo`, `Malgun Gothic`, `맑은 고딕`, and safe fallbacks. Render plain learner fields and the four conditional media fields only; keep all five curriculum evidence fields hidden. Do not import/read the Japanese template at runtime and do not claim rendered quality from these checks.
  </action>
  <verify>
    <automated>UV_OFFLINE=1 uv run --extra dev pytest tests/services/test_card_template_loader.py -q -k "kana or phoneme or hangul or dark_templates"</automated>
  </verify>
  <done>The Korean-owned template has exact safe references/layout mechanics and zero Japanese semantic/font leakage under static inspection.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Korean foundation note structures | Independent Hangul template and shared pronunciation mechanics. | COVERED |
| REQ | KHAN-01 | Kana-derived Korean note layout | Exact 15 fields, Korean classes/fonts, conditional media. | COVERED |
| REQ | KPRO-01 | Existing phoneme field/layout reuse | Neutral exact nine-field builder. | COVERED |
| RESEARCH | — | Preserve Russian/Polish/Greek bytes/APIs | Compatibility wrappers and exact regressions. | COVERED |
| RESEARCH | — | No Japanese leakage or visual overclaim | Static forbidden-token scan and claim limit. | COVERED |
| CONTEXT | — | Korean-owned Hangul template | Created without parameterizing Japanese source. | COVERED |
| CONTEXT | — | Extract only language-neutral phoneme mechanics | Narrow module; legacy behavior remains isolated. | COVERED |

Excluded without gap: Korean model/deck IDs and actual packaging are Plan 31-06; observed Anki visuals/import/playback are Phase 34.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Template text -> Anki rendering | Invalid/unsafe references or script markup could execute or leak fields. |
| Shared refactor -> shipped artifacts | A neutral extraction could silently change existing note identity/behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-05-01 | Tampering | existing phoneme contracts | mitigate | Exact pre/post fields/templates/CSS/IDs/GUID/public-import assertions. |
| T-31-05-02 | Elevation / Tampering | Hangul template | mitigate | Fixed source, strict reference allowlist, no manifest-supplied JS/classes/templates. |
| T-31-05-03 | Spoofing | Japanese/Korean identity | mitigate | Korean-owned file/classes/fonts plus forbidden-token scans and Japanese no-touch. |
| T-31-05-04 | Repudiation | visual claims | accept | Static proof is explicitly bounded; Phase 34 supplies observed evidence. |
</threat_model>

## Verification

- Run all three task commands and the complete existing kana/phoneme matrix.
- Confirm `git diff -- src/multilang/templates/japanese_kana_card.md src/multilang/services/japanese_kana_deck.py` is empty.

## Success Criteria

- Korean has isolated static Hangul structure and exact shared pronunciation mechanics; every existing phoneme/kana regression remains green.

## High-Leverage Review

- Compare all existing model outputs and inspect Korean template fields/references/classes/fonts manually after automated checks.

## Leverage Review

- Lost: Private generic helpers move into a public neutral module.
- Kept: All shipped note identity and visuals.
- Gained: Safe exact contract reuse for Korean without unsafe synthesis duplication.

## Notes

- The dependency on 31-04 is an intentional execution mutex: Phase 31 plans and their `uv` verification commands run serially against the shared environment.

<output>
After completion, create `.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-05-SUMMARY.md`.
</output>
