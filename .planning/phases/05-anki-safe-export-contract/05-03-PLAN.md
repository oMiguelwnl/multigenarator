---
phase: 05-anki-safe-export-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - pyproject.toml
  - uv.lock
  - src/multilang/services/export_anki_package.py
  - tests/services/test_export_anki_package.py
autonomous: true
requirements:
  - CARD-03
  - EXPT-01
  - EXPT-03
must_haves:
  truths:
    - "Users can receive a packaged `.apkg` artifact built from the fixed export-card schema."
    - "Users get translation hidden on the front and shown on the back through the note template."
    - "Users get packaged media files that match the exported sound references."
  artifacts:
    - path: "src/multilang/services/export_anki_package.py"
      provides: "genanki-backed model, note, deck, and package export service"
      min_lines: 120
    - path: "pyproject.toml"
      provides: "genanki runtime dependency"
      contains: "genanki"
  key_links:
    - from: "src/multilang/services/export_anki_package.py"
      to: "src/multilang/domain/exporting.py"
      via: "stable note GUID and field order"
      pattern: "guid|SortIndex|Translation"
    - from: "src/multilang/services/export_anki_package.py"
      to: "tests/services/test_export_anki_package.py"
      via: "template and package verification"
      pattern: "FrontSide|sound|media_files"
---

<objective>
Generate real `.apkg` packages with `genanki`, stable note identity, correct front/back template behavior, and bundled media.

Purpose: Deliver the primary Anki-native export artifact that users can import without manual repair.
Output: `genanki` dependency plus package-export service and unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-anki-safe-export-contract/05-RESEARCH.md
@.planning/phases/05-anki-safe-export-contract/05-PATTERNS.md
@.planning/phases/05-anki-safe-export-contract/05-02-PLAN.md
@pyproject.toml
@src/multilang/domain/exporting.py
@src/multilang/services/assemble_export_cards.py
@src/multilang/repositories/audio_repository.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the `genanki` dependency and stable note-type package service</name>
  <files>pyproject.toml, uv.lock, src/multilang/services/export_anki_package.py, tests/services/test_export_anki_package.py</files>
  <read_first>pyproject.toml, src/multilang/domain/exporting.py, src/multilang/services/assemble_export_cards.py, tests/services/test_azure_speech_adapter.py</read_first>
  <behavior>
    - Test 1: the generated model contains the exact ten field names from `CARD-01` and a front/back template where `Translation` is absent on the front but present on the back.
    - Test 2: packaged notes reuse deterministic GUIDs across reruns even when mutable fields change.
    - Test 3: the package includes all referenced media files and rejects missing on-disk audio before writing the `.apkg`.
    - Test 4: sound references inside notes match packaged media basenames exactly.
  </behavior>
  <action>Add `genanki>=0.13,<0.14` to `pyproject.toml` and refresh `uv.lock`. Create `src/multilang/services/export_anki_package.py` that wraps `genanki` behind one export boundary: define stable hardcoded `MODEL_ID` and `DECK_ID` constants, build a single Multilang note model with the exact Phase 5 field order, and create a custom `genanki.Note` subclass whose `guid` property uses the deterministic export identity from `src/multilang/domain/exporting.py`. The front template must show `Front of Card`/supporting front-side fields but must not reveal `Translation`; the back template must include `{{FrontSide}}`, an answer separator, `Definitions`, `Example Sentence`, `Translation`, and audio fields. Collect media files from the exported card audio references, verify every referenced file exists on disk, and pass the real file paths to `genanki.Package.media_files` while keeping note fields basename-only.</action>
  <acceptance_criteria>`pyproject.toml` contains `genanki`; `tests/services/test_export_anki_package.py` proves template behavior, deterministic GUID stability, bundled media, and failure on missing media; `src/multilang/services/export_anki_package.py` contains stable model/deck IDs and explicit `Translation` placement only on the back template.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_export_anki_package.py -q</automated>
  </verify>
  <done>The codebase can build real `.apkg` files with a stable note type, deterministic note identity, and bundled audio media.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export-card rows → genanki model/package | fixed-schema rows become Anki-native notes and package metadata |
| filesystem media paths → packaged deck | local media files are bundled into a shareable archive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | T | `src/multilang/services/export_anki_package.py` | mitigate | hardcode stable model/deck IDs and exact field names so package structure cannot drift between reruns |
| T-05-10 | S | note GUID generation | mitigate | compute GUID from stable identity only, preventing accidental duplicate-note spoofing from mutable content changes |
| T-05-11 | T | packaged media mapping | mitigate | verify every `[sound:basename]` maps to an existing file and reject missing files before package write |
| T-05-12 | I | front/back template | mitigate | keep `Translation` out of the front template and include it only on the back template with explicit unit assertions |
</threat_model>

<verification>
- `uv run pytest tests/services/test_export_anki_package.py -q`
</verification>

<success_criteria>
- `genanki` is installed and locked in the project.
- `.apkg` export code uses stable note identity and packaged media.
- The template hides `Translation` on the front and reveals it on the back.
</success_criteria>

<output>
After completion, create `.planning/phases/05-anki-safe-export-contract/05-03-SUMMARY.md`
</output>
