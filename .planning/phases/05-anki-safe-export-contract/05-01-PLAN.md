---
phase: 05-anki-safe-export-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/domain/exporting.py
  - src/multilang/db/models.py
  - src/multilang/repositories/export_repository.py
  - alembic/versions/20260426_05_export_contract_tables.py
  - tests/domain/test_exporting.py
  - tests/repositories/test_export_repository.py
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - EXPT-01
must_haves:
  truths:
    - "Users can rely on one frozen card field order for every export artifact."
    - "Exported cards have a stable identity that survives reruns and content repairs."
    - "Export artifacts can be tracked per job instead of being ephemeral files only."
  artifacts:
    - path: "src/multilang/domain/exporting.py"
      provides: "typed export-card contract with fixed field order and deterministic GUID support"
      min_lines: 60
    - path: "src/multilang/repositories/export_repository.py"
      provides: "job-scoped persistence for frozen card snapshots and artifact manifests"
      min_lines: 80
    - path: "alembic/versions/20260426_05_export_contract_tables.py"
      provides: "schema for export snapshots and deck export manifests"
      contains: "card_exports"
  key_links:
    - from: "src/multilang/repositories/export_repository.py"
      to: "src/multilang/db/models.py"
      via: "ORM row mapping"
      pattern: "CardExportModel|DeckExportModel"
    - from: "alembic/versions/20260426_05_export_contract_tables.py"
      to: "src/multilang/db/models.py"
      via: "matching table names and constraints"
      pattern: "card_exports|deck_exports"
---

<objective>
Freeze the export contract and persist export snapshots so later plans can build `.apkg`, CSV, and TSV artifacts against one deterministic schema.

Purpose: Prevent field-order drift and duplicate-prone note identity before any serializer or CLI wiring lands.
Output: Typed export models, export repository, and the Phase 5 schema migration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-anki-safe-export-contract/05-RESEARCH.md
@.planning/phases/05-anki-safe-export-contract/05-VALIDATION.md
@.planning/phases/05-anki-safe-export-contract/05-PATTERNS.md
@src/multilang/domain/lexicon.py
@src/multilang/domain/text_quality.py
@src/multilang/domain/audio.py
@src/multilang/db/models.py
@src/multilang/repositories/text_repository.py
@src/multilang/repositories/audio_repository.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the frozen export-card contract</name>
  <files>src/multilang/domain/exporting.py, tests/domain/test_exporting.py</files>
  <read_first>src/multilang/domain/lexicon.py, src/multilang/domain/text_quality.py, src/multilang/domain/audio.py</read_first>
  <behavior>
    - Test 1: export field names remain exactly `SortIndex`, `word`, `Front of Card`, `IPA`, `Definitions`, `Example Sentence`, `Translation`, `word_audio`, `sentence_audio`, `Image` in that order.
    - Test 2: deterministic GUID generation stays stable when mutable fields like `Definitions`, `Example Sentence`, `Translation`, or audio filenames change.
    - Test 3: `Image` defaults to an empty string and `Definitions` accepts a single HTML field value instead of list markup.
  </behavior>
  <action>Create `src/multilang/domain/exporting.py` with typed Pydantic contracts for the Phase 5 export boundary: a fixed ordered field-name tuple, a stable identity model, a frozen export-card row model, and export artifact metadata enums for `apkg`, `csv`, and `tsv`. Hardcode the exact `CARD-01` field order and make the model reject missing or reordered fields. Add a deterministic GUID helper that hashes only stable identity inputs such as language, source type, job/item identity, and lemma/sort identity — not mutable text or audio fields. Preserve `Image` as `""` per `CARD-02` and keep `Definitions` as a single HTML-capable string.</action>
  <acceptance_criteria>tests/domain/test_exporting.py asserts the exact field order; `src/multilang/domain/exporting.py` contains the literal field names from `CARD-01`; GUID tests prove mutable-content changes do not change note identity; `Image` defaults to an empty string.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/domain/test_exporting.py -q</automated>
  </verify>
  <done>The codebase has one canonical export contract and it encodes the fixed schema and stable identity rules needed by all later export work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist frozen card snapshots and artifact manifests</name>
  <files>src/multilang/db/models.py, src/multilang/repositories/export_repository.py, tests/repositories/test_export_repository.py</files>
  <read_first>src/multilang/db/models.py, src/multilang/repositories/text_repository.py, src/multilang/repositories/audio_repository.py</read_first>
  <behavior>
    - Test 1: one export-card snapshot row is upserted per stable `(job_id, item_key)` identity instead of duplicating rows on rerun.
    - Test 2: deck export artifacts persist format, output path, deck name, and card count per job.
    - Test 3: repository queries return job-scoped snapshots in deterministic sort order for later serializers.
  </behavior>
  <action>Extend `src/multilang/db/models.py` with `CardExportModel` and `DeckExportModel` tables that store frozen export rows and produced artifact metadata. Include explicit columns for the ten fixed card fields, deterministic note GUID, stable sort index, export format, output path, artifact status, and timestamps. Create `src/multilang/repositories/export_repository.py` following the existing repository pattern: constructor takes `Session`, upsert methods map domain contracts to ORM rows, list methods return rows ordered by `sort_index` then `item_key`, and artifact writes remain job-scoped. Add repository tests that prove reruns update existing rows instead of duplicating them.</action>
  <acceptance_criteria>`src/multilang/db/models.py` contains `CardExportModel` and `DeckExportModel`; `src/multilang/repositories/export_repository.py` exposes upsert/list helpers; tests confirm exactly one snapshot row per `(job_id, item_key)` and deterministic retrieval order.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/repositories/test_export_repository.py -q</automated>
  </verify>
  <done>Later plans can read and write frozen export data through a repository boundary instead of reconstructing note payloads ad hoc.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply and verify the Phase 5 export schema migration</name>
  <files>alembic/versions/20260426_05_export_contract_tables.py</files>
  <read_first>alembic/versions/20260424_04_audio_synthesis_tables.py, src/multilang/db/models.py</read_first>
  <action>Create an Alembic migration named `20260426_05_export_contract_tables.py` that creates `card_exports` and `deck_exports` with the same table names, unique constraints, and indexes expected by the new ORM models. After writing the migration, apply it against a disposable SQLite database using `MULTILANG_DATABASE_URL=sqlite+pysqlite:///$(pwd)/.tmp-phase05.db uv run alembic upgrade head`, then run the repository tests against the migrated schema. Remove the disposable database file after verification if the test harness does not clean it up automatically.</action>
  <acceptance_criteria>The migration file contains `card_exports` and `deck_exports`; Alembic upgrades a disposable database to head without error; repository tests pass against the migrated schema.</acceptance_criteria>
  <verify>
    <automated>rm -f .tmp-phase05.db && MULTILANG_DATABASE_URL=sqlite+pysqlite:///$(pwd)/.tmp-phase05.db uv run alembic upgrade head && uv run pytest tests/domain/test_exporting.py tests/repositories/test_export_repository.py -q</automated>
  </verify>
  <done>The schema state matches the export contract before any serializer, package, or CLI work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lexical/text/audio persistence → export contract | persisted content becomes HTML-capable card fields |
| export repository → filesystem artifacts | database-backed content is frozen into shareable files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `src/multilang/domain/exporting.py` | mitigate | enforce exact field order and explicit HTML-capable field semantics so later plans cannot tamper with schema shape silently |
| T-05-02 | T | `src/multilang/repositories/export_repository.py` | mitigate | scope snapshot/artifact upserts to stable job/item keys and reject duplicate row creation on rerun |
| T-05-03 | D | `alembic/versions/20260426_05_export_contract_tables.py` | mitigate | require blocking migration verification before downstream export work so runtime code cannot operate against missing tables |
| T-05-04 | R | export snapshot rows | mitigate | persist deterministic note GUID and artifact metadata so exported note identity is auditable per job |
</threat_model>

<verification>
- `uv run pytest tests/domain/test_exporting.py tests/repositories/test_export_repository.py -q`
- `MULTILANG_DATABASE_URL=sqlite+pysqlite:///$(pwd)/.tmp-phase05.db uv run alembic upgrade head`
</verification>

<success_criteria>
- One canonical export contract exists with the exact ten required field names in order.
- Export snapshots and artifact manifests persist through a repository boundary.
- The Phase 5 schema migration applies cleanly on a disposable database.
</success_criteria>

<output>
After completion, create `.planning/phases/05-anki-safe-export-contract/05-01-SUMMARY.md`
</output>
