---
phase: 05-anki-safe-export-contract
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/multilang/settings.py
  - src/multilang/runtime.py
  - src/multilang/cli.py
  - tests/cli/test_export_command.py
  - tests/integration/test_export_job_flow.py
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
  - EXPT-03
must_haves:
  truths:
    - "Users can export an existing job as `.apkg`, CSV, or TSV from the shipped CLI path."
    - "Users get explicit failures instead of silent success when export prerequisites are missing."
    - "Users can rerun export for the same job without changing note identity or packaged media references."
  artifacts:
    - path: "src/multilang/cli.py"
      provides: "CLI export command on the shipped app surface"
      min_lines: 40
    - path: "tests/integration/test_export_job_flow.py"
      provides: "runtime-path export verification for real SQLite job data"
      min_lines: 80
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/runtime.py"
      via: "export command service resolution"
      pattern: "export"
    - from: "src/multilang/runtime.py"
      to: "src/multilang/services/export_anki_package.py"
      via: "runtime export orchestration"
      pattern: "Export"
---

<objective>
Expose the export workflow on the shipped CLI/runtime path and prove end-to-end artifact generation against real persisted job data.

Purpose: Turn the export services into the actual user-facing Phase 5 workflow.
Output: `multilang export` command, runtime export orchestration, and end-to-end tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-anki-safe-export-contract/05-RESEARCH.md
@.planning/phases/05-anki-safe-export-contract/05-03-PLAN.md
@src/multilang/settings.py
@src/multilang/runtime.py
@src/multilang/cli.py
@tests/cli/test_generate_command.py
@tests/integration/test_audio_job_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire `multilang export` onto the shipped runtime path</name>
  <files>src/multilang/settings.py, src/multilang/runtime.py, src/multilang/cli.py, tests/cli/test_export_command.py, tests/integration/test_export_job_flow.py</files>
  <read_first>src/multilang/settings.py, src/multilang/runtime.py, src/multilang/cli.py, tests/cli/test_generate_command.py, tests/integration/test_audio_job_flow.py</read_first>
  <behavior>
    - Test 1: `multilang export --job-id <id> --format apkg|csv|tsv --output-dir <dir>` writes the requested artifact and prints its path.
    - Test 2: exporting a job with flagged text, missing audio, or missing snapshots exits non-zero with explicit diagnostics.
    - Test 3: rerunning export for the same job preserves deterministic note GUIDs and media references.
    - Test 4: the runtime path works against a real SQLite database populated through the existing generate flow.
  </behavior>
  <action>Add a new shipped CLI command `multilang export` instead of overloading `multilang generate`. Accept `--job-id`, `--format` (`apkg`, `csv`, `tsv`), `--output-dir`, and optional `--deck-name`. Extend `src/multilang/settings.py` with a default export directory setting if the command needs one. In `src/multilang/runtime.py`, compose a runtime export service that reads frozen export snapshots or assembles them on demand from existing repositories, then dispatches to the tabular exporter or `genanki` package exporter. In `src/multilang/cli.py`, print the created artifact path and card count, and exit with a non-zero code plus explicit reason when prerequisites are missing. Add CLI tests and an integration test that builds a real SQLite job via the existing runtime path, exports `.apkg` and tabular artifacts, and asserts the files exist with deterministic content markers.</action>
  <acceptance_criteria>`src/multilang/cli.py` contains a new `export` command; CLI tests cover success and failure diagnostics; integration tests prove the shipped runtime can export real job data to `.apkg`, CSV, and TSV artifacts.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cli/test_export_command.py tests/integration/test_export_job_flow.py -q</automated>
  </verify>
  <done>Users can run one shipped CLI command to export completed jobs into Anki-native or plain-text artifacts with explicit diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arguments → runtime export orchestration | user-supplied job ids, formats, and output paths drive artifact creation |
| runtime export service → filesystem | exported deck artifacts are written to user-visible paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | T | `src/multilang/cli.py` | mitigate | validate `--format` against explicit enum values and reject unsupported output requests |
| T-05-14 | I | runtime export diagnostics | mitigate | fail non-zero with explicit messages when text/audio prerequisites are missing instead of producing misleading empty artifacts |
| T-05-15 | D | output path handling | mitigate | require writable output directory and let tests assert artifact existence after export |
| T-05-16 | R | rerun behavior | mitigate | reuse deterministic note GUID and frozen snapshot ordering so repeated exports remain auditable and stable |
</threat_model>

<verification>
- `uv run pytest tests/cli/test_export_command.py tests/integration/test_export_job_flow.py -q`
</verification>

<success_criteria>
- `multilang export` exists on the shipped CLI surface.
- The runtime path emits `.apkg`, CSV, and TSV artifacts from real job data.
- Missing export prerequisites fail loudly instead of silently generating broken output.
</success_criteria>

<output>
After completion, create `.planning/phases/05-anki-safe-export-contract/05-04-SUMMARY.md`
</output>
