---
phase: 05-anki-safe-export-contract
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified: []
autonomous: false
requirements:
  - CARD-03
  - EXPT-01
  - EXPT-03
must_haves:
  truths:
    - "Users can import the generated `.apkg` into Anki without manual field remapping."
    - "Users see `Translation` hidden on the front and revealed on the back in Anki itself."
    - "Users hear packaged word and sentence audio after import."
  artifacts:
    - path: ".planning/phases/05-anki-safe-export-contract/05-04-SUMMARY.md"
      provides: "generated sample export command and artifact path from the shipped runtime"
      min_lines: 20
  key_links:
    - from: "generated .apkg artifact"
      to: "Anki Desktop import flow"
      via: "manual import preview and review session"
      pattern: "field mapping / playable audio"
---

<objective>
Validate the real Anki import experience for the Phase 5 export artifacts.

Purpose: Close the last gap that automated tests cannot fully prove — import UI behavior and real playback inside Anki.
Output: Human verification approval or issue report for `.apkg` import, template behavior, and packaged audio.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-anki-safe-export-contract/05-RESEARCH.md
@.planning/phases/05-anki-safe-export-contract/05-VALIDATION.md
@.planning/phases/05-anki-safe-export-contract/05-04-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify real Anki import, template reveal behavior, and audio playback</name>
  <read_first>.planning/phases/05-anki-safe-export-contract/05-04-PLAN.md, .planning/phases/05-anki-safe-export-contract/05-04-SUMMARY.md</read_first>
  <what-built>The shipped CLI can export a sample `.apkg` deck from real Multilang job data, with fixed fields, bundled media, and the Multilang front/back card template.</what-built>
  <how-to-verify>
    1. Run the sample export command recorded in `05-04-SUMMARY.md` to produce a fresh `.apkg` artifact.
    2. Open Anki Desktop and import the `.apkg`.
    3. Confirm the import preview does not require manual field remapping.
    4. Open the imported note type preview and verify the front side does not show `Translation`.
    5. Reveal the back side and verify `Translation` appears there together with `Definitions` and `Example Sentence`.
    6. Review one imported card and confirm both word and sentence audio play successfully.
    7. If any step fails, capture the exact symptom (field remap prompt, missing audio, translation visible on front, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the failure precisely.</resume-signal>
  <acceptance_criteria>The user explicitly confirms no field remapping was needed, `Translation` stayed hidden on the front, the back revealed it correctly, and both packaged audio assets played in Anki Desktop.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_export_anki_package.py tests/cli/test_export_command.py tests/integration/test_export_job_flow.py -q</automated>
  </verify>
  <done>Phase 05 has real-product confirmation that the shipped `.apkg` behaves correctly inside Anki, not only in automated artifact tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated `.apkg` → Anki Desktop | final artifact is exercised in the real import/runtime environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | T | imported note template behavior | mitigate | require human verification that `Translation` is hidden on front and shown on back inside Anki itself |
| T-05-18 | D | packaged media playback | mitigate | require human verification that word and sentence audio both play after import |
</threat_model>

<verification>
- Automated pre-check: `uv run pytest tests/services/test_export_anki_package.py tests/cli/test_export_command.py tests/integration/test_export_job_flow.py -q`
- Human verification in Anki Desktop per the checkpoint instructions
</verification>

<success_criteria>
- Anki import does not require manual field remapping.
- Translation is hidden on the front and revealed on the back.
- Packaged audio plays after import.
</success_criteria>

<output>
After completion, create `.planning/phases/05-anki-safe-export-contract/05-05-SUMMARY.md`
</output>
