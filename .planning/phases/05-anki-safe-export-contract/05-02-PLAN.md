---
phase: 05-anki-safe-export-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/multilang/services/assemble_export_cards.py
  - src/multilang/services/export_tabular_bundle.py
  - tests/services/test_assemble_export_cards.py
  - tests/services/test_export_tabular_bundle.py
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-04
  - EXPT-02
  - EXPT-03
must_haves:
  truths:
    - "Users can receive one deterministic export row per accepted card."
    - "Users get `Definitions` as one `<br>`-joined field and `Image` as blank in every row."
    - "Users can open UTF-8-safe CSV and TSV fallback files without losing field structure."
  artifacts:
    - path: "src/multilang/services/assemble_export_cards.py"
      provides: "assembly from lexical/text/audio persistence into fixed export-card rows"
      min_lines: 100
    - path: "src/multilang/services/export_tabular_bundle.py"
      provides: "CSV/TSV serialization with Anki import headers and UTF-8 output"
      min_lines: 80
  key_links:
    - from: "src/multilang/services/assemble_export_cards.py"
      to: "src/multilang/repositories/export_repository.py"
      via: "snapshot persistence"
      pattern: "upsert_.*export"
    - from: "src/multilang/services/export_tabular_bundle.py"
      to: "src/multilang/domain/exporting.py"
      via: "ordered export fields"
      pattern: "SortIndex|Translation|Image"
---

<objective>
Turn persisted lexical, text, and audio rows into deterministic fixed-schema export cards and UTF-8-safe CSV/TSV fallback artifacts.

Purpose: Deliver the actual card contract users see before any `.apkg` packaging logic is layered on top.
Output: Export-card assembly service plus tabular serializers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-anki-safe-export-contract/05-RESEARCH.md
@.planning/phases/05-anki-safe-export-contract/05-PATTERNS.md
@.planning/phases/05-anki-safe-export-contract/05-01-PLAN.md
@src/multilang/domain/exporting.py
@src/multilang/repositories/export_repository.py
@src/multilang/repositories/lexical_repository.py
@src/multilang/repositories/text_repository.py
@src/multilang/repositories/audio_repository.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble accepted-card export rows with deterministic identity</name>
  <files>src/multilang/services/assemble_export_cards.py, tests/services/test_assemble_export_cards.py</files>
  <read_first>src/multilang/domain/exporting.py, src/multilang/repositories/lexical_repository.py, src/multilang/repositories/text_repository.py, src/multilang/repositories/audio_repository.py, tests/services/test_generate_audio_items.py</read_first>
  <behavior>
    - Test 1: accepted text + synthesized/reused audio rows assemble into one export card with the exact `CARD-01` field order.
    - Test 2: multi-sense definitions are rendered into one `Definitions` field with `<br>` separators and never contain `<ul>` or `<li>`.
    - Test 3: audio fields contain basename-only `[sound:filename.mp3]` references; `Image` is always `""`; GUIDs stay stable across mutable text changes.
    - Test 4: flagged text rows, missing lexical rows, or missing required audio fail with explicit diagnostics instead of silently producing broken export cards.
  </behavior>
  <action>Create `src/multilang/services/assemble_export_cards.py` as the deterministic Phase 5 assembly layer. It should read accepted text rows, matching lexical candidates, and successful audio assets, then emit export-card domain objects in exact field order: `SortIndex`, `word`, `Front of Card`, `IPA`, `Definitions`, `Example Sentence`, `Translation`, `word_audio`, `sentence_audio`, `Image`. Escape literal HTML-sensitive characters in text fields, but intentionally preserve `<br>` when joining multiple definitions for `CARD-04`. Convert persisted audio `storage_path` values into basename-only `[sound:...]` field values for `EXPT-03`. Use the stable identity helper from `src/multilang/domain/exporting.py` so mutable sentence/translation edits do not change the note GUID. Persist assembled snapshots through `ExportRepository` instead of returning ad-hoc dicts.</action>
  <acceptance_criteria>`tests/services/test_assemble_export_cards.py` proves field order, `<br>`-joined definitions, blank `Image`, basename-only sound tags, stable GUIDs, and explicit failures for missing required export inputs; `src/multilang/services/assemble_export_cards.py` contains the literal field names from `CARD-01`.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_assemble_export_cards.py -q</automated>
  </verify>
  <done>The project can freeze real accepted cards into deterministic export rows that already satisfy the fixed schema before serialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write UTF-8-safe CSV and TSV fallback bundles</name>
  <files>src/multilang/services/export_tabular_bundle.py, tests/services/test_export_tabular_bundle.py</files>
  <read_first>src/multilang/domain/exporting.py, src/multilang/services/assemble_export_cards.py</read_first>
  <behavior>
    - Test 1: TSV output includes Anki text-import headers for separator, HTML mode, note type, deck, and exact column names.
    - Test 2: CSV output stays UTF-8, quotes multiline/separator-heavy fields correctly, and preserves the fixed field order.
    - Test 3: non-Latin text, embedded commas, quotes, and `<br>` values round-trip through the serializer without mojibake or broken columns.
  </behavior>
  <action>Create `src/multilang/services/export_tabular_bundle.py` to serialize frozen export-card rows into both `.tsv` and `.csv` fallback artifacts using Python's `csv` module — not hand-rolled escaping. Always write UTF-8 text. Prefix the file with Anki-friendly headers: `#separator:<Tab or Comma>`, `#html:true`, `#notetype:<stable Multilang note type name>`, `#deck:<requested deck name>`, and `#columns:` followed by the exact `CARD-01` field order. Preserve `Translation` as a normal exported field value even though the Anki template will hide it on the front. Keep output deterministic by writing rows in stored `sort_index` order and by using the frozen export snapshots from Plan 01/Task 2.</action>
  <acceptance_criteria>`tests/services/test_export_tabular_bundle.py` asserts UTF-8 output, explicit Anki headers, exact column order, and correct quoting/escaping for multiline and non-Latin content; serializer code uses the stdlib `csv` module.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_export_tabular_bundle.py -q</automated>
  </verify>
  <done>Users have CSV and TSV fallback artifacts that keep the same card schema and import cleanly as UTF-8 plain text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted lexical/text/audio rows → export-card assembly | user-visible card fields are composed from mixed persisted sources |
| export-card rows → CSV/TSV files | structured card data crosses into plain-text artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | T | `src/multilang/services/assemble_export_cards.py` | mitigate | escape literal HTML-sensitive text while allowing intentional `<br>` joins only in `Definitions` |
| T-05-06 | T | audio field generation | mitigate | derive `[sound:...]` values from basename-only filenames so path tampering cannot leak subdirectory references into Anki fields |
| T-05-07 | I | CSV/TSV artifacts | mitigate | write UTF-8 with explicit headers and deterministic column order so import does not mis-map or drop fields |
| T-05-08 | D | export assembly prerequisites | mitigate | fail fast when required accepted text or synthesized audio is missing instead of producing broken fallback files |
</threat_model>

<verification>
- `uv run pytest tests/services/test_assemble_export_cards.py tests/services/test_export_tabular_bundle.py -q`
</verification>

<success_criteria>
- Accepted rows assemble into deterministic export cards with the exact fixed schema.
- `Definitions` uses one `<br>`-joined field and `Image` is empty on every exported card.
- CSV and TSV artifacts are UTF-8-safe and keep the same field order.
</success_criteria>

<output>
After completion, create `.planning/phases/05-anki-safe-export-contract/05-02-SUMMARY.md`
</output>
