---
phase: 33-grammar-and-personal-sources
plan: "10"
type: execute
wave: 4
runtime: opencode
assurance: unreviewed
depends_on: [32-03, 32-07, 33-01, 33-02, 33-03, 33-04, 33-05, 33-06, 33-08, 33-09, 33-18]
files-modified:
  - src/multilang/repositories/job_repository.py
  - src/multilang/services/phase33_jobs.py
  - src/multilang/services/generate_text_items.py
  - tests/repositories/test_job_repository.py
  - tests/services/test_phase33_jobs.py
  - tests/services/test_generate_text_items.py
autonomous: true
requirements: [KGRAM-01, KGRAM-02, KPERS-01, KPERS-02, GJOB-01, GREV-01]
upstream_blockers:
  - Do not execute until `.planning/phases/32-frequency-portuguese-text-and-audio/32-07-SUMMARY.md` exists and Plan 18 has produced `33-18-SUMMARY.md` for the shared text-generation/private-context join.
non_goals:
  - Do not add FastAPI/UI/listeners, call live providers, auto-accept review/media, auto-create bridge cards, expose private context without an exact capability, package/publish decks, or collapse outcomes into succeeded.
hard_boundaries:
  - The coordinator composes existing domain/repository ports and owns no lexical, grammar, privacy, review, or readiness truth of its own.
  - Job stage completion is derived from terminal statuses, orthogonal attempt/processed facts, explicit run denominators, and current exact AI-review/audio obligations; it cannot be forced or inferred from loop completion.
escalation_triggers:
  - Stop on a repository/domain version mismatch, missing active exact authority, systemic schema/config error, request for hidden provider fallback, inability to resume without duplicate effects, or any path that catches an exception and reports accepted/completed.
approval_gates:
  - Live/paid provider execution, production data mutation, real private imports, external authority creation, asset commit, activation, export delivery, and publication remain outside this autonomous plan; exact AI-policy review and automated/AI acoustic evidence are machine-evaluable.
anti_regression_targets:
  - Preserve existing generation commands/services, source modes, item ordering/GUID inputs, provider-boundary-only ko-KR, NFC/source identity, local-only private default, bounded retries, and Phase 32 text/audio behavior.
known_unknowns:
  - Production provider/reviewer/media bindings may be absent; coordinator must report blocked/review_required precisely rather than synthesize them.
no_ui_proof_rationale: Backend coordinator and repository integration only; CLI surface arrives in Plan 11.
high_leverage_surfaces:
  - src/multilang/repositories/job_repository.py
  - src/multilang/services/generate_text_items.py
  - src/multilang/services/phase33_jobs.py
second_pass_required: true
closure_claim_limit: Proves isolated persisted orchestration with deterministic fakes; it does not prove authorized live provider execution, production AI/audio evidence, CLI UX, export delivery, or learner-ready phase closure.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Existing coarse generate-all and success shortcuts cannot drive Phase 33 completion.
  kept: Repository-backed jobs, established text/audio adapters, typed service boundaries, and resumable deterministic processing.
  gained: One coordinator for grammar/custom/highlight work with per-item isolation and truthful readiness.
must_haves:
  truths:
    - Grammar, custom-list, and highlight items enter one persisted pipeline while retaining source-specific identity, order, privacy, graph, and review evidence.
    - One item-local failure records terminal status plus attempt/processed facts and processing continues for eligible siblings; systemic failures stop fail-closed.
    - Missing/stale AI linguistic consensus, translation, word audio, sentence audio, integrity, or acoustic evidence keeps the affected item review_required and the stage incomplete.
    - Reports distinguish attempted, processed, accepted, review_required, failed, skipped_current, and not_attempted with the exact run-partition/processed-subset/current-status relations; retry/resume does not duplicate revisions/events or re-invoke disclosing/disclosed/failed_unknown private calls.
    - Personal-source aggregation starts from the exact persisted custom/highlight eligible inventory IDs, source order, and recomputed roots; no processing status can add, drop, or reorder inventory.
    - Custom and highlight aggregation reports separate eligible card-bearing and ready counts; neither family can be treated as achieved when its eligible or ready count is zero.
    - Field regeneration dispatch is closed: definition and sentence/microexample use the text generator, translation uses the translation adapter, and word/sentence audio use role-specific audio ports.
  artifacts:
    - {path: src/multilang/services/phase33_jobs.py, provides: "Phase 33 source-specific orchestration over shared item outcome/review/media contracts"}
    - {path: src/multilang/repositories/job_repository.py, provides: "Terminal status, attempt/processed facts, denominator/event persistence, and exact resume queries"}
    - {path: src/multilang/services/generate_text_items.py, provides: "Compatible delegation for Phase 33 review-aware text work"}
  key_links:
    - {from: grammar/custom/highlight source records, to: per-item processing handlers, via: "explicit source dispatcher preserving source-specific contracts"}
    - {from: persisted revisions/AI review/media/status/attempt facts, to: stage aggregate, via: "repository reload plus exact denominator/completion predicate"}
---

# Phase 33 Plan 10: Persisted Multi-Source Job Coordination

<objective>
Join grammar, ordered custom lists, and private highlights into resumable jobs with granular persisted outcomes and exact review/media completion truth.

Purpose: Phase 33 must continue after item-local failures without hiding incomplete review, audio, privacy, or graph obligations.
Output: Extended job repository, source-aware coordinator, compatible text-generation delegation, and mixed-run/resume tests.
</objective>

## Anti-Goals
- No server/UI, live provider, auto-review/bridge, privacy bypass, package/publication, or coarse success reporting.

## Hard Boundaries
- The coordinator delegates all source truth and computes completion only from persisted exact outcome/review/media facts.

## Evidence Contract
- Repository race/replay, mixed-source, item/systemic failure, stale obligation, resume, legacy regression, and sentinel tests must pass.

## Common Pitfalls
- Duplicating domain logic in dispatch, using optimistic counters, broad-catching systemic errors, or regenerating already-complete effects.

## Stop-And-Challenge
- Stop on version mismatch, missing exact authority, unresumable effects, hidden fallback, or exception paths that report accepted/completed.

## Approval Gates
- Live/paid calls, production/private data, external authority creation, asset commit, activation, delivery, and publication remain outside this plan; optional human UAT is not an approval or closure gate.

## Leverage Review
- Lost: generate-all shortcut. Kept: typed adapters/repositories/jobs. Gained: source-aware resumable truthful orchestration.

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
@.agents/skills/llm-security/SKILL.md
</execution_context>

<context>
@.planning/phases/33-grammar-and-personal-sources/33-01-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-02-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-03-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-04-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-05-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-06-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-08-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-09-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-18-SUMMARY.md
@.planning/AI-LINGUISTIC-REVIEW-POLICY.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-03-SUMMARY.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-07-SUMMARY.md
@src/multilang/repositories/job_repository.py
@src/multilang/services/generate_text_items.py

<interfaces>
Use terminal-status/attempt/processed/denominator contracts from Plan 06, repositories from Plans 08/09, complete immutable review operations from Plan 05, and shared text-generation private-context wiring from Plan 18. The coordinator receives adapters/repositories through constructor injection. Private disclosure uses Plan 04's committed reservation, transaction-free callback, and separate finalization; replays of disclosing/disclosed/failed_unknown never call. Existing `GenerateTextItemsService.execute()` behavior remains stable.
</interfaces>
</context>

<checks><plan_check>
checker: self
checker_runtime: opencode
status: skipped
blocking: false
notes: Previous self-check was superseded by independent checker blockers; revised AI/status/disclosure orchestration awaits fresh independent recheck.
</plan_check></checks>

<tasks>
<task id="33-10-01" type="auto" tdd="true">
  <name>Persist granular outcomes and recompute truthful job aggregates</name>
  <files>
    - MODIFY: tests/repositories/test_job_repository.py
    - MODIFY: src/multilang/repositories/job_repository.py
  </files>
  <behavior>
    - Repository appends terminal-status events and separate immutable attempt/processed facts per exact job/item/attempt/idempotency command in deterministic order.
    - Exact retry returns prior facts; changed payload conflicts; concurrent writers create one attempt/status transition.
    - Aggregate counts reload from eligible sets, run dispositions, terminal statuses, attempted_at/processed_at, and current AI/audio obligations; callers cannot supply totals.
    - Reports distinguish all seven counts/ID sets: attempted/skipped_current/not_attempted partition eligible work, processed is a subset of attempted, and accepted/review_required/failed are an orthogonal current-terminal-status view.
    - Duplicate/deferred dispositions remain visible; private values and raw exceptions are absent.
    - Custom/highlight aggregate reads expose exact persisted eligible inventory IDs/order/root separately from processing status and disposition.
  </behavior>
  <action>Write PostgreSQL replay/concurrency/aggregate/denominator/private-sentinel tests first. Add methods for attempt/status facts, safe listing, skipped-current recording, resumable selection, and aggregate recomputation. For custom/highlight, load exact persisted eligible inventory from Plans 08/09, preserve source order, recompute/verify each inventory root, and join statuses/dispositions without filtering. `processed_at` follows only a controlled terminal result. Use one transaction per local transition; never update history or accept caller totals/inventory. Query safe IDs/hashes/reason codes and current AI/audio pointers only.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/repositories/test_job_repository.py -k 'granular or seven_denominators or personal_inventory_ids_order_root or no_inventory_drop or outcome or retry or concurrent or aggregate or duplicate or deferred or private' -q</automated></verify>
  <done>Persisted item truth and current obligations—not loop counters—drive exact resumable job state.</done>
</task>

<task id="33-10-02" type="auto" tdd="true">
  <name>Coordinate grammar, custom-list, and highlight items with isolation</name>
  <files>
    - CREATE: tests/services/test_phase33_jobs.py
    - CREATE: src/multilang/services/phase33_jobs.py
  </files>
  <behavior>
    - Grammar validates exact Phase 31/root/graph/source/AI-review/audio gates; custom lists preserve position/duplicate/bridge/defer; highlights preserve local identity/private authority/microexample separation.
    - Source handlers process deterministic eligible items, continue after item-local controlled exceptions, and stop on systemic authority/schema/config failures.
    - Generated fields append pending revisions; accepted requires exact current 2/3-pass consensus or exact integrity/acoustic evidence and graph readiness.
    - `start` selects only eligible items with no prior attempt/disposition; `resume` skips current accepted, retries only pending or explicitly retryable failures, leaves review_required unchanged, and never calls private items already disclosing/disclosed/failed_unknown.
  </behavior>
  <action>Create mixed-source and resume-matrix fakes first. Implement explicit `start|resume` mode in the thin coordinator. `start` selects untouched eligible IDs only. `resume` reloads persisted facts, records current accepted as skipped_current, retries only pending or failures carrying the closed explicit retryable reason/flag, does not reprocess review_required, and rejects callback invocation for disclosing/disclosed/failed_unknown. In both modes preserve deterministic source order, isolate controlled item failures, stop systemic authority/schema/config failures, enforce `max_items`, derive not_attempted, and recompute all seven denominators from persistence. Never treat attempted/processed as accepted.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_phase33_jobs.py -k 'grammar or custom or highlight or start_new_only or resume_persisted_facts or skip_current_accepted or retry_pending or explicitly_retryable_failure or leave_review_required or no_repeat_disclosing_disclosed_failed_unknown or seven_denominators or deterministic_order or isolation or systemic or bounded' -q</automated></verify>
  <done>All three source families run through one thin resumable coordinator while retaining their locked source-specific invariants.</done>
</task>

<task id="33-10-03" type="auto" tdd="true">
  <name>Dispatch all five regenerable fields through exact injected provider ports</name>
  <files>
    - MODIFY: tests/services/test_generate_text_items.py
    - MODIFY: src/multilang/services/generate_text_items.py
    - MODIFY: tests/services/test_phase33_jobs.py
    - MODIFY: src/multilang/services/phase33_jobs.py
  </files>
  <behavior>
    - Existing frequency/custom/Latin generation calls and result contracts retain their snapshots.
    - Closed dispatch maps `definition` and `sentence|microexample` to the exact text generator, `translation` to the exact translation adapter, and `word_audio|sentence_audio` to exact role-specific audio generation ports; unknown fields fail before any call.
    - Every provider/audio call occurs outside DB transactions. Audio dispatch first commits the exact publication reservation, then calls the recovery-capable port; text/translation output is validated before one pending candidate append.
    - Sentence candidate creation does not stale translation/sentence audio; approving that exact candidate delegates to Plan 05 to stale only prior-sentence-bound dependents.
    - Provider/validation exception affects one item, stores no raw/private payload, and leaves approved/unrelated fields untouched; missing adapter/authority is controlled review_required with no fallback.
  </behavior>
  <action>Lock legacy tests, then keep the closed dispatch table: text generator for definition/sentence/microexample, translation adapter for translation, and recovery-capable role audio ports. Text/translation calls run transaction-free and append validated pending candidates. Audio dispatch derives the unique safe item/revision/request-qualified final path, commits Plan 08 reservation first, then invokes the port outside every transaction; later finalization is a separate CAS. Resume routes existing reservation state to reconciliation rather than generating a new path/request. Private reservation/finalization remains separate. Candidate sentence creation emits no stale event; only approval invalidates exact dependents. Poison unintended adapters/fallbacks.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_generate_text_items.py tests/services/test_phase33_jobs.py -k 'definition_text_dispatch or translation_adapter_dispatch or word_audio_port_dispatch or sentence_audio_port_dispatch or audio_reservation_before_call or unique_revision_path or audio_resume_reconcile or external_call_outside_transaction or pending_candidate or approved_pointer_unchanged or sentence_candidate_no_stale or private_closed_no_call or no_fallback or eligible_custom_nonzero or eligible_highlight_nonzero or ready_counts_separate' -q</automated></verify>
  <done>Phase 33 gains revision-aware text orchestration without regressing or weakening prior generation paths.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01–SC-07 orchestration | All source families, isolation, review/media truth, regeneration | COVERED |
| REQ | All Phase 33 IDs | Source-aware persisted coordinator | COVERED |
| RESEARCH | Layered coordinator and outcome state machine | Thin dispatch after exact Phase 32 job-repository owner 32-03, text owner 32-07, and blocked Plan 18 private-context text-generation join; domain/repository delegation and resume | COVERED |
| CONTEXT | Exact start/resume; non-vacuous custom/highlight counts; five-field dispatch; reservation-first recoverable audio; no fallback/auto bridge; at-most-once privacy | Locked decisions preserved | COVERED |
</source_audit>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-10-01 | Elevation / Tampering | coordinator readiness | mitigate | Derive from exact graph/AI/audio obligations and denominator facts; no force-complete or processed status. |
| T-33-10-02 | Denial of Service | per-item processing | mitigate | Item isolation, systemic prevalidation, bounded retries, deterministic resume. |
| T-33-10-03 | Information Disclosure / Tampering | text generation | mitigate | Exact private broker, prompt-injection-safe structured output, content-free failures, and no authority from model text. |
</threat_model>

<high_leverage_review>Second pass: trace a mixed job containing accepted, duplicate, deferred, review-required, private-authorized, private-denied, item-failed, and stale-audio items through resume and aggregate completion.</high_leverage_review>
<verification>Run all task commands against isolated PostgreSQL with deterministic fakes, fake clocks, poisoned live adapters, and private/exception sentinels.</verification>
<success_criteria>
- Three source families preserve their exact invariants in one resumable coordinator.
- Item-local failures continue; systemic failures stop.
- AI-review/audio debt remains incomplete and visible; optional human UAT is non-authoritative.
- Retry/resume preserves at-most-once private disclosure and existing generation behavior.
</success_criteria>
<output>After completion, create `.planning/phases/33-grammar-and-personal-sources/33-10-SUMMARY.md` with coordinator graph, mixed-run outcome table, resume/idempotency trace, legacy regression evidence, sentinel scan, and bounded claim.</output>
