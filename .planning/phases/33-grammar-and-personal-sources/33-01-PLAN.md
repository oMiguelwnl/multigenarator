---
phase: 33-grammar-and-personal-sources
plan: "01"
type: execute
wave: 1
runtime: opencode
assurance: unreviewed
depends_on: []
files-modified:
  - src/multilang/domain/korean_grammar.py
  - src/multilang/services/korean_grammar.py
  - tests/domain/test_korean_grammar.py
  - tests/services/test_korean_grammar.py
autonomous: true
requirements: [KGRAM-01, KGRAM-02]
non_goals:
  - Do not create or commit production grammar/bootstrap content, mutate Phase 31, infer Frequency Level 1 mastery, call a provider, synthesize media, or activate/publish a deck.
hard_boundaries:
  - Resolve the canonical active approved Phase 31 snapshot exactly once per operation and import immutable concept/hash authority without copying or editing it.
  - Synthetic fixtures prove contracts and refusal paths only; they cannot satisfy source/license authority, exact AI-policy linguistic review, audio integrity/acoustic review, or learner-ready gates.
escalation_triggers:
  - Stop on inactive/candidate/history/test Phase 31 input, pointer drift, missing exact source authority, concept collision, graph invalidity, or any request to override deterministic strict-i+1 truth.
approval_gates:
  - Learner-ready grammar requires exact source/version/license authority, Phase 31 activation, `multilang-ai-linguistic-review-v1` consensus, and exact audio integrity/acoustic evidence; this plan grants no external legal/provider/TTS/database/asset power.
anti_regression_targets:
  - Preserve canonical ko, provider-only ko-KR, NFC, Phase 30 identity/morphology, imported Phase 31 objects, existing layouts/fields/GUIDs, and blank Image.
known_unknowns:
  - The exact production grammar inventory, smallest reviewed bootstrap entries, and approved audio/review bindings remain external gated inputs.
no_ui_proof_rationale: CLI/domain contracts and prepared grammar records only; Phase 34 owns observed Anki rendering/playback evidence.
high_leverage_surfaces:
  - src/multilang/services/korean_grammar.py
second_pass_required: true
closure_claim_limit: Proves immutable bundle and strict graph machinery plus fail-closed readiness only; no synthetic or merely serialized record may support a learner-ready grammar claim.
parallelism_budget:
  max_concurrent_plans: 6
  safe_parallelism: [33-02, 33-03, 33-04, 33-05, 33-06]
leverage:
  lost: Grammar cannot proceed from a convenient mutable pointer or silently assumed vocabulary.
  kept: Phase 30 concept contracts and the canonical Phase 31 snapshot boundary.
  gained: One hash-bound additive grammar authority with non-overridable strict-i+1 evidence.
must_haves:
  truths:
    - A grammar operation imports one exact active approved Phase 31 known-state root and never requires Frequency Level 1 or standalone assumptions.
    - The ordered learner-visible bootstrap contains only source-backed lexical prerequisites required by reviewed grammar examples.
    - Every strict grammar card recomputes exactly one unknown form-function-register construction and graph invalidity cannot be approved away.
    - Production readiness remains blocked until source/version/license authority, exact AI-policy consensus, and exact audio integrity/acoustic bindings all pass.
  artifacts:
    - {path: src/multilang/domain/korean_grammar.py, provides: "Frozen grammar bundle, bootstrap, entry, binding, and readiness contracts"}
    - {path: src/multilang/services/korean_grammar.py, provides: "Single-resolution Phase 31 join, immutable overlay validation, and strict graph recomputation"}
  key_links:
    - {from: resolve_active_korean_foundation_snapshot, to: KoreanGrammarBundle phase31_binding, via: "one resolver call plus exact bundle/receipt/manifest/root/registry hashes"}
    - {from: imported known root and ordered bootstrap, to: grammar entry approval eligibility, via: "recomputed observed-minus-known equality to exactly the target construction"}
---

# Phase 33 Plan 01: Immutable Grammar Bundle and Strict Concept Graph

<objective>
Create the versioned Korean grammar/bootstrap contracts and deterministic strict-i+1 validator over the exact active Phase 31 root.

Purpose: KGRAM-01 and KGRAM-02 require a real dependency graph and reviewed learner-visible prerequisites, not a standalone list or reviewer-overridable label.
Output: Frozen bundle models, exact dependency resolver, strict graph/readiness service, and focused mutation tests.
</objective>

## Anti-Goals
- No production content asset, frequency prerequisite, standalone grammar, provider/media call, Phase 31 mutation, activation, or publication.

## Hard Boundaries
- One exact active Phase 31 resolution feeds an immutable additive overlay; deterministic graph failure has no review override.

## Evidence Contract
- Focused contract/graph mutation tests must prove resolver call count, hash closure, exactly-one unknown, and independently blocked production gates.

## Common Pitfalls
- Treating broad progression labels as atomic card targets, admitting bootstrap words as pre-known, or letting serialized evidence outrank recomputation.

## Stop-And-Challenge
- Stop on missing/non-active/drifted Phase 31 authority, concept collision, unresolved source/license/review/media input, or requested graph bypass.

## Approval Gates
- Real source/license authority, exact AI review consensus, and deterministic/AI acoustic media evidence must exist independently before learner-ready status; optional human UAT is never a closure gate.

## Leverage Review
- Lost: mutable/convenient root selection. Kept: Phase 30/31 contracts. Gained: hash-bound strict grammar authority.

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>

<context>
@.planning/phases/33-grammar-and-personal-sources/33-APPROACH.md
@.planning/phases/33-grammar-and-personal-sources/33-PATTERNS.md
@.planning/research/33-RESEARCH.md
@.planning/AI-LINGUISTIC-REVIEW-POLICY.md
@.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-32-PLAN.md
@src/multilang/domain/korean.py
@src/multilang/services/korean_curriculum.py
@src/multilang/services/korean_foundation_snapshot.py

<interfaces>
Use existing `KoreanConcept`, `KoreanCurriculumEvidence`, `canonicalize_korean`, and `resolve_active_korean_foundation_snapshot()`. The resolved Phase 31 object supplies bundle, receipt, snapshot-manifest, snapshot-root, and exact member hashes. New contracts must remain frozen Pydantic models with `extra="forbid"`, hidden inputs, ordered tuples, lowercase SHA-256 fields, canonical UTF-8 JSON hashing, and NFC Korean.
</interfaces>
</context>

<checks><plan_check>
checker: self
checker_runtime: opencode
status: skipped
blocking: false
notes: Previous self-check was superseded by independent checker blockers; revised artifact awaits fresh independent recheck.
</plan_check></checks>

<tasks>
<task id="33-01-01" type="auto" tdd="true">
  <name>Define frozen grammar bundle, bootstrap, entry, and authority contracts</name>
  <files>
    - CREATE: tests/domain/test_korean_grammar.py
    - CREATE: src/multilang/domain/korean_grammar.py
  </files>
  <behavior>
    - Phase 31 bindings require exact bundle, receipt, snapshot manifest/root, and concept-registry member hashes.
    - Bootstrap records target one source-backed lexicon concept and remain learner-visible rather than pre-known.
    - Grammar entries keep form, function, attachment/allomorph rule, register, example, Portuguese translation, pronunciation/spoken sample, provenance, graph evidence, exact AI-review binding, and media binding separate.
    - Candidate/synthetic, missing-license, missing-review, missing-media, wrong-language, non-NFC, unknown-field, mutable, or hash-drifted payloads cannot be learner-ready.
  </behavior>
  <action>Write the failing contract/mutation tests first and confirm they fail because the module is absent. Then implement `Phase31GrammarRootBinding`, source/license and exact consumed Phase 32 lexical bindings, ordered bootstrap and grammar concept records, structured learner fields, `multilang-ai-linguistic-review-v1` evidence bindings, media integrity/acoustic bindings, bundle member declarations, root/member hashes, and a controlled readiness result. AI evidence records actor_type=`ai_model`, is_human=false, canonical policy/provider/model/route/prompt/schema/source/candidate/analyzer/curriculum/media hashes, deterministic validator results, fresh-context pass IDs, and consensus status. Per APPROACH §Grammar Authority, never embed the Phase 31 registry, infer vocabulary from Frequency Level 1 membership, require unrelated Korean frequency licensing, or let synthetic fixtures carry production authority/review/media status. Model G0-G13 as ordered category metadata while each actual entry names one stable atomic construction target.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/domain/test_korean_grammar.py -k 'frozen or binding or bootstrap or structured_fields or hash or production_gate or synthetic or nfc' -q</automated></verify>
  <done>All bundle members are immutable, bounded, canonically hashable, structurally separate, and incapable of promoting unauthoritative content.</done>
</task>

<task id="33-01-02" type="auto" tdd="true">
  <name>Resolve the active Phase 31 root once and validate the additive overlay</name>
  <files>
    - CREATE: tests/services/test_korean_grammar.py
    - CREATE: src/multilang/services/korean_grammar.py
  </files>
  <behavior>
    - One operation invokes the active snapshot resolver exactly once and binds immutable returned members/hashes.
    - Candidate, history-only, request-only, test, inactive, stale-pointer, second-read, or hash-drifted snapshots fail closed.
    - Overlay IDs cannot collide with imported IDs; every prerequisite exists, precedes its dependent, is acyclic, and has complete closure.
    - Imported Phase 31 concepts are never mutated.
  </behavior>
  <action>Write resolver-call-count and graph mutation tests before implementation. Build a service that accepts an injected resolver for tests and defaults to `resolve_active_korean_foundation_snapshot`; immediately convert its one result into the immutable root binding and pass that object through all later validation. Validate imported member identity and the additive lexicon/grammar overlay with `TopologicalSorter`, explicit collision/existence/forward-edge/closure checks, and canonical root rehashing. Per the locked exact-root decision, reject any attempt to reread `current-candidate`, substitute a fixture for production, or copy/update an imported concept.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_grammar.py -k 'resolve_once or active or candidate or history or drift or collision or cycle or closure or imported_immutable' -q</automated></verify>
  <done>The grammar service has one fail-closed production entry point and a closed immutable additive graph over exact Phase 31 authority.</done>
</task>

<task id="33-01-03" type="auto" tdd="true">
  <name>Recompute strict grammar evidence and enforce non-overridable readiness</name>
  <files>
    - MODIFY: tests/services/test_korean_grammar.py
    - MODIFY: src/multilang/services/korean_grammar.py
  </files>
  <behavior>
    - Known state begins with imported approved Phase 31 concepts, then admits each validated learner-visible bootstrap target, then earlier grammar targets.
    - Every grammar card uses policy strict, observes its target, declares only preceding prerequisites, and recomputes unknowns exactly as `(target_concept_id,)`.
    - Hidden lexical/orthographic/phonological/morphological/attachment/allomorph/register concepts, repeated targets, broad targets, mixed unlabelled speech levels, and serialized/recomputed disagreement block.
    - AI linguistic approval cannot override deterministic graph failure; readiness additionally requires exact source/license, policy, consensus, and media hashes.
  </behavior>
  <action>Begin with failing exactly-one-unknown and mutation-table tests. Implement ordered bootstrap admission and grammar recomputation using `known_before`, not serialized unknown claims. Require every learner-observed concept to exist and make construction atomicity explicit; broad category labels are never card targets. Keep default examples in modern Standard-Seoul `해요체` unless the target explicitly teaches another labelled register. Bind standard linguistic gates to two fresh-context passes and critical strict-i+1, P11-P13, pronunciation-rule, morphology-sensitive, homograph, register-exception, or validator-flagged gates to three passes. Any deterministic failure, missing pass, disagreement, or uncertainty blocks and cannot be majority-overridden. Produce controlled reason codes and content-free errors. A valid synthetic graph may pass structural validation but must still return blocked production readiness.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/domain/test_korean_grammar.py tests/services/test_korean_grammar.py -k 'strict or exactly_one or hidden or repeated or broad or register or serialized or review_cannot_override or readiness' -q</automated></verify>
  <done>Strict grammar truth is recomputed over the exact root and ordered prelude, with one construction unknown and independent production gates.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-02 | Reviewed progression and exactly-one construction | COVERED |
| REQ | KGRAM-01 / KGRAM-02 | Bundle fields, progression, exact root, strict graph | COVERED |
| RESEARCH | Grammar bundle / strict algorithm / AI-policy production gates | Immutable members, recomputation, 2/3-pass consensus, fail-closed readiness | COVERED |
| CONTEXT | Exact Phase 31 root; smallest learner-visible bootstrap; no Level 1/standalone; no graph override | Locked decisions preserved | COVERED |
</source_audit>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-01-01 | Spoofing / Tampering | Phase 31 and source bindings | mitigate | Resolve canonical active authority once and rehash every immutable member/root. |
| T-33-01-02 | Tampering | strict curriculum evidence | mitigate | Recompute unknowns and reject hidden/repeated/broad targets; approval has no override path. |
| T-33-01-03 | Elevation | learner-ready status | mitigate | Separate source/license/review/media gates; synthetic fixtures can never set production readiness. |
</threat_model>

<high_leverage_review>Second pass: trace one imported concept, one bootstrap lexeme, and one grammar target through hashes, known-state admission, observed evidence, exact unknown recomputation, and blocked/ready gate derivation.</high_leverage_review>
<verification>Run all three task commands offline with provider/network/asset writers poisoned by test fixtures.</verification>
<success_criteria>
- The exact active Phase 31 root is imported once and remains immutable.
- The grammar-owned bootstrap is learner-visible, source-backed, bounded by actual example prerequisites, and independent of Frequency Level 1.
- Every strict grammar entry has exactly one recomputed construction unknown and no review override.
- Missing production authority or exact AI/audio evidence yields a controlled blocked result rather than fabricated learner-ready content.
</success_criteria>
<output>After completion, create `.planning/phases/33-grammar-and-personal-sources/33-01-SUMMARY.md` with contract inventory, root-binding matrix, graph mutation results, production gates, and bounded claim.</output>
