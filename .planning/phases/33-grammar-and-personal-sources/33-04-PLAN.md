---
phase: 33-grammar-and-personal-sources
plan: "04"
type: execute
wave: 1
runtime: opencode
assurance: unreviewed
depends_on: []
files-modified:
  - src/multilang/domain/private_processing.py
  - src/multilang/services/private_context.py
  - tests/domain/test_private_processing.py
  - tests/services/test_private_context.py
autonomous: true
requirements: [KPERS-02]
non_goals:
  - Do not grant provider authority at job/source/account scope, send an entire excerpt by default, log/store prompts or payloads, permit provider text to determine identity/approval, call a live/paid provider, or modify `src/multilang/services/text_generation.py` before Phase 32 Plan 07 lands.
hard_boundaries:
  - Remote private processing defaults to denied and requires an exact per-run capability bound to item, immutable excerpt revision/hash, provider/model/route, purpose, policy hash, budget, and provider idempotency capability.
  - Offline broker tests use injected fake callbacks only; actual text-generation provider-boundary wiring is moved to Plan 18 and remains blocked on `32-07-SUMMARY.md`.
escalation_triggers:
  - Stop on wildcard authority, stale revision/hash, provider/model/route/purpose mismatch, capability replay, context over budget, a transaction-held provider call, unsafe retry, inability to prove zero adapter calls on refusal, or any prompt/payload persistence request.
approval_gates:
  - An externally authorized local action must mint the exact run capability later wired by Plan 10; absence/mismatch refuses, and `disclosing|disclosed|failed_unknown` replays never invoke the injected callback. Shared `text_generation.py` wiring is not executable until Plan 18 after Phase 32 Plan 07.
anti_regression_targets:
  - Preserve local-only default highlights, provider-boundary-only ko-KR, NFC, fail-closed Korean identity, private-value-free diagnostics, and all existing text-generation behavior by leaving shared wiring to Plan 18.
known_unknowns:
  - Provider tokenizers may impose stricter limits, but the locked local disclosure ceiling is always `max_context_tokens <= 24` under `phase33-private-token-v1`; code-point and serialized-byte ceilings are additional defenses.
no_ui_proof_rationale: Backend/CLI authority and disclosure boundary only; no visible UI claim.
high_leverage_surfaces:
  - src/multilang/services/private_context.py
second_pass_required: true
closure_claim_limit: Proves exact authority refusal and bounded fake-callback disclosure; it does not prove text-generation integration, provider privacy terms, live provider behavior, microexample quality, or publication safety.
parallelism_budget:
  max_concurrent_plans: 6
  safe_parallelism: [33-01, 33-02, 33-03, 33-05, 33-06]
leverage:
  lost: Highlight context cannot flow through the ordinary provider path implicitly.
  kept: Existing typed text-generation adapter and local Korean sanitization boundary.
  gained: Single-use least-privilege capabilities with an explicit at-most-once external-effect state machine.
must_haves:
  truths:
    - No private excerpt context reaches any remote adapter unless an exact per-run capability matches the item, immutable revision/hash, provider/model, purpose, policy, and budget.
    - A valid capability reveals only deterministic bounded context around the locally resolved target and never more than 24 `phase33-private-token-v1` tokens; byte/code-point ceilings remain additional defenses.
    - Missing, stale, mismatched, over-budget, replayed, or malformed authority yields a controlled refusal and zero provider calls; unknown external results become terminal failed_unknown without automatic retry.
    - Prompt-like private text remains quoted data and cannot set identity, source authority, policy, review state, or export fields.
  artifacts:
    - {path: src/multilang/domain/private_processing.py, provides: "Exact capability, pending/disclosing/disclosed/failed_unknown states, receipt, and refusal contracts"}
    - {path: src/multilang/services/private_context.py, provides: "Local context derivation and transaction-free at-most-once provider state machine"}
  key_links:
    - {from: immutable excerpt revision plus target span, to: bounded provider context, via: "local policy derivation before adapter construction/call"}
    - {from: PrivateProcessingCapability, to: injected adapter callback, via: "commit pending-to-disclosing CAS, call outside transaction, finalize disclosed or failed_unknown"}
---

# Phase 33 Plan 04: Exact Private-Context Authority and Offline Broker Contracts

<objective>
Require exact one-run authority before bounded private highlight context can reach an injected fake callback. Shared text-generation wiring is split to Plan 18 after Phase 32 Plan 07.

Purpose: KPERS-02 permits useful generated microexamples without turning private reading excerpts into ambient provider context.
Output: Least-privilege capability contracts, local context broker, and zero-call/privacy tests that are executable without missing Phase 31/32 summaries.
</objective>

## Anti-Goals
- No broad consent flag, ambient provider context, prompt/payload persistence, identity/approval from model text, live provider call, or `text_generation.py` shared-file edit.

## Hard Boundaries
- Exact one-run capability validation, local target-centered bounds, and committed `pending -> disclosing` CAS occur before the adapter call; the call runs outside every database transaction.

## Evidence Contract
- Fake-adapter call counts, reservation/finalization crash tables, replay/concurrency tests, provider-idempotency branches, budget tests, exact-copy rejection, and private-sentinel scans must pass.

## Common Pitfalls
- Treating `allow_private=true` as authority, holding a transaction around a provider callback, auto-retrying an unknown result without provider idempotency, or logging content while building a safe receipt.

## Stop-And-Challenge
- Stop on wildcard/stale authority, binding mismatch, over-budget context, `disclosing|disclosed|failed_unknown` replay, unsafe retry, or inability to prove zero calls on refusal.

## Approval Gates
- A later explicit local command must mint exact authority; no plan or job-level inheritance is permitted. Plan 18 owns the blocked Phase 32 text-generation join.

## Leverage Review
- Lost: implicit context flow and exactly-once assumptions. Kept: typed adapter/sanitizer. Gained: auditable single-use at-most-once disclosure.

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
@.agents/skills/llm-security/SKILL.md
</execution_context>

<context>
@.planning/phases/33-grammar-and-personal-sources/33-APPROACH.md
@.planning/phases/33-grammar-and-personal-sources/33-PATTERNS.md
@.planning/research/33-RESEARCH.md
@src/multilang/domain/highlights.py

<interfaces>
Make disclosure eligibility an explicit offline broker boundary before any provider adapter can be called. New authority must be an unforgeable random capability ID stored/consumed locally and bound to hashes, never a caller-supplied boolean such as `allow_private=True`. The `text_generation.py` defense-in-depth sanitizer is reused later by Plan 18 after `32-07-SUMMARY.md` exists.
</interfaces>
</context>

<checks><plan_check>
checker: self
checker_runtime: opencode
status: skipped
blocking: false
notes: Previous self-check was superseded by independent checker blockers; revised at-most-once design awaits fresh independent recheck.
</plan_check></checks>

<tasks>
<task id="33-04-01" type="auto" tdd="true">
  <name>Define exact one-run private-processing capabilities and policies</name>
  <files>
    - CREATE: tests/domain/test_private_processing.py
    - CREATE: src/multilang/domain/private_processing.py
  </files>
  <behavior>
    - Capability binds random capability ID, job/run/item IDs, exact excerpt revision/content hash, provider/model/route, purpose, policy version/hash, `tokenization_rule_id=phase33-private-token-v1`, `max_context_tokens &lt;= 24`, byte/code-point/call/cost budgets, exact provider idempotency support/key, issued/expiry state, and issuer intent evidence without private values.
    - No wildcard, source-wide, job-wide, provider-any, model-any, or inherited authority validates.
    - Capability/rejection serialization and validation errors reveal hashes/controlled reason codes only.
    - Unknown fields, hidden-input leakage, mutable state, invalid hashes, an unknown tokenization rule, `max_context_tokens` outside 1..24, and oversized byte/code-point policy bounds fail closed.
  </behavior>
  <action>Write failing Pydantic contract and mutation tests first. Implement frozen strict models for the derivation policy, exact capability, controlled refusal, disclosure attempt, content-free receipt, and closed state `pending|disclosing|disclosed|failed_unknown`. Define the documented deterministic counter `phase33-private-token-v1`: NFC-normalize; each maximal run of Unicode Letter/Number/Mark code points is one token; each non-whitespace punctuation/symbol code point is one token; whitespace/control separators add no token. Require `1 &lt;= max_context_tokens &lt;= 24` and persist the rule ID plus actual token count; byte and code-point limits are additional independent maxima. State transitions carry immutable expected-version input/output for compare-and-set persistence. Record exact route idempotency support/key. Do not accept booleans, broad grants, alternate token counters, or closed-state reset.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/domain/test_private_processing.py -k 'exact or wildcard or provider or purpose or budget or token_v1 or token_cap_24 or unicode_count or consumed or private or hidden or frozen' -q</automated></verify>
  <done>Private processing has a narrow immutable authority contract that cannot silently broaden disclosure.</done>
</task>

<task id="33-04-02" type="auto" tdd="true">
  <name>Derive bounded local context and verify capability before disclosure</name>
  <files>
    - CREATE: tests/services/test_private_context.py
    - CREATE: src/multilang/services/private_context.py
  </files>
  <behavior>
    - Context derivation starts from the exact immutable excerpt and verified target span/identity, normalizes NFC, and returns a deterministic target-centered window, `phase33-private-token-v1` count, and hash with count at most both the capability limit and 24.
    - Invalid span, absent target, stale excerpt hash, over-budget context, authority mismatch/expiry/replay, and injected authority metadata refuse before any adapter callback.
    - Broker commits `pending -> disclosing` by CAS and closes that transaction before invoking the provider callback.
    - Known success finalizes `disclosed` with a content-free receipt; timeout/unknown without provider idempotency finalizes `failed_unknown` and performs zero automatic retries.
    - If and only if the exact provider route supports idempotency, bounded retry uses the identical provider idempotency key; no new key or widened authority is permitted.
    - Replay of disclosing, disclosed, or failed_unknown returns controlled inspect-required/prior-receipt state and never invokes the callback.
    - Receipt records hashes/counts/provider/model/purpose/policy only, never context, excerpt, path, prompt, or payload.
  </behavior>
  <action>Write capability, Unicode token-count golden, 24/25-token boundary, transition, crash, timeout, and idempotency tests before code. Implement a broker with injected capability store and adapter callback so tests assert transaction boundaries and call count. Derive context locally with explicit target-centered sentence/window boundaries, count only with `phase33-private-token-v1`, and enforce capability token limit, hard 24-token ceiling, code-point ceiling, and UTF-8 byte ceiling independently before reservation. Verify every binding, commit exact `pending -> disclosing` CAS, close the transaction, then invoke the provider. Finalize success/unknown in new transactions; non-idempotent unknown gets zero retry, exact-idempotency retry reuses only the identical key. Closed-state replays never call. Never promise exactly-once effects or hold a transaction around the callback.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_private_context.py -k 'bounded or token_v1 or token_cap_24 or token_25_refused or unicode or target or stale or mismatch or expiry or replay or cas or receipt or zero_call or injection' -q</automated></verify>
  <done>Only exact authorized bounded context can cross the callback boundary under a transaction-free at-most-once state machine with content-free evidence and explicit unknown-result handling.</done>
</task>

</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-05 | Explicit source/context/microexample separation and privacy | COVERED |
| REQ | KPERS-02 | Exact per-run remote-processing authority and local default | COVERED |
| RESEARCH | Private-context capability / prompt-injection boundary | Exact bindings, bounded local derivation, transaction-free at-most-once disclosure, zero-call replay; `text_generation.py` join moved to blocked Plan 18 | COVERED |
| CONTEXT | Remote context requires exact per-run authority; `max_context_tokens <= 24`; no copied private values | Locked token cap and privacy decisions preserved | COVERED |
</source_audit>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-04-01 | Elevation | processing authority | mitigate | Exact single-use bindings, committed reservation, closed transitions, and no boolean/wildcard grant. |
| T-33-04-02 | Information Disclosure | provider context | mitigate | `phase33-private-token-v1` hard 24-token cap plus independent byte/code-point bounds and content-free receipts/errors. |
| T-33-04-03 | Tampering / Elevation | private excerpt and callback output | mitigate | Quote prompt-injection content as untrusted data; callback output cannot set identity/policy/approval and exact-copy output blocks review. |
</threat_model>

<high_leverage_review>Second pass: prove deny/replay states call no callback, reservation commits before the transaction-free callback, unknown non-idempotent results stop at failed_unknown, and no private value survives logs/errors/receipts/output.</high_leverage_review>
<verification>Run all task commands with fake callbacks and no shared text-generation file edits; scan captured requests and serialized errors for private sentinels.</verification>
<success_criteria>
- Private remote processing is denied by default and exact per run.
- Allowed disclosure is locally bounded and at-most-once; unknown non-idempotent results become failed_unknown with no automatic retry.
- Prompt-like source text and callback output cannot change authority, identity, or approval.
- Existing non-private generation behavior remains unchanged because the shared boundary is untouched until Plan 18.
</success_criteria>
<output>After completion, create `.planning/phases/33-grammar-and-personal-sources/33-04-SUMMARY.md` with offline authority matrix, zero-call callback evidence, disclosure bounds, sentinel scan, and bounded claim.</output>
