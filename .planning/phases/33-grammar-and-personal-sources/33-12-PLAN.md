---
phase: 33-grammar-and-personal-sources
plan: "12"
type: execute
wave: 12
runtime: opencode
assurance: self_checked
coverage_mode: enabling_checkpoint_not_phase_closure
depends_on: [32-49, 33-11, 33-18]
files-modified:
  - .planning/phases/33-grammar-and-personal-sources/evidence-inbox/phase33-authority-preflight.json
  - .planning/phases/33-grammar-and-personal-sources/evidence-inbox/phase33-authority-preflight.json.sha256
  - .planning/phases/33-grammar-and-personal-sources/33-12-SUMMARY.md
autonomous: false
requirements: [KGRAM-01, KGRAM-02, KPERS-01, KPERS-02, GJOB-01, GREV-01]
non_goals:
  - Do not acquire source bytes, create grammar/personal cards, call providers, call Azure, synthesize audio, migrate a database, create audio roots, publish assets, export APKG/CSV/TSV, release, deliver, commit, or close Phase 33.
hard_boundaries:
  - This plan only reconciles the authority checkpoint around the current Plan 32-49 Korean voice-profile artifact and the implemented narrow Phase 33 CLI preflight contract.
  - The only allowed profile input is `.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/korean-voice-profile.json` with profile SHA-256 `5000e49386404e18e7501eec99ef31a4d32a0131484ced36acbef61d033681ce` and validation SHA-256 `87eeb00a30d4c3e68b57872c2ec238107b6d55157d72ee8bbf704b529668eaf7`.
  - The prior `approved-audio-profile.json` and `32-30-SUMMARY.md` audio-profile references are stale for this checkpoint; do not execute downstream Plans 13-17 until they are separately reconciled against Plan 32-49 and this plan's summary.
escalation_triggers:
  - Stop on any request to use a different Korean profile, use `approved-audio-profile.json`, infer audio authority from catalog/profile evidence, approve empty custom or highlight sources, expose private/custom values, use a production target, omit any of the four audio root identities, widen output paths, or perform side effects.
approval_gates:
  - A user/operator must explicitly approve or deny the exact hash/count/path values before the preflight command runs.
  - Any provider spend, Azure synthesis, private-context disclosure, local migration, asset write, or publication action requires a later separate authority and execution plan.
anti_regression_targets:
  - Korean remains absent from the static audio voice registry; `select_voice(SupportedLanguage.KO)` must continue to fail until a separate activation plan.
  - Phase 33 status/process/review scaffolding remains content-free and privacy-safe.
  - Custom and highlight source evidence remains IDs/hashes/counts only, never submitted words, excerpts, private paths, titles, prompts, completions, provider payloads, credentials, or audio bytes.
known_unknowns:
  - Exact legal/source rights, local target locator/prestate, custom and highlight safe IDs, provider route/budgets, TTS budgets, private-processing authority, and downstream production command contracts remain unresolved until the user/operator supplies them.
  - Plans 13-17 still contain stale references and must be replanned or patched after this checkpoint before execution.
no_ui_proof_rationale: Planning and CLI/evidence checkpoint only; no user-visible UI or Anki rendering claim.
high_leverage_surfaces:
  - .planning/phases/33-grammar-and-personal-sources/33-12-PLAN.md
  - .planning/phases/33-grammar-and-personal-sources/evidence-inbox/phase33-authority-preflight.json
second_pass_required: true
closure_claim_limit: This enabling checkpoint may claim only that exact non-secret Phase 33 preflight inputs were accepted or denied with zero side effects; it cannot claim Phase 33 requirement completion, authority, production readiness, text/audio quality, export readiness, publication, delivery, or Phase 33 completion.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: The broad old Plan 12 authority bundle is not executable as written and downstream production plans must wait for reconciliation.
  kept: Existing Plan 11 CLI/authority scaffolding, Plan 18 private-context wiring, Phase 32 Plan 49 profile artifact, and Phase 34 ownership of export/render/playback proof.
  gained: A small executable checkpoint that prevents stale profile references and side-effect authority from being inferred.
must_haves:
  truths:
    - Phase 33 authority preflight consumes the current Plan 32-49 Korean profile SHA, not stale `approved-audio-profile.json` references.
    - Custom and highlight inputs are non-vacuous by safe IDs and counts only.
    - The local migration target is represented only by hash/prestate identities for revision `20260828_19` and grants no mutation.
    - The four future audio roots are bound by path identities without creating roots or granting synthesis.
    - Missing or denied values block downstream production work instead of permitting fallback execution.
  artifacts:
    - path: .planning/phases/33-grammar-and-personal-sources/evidence-inbox/phase33-authority-preflight.json
      provides: Side-effect-free safe inventory and option binding for Phase 33 downstream authority work
    - path: .planning/phases/33-grammar-and-personal-sources/evidence-inbox/phase33-authority-preflight.json.sha256
      provides: Whole-file SHA-256 binding for the preflight artifact
    - path: .planning/phases/33-grammar-and-personal-sources/33-12-SUMMARY.md
      provides: Checkpoint result, denials if any, zero-side-effect proof, and downstream replan blockers
  key_links:
    - from: .planning/phases/32-frequency-portuguese-text-and-audio/32-49-SUMMARY.md
      to: phase33 authority preflight
      via: exact profile SHA `5000e49386404e18e7501eec99ef31a4d32a0131484ced36acbef61d033681ce`
    - from: src/multilang/cli.py phase33 authority-preflight
      to: phase33-authority-preflight.json
      via: implemented narrow hash/count/path CLI contract
    - from: phase33-authority-preflight.json
      to: future Plans 13-17 reconciliation
      via: safe IDs, hashes, counts, root identities, and explicit no-side-effect summary
---

# Phase 33 Plan 12: Reconciled Authority Preflight Checkpoint

## Objective
Replace the stale Phase 33 Plan 12 authority checkpoint with an executable, side-effect-free checkpoint that consumes the current Phase 32 Plan 49 Korean voice-profile artifact and the implemented narrow Phase 33 CLI preflight contract.

## Context
- `.planning/phases/32-frequency-portuguese-text-and-audio/32-49-SUMMARY.md` selected `ko-KR-SunHi:DragonHDLatestNeural` and created `korean-voice-profile.json` plus `korean-voice-profile-validation.json` without Azure synthesis, fallback, registry activation, review, export, release, publication, or delivery authority.
- `.planning/phases/33-grammar-and-personal-sources/33-11-SUMMARY.md` added content-free Phase 33 CLI/status/review/authority scaffolding only.
- `.planning/phases/33-grammar-and-personal-sources/33-18-SUMMARY.md` wired private-context gating into text generation with fake adapters only and did not grant production authority.
- `src/multilang/cli.py` currently exposes `phase33 authority-preflight` with `--job-id`, five SHA inputs, repeated `--output-pair`, repeated `--audio-root name=path`, repeated custom/highlight safe IDs, `--migration-revision`, and `--output`.
- The old Plan 12 references to `approved-audio-profile.json` and profile ownership by Phase 32 Plan 30 are stale and must not be followed.

<assurance_check>
source_artifact: .planning/phases/33-grammar-and-personal-sources/33-18-SUMMARY.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: self_checked
status: passed
warning: Fresh-context same-runtime plan check passed; assurance remains self_checked rather than cross_runtime_checked.
</assurance_check>

## Requirements Enabled, Not Completed
- `KGRAM-01`
- `KGRAM-02`
- `KPERS-01`
- `KPERS-02`
- `GJOB-01`
- `GREV-01`

This plan is an enabling checkpoint for the listed Phase 33 requirements. It does not complete any Phase 33 requirement or success criterion by itself; downstream production Plans 13-17, after reconciliation, must provide the actual curriculum, personal-source, job, review, audio, and readiness evidence.

## Must-Haves
1. The preflight binds the current Plan 32-49 Korean profile hash and refuses stale audio-profile names.
2. Custom and highlight scopes are nonempty by safe identities only.
3. The migration target and four audio roots are represented by exact identities without mutation or root creation.
4. The output artifact and sidecar are canonical, non-secret, and create-only.
5. The summary records any denied or missing authority as a downstream blocker, not as permission to continue.

## Anti-Goals
- Do not make live provider or Azure calls.
- Do not read `.env`, credentials, provider payloads, private highlights, source bytes, database URLs, or audio bytes.
- Do not migrate a database, create `.multilang/phase33/audio/` roots, create production assets, export, publish, or release.
- Do not update static Korean voice registry behavior or claim `select_voice(SupportedLanguage.KO)` should pass.

## Hard Boundaries
- Use only hash/count/path identities and safe IDs in public evidence.
- The only accepted Korean profile identity for this checkpoint is profile SHA `5000e49386404e18e7501eec99ef31a4d32a0131484ced36acbef61d033681ce` from `korean-voice-profile.json`.
- The four audio root names must be exactly `word`, `sentence`, `staging`, and `journal`; paths are identities for later authority, not directories to create in this plan.
- Downstream Plans 13-17 are not execution-ready after this plan unless their stale profile and authority references are reconciled separately.

## Evidence Contract
- The user/operator decision must explicitly approve or deny each safe value needed by the narrow CLI.
- If approved, `phase33-authority-preflight.json` and its sidecar must exist and match exact bytes.
- If denied or incomplete, the preflight artifact must remain absent and the execution summary must record the denied scopes and downstream blocker.
- Plan 32-49 `korean-voice-profile-validation.json` must be rechecked for validation SHA `87eeb00a30d4c3e68b57872c2ec238107b6d55157d72ee8bbf704b529668eaf7`, matching profile SHA, and no downstream grants.
- The focused Phase 33 CLI/authority tests must pass offline.
- The static Korean audio registry refusal must remain intact.
- The summary must state zero provider calls, zero TTS calls, zero DB mutations, zero audio root creations, zero asset writes, zero export/publication/delivery actions, and whether downstream plans remain blocked.

## Common Pitfalls
- Treating Plan 32-49 profile evidence as audio authority, review approval, or export readiness.
- Reintroducing `approved-audio-profile.json` because old Plans 14 and 16 mention it.
- Writing custom submitted words, private excerpts, source paths, titles, prompts, completions, or credentials into `.planning/`.
- Creating audio roots while merely trying to bind future root identities.
- Running downstream production plans before they are reconciled against this checkpoint.

## Stop-And-Challenge
- Stop if any approved value is not a lowercase SHA-256 where a SHA is required.
- Stop if custom or highlight safe IDs are empty.
- Stop if `profile_sha256` differs from `5000e49386404e18e7501eec99ef31a4d32a0131484ced36acbef61d033681ce`.
- Stop if the migration revision is not exactly `20260828_19`.
- Stop if the task requires provider/TTS credentials, a production database, raw source/private values, or broader output authority.

## Approval Gates
- The user/operator must answer the checkpoint before any preflight artifact is written.
- A later explicit plan is required before legal/source authority, provider review authority, private disclosure authority, TTS authority, local migration, production build/import, audio publication, readiness, export, publication, or delivery can proceed.

<checks>
<plan_check>
checker: gsdd-plan-checker
checker_runtime: opencode
status: passed
blocking: false
notes: Fresh-context same-runtime checker passed after one revision cycle. It verified the plan as a non-closing authority preflight checkpoint with runnable branch verification, Plan 32-49 profile/validation binding, privacy and no-side-effect boundaries, static Korean registry anti-regression, and downstream stale-plan blockers.
</plan_check>
</checks>

## Tasks

<task id="33-12-01" type="checkpoint:user">
  <name>Approve or deny exact narrow preflight inputs</name>
  <files>
    - CREATE: .planning/phases/33-grammar-and-personal-sources/evidence-inbox/phase33-authority-preflight.json
    - CREATE: .planning/phases/33-grammar-and-personal-sources/evidence-inbox/phase33-authority-preflight.json.sha256
  </files>
  <action>
    Ask the user/operator for an explicit approval or denial of the exact narrow values required by the implemented CLI: job ID, policy SHA, curriculum SHA, source SHA, local target SHA, output pair names, four `name=path` audio-root identities, at least one custom safe ID, at least one highlight safe ID, and migration revision `20260828_19`. Require the profile SHA to be exactly `5000e49386404e18e7501eec99ef31a4d32a0131484ced36acbef61d033681ce` from the Plan 32-49 `korean-voice-profile.json` artifact. If any value is denied, ambiguous, empty where nonempty is required, not a lowercase SHA-256 where required, or asks for a stale profile filename, create no passing preflight artifact and record the blocker in the execution summary. If and only if all values are approved, run the implemented `phase33 authority-preflight` command with those values, then write a sidecar from the exact JSON bytes. Do not read private/custom values, source bytes, credentials, database URLs, or audio bytes; do not create audio roots or mutate any database.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_phase33_authority.py tests/cli/test_phase33_commands.py -q`.
    - For the approved branch, run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "import hashlib,json,pathlib; root=pathlib.Path('.planning/phases/33-grammar-and-personal-sources/evidence-inbox'); p=root/'phase33-authority-preflight.json'; d=json.loads(p.read_text()); assert hashlib.sha256(p.read_bytes()).hexdigest()==(root/'phase33-authority-preflight.json.sha256').read_text().strip(); assert d['status']=='ready'; assert d['profile_sha256']=='5000e49386404e18e7501eec99ef31a4d32a0131484ced36acbef61d033681ce'; assert d['migration_revision']=='20260828_19'; assert d['private_capability']=='phase33-private-token-v1' and d['max_private_tokens']==24; assert d['custom_count']>0 and d['highlight_count']>0; assert set(d['audio_root_identities'])=={'word','sentence','staging','journal'}"`.
    - For the denied or incomplete branch, run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from pathlib import Path; root=Path('.planning/phases/33-grammar-and-personal-sources/evidence-inbox'); assert not (root/'phase33-authority-preflight.json').exists(); assert not (root/'phase33-authority-preflight.json.sha256').exists()"`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "import json,pathlib; p=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/korean-voice-profile-validation.json'); d=json.loads(p.read_text()); assert d['evidence_sha256']=='87eeb00a30d4c3e68b57872c2ec238107b6d55157d72ee8bbf704b529668eaf7'; assert d['profile_sha256']=='5000e49386404e18e7501eec99ef31a4d32a0131484ced36acbef61d033681ce'; assert d['selected_voice_id']=='ko-KR-SunHi:DragonHDLatestNeural'; assert d['static_registry_activated'] is False; assert all(d[k] is False for k in ['grants_audio_authority','grants_delivery_authority','grants_export_authority','grants_publication_authority','grants_release_authority','grants_review_authority','grants_route_authority'])"`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_audio_voice_registry.py -k 'rejects_korean_without_an_approved_voice or resolves_supported_languages' -q`.
  </verify>
  <done>
    Either a sidecar-bound, content-free, current-profile preflight exists with nonempty custom/highlight safe identities and four audio root identities, or the summary records exact denied/missing blockers and no downstream authority.
  </done>
</task>

<task id="33-12-02" type="auto">
  <name>Record downstream reconciliation blockers without granting authority</name>
  <files>
    - CREATE: .planning/phases/33-grammar-and-personal-sources/33-12-SUMMARY.md
  </files>
  <action>
    Write the required execution summary. Include the user/operator decision, the preflight artifact hash when created, all denied or missing scopes, zero-attempt counters for provider/TTS/DB/audio-root/source/asset/export/publication/delivery actions, and an explicit handoff that Plans 13-17 must not execute from stale `approved-audio-profile.json` or broad Plan 12 authority assumptions. The summary must not contain private/custom/source values, local absolute paths, credentials, prompts, completions, provider payloads, or audio bytes.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from pathlib import Path; p=Path('.planning/phases/33-grammar-and-personal-sources/33-12-SUMMARY.md'); s=p.read_text(); required=['32-49','korean-voice-profile.json','5000e49386404e18e7501eec99ef31a4d32a0131484ced36acbef61d033681ce','zero provider','zero TTS','zero DB','zero audio root','Plans 13-17']; assert all(x in s for x in required); forbidden=['approved-audio-profile.json as current','AZURE_','OPENAI_','DEEPL_','/home/','provider payload','prompt text','completion text']; assert not any(x in s for x in forbidden)"`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from pathlib import Path; plan=Path('.planning/phases/33-grammar-and-personal-sources/33-12-PLAN.md').read_text(); summary=Path('.planning/phases/33-grammar-and-personal-sources/33-12-SUMMARY.md').read_text(); assert 'coverage_mode: enabling_checkpoint_not_phase_closure' in plan; assert 'This plan is an enabling checkpoint' in plan; assert 'approved-audio-profile.json' in plan and 'stale' in plan; assert '87eeb00a30d4c3e68b57872c2ec238107b6d55157d72ee8bbf704b529668eaf7' in summary; assert 'downstream Plans 13-17' in summary or 'Plans 13-17' in summary"`.
    - Run `git diff --check -- .planning/phases/33-grammar-and-personal-sources/33-12-PLAN.md .planning/phases/33-grammar-and-personal-sources/33-12-SUMMARY.md`.
  </verify>
  <done>
    The summary makes the checkpoint outcome and downstream stale-plan blockers explicit without granting production authority or leaking private data.
  </done>
</task>

## Verification
- Focused Phase 33 authority/CLI tests pass in the frozen offline environment.
- If approved, the preflight JSON sidecar matches the exact bytes and records the Plan 32-49 profile SHA.
- If denied or incomplete, no passing preflight JSON or sidecar exists and the summary records the blocker.
- The Plan 32-49 validation evidence SHA and no-grant flags are rechecked.
- Korean static registry refusal still holds.
- The summary records side-effect zeros and downstream blockers.
- `git diff --check` passes for the plan and summary files.

## Success Criteria
- Plan 12 no longer depends on stale `approved-audio-profile.json` or a missing Plan 32 audio-profile artifact name.
- Plan 12 uses the implemented narrow Phase 33 authority-preflight CLI contract.
- Denied or absent authority blocks downstream production work explicitly.
- No provider, TTS, DB, private, audio-root, export, release, publication, delivery, or Phase 34 claim is made.

## High-Leverage Review
- Review `33-12-PLAN.md` and any produced preflight JSON for stale profile references, private value leakage, and side-effect authority overclaiming.
- A second pass is required because the plan controls authority, privacy, filesystem-root, and migration-prestate gates; Task 33-12-02 rechecks checkpoint scope, stale-profile wording, Plan 32-49 validation SHA presence, and summary privacy before completion.

## Leverage Review
- Lost: The old broad authority bundle is replaced by a smaller checkpoint, so production Plans 13-17 need further reconciliation before execution.
- Kept: The current CLI contract, Plan 11 content-free scaffolding, Plan 18 private-boundary semantics, and Plan 32-49 exact profile artifact remain intact.
- Gained: The next checkpoint can run safely without stale artifact paths or hidden production permissions.

## Notes
- Research was skipped because this is a planning reconciliation over established repository contracts, not a new technology integration.
- `workflow.discuss` alignment is satisfied by `33-APPROACH.md` with `alignment_status: user_confirmed`.
- The lifecycle preflight for planning Phase 33 returned `allowed: true`; sibling temporary worktree warnings are noted but do not affect this planning artifact.
- This plan intentionally does not patch Plans 13-17; execution of those plans remains blocked until their stale references are reconciled after `33-12-SUMMARY.md` exists.
