---
phase: 33-grammar-and-personal-sources
plan: "11"
type: execute
wave: 5
runtime: opencode
assurance: unreviewed
depends_on: [31-32, 32-16, 33-10]
files-modified:
  - src/multilang/domain/exporting.py
  - src/multilang/services/assemble_export_cards.py
  - src/multilang/services/phase33_authority.py
  - src/multilang/cli.py
  - tests/services/test_assemble_export_cards.py
  - tests/services/test_phase33_authority.py
  - tests/cli/test_phase33_commands.py
  - tests/integration/test_phase33_grammar_personal_sources_flow.py
autonomous: true
requirements: [KGRAM-01, KGRAM-02, KPERS-01, KPERS-02, GJOB-01, GREV-01]
upstream_blockers:
  - Do not execute until `.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-32-SUMMARY.md` and `.planning/phases/32-frequency-portuguese-text-and-audio/32-16-SUMMARY.md` both exist, plus `33-10-SUMMARY.md`.
non_goals:
  - Do not add FastAPI/UI/auth/listeners, alter source-mode layouts or GUID formulas, expose authentic excerpts, call live providers, activate/publish/package APKG, or claim Phase 34 rendering/playback.
hard_boundaries:
  - Prepared rows project only exact current accepted revisions whose source/graph/review/media dependencies still match; blockers never fall back to an older/latest value.
  - Grammar uses the existing normal field layout internally without adding a public source mode; custom-list and highlight layouts remain unchanged.
  - List/inspect/private-display release nothing until a content-free access event commits. Review files are descriptor-safely read only below project-root `.multilang/phase33/review-inputs/`.
escalation_triggers:
  - Stop on authority/root drift, access-event failure, review-input escape/symlink/race/special-file/size/type/encoding failure, private sentinel, required layout/GUID/source-mode change, or `create_app` signature drift.
approval_gates:
  - This plan prepares/contracts only; Plan 12 owns external powers, Plans 13-17 own production/evidence, and Phase 34 owns activation/publication/APKG/delivery.
anti_regression_targets:
  - Preserve CLI-only topology, `create_app()` signature, source modes, layouts, field order, ko/ko-KR boundary, NFC, stable GUIDs, media references, blank Image, and immutable review history.
known_unknowns:
  - Missing authority or complete production evidence remains a truthful blocker; fixture success cannot establish production readiness.
no_ui_proof_rationale: CLI and prepared-row contracts only; Phase 34 owns observed Anki behavior.
high_leverage_surfaces:
  - src/multilang/cli.py
  - src/multilang/services/phase33_authority.py
  - src/multilang/services/assemble_export_cards.py
second_pass_required: true
closure_claim_limit: Proves deterministic CLI/projection/authority/security contracts and offline flow only.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Implicit migration, optional personal-family achievement, shared hash-path audio and prose-only root options are unavailable.
  kept: CLI-only topology, accepted projection, exact authority and immutable review operations.
  gained: Closed process/migration/four-root recovery commands with non-vacuous safe evidence.
must_haves:
  truths:
    - Exact current accepted grammar/custom/highlight revisions project through established layouts/GUIDs with blank Image; blocked dependencies produce no partial row.
    - Exact `process`, status, and complete review operations are CLI-only and preserve `create_app()` compatibility.
    - List, inspect and private-display require exact actor/request identity, commit content-free access evidence before release, and fail with no result/value when commit fails.
    - Evidence/value files are bounded typed regular files from the fixed inbox, hashed from the opened descriptor, and fail content-free before mutation/provider construction.
    - Authority preflight/validation deterministically bind job, policy, curriculum, profile, source, target, narrow output, private-cap, and both immutable audio-root path/prestate/ceiling/write-authority identities with zero side effects.
  artifacts:
    - {path: src/multilang/services/assemble_export_cards.py, provides: "Accepted-revision privacy-safe prepared projection"}
    - {path: src/multilang/services/phase33_authority.py, provides: "Read-only authority commands and descriptor-safe review-input reader"}
    - {path: src/multilang/cli.py, provides: "Exact status/review/authority CLI contracts"}
    - {path: tests/integration/test_phase33_grammar_personal_sources_flow.py, provides: "Offline mixed-source/refusal/privacy/layout/GUID evidence"}
  key_links:
    - {from: current source/graph/review/media pointers, to: prepared row, via: "exact dependency revalidation before projection"}
    - {from: review CLI, to: access-event repository, via: "commit-before-return list/inspect/private-display service methods"}
    - {from: fixed review inbox, to: approve/edit inputs, via: "no-follow descriptor read and exact-byte hash"}
---

# Phase 33 Plan 11: Safe CLI, Authority, and Accepted Projection

<objective>
Expose the local Phase 33 workflow, exact accepted projection, audited review reads, safe review-file inputs, and deterministic authority contracts.

Purpose: Preserve privacy and compatibility while making every Phase 33 blocker and review operation locally observable.
Output: Projection, CLI, authority service, and offline tests.
</objective>

## Anti-Goals
- No server/UI, live calls, production migration, audio write, source-value disclosure, activation, publication, APKG or delivery.

## Hard Boundaries
- CLI parsers expose only exact closed options; authority and access events validate before constructors, mutations or output release.

## Evidence Contract
- Command parsing, safe projections, no-value access events, four-root authority, migration refusal and mixed-source non-vacuity tests pass with deterministic fakes.

## Common Pitfalls
- Treating status as side-effect free without audit, allowing omitted root options, accepting empty custom/highlight families, or widening local migration target.

## Stop-And-Challenge
- Stop on create_app drift, private value/path leakage, target/root ambiguity, missing exact authority, shared audio path, server fallback or Phase 34 effect.

## Approval Gates
- Plan 12 supplies external powers; this plan implements and tests contracts only and grants no migration/provider/TTS/private/publication power.

## Leverage Review
- Second pass traces each literal process/review/authority/migration option to one typed service call and verifies all failure paths precede constructors or release.

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
@.agents/skills/llm-security/SKILL.md
</execution_context>

<context>
@.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-32-SUMMARY.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-16-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-10-SUMMARY.md
@.planning/AI-LINGUISTIC-REVIEW-POLICY.md
@src/multilang/domain/exporting.py
@src/multilang/domain/source_profiles.py
@src/multilang/services/assemble_export_cards.py
@src/multilang/cli.py

<interfaces>
Keep `SourceType` and public source modes unchanged. Use internal `korean-grammar` projection through the normal field contract. Reuse Plan 05/08 access-event service/repository contracts. Add services after shared CLI owner 32-16 and preserve exact `create_app()` signature.
</interfaces>
</context>

<checks><plan_check>
checker: self
checker_runtime: opencode
status: skipped
blocking: false
notes: Revised for audit-before-return and safe review-file handling; fresh independent recheck is pending.
</plan_check></checks>

<tasks>
<task id="33-11-01" type="auto" tdd="true">
  <name>Project exact accepted revisions and prove the offline mixed-source flow</name>
  <files>
    - MODIFY: tests/services/test_assemble_export_cards.py
    - MODIFY: src/multilang/domain/exporting.py
    - MODIFY: src/multilang/services/assemble_export_cards.py
    - CREATE: tests/integration/test_phase33_grammar_personal_sources_flow.py
  </files>
  <behavior>
    - Grammar uses normal field order internally; custom/highlight layouts remain exact and highlights project reviewed microexamples, never excerpts.
    - Missing/stale/rejected/review-required/failed source, graph, text, translation or audio dependency blocks the entire row.
    - GUIDs ignore revision/provider/private hashes, Image remains blank, and all outputs/errors are private-sentinel free.
    - Offline mixed flow covers strict grammar, ordered duplicates, bridge/defer, private local/authorized/denied context, field staleness, resume, disclosure replay and production refusal.
  </behavior>
  <action>Write golden field-order/GUID/privacy/blocking tests first. Implement a projection that loads exact current accepted pointers and revalidates every source/graph/review/media hash. Route internal grammar through normal layout without changing `SourceType`; preserve custom/highlight profiles and stable identity inputs. Build isolated deterministic integration fixtures with fake analyzer/provider/audio/clocks and no live constructors or delivery effects.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_assemble_export_cards.py tests/integration/test_phase33_grammar_personal_sources_flow.py -k 'phase33 or grammar_layout or custom_layout or highlight_layout or accepted or stale or audio or guid or image or private or mixed_source or disclosure or resume or production_refusal' -q</automated></verify>
  <done>Prepared projections are exact-current, layout/GUID compatible, blank-Image, private-safe, and blocked without complete evidence.</done>
</task>

<task id="33-11-02" type="auto" tdd="true">
  <name>Implement exact audited review and status CLI contracts</name>
  <files>
    - CREATE: tests/cli/test_phase33_commands.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Exact status contract is `multilang phase33 status --format json --require-exact-authority --no-private-values`, with authorized output/sidecar support.
    - Exact processing contract is `multilang phase33 process --job-id JOB --source grammar|custom|highlight --mode start|resume --max-items N` and accepts no omitted/alternate source or mode spelling.
    - `start` runs eligible new work only. `resume` restores persisted facts, skips current accepted, retries only pending/explicitly retryable failures, leaves review_required, and never repeats disclosing/disclosed/failed_unknown private calls.
    - Process/status output reports deterministic IDs/counts for attempted, processed, accepted, review_required, failed, skipped_current, and not_attempted with exact job/source identity, item isolation, no server/fallback, and validated authority before any required provider call.
    - Status exposes separate eligible card-bearing and ready counts for custom/highlight plus safe source IDs/hashes/counts; no custom/highlight values or excerpt metadata are serialized.
    - Exact audited commands are `review list --job-id JOB --actor-id ACTOR --request-id REQUEST --status STATUS --field FIELD --source SOURCE --format json`, `review inspect --job-id JOB --item-id ITEM --field FIELD --revision-id REVISION --actor-id ACTOR --request-id REQUEST`, and `review private-display --job-id JOB --item-id ITEM --excerpt-revision-id REVISION --actor-id ACTOR --request-id REQUEST --local-only --acknowledge-private-display`.
    - Each audited command invokes the Plan 05/08 commit-before-return method and returns `access_event_id`; event failure returns nonzero with no result metadata or private value.
    - Exact mutation contracts are approve/reject/edit/regenerate with job/item/field/revision/expected-base identity, plus bridge/defer with proposal/expected-base identity. Regenerate invokes Plan 10's closed field dispatcher; approve uses `--evidence-file` and edit `--value-file` beneath the fixed inbox.
  </behavior>
  <action>Lock `create_app()` and exact command parsing first. Register the literal process command and closed source/mode enums; pass mode/max-items to Plan 10 and serialize all seven persisted denominator sets/counts in deterministic source order. Invalid authority required by the selected path fails before adapter construction. Register audited review commands with mandatory actor/request: compute canonical command hash, let Plan 05/08 commit the event under stable `(actor_id,request_id,action)`, and release only the committed reference; changed-hash reuse returns conflict/no output. Wire regenerate to Plan 10's exact five-field dispatcher, then its validated pending-candidate append; never call a generic text path for translation/audio. Keep approve/reject/edit/bridge/defer immutable under CAS and private-display as the sole exact-value read.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_phase33_commands.py tests/integration/test_phase33_grammar_personal_sources_flow.py -k 'exact_process_contract or source_enum or mode_enum or start_new_only or resume_persisted_facts or seven_denominator_ids_counts_order or mixed_source_item_isolation or no_server or no_fallback or authority_before_provider or stable_access_key or command_hash_same_replay or changed_hash_conflict_no_output or audit_commit_before_output or regenerate_five_field_dispatch or pending_candidate or approved_pointer_unchanged or sentence_approval_stales_only_dependents or signature' -q</automated></verify>
  <done>Status/review CLI is exact, audit-before-return, content-free on event failure, immutable and topology-compatible.</done>
</task>

<task id="33-11-03" type="auto" tdd="true">
  <name>Implement deterministic authority commands and descriptor-safe review inputs</name>
  <files>
    - CREATE: tests/services/test_phase33_authority.py
    - CREATE: src/multilang/services/phase33_authority.py
    - MODIFY: tests/cli/test_phase33_commands.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - `authority-preflight` requires complete source/curriculum facts, nonempty custom/highlight safe identities, exact local target revision/schema/migration hash/head/rollback proof, narrow output pairs, and all four fixed audio roots/prestates/per-file-total-item-journal ceilings/create-only/reconcile options.
    - `validate-authority` validates optional preflight plus ordered authority/sidecar/kind triples; five exact kinds, expiry/scope/profile/source/target/curriculum and private `phase33-private-token-v1` cap 24 must match.
    - Review inputs are only regular files beneath canonical project-root `.multilang/phase33/review-inputs/`, opened by no-follow descriptor traversal with stable pre/post fstat and hashed from the same descriptor bytes.
    - Value files are `.txt`, strict UTF-8 without NUL, maximum 16 KiB. Evidence files are `.json`, strict UTF-8, maximum 64 KiB, and validate a closed AI/audio evidence schema with unknown fields rejected.
    - Absolute/escaped paths, symlinked root/component/final, device/socket/FIFO, races, oversize, extension/type/encoding/schema errors fail content-free before repository mutation or provider construction.
  </behavior>
  <action>Implement read-only authority parsing with the exact Plan 12 option allowlist. Safely inspect nonempty custom input and persisted highlight metadata while emitting only IDs/hashes/counts. Bind one local non-production migration to target locator/current revision/schema hash, exact file hash, sole head `20260828_19`, request/expiry and rollback proof. Bind word/sentence/staging/journal path identities and absent-or-exact prestates plus every fixed ceiling and closed write/recovery mode. Reject missing, duplicate, alternate, symlinked or wider options before constructor/mutation. Keep descriptor-safe review inputs unchanged. Authority permits unique request/revision-qualified children only; artifact hash never selects a shared path.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_phase33_authority.py tests/cli/test_phase33_commands.py tests/cli/test_korean_foundation_commands.py -k 'authority_preflight_exact_allowlist or nonempty_custom_highlight or safe_ids_hashes_counts_only or migration_local_nonproduction or migration_20260828_19 or rollback_proof or four_audio_roots or four_root_prestates or per_file_total_item_journal_ceilings or create_only or reconcile_mode or omitted_option or duplicate_option or alternate_root or wider_path or symlink or unique_revision_path or zero_mutation or zero_constructor or phase32_cli_regression' -q</automated></verify>
  <done>Authority and review-input paths are deterministic, fixed-root, descriptor-safe, bounded, typed, hashed and fail before side effects.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Feature / constraint | Plans | Status |
|---|---|---|---|---|
| GOAL | Phase 33 | CLI-only grammar, custom lists, private highlights, review/regeneration and learner-ready preparation | 01-18 | COVERED |
| GOAL | SC-01/SC-02 | Real G0-G13 content and exact strict-i+1 over Phase 31 | 01, 05, 07, 08, 10-17 | COVERED |
| GOAL | SC-03/SC-04 | Ordered custom inputs and morphology-safe highlights | 02, 03, 07-11 | COVERED |
| GOAL | SC-05/SC-06/SC-07 | Private bounded context, item isolation, immutable review and complete preparation | 03-18 | COVERED |
| REQ | KGRAM-01 / KGRAM-02 | Grammar content, exact graph, fields, AI/audio and production gates | 01, 05, 07, 08, 10-17 | COVERED |
| REQ | KPERS-01 / KPERS-02 | Ordered custom lists, privacy-safe highlights and bounded private processing | 02-05, 07-11, 14-18 | COVERED |
| REQ | GJOB-01 | Status/attempt denominators, isolation and non-vacuous closure | 06, 07, 10, 11, 14-17 | COVERED |
| REQ | GREV-01 | Audited review reads, safe inputs, complete operations, immutable CAS | 05, 07-11, 13-17 | COVERED |
| RESEARCH | R-01/R-02 | Immutable graph, source/license, AI policy and exact audio profile | 01, 05, 07, 08, 10-17 | COVERED |
| RESEARCH | R-03/R-04/R-05 | Ordered ledger, morphology authority and explicit bridge/defer | 02, 03, 07, 08, 10, 11 | COVERED |
| RESEARCH | R-06/R-07 | Private separation, hard 24-token cap, audited immutable review and safe inputs | 03-05, 07, 09-18 | COVERED |
| RESEARCH | R-08/R-09/R-10/R-11 | State machine, PostgreSQL evidence, exact CLI/artifacts and shared-owner order | 04, 06-18 | COVERED |
| CONTEXT | C-01/C-02/C-03 | Exact Phase 31/bootstrap, strict construction and production source/AI/audio evidence | 01, 05, 07, 08, 10-17 | COVERED |
| CONTEXT | C-04/C-05 | Preserve ordered duplicates; explicit bridge/defer/review only | 02, 07, 08, 10, 11 | COVERED |
| CONTEXT | C-06/C-07 | Exact private separation, hard cap, immutable events and atomic CAS | 03-05, 07-18 | COVERED |
| CONTEXT | C-08/C-09 | Exact statuses/denominators and complete non-overwriting review operations | 05-11, 13-17 | COVERED |
| CONTEXT | C-10/C-11/C-12 | Preserve identity/layout/GUID/Image; CLI only; external powers and Phase 34 boundary | 01-18 | COVERED |
| CONTEXT | C-13/C-14/C-15 | AI 2/3-pass policy, exact Plan 30 profile and complete non-vacuous curriculum closure | 01, 05-18 | COVERED |
| CONTEXT | C-16 | List/inspect/private-display commit content-free actor/request/query/result/policy/snapshot event before release | 05, 07, 08, 11 | COVERED |
| CONTEXT | C-17 | Review files are fixed-root, no-follow, regular, bounded, typed, hashed and pre-side-effect | 11 | COVERED |
| CONTEXT | C-18 | Legal-authority complete curriculum IDs/order/root; every construction has all five fields and bound forms use reviewed construction/carrier word audio | 12-17 | COVERED |
| CONTEXT | C-19 | Independent DB import and prepared-artifact validators emit declared JSON/sidecars and gate completion | 14, 15, 17 | COVERED |
| CONTEXT | C-20 | Exact process start/resume command, seven denominators and no replay of closed private disclosure states | 10, 11, 14, 17 | COVERED |
| CONTEXT | C-21 | Exact authorized immutable word/sentence audio roots, byte identities/ceilings and descriptor revalidation | 05, 07, 08, 11, 12, 14, 16, 17 | COVERED |
| CONTEXT | C-22 | Access identity exactly actor/request/action plus separate canonical command hash and atomic changed-hash conflict | 05, 07, 08, 11 | COVERED |
| CONTEXT | C-23 | Exhaustive persisted personal inventory closure into ready/review_required/failed/duplicate/deferred | 08-11, 14, 17 | COVERED |
| CONTEXT | C-24 | Readiness report/sidecar written once; preparation records only in distinct prepared artifacts | 15, 17 | COVERED |
| CONTEXT | C-25 | Exact authority-selected local migration to sole `20260828_19` precedes production import | 07, 11, 12, 14, 15 | COVERED |
| CONTEXT | C-26 | Unique revision-qualified audio paths plus reservation/staging/journal crash recovery | 05, 07, 08, 10-12, 14, 16, 17 | COVERED |
| CONTEXT | C-27 | Nonempty eligible and ready custom/highlight families with safe evidence | 08-12, 16, 17 | COVERED |
| CONTEXT | Deferred Ideas | No deferred idea is included | 01-18 | EXCLUDED — DEFERRED |
| RESEARCH | Out of scope | APKG/render/playback/delivery/activation/publication | Phase 34 | EXCLUDED — OTHER PHASE |
</source_audit>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-11-01 | Tampering / Elevation | prepared projection | mitigate | Exact current accepted dependency revalidation; block stale/missing facts. |
| T-33-11-02 | Information Disclosure | CLI/export | mitigate | Safe projections, private sentinel scans, content-free errors. |
| T-33-11-03 | Path Traversal / Tampering | review files | mitigate | Fixed-root no-follow descriptor walk, regular/type/size/UTF-8/schema checks, stable fstat and exact-byte hash. |
| T-33-11-04 | Repudiation | list/inspect/private-display | mitigate | Mandatory actor/request and committed content-free access event before release. |
| T-33-11-05 | Denial of Service | CLI batch/input | mitigate | Bounded files, deterministic item isolation, retries and resume. |
| T-33-11-06 | Elevation / Tampering | migration/root options | mitigate | Closed literal option allowlists, exact local target/head/hash and four-root pre-side-effect validation. |
</threat_model>

<high_leverage_review>Trace one list, inspect and private-display request through access-event commit before output; race every review-file boundary; inspect projection/GUID/privacy and authority zero-side-effect contracts.</high_leverage_review>
<verification>Run all task commands without provider credentials, then the full offline suite separately and inspect outputs for private sentinels.</verification>
<success_criteria>
- Prepared rows use only exact current accepted dependencies and established layouts/GUIDs.
- Every review read is audit-before-return and every review file is fixed-root descriptor-safe.
- Authority job/policy/curriculum/profile/source/target/output identities are deterministic and side-effect free.
- CLI topology and Phase 34 boundaries remain unchanged.
</success_criteria>
<output>After completion, create `.planning/phases/33-grammar-and-personal-sources/33-11-SUMMARY.md` with command contracts, access-event proof, safe-file matrix, authority identities, projection/GUID/privacy evidence and bounded claim.</output>
