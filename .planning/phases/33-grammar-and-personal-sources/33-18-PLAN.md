---
phase: 33-grammar-and-personal-sources
plan: "18"
type: execute
wave: 2
runtime: opencode
assurance: unreviewed
depends_on: [32-07, 33-04]
files-modified:
  - src/multilang/services/text_generation.py
  - tests/services/test_text_generation.py
autonomous: true
requirements: [KPERS-02]
upstream_blockers:
  - Do not execute this plan until `.planning/phases/32-frequency-portuguese-text-and-audio/32-07-SUMMARY.md` exists and confirms the settled Phase 32 text-generation interface/artifacts.
  - If `32-07-SUMMARY.md` is absent, stop before reading or modifying `src/multilang/services/text_generation.py`; Phase 33 Wave 1 remains executable without this plan.
non_goals:
  - Do not change private-processing contracts from Plan 04, grant authority, call live providers, read credentials, persist prompts/payloads, create source/content/audio assets, or modify any file outside `text_generation.py` and its focused tests.
hard_boundaries:
  - This is the shared Phase 32 text-generation join. It is blocked on exact Phase 32 Plan 07 completion and the Plan 04 offline broker summary; do not infer the interface from dirty source alone.
  - Korean highlight context remains denied by default. A provider adapter call may receive only Plan 04 bounded context after exact reservation and must preserve current non-highlight/non-Korean behavior.
escalation_triggers:
  - Stop on missing `32-07-SUMMARY.md`, Phase 32 text interface drift, constructor compatibility break, inability to prove zero provider calls on refusal/replay, unsafe retry, prompt/private leakage, or request to broaden authority.
approval_gates:
  - Execution is gated by the upstream summary/artifacts only; no user authority or live provider action is granted by this plan.
anti_regression_targets:
  - Preserve existing frequency, custom-list-without-private-context, Latin and other-language text generation snapshots, provider-boundary-only ko-KR, NFC, and private-value-free diagnostics.
known_unknowns:
  - The exact Phase 32 text-generation interface is not authoritative until `32-07-SUMMARY.md` exists; revise this plan rather than guessing if the landed interface differs.
no_ui_proof_rationale: Shared provider-boundary wiring only; no visible UI or Anki claim.
high_leverage_surfaces:
  - src/multilang/services/text_generation.py
second_pass_required: true
closure_claim_limit: Proves backward-compatible shared-file wiring under fake adapters after Phase 32 Plan 07; no provider privacy terms, live provider behavior, quality, readiness, publication, APKG, or delivery claim.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: [33-07]
leverage:
  lost: Phase 33 no longer edits shared text-generation code in the initial executable wave.
  kept: Plan 04 exact private broker and Phase 32 typed provider boundary.
  gained: A coherent blocked join with explicit upstream summary/artifact dependency.
must_haves:
  truths:
    - Korean highlight text generation with private context refuses by default and performs zero provider calls without a valid Plan 04 reservation.
    - Authorized context uses only Plan 04 bounded, target-centered, <=24 `phase33-private-token-v1` tokens and runs the provider call outside every transaction.
    - Replay of disclosing, disclosed or failed_unknown private states never invokes the adapter again; exact-idempotency retry reuses only the bound key.
    - Generated microexample output remains distinct from the excerpt and cannot set identity, source authority, policy, review state, or export fields.
    - Existing non-private text-generation behavior remains byte-for-byte compatible with Phase 32 Plan 07 contracts.
  artifacts:
    - {path: src/multilang/services/text_generation.py, provides: "Authority-gated Korean highlight provider boundary after Phase 32 Plan 07"}
    - {path: tests/services/test_text_generation.py, provides: "Fake-adapter zero-call, replay, copy/prompt, and legacy-regression tests"}
  key_links:
    - {from: Plan 04 PrivateContextBroker, to: text-generation adapter invocation, via: "optional injected broker reservation before adapter construction/call"}
    - {from: bounded provider context, to: generated microexample candidate, via: "structured-output validation and exact-copy/overlong-copy refusal"}
---

# Phase 33 Plan 18: Blocked Text-Generation Private-Context Wiring

<objective>
Wire the Plan 04 private-context broker into the shared text-generation boundary after the exact Phase 32 Plan 07 text-generation owner has landed.

Purpose: KPERS-02 needs the production provider path to enforce the offline authority contract, but that shared-file edit cannot execute while `32-07-SUMMARY.md` is missing.
Output: Focused `text_generation.py` wiring and regression tests only.
</objective>

## Execution Gate
- **Blocked until upstream exists:** `.planning/phases/32-frequency-portuguese-text-and-audio/32-07-SUMMARY.md` must exist before this plan runs.
- Executing Phase 33 now may skip this plan and run only Wave 1 offline scaffolding; full Phase 33 completion still waits for this exact join and later upstream-gated plans.

## Anti-Goals
- No authority-file creation, source/content acquisition, provider/TTS live call, DB migration/mutation, audio write, quality approval, publication, APKG, delivery, or Phase 34 claim.

## Hard Boundaries
- Treat private excerpt text as untrusted data and preserve the existing sanitizer as defense in depth after Plan 04 capability validation.

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
@.agents/skills/llm-security/SKILL.md
</execution_context>

<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-07-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-04-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-PATTERNS.md
@src/multilang/services/text_generation.py
@tests/services/test_text_generation.py

<interfaces>
Use the Phase 32 Plan 07 landed text-generation request/cache/retry/telemetry contracts exactly. Use the Plan 04 `PrivateContextBroker` contract as an optional injected dependency for Korean highlight requests only. Do not change existing constructor/caller compatibility unless `32-07-SUMMARY.md` explicitly requires it.
</interfaces>
</context>

<checks><plan_check>
checker: self
checker_runtime: opencode
status: skipped
blocking: false
notes: New split plan isolates the missing-32-07 shared-file join; fresh independent checker review is pending.
</plan_check></checks>

<tasks>
<task id="33-18-01" type="auto" tdd="true">
  <name>Gate Korean highlight text generation without regressing other sources</name>
  <files>
    - MODIFY: tests/services/test_text_generation.py
    - MODIFY: src/multilang/services/text_generation.py
  </files>
  <behavior>
    - Korean highlight generation with context requires a committed Plan 04 disclosing reservation before the provider adapter is invoked outside the transaction.
    - Missing, stale, mismatched, over-budget, disclosing, disclosed or failed_unknown private authority produces controlled refusal and zero adapter calls.
    - Local-only or context-free operation remains possible without private authority and sends no excerpt.
    - Generated microexample output is a separate structured field and exact normalized excerpt replay or long-copy policy violation becomes review_required.
    - Frequency, custom-list-without-private-context, Latin and other languages retain current Phase 32 request/cache/retry/telemetry behavior.
  </behavior>
  <action>First require `32-07-SUMMARY.md`; if it is missing, stop with a blocked-upstream summary and do not inspect or edit shared source. After the upstream gate passes, add fake-adapter call-capture tests, including prompt injection, exact-copy output, crash after reservation, known success before finalize, idempotent-route timeout, non-idempotent unknown result, and legacy source snapshots. Wire an optional Plan 04 private context broker into the Korean highlight request path while preserving the exact Phase 32 constructor/caller contracts. Keep `_sanitize_korean_highlight_context` as defense in depth after capability validation. Pass the exact provider idempotency key only when the route contract supports it. Validate structured output separately from source context and reject exact/overlong source copying into the microexample; never let model output alter lexical identity, approval, provenance, or source-mode metadata.</action>
  <verify><automated>test -f .planning/phases/32-frequency-portuguese-text-and-audio/32-07-SUMMARY.md &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_text_generation.py -k 'korean and (private or authority or zero_call or local_only or microexample or copy or prompt or existing_source)' -q</automated></verify>
  <done>The shared text-generation boundary is denied by default for private context, fake-adapter verified when exact authority exists, and backward-compatible with Phase 32 Plan 07 behavior.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-05 | Production text-generation boundary enforces source/context/microexample separation | COVERED |
| REQ | KPERS-02 | Exact per-run remote-processing authority on the shared provider path | COVERED |
| RESEARCH | Private-context capability / prompt-injection boundary | Phase 32 text-generation join, zero-call refusal, bounded context and legacy regression tests | COVERED |
| CONTEXT | Remote context requires exact per-run authority; `max_context_tokens <= 24`; no copied private values | Locked token cap and privacy decisions preserved | COVERED |
</source_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Private broker to provider adapter | Bounded derived context crosses to a configured adapter only after exact reservation. |
| Provider output to review candidate | Untrusted model output can propose content but cannot set identity, authority, policy or approval. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-18-01 | Information Disclosure | text-generation adapter | mitigate | Default denial, exact Plan 04 reservation, 24-token cap, sanitizer defense in depth and zero-call refusal tests. |
| T-33-18-02 | Tampering / Elevation | model output | mitigate | Closed structured output validation; prompt/excerpt text is quoted data and cannot author identity/policy/review fields. |
| T-33-18-03 | Denial of Service / Repudiation | retry/replay | mitigate | Exact idempotency-key reuse only when supported; closed states inspect-only and never auto-call again. |
</threat_model>

<high_leverage_review>Second pass: compare the landed Phase 32 Plan 07 interface with every constructor/caller touched, then trace denied, authorized, replayed and unknown-result private requests through call counts and content-free telemetry.</high_leverage_review>
<verification>Run the task command only after `32-07-SUMMARY.md` exists; keep live provider constructors poisoned and scan captured requests/errors for private sentinels.</verification>
<success_criteria>
- Plan 18 refuses to run while Phase 32 Plan 07 is missing.
- Exact private authority is required before Korean highlight context reaches the shared adapter.
- Refusal and closed replay states make zero provider calls.
- Existing non-private text-generation behavior remains compatible with the settled Phase 32 contract.
</success_criteria>
<output>After completion, create `.planning/phases/33-grammar-and-personal-sources/33-18-SUMMARY.md` with upstream gate proof, interface compatibility notes, call-count matrix, private sentinel scan, and bounded claim.</output>
