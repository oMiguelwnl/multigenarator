---
phase: 33-grammar-and-personal-sources
plan: "14"
type: execute
wave: 8
runtime: opencode
assurance: unreviewed
depends_on: [31-31, 32-08, 32-30, 33-11, 33-13]
files-modified:
  - src/multilang/services/phase33_audio_readiness.py
  - src/multilang/cli.py
  - tests/services/test_phase33_audio_readiness.py
  - tests/cli/test_phase33_commands.py
autonomous: true
requirements: [KGRAM-01, KPERS-01, KPERS-02, GJOB-01, GREV-01]
non_goals:
  - Do not call live TTS/review providers in tests, select or invent a voice/profile/SSML/format/route default, treat historical profile approval or metadata as current Phase 33 acoustic evidence, require a new human listening gate, overwrite revisions, activate/publish/package APKG, or claim observed Anki playback.
hard_boundaries:
  - Audio readiness consumes only exact accepted current text, the exact rehashed Phase 32 Plan 30 `approved-audio-profile.json` and backing authority identities, exact Plan 12 bounded-use TTS/provider authority, and Phase 31/32 integrity/acoustic contracts; every old artifact/evidence row remains immutable.
  - CLI commands validate authority/sidecar/expiry/source/target prestate before any constructor, callback, DB write, asset write, or production artifact output.
  - Every authoritative grammar construction requires both word and sentence audio; validators require exact curriculum/asset/import/evidence/prepared equality and never infer readiness from counts or unchecked producer output.
  - Local migration, audio reservation/staging/publication/finalization, and independent validation are separate authority-bound operations; provider/TTS work never runs inside a DB transaction.
escalation_triggers:
  - Stop on text/request/voice/profile/SSML/format/byte drift, integrity/ASR-G2P/acoustic failure or uncertainty, unsupported claim, authority mismatch/expiry, changed field dependency, or a requested human-listening prerequisite.
approval_gates:
  - This plan uses deterministic fakes only. Plans 15-17 may run build, live review/TTS/AI acoustic, local DB/asset, and readiness operations solely under exact Plan 12 powers.
anti_regression_targets:
  - Preserve ko as product identity, ko-KR only at provider boundary, exact word/sentence roles, sentence-to-translation/audio invalidation, five review fields, immutable history, CLI signature, artifact paths, and Phase 34 boundary.
known_unknowns:
  - The exact profile identity comes only from Phase 32 Plan 30; Plan 12 may authorize bounded Phase 33 route/use/spend but cannot select an alternate. Missing/drifted profile blocks audio without fallback.
no_ui_proof_rationale: Audio evidence and CLI tooling only; no rendered Anki playback or delivery claim.
high_leverage_surfaces:
  - src/multilang/services/phase33_audio_readiness.py
  - src/multilang/cli.py
second_pass_required: true
closure_claim_limit: Proves audio/CLI behavior with exact fake bytes/evidence; real build, calls, artifacts, and complete readiness require Plans 15-17 authorized execution.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Audio cannot be accepted from provider metadata or optional human testimony.
  kept: Phase 31 acoustic evidence, Phase 32 Azure/integrity adapters, immutable revisions, and CLI topology.
  gained: Exact machine/AI audio readiness plus authority-bound production command contracts.
must_haves:
  truths:
    - Each word/sentence revision has one immutable safe item/revision/request-qualified final path under its exact role root; artifact SHA-256 is separate and equal hashes across distinct paths are valid.
    - Committed reservation precedes TTS, private staging precedes create-only final publication, and matching published reservation precedes DB evidence/pointer finalization; all crash points are deterministically reconcilable.
    - Every authoritative curriculum ID requires both roles; word audio for bound particles/endings binds an AI-reviewed construction sample or minimal carrier context, and sentence audio binds the exact accepted sentence.
    - Decoder/format/duration/silence/clipping/reference checks pass; ASR/G2P/acoustic comparison runs where applicable; capable exact-byte AI evidence or policy-supported automated integrity determines readiness without human listening.
    - New sentence candidates stale nothing; approval of that exact candidate stales prior-sentence-bound translation/sentence audio, while word/pronunciation approval stales only prior-bound word audio.
    - Production CLI exposes exact authority-bound build/review/audio/prepare commands and refuses before constructors/writes when any power/prestate is missing or stale.
    - Literal read-only `validate-production-import` and `validate-prepared-artifacts` commands independently rehash inputs, inspect the authorized local target/artifacts, emit sidecar-bound JSON, and fail on any DB/CAS/idempotency or field/media/GUID/layout/sample/privacy mismatch.
  artifacts:
    - {path: src/multilang/services/phase33_audio_readiness.py, provides: "Exact audio synthesis/integrity/ASR-G2P/acoustic evidence and field readiness"}
    - {path: src/multilang/cli.py, provides: "Authorized production commands plus independent import/prepared validator commands consumed by Plans 15-17"}
  key_links:
    - {from: exact accepted text revision, to: audio readiness, via: "request/voice/profile/artifact hashes plus deterministic and AI acoustic evidence"}
    - {from: Phase 32 approved profile plus Plan 12 authorities, to: production CLI constructors and outputs, via: "immediate profile/authority/sidecar/expiry/source/target/budget validation"}
---

# Phase 33 Plan 14: Exact Audio Readiness and Production CLI Tooling

<objective>
Implement exact Korean word/sentence audio readiness and authority-bound production CLI commands over Plan 13 content/AI evidence.

Purpose: Split audio and production command wiring from content/AI tooling so each executor remains within context budget.
Output: Audio readiness service, production CLI contracts, and deterministic negative/integration tests.
</objective>

## Anti-Goals
- No live calls in tests, route defaults, metadata-only approval, human listening gate, history overwrite, activation/publication/APKG, or Phase 34 claim.

## Hard Boundaries
- Exact accepted text and Plan 12 authority precede every audio operation; deterministic/AI evidence is non-overridable and immutable.

## Evidence Contract
- Request/profile/byte/integrity/ASR-G2P/acoustic/invalidation/authority-refusal/CLI tests must pass with fake bytes and zero live constructors.

## Common Pitfalls
- Synthesizing stale text, reusing a different voice/profile, accepting undecoded bytes, confusing optional listening with evidence, or constructing adapters before authority validation.

## Stop-And-Challenge
- Stop on any authority/text/request/media hash drift, unsupported acoustic claim, unknown provider result, or required compatibility change.

## Approval Gates
- Plans 15-17 perform actual operations under Plan 12; this plan grants no power and requires no new human linguistic/listening review.

## Leverage Review
- Lost: metadata/listening shortcuts. Kept: Azure/integrity/acoustic adapters. Gained: exact audio readiness and production CLI.

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
@.agents/skills/llm-security/SKILL.md
@.agents/skills/azure-ai/SKILL.md
</execution_context>

<context>
@.planning/AI-LINGUISTIC-REVIEW-POLICY.md
@.planning/phases/31-hangul-and-pronunciation-i-plus-1/31-31-SUMMARY.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-08-SUMMARY.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-30-SUMMARY.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/approved-audio-profile.json
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/heard-review-authorization.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/heard-review-authorization.md.sha256
@.planning/phases/33-grammar-and-personal-sources/33-11-SUMMARY.md
@.planning/phases/33-grammar-and-personal-sources/33-13-SUMMARY.md
@src/multilang/services/ai_acoustic_review.py
@src/multilang/services/audio_integrity.py
@src/multilang/services/generate_audio_items.py
@src/multilang/cli.py

<interfaces>
Reuse exact Phase 31 `ai_acoustic_review_passed` evidence and Phase 32 Azure request/integrity contracts. Load and rehash the Phase 32 Plan 30 approved profile plus backing heard authority; bind exact catalog, voice, locale, output format, neutral-SSML, SDK/provider version, route, profile, and authority identities. Plan 12 can grant only bounded use/spend for that identity. New orchestration adds Phase 33 field/revision/dependency bindings and current global-policy acoustic evidence only. No service reads credentials or selects a route/profile; CLI resolves validated authority and injects adapters.
</interfaces>
</context>

<checks><plan_check>
checker: self
checker_runtime: opencode
status: skipped
blocking: false
notes: Split audio/CLI tooling plan created to keep the production lane implementation-sized; fresh independent recheck is pending.
</plan_check></checks>

<tasks>
<task id="33-14-01" type="auto" tdd="true">
  <name>Implement exact word and sentence audio readiness</name>
  <files>
    - CREATE: tests/services/test_phase33_audio_readiness.py
    - CREATE: src/multilang/services/phase33_audio_readiness.py
  </files>
  <behavior>
    - Audio requests bind exact accepted text revision, role, ko-KR provider boundary, exact profile identities, and one final relative path `SAFE_ITEM_ID/FIELD_REVISION_ID/REQUEST_SHA256.PROFILE_EXTENSION`; artifact hash never chooses the path.
    - Missing profile/backing authority, any field/hash drift, or a Plan 12 TTS authority naming another profile fails before adapter construction with no fallback.
    - Decode/format/duration/silence/clipping/reference checks and applicable ASR/G2P/acoustic comparisons are exact; capable exact-byte AI review records full policy provenance.
    - Readiness is ai_acoustic_review_passed or a claim specifically supported by automated_integrity_passed; unsupported, disagreement, uncertainty, missing bytes, or deterministic failure blocks.
    - Sentence and word dependency changes stale only exact dependent fields while preserving every old revision/artifact/evidence row.
    - No mandatory grammar item or audio role has an exception path; missing construction/carrier text, sentence text, bytes, or evidence blocks that item.
    - Role/root mismatch, path escape, symlink/special file, alternate destination, byte-length/hash mismatch, or unreferenced byte fails readiness.
    - Recovery covers crash before call, after staged fsync, after final publish before DB finalize, and after finalize. It removes only ephemeral staging after successful reconciliation and never final/history bytes.
  </behavior>
  <action>Write path/hash-equality and four crash-point tests first. Implement the Plan 05/08 reservation-first protocol over the exact word, sentence, staging and journal roots. Commit reservation before constructing/calling TTS. Use descriptor-relative no-follow staging, fsync and deterministic integrity/acoustic validation; create-only publish to the unique request/revision path and fsync its directory; finalize evidence/pointer/event in a later CAS transaction; append content-free journal transitions. Reconciliation rehashes staged/final bytes against reservation and resumes the next safe transition. Unknown provider outcomes keep existing failed_unknown rules. Equal artifact hashes at distinct revision paths pass; shared paths, mismatches and unreserved finals block.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_phase33_audio_readiness.py -k 'unique_request_revision_path or same_hash_distinct_paths or shared_path_refused or reserve_commit_before_call or no_transaction_during_tts or staging_fsync or publish_create_only or directory_fsync or finalize_cas or journal_transition or crash_before_call or crash_after_staging or crash_after_publish_before_finalize or crash_after_finalize or reconcile or failed_unknown or mismatch_blocks or staging_cleanup_only or no_new_human_gate' -q</automated></verify>
  <done>Audio readiness is exact, immutable, dependency-scoped, and established by deterministic/AI evidence without mandatory listening.</done>
</task>

<task id="33-14-02" type="auto" tdd="true">
  <name>Wire production commands and independent import/prepared validators</name>
  <files>
    - MODIFY: tests/cli/test_phase33_commands.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Commands are exactly `multilang phase33 migrate-local-target`, `production-build`, `production-review`, `production-audio`, and `production-prepare`; each consumes validated authority and accepts no undeclared target/profile/root widening.
    - `migrate-local-target` permits only the authority-selected local non-production target, exact prestate, migration file hash and sole head `20260828_19`; it validates backup/restore/rollback proof, reconnects for revision/schema/ORM parity, detects unexpected data mutation, and makes an already-at-head exact retry no-write.
    - `production-build` requires curriculum, curriculum-sidecar, asset, asset-sidecar, attribution, attribution-sidecar, manifest, and manifest-sidecar outputs and binds all of them to the legal-authority curriculum root before DB mutation.
    - Every command validates exact authority file/sidecar/kind/expiry, source bytes, target locator/revision/schema prestate, route/profile/budget, and output allowlist before constructors or writes.
    - Missing/denied/stale/mismatched authority yields controlled nonzero zero-constructor/zero-write output; credentials are read only after validation and never serialized.
    - `production-audio` requires exact fixed word/sentence/staging/journal roots, all four prestates, fixed per-file/total/item/journal ceilings, `create-only` write mode and `reconcile` recovery mode; omitted/alternate/wider options fail before constructor/write.
    - `production-prepare` requires exact curriculum/import-validation/readiness pairs, opens readiness read-only with no output/inout option, and records `preparation_status: ready_for_artifact_validation` plus `phase33_complete: false` only in the distinct prepared manifest.
    - `multilang phase33 validate-production-import` independently requires authority validation, legal/local-target authorities and sidecars, curriculum/asset/attribution/build files and sidecars, then read-only verifies exact before/after/retry DB facts, row/revision/source/root equality, CAS success, and idempotent no-op replay before writing only validation JSON/sidecar.
    - `multilang phase33 validate-prepared-artifacts` independently requires authority validation, all paired evidence, and both exact audio roots; it treats readiness as immutable read-only input, verifies exhaustive personal inventory closure, and writes the sole `phase33_complete: true` JSON/sidecar proof.
    - Prepared validation descriptor-opens/no-follow traverses final, staging and journal roots, re-hashes every unique revision path, allows equal hashes at distinct paths, and rejects unresolved reservations/journals, nonempty staging, unreserved finals, shared paths, missing/extra/drifted/role-swapped bytes.
    - Each validator rejects producer self-assertion without recomputation, unknown fields, duplicate/missing/extra/reordered IDs, stale hashes, unauthorized paths, and output-sidecar drift; validation failure exits nonzero with `passed: false` and content-free mismatch facts.
  </behavior>
  <action>Add exact CLI option-allowlist tests. Implement migration as an authority-first command that configures Alembic without shell interpolation, proves sole graph/head/file hash, migrates only the selected target, reconnects and writes the narrow result pair. Implement production audio with all four literal root options and the Task 1 recovery service. Update the independent validator to inspect DB reservations plus final/staging/journal roots descriptor-safely: each finalized revision maps to exactly one unique path; equal hashes on different paths pass; every final has matching finalized reservation; staging is empty and journals fully reconciled. Retain readiness read-only, exhaustive personal partition and Phase 34 exclusions.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_phase33_commands.py -k 'migrate_local_target_contract or migration_exact_head_20260828_19 or migration_file_hash or local_nonproduction_only or post_migration_parity or already_head_no_write or production_audio_four_exact_roots or omitted_root_refused or alternate_root_refused or prestate_required or per_file_total_item_ceilings or create_only or reconcile_mode or crash_recovery or unresolved_reservation or unresolved_journal or staging_not_empty or unreserved_final or same_hash_distinct_paths_allowed or shared_final_path_refused or validator_read_only or personal_nonvacuous or no_phase34' -q</automated></verify>
  <done>Plans 15-17 have exact production entry points and two independent, sidecar-bound, fail-closed validators for DB import and prepared artifacts.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | Real learner-ready lane tooling | Mandatory audio evidence, production commands, and independent import/prepared validators | COVERED |
| REQ | KGRAM-01 / KPERS-01 / KPERS-02 | Exact word/sentence audio and personal-source field readiness | COVERED |
| REQ | GJOB-01 / GREV-01 | Failure isolation, field dependencies, immutable audio revisions | COVERED |
| RESEARCH | Audio/CLI architecture | Ordered after shared CLI owner 32-16 via Plan 11 and exact approved-profile owner 32-30; reuses Phase 31/32 contracts | COVERED |
| CONTEXT | Exact local migration; unique request/revision audio paths; reservation/staging/journal recovery; non-vacuous personal closure; readiness read once | Exact Plan 30 profile immutable and all gates fail closed | COVERED |
</source_audit>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-14-01 | Spoofing / Tampering | audio request/evidence | mitigate | Rehash exact Plan 30 catalog/voice/profile/SSML/format/version/route/authority plus text/request/byte hashes and deterministic/AI validators. |
| T-33-14-02 | Elevation | production CLI | mitigate | Validate separate exact authorities/prestate/output allowlist before constructors or writes. |
| T-33-14-03 | Information Disclosure | credentials/media telemetry | mitigate | Credential aliases only in evidence, no raw payloads/secrets, content-safe errors. |
| T-33-14-04 | Denial of Service | synthesis/acoustic review | mitigate | Exact item/call/cost/time/retry ceilings and per-item isolation. |
| T-33-14-05 | Tampering / Repudiation | production import/prepared outputs | mitigate | Independent read-only recomputation, exact ID/hash/CAS/replay/field/media/GUID/layout/privacy checks, canonical JSON and sidecars. |
| T-33-14-06 | Tampering / Repudiation | audio publication crash gap | mitigate | Committed DB reservation, private no-follow staging, create-only final publish, directory fsync, later CAS finalize, append-only journal and reconciliation tests. |
| T-33-14-07 | Elevation / Tampering | local migration | mitigate | Exact non-production target/prestate/head/file hash, rollback proof, post-reconnect parity and no-write retry. |
</threat_model>

<high_leverage_review>Second pass: trace exact accepted sentence through TTS request/bytes/integrity/acoustic evidence/staleness, then prove every production command rejects stale authority before any constructor/write.</high_leverage_review>
<verification>Run both task commands with live constructors poisoned and no production data/evidence output paths created.</verification>
<success_criteria>
- Exact machine/AI audio evidence determines word/sentence readiness without human listening.
- Field dependency changes stale only affected current approvals and preserve history.
- Production commands derive job/policy identities from the validation aggregate and enforce exact Plan 12 powers, Phase 32 profile, and Plans 15-17 paths before every side effect.
- Literal import and prepared validators recompute and sidecar-bind closure facts instead of trusting producer output.
- Existing CLI and Phase 34 boundaries remain unchanged.
</success_criteria>
<output>After completion, create `.planning/phases/33-grammar-and-personal-sources/33-14-SUMMARY.md` with audio request/evidence matrix, invalidation table, command contracts, authority refusal counts, and bounded claim.</output>
