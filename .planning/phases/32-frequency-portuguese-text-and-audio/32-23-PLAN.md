---
phase: 32-frequency-portuguese-text-and-audio
plan: "23"
type: execute
wave: 35
runtime: opencode
assurance: self_checked
depends_on: [32-22]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-receipts/
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-aggregate.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-validation.json
autonomous: true
requirements: [KFREQ-01, KFREQ-02, GLEX-02, GMOR-01]
non_goals: ["No source/bundle edit, final approval, pointer activation, DB ingestion, provider call, commit, export, or publication."]
no_ui_proof_rationale: "Bounded review import and read-only aggregate evidence only."
second_pass_required: true
closure_claim_limit: "Proves exact qualified review receipt coverage; it does not create final-bundle authority or activate/commit content, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Every private batch is validated against immutable build identities before one content-free receipt is atomically installed."
    - "Receipts are disjoint, exact-replay stable and aggregate to all 5965 decisions with 3000/2965 and 1000/level."
    - "Bundle bytes and private review inputs remain unchanged; aggregate contains only hashes, counts, controlled codes and qualified role IDs."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-receipts, provides: "Immutable content-free batch receipts"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-aggregate.json, provides: "Complete source review root/count/status"}
  key_links:
    - {from: "private bounded review inputs", to: "safe review aggregate", via: "exact item/build validation and immutable receipts"}
---

# Phase 32 Plan 23: Source Review Receipt Import and Complete Aggregation

<objective>
Import private qualified source decisions into immutable content-free receipts and compute the exact complete aggregate.

Purpose: Final bundle approval must bind machine-verifiable reviewer coverage without exposing or mutating content.
Output: Receipt directory, complete aggregate and independent validation.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-22-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-input/manifest.json
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-role-registry.json
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-progress.json
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-23-01" type="auto">
  <name>Import each private batch into one immutable content-free receipt</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-receipts/
  </files>
  <action>Snapshot bundle and review-input hashes, validate the pseudonymous role registry sidecar and complete CAS progress root, then invoke `import-korean-bundle-review-batch` once for every manifest-declared batch in ascending ID order with explicit build root/result, batch path, registry and receipt directory. Require at most 100 items, exact build/item/registry/progress hashes, qualified role/class, controlled decisions/reasons and no overlap. Atomically install one content-free receipt per batch. Exact replay is no-write; first failure stops before later imports and changes neither bundle nor private input.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; b=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox'); i=json.loads((b/'source-review-input/manifest.json').read_text()); p=json.loads((b/'source-review-progress.json').read_text()); r=b/'source-review-receipts'; fs=sorted(r.glob('receipt-*.json')); assert p['status']=='complete' and len(fs)==len(i['batches']) and all(json.loads(x.read_text())['status']=='imported' for x in fs)"</automated></verify>
  <done>Every exact private decision has one stable content-free receipt and no source/bundle mutation.</done>
</task>

<task id="32-23-02" type="auto">
  <name>Aggregate and independently validate complete qualified coverage</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-aggregate.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-validation.json
  </files>
  <action>Run `validate-korean-bundle-review-batches` over the fixed receipt directory and immutable build, binding the role-registry sidecar, complete progress root and manifest root. Require disjoint exact coverage of 5965 dispositions, 3000 accepted, 2965 rejected, 1000 per level and all enumerated modernity/content/analyzer/license/attribution risks by qualified pseudonymous roles. Re-run independently into validation output and compare canonical roots/counts. Reject gaps, overlap, stale identities, changed bundle/input bytes, bad/expired roles, incomplete progress or content/path/note/PII leakage.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync multilang validate-korean-bundle-review-batches --bundle-root .multilang/phase32/bundles/multilang-korean-frequency-v1 --build-result .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-build-result.json --receipts-root .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-receipts --expected-dispositions 5965 --expected-accepted 3000 --expected-rejected 2965 --expected-per-level 1000 --output .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-validation.json</automated></verify>
  <done>One safe exact aggregate proves complete qualified source review while granting no final approval.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-02 / SC-03 | Complete reviewed inventory evidence | COVERED |
| REQ | KFREQ-01 / KFREQ-02 / GLEX-02 / GMOR-01 | Review-bound identity and rejection accounting | COVERED |
| RESEARCH | Bounded immutable review | Receipts, disjoint coverage and safe aggregate | COVERED |
| CONTEXT | Qualified modernity/content/source review before final asset | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-23-01 | Tampering / Repudiation | receipt import | mitigate | Exact build/item hashes, disjoint IDs, atomic receipts and no-write replay. |
| T-32-23-02 | Information Disclosure | aggregate | mitigate | Content-free controlled fields and leakage rejection. |
</threat_model>
<high_leverage_review>Second pass: reconcile every batch/receipt/count/root, qualified role and unchanged bundle/private-input hash.</high_leverage_review>
<verification>Run both automated checks and compare aggregate/validation canonical roots.</verification>
<success_criteria>
- All review batches import exactly once.
- Aggregate covers all 5965 dispositions and risk classes.
- Outputs are private-safe and non-authorizing.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-23-SUMMARY.md` with receipt/aggregate hashes, counts, role coverage, invariance and bounded claim.</output>
