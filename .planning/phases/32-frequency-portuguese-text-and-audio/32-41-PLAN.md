---
phase: 32-frequency-portuguese-text-and-audio
plan: "41"
type: execute
wave: 53
runtime: opencode
assurance: self_checked
depends_on: [32-40]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-preflight.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authorization.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authorization.md.sha256
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authority-validation.json
autonomous: false
requirements: [KFREQ-01, KFREQ-02, KTXT-01, KAUD-01, GPRO-01, GAUD-01]
non_goals: ["No release-tree/pointer mutation, Git stage/commit, remote query/upload, delivery, publication, arbitrary destination selection, or Phase 34 closure."]
no_ui_proof_rationale: "Read-only exact local-release authorization checkpoint for optional commit/publication channels."
second_pass_required: true
closure_claim_limit: "Authorizes exact commit/publication channel scopes or explicit zero action; performs no external action, does not claim observed Anki acceptance, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Preflight rehashes current pointer, selected immutable tree, 13 members+manifest, safety/build evidence and actual Git/approved destination baseline read-only."
    - "Commit and publication decisions/tokens/allowlists are separate; source-derived eligibility is mechanically zero when exact-byte redistribution was denied, and absent token is a valid zero-action scope."
    - "Final-release authority accepts only fixed adapter/destination IDs and exact release-relative safe member allowlists, never arbitrary URL/module/command."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-preflight.json, provides: "Exact release/tree/safety and actual-channel baseline"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authorization.md, provides: "Separate exact commit/publication or zero-action scopes"}
  key_links:
    - {from: "pointer-selected immutable release and safety", to: "optional constrained delivery", via: "human separate channel tokens and exact allowlists"}
---

# Phase 32 Plan 41: Final Release Authorization Checkpoint

<objective>
Revalidate the exact current immutable release and obtain separate human commit/publication scopes—or explicit zero action—before delivery.

Purpose: A safe local release does not authorize Git, upload, publication or distribution.
Output: Read-only channel preflight and final-release authority.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-40-PLAN.md
@.multilang/releases/ko-frequency/current-release.json
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-41-01" type="auto">
  <name>Produce read-only pointer-selected release and channel baseline preflight</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-preflight.json
  </files>
  <action>Run `validate-korean-release-authorization` in preflight mode over exact immutable release directory, current pointer, final-promotion authority/sidecar, Plan 20 independent rights decision, Plan 24 exact-bundle disposition, release-safety and production-build-result. Rehash manifest/root/all 13 members and require every member locally safe. Derive separate eligible commit/publication allowlists containing only candidates with `safe_to_publish=true` and channel eligibility true. For every source-derived candidate, require Plan 20 exact-byte redistribution approval plus Plan 24 source-derived commit/publication eligibility; if either is denied, force that candidate's commit/publication eligibility false and exclude it mechanically while retaining `safe_for_local_release=true` when controls pass. Snapshot actual Git HEAD/index/worktree and only fixed registered destination metadata/counters without mutation or network unless a read-only destination adapter is explicitly available. Present separate commit/publication allowlist/token fields and explicit absent-token zero-action options; no arbitrary endpoint/module/command.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-preflight.json').read_text()); assert d['status']=='ready_for_release_decision' and d['release_member_count']==13 and d['manifest_valid'] is True and d['safety_passed'] is True and d['all_members_safe_for_local_release'] is True and d['nonpublishable_member_in_enabled_allowlist_count']==0 and (d['source_derived_commit_eligible_count']==d['source_derived_publication_eligible_count']==0 if not d['exact_byte_redistribution_approved'] else True) and d['git_mutation_count']==d['remote_mutation_count']==0 and d['release_unchanged'] is True"</automated></verify>
  <done>Human sees exact local release/safety and actual channel baselines without external action.</done>
</task>

<task id="32-41-02" type="checkpoint:user" gate="blocking">
  <name>Separately authorize and materialize commit, publication, both, or neither</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authorization.md
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authorization.md.sha256
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authority-validation.json
  </files>
  <action>Review only the exact release/safety/rights/channel baseline, select one option, and supply fixed token IDs/allowlists/registered destination IDs without secret values. Then write/sidecar kind=`final-release` with exact pointer/tree/manifest/member/safety/build/rights/preflight hashes and separate commit/publication token-presence, allowlist and fixed adapter/destination fields; validate in the isolated environment. Absent token encodes zero actions. An enabled scope may include only mechanically eligible candidates. If exact-byte redistribution or source-derived channel eligibility is denied, source-derived allowlists must be empty and no human option can override them; local release remains valid. Do not stage/commit Git, query/upload to a remote, mutate release/pointer state, or provide arbitrary URLs/modules/commands.</action>
  <decision>Select independent exact scopes for repository commit and fixed-destination publication, with absent token meaning provable zero action for that channel.</decision>
  <context>Each enabled channel binds current pointer/tree/root/manifest/member/safety/build/rights hashes, a fixed release-relative allowlist containing only `safe_to_publish=true` and channel-eligible candidates, and either structured Git action or one registered adapter/destination ID. Plan 20 exact-byte redistribution and Plan 24 source-derived channel eligibility must both be present for relevant source-derived members. Approved local-only members remain in the immutable local release but can never enter an enabled channel allowlist. No arbitrary URL/module/command/path, no mutable staging tree and no implication of Phase 34 observed Anki/generalized closure.</context>
  <options>
    <option id="local-only-zero-action"><name>Keep local release only</name><pros>Both channels have absent tokens and must prove zero Git/remote action.</pros><cons>No external commit or publication occurs.</cons></option>
    <option id="authorize-commit-only"><name>Authorize exact commit only</name><pros>Permits structured Git action for the exact allowlist.</pros><cons>Publication remains mechanically zero-action.</cons></option>
    <option id="authorize-publication-only"><name>Authorize fixed-destination publication only</name><pros>Permits only the exact registered destination/allowlist.</pros><cons>Git commit remains mechanically zero-action.</cons></option>
    <option id="authorize-both-separately"><name>Authorize both independent exact scopes</name><pros>Permits each channel under its own token/allowlist.</pros><cons>Neither scope may widen or substitute for the other.</cons></option>
  </options>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync multilang validate-korean-checkpoint-authority --authority .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authorization.md --sidecar .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authorization.md.sha256 --expected-kind final-release --output .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authority-validation.json</automated></verify>
  <done>The user independently authorizes exact eligible commit/publication scopes or explicit zero action, and authority is immutable before delivery.</done>
  <resume-signal>Select one option and supply only fixed token IDs, adapter/destination IDs and exact allowlist/root hashes—never token values, arbitrary URLs or commands.</resume-signal>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-02 / SC-05 / SC-09 / SC-10 | Separate safe optional release action | COVERED |
| REQ | KFREQ-01 / KFREQ-02 / KTXT-01 / KAUD-01 / GPRO-01 / GAUD-01 | Exact approved output/release boundaries | COVERED |
| RESEARCH | Publication checkpoint | Local artifacts distinct from commit/publication | COVERED |
| CONTEXT | Asset commit/local activation/publication separate; Phase 34 claims excluded | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-41-01 | Elevation | release channels | mitigate | Separate exact tokens/allowlists and absent-token zero action. |
| T-32-41-02 | Injection / SSRF | destination | mitigate | Fixed adapter/destination IDs; no arbitrary URL/module/command. |
| T-32-41-03 | Tampering | release drift | mitigate | Pointer/tree/member/safety rehash before authority and action. |
</threat_model>
<high_leverage_review>Second pass: verify exact pointer-selected release, safe member allowlists, channel/token separation, zero-action semantics and no external action in checkpoint.</high_leverage_review>
<verification>Run preflight and fixed-kind authority validation only; compare Git/release/remote baselines unchanged.</verification>
<success_criteria>
- Exact current immutable release is revalidated.
- Commit/publication scopes are independent or explicitly absent.
- No arbitrary/wider destination or action can be authorized.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-41-SUMMARY.md` with safe release/preflight/authority roots, channel scopes/allowlist counts and bounded claim.</output>
