---
phase: 32-frequency-portuguese-text-and-audio
plan: "12"
type: execute
wave: 24
runtime: opencode
assurance: self_checked
depends_on: [32-11]
files-modified:
  - tests/services/test_assemble_export_cards.py
  - tests/services/test_export_tabular_bundle.py
  - tests/services/test_export_anki_package.py
  - tests/services/test_generation_report.py
  - tests/cli/test_export_command.py
  - src/multilang/domain/exporting.py
  - src/multilang/services/assemble_export_cards.py
  - src/multilang/services/export_tabular_bundle.py
  - src/multilang/services/export_anki_package.py
  - src/multilang/services/generation_report.py
  - src/multilang/runtime.py
  - src/multilang/cli.py
autonomous: true
requirements: [KFREQ-02, KTXT-01, KAUD-01, GTXT-01, GAUD-01]
non_goals: ["No real output, production evidence/release action, partial Korean export, or observed Anki import/render/playback claim."]
no_ui_proof_rationale: "Archive/SQLite and generated-report structure only; Phase 34 owns observed behavior."
second_pass_required: true
closure_claim_limit: "Proves synthetic export eligibility, packaging and report wiring only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Korean final rows require exact bundle/level, current approved text and both current reviewed non-fallback assets before output."
    - "One package contains the canonical parent and exactly Level 1/2/3 children routed by persisted manifest level with stable schema/GUIDs."
    - "Explicit-database export creates APKG plus JSON/Markdown reports from one canonical evidence object and preserves prior outputs on failure."
  artifacts:
    - {path: src/multilang/services/assemble_export_cards.py, provides: "Exact final eligibility"}
    - {path: src/multilang/services/export_anki_package.py, provides: "Atomic parent/three-child package"}
    - {path: src/multilang/services/generation_report.py, provides: "Privacy-safe exact reports"}
  key_links:
    - {from: "persisted explicit level", to: "registered child deck", via: "no rank/item-key arithmetic"}
    - {from: "approved rows/media", to: "APKG and reports", via: "all-evidence prewrite gate and one canonical evidence object"}
---

# Phase 32 Plan 12: Export Eligibility, Child Packaging, and Reports

<objective>
Implement exact Korean export eligibility, atomic parent/three-child APKG packaging and explicit-database generation reports without changing learner identity.

Purpose: Real subdecks must derive from reviewed manifest membership and complete exact media, never arithmetic or partial output.
Output: Eligibility gates, package service and final export/report command.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-11-PLAN.md
@src/multilang/domain/exporting.py
@src/multilang/services/export_anki_package.py
<interfaces>
Create `export-korean-frequency-apkg --database DB --job-id ID --binding-receipt FILE --bundle-root ROOT --manifest-file FILE --output FILE --generation-report-json FILE --generation-report-markdown FILE --cards-per-level 1000 --expected-items 3000 --expected-word-assets 3000 --expected-sentence-assets 3000 --no-partial`.
</interfaces>
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-12-01" type="auto" tdd="true">
  <name>Gate export rows on explicit level and complete current evidence</name>
  <files>
    - MODIFY: tests/services/test_assemble_export_cards.py
    - MODIFY: tests/services/test_export_tabular_bundle.py
    - MODIFY: src/multilang/domain/exporting.py
    - MODIFY: src/multilang/services/assemble_export_cards.py
    - MODIFY: src/multilang/services/export_tabular_bundle.py
  </files>
  <behavior>
    - Rows require one exact bundle, unique rank/explicit level, current approved text and exact reviewed word/sentence assets.
    - Arithmetic/inferred level, partial override, stale/fallback media or missing review fails before output.
    - Existing field order, blank Image, tags and stable GUID input remain byte-identical.
  </behavior>
  <action>Add internal level/bundle/text-review/two-asset hashes without changing learner fields or GUID identity. Require one immutable bundle and exact explicit-level counts in final mode; make partial flags ineffective for Korean final and preserve all existing export modes. Test mismatched level/rank, stale review/media and identity goldens.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_assemble_export_cards.py -k 'korean or guid or field or level or audio or partial' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_export_tabular_bundle.py -k 'korean or field or level' -q</automated></verify>
  <done>Korean rows are eligible only from exact reviewed manifest/text/audio evidence with unchanged learner identity.</done>
</task>

<task id="32-12-02" type="auto" tdd="true">
  <name>Package one parent and three registered child decks atomically</name>
  <files>
    - MODIFY: tests/services/test_export_anki_package.py
    - MODIFY: src/multilang/services/export_anki_package.py
  </files>
  <behavior>
    - Package has one explicit parent and Level 1/2/3 children using canonical registry keys.
    - Archive inspection verifies deck/model IDs, card routing, fields, GUIDs, tags and media before replacement.
    - Failed validation preserves existing destination bytes and leaves no owned temporary.
  </behavior>
  <action>Implement explicit parent/children construction. Run registry guard and complete row/media validation before temp creation; create package, reopen collection/archive/media map, verify all structure, then atomically replace. Reject unknown/inferred levels, ID collisions and any required GUID-input change; preserve existing-mode topology.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_export_anki_package.py -k 'korean or parent or child or atomic or guid or media' -q</automated></verify>
  <done>Manifest-routed parent/three-child packaging is complete, verified and atomic.</done>
</task>

<task id="32-12-03" type="auto" tdd="true">
  <name>Expose explicit-database export with exact JSON and Markdown reports</name>
  <files>
    - MODIFY: tests/services/test_generation_report.py
    - MODIFY: tests/cli/test_export_command.py
    - MODIFY: src/multilang/services/generation_report.py
    - MODIFY: src/multilang/runtime.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Command requires explicit production DB, job, bundle, manifest, receipt, APKG and both report paths.
    - APKG and reports derive from the same reloaded evidence and bind exact package hash/counts.
    - Reports expose only hashes, controlled IDs, counts, distributions and missing-value denominators.
  </behavior>
  <action>Implement the exact command. Extend reports with bundle/version/hash, 1000/1000/1000 counts, lexical/rejection accounting, adaptive distributions, 2+1 histories, review counts, route/retry/cache/token/cost/latency denominators, 3000/3000 audio approval and zero fallback. Open only explicit database/paths, stage all three outputs, derive JSON/Markdown from one canonical evidence object, verify APKG hash and preserve all prior destination bytes on failure. Exclude learner text, prompts, payloads, credentials, notes and private paths.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_generation_report.py -k 'korean or denominator or privacy or exact_apkg' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_export_command.py -k 'korean_frequency or explicit_database or generation_report or no_partial or registry or output' -q</automated></verify>
  <done>One explicit producer creates exact APKG and named reports from the same privacy-safe evidence.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-02 / SC-05 / SC-10 | Three real subdecks with complete reviewed media | COVERED |
| REQ | KFREQ-02 / KTXT-01 / KAUD-01 / GTXT-01 / GAUD-01 | Eligibility, packaging and reports | COVERED |
| RESEARCH | Explicit level/stable note identity | Parent/children, archive inspection, atomic replace | COVERED |
| CONTEXT | Fields/Image/tags/GUID stable; no partial; Phase 34 boundary | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-12-01 | Tampering | eligibility/routing | mitigate | Reload exact review/audio/bundle evidence and explicit manifest level. |
| T-32-12-02 | Injection | fields/media | mitigate | Existing contextual sanitization and approved basenames. |
| T-32-12-03 | Race Condition / Integrity | output | mitigate | Prewrite validation, verified temp and atomic replacement. |
</threat_model>
<high_leverage_review>Second pass: inspect exact eligibility, registry/level routing, GUID/schema stability, media completeness, report privacy and atomic rollback.</high_leverage_review>
<verification>Run all three focused task commands; no real output or observed Anki claim.</verification>
<success_criteria>
- Eligibility is exact and non-partial.
- Parent plus three child decks preserve schema/GUID/media.
- APKG and reports share one exact evidence object.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-12-SUMMARY.md` with eligibility matrix, package audit, report contract, rollback tests and bounded self-checked claim.</output>
