---
phase: 32-frequency-portuguese-text-and-audio
plan: "10"
type: execute
wave: 22
runtime: opencode
assurance: self_checked
depends_on: [32-09]
files-modified:
  - tests/services/test_anki_id_registry.py
  - tests/services/test_export_anki_package.py
  - tests/services/test_latin_export.py
  - tests/services/test_russian_phoneme_deck.py
  - tests/services/test_phoneme_deck.py
  - src/multilang/services/anki_id_registry.py
  - src/multilang/services/export_anki_package.py
  - src/multilang/services/latin_export.py
  - src/multilang/services/russian_phoneme_deck.py
  - src/multilang/services/phoneme_deck.py
autonomous: true
requirements: [KFREQ-02]
non_goals: ["No Korean frequency packaging, scanner/prewrite enforcement, ID replacement, GUID change, or observed Anki claim."]
no_ui_proof_rationale: "Numeric ID registry and family migrations only."
second_pass_required: true
closure_claim_limit: "Proves registry declarations and core/Latin/phoneme migration identity only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "One typed registry owns every production model/deck numeric ID without changing existing values."
    - "Korean frequency reserves model 1762801101, parent 1762801102 and levels 1762801103-1762801105 without packaging."
    - "Core, Latin, Russian and phoneme consumers preserve IDs, topology, fields and GUID snapshots."
  artifacts:
    - {path: src/multilang/services/anki_id_registry.py, provides: "Sole typed numeric Anki ID declaration source"}
  key_links:
    - {from: "production deck/model constructors", to: "registry keys", via: "typed lookup with golden preserved values"}
---

# Phase 32 Plan 10: Canonical Anki ID Registry and First Family Migrations

<objective>
Declare the canonical collision-free ID registry, then migrate core/Latin and Russian/phoneme families without identity drift.

Purpose: Korean parent/child IDs require one global authority while every shipped numeric value and GUID remains stable.
Output: Registry plus first two family migrations and goldens.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-09-PLAN.md
@src/multilang/services/export_anki_package.py
@src/multilang/services/latin_export.py
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-10-01" type="auto" tdd="true">
  <name>Declare the canonical registry and lock the complete numeric baseline</name>
  <files>
    - CREATE: tests/services/test_anki_id_registry.py
    - CREATE: src/multilang/services/anki_id_registry.py
  </files>
  <behavior>
    - Every current production ID appears once with typed family, role, kind and exact numeric value.
    - Korean frequency registrations use model 1762801101, parent 1762801102 and levels 1762801103-1762801105.
    - Same-kind duplicates, model/deck cross-kind collisions and unresolved dynamic declarations stop rather than inventing replacements.
  </behavior>
  <action>Inventory source/config/data declarations first and freeze exact values as goldens. Define typed keys/registrations for generic/manual/highlight/Mandarin, Latin, Russian/Polish/Greek phoneme, Japanese frequency/kana, Korean foundation and Korean frequency. Preserve all existing numbers. Stop and report any baseline conflict or unresolved use; do not choose a substitute.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_anki_id_registry.py -k 'baseline or declaration or duplicate or cross_kind or korean_frequency' -q</automated></verify>
  <done>One collision-free registry contains all current values unchanged plus fixed Korean frequency registrations.</done>
</task>

<task id="32-10-02" type="auto" tdd="true">
  <name>Migrate core and Latin consumers without identity drift</name>
  <files>
    - MODIFY: tests/services/test_anki_id_registry.py
    - MODIFY: tests/services/test_export_anki_package.py
    - MODIFY: tests/services/test_latin_export.py
    - MODIFY: src/multilang/services/export_anki_package.py
    - MODIFY: src/multilang/services/latin_export.py
  </files>
  <behavior>
    - Core and Latin constructors resolve typed keys instead of local numeric declarations.
    - Existing model/deck values, names, topology, fields, media and GUID snapshots remain identical.
    - Compatibility exports may alias registry values but declare no numbers.
  </behavior>
  <action>Migrate generic package and dedicated Latin constructors/helpers to typed registry resolution. Reject unchecked raw integer inputs and preserve all output/golden/GUID behavior exactly. Add intermediate tests so the single registry remains atomic while family migration is serial.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_export_anki_package.py -k 'registry or id or guid' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_latin_export.py -k 'id or registry or guid' -q</automated></verify>
  <done>Core and Latin use canonical IDs with no output or GUID drift.</done>
</task>

<task id="32-10-03" type="auto" tdd="true">
  <name>Migrate Russian and shared phoneme consumers without identity drift</name>
  <files>
    - MODIFY: tests/services/test_anki_id_registry.py
    - MODIFY: tests/services/test_russian_phoneme_deck.py
    - MODIFY: tests/services/test_phoneme_deck.py
    - MODIFY: src/multilang/services/russian_phoneme_deck.py
    - MODIFY: src/multilang/services/phoneme_deck.py
  </files>
  <behavior>
    - Russian, Polish, Greek and shared phoneme paths resolve registered typed keys.
    - Existing IDs, names, fields, topology, media and GUID goldens remain identical.
    - Dynamic helpers reject unregistered integer input.
  </behavior>
  <action>Migrate Russian and shared phoneme constructors/helpers family-by-family against the same registry. Preserve current values and all snapshots, reject ad hoc integers, and run intermediate registry collision tests before the next family plan.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_russian_phoneme_deck.py -k 'id or registry or guid' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_phoneme_deck.py -k 'id or registry or guid' -q</automated></verify>
  <done>Russian and phoneme families are registry-backed with byte-stable identity.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-02 | Collision-free real-child-deck identity foundation | COVERED |
| REQ | KFREQ-02 | Fixed parent/child/model identities | COVERED |
| RESEARCH | Stable IDs/GUIDs | One registry and unchanged consumers | COVERED |
| CONTEXT | Preserve IDs, GUID formula, fields and existing modes | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-10-01 | Tampering | registry baseline | mitigate | Complete golden inventory and stop-on-conflict policy. |
| T-32-10-02 | Spoofing | consumer lookup | mitigate | Typed key resolution and rejection of raw integers. |
</threat_model>
<high_leverage_review>Second pass: compare every migrated number and GUID golden; confirm no family-local numeric declaration remains.</high_leverage_review>
<verification>Run all three task commands; this plan must not package a Korean frequency deck.</verification>
<success_criteria>
- One registry owns all current and Korean frequency IDs.
- Core/Latin/Russian/phoneme identities are unchanged.
- Registry conflicts stop without invented IDs.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-10-SUMMARY.md` with ID inventory, first-family migration matrix, goldens and bounded self-checked claim.</output>
