---
phase: 32-frequency-portuguese-text-and-audio
plan: "06"
type: execute
wave: 18
runtime: opencode
assurance: self_checked
depends_on: [32-05]
files-modified:
  - tests/services/test_korean_text_quality.py
  - tests/services/test_provider_text_adapters.py
  - tests/domain/test_korean_provider_policy.py
  - tests/repositories/test_provider_call_log_repository.py
  - src/multilang/domain/text_quality.py
  - src/multilang/services/korean_text_quality.py
  - src/multilang/services/provider_text_adapters.py
  - src/multilang/domain/korean_provider.py
  - src/multilang/settings.py
autonomous: true
requirements: [KFREQ-03, KTXT-01, GTXT-01, GPRO-01]
non_goals: ["No live provider/model choice, call, budget approval, content approval, fallback, or reviewer authority inference."]
no_ui_proof_rationale: "Offline review, pt-BR, and provider policy contracts only."
second_pass_required: true
closure_claim_limit: "Proves immutable review schemas, pt/PT-BR separation, and bounded no-secret provider policy only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Text review decisions bind exact job/run/item/candidate/policy/root hashes, qualified roles, coverage and controlled outcomes without mutation power."
    - "Canonical project language remains pt while only provider/editorial boundaries use PT-BR."
    - "Every provider operation has an explicit route-or-disabled state, budgets, attempts, cache identity, fallback none, and privacy-safe denominators."
  artifacts:
    - {path: src/multilang/domain/text_quality.py, provides: "Immutable text review decision/rejection contracts"}
    - {path: src/multilang/services/provider_text_adapters.py, provides: "KO→PT-BR boundary and strict output handling"}
    - {path: src/multilang/domain/korean_provider.py, provides: "Hashable task route/budget policy"}
  key_links:
    - {from: "machine quality evidence", to: "later review application", via: "separate immutable qualified review decision"}
    - {from: "canonical pt", to: "DeepL PT-BR", via: "provider-only code mapping and editorial policy ID"}
---

# Phase 32 Plan 06: Review Contracts, pt-BR Policy, and Provider Controls

<objective>
Define immutable review decisions, enforce canonical-pt/pt-BR output policy and deterministic scoring, and establish explicit bounded provider routes/telemetry.

Purpose: Providers and machine gates supply candidates and metrics, never identity, approval, or implicit budget/fallback policy.
Output: Review schemas, PT-BR adapter gates, and no-secret provider policy contracts.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-05-PLAN.md
@src/multilang/services/provider_text_adapters.py
@src/multilang/repositories/provider_call_log_repository.py
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-06-01" type="auto" tdd="true">
  <name>Define strict immutable text-review decision and rejection contracts</name>
  <files>
    - MODIFY: tests/services/test_korean_text_quality.py
    - MODIFY: src/multilang/domain/text_quality.py
    - MODIFY: src/multilang/services/korean_text_quality.py
  </files>
  <behavior>
    - Decisions bind exact production/job/run/item/candidate/policy/root hashes, qualified role, coverage, outcome and rejection codes.
    - Missing/extra/mutable/private fields, stale identity, invalid role, contradiction, or model/machine-authored approval fails.
    - Contracts grant no import, DB mutation, promotion, export, or release power.
  </behavior>
  <action>Add frozen strict review-decision, qualification, coverage, rejection, and bounded batch-entry contracts without free-form notes or private paths. Mutation-test stale/extra/contradictory fields and prove no model, judge, or machine gate can construct qualified acceptance. Define schemas only; executable import/application is owned later.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_text_quality.py -k 'review_decision or rejection or role or coverage or immutable or authority_separation' -q</automated></verify>
  <done>Qualified review is an exact immutable evidence type with no mutation or promotion power.</done>
</task>

<task id="32-06-02" type="auto" tdd="true">
  <name>Keep canonical pt while enforcing Brazilian-Portuguese quality and deterministic scoring</name>
  <files>
    - MODIFY: tests/services/test_korean_text_quality.py
    - MODIFY: tests/services/test_provider_text_adapters.py
    - MODIFY: src/multilang/services/korean_text_quality.py
    - MODIFY: src/multilang/services/provider_text_adapters.py
  </files>
  <behavior>
    - Domain, tags, persistence and cache identity remain pt; the translation adapter alone maps KO to PT-BR.
    - Wrong sense/context, contradiction, English leakage, isolated-word translation, provider errors, or unsafe markup fails.
    - Only complete hard-gate passers score; candidate timing/order permutations choose the same hash/ordinal winner.
  </behavior>
  <action>Map `KO`→`PT-BR` only inside the provider adapter and persist canonical `pt` plus a versioned Brazilian-Portuguese editorial policy identifier. Strictly validate structured provider output, context-escape selected fields, and reject invented context, unsafe HTML, contradictory translation and regional identity drift. Among hard-gate passers minimize incidental novelty, tie-breaking by canonical candidate hash then ordinal.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_text_quality.py -k 'pt_br or translation or score or tie or hard_gate' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_provider_text_adapters.py -k 'korean or PT_BR or canonical_pt' -q</automated></verify>
  <done>pt/PT-BR identity is separated and deterministic scoring never rescues failed quality.</done>
</task>

<task id="32-06-03" type="auto" tdd="true">
  <name>Define immutable routes, budgets, attempts, cache, and privacy-safe telemetry</name>
  <files>
    - CREATE: tests/domain/test_korean_provider_policy.py
    - MODIFY: tests/repositories/test_provider_call_log_repository.py
    - CREATE: src/multilang/domain/korean_provider.py
    - MODIFY: src/multilang/settings.py
  </files>
  <behavior>
    - Definition, sentence generation, repair, translation, optional judge, catalog, word audio and sentence audio each have an explicit provider/model-or-disabled route.
    - Every route binds attempts, token/cost/latency/batch/concurrency limits, timeout, policy version and fallback none.
    - Telemetry stores controlled hashes/metrics and explicit availability denominators, never prompts, outputs, roots, credentials, private context, or raw exceptions.
  </behavior>
  <action>Add strict frozen route, budget, attempt, cache and result-summary models. Environment values may propose but never approve policy. Enforce per-call/item/job ceilings before work, same-route bounded transport retry, cache-hit separation, and redacted hash/metric telemetry. Test cost/resource exhaustion, missing metadata denominators, forbidden fallback, credential/name separation and all sensitive-content exclusions.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/domain/test_korean_provider_policy.py -k 'route or budget or denominator or privacy or fallback' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/repositories/test_provider_call_log_repository.py -k 'route or cache or token or cost or latency or redaction' -q</automated></verify>
  <done>GPRO-01 has one explicit bounded no-fallback policy and content-free observable attempt vocabulary.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-04 / SC-08 / SC-09 | pt-BR quality, bounded candidate policy, observable providers | COVERED |
| REQ | KFREQ-03 / KTXT-01 / GTXT-01 / GPRO-01 | Review, translation, scoring and provider control | COVERED |
| RESEARCH | Review separation / PT-BR / explicit routes | Strict output, canonical pt, bounded no-fallback policy | COVERED |
| CONTEXT | Qualified review, pt-BR under pt, exact routes/budgets external | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-06-01 | Elevation / Repudiation | review schema | mitigate | Exact immutable hashes/roles/coverage and no mutation powers. |
| T-32-06-02 | Injection / Misinformation | provider output | mitigate | Strict schema, contextual escaping, immutable identity, human authority separation. |
| T-32-06-03 | Information Disclosure / DoS | provider policy | mitigate | Sensitive-field exclusion and explicit attempt/token/cost/time ceilings. |
</threat_model>
<high_leverage_review>Second pass: inspect review authority separation, pt/PT-BR identity, strict untrusted output handling, denominators, budgets, and fallback none.</high_leverage_review>
<verification>Run all task commands offline with deterministic provider fixtures.</verification>
<success_criteria>
- Review contracts cannot self-authorize mutation.
- Portuguese output follows pt-BR policy without creating a second product language.
- Every provider route is explicit, bounded, observable, and private.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-06-SUMMARY.md` with review schema, pt/PT-BR matrix, route/budget table, privacy tests, and bounded self-checked claim.</output>
