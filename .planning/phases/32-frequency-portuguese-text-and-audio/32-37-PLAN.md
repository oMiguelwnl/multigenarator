---
phase: 32-frequency-portuguese-text-and-audio
plan: "37"
type: execute
wave: 49
runtime: opencode
assurance: self_checked
depends_on: [32-36]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-text-review-ledger/
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-audio-review-ledger/
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-import-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-aggregate.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-state.json
autonomous: true
requirements: [KFREQ-03, KTXT-01, KAUD-01, GTXT-01, GPRO-01, GAUD-01]
non_goals: ["No review application, DB content/audio status mutation, further repair/synthesis, promotion, APKG build, release, delivery, or publication."]
no_ui_proof_rationale: "Final changed-review import and complete read-only aggregate only."
second_pass_required: true
closure_claim_limit: "Proves a complete current zero-rejection aggregate or blocks; it grants no promotion/build/release authority, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Only changed-item batches append to private final ledgers; zero-change manifests cause no new entries and preserve exact continuity."
    - "Final aggregate resolves initial unchanged plus final changed ledger entries against current hashes into full 3000/6000 coverage."
    - "Any final rejection, gap, stale hash, role/coverage failure or drift blocks before promotion authority."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-aggregate.json, provides: "Current complete zero-rejection review root or blocked result"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-state.json, provides: "Read-only exact current review/production state"}
  key_links:
    - {from: "initial unchanged and final changed ledger entries", to: "current complete aggregate", via: "exact identity/request/byte continuity reconciliation"}
---

# Phase 32 Plan 37: Final Review Ledger Import and Complete Aggregate

<objective>
Append only changed-item final decisions, combine them with exact unchanged ledger decisions, and require one current complete zero-rejection aggregate while canonical status remains pending.

Purpose: Final promotion can bind only current reviewed content/bytes, never stale or partially repaired evidence.
Output: Final append-only ledger/import result, aggregate and read-only state proof.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-36-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-preflight.json
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-37-01" type="auto">
  <name>Append only exact changed-item final reviews</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-text-review-ledger/
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-audio-review-ledger/
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-import-result.json
  </files>
  <action>For each nonempty final manifest, invoke the existing text/audio import commands against exact current DB/job/run/full/remediation state, final preflight, role registry/sidecar and completed CAS progress. Require batch identities equal the declared changed sets exactly; atomically append private chain-linked entries and content-free receipts, applying no canonical review state. For zero-batch manifests, validate no-remediation and initial aggregate continuity and append nothing. Reject old/current mismatch, extra/omitted item, broken previous-entry root, unqualified role, stale byte/profile/progress or replay divergence.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-import-result.json').read_text()); assert d['status'] in {'appended_changed_reviews','validated_no_change'} and d['imported_text_count']==d['expected_changed_text_count'] and d['imported_word_count']==d['expected_changed_word_count'] and d['imported_sentence_count']==d['expected_changed_sentence_count'] and d['canonical_review_mutation_count']==0 and d['ledger_chains_valid'] is True"</automated></verify>
  <done>Only exact changed decisions are appended, or no-change continuity is validated with zero new ledger entries.</done>
</task>

<task id="32-37-02" type="auto">
  <name>Compute the current complete final review aggregate</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-aggregate.json
  </files>
  <action>Run `validate-korean-production-review-batches` over current DB/files, initial/final review ledgers, remediation ledger/result, role registry/progress roots, initial aggregate and final preflight. Require exactly one effective current decision per 3000 text and all 6000 integrity identities, minimum heard strata/all risks over unchanged plus every changed byte, qualified roles and zero current rejections. Reject stale superseded entries instead of double-counting; require every ledger chain valid and every canonical review status still pending; emit safe roots/counts and no mutation.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-aggregate.json').read_text()); assert d['status']=='passed_zero_rejections' and d['text_covered_count']==3000 and d['word_integrity_covered_count']==3000 and d['sentence_integrity_covered_count']==3000 and d['rejected_text_count']==d['rejected_audio_count']==0 and d['all_current_risk_cases_heard'] is True and d['ledger_chains_valid'] is True and d['canonical_pending_text_count']==3000 and d['canonical_pending_audio_count']==6000"</automated></verify>
  <done>One exact zero-rejection aggregate covers every current text/audio identity and required human evidence.</done>
</task>

<task id="32-37-03" type="auto">
  <name>Revalidate current review and production state without application</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-state.json
  </files>
  <action>Reload all current rows/files and recompute production-run plus final-review/ledger roots, changed/unchanged continuity, 2+1 histories, exact profile/requests/bytes, attempts/budgets/denominators and zero fallback. Confirm all 3000 text and 6000 audio canonical statuses remain pending, no further repair budget was consumed, and validator changes no DB/file/input byte. Write content-free current prestate for the promotion checkpoint.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-state.json').read_text()); assert d['status']=='ready_for_final_promotion_decision' and d['review_aggregate_zero_rejections'] is True and d['promotion_mutation_count']==0 and d['canonical_pending_text_count']==3000 and d['canonical_pending_audio_count']==6000 and d['fallback_attempt_count']==0 and d['inputs_unchanged'] is True"</automated></verify>
  <done>Exact current zero-rejection state is ready for a separate human promotion decision.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-04 / SC-05 / SC-08 / SC-10 | Current fully reviewed text/audio | COVERED |
| REQ | KFREQ-03 / KTXT-01 / KAUD-01 / GTXT-01 / GPRO-01 / GAUD-01 | Final current-hash review evidence | COVERED |
| RESEARCH | Exact hash-bound human approval | Changed ledger-entry supersession and unchanged continuity | COVERED |
| CONTEXT | One remediation cycle, all current outputs reviewed, promotion separate | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-37-01 | Spoofing / Repudiation | stale review reuse | mitigate | Current hashes supersede changed ledger entries and prove unchanged continuity. |
| T-32-37-02 | Elevation | aggregate/import | mitigate | No application path; zero-rejection state only informs later checkpoint. |
</threat_model>
<high_leverage_review>Second pass: reconcile changed appends, superseded ledger entries, full current coverage/heard risks, zero rejections and no canonical status mutation.</high_leverage_review>
<verification>Run all three exact checks and rerun validators read-only.</verification>
<success_criteria>
- Changed reviews import exactly or no-change is proven.
- Final aggregate is complete, current and zero-rejection.
- Promotion remains absent pending separate authority.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-37-SUMMARY.md` with safe current aggregate/state roots, coverage/counts, continuity and bounded claim.</output>
