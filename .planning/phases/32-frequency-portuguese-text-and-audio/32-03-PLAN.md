---
phase: 32-frequency-portuguese-text-and-audio
plan: "03"
type: execute
wave: 15
runtime: opencode
assurance: self_checked
depends_on: [32-02]
files-modified:
  - tests/repositories/test_job_repository.py
  - tests/repositories/test_lexical_repository.py
  - tests/repositories/test_phase32_text_audio_evidence.py
  - src/multilang/domain/korean.py
  - src/multilang/domain/lexicon.py
  - src/multilang/domain/text_quality.py
  - src/multilang/domain/audio.py
  - src/multilang/repositories/job_repository.py
  - src/multilang/repositories/lexical_repository.py
  - src/multilang/repositories/text_repository.py
  - src/multilang/repositories/audio_repository.py
  - src/multilang/services/authority_locator.py
autonomous: true
requirements: [KFREQ-01, GLEX-02, GPRO-01, KAUD-01, GAUD-01]
non_goals: ["No production database migration, provider call, raw-path persistence, human approval inference, or GUID change."]
no_ui_proof_rationale: "Repository and staged-CAS contracts only."
second_pass_required: true
closure_claim_limit: "Proves disposable-database CAS and evidence round trips only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Pilot-base, pilot-audio, and full authority bind exact content plus non-sensitive canonical locator hashes and are exact-retry no-ops."
    - "The genuine Phase 31 pointer/receipt/selected-snapshot tuple is rehashed and persisted without inventing an authority file."
    - "Lexical, text, and audio machine/review evidence reload strictly while legacy rows remain readable."
  artifacts:
    - {path: src/multilang/services/authority_locator.py, provides: "Sole locator canonicalization helper"}
    - {path: src/multilang/repositories/job_repository.py, provides: "Transactional staged CAS and attempt guards"}
    - {path: src/multilang/repositories/lexical_repository.py, provides: "Source-backed identity round trip"}
    - {path: src/multilang/repositories/audio_repository.py, provides: "Exact catalog/request/artifact/review round trip"}
  key_links:
    - {from: "explicit authority files and roots", to: "job stage", via: "descriptor rehash plus atomic insert-or-compare"}
    - {from: "job stage", to: "provider attempt insert", via: "zero-attempt drift guard"}
---

# Phase 32 Plan 03: Locator-Bound CAS and Evidence Repositories

<objective>
Implement the one canonical locator hash, transactional staged job authority, and strict lexical/text/audio evidence round trips.

Purpose: Every later adapter must consume exact reloaded authority and separately persisted machine/review evidence.
Output: CAS helper/repository and complete evidence mappings.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-02-PLAN.md
@src/multilang/repositories/job_repository.py
@src/multilang/db/models.py
<interfaces>
`canonical_authority_locator_sha256(path)` rejects missing, traversal, symlink/reparse, or descriptor-drifted inputs; anchors to the validated repository root; NFC/normcase canonicalizes separators; hashes an 8-byte length prefix plus UTF-8 locator bytes; and immediately discards the raw locator. No later plan may redefine it.
</interfaces>
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-03-01" type="auto" tdd="true">
  <name>Implement canonical locator hashing, staged job CAS, exact retry, and attempt guards</name>
  <files>
    - MODIFY: tests/repositories/test_job_repository.py
    - MODIFY: src/multilang/domain/korean.py
    - MODIFY: src/multilang/repositories/job_repository.py
    - CREATE: src/multilang/services/authority_locator.py
  </files>
  <behavior>
    - Pilot-base binds genuine Phase 31 pointer locator/content, validation receipt, selected manifest/root, bundle locator/content, source authorities, policy, and pilot authority.
    - Pilot-audio adds catalog locator/content and profile-sample authority; full additionally binds distinct provider-review and heard-review authority.
    - Exact tuple retry performs no UPDATE; any locator/content/order/stage drift fails before provider-attempt insertion.
  </behavior>
  <action>Write locator, concurrency, and no-update tests first. Implement the sole locator helper and atomic uniqueness/version-guarded insert-or-compare for `bind_execution_authority`, `bind_audio_authority`, and strict reload. Require explicit `data/korean_foundations/active-foundations.json`, exact validation receipt, expected pointer-selected manifest/root hashes, and genuine pathless `korean-foundations verify-active --expected-receipt-sha256`; persist only locator/content hashes. Compare every source, bundle, catalog, policy, provider-review, profile-sample, heard-review, stage, and prestate fact. Operation guards require base for pilot text/catalog, audio for samples, and full for production. Test unchanged row version/timestamp and zero attempt delta on exact retry or any one-fact drift.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/repositories/test_job_repository.py -k 'execution_authority or locator or compare_and_set or exact_retry or attempt_guard' -q</automated></verify>
  <done>Staged authority is locator/content-bound, transactional, private, exact-retry stable, and dominant over attempts.</done>
</task>

<task id="32-03-02" type="auto" tdd="true">
  <name>Round-trip complete lexical authority without identity drift</name>
  <files>
    - MODIFY: tests/repositories/test_lexical_repository.py
    - MODIFY: src/multilang/domain/lexicon.py
    - MODIFY: src/multilang/repositories/lexical_repository.py
  </files>
  <behavior>
    - Trusted source/version/POS/sense/confidence/license/curation evidence survives commit, expire, and reload.
    - Rank/level/bundle agree with the embedded Korean identity.
    - Historical non-Korean payloads and current GUID inputs remain unchanged.
  </behavior>
  <action>Add strict optional lexical provenance fields and explicit repository mappings. Reconstruct typed evidence on reload, require agreement with `KoreanLexicalIdentity`, rank, explicit manifest level, analyzer fingerprint, manifest and bundle hashes, and reject malformed/mismatched storage. Do not reanalyze or let provider output rewrite identity, and do not change identity columns or GUID inputs.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/repositories/test_lexical_repository.py -k 'korean_frequency or provenance or identity or non_korean' -q</automated></verify>
  <done>Complete source-backed lexical authority reloads exactly without replacing identity or legacy behavior.</done>
</task>

<task id="32-03-03" type="auto" tdd="true">
  <name>Round-trip text and audio machine, artifact, and review evidence separately</name>
  <files>
    - CREATE: tests/repositories/test_phase32_text_audio_evidence.py
    - MODIFY: src/multilang/domain/text_quality.py
    - MODIFY: src/multilang/domain/audio.py
    - MODIFY: src/multilang/repositories/text_repository.py
    - MODIFY: src/multilang/repositories/audio_repository.py
  </files>
  <behavior>
    - Candidate/adaptive/judge/machine evidence cannot self-approve human review.
    - Catalog/profile/request/artifact/integrity/review evidence reloads exactly for separate word and sentence assets.
    - Legacy missing evidence remains readable but never satisfies Korean final gates.
  </behavior>
  <action>Add strict text selection/adaptive/editorial/judge/human-review evidence and audio catalog/profile/request/artifact/integrity/human-review evidence. Keep synthesized-pending distinct from approved, map each field explicitly, and revalidate on reload. Never persist provider payloads, prompts, private roots, reviewer notes, credentials, or raw exceptions.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/repositories/test_phase32_text_audio_evidence.py -k 'text or audio or selection or adaptive or catalog or profile or artifact or review or legacy' -q</automated></verify>
  <done>Text/audio machine, artifact, and qualified-review states remain separate and durable.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-05 / SC-06 / SC-09 / SC-10 | Exact staged authority and durable evidence | COVERED |
| REQ | KFREQ-01 / GLEX-02 / GPRO-01 / KAUD-01 / GAUD-01 | CAS and repository mappings | COVERED |
| RESEARCH | Persistence / atomic CAS | Locator secrecy, strict reload, machine/review separation | COVERED |
| CONTEXT | Genuine Phase 31 tuple, stable identity, private telemetry | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-03-01 | Spoofing / Tampering | staged CAS | mitigate | Genuine pointer/receipt/snapshot plus locator/content hashes and transactional compare-and-set. |
| T-32-03-02 | Information Disclosure | repositories | mitigate | Typed hash/metric allowlist and prohibited-content tests. |
| T-32-03-03 | Injection | persistence | mitigate | Parameterized SQLAlchemy operations and strict reconstruction. |
</threat_model>
<high_leverage_review>Second pass: inspect locator canonicalization, PostgreSQL/SQLite insert-or-compare, exact-retry no-UPDATE, and every privacy exclusion.</high_leverage_review>
<verification>Run all task commands against disposable DBs; verify no raw authority locator is persisted.</verification>
<success_criteria>
- Exact staged authority dominates every attempt.
- Lexical identity/provenance reloads without drift.
- Text/audio evidence remains typed, separate, and privacy-safe.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-03-SUMMARY.md` with locator algorithm, CAS matrix, repository round trips, privacy scan, and bounded self-checked claim.</output>
