---
phase: 32-frequency-portuguese-text-and-audio
plan: "42"
type: execute
wave: 54
runtime: opencode
assurance: self_checked
depends_on: [32-41]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-action.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-validation.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-retry-validation.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/phase32-release-result.json
autonomous: true
requirements: [KFREQ-01, KFREQ-02, KTXT-01, KAUD-01, GPRO-01, GAUD-01]
non_goals: ["No unlisted Git path, shell command, arbitrary remote/URL/module, mutable staging delivery, content/build/release-tree change, provider call, or Phase 34 observed/generalized closure claim."]
no_ui_proof_rationale: "Constrained optional delivery and independent actual-state validation only."
second_pass_required: true
closure_claim_limit: "Proves exactly authorized commit/publication action or corresponding zero action and retry idempotence; Phase 34 still owns observed Anki/generalized milestone closure, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Delivery revalidates exact authority and pointer-selected immutable release immediately before each enabled channel."
    - "Git uses fixed structured argv with shell=False and publication uses only the fixed registered adapter/destination over exact safe allowlists; absent tokens perform zero action."
    - "Independent validator inspects actual Git objects/remote checksums or mechanical no-action baselines, and exact retry creates no duplicate or drift."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-action.json, provides: "Content-free exact action/zero-action result"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-validation.json, provides: "Independent actual-state proof"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/phase32-release-result.json, provides: "Bounded final Phase 32 release outcome"}
  key_links:
    - {from: "final-release authority and immutable current tree", to: "actual Git/fixed destination or zero action", via: "constrained executor then independent actual-state/idempotence validator"}
---

# Phase 32 Plan 42: Constrained Delivery and Independent Actual-State Proof

<objective>
Execute only the separately authorized fixed delivery channels, independently prove actual or zero side effects, and verify exact retry idempotence.

Purpose: Action labels cannot substitute for actual Git/remote/no-action evidence, and local-only completion remains valid.
Output: Action, independent validation, retry proof and bounded Phase 32 release result.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-41-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-release-authorization.md
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-42-01" type="auto">
  <name>Execute only exact token-authorized fixed delivery channels</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-action.json
  </files>
  <action>Invoke `execute-korean-release-delivery` with exact immutable release directory/current pointer, final-release authority/sidecar, Plan 20/24 rights decisions, release safety/build result and preflight. Immediately rehash all bindings before each channel and prove every enabled allowlisted member has `safe_to_publish=true` plus exact channel eligibility; approved local-only members stay in the immutable release but are never delivered. If exact-byte redistribution was denied, require zero source-derived members and zero source-derived actions in both channels regardless of token text. For commit token, use only fixed structured Git argv with `shell=False` and exact allowlisted release-relative members; no shell or user command. For publication token, invoke only the registered fixed adapter/destination ID with exact publishable member allowlist. If a token is absent, perform zero action for that channel. Snapshot actual Git/destination state/counters before/after and write safe hashes/IDs/counts only; partial/divergent prestate fails before action.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-action.json').read_text()); assert d['status'] in {'completed','completed_local_only'} and d['shell_used'] is False and d['unallowlisted_path_count']==0 and d['arbitrary_destination_count']==0 and d['commit_action_count']==(1 if d['commit_token_present'] else 0) and d['publication_action_count']==(1 if d['publication_token_present'] else 0) and (d['source_derived_commit_action_count']==d['source_derived_publication_action_count']==0 if not d['exact_byte_redistribution_approved'] else True)"</automated></verify>
  <done>Each channel performs exactly its fixed authorized action or mechanically zero action.</done>
</task>

<task id="32-42-02" type="auto">
  <name>Independently validate actual Git, destination, or zero-action state</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-validation.json
  </files>
  <action>Invoke `validate-korean-release-delivery` independently; do not trust action labels. Rehash release/pointer/authority/rights/safety/build/preflight/action inputs. For commit scope, inspect actual HEAD/parent/commit/tree/changed paths/blob IDs/modes and hash blobs against allowlisted release members. For publication scope, query only the approved fixed destination and compare object IDs/checksums/lengths. For absent or rights-forced-zero scopes, compare mechanical Git/destination state and attempt counters to baseline. Reject forged labels, source-derived action when redistribution is denied, extra/missing paths/objects, partial state, drift, duplicate action or release mutation.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-validation.json').read_text()); assert d['status']=='passed' and d['actual_state_matches_authority'] is True and d['unallowlisted_object_count']==0 and d['zero_action_channels_verified'] is True and d['source_derived_rights_enforced'] is True and d['release_unchanged'] is True"</automated></verify>
  <done>Actual Git/fixed-destination state—or exact no-action baseline—matches the authority independently.</done>
</task>

<task id="32-42-03" type="auto">
  <name>Prove exact retry idempotence and emit bounded Phase 32 release outcome</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/release-delivery-retry-validation.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/phase32-release-result.json
  </files>
  <action>Rerun the exact same delivery tuple, then independently validate again. Require no new commit, upload/object, attempt counter, timestamp or byte change; any partial/divergent state fails instead of retrying. Emit a canonical final result binding current local release root/pointer, rights/safety/build/full-suite, channel actual/zero-action evidence and retry proof. Product/release evidence must contain no planner/checker model, cycle, runtime or pass assertions. State explicitly that Phase 32 proves local APKG structure and reviewed source bytes only—not observed Anki Desktop/mobile import/render/font/responsive/playback, all-family generalization, worker closure or Phase 34 milestone evidence.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; r=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox'); d=json.loads((r/'release-delivery-retry-validation.json').read_text()); f=json.loads((r/'phase32-release-result.json').read_text()); banned={'plan_'+'checker_status','plan_'+'checker_cycle_'+'count','checker_'+'model','checker_'+'runtime','cycle_'+'count'}; assert d['status']=='passed_noop' and d['new_commit_count']==d['new_remote_object_count']==d['byte_change_count']==0 and f['status'] in {'released_local_only','released_with_authorized_delivery'} and f['phase34_observed_claimed'] is False and not (banned &amp; set(f))"</automated></verify>
  <done>Delivery is idempotent and the final outcome truthfully records exact action/zero-action scope and Phase 34 claim limits.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-02 / SC-05 / SC-09 / SC-10 | Exact approved deliverable or safe local-only result | COVERED |
| REQ | KFREQ-01 / KFREQ-02 / KTXT-01 / KAUD-01 / GPRO-01 / GAUD-01 | Constrained evidence-backed delivery | COVERED |
| RESEARCH | Separate publication checkpoint | Fixed action, zero-action and actual-state proof | COVERED |
| CONTEXT | Publication separate; Phase 34 observed/generalized closure retained | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-42-01 | Injection / Elevation | Git/destination action | mitigate | Structured fixed argv shell=False, fixed adapter/destination and exact allowlists. |
| T-32-42-02 | Spoofing / Repudiation | action result | mitigate | Independent actual Git/object/checksum/no-action inspection. |
| T-32-42-03 | Tampering / Race Condition | retry | mitigate | Rehash immediately before action; exact prior state is no-op, partial drift fails. |
</threat_model>
<high_leverage_review>Second pass: inspect shell-free argv, fixed adapter/destination, token absence, actual-state proof, no duplicate retry and explicit Phase 34 claim limits.</high_leverage_review>
<verification>Run all three exact checks and a final read-only release/pointer hash comparison; do not rerun the full suite.</verification>
<success_criteria>
- Only exact authorized channels act; absent channels provably do nothing.
- Independent actual-state validation passes.
- Exact retry is no-op and final claims remain bounded.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-42-SUMMARY.md` with safe action/actual-state/zero-action/idempotence roots, final local release hash and bounded Phase 32 claim.</output>
