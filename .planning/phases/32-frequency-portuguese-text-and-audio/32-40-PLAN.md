---
phase: 32-frequency-portuguese-text-and-audio
plan: "40"
type: execute
wave: 52
runtime: opencode
assurance: self_checked
depends_on: [32-39]
files-modified:
  - ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/evidence-after-offline.json"
  - ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/offline-suite.json"
  - ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/dependency-evidence.json"
  - ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/release-safety.json"
  - ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/production-build-result.json"
  - ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/release-manifest.json"
  - .multilang/releases/ko-frequency/multilang-korean-frequency-v1/
  - .multilang/releases/ko-frequency/current-release.json
autonomous: true
requirements: [KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, KAUD-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, GPRO-01, GAUD-01]
non_goals: ["No live network/provider call, credential use, content/review/DB mutation, Git/remote action, delivery, publication, or Phase 34 observed Anki/generalized closure claim."]
no_ui_proof_rationale: "Credential-empty full regression, release safety and atomic local immutable promotion only."
second_pass_required: true
closure_claim_limit: "Proves final full offline regression, safe immutable local release and current pointer; no commit/upload/publication or observed Anki acceptance, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "The required final pre-release full unfiltered repository suite runs from the frozen isolated Python 3.12 environment in a disposable credential-empty network/provider-poisoned harness while eight staged members, dependencies and the shared .venv remain byte-identical."
    - "After the suite, exactly 11 candidate members exist; safety is member 12 and classifies only those 11; production build result is member 13 and binds the 11 plus safety without any self-reference."
    - "Manifest is created last and a reopened complete tree atomically enters an absent immutable target before current pointer replacement; interruption preserves old pointer/bytes."
  artifacts:
    - {path: ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/offline-suite.json", provides: "Isolated full unfiltered repository-suite evidence"}
    - {path: ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/release-safety.json", provides: "Per-member fail-closed release classification"}
    - {path: .multilang/releases/ko-frequency/current-release.json, provides: "Atomic pointer to durable immutable local release"}
  key_links:
    - {from: "eight protected staging members", to: "11 candidates plus safety member 12 plus build-result member 13", via: "isolated suite, acyclic post-suite evidence, manifest-outside-count fsync/reopen and atomic renames"}
---

# Phase 32 Plan 40: Isolated Full Suite, Release Safety, and Atomic Local Promotion

<objective>
Run the final pre-release full repository suite under hard isolation, complete the acyclic 13-member safe staging inventory, then durably promote one immutable local release and current pointer.

Purpose: No regression, dependency drift, private path/content leakage, locally unsafe member or partial tree may become current; approved local-only status must not be mistaken for publication eligibility.
Output: Suite/dependency/post-suite/safety/build evidence, immutable target, manifest and pointer.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-39-PLAN.md
@scripts/run_phase32_isolated_suite.py
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-40-01" type="auto">
  <name>Run the final pre-release unfiltered suite under credential-empty poisoned isolation</name>
  <files>
    - CREATE: ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/offline-suite.json"
    - CREATE: ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/dependency-evidence.json"
    - CREATE: ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/evidence-after-offline.json"
  </files>
  <action>Hash the eight protected Plan 39 members, all genuine Phase31/source/bundle/policy/review/promotion evidence, `pyproject.toml`, `uv.lock`, the shared `.venv` and isolated environment receipt. Invoke `scripts/run_phase32_isolated_suite.py` in full mode only via `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync` with repeated protected/dependency paths, fixed result/dependency outputs and a 7200-second outer timeout. The child uses fixed argv `shell=False`, credential/endpoint/token-empty allowlist environment, poisoned DNS/socket/LLM/DeepL/Azure/other transports, disposable CWD/HOME/XDG/DB/assets/audio/export/cache/staging roots and unfiltered `tests` tree. It may not sync/install dependencies or write the shared `.venv`. After success, rehash all protected/dependency/shared-environment inputs and write `evidence-after-offline.json` from canonical before/after facts. Plan 18 remains the separate pre-external full-suite gate; this is the required final pre-release rerun.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; h=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-promotion-authorization.md.sha256').read_text().split()[0]; r=pathlib.Path(f'.multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-{h}'); s=json.loads((r/'offline-suite.json').read_text()); d=json.loads((r/'dependency-evidence.json').read_text()); a=json.loads((r/'evidence-after-offline.json').read_text()); assert s['status']=='passed' and s['pytest_scope']=='tests' and s['python_minor']=='3.12' and s['uv_frozen'] is True and s['uv_no_sync'] is True and s['network_attempt_count']==s['provider_attempt_count']==0 and s['credential_name_count']==0 and d['dependencies_unchanged'] is True and d['shared_venv_unchanged'] is True and a['protected_member_count']==8 and a['protected_members_unchanged'] is True"</automated></verify>
  <done>Full repository tests pass only in isolation and every protected/member/dependency byte remains exact.</done>
</task>

<task id="32-40-02" type="auto">
  <name>Build acyclic safety member 12 and production-build-result member 13</name>
  <files>
    - CREATE: ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/release-safety.json"
    - CREATE: ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/production-build-result.json"
  </files>
  <action>First require exactly the fixed 11 candidate names: the eight Plan 39 members plus `evidence-after-offline.json`, `offline-suite.json` and `dependency-evidence.json`. Invoke `build-korean-release-safety` over only those 11 candidate hashes/bytes; it must classify visibility, source-derived status, sensitivity/control, retention, private-root exclusion, scans, license, local-use, transformation, exact-byte redistribution, attribution, `safe_for_local_release`, `safe_to_publish`, commit eligibility and publication eligibility per candidate. It must not list or hash itself, production-build-result or manifest. Unknown local-use/transformation, failed controls or private path/content leakage block local promotion. If local use/transformation are approved but redistribution is denied, source-derived candidates—including APKG and derivative reports/audits—may be `safe_for_local_release=true` only when controls pass, but must have `safe_to_publish=false`, `commit_eligible=false` and `publication_eligible=false`; this does not block the immutable local release. Atomically write `release-safety.json` as member 12. Then write `production-build-result.json` as member 13, binding exact hashes/root of the 11 candidates plus safety member 12 and declaring `release_member_count=13`; it must not hash itself or a manifest. Before promotion require exactly those 13 files, no manifest, no extras, links or traversal.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import hashlib,json,pathlib; h=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-promotion-authorization.md.sha256').read_text().split()[0]; r=pathlib.Path(f'.multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-{h}'); c={'text-review-application.json','audio-review-application.json','multilang-korean-frequency-v1.apkg','generation-report.json','generation-report.md','audit.json','audit.md','evidence-before-offline.json','evidence-after-offline.json','offline-suite.json','dependency-evidence.json'}; expected=c|{'release-safety.json','production-build-result.json'}; assert {p.name for p in r.iterdir()}==expected; s=json.loads((r/'release-safety.json').read_text()); b=json.loads((r/'production-build-result.json').read_text()); assert s['classified_member_count']==11 and set(s['classified_member_names'])==c and 'release-safety.json' not in s['classified_member_names'] and 'production-build-result.json' not in s['classified_member_names'] and s['all_required_controls_passed'] is True and s['all_candidates_safe_for_local_release'] is True and s['private_path_or_content_leak_count']==0; assert b['candidate_member_count']==11 and b['bound_member_count']==12 and b['release_member_count']==13 and b['safety_sha256']==hashlib.sha256((r/'release-safety.json').read_bytes()).hexdigest() and 'production-build-result.json' not in b['bound_member_names']"</automated></verify>
  <done>Exactly 11 candidates, safety member 12 and non-self-referential build-result member 13 exist with rights-derived local/publication classifications.</done>
</task>

<task id="32-40-03" type="auto">
  <name>Durably promote complete release tree and atomically replace current pointer</name>
  <files>
    - CREATE: ".multilang/releases/ko-frequency/.multilang-korean-frequency-v1.staging-$CONTENT_PROMOTION_AUTHORIZATION_SHA256/release-manifest.json"
    - CREATE: .multilang/releases/ko-frequency/multilang-korean-frequency-v1/
    - CREATE: .multilang/releases/ko-frequency/current-release.json
  </files>
  <action>Invoke `promote-korean-release-bundle` with fixed release parent, exact staging/target/pointer, final-promotion authority/sidecar and all 13 repeated expected-member arguments. Require each of the 11 candidates and both evidence members locally safe, but do not require publication eligibility. Fsync the 13 members, create `release-manifest.json` last as an envelope outside the 13-member count containing every member name/hash, candidate root, safety hash, build-result hash and graph-order assertion, fsync directories, close/reopen and revalidate all 13 hashes/root/safety/build evidence, then rename only into absent `.multilang/releases/ko-frequency/multilang-korean-frequency-v1` and fsync parent. Only after durable target, write/fsync an owned pointer temp with target basename, locator/content/root/manifest/member/prior-pointer hashes and atomically replace/fsync pointer. Partial/non-identical target, collision or interruption preserves prior pointer/bytes; exact complete retry is no-write.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; r=pathlib.Path('.multilang/releases/ko-frequency/multilang-korean-frequency-v1'); p=json.loads(pathlib.Path('.multilang/releases/ko-frequency/current-release.json').read_text()); expected={'text-review-application.json','audio-review-application.json','multilang-korean-frequency-v1.apkg','generation-report.json','generation-report.md','audit.json','audit.md','evidence-before-offline.json','evidence-after-offline.json','offline-suite.json','dependency-evidence.json','release-safety.json','production-build-result.json','release-manifest.json'}; m=json.loads((r/'release-manifest.json').read_text()); assert r.is_dir() and {x.name for x in r.iterdir()}==expected and m['member_count']==13 and len(m['members'])==13 and 'release-manifest.json' not in {x['name'] for x in m['members']} and m['graph_acyclic'] is True and p['target_basename']=='multilang-korean-frequency-v1' and p['member_count']==13"</automated></verify>
  <done>One complete durable immutable local release is current; no partial tree, overwrite or external action occurs.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01..SC-10 | Regression-safe immutable local release | COVERED |
| REQ | All 11 Phase 32 IDs | Full offline evidence and safe local promotion | COVERED |
| RESEARCH | Phase gate / atomic activation / security | Isolated full suite, exact inventory, safety, fsync/reopen/pointer | COVERED |
| CONTEXT | Existing modes preserved; commit/publication separate; Phase 34 observed claims excluded | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-40-01 | Information Disclosure / SSRF | full suite | mitigate | Credential-empty environment, poisoned network/providers and disposable roots. |
| T-32-40-02 | Tampering | staged inventory/safety | mitigate | Protected hashes, exact 13 members, scans/classification and no extras/links. |
| T-32-40-03 | Race Condition / Repudiation | release/pointer | mitigate | Fsync, manifest-last, reopen, absent-target rename, parent fsync and recoverable pointer. |
</threat_model>
<high_leverage_review>Second pass: inspect full-suite isolation, eight protected bytes, 13-member exact inventory, safety fail-closed states, manifest-last durability and interruption/pointer retry behavior.</high_leverage_review>
<verification>Run the three exact checks, `git diff --check`, and confirm no network/provider/Git/remote actions.</verification>
<success_criteria>
- Unfiltered full repository suite passes in hard isolation.
- Exactly 13 members pass local-release safety/build validation with publication eligibility represented independently.
- Complete immutable release and current pointer promote atomically.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-40-SUMMARY.md` with suite/dependency/protected/safety/build/release/pointer roots, 13-member inventory and bounded claim.</output>
