---
phase: 32-frequency-portuguese-text-and-audio
plan: "53"
type: execute
wave: 65
runtime: opencode
assurance: self_checked
depends_on: [32-52]
files-modified:
  - .planning/.state-fingerprint.json
  - .planning/SPEC.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-53-PLAN.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-53-SUMMARY.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_database_preflight_and_job_binding.py
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-target-reclassification-authority.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-preflight-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-post-migration-validation.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-job-binding.json
autonomous: true
requirements:
  - KFREQ-01
  - KFREQ-02
  - KFREQ-03
  - KTXT-01
  - KAUD-01
  - GLEX-01
  - GLEX-02
  - GMOR-01
  - GTXT-01
  - GPRO-01
  - GAUD-01
non_goals:
  - Do not reset, drop, truncate, delete, or manually rewrite any production schema, table, role, row, source asset, provider cache, generated content, audio asset, export, or release artifact.
  - Do not ingest Korean frequency candidates, call OpenAI/LiteLLM/DeepL, query Azure, synthesize audio, import or apply review, promote content, export APKG/CSV/TSV, release, publish, deliver, commit, PR, or close Phase 32.
  - Do not overwrite existing disposable/test evidence, local SQLite pilot evidence, provider/catalog pilot evidence, voice-profile evidence, source/final-bundle evidence, Phase 33 artifacts, or Phase 34 artifacts.
  - Do not print or persist a raw database URL, credential, username, password, host, database name, private path, SQL row content, prompt, completion, provider payload, generated Korean text, Portuguese text, or audio bytes.
hard_boundaries:
  - Treat the user's `2` continuation answer as authority only to reclassify the current `.env` DB target from prior disposable/test history into the production DB target for this narrow plan.
  - The `.env` exception remains limited to parsing `MULTILANG_DATABASE_URL` in process; do not `source .env`, inspect other keys, pass the URL on the command line, or write it to artifacts.
  - The allowed DB mutation scope is Alembic `upgrade head` through `ensure_database_schema` plus one idempotent `pilot_base` Korean frequency job/authority binding row for the production target.
  - If the target is not PostgreSQL, the Alembic graph does not have sole head `20260828_19`, the DB is read-only/replica, app rows already exist beyond an exact idempotent same-job replay, Phase 31 fallback authority drifts, or binding would require `full` authority, stop with sanitized blocker evidence.
escalation_triggers:
  - Stop if the reclassification authority is considered ambiguous or insufficient by the checker or by execution-time validation.
  - Stop if Alembic would need manual SQL, destructive DDL, multiple heads, downgrade, revision surgery, schema reset, backup restore, or target switching.
  - Stop if the production DB contains unexpected application rows before this plan's single job binding.
  - Stop if any exception, verification command, or artifact would reveal a raw locator, locator component, credential, private path, provider payload, prompt/completion, generated content body, SQL row content, or audio bytes.
approval_gates:
  - The current-session approval covers only target reclassification, secret-safe `.env` parsing, non-destructive preflight, Alembic current-head migration, and one `pilot_base` job authority binding.
  - Ask before any destructive database action, provider/model/budget use, Azure catalog/synthesis call, generated text/audio production, review application, export, release, publication, delivery, Git action, or Phase 32 closure.
anti_regression_targets:
  - Prior Plan 32-47/32-48 disposable/test evidence remains historically true and non-production; this plan adds a new target reclassification authority instead of editing old evidence.
  - Existing `pilot-job-binding.json` remains local ignored SQLite pilot evidence and must not be overwritten by production DB binding evidence.
  - Korean remains absent from the static `audio_voice_registry`; `select_voice(SupportedLanguage.KO)` must still fail.
  - Full production text/audio authority remains blocked because `heard_review_authority_sha256` and full-run provider/review/audio evidence are absent.
known_unknowns:
  - Whether the DB is still exactly at Alembic head and empty after the prior disposable reset is only known after secret-safe connection.
  - The stable production job authority SHA-256 and row-count deltas are derived during execution and must be persisted only as hashes/counts; the job ID is capped to fit the existing 36-character `generation_jobs.id` column.
  - Full-run provider/model/budget/retry authority, text review, Azure synthesis, audio review, export, release, publication, delivery, and Phase 32 closure remain unproven after this plan.
ui_proof_slots: []
no_ui_proof_rationale: Backend/planning/database/job-binding gate only; no UI behavior, visual rendering, or observed Anki import/playback claim is made.
high_leverage_surfaces:
  - Reclassifying a previously disposable/reset DB target into production scope.
  - Running non-destructive Alembic against a live PostgreSQL target without leaking secrets.
  - Binding the first production DB Korean frequency job authority while full generation remains blocked.
second_pass_required: true
closure_claim_limit: This plan can prove only that the current DB target was explicitly reclassified for production DB use, reached sole Alembic head `20260828_19`, and has one `pilot_base` Korean frequency job authority bound without provider, Azure, audio, review, export, release, publication, delivery, Git, or Phase 32 closure effects.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: No generated learner content is produced because DB authority is deliberately separated from provider, audio, review, export, and delivery authority.
  kept: Existing source/final-bundle hashes, Phase 31 fallback seam, production/disposable history, Alembic single-source migration path, and no-secret evidence rules remain authoritative.
  gained: A production DB target can be safely proven and bound for later side-effect lanes without smuggling full-run or release approval.
must_haves:
  truths:
    - The current `.env` DB target is explicitly reclassified from prior disposable/test history into the narrow production DB target for this plan.
    - The production target is PostgreSQL, writable primary, connected without raw locator disclosure, and migrates or remains at sole Alembic head `20260828_19` through `ensure_database_schema`.
    - A single production DB Korean frequency job is idempotently bound with `pilot_base` authority and the exact Phase 31, final-bundle, source, provider-policy, and pilot-authority hashes.
    - No provider, Azure, audio synthesis, review application, export, release, publication, delivery, Git, or Phase 32 closure side effects occur.
  artifacts:
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-target-reclassification-authority.md
      provides: Sanitized authority recording the user's target reclassification choice and the denied destructive/downstream powers.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_database_preflight_and_job_binding.py
      provides: Phase-local helper that loads only `MULTILANG_DATABASE_URL`, runs secret-safe preflight/migration/job binding, and writes sanitized evidence.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-preflight-result.json
      provides: Hash/count-only target, Alembic, schema, read-only, and pre-binding row-state evidence.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-result.json
      provides: Alembic `ensure_database_schema` result with before/after revision and schema hashes.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-post-migration-validation.json
      provides: Runtime provisioning, Phase 32 column, Phase 33 table, row-count, and non-closing production DB readiness evidence.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-job-binding.json
      provides: Idempotent production DB `pilot_base` Korean frequency job authority binding evidence.
  key_links:
    - from: current-session target reclassification choice
      to: production-database-target-reclassification-authority.md
      via: sanitized authority capture that acknowledges prior disposable/test classification and reset history.
    - from: production-database-target-reclassification-authority.md
      to: production_database_preflight_and_job_binding.py
      via: helper-side authority file validation before any DB connection.
    - from: .env `MULTILANG_DATABASE_URL`
      to: production-database-preflight-result.json
      via: in-process SQLAlchemy URL parsing and hash-only locator classification without command-line exposure.
    - from: src/multilang/db/provisioning.py
      to: production-database-migration-result.json
      via: `ensure_database_schema` using Alembic for non-SQLite targets.
    - from: src/multilang/repositories/job_repository.py
      to: production-database-job-binding.json
      via: `KoreanFrequencyJobAuthority` plus `JobRepository.bind_execution_authority` at `pilot_base`.
---

# Phase 32 Plan 53: Production DB Reclassification, Migration, And Job Binding

## Objective
Record the user's current-target reclassification decision, then safely connect to the reclassified PostgreSQL target without exposing secrets, run the Alembic head path, and bind exactly one `pilot_base` Korean frequency job authority. This unblocks later production side-effect lanes only at the DB/job-binding layer and does not authorize provider calls, Azure synthesis, review application, export, release, publication, delivery, or Phase 32 closure.

## Context
- Plan 32-50 identified production database authority as the first remaining side-effect gate.
- Plan 32-51 captured narrow production DB gate approval but blocked because the process environment lacked the DB locator.
- Plan 32-52 captured narrow `.env` parsing approval and blocked because the current locator hash matched the Plan 32-47/32-48 disposable/test target.
- The user then replied `2`, which the session handoff records as reclassifying the current `.env` target as production while acknowledging the prior disposable/test classification and reset history.
- Plan 32-48 reset and migrated the same target as a disposable rehearsal, leaving Alembic head `20260828_19` and zero application rows, but that evidence remains non-production until this new reclassification authority is recorded.
- Existing `evidence-inbox/pilot-job-binding.json` is local ignored SQLite pilot evidence and must not be overwritten.

<assurance_check>
source_artifact: .planning/phases/32-frequency-portuguese-text-and-audio/32-52-SUMMARY.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: self_checked
status: matched_after_checker
warning: None; gsdd-plan-checker passed in the same runtime family, so assurance remains self_checked rather than cross_runtime_checked.
</assurance_check>

## Requirements Covered
- KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, KAUD-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, GPRO-01, GAUD-01

## Must-Haves
1. Record a sanitized target reclassification authority that acknowledges prior disposable/test history and denies destructive/downstream powers.
2. Use a phase-local helper so the raw DB URL is read only in process, never passed in argv, printed, or persisted.
3. Confirm PostgreSQL writable-primary connectivity, sole Alembic head `20260828_19`, Phase 32 columns, and Phase 33 current-head tables after `ensure_database_schema`.
4. Bind one production DB Korean frequency job with `pilot_base` authority using the exact approved hashes and no adapter/provider/audio/review/export side effects.
5. Keep Phase 32 in progress and leave full text/audio/review/export/release/delivery authority blocked.

## Anti-Goals
- No destructive DB work, schema reset, manual SQL migration, data deletion, target switching, or rollback/restore operation.
- No candidate ingestion, provider calls, DeepL calls, Azure calls, audio synthesis, generated text/audio persistence, review application, content promotion, export, release, publication, delivery, commit, PR, or Phase 32 closure.
- No overwrite of prior disposable/test or local SQLite pilot evidence.
- No raw secrets, locator components, private paths, generated text bodies, provider payloads, prompts/completions, SQL row content, or audio bytes in artifacts or terminal output.

## Hard Boundaries
- The helper must be invoked without `--database-url` and must parse only `MULTILANG_DATABASE_URL` from `.env`.
- The helper must validate `production-database-target-reclassification-authority.md` before connecting.
- Existing app rows before binding, other than an exact same-job idempotent replay, block instead of silently mutating a non-empty production DB.
- Job authority stage is limited to `pilot_base`; missing `heard_review_authority_sha256` keeps `full` production text/audio blocked.

## Evidence Contract
- `production-database-target-reclassification-authority.md` records `target_reclassification_approved=true`, `prior_disposable_reset_acknowledged=true`, `destructive_reset_authorized=false`, `production_database_connection_allowed=true`, `alembic_upgrade_head_allowed=true`, `pilot_base_job_binding_allowed=true`, and all downstream denials.
- `production-database-preflight-result.json` records only sanitized DB locator/identity hashes, Alembic graph state, read-only/replica checks, advisory-lock result, schema hash/count, app row counts/count hash, and zero provider/audio/export deltas.
- `production-database-migration-result.json` records `alembic_upgrade_head_ran=true`, before/after revision, expected/sole head `20260828_19`, schema hashes/counts, `manual_sql_used=false`, `destructive_statement_count=0`, and zero provider/audio/export deltas.
- `production-database-post-migration-validation.json` records `status=passed`, current revision/head, Phase 32 columns present, Phase 33 tables present, runtime provisioning no-op state, row-count delta before binding, and `production_database_ready_for_pilot_base_binding=true`.
- `production-database-job-binding.json` records `status=bound`, `database_kind=production_postgresql_reclassified`, `authority_stage=pilot_base`, exact job ID `phase32-prod-freq-pilot-base`, authority SHA-256, expected hashes, persisted/reloaded equality, row-count delta limited to one job row or exact replay, zero candidates, zero provider attempts, zero Azure calls, zero synthesis attempts, zero review applications, and zero exports.
- `32-53-SUMMARY.md` records actual command outcomes and unresolved downstream blockers without claiming learner-ready output.

## Common Pitfalls
- Treating historical disposable/test reset evidence as if it had always been production evidence instead of recording a new reclassification authority.
- Passing the DB URL through CLI arguments or shell-sourcing `.env`.
- Overwriting `pilot-job-binding.json` and losing the distinction between local SQLite pilot and production PostgreSQL binding.
- Binding `full` authority even though full-run provider, heard-review, text-review, audio-review, and export evidence are absent.
- Counting a successful Alembic/job binding as permission to run provider, Azure, review, export, release, publication, delivery, or closure steps.

## Stop-And-Challenge
- Stop if the checker says the user's `2` answer is insufficient reclassification authority.
- Stop if DB preflight sees unexpected app rows, unexpected Alembic graph state, read-only/replica mode, non-PostgreSQL dialect, non-public application tables, or schema drift that cannot be repaired by `ensure_database_schema`.
- Stop if job binding would overwrite a different existing authority, bind a non-Korean/non-frequency job, ingest candidates, construct provider/audio adapters, or require `full` authority.
- Stop if evidence generation or failure reporting risks leaking secrets, private paths, payloads, generated content, row content, or audio bytes.

## Approval Gates
- Already approved for this plan: target reclassification of the current `.env` DB locator, secret-safe `.env` parsing, non-destructive production DB preflight, Alembic `upgrade head`, and one `pilot_base` job authority binding.
- Still not approved: destructive DB action, provider/model/budget execution, DeepL execution, Azure catalog/synthesis, generated text/audio production, review application, export, release, publication, delivery, Git action, and Phase 32 closure.

<checks>
<plan_check>
checker: gsdd-plan-checker
checker_task_id: ses_f7e6ed2d9ffeUyROQbGY1j6rW3
checker_runtime: opencode subagent
status: passed
blocking: false
notes: Independent checker passed after two revisions: the final leak scan now matches only anchored `MULTILANG_DATABASE_URL`, rejects duplicates, and scans raw/rendered URL components without parsing unrelated `.env` entries; the production job ID was shortened to fit the existing 36-character `generation_jobs.id` column.
</plan_check>
</checks>

## Tasks

<task id="32-53-01" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-target-reclassification-authority.md
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_database_preflight_and_job_binding.py
  </files>
  <action>
    Write the sanitized reclassification authority and the phase-local helper. The helper must parse only `MULTILANG_DATABASE_URL` from `.env`, require PostgreSQL, validate the reclassification authority before connection, compute locator/identity hashes without persisting raw components, use parameterized SQLAlchemy inspection queries, run migrations only through `ensure_database_schema`, bind `KoreanFrequencyJobAuthority(stage="pilot_base")` through `JobRepository.bind_execution_authority`, catch failures into controlled sanitized blocker envelopes, and print only a generic status line.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_database_preflight_and_job_binding.py --self-check`
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -m py_compile .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_database_preflight_and_job_binding.py`
  </verify>
  <done>
    The authority and helper exist, the helper self-check passes without a DB connection, and the helper compiles.
  </done>
</task>

<task id="32-53-02" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-preflight-result.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-result.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-post-migration-validation.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-job-binding.json
  </files>
  <action>
    Run the helper in execute mode without passing any database URL argument. It must revalidate the authority, read the DB URL only in process, connect to the reclassified target, write preflight evidence, run `ensure_database_schema`, validate the post-migration schema/head state, and then bind exactly one production PostgreSQL Korean frequency job at `pilot_base` stage. If any precondition fails, write sanitized blocked evidence and stop before later side effects.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_database_preflight_and_job_binding.py --execute-preflight-migrate-bind`
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python - <<'PY'
import json
from pathlib import Path

root = Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox')
preflight = json.loads((root / 'production-database-preflight-result.json').read_text(encoding='utf-8'))
migration = json.loads((root / 'production-database-migration-result.json').read_text(encoding='utf-8'))
validation = json.loads((root / 'production-database-post-migration-validation.json').read_text(encoding='utf-8'))
binding = json.loads((root / 'production-database-job-binding.json').read_text(encoding='utf-8'))

expected_head = '20260828_19'
expected_locator = '19871fdb496eed265952e0358b94fa795d1b74c37d89a674b0189ee1381f32b6'
assert preflight['status'] == 'passed'
assert preflight['database_kind'] == 'production_postgresql_reclassified'
assert preflight['database_locator_sha256'] == expected_locator
assert preflight['target_reclassified_from_disposable_test'] is True
assert preflight['replica'] is False
assert preflight['transaction_read_only'] is False
assert preflight['sole_head'] == expected_head
assert migration['status'] == 'migrated'
assert migration['alembic_upgrade_head_ran'] is True
assert migration['after_revision'] == expected_head
assert migration['manual_sql_used'] is False
assert migration['destructive_statement_count'] == 0
assert validation['status'] == 'passed'
assert validation['current_revision'] == expected_head
assert validation['phase32_columns_present'] is True
assert validation['phase33_tables_present'] is True
assert validation['production_database_ready_for_pilot_base_binding'] is True
assert binding['status'] == 'bound'
assert binding['database_kind'] == 'production_postgresql_reclassified'
assert binding['job_id'] == 'phase32-prod-freq-pilot-base'
assert binding['authority_stage'] == 'pilot_base'
assert binding['persisted_reloaded_equal'] is True
assert binding['lexical_candidate_count_for_job'] == 0
assert binding['provider_attempt_count_after'] == binding['provider_attempt_count_before'] == 0
assert binding['azure_call_count'] == 0
assert binding['audio_synthesis_attempt_count'] == 0
assert binding['review_application_count'] == 0
assert binding['export_count'] == 0
PY`
  </verify>
  <done>
    The reclassified production PostgreSQL target reaches Alembic head and has exactly one `pilot_base` Korean frequency job authority bound, with no provider/audio/review/export side effects.
  </done>
</task>

<task id="32-53-03" type="auto">
  <files>
    - MODIFY: .planning/SPEC.md
    - MODIFY: .planning/.state-fingerprint.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/32-53-SUMMARY.md
  </files>
  <action>
    Update `.planning/SPEC.md` Current State to record Plan 32-53 as a non-closing production DB reclassification, Alembic/head validation, and `pilot_base` job-binding step. Keep Phase 32 in progress and list the remaining blockers for full-run provider/model/budget, text review, Azure synthesis, audio review, export, release, publication, delivery, and closure. Write `32-53-SUMMARY.md` with actual verification results, command outcomes, leak-scan result, and structured judgment.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python - <<'PY'
import json
import re
from pathlib import Path
from sqlalchemy.engine import make_url

def load_url() -> str:
    pattern = re.compile(r'^\s*(?:export\s+)?MULTILANG_DATABASE_URL\s*=\s*(.*?)\s*$')
    matches = []
    for raw_line in Path('.env').read_text(encoding='utf-8').splitlines():
        match = pattern.match(raw_line)
        if match:
            matches.append(match.group(1).strip().strip('"').strip("'"))
    assert len(matches) == 1
    return matches[0]

root = Path('.planning/phases/32-frequency-portuguese-text-and-audio')
paths = [
    root / 'evidence-inbox/production-database-target-reclassification-authority.md',
    root / 'evidence-inbox/production-database-preflight-result.json',
    root / 'evidence-inbox/production-database-migration-result.json',
    root / 'evidence-inbox/production-database-post-migration-validation.json',
    root / 'evidence-inbox/production-database-job-binding.json',
    root / '32-53-SUMMARY.md',
    Path('.planning/SPEC.md'),
]
body = '\n'.join(path.read_text(encoding='utf-8') for path in paths)
raw_url = load_url()
url = make_url(raw_url)
components = {url.render_as_string(hide_password=False)}
components.update(str(value) for value in (url.username, url.password, url.host, url.port, url.database) if value)
for key, value in url.query.items():
    if isinstance(value, (tuple, list)):
        components.update(f'{key}={item}' for item in value if item)
    elif value:
        components.add(f'{key}={value}')
assert raw_url not in body
assert not re.search(r'postgres(?:ql)?(?:\+[a-z0-9_]+)?://', body, re.I)
for value in components:
    pattern = r'(?<![A-Za-z0-9_.-])' + re.escape(value) + r'(?![A-Za-z0-9_.-])'
    assert not re.search(pattern, body)
assert 'Plan 32-53' in body
assert 'phase32-prod-freq-pilot-base' in body
assert 'full-run provider/model/budget' in body
assert 'Phase 32 remains in progress' in body
PY`
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from multilang.domain.jobs import SupportedLanguage; from multilang.services.audio_voice_registry import VoiceSelectionError, select_voice; ok=False\ntry:\n    select_voice(SupportedLanguage.KO)\nexcept VoiceSelectionError:\n    ok=True\nassert ok\nassert select_voice(SupportedLanguage.PT).voice_id"`
    - Run `node .planning/bin/gsdd.mjs phase-status 32 in_progress`
    - Run `node .planning/bin/gsdd.mjs session-fingerprint write`
    - Run `git diff --check`
  </verify>
  <done>
    Phase 32 planning state records the production DB/job-binding progress, evidence remains secret-safe, Korean static voice refusal still holds, and downstream gates remain blocked.
  </done>
</task>

## Verification
- Helper self-check and `py_compile` must pass before any DB connection.
- Execution JSON assertions must prove the target, Alembic head, schema readiness, row-count boundaries, and `pilot_base` job binding.
- Leak scan must cover the new authority, evidence, summary, and SPEC sections against the in-memory URL and parsed/rendered URL components without parsing unrelated `.env` entries.
- Korean static voice refusal must remain true.
- GSDD state must remain `Phase 32 in_progress`, with a refreshed session fingerprint and no whitespace errors.

## Success Criteria
- The current DB target has new reclassification authority and sanitized evidence acknowledging its disposable/reset history.
- The target reaches or remains at Alembic head `20260828_19` using repo-native provisioning and no destructive/manual SQL.
- `phase32-prod-freq-pilot-base` is bound in the production PostgreSQL DB with exact `pilot_base` Korean frequency authority.
- No provider, Azure, audio, review, export, release, publication, delivery, Git, or closure authority is claimed.

## High-Leverage Review
- Second pass required because this plan converts a previously disposable target into a production DB target and performs live DB mutation. The checker must verify that reclassification is explicit, the helper avoids secret disclosure, mutation scope is non-destructive and minimal, and downstream side-effect authority is not smuggled into the binding.

## Leverage Review
- Lost: The plan spends another narrow gate instead of jumping directly to full production generation.
- Kept: Hash-bound authority, Alembic single-source schema control, Phase 31 fallback proof, and evidence sanitation remain intact.
- Gained: Later production provider/audio/review steps can depend on a real DB/job binding instead of disposable rehearsal or SQLite pilot artifacts.

## Notes
- Approach exploration is skipped because `32-APPROACH.md` already records `alignment_status: user_confirmed`, and this plan follows the locked production database checkpoint sequence rather than adding a new product approach.
- Additional research is skipped because the plan uses established SQLAlchemy, Alembic, Pydantic, and repository patterns already present in the codebase.
- The current Alembic head is `20260828_19` because Phase 33 DDL is already at head; Plan 32-27's older `20260821_18` expectation is superseded by the live migration graph.
- Control-map warnings are acknowledged: the canonical worktree is dirty with existing planning artifacts, and detached `/tmp/multilang-phase31-*` worktrees are not used as execution surfaces.
