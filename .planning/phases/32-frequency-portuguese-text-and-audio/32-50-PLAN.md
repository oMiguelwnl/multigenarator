---
phase: 32-frequency-portuguese-text-and-audio
plan: "50"
type: execute
wave: 62
runtime: opencode
assurance: self_checked
depends_on: [32-43, 32-46, 32-48, 32-49]
files-modified:
  - .planning/.state-fingerprint.json
  - .planning/SPEC.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-50-PLAN.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-50-SUMMARY.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/phase32-remaining-production-authority-map.md
autonomous: true
requirements:
  - KFREQ-01
  - KFREQ-02
  - KFREQ-03
  - KTXT-01
  - KAUD-01
  - GLEX-01
  - GLEX-02
  - GMOR-01
  - GTXT-01
  - GPRO-01
  - GAUD-01
non_goals:
  - Do not connect to a production database, read `.env`, print or persist credentials, run Alembic against production, call OpenAI, call DeepL, query Azure, synthesize audio, import or apply review, export APKG/CSV/TSV, release, publish, deliver, commit, PR, or close Phase 32.
  - Do not treat the disposable/test database rehearsal, bounded 10-item live pilot, or voice-profile artifact as production approval, full-run approval, heard approval, export readiness, release readiness, or learner delivery.
  - Do not change application code, migrations, provider policy, audio registry, source assets, card templates, note GUIDs, or Phase 33/34 artifacts in this plan.
hard_boundaries:
  - Consume only existing Phase 32 planning summaries, sanitized evidence artifacts, roadmap/spec contracts, and CLI command names; do not open secrets, raw provider payloads, raw prompts/completions, private paths/context, generated content bodies, or audio bytes.
  - The produced map is advisory execution control evidence only. It cannot grant legal, production database, provider, budget, audio, review, export, release, publication, delivery, or phase-closure authority.
  - The first consuming side-effect lane must remain blocked until an explicit user/operator production database authority exists and names the permitted target class, mutation scope, migration/preflight command shape, and no-secret evidence handling.
  - Later provider, Azure synthesis, review application, export, release, publication, and delivery lanes each require their own exact authority and evidence rather than inheriting approval from this sequencer.
escalation_triggers:
  - Stop if any artifact needed for the map appears to contain secrets, raw DB URLs, prompts, completions, provider payloads, private source text, or audio bytes.
  - Stop if the current roadmap/spec contradict the Phase 32 open-gap posture or claim Phase 32 is complete without production runtime and delivery evidence.
  - Stop if producing the map would require live provider calls, production database access, Azure access, content generation, audio synthesis, review mutation, export, release, publication, or delivery.
  - Stop if the first side-effect gate is not production database authority after reconciling current summaries and command surfaces.
approval_gates:
  - Ask before any production database connection, Alembic mutation, schema reset, data deletion, job binding, or production candidate ingestion.
  - Ask before any provider, DeepL, Azure catalog, Azure synthesis, or other paid/network call, including retries.
  - Ask before importing or applying text/audio review, promoting content, staging or exporting learner artifacts, release promotion, publication, delivery, Git commit, PR, or Phase 32 status closure.
anti_regression_targets:
  - Phase 32 remains `[-] In progress`; the map and summary must not mark requirements complete or phase closed.
  - Korean remains absent from the static `audio_voice_registry`; `select_voice(SupportedLanguage.KO)` must still fail until a separate registry activation plan.
  - Existing source/final-bundle, pilot, disposable DB, provider policy, and voice-profile evidence hashes remain distinct and cannot substitute for production DB, full-run, review, audio, export, release, or delivery evidence.
  - Evidence stays sanitized and count/hash based, with no raw prompts, completions, provider payloads, credentials, DB URLs, private paths, generated Korean/Portuguese text bodies, or audio bytes.
known_unknowns:
  - The exact production database target and mutation authority are not present in current artifacts.
  - Full-run provider/budget/retry authority, text quality review, Azure synthesis authority, audio review, APKG staging, release, publication, and delivery authority are not present in current artifacts.
  - The selected Korean voice profile records provider SDK version as `not-captured`; any later synthesis plan must decide whether the existing sanitized catalog metadata is sufficient or reauthorize catalog capture.
ui_proof_slots: []
no_ui_proof_rationale: Backend/CLI/planning control evidence only; no UI behavior, visible rendering, or observed Anki import/playback claim is made.
high_leverage_surfaces:
  - Remaining Phase 32 execution order controls production database mutation, provider spend, Azure audio, review promotion, export, release, and delivery gates.
second_pass_required: true
closure_claim_limit: This plan can prove only that the remaining Phase 32 side-effect sequence and first missing authority gate are documented from current evidence. It must not claim production DB readiness, production text, audio synthesis, heard/acoustic approval, review completion, APKG export, release, publication, delivery, or Phase 32 completion.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: No product output is generated because authority boundaries outrank speed when production data, paid providers, audio, review, export, and delivery are involved.
  kept: Existing Phase 32 source, pilot, disposable DB, provider, voice-profile, CLI, and verification evidence remains authoritative and unmodified.
  gained: A single scanner-readable handoff that lets the next execution step proceed directly to the correct user/operator gate instead of cycling through obsolete waiver plans.
must_haves:
  truths:
    - Current Phase 32 evidence is reconciled into one remaining-execution map that names each open production gate and its blocking authority/evidence.
    - The first side-effect blocker is explicit production database authority before any production mutation or full-run job binding.
    - The map and summary preserve all no-secret, no-provider-call, no-Azure-synthesis, no-review-mutation, no-export, no-release, no-delivery, and no-closure boundaries.
  artifacts:
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/phase32-remaining-production-authority-map.md
      provides: Hash/count-only map of current available evidence, missing production authorities, requirement-to-gate coverage, first blocking gate, and next command families.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/32-50-SUMMARY.md
      provides: Non-closing execution summary stating that Phase 32 remains blocked at production database authority and that no external side effects occurred.
  key_links:
    - from: .planning/phases/32-frequency-portuguese-text-and-audio/32-43-SUMMARY.md
      to: phase32-remaining-production-authority-map.md
      via: Source-review and final-bundle authority are marked available but non-sufficient for provider, DB, audio, review, export, or delivery.
    - from: .planning/phases/32-frequency-portuguese-text-and-audio/32-46-SUMMARY.md
      to: phase32-remaining-production-authority-map.md
      via: Bounded 10-item live text/catalog pilot is marked mechanics evidence only and not production/full-run authority.
    - from: .planning/phases/32-frequency-portuguese-text-and-audio/32-48-SUMMARY.md
      to: phase32-remaining-production-authority-map.md
      via: Disposable/test DB migration rehearsal is marked available but non-production.
    - from: .planning/phases/32-frequency-portuguese-text-and-audio/32-49-SUMMARY.md
      to: phase32-remaining-production-authority-map.md
      via: Voice profile is marked available but not synthesis, heard-review, export, release, or delivery authority.
    - from: src/multilang/cli.py
      to: phase32-remaining-production-authority-map.md
      via: Existing Korean production, text, audio, review, export, and release command surfaces are named only as future command families gated by missing authority.
---

# Phase 32 Plan 50: Production Authority Sequencer And First Gate

## Objective
Create a concise, scanner-readable map of the remaining Phase 32 production sequence and stop at the first real side-effect gate: explicit production database authority. This keeps the user's request moving quickly without fabricating approvals, touching secrets, mutating production data, calling providers, synthesizing audio, or claiming learner-ready output.

## Context
- Phase 32 remains in progress. Its learner-visible goal still lacks production database, full-run text, approved audio, review/remediation, export, release, publication, and delivery evidence.
- Plan 32-43 replaced source-review/final-bundle waivers with real source/final-bundle authority, but explicitly did not grant provider, production DB, audio, export, release, publication, or delivery power.
- Plan 32-46 proved only a bounded local ignored 10-item text/catalog pilot, with zero synthesis, zero fallback, no production DB, and no downstream grants.
- Plan 32-48 proved disposable/test reset plus Alembic migration rehearsal only, with `production_authority=false`.
- Plan 32-49 bound the selected Azure `ko-KR` voice into a neutral profile artifact, with zero synthesis, no heard approval, no static registry activation, and no export/release/delivery grants.
- The existing CLI exposes the future command families, but invoking them for production requires authority files and hashes that are not present.

<assurance_check>
source_artifact: .planning/phases/32-frequency-portuguese-text-and-audio/32-49-SUMMARY.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: self_checked
status: matched_after_checker
warning: None; gsdd-plan-checker passed after scoped revisions in the same runtime family, so assurance remains self_checked rather than cross_runtime_checked.
</assurance_check>

## Requirements Covered
- KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, KAUD-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, GPRO-01, GAUD-01

## Must-Haves
1. The remaining Phase 32 work is represented as ordered gates with current evidence, missing authority, first blocker, and next command families.
2. Source/final-bundle approval, disposable DB rehearsal, bounded pilot, and voice-profile evidence remain distinct and cannot substitute for missing production/full-run/review/audio/export/release/delivery evidence.
3. The first side-effect gate is production database authority because production job binding and complete text/audio runs require a target database before provider or review work can be meaningful.
4. The plan produces no external side effects and leaks no secrets, raw DB URLs, prompts, completions, provider payloads, private paths/context, generated content bodies, or audio bytes.
5. Phase 32 remains in progress after this plan.

## Anti-Goals
- No production DB connection or mutation.
- No `.env` read, credential handling, raw database URL capture, or secret persistence.
- No OpenAI, DeepL, Azure, catalog, TTS, audio-byte, review-application, export, release, publication, delivery, Git commit, PR, or phase-closure action.
- No edits to application code, migrations, provider policy, audio registry, frequency assets, export templates, Phase 33, or Phase 34.

## Hard Boundaries
- Use only sanitized current planning/evidence summaries and known CLI command names.
- The map must be a control artifact, not an authority artifact.
- The summary must state exact non-actions and preserve Phase 32 open blockers.
- If current evidence changes underfoot in a way that contradicts the known open-gate state, execution must stop and re-read before writing the map.

## Evidence Contract
- `phase32-remaining-production-authority-map.md` exists, is substantive, and names these gate groups: source/final bundle, production database, full-run provider/budget, full text generation, text review/application, Azure synthesis, audio review/application, production evidence validation, APKG/export staging, release safety/promotion, publication/delivery, and Phase 34 handoff.
- The map includes a requirement-to-gate table covering every Phase 32 requirement ID: `KFREQ-01`, `KFREQ-02`, `KFREQ-03`, `KTXT-01`, `KAUD-01`, `GLEX-01`, `GLEX-02`, `GMOR-01`, `GTXT-01`, `GPRO-01`, and `GAUD-01`.
- The map explicitly cites `32-43-SUMMARY.md`, `32-46-SUMMARY.md`, `32-48-SUMMARY.md`, and `32-49-SUMMARY.md`, including their non-sufficiency limits.
- The map marks source/final-bundle and voice-profile evidence as available but non-sufficient for downstream authority.
- The map sets `first_blocking_gate: production_database_authority` and explains that no production DB authority artifact currently exists.
- `32-50-SUMMARY.md` records zero production DB connections, zero DB mutations, zero provider calls, zero Azure calls, zero synthesis attempts, zero review applications, zero exports, zero release/publication/delivery actions, and no Phase 32 closure.
- Verification checks the map and summary content, runs focused no-regression tests for Korean audio registry refusal, and refreshes the GSDD session fingerprint.

## Common Pitfalls
- Reusing old waiver plans as proof of production completion.
- Treating the disposable/test DB migration rehearsal as production readiness.
- Treating the 10-item pilot as approval for a 3000-card run.
- Treating a voice-profile artifact as audio synthesis or heard approval.
- Asking for provider or Azure spend before the production job target is authorized and bound.
- Emitting raw credentials, paths, prompts, completions, provider payloads, generated text bodies, or audio bytes in a planning/evidence artifact.

## Stop-And-Challenge
- Stop if a real production DB URL, credential, provider payload, prompt/completion, private text/path, generated content body, or audio byte would need to be read or written.
- Stop if the command sequence would mutate a DB, call a network provider, synthesize audio, apply review, export, release, publish, deliver, commit, or close Phase 32.
- Stop if the map cannot prove the first blocker from existing sanitized evidence alone.

## Approval Gates
- The next execution lane after this plan must ask for explicit production database authority before any production connection, preflight, migration, job binding, or ingestion.
- Separate later approvals are required for full-run provider/model/budget/retry authority, provider credentials, DeepL translation, Azure synthesis, text/audio review application, content promotion, APKG/CSV/TSV export, release promotion, publication, delivery, Git actions, and Phase 32 closure.

<checks>
<plan_check>
checker: gsdd-plan-checker
checker_task_id: ses_f7edb0cfcffeMv9kPH26wQGDm6
checker_runtime: opencode subagent
status: passed
blocking: false
notes: Independent checker passed after requiring explicit requirement-to-gate coverage, direct consumed-plan dependencies, prior-summary citations, a runnable Python registry refusal check, and a scoped SPEC Current State update.
</plan_check>
</checks>

## Tasks

<task id="32-50-01" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/phase32-remaining-production-authority-map.md
  </files>
  <action>
    Create a hash/count-only remaining-execution map from existing Phase 32 summaries, sanitized evidence filenames, and known Korean CLI command families. The map must identify the available non-production evidence, each missing authority/evidence group, a requirement-to-gate table for all 11 Phase 32 requirement IDs, the exact first blocking gate `production_database_authority`, and the future command families that remain blocked behind that gate. Cite `32-43-SUMMARY.md`, `32-46-SUMMARY.md`, `32-48-SUMMARY.md`, and `32-49-SUMMARY.md` with their non-sufficiency limits. Do not read `.env`, connect to any database, inspect or include secrets, or call any external provider.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from pathlib import Path; p=Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/phase32-remaining-production-authority-map.md'); text=p.read_text(encoding='utf-8'); gates=['first_blocking_gate: production_database_authority','production_database_authority: missing','full_run_provider_budget_authority: missing','azure_synthesis_authority: missing','text_review_application: missing','audio_review_application: missing','export_release_delivery: missing','phase32_closure: blocked','no_external_side_effects: true']; reqs=['KFREQ-01','KFREQ-02','KFREQ-03','KTXT-01','KAUD-01','GLEX-01','GLEX-02','GMOR-01','GTXT-01','GPRO-01','GAUD-01']; deps=['32-43-SUMMARY.md','32-46-SUMMARY.md','32-48-SUMMARY.md','32-49-SUMMARY.md']; missing=[item for item in gates+reqs+deps if item not in text]; assert not missing, missing; banned=['postgresql://','postgres://','MULTILANG_DATABASE_URL','sk-','BEGIN PRIVATE','audio_bytes','provider_payload']; assert not any(token in text for token in banned)"`
  </verify>
  <done>
    The remaining Phase 32 production sequence is mapped from current evidence, and the first blocker is clearly production database authority.
  </done>
</task>

<task id="32-50-02" type="auto">
  <files>
    - MODIFY: .planning/SPEC.md
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/32-50-SUMMARY.md
  </files>
  <action>
    Write a non-closing summary that records the completed map, the first required user/operator gate, all external side effects that did not occur, and the exact next decision needed. Update `.planning/SPEC.md` Current State compactly to record Plan 32-50 as a non-closing authority sequencer and to keep production database authority as the next blocker. Keep Phase 32 in progress and do not create any authority file for production DB, provider budget, Azure synthesis, review application, export, release, publication, delivery, or Phase 32 closure.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from pathlib import Path; phase=Path('.planning/phases/32-frequency-portuguese-text-and-audio'); summary=(phase/'32-50-SUMMARY.md').read_text(encoding='utf-8'); evidence=(phase/'evidence-inbox/phase32-remaining-production-authority-map.md').read_text(encoding='utf-8'); spec=Path('.planning/SPEC.md').read_text(encoding='utf-8'); assert 'Phase 32 remains in progress' in summary; assert 'production database authority' in summary; assert 'No production database connection' in summary; assert 'No provider call' in summary; assert 'No Azure call' in summary; assert 'No audio synthesis' in summary; assert 'No review application' in summary; assert 'No export' in summary; assert 'No release, publication, or delivery' in summary; assert 'Plan 32-50' in spec; assert 'production database authority' in spec; assert 'first_blocking_gate: production_database_authority' in evidence"`
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from multilang.domain.jobs import SupportedLanguage; from multilang.services.audio_voice_registry import VoiceSelectionError, select_voice; ns={'ok': False}; exec('try:\n    select_voice(SupportedLanguage.KO)\nexcept VoiceSelectionError:\n    ok = True', globals(), ns); assert ns['ok']; assert select_voice(SupportedLanguage.PT).voice_id"`
    - Run `node .planning/bin/gsdd.mjs phase-status 32 in_progress`
    - Run `node .planning/bin/gsdd.mjs session-fingerprint write`
  </verify>
  <done>
    The summary makes the first authority gate actionable while preserving no-side-effect and non-closing boundaries.
  </done>
</task>

## Verification
- The map assertion verifies that all named remaining gates are present and that the first blocker is `production_database_authority`.
- The summary assertion verifies non-closing status and zero external side effects.
- Focused registry tests verify Korean is still not silently approved for static audio voice selection.
- `phase-status 32 in_progress` keeps the roadmap state honest.
- `session-fingerprint write` records the new planning/execution state.

## Success Criteria
- `phase32-remaining-production-authority-map.md` identifies the remaining production sequence and first authority blocker.
- `32-50-SUMMARY.md` records that no production DB, provider, Azure, synthesis, review, export, release, publication, delivery, or closure action occurred.
- Phase 32 remains in progress with the next operator decision clearly scoped to production database authority.

## High-Leverage Review
- Second pass required because this plan controls which side-effect lane is allowed to run next. The checker must confirm it does not smuggle provider, DB, Azure, review, export, release, publication, delivery, or closure authority into a documentation-only artifact.

## Leverage Review
- Lost: This plan spends one small documentation step instead of rushing into blocked production commands.
- Kept: Existing Phase 32 evidence, command surfaces, and fail-closed authority boundaries are preserved.
- Gained: The next step is unambiguous and safe to ask for: production database authority first.

## Notes
- Approach exploration is skipped because `.planning/phases/32-frequency-portuguese-text-and-audio/32-APPROACH.md` already records `alignment_status: user_confirmed` and this plan does not introduce a new product or technical approach.
- Additional research is skipped because this plan uses only existing Phase 32 summaries, validators, command names, and authority boundaries already established in code and prior plans.
- Control-map warnings are acknowledged: canonical worktree is dirty only from the existing Phase 33 `33-12-PLAN.md`, and invalid `/tmp/multilang-phase31-*` worktrees are not used as execution surfaces.
