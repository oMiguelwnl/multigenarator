---
phase: 32-frequency-portuguese-text-and-audio
plan: "27"
type: execute
wave: 39
runtime: opencode
assurance: self_checked
depends_on: [31-32, 32-25, 32-26]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-post-migration-validation.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pilot-job-binding.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/text-pilot-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/azure-catalog-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/provider-catalog-pilot-evidence.json
autonomous: true
requirements: [KFREQ-03, KTXT-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, GPRO-01, KAUD-01]
non_goals: ["No audio synthesis, profile approval, full run, human linguistic approval, fallback, review mutation, export, release, or publication."]
no_ui_proof_rationale: "Bounded authorized provider text and live catalog pilot only."
second_pass_required: true
closure_claim_limit: "Proves exact authorized migration/pilot execution and row-derived machine evidence; provider success cannot approve text or voice, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "The exact authorized production target reaches sole Alembic head 20260821_18 with post-migration schema proof before any job or adapter."
    - "Pilot job binds genuine Phase 31, exact final bundle and policy authorities only after migration proof."
    - "Text pilot executes exact 2+1, hard-gate-before-score, canonical pt/provider PT-BR and no fallback within approved ceilings."
    - "Live catalog capture is separately job-attributed and combined validation recomputes all attempts, budgets, denominators and zero synthesis."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pilot-job-binding.json, provides: "Exact staged pilot CAS"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-post-migration-validation.json, provides: "Exact target/head/schema post-migration proof"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/provider-catalog-pilot-evidence.json, provides: "Read-only row-derived pilot machine result"}
  key_links:
    - {from: "pilot/policy/final-bundle/Phase31 authorities", to: "text and catalog constructors", via: "fresh pre-constructor CAS and exact attempt ledger"}
---

# Phase 32 Plan 27: Authorized Migration, Text, and Catalog Pilot

<objective>
Prepare one exact production-bound job, run only the approved bounded text/catalog pilot, and reconcile all machine evidence read-only.

Purpose: A pilot must prove route/budget/catalog mechanics before any voice sample or full spend is considered.
Output: Job binding, text result, catalog result and combined pilot evidence.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-26-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-25-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/provider-policy.json
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/provider-review-authorization.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pilot-authorization.md
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-27-01" type="auto">
  <name>Apply the authorized sole migration, prove head, then prepare the pilot job</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-result.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-post-migration-validation.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pilot-job-binding.json
  </files>
  <action>Before any DB write, re-run Plan 26 read-only preflight and fixed-kind validation; require unexpired authority, exact non-secret target ID/locator hash, unchanged current revision/schema prestate, valid backup/restore/rollback hashes and expected sole head `20260821_18`. Point Alembic at that same secret environment target without persisting/logging the URL and run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync alembic upgrade head`. If already at exact head/schema, record exact no-write retry; otherwise require only revision `20260821_18` applied. Immediately reconnect, prove current revision and sole graph head equal `20260821_18`, expected schema constraints/ORM parity, no unexpected row mutation, target locator equality and recovery evidence continuity; atomically write migration result/post-validation. Only after that proof invokes `prepare-korean-frequency-job` against the same exact DB with one fixed job ID, genuine Phase 31 active pointer/receipt/selected snapshot hashes, exact immutable bundle root/manifest, source/build/final-bundle authorities and policy/provider-review/pilot authorities. Persist only locator/content/authority hashes and approved policy/counts, then run `check-korean-frequency-job-binding` after commit/expire/reload. No adapter constructor or second migration may run; failure stops before job creation.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; r=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox'); m=json.loads((r/'production-database-post-migration-validation.json').read_text()); d=json.loads((r/'pilot-job-binding.json').read_text()); assert m['status']=='passed' and m['current_revision']==m['sole_head']=='20260821_18' and m['target_matches_authority'] is True and m['unexpected_row_mutation_count']==0 and d['status']=='bound' and d['stage']=='pilot_base' and d['database_locator_sha256']==m['database_locator_sha256'] and d['adapter_constructor_count']==0 and d['fallback_policy']=='none'"</automated></verify>
  <done>The authorized exact target is proven at sole head 20260821_18 before one exact pilot job is durably bound and before any adapter attempt.</done>
</task>

<task id="32-27-02" type="auto">
  <name>Execute only the authorized bounded text pilot</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/text-pilot-result.json
  </files>
  <action>Run `generate-korean-frequency-text` for exactly the pilot item set/count in pilot authority. Immediately before each adapter constructor rerun genuine Phase 31 verify-active and full locator/content/authority CAS. Use exact approved definition/generation/repair/translation/judge routes and ceilings, exactly two initial candidates plus no more than one distinct repair, hard gates before adaptive score, canonical `pt` with provider-only `PT-BR`, and no Tatoeba/local/cross-provider fallback. Stop on any route/budget/policy drift and preserve item-isolated resumable state.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/text-pilot-result.json').read_text()); assert d['status'] in {'completed','completed_with_review_required'} and d['requested_item_count']==d['authorized_item_count'] and d['fallback_attempt_count']==0 and d['max_initial_candidates']==2 and d['max_repairs_per_item']&lt;=1"</automated></verify>
  <done>Only the approved pilot items use exact bounded routes and no fallback, with complete attempt evidence.</done>
</task>

<task id="32-27-03" type="auto">
  <name>Capture the authorized live catalog and reconcile combined pilot evidence</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/azure-catalog-result.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/provider-catalog-pilot-evidence.json
  </files>
  <action>Invoke `capture-korean-azure-catalog` once under the exact pilot route/region/endpoint ceiling after fresh genuine Phase 31 and pilot-base CAS. Persist complete response hash and validated rows, not secrets or payload content in telemetry. Then run `validate-korean-provider-catalog-pilot-result` read-only over DB/job/binding, Phase 31 tuple, bundle/source/build/final authorities, policy/review/pilot authorities, text and catalog results. Recompute attempt order, 2+1 histories, routes, retries/cache/latency/tokens/cost/denominators and prove zero synthesis/forbidden/fallback attempts and input invariance.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/provider-catalog-pilot-evidence.json').read_text()); assert d['status']=='passed' and d['catalog_attempt_count']==1 and d['synthesis_attempt_count']==0 and d['fallback_attempt_count']==0 and d['inputs_unchanged'] is True"</automated></verify>
  <done>Text/catalog pilot evidence is complete, row-derived, private-safe and non-authorizing.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-04 / SC-05 / SC-06 / SC-07 / SC-08 / SC-09 | Exact pilot text/catalog mechanics | COVERED |
| REQ | KFREQ-03 / KTXT-01 / GLEX-01 / GLEX-02 / GMOR-01 / GTXT-01 / GPRO-01 / KAUD-01 | Authority-bound pilot and telemetry | COVERED |
| RESEARCH | Bounded pilot then separate approval | Exact calls and row-derived validation | COVERED |
| CONTEXT | 2+1, pt-BR, one route/task, explicit budget, no fallback | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-27-01 | Prompt Injection / Misinformation | text pilot | mitigate | Strict untrusted output, immutable identity and hard local gates; no approval power. |
| T-32-27-02 | Denial of Service | paid calls | mitigate | Authority-bound item/attempt/token/cost/latency/batch ceilings. |
| T-32-27-03 | Spoofing / SSRF | Azure catalog | mitigate | Fixed approved regional endpoint and full pre-constructor CAS. |
</threat_model>
<high_leverage_review>Second pass: trace every constructor to fresh CAS, every call to exact authority/budget, all 2+1 histories, catalog attribution and zero synthesis/fallback.</high_leverage_review>
<verification>Parse all four exact result artifacts and rerun combined validator read-only.</verification>
<success_criteria>
- Job is exactly authority-bound before calls.
- Text and catalog pilot stay within approved routes/ceilings.
- Combined evidence is row-derived and non-authorizing.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-27-SUMMARY.md` with safe job/policy/root hashes, pilot counts, attempts/budgets/denominators, catalog status and bounded claim.</output>
