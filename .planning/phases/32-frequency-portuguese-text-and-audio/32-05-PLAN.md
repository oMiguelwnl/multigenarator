---
phase: 32-frequency-portuguese-text-and-audio
plan: "05"
type: execute
wave: 17
runtime: opencode
assurance: self_checked
depends_on: [32-04]
files-modified:
  - tests/test_runtime.py
  - tests/services/test_frequency_decks.py
  - tests/services/test_korean_text_quality.py
  - tests/repositories/test_text_repository.py
  - tests/services/test_text_validation.py
  - src/multilang/runtime.py
  - src/multilang/services/lexical_grounding.py
  - src/multilang/domain/korean.py
  - src/multilang/domain/text_quality.py
  - src/multilang/services/korean_text_quality.py
  - src/multilang/services/text_validation.py
autonomous: true
requirements: [KFREQ-03, KTXT-01, GMOR-01]
non_goals: ["No provider selection/call, human approval, Phase 33 grammar mastery, or strict-i+1 label for adaptive evidence."]
no_ui_proof_rationale: "Offline runtime composition and Korean quality policy only."
second_pass_required: true
closure_claim_limit: "Proves deterministic one-Kiwi, frozen-prefix, and hard-gate behavior only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "One lazy shared Kiwi serves grounding, validation, generation, and regeneration under explicit authority inputs."
    - "Known state is the genuine active Phase 31 snapshot plus frozen lexical ranks below n, independent of lower-rank text status."
    - "NFC, language, selected morphology, sense, register, naturalness, leakage, and translation consistency hard-gate before scoring."
  artifacts:
    - {path: src/multilang/runtime.py, provides: "Explicit authority inputs and one shared Kiwi"}
    - {path: src/multilang/services/korean_text_quality.py, provides: "Frozen-prefix adaptive evidence and hard gates"}
  key_links:
    - {from: "Phase 31 pointer/receipt/selected snapshot plus prior ranks", to: "adaptive evidence", via: "canonical text-status-independent prefix hash"}
    - {from: "candidate", to: "adaptive score", via: "all hard gates pass first"}
---

# Phase 32 Plan 05: Shared Kiwi, Frozen Adaptive Evidence, and Hard Gates

<objective>
Compose one explicit-authority runtime, build text-status-independent cumulative known state, and enforce Korean quality gates before scoring.

Purpose: Adaptive evidence must be reproducible and linguistic failure must never be traded for novelty.
Output: One-Kiwi wiring, frozen-prefix evidence, and Korean hard-gate service.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-04-PLAN.md
@src/multilang/services/korean_foundation_snapshot.py
@src/multilang/services/korean_morphology.py
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-05-01" type="auto" tdd="true">
  <name>Compose explicit authority inputs and one shared Kiwi without legacy regressions</name>
  <files>
    - MODIFY: tests/test_runtime.py
    - MODIFY: tests/services/test_frequency_decks.py
    - MODIFY: src/multilang/runtime.py
    - MODIFY: src/multilang/services/lexical_grounding.py
  </files>
  <behavior>
    - Runtime requires exact job/root/receipt inputs and never discovers Korean final authority from settings.
    - Grounding, validation, generation, and regeneration receive the same lazy Kiwi object.
    - Missing Korean authority blocks only Korean final work; existing modes remain unchanged.
  </behavior>
  <action>Wire explicit authority inputs through composition and all Korean final generation/resume consumers. Inject one lazy `KiwiKoreanMorphologyService`, disable Korean final seed grounding, preserve all non-Korean paths, and assert canonical `ko` while `ko-KR` occurs only at provider boundaries. Poison secondary analyzer construction and mutable-settings authority discovery.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/test_runtime.py -k 'korean and (locator or one or lazy or frequency or unavailable)' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_frequency_decks.py -k 'supported_frequency_assets' -q</automated></verify>
  <done>One explicit authority/analyzer path serves Korean final work without changing legacy modes.</done>
</task>

<task id="32-05-02" type="auto" tdd="true">
  <name>Build frozen-rank cumulative known state and adaptive evidence</name>
  <files>
    - CREATE: tests/services/test_korean_text_quality.py
    - MODIFY: tests/repositories/test_text_repository.py
    - MODIFY: src/multilang/domain/korean.py
    - MODIFY: src/multilang/domain/text_quality.py
    - CREATE: src/multilang/services/korean_text_quality.py
  </files>
  <behavior>
    - Rank 1 uses only genuine active Phase 31 concepts; ranks n use exactly accepted lexical identities at ranks below n.
    - Permuting lower-rank text/generation/review states cannot alter prefix count/hash.
    - Missing, reordered, duplicate, authority-drifted, or inconclusive concepts block or remain incidental.
  </behavior>
  <action>Implement `known_prefix(n)` from descriptor-safe reads of `data/korean_foundations/active-foundations.json`, its exact validation receipt and pointer-selected manifest/root plus frozen lexical ranks 1..n-1. Run genuine pathless `korean-foundations verify-active --expected-receipt-sha256`; bind pointer locator/content, receipt, snapshot and frozen-rank hashes. Never scan for or invent a Phase 31 authority file and never query text status. Persist target, canonical sorted known IDs/count/hash, observed/incidental IDs, candidate hash/ordinal, gates, score components and adaptive policy/scorer versions; selected-Kiwi-conclusive observations only.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_text_quality.py -k 'known_state or frozen_prefix or rank_boundary or text_status_independent or incidental' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/repositories/test_text_repository.py -k 'adaptive or prefix' -q</automated></verify>
  <done>Adaptive evidence is genuine-authority-bound, cumulative, text-status-independent, and reloadable.</done>
</task>

<task id="32-05-03" type="auto" tdd="true">
  <name>Enforce Korean naturalness, sense, register, and morphology hard gates</name>
  <files>
    - MODIFY: tests/services/test_korean_text_quality.py
    - MODIFY: tests/services/test_text_validation.py
    - MODIFY: src/multilang/services/korean_text_quality.py
    - MODIFY: src/multilang/services/text_validation.py
    - MODIFY: src/multilang/domain/text_quality.py
  </files>
  <behavior>
    - Wrong language/sense/register, mixed speech level, leakage, template/unnatural text, contradiction, or failed selected morphology is non-selectable.
    - Everyday Standard-Seoul 해요체 is default; exceptions require explicit context evidence and later qualified review.
    - Judge/machine evidence may reject or require review but cannot approve.
  </behavior>
  <action>Implement NFC/script/language/source-sense/target-morphology/naturalness/template/leakage/translation gates and Standard-Seoul 해요체 policy with controlled exception evidence. Treat judge/provider output as bounded untrusted data, reject identity/policy/approval fields, and require every hard gate before adaptive scoring. Preserve generic, Japanese, Mandarin, Latin, custom-list and highlight behavior.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_text_quality.py -k 'hard_gate or register or naturalness or sense or review' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_text_validation.py -k 'korean' -q</automated></verify>
  <done>No Korean/Portuguese hard failure can enter scoring, selection, or approval.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-04 / SC-07 | Natural Standard-Seoul adaptive examples and selected morphology | COVERED |
| REQ | KFREQ-03 / KTXT-01 / GMOR-01 | Frozen known state and hard gates | COVERED |
| RESEARCH | Cumulative adaptive evidence / hard-gate-first | Genuine Phase 31 plus prior ranks; naturalness before novelty | COVERED |
| CONTEXT | 해요체, adaptive not strict, one Kiwi, Phase 31 authority | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-05-01 | Spoofing | known-state authority | mitigate | Exact pointer/receipt/selected-snapshot and frozen-rank hashes. |
| T-32-05-02 | Tampering / Misinformation | scoring/provider evidence | mitigate | Hard-gate-first deterministic evidence and no model approval power. |
| T-32-05-03 | Denial of Service | analyzer | mitigate | One lazy shared Kiwi instance. |
</threat_model>
<high_leverage_review>Second pass: prove one Kiwi, frozen-prefix text-status independence, selected-morphology dominance, and hard-gate-before-score ordering.</high_leverage_review>
<verification>Run all three task commands offline and mutation-test rank boundaries, analyzer drift, and quality failures.</verification>
<success_criteria>
- One shared Kiwi and explicit authority compose cleanly.
- Frozen known prefixes are deterministic.
- Linguistic/translation failures cannot be scored or approved.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-05-SUMMARY.md` with one-Kiwi proof, prefix hashes, gate matrix, regressions, and bounded self-checked claim.</output>
