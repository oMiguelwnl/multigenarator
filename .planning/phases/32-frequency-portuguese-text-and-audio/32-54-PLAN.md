---
phase: 32-frequency-portuguese-text-and-audio
plan: "54"
type: execute
wave: 66
runtime: opencode
assurance: self_checked
depends_on: [32-53]
files-modified:
  - .planning/.state-fingerprint.json
  - .planning/SPEC.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-54-PLAN.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-54-SUMMARY.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-authorization.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-preflight.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-result.json
autonomous: false
terminal_checkpoint: wait_for_user_before_3000_item_execution
requirements:
  - KFREQ-01
  - KFREQ-02
  - KFREQ-03
  - KTXT-01
  - GLEX-01
  - GLEX-02
  - GMOR-01
  - GTXT-01
  - GPRO-01
non_goals:
  - Do not process more or fewer than 20 Korean frequency candidates, and do not run any intermediate pilot after this smoke.
  - Do not call Azure, inspect the Azure catalog, synthesize audio, import or apply human review, promote content, export APKG/CSV/TSV, release, publish, deliver, commit, PR, close Phase 32, or execute the 3000-item full run.
  - Do not mutate, delete, reset, truncate, or overwrite the existing `phase32-prod-freq-pilot-base` job or any prior pilot evidence.
  - Do not print or persist credentials, a raw database URL or locator component, actual prompt/completion values, serialized provider payload/body values, generated Korean/Portuguese learner text, SQL row content, or audio bytes. Policy prose naming these forbidden classes is allowed.
hard_boundaries:
  - Treat the user's current-session instruction as authority for one text-only smoke of exactly 20 items, split into two sequential units of at most 10; it is not authority for 3000 items.
  - Use job ID `phase32-prod-text-smoke-20`, a new `pilot_base` authority binding, and a new authorization-file SHA-256; preserve the Plan 32-53 job unchanged.
  - Create the new job directly with persisted `total_items=20`, `language=ko`, and `source_type=frequency`; do not call or copy the legacy `_ensure_korean_frequency_job` behavior that hard-codes `total_items=3000`.
  - Load secrets only in process from `.env`; do not `source .env`, pass secrets in argv, print them, persist them, or include them in exceptions/evidence.
  - Pin settings to LiteLLM/OpenAI `gpt-4.1-mini`, DeepL, two attempts, no fallback, concurrency 1, no Tatoeba, and no audio invocation. The approved provider policy remains SHA-256 `6174ebd73b7e2963285bb2e44205dde10ef9d3917e50c90850b483b3c8a6fc79`.
  - Before each unit, revalidate Phase 31 authority, final bundle authority, production DB locator hash, persisted job authority, policy/model/provider settings, candidate and text-record counts, and the 10-item maximum.
  - Unit 2 may run only if unit 1 processed exactly 10 items, created exactly 10 text records, produced no audio/export rows, and retained matching policy/model authority. Stop otherwise.
  - A passing smoke permits only creation/checking of a direct full-run plan; execution of 3000 items requires a fresh explicit user confirmation after advance notice.
  - Because `.planning/config.json` enables `workflow.auto_advance`, this plan must terminate in a manual `wait_for_user_before_3000_item_execution` checkpoint and return control to the user. It must not auto-start another execution workflow.
escalation_triggers:
  - Stop before provider construction if required DB/OpenAI/DeepL credentials are absent, duplicated, malformed, or cannot be loaded without disclosure.
  - Stop if the production DB is not the Plan 32-53 PostgreSQL locator at Alembic head `20260828_19`, the prior production job changed unexpectedly, or the smoke job exists with different authority/content.
  - Stop if deterministic selection does not yield exactly level-1 ranks 1 through 20, if any candidate/text/provider attempt already exists outside an exact same-job replay, or if any selected identity/hash drifts.
  - Stop if the runtime model/provider differs from the pinned routes, fallback is enabled, concurrency exceeds 1, attempts exceed 2, a unit exceeds 10 items, audio could be invoked, or provider telemetry lacks required route/budget/cache/schema hashes.
  - Stop after the first failing item/unit; do not compensate by selecting replacement candidates or expanding the denominator.
  - Stop if any output or error path could reveal secrets, locators, generated text values, actual prompts/completions, serialized provider payload values, SQL row content, or unapproved private paths.
approval_gates:
  - Approved now: secret-safe credential loading; one production PostgreSQL text-only smoke of exactly 20 items; two sequential units of at most 10; OpenAI `gpt-4.1-mini`; DeepL PT-BR; policy-bounded retry; and sanitized hash/count/status evidence.
  - Not approved: any 21st item, 3000-item execution, Azure/audio, human-review application, promotion, export, release, publication, delivery, Git action, or Phase 32 closure.
  - If this smoke passes, notify the user with counts/status and ask for explicit confirmation immediately before any 3000-item provider execution.
  - The only allowed next planning route is one direct 3000-item plan with no intermediate pilot; planning may be proposed after this checkpoint, but execution remains blocked until a fresh recorded answer.
anti_regression_targets:
  - The Plan 32-46 local SQLite 10-item pilot and Plan 32-53 production DB-only job binding remain immutable historical evidence.
  - Korean remains absent from `audio_voice_registry`; `select_voice(SupportedLanguage.KO)` must still fail.
  - Text rows from this smoke remain unreviewed production smoke evidence and cannot satisfy full-run, human-review, audio, export, release, or delivery gates.
known_unknowns:
  - The live providers' availability, returned quality, validation outcomes, token usage, and estimated cost are known only after execution.
  - Accepted versus review-required distribution is not predetermined; success means safe bounded execution and complete evidence, not a required acceptance percentage.
  - Full-run provider budgets, 3000-item authority, review workflow, Azure voice/audio authority, export, and release remain unresolved after this plan.
requirements_progress: partial_evidence_only
ui_proof_slots: []
no_ui_proof_rationale: Backend production smoke only; no UI, Anki rendering, audio playback, or learner-ready claim is made.
high_leverage_surfaces:
  - First live provider-backed Korean text rows in the reclassified production PostgreSQL target.
  - Exact 20-item denominator split across two policy-compliant 10-item units.
  - Secret-safe, content-free evidence suitable for deciding whether to plan the direct 3000-item run.
second_pass_required: true
closure_claim_limit: This plan can prove only one text-only 20-item production smoke under the pinned provider policy and sanitized evidence. It cannot claim human-reviewed quality, audio readiness, export readiness, full-run authority, release readiness, delivery, or Phase 32 completion.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: The smoke intentionally exercises only level-1 ranks 1-20 and cannot establish corpus-wide quality.
  kept: Existing Phase 31, source, bundle, provider-policy, DB, and no-audio authority boundaries remain fail-closed.
  gained: A production-backed live signal for 20 deterministic items before requesting authority for the direct 3000-item run.
must_haves:
  truths:
    - Exactly 20 deterministic Korean frequency candidates are ingested into a separate production smoke job and no prior job is changed.
    - The separate GenerationJob is persisted and reloaded with `total_items=20`; no 3000-item denominator is bound to smoke authority.
    - Text generation runs sequentially as two units of exactly 10 with `missing_only`, `gpt-4.1-mini`, DeepL, at most two attempts, concurrency 1, no fallback, and no audio.
    - Evidence contains only hashes, counts, controlled identifiers/statuses, aggregate telemetry, and no learner/provider/secret content.
    - Passing the smoke does not execute or authorize the 3000-item full run; the user is notified before that run.
  artifacts:
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-authorization.md
      provides: Sanitized current-session authority for exactly 20 text-only items and explicit denial of the 3000-item run.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py
      provides: Phase-local fail-closed helper for secret-safe preflight, deterministic setup, two bounded provider units, and content-free evidence.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-preflight.json
      provides: Hash/count-only proof of exact authority, settings, credentials-present booleans, DB/job state, selected denominator, and zero pre-call downstream side effects.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-result.json
      provides: Per-unit and aggregate counts/status/telemetry hashes proving exactly 20 processed items and no audio/export/full-run execution.
  key_links:
    - from: current-session 20-item instruction
      to: production-text-smoke-20-authorization.md
      via: exact bounded powers and full-run denial captured before execution.
    - from: production-text-smoke-20-authorization.md
      to: KoreanFrequencyJobAuthority.pilot_authority_sha256
      via: raw authorization-file SHA-256 bound to the separate smoke job.
    - from: final Korean frequency bundle ranks 1-20
      to: phase32-prod-text-smoke-20 lexical candidates
      via: authority-revalidated load, level-1 build, deterministic slice, and content-free identity-root hash.
    - from: provider-policy.json
      to: two generate_text calls
      via: policy SHA/model/provider/budget validation and `max_items=10`, `missing_only=true`, `synthesize_audio=false`, `concurrency=1`.
---

# Phase 32 Plan 54: Production Text Smoke Of 20 Items

## Objective
Execute the user-authorized production text smoke for exactly 20 deterministic Korean frequency items, using two sequential policy-bounded units of 10 against a separate PostgreSQL job. Produce only sanitized decision evidence and stop before any audio, review application, export, full run, or closure action.

## Context
- Plan 32-53 proved the current reclassified PostgreSQL target is writable, at Alembic head `20260828_19`, and bound one zero-candidate `pilot_base` job without external calls.
- The user explicitly requested a 20-item smoke now and, if it passes, a direct plan for all 3000 without intermediate pilots, with advance notice before the full run.
- The approved provider policy permits at most 10 batch items, concurrency 1, two attempts, no fallback, OpenAI `gpt-4.1-mini` for text routes, and DeepL `PT-BR` for translation. Therefore 20 items require two sequential units.
- The existing setup helper is intentionally local-SQLite-only and exactly 10 items. This plan uses a separate phase-local production helper rather than weakening or repurposing that historical pilot path.

<assurance_check>
source_artifact: .planning/phases/32-frequency-portuguese-text-and-audio/32-53-SUMMARY.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: self_checked
status: matched_after_checker
warning: None; independent gsdd-plan-checker passed before any paid provider call.
</assurance_check>

## Requirements Covered
- Partial evidence only for KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, and GPRO-01. This smoke satisfies none of those requirements or any ROADMAP success criterion by itself.

## Must-Haves
1. Record exact 20-item authority separately from the historical 10-item pilot authority.
2. Preflight credentials, policy, model, Phase 31, bundle, DB, job, denominator, and no-audio boundaries without disclosing sensitive data.
3. Ingest only level-1 ranks 1-20 into `phase32-prod-text-smoke-20` and bind `pilot_base` authority using the new authorization-file hash.
4. Generate text as unit 1 (10) and unit 2 (10), sequentially, with a hard gate between them.
5. Persist content-free evidence and stop; if passed, prepare/offer the direct 3000-item plan but do not execute it without fresh confirmation.

## Evidence Contract
- Authorization records `authorized_item_count=20`, `unit_count=2`, `max_items_per_unit=10`, `model=gpt-4.1-mini`, `translation=deepl-api-PT-BR`, `max_attempts=2`, `max_concurrency=1`, `fallback=none`, `audio=false`, and `full_run_authorized=false`.
- Preflight records only hashes, controlled identifiers, counts, booleans, Alembic head, production locator hash, separate job ID, authority hash, selected identity-root hash, and zero text/provider/audio/export counts before execution.
- Preflight records the reloaded job contract `total_items=20`, `language=ko`, and `source_type=frequency`, and proves the Plan 32-53 job row/authority hash is unchanged.
- Result records each unit's processed/accepted/review-required counts; aggregate candidate/text/provider-attempt counts and telemetry completeness; policy/model/provider sets; token/cost aggregates when supplied by providers; zero audio/export counts; `full_run_attempted=false`; and a controlled passed/blocked/failed status.
- No artifact or terminal output may contain generated learner values, definition/translation values, actual prompts/completions, raw serialized provider payload/response-body values, DB rows, secrets, URLs, locator components, or private absolute home paths. Control-language occurrences in plans/summaries and the approved `/tmp/opencode` literal are not leaks.

## Stop-And-Challenge
- A same-job rerun is allowed only as an idempotent evidence check when all 20 text records already exist and no new provider call can occur; partial existing execution requires a newly checked recovery plan, not automatic continuation.
- A failed or incomplete first unit blocks the second. A failed second unit records the bounded failure and stops without replacement items.
- Any discrepancy between configured adapter model/provider and policy route blocks before calls.
- Provider responses may be persisted by existing repositories as application data, but they must never be copied into planning evidence or terminal output.

## Approval Gates
- This plan consumes the user's explicit current-session 20-item smoke instruction as paid provider authority for the exact bounded operation above.
- The checker must pass before execution.
- Any full-run execution still requires advance notice and fresh explicit confirmation from the user, even when this result is `passed`.
- Plan completion must set and report `wait_for_user_before_3000_item_execution`; it may not rely on prose alone because workflow auto-advance is enabled.

<checks>
<plan_check>
checker: gsdd-plan-checker
checker_task_id: ses_f7a2260edffeDxXxu1z3nW7clT
checker_runtime: opencode subagent
status: passed
blocking: false
notes: Independent checker passed after revisions requiring persisted total_items=20, contained runtime-log scanning, contextual artifact leak rules, exact runnable checks, partial-requirement wording, and a terminal wait-for-user checkpoint.
</plan_check>
</checks>

## Tasks

<task id="32-54-01" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-authorization.md
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py
  </files>
  <action>
    Capture the exact current-session authority and implement a phase-local helper with `--self-check`, `--preflight-setup`, `--verify-preflight`, `--execute`, private `--execute-worker`, `--verify-result`, and `--verify-idempotent` modes. Parse `.env` in process without shell execution; reject duplicate required keys; construct `Settings` with explicit `text_generation_provider="litellm"`, `text_generation_model="gpt-4.1-mini"`, `translation_provider="deepl"`, `default_retry_attempts=2`, and `tatoeba_enabled=false`; inject a forbidden audio adapter so construction cannot contact Azure. Reuse production code for Phase 31 fallback verification, final-bundle loading, `KoreanFrequencyJobAuthority`, repositories, and runtime generation. Create `phase32-prod-text-smoke-20` directly with persisted/reloaded `total_items=20`, `language=ko`, and `source_type=frequency`; do not reuse the legacy helper's 3000 denominator. Select exactly level-1 ranks 1-20, persist candidates with deterministic item keys, and write only sanitized evidence through atomic replacement. Public `--execute` must spawn the private worker with provider debug logging disabled, capture stdout/stderr into mode-0600 temporary files under `/tmp/opencode`, scan them before emitting output against exact loaded secrets, parsed locator components, URL forms, credential assignments, private absolute home paths, and Hangul; inspect prompt/completion/payload/body/generated-content terms contextually only as anchored JSON keys, assignments, serialized values, or captured runtime-log records, not as policy prose. Record only hashes/byte counts/pass status, securely remove the logs in `finally`, and print only a generic status/count line. The approved `/tmp/opencode` directory literal and documented control-language terms are allowlisted only in planning prose, never in runtime values. Unscannable or leaking logs make execution fail closed without replay.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --self-check`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -m py_compile .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py`.
    - Self-check must use fakes only, prove 20 becomes `[10, 10]`, reject 19/21, reject a persisted denominator other than 20, reject model/provider/policy drift, reject audio invocation, reject partial replay, and prove subprocess log capture/redaction/removal plus rendered-evidence scans. Include fixtures proving policy prose and the approved `/tmp/opencode` literal pass while exact secrets, locator components, URL forms, Hangul, anchored generated-content keys/assignments, and serialized prompt/completion/payload/body values fail.
  </verify>
  <done>
    The helper compiles and its offline self-check proves denominator splitting, fail-closed authority/settings checks, no-audio construction, replay rules, and evidence redaction without a DB/provider call.
  </done>
</task>

<task id="32-54-02" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-preflight.json
  </files>
  <action>
    Run `--preflight-setup`. Revalidate authorization and all immutable hashes, check only credentials-present booleans, verify the PostgreSQL locator hash and Alembic head, prove the Plan 32-53 job is unchanged, directly create/bind the separate smoke job with `total_items=20`, ingest exactly the deterministic 20 candidates, reload and verify its complete job contract, and prove zero preexisting text/provider/audio/export side effects. Stop before provider construction on any mismatch.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --preflight-setup`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-preflight` and require exit 0. It must assert `status=ready`, the exact job/policy/DB/head identifiers, `total_items=20`, `language=ko`, `source_type=frequency`, `candidate_count=20`, `text_record_count=0`, `provider_attempt_count=0`, all audio/export counts zero, `[10,10]` units, required credential booleans true, unchanged Plan 32-53 job hash, and `full_run_authorized=false`.
    - The same `--verify-preflight` command must scan exact in-process secrets, parsed locator components, PostgreSQL URL forms, credential assignments, Hangul, and private absolute home paths across artifacts; prompt/completion/generated-content/provider-payload/body detection is limited to anchored keys, assignments, serialized values, or runtime logs so documented policy prose does not false-positive.
  </verify>
  <done>
    The separate production smoke job is authority-bound with exactly 20 candidates and no provider/audio/export effects, and sanitized preflight evidence passes leak checks.
  </done>
</task>

<task id="32-54-03" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-result.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/32-54-SUMMARY.md
    - MODIFY: .planning/SPEC.md
    - MODIFY: .planning/.state-fingerprint.json
  </files>
  <action>
    Run public `--execute` once through its contained subprocess wrapper. The worker must repeat all preflight checks, call `generate_text` for unit 1 with `missing_only=true`, `max_items=10`, `synthesize_audio=false`, and `concurrency=1`, then query and validate the content-free unit result. Only when unit 1 is exactly complete and policy-clean may it repeat the authority checks and process unit 2 identically. The parent must scan and remove captured logs before reporting. Record sanitized aggregate evidence, run second-pass DB/evidence/leak checks, update SPEC truthfully to say this is partial evidence and no requirement/ROADMAP criterion is newly satisfied, refresh the state fingerprint, and summarize without reproducing generated content. Do not create or execute a full-run command. Set the terminal state to `wait_for_user_before_3000_item_execution`, stop auto-advance, report sanitized counts/status to the user, and return control.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --execute`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-result` and require exit 0. It must assert `status=passed`, two unit results each with `processed_items=10`, aggregate `processed_items=20`, `total_items=20`, `text_record_count=20`, `accepted_items + review_required_items = 20`, complete policy telemetry hashes, only approved provider/model identifiers, attempts within policy, captured-log scan/removal status passed, `audio_synthesis_enabled=false`, all audio/export counts zero, `full_run_attempted=false`, and terminal state `wait_for_user_before_3000_item_execution`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-idempotent` and require exit 0 with no additional provider attempts and unchanged candidate/text identity roots.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest -q tests/domain/test_korean_provider_policy.py tests/cli/test_korean_provider_commands.py tests/services/test_korean_provider_pilot_evidence.py`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync ruff check .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py`.
    - Run the helper's `--verify-result` again after summary/SPEC/fingerprint writes so its contextual exact-secret/content scanner covers every Plan 32-54 artifact without treating policy prose or the approved `/tmp/opencode` literal as leaks; require exit 0.
    - Through the orchestrator Task tool, launch exactly one available `gsd-verifier` subagent against Plan 32-54. Require a passing verdict on exact denominator, DB state, policy telemetry, no-audio/export/full-run deltas, leak evidence, partial-requirement wording, and the terminal user checkpoint. Record its task ID and machine token `status: passed` in the `32-54-SUMMARY.md` `<checks><verification>` block before final reporting.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from pathlib import Path; body=Path('.planning/phases/32-frequency-portuguese-text-and-audio/32-54-SUMMARY.md').read_text(encoding='utf-8'); block=body.split('<verification>',1)[1].split('</verification>',1)[0]; assert 'verifier: gsd-verifier' in block and 'task_id: ' in block and 'status: passed' in block"` and require exit 0 before the terminal user report.
  </verify>
  <done>
    Exactly 20 text items were processed in two bounded units with no audio/export/full-run action; sanitized evidence is internally consistent and leak-free; none of the mapped requirements or ROADMAP criteria is marked satisfied; Phase 32 remains in progress; execution is stopped at `wait_for_user_before_3000_item_execution`; and control has returned to the user before any 3000-item action.
  </done>
</task>
