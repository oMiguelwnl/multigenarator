---
phase: 32-frequency-portuguese-text-and-audio
plan: "16"
type: execute
wave: 28
runtime: opencode
assurance: self_checked
depends_on: [32-15]
files-modified:
  - tests/services/test_korean_release_safety.py
  - tests/cli/test_korean_release_safety_commands.py
  - tests/services/test_korean_release_delivery.py
  - tests/cli/test_korean_release_delivery_commands.py
  - src/multilang/services/korean_release_safety.py
  - src/multilang/services/korean_release_delivery.py
  - src/multilang/cli.py
autonomous: true
requirements: [KFREQ-01, KFREQ-02, KTXT-01, KAUD-01, GPRO-01, GAUD-01]
non_goals: ["No real Git/remote action, production output, arbitrary URL/module/command, generalized release platform, or Phase 34 closure."]
no_ui_proof_rationale: "Offline final authorization and constrained delivery tooling only."
second_pass_required: true
closure_claim_limit: "Proves fake/offline authorization, action, actual-state and idempotence controls only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Final authorization binds exact pointer-selected immutable tree, safety/build evidence and separate commit/publication token scopes or absence."
    - "Delivery uses structured Git argv with shell=False and fixed adapter/destination IDs over allowlisted release-relative members only."
    - "Independent validation inspects actual Git/remote/no-action state and exact retry idempotence rather than trusting action labels."
  artifacts:
    - {path: src/multilang/services/korean_release_safety.py, provides: "Read-only final release authorization validator"}
    - {path: src/multilang/services/korean_release_delivery.py, provides: "Constrained delivery action and independent validator"}
  key_links:
    - {from: "pointer-selected tree and safety", to: "delivery action", via: "validated exact token/allowlist preflight"}
    - {from: "action result", to: "closure", via: "independent actual Git/remote or zero-action inspection"}
---

# Phase 32 Plan 16: Final Authorization and Constrained Delivery Tooling

<objective>
Implement exact release-authorization validation, constrained token-scoped delivery, and independent actual-state/idempotence proof.

Purpose: Optional commit/publication actions must never exceed exact safe immutable bytes, and absent tokens must mechanically prove zero action.
Output: Authorization, action and independent validator commands with fake/offline tests.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-15-PLAN.md
@src/multilang/cli.py
<interfaces>
Create `validate-korean-release-authorization`, `execute-korean-release-delivery`, and `validate-korean-release-delivery`. Every command consumes one explicit immutable release directory plus the current-release pointer selecting it, final authority/sidecar, safety report and build result; action/validation additionally consume preflight and action results.
</interfaces>
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-16-01" type="auto" tdd="true">
  <name>Validate exact safety-bound final release authorization</name>
  <files>
    - MODIFY: tests/services/test_korean_release_safety.py
    - MODIFY: tests/cli/test_korean_release_safety_commands.py
    - MODIFY: src/multilang/services/korean_release_safety.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Validator rehashes authority/sidecar, pointer, selected immutable tree/manifest/members, safety report and build result.
    - Unknown, drifted, private/local-only, scan/license/retention-unknown or safe_to_publish=false publication entries fail.
    - Commit and publication tokens/allowlists are separate; absent token is valid zero-action scope.
  </behavior>
  <action>Implement read-only authorization validation. Require the pointer to descriptor-safely select the supplied immutable directory and bind pointer locator/content plus full tree root. Parse one canonical machine section, resolve requested IDs against safety evidence and enforce separate commit/publication scopes. Reject loose/staging/alternate paths and arbitrary destination data; emit safe preflight facts only and mutate nothing.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_release_safety.py -k 'authorization or unsafe_scope or token or drift or read_only' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_release_safety_commands.py -k 'validate_authorization or output' -q</automated></verify>
  <done>Only pointer-selected safety-cleared immutable members and explicit/absent channel scopes pass.</done>
</task>

<task id="32-16-02" type="auto" tdd="true">
  <name>Execute only constrained token-authorized Git and fixed-destination actions</name>
  <files>
    - CREATE: tests/services/test_korean_release_delivery.py
    - CREATE: tests/cli/test_korean_release_delivery_commands.py
    - CREATE: src/multilang/services/korean_release_delivery.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Git uses fixed structured argv with shell=False and exact allowlisted paths.
    - Publication uses only fixed adapter/destination IDs and exact token-bound allowlists.
    - Missing token performs zero channel action; exact prior result plus actual state is no-op, partial/divergent state fails.
  </behavior>
  <action>Implement delivery action over the revalidated pointer-selected tree, authority, safety/build and preflight. Reject arbitrary URLs/modules/commands/shell input and mutable paths. Stage/commit only allowlisted release-relative members with structured argv `shell=False`; call only fixed publication adapters/destinations. Snapshot actual Git/destination attempt state before/after. Missing token does nothing for that channel. Exact retry returns no-op; partial/divergent state fails.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_release_delivery.py -k 'action or shell_false or allowlist or fixed_adapter or zero_action or retry' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_release_delivery_commands.py -k 'execute or token or no_action' -q</automated></verify>
  <done>Delivery is fixed-purpose, shell-free, token-scoped, zero-action-safe and idempotence-aware.</done>
</task>

<task id="32-16-03" type="auto" tdd="true">
  <name>Independently validate actual Git, remote, zero-action, and idempotence state</name>
  <files>
    - MODIFY: tests/services/test_korean_release_delivery.py
    - MODIFY: tests/cli/test_korean_release_delivery_commands.py
    - MODIFY: src/multilang/services/korean_release_delivery.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Git validation resolves actual HEAD/parent/commit/tree/changed paths/blob IDs/modes and hashes blobs.
    - Token-authorized publication queries only the fixed destination and compares object IDs/checksums/lengths.
    - Absent tokens prove unchanged actual state/counters; forged action labels fail and identical retry is no-op.
  </behavior>
  <action>Implement independent validation by rehashing pointer/tree/authority/preflight/action inputs, inspecting actual Git objects/working/staged/committed bytes, or querying the one token-approved fixed destination. For absent tokens compare mechanical snapshots/counters. Re-run action/validation tuples in tests to prove no-op idempotence and reject partial/drifted states. Use temporary Git repositories and fake fixed destinations only.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_release_delivery.py -k 'validator or git_object or remote_checksum or forged_label or zero_action or idempotence' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_release_delivery_commands.py -k 'validate_delivery or actual_state or idempotence' -q</automated></verify>
  <done>Independent actual-state evidence proves exact authorized action or corresponding zero action and retry idempotence.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-02 / SC-05 / SC-09 / SC-10 | Safe exact optional delivery or zero action | COVERED |
| REQ | KFREQ-01 / KFREQ-02 / KTXT-01 / KAUD-01 / GPRO-01 / GAUD-01 | Release scope and delivery evidence | COVERED |
| RESEARCH | Authorization / constrained delivery | Exact pointer, shell-free action, actual-state proof | COVERED |
| CONTEXT | Separate commit/publication checkpoint and valid local-only result | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-16-01 | Elevation | authorization | mitigate | Exact safety-bound pointer/tree and separate token scopes. |
| T-32-16-02 | Injection | Git/destination | mitigate | Fixed structured argv shell=False and fixed adapter registry. |
| T-32-16-03 | Spoofing / Repudiation | action result | mitigate | Independent actual Git/remote/no-action inspection. |
</threat_model>
<high_leverage_review>Second pass: inspect scope derivation, token absence, shell-free argv, fixed destination, actual-state validation and exact retry behavior.</high_leverage_review>
<verification>Run all three test commands offline with temporary Git and fake destinations only.</verification>
<success_criteria>
- Authorization binds exact safe immutable bytes.
- Action cannot exceed fixed token scope.
- Independent validator proves actual or zero side effects and idempotence.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-16-SUMMARY.md` with authorization/action/validator matrices, shell-free proof, zero-action/idempotence cases and bounded self-checked claim.</output>
