---
phase: 32-frequency-portuguese-text-and-audio
plan: "26"
type: execute
wave: 38
runtime: opencode
assurance: self_checked
depends_on: [32-25]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-preflight.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-authorization.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-authorization.md.sha256
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-authority-validation.json
autonomous: false
requirements: [KFREQ-01, GLEX-02, GPRO-01, KAUD-01, GAUD-01]
user_setup:
  - service: "Phase 32 production PostgreSQL target"
    why: "Exact authorized Alembic migration target for the following action plan"
    env_vars:
      - name: "MULTILANG_DATABASE_URL"
        source: "Operator-selected production database secret; never persist or disclose the value"
non_goals: ["No migration, schema/data mutation, job preparation, provider call, generation, review import, release, Git action, or secret persistence."]
hard_boundaries: ["Inspect only the explicitly selected target with read-only SQL and persist only a canonical locator hash/non-secret target ID.", "Authorization is valid only for sole head 20260821_18, one target/prestate, one expiry, and the next plan's migration action."]
escalation_triggers: ["Stop on ambiguous target, unexpected revision/head, failed read-only connectivity, schema drift, missing backup/restore proof, inadequate rollback evidence, or stale/expired authority."]
approval_gates: ["Plan 27 may run `alembic upgrade head` only after this sidecar-bound target-specific authority validates immediately before mutation."]
anti_regression_targets: ["The checkpoint itself performs zero DDL/DML and cannot authorize a second revision, downgrade, alternate database, provider work, or job creation."]
known_unknowns: ["The production target, current revision, backup/restore evidence, maintenance scope, and expiry remain execution-time operator facts."]
no_ui_proof_rationale: "Read-only production database preflight and human migration authorization only."
high_leverage_surfaces: ["Production target identity, Alembic prestate/head, backup/rollback evidence, and least-power migration authority."]
second_pass_required: true
closure_claim_limit: "Authorizes at most one exact production migration to sole head 20260821_18; no migration or job preparation occurs here, and fresh external plan recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
leverage: {lost: "No implicit database target or automatic migration convenience.", kept: "The sole reversible Phase 32 Alembic revision and explicit database selection.", gained: "Target/prestate/backup/expiry-bound migration authority with zero checkpoint mutation."}
must_haves:
  truths:
    - "Read-only preflight binds one non-secret database target ID and canonical locator hash, connectivity, current revision/schema prestate, and expected sole head 20260821_18."
    - "Backup/restore and rollback evidence is current, exact, scoped, and independently identified before approval."
    - "The immutable authority expires, names one target/prestate/head, and grants migration only—not job/provider/content/release power."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-preflight.json, provides: "Read-only exact production DB readiness"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-authorization.md, provides: "Human exact-target migration authority"}
  key_links:
    - {from: "target locator/revision/schema and backup/rollback evidence", to: "Plan 27 migration", via: "expiring sidecar-bound exact target/prestate/head authority"}
---

# Phase 32 Plan 26: Production Database Migration Authorization Checkpoint

## Objective
<objective>
Read-only identify the exact production PostgreSQL target and obtain expiring least-power authority for Plan 27 to apply the sole Phase 32 revision.

Purpose: Provider readiness cannot imply permission to mutate an ambiguous or unprotected database.
Output: Target/prestate/backup preflight and immutable migration authority.
</objective>

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
@.agents/skills/supabase-postgres-best-practices/SKILL.md
</execution_context>

## Context
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-25-PLAN.md
@alembic/versions/20260821_18_frequency_text_audio_evidence.py
@alembic/versions/20260804_17_korean_lexical_identity.py
</context>

## Requirements Covered
- KFREQ-01, GLEX-02, GPRO-01, KAUD-01, and GAUD-01 persistence prerequisites.

## Must-Haves
- The frontmatter truths, artifacts, and key link are normative.

## Anti-Goals
- Do not run Alembic, write the database, create a job, or expose the database URL.

## Hard Boundaries
- Read-only target inspection plus checkpoint authority evidence only.

## Evidence Contract
- Preflight must contain canonical target locator hash, non-secret target ID, current revision/head graph, schema prestate hash, read-only connectivity, backup/restore and rollback evidence hashes, expiry, scope, and zero mutations.

## Common Pitfalls
- Hostname labels, successful connection, a backup claim without restore evidence, or a generic “production” token do not identify or authorize the target.

## Stop-And-Challenge
- Stop on any target/revision/schema/backup/expiry ambiguity or if current revision is neither verified predecessor `20260804_17` nor exact already-applied head `20260821_18`.

## Approval Gates
- The user must explicitly approve the exact target/prestate/head/backup/rollback/expiry tuple before Plan 27.

## Checks
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

## Tasks
<tasks>
<task id="32-26-01" type="auto">
  <name>Produce exact read-only production database and rollback preflight</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-preflight.json
  </files>
  <action>Require explicit `MULTILANG_DATABASE_URL` presence without logging its value. Canonicalize it into the sole locator SHA-256 helper and discard the raw locator; record a non-secret operator target ID. Connect read-only, prohibit DDL/DML, enumerate the current Alembic revision and sole-head graph, hash the relevant schema/catalog prestate, and require predecessor `20260804_17` or exact already-applied `20260821_18`. Bind current backup ID/hash/time/scope, successful restore-test evidence, rollback procedure hash, maintenance/expiry scope, application compatibility preflight, and zero mutation counters. Reject replicas/alternate databases, multiple heads, unknown revisions, stale backup/restore proof, raw URL/credentials/private paths, or a migration already in progress.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-preflight.json').read_text()); assert d['status']=='ready_for_migration_decision' and d['expected_head']=='20260821_18' and d['current_revision'] in {'20260804_17','20260821_18'} and d['read_only_connectivity'] is True and d['backup_restore_evidence_valid'] is True and d['mutation_count']==0 and 'database_url' not in d"</automated></verify>
  <done>One exact target/prestate/head/backup/rollback tuple is ready for human decision with zero DB mutation or secret persistence.</done>
</task>

<task id="32-26-02" type="checkpoint:user" gate="blocking">
  <name>Authorize or deny the exact target-bound sole-head migration</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-authorization.md
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-authorization.md.sha256
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-authority-validation.json
  </files>
  <action>Review only the read-only preflight. Select approval or denial without providing the database URL/credentials. On approval, materialize one canonical machine section with kind=`production-database-migration`, exact target ID/locator hash, current revision/schema prestate, expected sole head `20260821_18`, revision-file hash, backup/restore/rollback evidence hashes, scope, expiry, and migration-only power; write its detached SHA-256 sidecar and validate it in the isolated environment. Deny alternate targets, a second revision, downgrade, job creation, provider work, content/review mutation, release, Git, and publication. This plan never runs the migration.</action>
  <decision>Whether Plan 27 may apply the sole revision `20260821_18` to the exact selected target before preparing the pilot job.</decision>
  <context>Approval is single-target, prestate-bound, expiring, backup/restore-backed, and migration-only. If already at exact head, it authorizes read-only exact-retry validation rather than another write.</context>
  <options>
    <option id="authorize-exact-production-migration"><name>Authorize exact target migration</name><pros>Permits Plan 27 to validate authority then run the sole-head upgrade and post-check.</pros><cons>Any drift or expiry invalidates authority and requires a new checkpoint.</cons></option>
    <option id="deny-production-migration"><name>Deny migration</name><pros>Guarantees no production schema mutation.</pros><cons>Pilot job preparation and all production/provider actions remain blocked.</cons></option>
  </options>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync multilang validate-korean-checkpoint-authority --authority .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-authorization.md --sidecar .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-migration-authorization.md.sha256 --expected-kind production-database-migration --output .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-authority-validation.json</automated></verify>
  <done>An exact approved/denied target-bound migration authority is immutable and validated; the database remains unchanged.</done>
  <resume-signal>Select `authorize-exact-production-migration` or `deny-production-migration` and confirm only target/prestate/backup/expiry IDs and hashes—never secrets.</resume-signal>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-06 / SC-09 | Exact auditable production persistence gate | COVERED |
| REQ | KFREQ-01 / GLEX-02 / GPRO-01 / KAUD-01 / GAUD-01 | Schema authority before production | COVERED |
| RESEARCH | Additive sole migration / operational safety | Target, backup, rollback and expiry binding | COVERED |
| CONTEXT | Sole revision 20260821_18; migration before job/provider action | Locked decisions preserved | COVERED |
</source_audit>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-26-01 | Spoofing | production DB target | mitigate | Canonical locator hash, non-secret target ID, schema/revision prestate and exact authority binding. |
| T-32-26-02 | Tampering / Availability | migration recovery | mitigate | Current backup plus restore-test and rollback evidence before approval. |
| T-32-26-03 | Information Disclosure | connection secret | mitigate | Presence-only use; raw URL and credentials never enter evidence. |
</threat_model>

## High-Leverage Review
<high_leverage_review>Second pass: inspect target canonicalization, read-only guarantees, sole-head graph, backup/restore/rollback evidence, expiry and migration-only power.</high_leverage_review>

## Verification
<verification>Run the two isolated checks and independently confirm DB mutation count remains zero.</verification>

## Success Criteria
<success_criteria>
- One exact production target/prestate/head and recovery tuple is known read-only.
- The user explicitly approves or denies that exact migration.
- Any approval is expiring, sidecar-bound, and migration-only.
</success_criteria>

## Leverage Review
- Lost: Implicit database targeting and automatic migration convenience.
- Kept: Sole reversible Alembic revision and explicit target selection.
- Gained: Recoverable least-power production migration authorization.

## Notes
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-26-SUMMARY.md` with only target locator hash/ID, revision/schema/backup/rollback/expiry IDs, authority status and bounded claim.</output>
