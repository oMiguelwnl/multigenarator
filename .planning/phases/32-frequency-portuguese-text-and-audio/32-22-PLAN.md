---
phase: 32-frequency-portuguese-text-and-audio
plan: "22"
type: execute
wave: 34
runtime: opencode
assurance: self_checked
depends_on: [32-21]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-preflight.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-role-registry.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-role-registry.json.sha256
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-progress.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-input/manifest.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-input/
autonomous: false
requirements: [KFREQ-01, KFREQ-02, GLEX-02, GMOR-01]
non_goals: ["No source/bundle mutation, review import, database write, pointer activation, provider call, commit, build, export, or publication."]
no_ui_proof_rationale: "Qualified private source/content review checkpoint over immutable bundle bytes."
second_pass_required: true
closure_claim_limit: "Collects bounded human decisions over every source disposition; no receipt, aggregate, final approval or activation exists yet, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Review sees the immutable build and exact 5965 accounting, never mutable/generated/provider identity."
    - "A private pseudonymous role registry and CAS progress manifest authorize qualified roles and resumable bounded sessions without PII or reauthorization."
    - "Qualified decisions cover every accepted/rejected row, modernity and risk case exactly once in at-most-100 private batches."
    - "Review inputs bind exact identities/build root and contain no credentials, local absolute paths or authority-widening fields."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-preflight.json, provides: "Read-only source-review population summary"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-role-registry.json, provides: "Private pseudonymous source-review qualifications and scopes"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-progress.json, provides: "CAS resumable review cursor and completed batch hashes"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-input/manifest.json, provides: "Private bounded human review batch index"}
  key_links:
    - {from: "immutable 5965-disposition candidate", to: "qualified human decisions", via: "disjoint at-most-100 exact-hash batches"}
---

# Phase 32 Plan 22: Resumable Bounded Source and Inventory Review Checkpoint

<objective>
Present the exact inactive candidate for qualified source, modernity, identity, sense, curation and license/attribution review in bounded private batches.

Purpose: Machine curation cannot grant content or redistribution approval.
Output: Read-only preflight and complete private review input set.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-21-PLAN.md
@.multilang/phase32/bundles/multilang-korean-frequency-v1/manifest-v1.json
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-build-result.json
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-build-validation.json
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-22-01" type="auto">
  <name>Revalidate immutable build and produce a hash/count-only review preflight</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-preflight.json
  </files>
  <action>Run the source-build validator read-only and produce a scanner-safe review summary binding retrieval/source/build/root/member hashes, 5965 denominator, 3000/2965 split, per-level counts and counts for all modernity/sensitive/proper-name/script/function-morpheme/homograph/analyzer/license/attribution risk classes. Define the required registry schema, controlled role/qualification/scope codes, `max_batch_items=100`, `max_session_decisions=100`, manifest/progress compare-and-swap fields, exact `continue-review` resume command inputs, and no-reauthorization rule for unchanged registry/build/progress hashes. Do not emit source rows, Korean text, glosses, reviewer notes, PII or paths. Snapshot and compare all bundle/input bytes before and after.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-preflight.json').read_text()); assert d['status']=='ready_for_review' and d['source_disposition_count']==5965 and d['accepted_count']==3000 and d['rejected_count']==2965 and d['max_batch_items']==d['max_session_decisions']==100 and d['bundle_unchanged'] is True"</automated></verify>
  <done>The exact review population and risk denominators are visible without content leakage or mutation.</done>
</task>

<task id="32-22-02" type="checkpoint:review" gate="blocking">
  <name>Complete qualified review of every accepted and rejected source disposition</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-role-registry.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-role-registry.json.sha256
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-progress.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-input/manifest.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-review-input/
  </files>
  <action>Perform only private role registration, human review, and bounded artifact creation. Create a canonical pseudonymous role registry containing opaque reviewer IDs, controlled qualification class/evidence hash, allowed decision classes and expiry—never names, contact data or free text—and its detached sidecar. Initialize/update `source-review-progress.json` with build/registry/manifest roots, next batch/cursor, completed batch hashes/counts and CAS generation. Process at most 100 decisions per session and batch; `continue-review` must reload exact registry/build/progress hashes, no-op completed batches, reject overlap/drift, and resume without recreating authorization. Never place the full 5965-item corpus in an agent/model context; private tooling pages only the current bounded batch. Do not invoke review import/aggregate commands, mutate source/bundle/DB state, activate a pointer, or copy private content into logs/chat.</action>
  <what-built>The offline path produced one immutable 5965-disposition bundle candidate and a hash/count-only preflight. No reviewer decision has been imported.</what-built>
  <how-to-verify>
    1. Use a private review workspace to inspect every accepted/rejected source row against exact build-root identity; do not paste source content or notes into chat/logs.
    2. Confirm original rank/POS/homonym/gloss/grade preservation, dominant source-backed sense, Phase 30 identity/Kiwi evidence, modernity, sensitive/proper-name/script/function-morpheme/inflection/homograph decisions, same-source backfill, 3000/2965 accounting and 1000 per level.
    3. Record only pseudonymous registry ID/role/class, exact item/build hashes, decision and controlled reason in disjoint batches of at most 100; stop each session at 100 decisions and persist CAS progress. Cover all 5965 dispositions and every risk case exactly once.
    4. Use `continue-review` for later sessions against unchanged registry/build/progress hashes; never recreate authority or load all content into context.
    5. Write a fixed `source-review-input/manifest.json` indexing all batch IDs/files/hashes/counts and the role-registry/progress roots. Keep batches private and exclude credentials, PII, absolute paths and unbounded notes.
  </how-to-verify>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; b=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox'); r=b/'source-review-input'; m=json.loads((r/'manifest.json').read_text()); p=json.loads((b/'source-review-progress.json').read_text()); q=json.loads((b/'source-review-role-registry.json').read_text()); fs=sorted(r.glob('batch-*.json')); assert 1&lt;=len(fs)&lt;=60 and len(fs)==len(m['batches']) and sum(x['item_count'] for x in m['batches'])==5965 and all(1&lt;=x['item_count']&lt;=100 for x in m['batches']) and p['status']=='complete' and p['completed_item_count']==5965 and p['max_session_decisions']==100 and q['contains_pii'] is False and m['role_registry_sha256']==p['role_registry_sha256']"</automated></verify>
  <done>Qualified private batches cover all 5965 exact dispositions once, remain bounded, and have not been imported or applied.</done>
  <resume-signal>Type `continue-review` to resume the next bounded session or `source-review-complete` after registry, CAS progress, manifest and all batches exist; report blockers without source text or PII.</resume-signal>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-02 / SC-03 | Reviewed exact inventory and curation | COVERED |
| REQ | KFREQ-01 / KFREQ-02 / GLEX-02 / GMOR-01 | Qualified identity/rank/rejection review | COVERED |
| RESEARCH | Exact inventory checkpoint / source review | Complete bounded qualified review | COVERED |
| CONTEXT | One dominant sense, modernity, rejected particles/endings/noise/homographs | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-22-01 | Information Disclosure | private review | mitigate | Content stays in private bounded files; public preflight/receipts are hash/count-only. |
| T-32-22-02 | Repudiation | reviewer coverage | mitigate | Qualified role plus exact disjoint 5965-item hash binding. |
</threat_model>
<high_leverage_review>Second pass: ensure exactly-once complete coverage, required reviewer qualifications, risk strata and zero bundle/DB mutation.</high_leverage_review>
<verification>Run both automated checks; review batches remain private and unimported.</verification>
<success_criteria>
- Exact immutable candidate is presented read-only.
- Qualified review covers every disposition and risk once.
- Private batches are bounded, complete and hash-bound.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-22-SUMMARY.md` with only review batch counts/hashes/roles/status and bounded claim—never source text or notes.</output>
