---
phase: 32-frequency-portuguese-text-and-audio
plan: "35"
type: execute
wave: 47
runtime: opencode
assurance: self_checked
depends_on: [32-34]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/remediation-ledger/
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/initial-text-review-ledger-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/initial-audio-review-ledger-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/remediation-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/post-remediation-run-evidence.json
autonomous: true
requirements: [KFREQ-03, KTXT-01, KAUD-01, GTXT-01, GPRO-01, GAUD-01]
non_goals: ["No new two-candidate set, second repair, changed route/model/prompt/policy/profile/voice/SSML/request/budget, unreviewed promotion, APKG build, release, delivery, or publication."]
no_ui_proof_rationale: "Authority-gated append-only remediation branch and optional exact remediation only."
second_pass_required: true
closure_claim_limit: "Appends only authorized remediation evidence and performs at most one bounded remediation cycle; every canonical review status remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Zero-rejection continuity appends a no-remediation event; remediation authority appends only aggregate-declared rejection/remediation events, and neither changes canonical review status."
    - "Remediation uses only one unused original text repair and same approved audio request/profile; repaired text installs only when every dependent NFC spoken-text/request hash remains unchanged."
    - "Post-action validator proves no extra candidates/repairs/routes/profile drift/fallback and preserves unaffected rows/files."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/remediation-result.json, provides: "Exact no-op or bounded changed-item result"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/post-remediation-run-evidence.json, provides: "Read-only post-action machine proof"}
  key_links:
    - {from: "selected no-remediation/remediation authority", to: "append-only remediation ledger and optional changed outputs", via: "serializable CAS and original-history/same-request execution with canonical statuses pending"}
---

# Phase 32 Plan 35: Append Rejections and Perform Optional Bounded Remediation

<objective>
Append the exact first aggregate branch under its selected authority, perform only eligible bounded remediation, and revalidate the resulting machine state while canonical review status remains pending.

Purpose: Rejected items may be corrected once without resetting 2+1, changing profile/routes or promoting unreviewed bytes.
Output: Append-only remediation ledger results, remediation/no-op result and post-run evidence.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-34-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/initial-review-aggregate.json
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/remediation-authority-validation.json
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-35-01" type="auto">
  <name>Append the selected first-aggregate branch without canonical application</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/remediation-ledger/
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/initial-text-review-ledger-result.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/initial-audio-review-ledger-result.json
  </files>
  <action>Revalidate exactly one selected authority and the initial append-only review ledgers before writing. For kind=`no-remediation-continuation`, append one chain-linked zero-rejection continuity event and no item mutation. For kind=`remediation`, append chain-linked rejection/remediation-intent events only for aggregate-declared exact hashes; do not write canonical selected-text or audio accepted/rejected/promotion fields. Bind every event to prior ledger root, aggregate, authority, current prestate and pseudonymous role roots. Dual/missing/wrong authority, prestate drift or partial coverage yields zero write. Persist content-free ledger results; exact replay is no-write and conflicting replay stops.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; r=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox'); t=json.loads((r/'initial-text-review-ledger-result.json').read_text()); a=json.loads((r/'initial-audio-review-ledger-result.json').read_text()); assert t['mode']==a['mode'] and t['mode'] in {'no_remediation_continuation','remediation_intent'} and t['authority_validated_before_append'] is True and a['authority_validated_before_append'] is True and t['canonical_review_mutation_count']==a['canonical_review_mutation_count']==0 and t['ledger_chain_valid'] is True and a['ledger_chain_valid'] is True"</automated></verify>
  <done>The selected branch is append-only and exact, with no canonical review-state mutation.</done>
</task>

<task id="32-35-02" type="auto">
  <name>Perform only authorized original-history and same-request remediation</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/remediation-result.json
  </files>
  <action>If no-remediation continuity was selected, create a canonical no-remediation result proving zero calls/changes. If remediation was selected, rerun full CAS and invoke `generate-korean-frequency-text` only for exact rejected text identities whose persisted original history has one unused repair; it may issue that one `sentence_repair` call and no new initial candidates. Stage each repair result before row installation, recompute its NFC word/sentence spoken-text hashes, and require equality with every authority-bound dependent audio request; mismatch discards the staged result, performs zero text/audio installation, creates no request, and stops for replanning. Invoke `synthesize-korean-frequency-audio` only for independently rejected audio identities using the unchanged approved profile/SSML/request and unchanged bound spoken text. Append attempt/outcome/new-hash events to the remediation ledger; do not mark any output reviewed/accepted/rejected/promoted. Preserve accepted/unaffected rows and bytes. Any need for another repair or route/model/prompt/policy/profile/voice/format/SSML/request/budget change stops and requires replanning.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/remediation-result.json').read_text()); assert d['status'] in {'not_required','completed_pending_final_review'} and d['new_initial_candidate_count']==0 and d['max_new_repairs_per_rejected_text']&lt;=1 and d['route_change_count']==d['profile_change_count']==d['fallback_attempt_count']==d['new_audio_request_count']==d['spoken_text_hash_change_count']==d['stale_dependent_audio_count']==0 and d['canonical_review_mutation_count']==0 and d['remediation_ledger_chain_valid'] is True and d['unaffected_items_unchanged'] is True"</automated></verify>
  <done>Optional remediation changes only authorized rejected identities once, preserves every dependent audio request, and leaves changed outputs pending final review.</done>
</task>

<task id="32-35-03" type="auto">
  <name>Reconcile post-remediation machine state read-only</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/post-remediation-run-evidence.json
  </files>
  <action>Run `validate-korean-production-run-result` over the current DB/files and exact append-only first/remediation ledger/results in addition to the full authority chain. Recompute all 3000 histories, 6000 requests/assets, changed/unaffected sets, attempts/budgets/denominators and authority order. Require no history reset, no fourth candidate, at most one total repair per item, exact dependent spoken-text/request continuity for every text repair, no new audio request, same profile/request semantics, zero fallback, valid ledger chains, all 3000 text/6000 audio canonical review statuses still pending and input invariance during validation. Emit exact changed hash sets for final review.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/post-remediation-run-evidence.json').read_text()); assert d['status']=='passed' and d['identity_count']==3000 and d['max_initial_candidates']==2 and d['max_total_repairs_per_item']&lt;=1 and d['fallback_attempt_count']==d['new_audio_request_count']==d['stale_dependent_audio_count']==0 and d['canonical_pending_text_count']==3000 and d['canonical_pending_audio_count']==6000 and d['ledger_chains_valid'] is True and d['validation_inputs_unchanged'] is True"</automated></verify>
  <done>Current machine state is exact and its changed identities are ready for final human review.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-04 / SC-05 / SC-08 / SC-10 | Bounded correction without authority drift | COVERED |
| REQ | KFREQ-03 / KTXT-01 / KAUD-01 / GTXT-01 / GPRO-01 / GAUD-01 | Exact one-cycle remediation | COVERED |
| RESEARCH | One distinct repair / exact audio reuse | Persisted history and same profile/request only | COVERED |
| CONTEXT | Exactly 2+1, no route/profile/fallback changes, review before promotion | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-35-01 | Elevation / Tampering | remediation ledger | mitigate | Exact authority, chain-linked append and serializable prestate CAS before any bounded repair. |
| T-32-35-02 | Denial of Service / Cost | remediation | mitigate | Exact rejected set, one remaining repair or same request only. |
| T-32-35-03 | Spoofing | post-state | mitigate | Independent row/file/history/request reconciliation. |
</threat_model>
<high_leverage_review>Second pass: inspect authority-before-mutation, promote/reject-only separation, total 2+1 history, same audio identity, unchanged accepted items and no fallback.</high_leverage_review>
<verification>Run all three exact artifact checks and rerun post-state validator read-only.</verification>
<success_criteria>
- First-review branch appends exactly one valid no-remediation/remediation mode without canonical status mutation.
- Remediation is absent or strictly bounded to rejected identities.
- Current state is machine-reconciled and awaits final review.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-35-SUMMARY.md` with safe ledger/remediation/changed-set roots, counts, attempts and bounded claim.</output>
