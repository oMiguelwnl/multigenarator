---
phase: 32-frequency-portuguese-text-and-audio
plan: "04"
type: execute
wave: 16
runtime: opencode
assurance: self_checked
depends_on: [32-03]
files-modified:
  - tests/integration/test_frequency_evidence_persistence.py
  - tests/services/test_korean_frequency.py
  - tests/services/test_frequency_decks.py
  - tests/services/test_lexical_grounding.py
  - tests/services/test_text_validation.py
  - src/multilang/domain/exporting.py
  - src/multilang/repositories/export_repository.py
  - src/multilang/repositories/provider_call_log_repository.py
  - src/multilang/services/korean_frequency.py
  - src/multilang/services/frequency_decks.py
  - src/multilang/services/ingest_lexical_items.py
  - src/multilang/services/lexical_grounding.py
  - src/multilang/services/text_validation.py
autonomous: true
requirements: [KFREQ-01, GLEX-01, GLEX-02, GMOR-01, GPRO-01]
non_goals: ["No live bundle activation, text/audio generation, provider call, generic Korean rescue, or legacy-language behavior change."]
no_ui_proof_rationale: "Persistence-to-runtime and selected morphology wiring only."
second_pass_required: true
closure_claim_limit: "Proves synthetic reload, strict final loading, and fail-closed selected morphology only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Export/provider evidence survives reload with denominators and no sensitive content or GUID drift."
    - "Korean final ingestion requires explicit job/root/receipt and rehashes exact locator/content authority before persistence."
    - "Only selected Kiwi MATCHED promotes; mismatch and every inconclusive state block before generic logic."
  artifacts:
    - {path: tests/integration/test_frequency_evidence_persistence.py, provides: "Cross-repository staged reload and privacy proof"}
    - {path: src/multilang/services/frequency_decks.py, provides: "Strict frozen Korean final branch"}
    - {path: src/multilang/services/text_validation.py, provides: "Selected-Kiwi tri-state gate"}
  key_links:
    - {from: "reloaded job CAS", to: "candidate ingestion", via: "explicit root locator/content comparison"}
    - {from: "Korean validation", to: "shared Kiwi", via: "Korean-first return before generic fallback"}
---

# Phase 32 Plan 04: Evidence Reload, Frozen Ingestion, and Morphology Gate

<objective>
Complete export/provider evidence persistence, route Korean final ingestion only through the bound immutable bundle, and enforce the selected-Kiwi tri-state gate.

Purpose: Persisted authority must dominate the final runtime call graph and no analyzer uncertainty may become acceptance.
Output: Cross-repository evidence proof, frozen final ingestion, and morphology refusal paths.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-03-PLAN.md
@src/multilang/services/frequency_decks.py
@src/multilang/services/korean_morphology.py
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-04-01" type="auto" tdd="true">
  <name>Round-trip export and privacy-safe provider evidence across repositories</name>
  <files>
    - CREATE: tests/integration/test_frequency_evidence_persistence.py
    - MODIFY: src/multilang/domain/exporting.py
    - MODIFY: src/multilang/repositories/export_repository.py
    - MODIFY: src/multilang/repositories/provider_call_log_repository.py
  </files>
  <behavior>
    - Explicit manifest level and bundle evidence reload outside stable GUID input.
    - Attempts retain route/cache/retry/fallback/token/cost/latency values and availability denominators without content.
    - Pilot-base/audio/full retain exact Phase 31 pointer/receipt/snapshot and bundle/catalog locator/content tuples after commit/expire/reload.
  </behavior>
  <action>Persist explicit export level/bundle/evidence hashes without learner-field or GUID changes. Extend telemetry with operation, route/policy/request/cache identities, attempts/retries/fallback, token/cost/latency and missing-value denominators, plus controlled redacted failures. Integration tests scan every persisted value for roots, prompts, payloads, credentials, source rows and notes; mutate one Phase 31/bundle/catalog fact and require zero attempt delta; preserve blank Image and existing GUIDs.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/integration/test_frequency_evidence_persistence.py -k 'export or provider or frequency_level or guid or route or denominator or redaction or staged_reload' -q</automated></verify>
  <done>All cross-repository evidence is durable, locator-aware, private, and identity-compatible.</done>
</task>

<task id="32-04-02" type="auto" tdd="true">
  <name>Route final ingestion through the explicit locator-bound job bundle</name>
  <files>
    - MODIFY: tests/services/test_korean_frequency.py
    - MODIFY: tests/services/test_frequency_decks.py
    - MODIFY: src/multilang/services/korean_frequency.py
    - MODIFY: src/multilang/services/frequency_decks.py
    - MODIFY: src/multilang/services/ingest_lexical_items.py
  </files>
  <behavior>
    - Production-size Korean ingestion requires explicit job ID, bundle root, binding receipt, and exact reloaded authority.
    - Root locator, manifest/member hashes, source authorities, and receipt must match before candidate persistence.
    - Mutable settings, wordfreq, seed, first-sense, or provider-authored identity cannot substitute.
  </behavior>
  <action>Require explicit authority inputs at every Korean final ingestion/resume entry. Import Plan 03's sole locator helper, hash the supplied root without logging/storing it, reload CAS, and compare locator, bundle, manifest, source authorities, Phase 31 tuple, and receipt before reading entries. Preserve exact source rank/final rank/level/identity/provenance/canonical pt. Remove every Korean final edge to live wordfreq, seeds, first match, provider identity, or mutable pointer discovery; exact retry is read-only and any drift yields zero provider attempts.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_frequency.py -k 'final_runtime or locator or binding_drift or idempotent or poison or persisted_candidate' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_frequency_decks.py -k 'korean or build_frequency' -q</automated></verify>
  <done>Korean final ingestion has one immutable locator/content authority and no live replacement edge.</done>
</task>

<task id="32-04-03" type="auto" tdd="true">
  <name>Promote only selected-Kiwi match and block every other state</name>
  <files>
    - MODIFY: tests/services/test_lexical_grounding.py
    - MODIFY: tests/services/test_text_validation.py
    - MODIFY: src/multilang/services/korean_frequency.py
    - MODIFY: src/multilang/services/lexical_grounding.py
    - MODIFY: src/multilang/services/text_validation.py
  </files>
  <behavior>
    - Exact top-two selected-adapter MATCHED becomes match and MISMATCH becomes mismatch.
    - Missing, invalid, ambiguous, OOV, unavailable, malformed, or fingerprint-drifted evidence is inconclusive and blocking.
    - Korean returns before suffix, token, substring, whitespace, Stanza, Japanese, or Mandarin logic.
  </behavior>
  <action>Implement one stable match/mismatch/inconclusive projection reused by grounding and text validation. Persist only controlled reason/hash evidence and keep source identity immutable. Add real-Kiwi noun, particle, regular/irregular predicate, compound, homograph, NFC and drift cases with poisoned generic fallback counters.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_lexical_grounding.py -k 'korean and (consensus or mismatch or ambiguous or oov or fingerprint)' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_text_validation.py -k 'korean' -q</automated></verify>
  <done>Every non-conclusive Korean analyzer state blocks without identity rewrite or generic rescue.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-03 / SC-06 / SC-07 / SC-09 | Frozen final runtime and selected morphology | COVERED |
| REQ | KFREQ-01 / GLEX-01 / GLEX-02 / GMOR-01 / GPRO-01 | No live replacement and fail-closed matching | COVERED |
| RESEARCH | Final call graph / tri-state gate | Explicit CAS, poisoned fallbacks, one selected adapter | COVERED |
| CONTEXT | NFC, source identity, Kiwi top-two, no suffix rescue | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-04-01 | Information Disclosure | telemetry | mitigate | Hash/metric/denominator allowlist and persisted-value scan. |
| T-32-04-02 | Spoofing / Tampering | bundle runtime | mitigate | Explicit locator/content rehash against reloaded CAS. |
| T-32-04-03 | Elevation | morphology fallback | mitigate | Korean-first selected-adapter tri-state and poisoned generic edges. |
</threat_model>
<high_leverage_review>Second pass: inspect the Korean final call graph for CAS dominance and zero wordfreq/seed/first-sense/provider/generic rescue.</high_leverage_review>
<verification>Run all three task commands offline and inspect imports/call edges for final-mode fallbacks.</verification>
<success_criteria>
- Export/provider evidence reloads privately without GUID drift.
- Frozen authority controls final ingestion.
- Only selected-Kiwi match can promote.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-04-SUMMARY.md` with persistence scan, locator trace, fallback poison matrix, morphology cases, and bounded self-checked claim.</output>
