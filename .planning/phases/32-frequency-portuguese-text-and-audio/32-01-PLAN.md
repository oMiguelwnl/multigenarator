---
phase: 32-frequency-portuguese-text-and-audio
plan: "01"
type: execute
wave: 13
runtime: opencode
assurance: self_checked
depends_on: []
files-modified:
  - .planning/.local/phase32-py312/
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/python-environment-receipt.json
  - tests/services/test_korean_frequency.py
  - tests/cli/test_korean_source_retrieval_commands.py
  - tests/integration/test_phase32_dependency_guard.py
  - tests/services/test_korean_checkpoint_authority.py
  - src/multilang/domain/korean.py
  - src/multilang/services/korean_frequency.py
  - src/multilang/services/korean_checkpoint_authority.py
  - src/multilang/cli.py
autonomous: true
requirements: [KFREQ-01, GLEX-01, GLEX-02]
non_goals:
  - "No live source access, source transformation, production bundle, provider call, or fabricated attachment/legal/reviewer fact."
no_ui_proof_rationale: "Offline source and least-power authority contracts only; Phase 34 owns observed Anki behavior."
second_pass_required: true
closure_claim_limit: "Proves synthetic offline contracts, exact response-derived TXT resolution, and authority validation only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 2, safe_parallelism: [32-10]}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Retrieval, transformation/build, AI review, and final external authority remain distinct typed powers."
    - "The fixed NIKL landing response resolves exactly one NFC filename 한국어 학습용 어휘 목록.txt; no attachment URL is guessed."
    - "Authority prose, stale sidecars, wider powers, unsafe paths, and private data fail closed."
    - "Remediation authority binds every dependent audio request and cannot authorize a text repair that changes an already-bound NFC spoken-text hash."
  artifacts:
    - {path: src/multilang/domain/korean.py, provides: "Strict source/retrieval/build/identity contracts"}
    - {path: src/multilang/services/korean_frequency.py, provides: "Bounded official TXT resolver/retriever"}
    - {path: src/multilang/services/korean_checkpoint_authority.py, provides: "Typed fixed-power authority validator"}
  key_links:
    - {from: "official landing bytes", to: "retrieval result", via: "one response-derived attachment identity and exact byte hashes"}
    - {from: "checkpoint sidecar", to: "later external commands", via: "canonical whole-file and nested binding rehash"}
---

# Phase 32 Plan 01: Source, Resolver, and Authority Contracts

<objective>
Define strict source/result schemas, the deterministic official-TXT resolver/retriever, and the fixed least-power checkpoint validator before any external work.

Purpose: No guessed locator, loose prose, or earlier authority may become source transformation or production power.
Output: Tested contracts, retrieval commands, dependency guard, and authority validator.
</objective>

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>

<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-APPROACH.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-FREQUENCY-SOURCE-DECISION.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-RESEARCH.md
@.planning/AI-LINGUISTIC-REVIEW-POLICY.md

<interfaces>
Create `retrieve-korean-frequency-source`, `validate-korean-source-retrieval-result`, and `validate-korean-checkpoint-authority`. The landing URL is exactly `https://www.korean.go.kr/front/etcData/etcDataView.do?mn_id=46&amp;etc_seq=70`; the accepted normalized filename is exactly `한국어 학습용 어휘 목록.txt`. Authority kinds are source-access, transformation-build, final-bundle, pilot, provider-review, production-database-migration, profile-sample, heard-review, full-run, no-remediation-continuation, remediation, final-promotion, and final-release, each with a fixed non-widenable power registry.
</interfaces>
</context>


<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-01-01" type="auto" tdd="true">
  <name>Establish the isolated Python 3.12 environment, then define strict source and authority contracts</name>
  <files>
    - CREATE: .planning/.local/phase32-py312/
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/python-environment-receipt.json
    - CREATE: tests/services/test_korean_frequency.py
    - MODIFY: src/multilang/domain/korean.py
  </files>
  <behavior>
    - Separate typed schemas grant only their named retrieval, build, review, or approval power.
    - Exactly 5965 dispositions reconcile to 3000 unique lemma/POS/sense identities plus 2965 rejections and 1000/1000/1000 explicit levels.
    - Unknown, mutable, malformed, synthetic-as-production, analyzer-drifted, rights-incomplete, or path-bearing payloads fail closed.
  </behavior>
  <action>Before writing or running the first Phase 32 test, descriptor-safely require `.planning/.local` to exist, reject symlink/reparse traversal, snapshot a deterministic shared `.venv` tree hash without following links, and run exactly `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv sync --frozen --extra dev --python 3.12` followed by `UV_OFFLINE=1 uv lock --check`. Verify the selected interpreter is Python 3.12, the environment remains repo-local/ignored, and shared `.venv` is byte-identical; write a content-free receipt containing Python/uv versions, environment descriptor, `pyproject.toml`/`uv.lock` hashes, sync/lock status, and shared-venv pre/post hashes. If setup cannot complete offline, stop before any test or implementation edit. Then write failing contract tests and add frozen strict Pydantic models for landing/attachment/redirect/header/source-byte evidence, retrieval result, transformation/build policy and result, bundle members/manifest, accepted Phase 30 identities, rejection/curation accounting, and review roots. Require NFC, canonical UTF-8 JSON SHA-256, exact raw-byte SHA-256, bounded values, selected Kiwi fingerprint, rights/attribution/storage/disposition fields, explicit contiguous ranks/levels, `extra="forbid"`, hidden inputs, and immutable nested structures. Retrieval models contain no parse/build claim; build models bind exact retrieval/source bytes and remain inactive. Never retain source rows, HTML, private paths, notes, credentials, or raw exceptions in safe results.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_frequency.py -k 'contract or retrieval or build_result or identity or accounting or authority_separation' -q &amp;&amp; .planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/python-environment-receipt.json').read_text()); assert d['python_minor']=='3.12' and d['sync_frozen'] is True and d['lock_check_passed'] is True and d['shared_venv_unchanged'] is True"</automated></verify>
  <done>The dedicated frozen Python 3.12 environment exists without shared `.venv` drift, and every source-to-bundle state is strict, hashable, power-separated, and count/identity constrained.</done>
</task>

<task id="32-01-02" type="auto" tdd="true">
  <name>Implement response-derived exact-TXT resolution, bounded retrieval, and read-only result validation</name>
  <files>
    - MODIFY: tests/services/test_korean_frequency.py
    - CREATE: tests/cli/test_korean_source_retrieval_commands.py
    - CREATE: tests/integration/test_phase32_dependency_guard.py
    - CREATE: src/multilang/services/korean_frequency.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Only the fixed NIKL HTTPS landing page is requested before exactly one normalized filename match derives the attachment ID and URL from bounded official bytes.
    - Wrong/ambiguous host, path, query, DNS/IP, redirect, headers, bytes, encoding, filename, or TXT schema fails before source installation.
    - Retrieval output records content-free landing/attachment/redirect/header/byte hashes and grants no transform power.
  </behavior>
  <action>Implement the two retrieval commands. Parse bounded HTML with a real parser, require one NFC/case-preserving filename match and one allowlisted response-derived attachment target, and never accept a target from authority prose. Enforce HTTPS, fixed host/path/query keys, public DNS/IP at every hop, normal TLS, no credential/header forwarding, bounded redirects/time/bytes, content disposition/type, encoding, and expected lexical TXT schema. Descriptor-safely create one owned quarantine temporary, fsync and atomically install only after validation, and remove only that temporary on failure. The validator opens all inputs read-only and recomputes every fact. Extend the dependency guard to reject spreadsheets, XLS/HWP parsers, Korean final wordfreq/seed/provider identity, and production/network access in tests.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_source_retrieval_commands.py -k 'resolver or attachment or redirect or bounded or retrieval_result or mutation or read_only' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/integration/test_phase32_dependency_guard.py -k 'txt or spreadsheet or dependency or official_source' -q</automated></verify>
  <done>The official attachment is discovered rather than guessed, retrieved under fixed bounds, and independently validated without transformation.</done>
</task>

<task id="32-01-03" type="auto" tdd="true">
  <name>Implement typed checkpoint-authority validation with fixed powers</name>
  <files>
    - CREATE: tests/services/test_korean_checkpoint_authority.py
    - CREATE: src/multilang/services/korean_checkpoint_authority.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Exactly one canonical machine section and detached whole-file sidecar must match kind-specific bindings, expectations, and powers.
    - CLI arguments may confirm but never widen the fixed registry.
    - Source access cannot transform; build cannot retrieve or approve; review and release powers remain distinct.
    - Remediation text entries bind pre-repair word/sentence spoken-text hashes and their exact dependent audio requests; a new audio request is never implicit remediation power.
  </behavior>
  <action>Implement `validate-korean-checkpoint-authority`. Parse exactly one fenced canonical JSON section, reject duplicate/unknown fields, and descriptor-safely rehash every file/tree binding and nested sidecar. Compare exact hashes/bytes/scalars against a fixed kind/power registry and emit only safe status/hash/control IDs. For kind=`remediation`, require each text identity to bind its pre-repair NFC word/sentence spoken-text hashes and exact dependent audio request/profile identities; reject any authority that permits a new request, omits a dependency, or conflates text and audio rejection powers. Mutation-test marker-only prose, duplicate sections, wrong kind/power, missing/extra/drifted bindings, links, tree drift, power conflation, dependent-audio omission, and private-data output.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_checkpoint_authority.py -k 'kind or binding or nested_sidecar or power or source_build_separation or remediation_audio_dependency or mutation or privacy' -q</automated></verify>
  <done>Every later checkpoint has one machine-revalidated least-power vocabulary independent of prose.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-03 / SC-06 | Exact official provenance and identity authority | COVERED |
| REQ | KFREQ-01 / GLEX-01 / GLEX-02 | Auditable source/result/identity contracts | COVERED |
| RESEARCH | Source / authority / SSRF | Response-derived TXT, strict schemas, fixed powers | COVERED |
| CONTEXT | NIKL TXT; no guessed URL, spreadsheet, or fallback | Locked decisions preserved | COVERED |
</source_audit>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-01-01 | Spoofing / SSRF | resolver | mitigate | Fixed HTTPS host/path/query, public-IP validation, exact response-derived match, bounded redirects. |
| T-32-01-02 | Tampering | source/result | mitigate | Descriptor-safe quarantine plus header/encoding/schema/byte hashes and read-only recomputation. |
| T-32-01-03 | Elevation / Repudiation | authority | mitigate | Canonical sidecar-bound fixed kind/power registry. |
</threat_model>

<high_leverage_review>Second pass: trace landing bytes to one attachment; inspect every network/path/byte bound and prove no authority widening or private output.</high_leverage_review>
<verification>Run all three task commands offline; no test may reach a real source/provider endpoint.</verification>
<success_criteria>
- Retrieval/build/review powers are mechanically separate.
- Exact official TXT resolution is response-derived and fail-closed.
- Every authority is sidecar-bound, typed, and least-power.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-01-SUMMARY.md` with schemas, resolver/security matrix, power table, tests, and bounded self-checked claim.</output>
