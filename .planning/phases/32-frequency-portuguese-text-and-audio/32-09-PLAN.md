---
phase: 32-frequency-portuguese-text-and-audio
plan: "09"
type: execute
wave: 21
runtime: opencode
assurance: self_checked
depends_on: [32-08]
files-modified:
  - tests/services/test_korean_audio_pilot_evidence.py
  - tests/services/test_korean_audio_review_application.py
  - tests/services/test_korean_audio.py
  - tests/scripts/test_run_phase32_isolated_suite.py
  - tests/support/phase32_offline_guard.py
  - src/multilang/services/korean_audio_pilot_evidence.py
  - src/multilang/services/korean_audio_review.py
  - src/multilang/runtime.py
  - src/multilang/cli.py
  - scripts/run_phase32_isolated_suite.py
autonomous: true
requirements: [KAUD-01, GAUD-01, GPRO-01]
non_goals: ["No live Azure/provider call, actual review application, production output, source access, or full repository suite execution in this implementation plan."]
no_ui_proof_rationale: "Offline audio validators, review commands, and isolated-suite harness only."
second_pass_required: true
closure_claim_limit: "Proves fake audio reconciliation, bounded review mutation contracts, and harness isolation only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Audio pilot validation read-only reconciles genuine Phase 31, pilot-audio CAS, exact requests/bytes/profile, budgets and zero fallback."
    - "Audio review import is bounded/idempotent and reject-only versus promote requires distinct exact powers."
    - "The full-suite harness is credential-empty, network/provider-poisoned, disposable, dependency-aware and protects declared bytes using fixed argv with shell=False."
  artifacts:
    - {path: src/multilang/services/korean_audio_pilot_evidence.py, provides: "Strict read-only audio pilot reconciler"}
    - {path: src/multilang/services/korean_audio_review.py, provides: "Bounded audio review import/application"}
    - {path: scripts/run_phase32_isolated_suite.py, provides: "Credential-empty dependency/full-suite harness"}
  key_links:
    - {from: "pilot-audio rows/files", to: "machine pilot result", via: "read-only locator/content/request/byte reconciliation"}
    - {from: "isolated child suite", to: "protected authorities/outputs", via: "clean environment, fixed argv and pre/post hash trees"}
---

# Phase 32 Plan 09: Audio Evidence, Review Application, and Isolation Harness

<objective>
Implement strict audio-pilot evidence, authority-gated audio review mutation, and the credential-empty isolated-suite/dependency harness.

Purpose: Human audio decisions and late full-suite closure require independently recomputed machine evidence and protected-state execution.
Output: Audio validators/review commands and isolation tooling.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-08-PLAN.md
@src/multilang/services/generate_audio_items.py
@tests/integration/test_phase32_dependency_guard.py
<interfaces>
Create `validate-korean-audio-pilot-result`, `import-korean-production-audio-review-batch`, `apply-korean-frequency-audio-review`, and `scripts/run_phase32_isolated_suite.py`. Harness full mode accepts repeated protected/dependency paths plus one result and dependency-evidence output; dependency-only mode verifies the genuine Phase 31 tuple and declared command/script help surfaces without running the suite.
</interfaces>
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-09-01" type="auto" tdd="true">
  <name>Implement strict read-only audio-pilot result validation and runtime stage guards</name>
  <files>
    - CREATE: tests/services/test_korean_audio_pilot_evidence.py
    - CREATE: src/multilang/services/korean_audio_pilot_evidence.py
    - MODIFY: src/multilang/runtime.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Validator reconciles genuine Phase 31 tuple, pilot-audio CAS/receipt, bundle/catalog locators/content, profile authority, requests/bytes, budgets, failures and invariance.
    - Fallback, stale, unbound, pre-CAS, wrong-job, wrong-profile or denominator-missing evidence fails.
    - Output is safe canonical read-only JSON and cannot grant heard approval.
  </behavior>
  <action>Implement the exact validator with explicit database/job/binding receipt, six Phase 31 flags, bundle root, catalog result, profile authority/sidecar/profile, sample result/count and output. Run genuine verify-active, reload pilot-audio CAS and recompute every locator/content/authority/request/artifact/budget/retry/failure/denominator fact plus before/after hashes. Extend runtime constructor guards for pilot-audio/full and poison settings-derived authority. Emit content-free machine evidence only.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_audio_pilot_evidence.py -k 'reconcile or request or bytes or budget or zero_fallback or invariance or mutation or cli or runtime_stage' -q</automated></verify>
  <done>Strict audio machine evidence exists before any heard/full-run decision and grants no approval.</done>
</task>

<task id="32-09-02" type="auto" tdd="true">
  <name>Implement bounded audio-review import and authority-gated reject/promote application</name>
  <files>
    - CREATE: tests/services/test_korean_audio_review_application.py
    - CREATE: src/multilang/services/korean_audio_review.py
    - MODIFY: tests/services/test_korean_audio.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - At-most-100 batches bind run/job/receipt plus exact identity/request/word-byte/sentence-byte hashes and qualified role; replay is no-write.
    - Reject-only marks exactly aggregate-declared rejections under remediation authority and leaves accepted assets pending.
    - Promote requires complete zero-rejection 3000 word/3000 sentence coverage and exact content-promotion authority; any drift yields zero mutation.
  </behavior>
  <action>Implement bounded import and apply commands. Import atomically stores immutable content-free receipt/decision rows and never aggregates. Application validates aggregate, production roots and exactly one mode-specific typed authority/sidecar before one serializable CAS; reject-only marks exact rejected request/byte identities resumable, while promote accepts only current complete exact bytes. Test crash/retry/concurrency, stale/extra/omitted assets, wrong mode/power, missing/dual authority, request/byte drift and zero-write failure; never store private payloads, notes or paths.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_audio_review_application.py tests/services/test_korean_audio.py -k 'audio_review and (batch or bounded or exact_retry or ledger or final_promote or authority or request or bytes or rollback or mutation)' -q</automated></verify>
  <done>Audio review mutations are bounded, exact and power-separated; aggregation remains read-only elsewhere.</done>
</task>

<task id="32-09-03" type="auto" tdd="true">
  <name>Create the credential-empty network-poisoned isolated suite and dependency harness</name>
  <files>
    - CREATE: tests/support/phase32_offline_guard.py
    - CREATE: tests/scripts/test_run_phase32_isolated_suite.py
    - CREATE: scripts/run_phase32_isolated_suite.py
  </files>
  <behavior>
    - Child environment omits credentials/endpoints/tokens and repository dotenv; DNS/socket and real LLM/DeepL/Azure/other transports are poisoned.
    - Full mode uses disposable CWD/HOME/XDG/DB/assets/audio/export/cache/staging roots and compares every repeated protected path before/after.
    - Dependency-only mode verifies genuine Phase 31 active state, pyproject/lock invariance, dependency guard, and every declared command/script help surface.
    - Every child uses a fixed argv vector with `shell=False`; no path/environment/provider/evidence value enters a shell.
  </behavior>
  <action>Implement both fixed-purpose modes. Build a clean allowlist environment, forced offline guard, provider/network constructor poison and canonical pre/post SHA trees. Full mode runs the unfiltered tests tree only when invoked by the dedicated late plan. Dependency-only mode descriptor-verifies the real active pointer, exact receipt and selected snapshot, runs pathless verify-active, the fixed dependency guard and each required CLI/script `--help` exactly once. Emit safe canonical JSON with exit status, Phase 31 hashes, command/script inventories, official-TXT/spreadsheet counts, dependency/protected invariance, environment-name allowlist, zero network/provider counts and disposable categories. Write Markdown readiness from the same in-memory evidence. Nested harness tests must never recursively invoke the repository suite.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/scripts/test_run_phase32_isolated_suite.py -q</automated></verify>
  <done>The late suite can prove credential/network/provider/root/dependency isolation without shell interpretation.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-05 / SC-09 / SC-10 | Exact audio evidence, review power and isolated closure | COVERED |
| REQ | KAUD-01 / GAUD-01 / GPRO-01 | Audio reconciliation, review, no fallback, telemetry | COVERED |
| RESEARCH | Synthesis-not-approval / isolation | Strict pilot proof, exact reviews, poisoned suite | COVERED |
| CONTEXT | 100% integrity, qualified playback, private telemetry, no fallback | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-09-01 | Spoofing / Repudiation | audio evidence | mitigate | Read-only row/file/request/byte recomputation. |
| T-32-09-02 | Elevation | review application | mitigate | Mode-specific sidecar authority and serializable CAS. |
| T-32-09-03 | Information Disclosure / Injection | harness | mitigate | Credential-empty allowlist, network poison, fixed argv shell=False, hash-only output. |
</threat_model>
<high_leverage_review>Second pass: audit pilot reconciliation, review power separation, credential stripping, network poison, fixed argv, protected hashes, and dependency-only command inventory.</high_leverage_review>
<verification>Run all three focused test commands only; Plan 18 owns the pre-external full suite and Plan 40 owns the final pre-release full suite.</verification>
<success_criteria>
- Audio pilot evidence is strict and read-only.
- Audio review mutation requires exact authority.
- The isolated harness safely supports readiness and late full-suite modes.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-09-SUMMARY.md` with validator/review matrices, harness environment/argv contract, mutation tests and bounded self-checked claim.</output>
