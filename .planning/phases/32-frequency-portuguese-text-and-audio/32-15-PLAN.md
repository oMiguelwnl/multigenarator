---
phase: 32-frequency-portuguese-text-and-audio
plan: "15"
type: execute
wave: 27
runtime: opencode
assurance: self_checked
depends_on: [32-14]
files-modified:
  - tests/services/test_korean_production_evidence.py
  - tests/cli/test_korean_production_evidence_commands.py
  - tests/services/test_korean_release_safety.py
  - tests/cli/test_korean_release_safety_commands.py
  - src/multilang/services/korean_production_evidence.py
  - src/multilang/services/korean_release_safety.py
  - src/multilang/cli.py
autonomous: true
requirements: [KFREQ-01, KFREQ-02, KTXT-01, KAUD-01, GPRO-01, GAUD-01]
non_goals: ["No review import/application, live release, output creation, Git/remote action, arbitrary destination, or Phase 34 closure."]
no_ui_proof_rationale: "Read-only review aggregate and offline release safety/promotion tooling only."
second_pass_required: true
closure_claim_limit: "Proves fake bounded review aggregation and durable release tooling only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Production review aggregation reads only immutable receipts and proves complete text/integrity/heard/risk coverage without import or DB mutation."
    - "Safety separately classifies every fixed deliverable for approved local release and publication using exact hashes, private-path/content exclusion, scans, license/attribution, retention and safe-to-publish state."
    - "A complete authorization-hash-named staging tree is fsynced/reopened and atomically installed into an absent immutable release before pointer replacement."
  artifacts:
    - {path: src/multilang/services/korean_production_evidence.py, provides: "Read-only bounded review aggregate"}
    - {path: src/multilang/services/korean_release_safety.py, provides: "Safety builder and durable release promoter"}
  key_links:
    - {from: "immutable review receipts", to: "production review aggregate", via: "complete disjoint read-only coverage"}
    - {from: "owned staging tree", to: "immutable release/current pointer", via: "manifest-last fsync/reopen and atomic replacements"}
---

# Phase 32 Plan 15: Review Aggregation and Durable Release Safety Tooling

<objective>
Implement read-only production review aggregation, canonical release-safety/build evidence, and crash-safe complete-tree promotion/current-pointer replacement.

Purpose: Review completeness and release visibility must be recomputed before immutable promotion, with no partial tree or private/unsafe member exposed.
Output: Review aggregate command, safety builder and release promoter.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-14-PLAN.md
@src/multilang/services/korean_production_evidence.py
<interfaces>
Create `validate-korean-production-review-batches`, `build-korean-release-safety`, and `promote-korean-release-bundle`. Promotion expects exactly 13 members—text/audio review applications, APKG, JSON/Markdown generation reports, JSON/Markdown audits, before/after evidence, offline-suite, dependency-evidence, release-safety and production-build-result—then creates `release-manifest.json` last.
</interfaces>
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-15-01" type="auto" tdd="true">
  <name>Implement read-only bounded production-review receipt aggregation</name>
  <files>
    - MODIFY: tests/services/test_korean_production_evidence.py
    - MODIFY: tests/cli/test_korean_production_evidence_commands.py
    - MODIFY: src/multilang/services/korean_production_evidence.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Each at-most-100 immutable receipt is job/run/full-CAS bound and covers exact text identity or audio request/bytes with qualified role/class/decision.
    - Aggregate requires 3000 text, 3000 word integrity, 3000 sentence integrity, at least 300 heard word and sentence samples, and every risk/flagged/repaired case exactly once.
    - Overlap, gap, stale bytes, oversize, bad role or private-content leakage fails without DB/input/prior-output mutation.
  </behavior>
  <action>Implement only the read-only aggregate command over receipt directories produced by the text/audio import owners. It must not implement, alias or call import/application. Recompute disjoint complete coverage, qualifications and exact roots from DB/receipts; atomically replace only the content-free aggregate output. Test overlap, omission, risk omission, changed item/audio bytes, wrong job/profile/authority, batch size/role/count, raw text/path/note leakage, exact retry and complete input invariance.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_production_evidence.py -k 'review_aggregate or bounded or risk_coverage or read_only' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_production_evidence_commands.py -k 'validate_review_batches or no_import or read_only' -q</automated></verify>
  <done>Qualified bounded review receipts yield one exact complete aggregate without import, application or DB mutation.</done>
</task>

<task id="32-15-02" type="auto" tdd="true">
  <name>Build canonical release safety and production build evidence</name>
  <files>
    - CREATE: tests/services/test_korean_release_safety.py
    - CREATE: tests/cli/test_korean_release_safety_commands.py
    - CREATE: src/multilang/services/korean_release_safety.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Every fixed candidate receives exact hash/bytes, visibility, sensitivity/control, retention, private-root exclusion, scans, license/attribution IDs and safe_to_publish.
    - Approved private/local-only status forces safe_to_publish=false but may set safe_for_local_release=true when local-use authority and every privacy/security/retention control pass; unknown/failed controls block both.
    - Safety report is evidence, not a candidate; separate build result hashes its final bytes without self-reference.
  </behavior>
  <action>Implement `build-korean-release-safety` for only the descriptor-safe owned staging tree and fixed expected member paths. Revalidate source-access/retrieval/transformation/build/final-bundle/content-promotion/production/suite/dependency chains; resolve fixed release-relative candidates and emit only control IDs/hashes/counts. Derive `safe_for_local_release` separately from `safe_to_publish`: approved local-only/private source disposition may pass local release only when no private path/content leaks and all privacy/security/license/use/attribution/retention controls pass, while publication stays false. Reject arbitrary roots, links, traversal, undeclared members and unknown/failed controls. Write safety report then separate build result in prescribed order without claiming promotion.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_release_safety.py -k 'builder or schema or private or scan or attribution or safe_to_publish or build_result or acyclic' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_release_safety_commands.py -k 'build_safety or fixed_roots or privacy or release_graph' -q</automated></verify>
  <done>Canonical fail-closed safety distinguishes approved local release from publication eligibility without performing release action.</done>
</task>

<task id="32-15-03" type="auto" tdd="true">
  <name>Implement durable complete-tree release promotion and current-pointer replacement</name>
  <files>
    - MODIFY: tests/services/test_korean_release_safety.py
    - MODIFY: tests/cli/test_korean_release_safety_commands.py
    - MODIFY: src/multilang/services/korean_release_safety.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Only an authorization-hash-named owned staging sibling with exactly 13 declared members can promote.
    - Members are fsynced, manifest/root created last, directories fsynced and tree closed/reopened/revalidated before absent-target rename and parent fsync.
    - Pointer changes only after durable target; interruptions, partial/non-identical targets and collisions preserve old target/pointer; exact retry is no-write.
  </behavior>
  <action>Implement `promote-korean-release-bundle` with fixed release parent, staging/target sibling checks, exact content-promotion authority/sidecar, repeated expected-member arguments and `release-manifest.json`. Require every member `safe_for_local_release=true`; do not require `safe_to_publish=true`. Reject extras, links, traversal, private path/content leakage and failed/unknown local controls; flush/fsync members, create manifest last, reopen/validate, rename only into absent target and fsync parent. Then write/fsync one owned pointer temporary containing target basename plus locator/root/manifest/member/prior-pointer hashes and atomically replace/fsync pointer. Inject failure at every file/dir/rename/pointer boundary, prove recoverable orphan complete target, owned-only cleanup and exact retry timestamp/byte stability.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_release_safety.py tests/cli/test_korean_release_safety_commands.py -k 'release_promoter or staging or fsync or reopen or atomic_rename or pointer or interruption or collision or exact_retry' -q</automated></verify>
  <done>Complete validated release trees and current pointer promote durably without partial visibility or overwrite.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-02 / SC-05 / SC-09 / SC-10 | Complete review and safe immutable outputs | COVERED |
| REQ | KFREQ-01 / KFREQ-02 / KTXT-01 / KAUD-01 / GPRO-01 / GAUD-01 | Review, safety and release authority tooling | COVERED |
| RESEARCH | Read-only review / atomic release / security | Complete aggregate, classifications, fsync/reopen/pointer | COVERED |
| CONTEXT | Separate commit/publication, private safety, exact local release, Phase 34 boundary | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-15-01 | Tampering / Repudiation | review receipts | mitigate | Read-only disjoint exact-hash coverage and input invariance. |
| T-32-15-02 | Information Disclosure | safety | mitigate | Fixed roots and hash/count/control-ID output only. |
| T-32-15-03 | Tampering / Race Condition | release/pointer | mitigate | Owned staging, exact inventory, fsync/reopen, absent-target rename and recoverable pointer. |
</threat_model>
<high_leverage_review>Second pass: prove no import path in aggregation, safe-to-publish fail-closed derivation, exact 13-member inventory, fsync/reopen/rename/pointer interruption recovery and no-write retry.</high_leverage_review>
<verification>Run all three focused commands offline in disposable roots; no live release occurs.</verification>
<success_criteria>
- Review aggregation is complete and read-only.
- Safety/build evidence is canonical and private.
- Immutable release/pointer promotion is durable and recoverable.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-15-SUMMARY.md` with review coverage, safety matrix, 13-member inventory, interruption/pointer proof and bounded self-checked claim.</output>
