---
phase: 32-frequency-portuguese-text-and-audio
plan: "07"
type: execute
wave: 19
runtime: opencode
assurance: self_checked
depends_on: [32-06]
files-modified:
  - tests/services/test_korean_text_generation.py
  - tests/services/test_generate_text_items.py
  - tests/services/test_regenerate_text_item.py
  - tests/cli/test_korean_provider_commands.py
  - tests/test_runtime.py
  - src/multilang/services/korean_text_generation.py
  - src/multilang/services/text_generation.py
  - src/multilang/services/provider_text_adapters.py
  - src/multilang/services/generate_text_items.py
  - src/multilang/services/regenerate_text_item.py
  - src/multilang/services/lexical_grounding.py
  - src/multilang/runtime.py
  - src/multilang/cli.py
autonomous: true
requirements: [KFREQ-03, KTXT-01, GTXT-01, GPRO-01]
non_goals: ["No live calls, production route/budget choice, fallback provider, content approval, or implicit authority path."]
no_ui_proof_rationale: "Offline selector, resumability, and staged command contracts only."
second_pass_required: true
closure_claim_limit: "Proves fake-provider exact 2+1 history and genuine-Phase-31-bound command behavior only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Exactly two initial candidate bundles and at most one cache-distinct repair exist across generation/regeneration history."
    - "All text adapters are constructed only after fresh genuine Phase 31 verification and explicit locator/content CAS reload."
    - "Missing policy, budget, authority, or acceptable output becomes resumable failure/review, never local/Tatoeba/cross-provider fallback."
    - "After audio binding, a repair may install only when NFC spoken word/sentence hashes preserve every existing audio request; otherwise it stops for replanning before content mutation."
  artifacts:
    - {path: src/multilang/services/korean_text_generation.py, provides: "Shared exact 2+1 selector"}
    - {path: src/multilang/services/generate_text_items.py, provides: "Persisted selector-history orchestration"}
    - {path: src/multilang/cli.py, provides: "Prepare/bind/check/generate command contracts"}
  key_links:
    - {from: "persisted candidate history", to: "generation and regeneration", via: "one selector and one remaining repair budget"}
    - {from: "Phase 31/source/policy CAS", to: "adapter constructor", via: "immediate verify-active and locator/content rehash"}
---

# Phase 32 Plan 07: Exact 2+1 Text State Machine and Staged Commands

<objective>
Implement the shared exact 2+1 selector, preserve its history across regeneration, and expose genuine-Phase-31-bound job/text commands.

Purpose: Korean final generation must be deterministic, bounded, no-fallback, and dominated by fresh immutable authority immediately before each adapter.
Output: Selector, shared resume history, prepare/bind/check/generate CLI contracts.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-06-PLAN.md
@src/multilang/services/text_generation.py
@src/multilang/runtime.py
<interfaces>
Create `prepare-korean-frequency-job`, `bind-korean-frequency-audio-authority`, `check-korean-frequency-job-binding`, and `generate-korean-frequency-text`. Every command accepts explicit database/job, the genuine Phase 31 active pointer/content hash, validation receipt/hash, pointer-selected snapshot manifest/root hashes, bundle root/manifest, authority files/sidecars, binding receipt and applicable catalog/profile inputs; settings supply none of these paths.
</interfaces>
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-07-01" type="auto" tdd="true">
  <name>Implement the exact two-initial-plus-one-repair selector core</name>
  <files>
    - CREATE: tests/services/test_korean_text_generation.py
    - CREATE: src/multilang/services/korean_text_generation.py
    - MODIFY: src/multilang/services/text_generation.py
    - MODIFY: src/multilang/services/provider_text_adapters.py
  </files>
  <behavior>
    - Initial output contains exactly two strict complete candidate bundles; one distinct repair occurs only when neither passes.
    - Hard gates precede deterministic scoring and provider output cannot assign identity, policy, path, command, SQL, review, or approval fields.
    - No fourth path, loose JSON salvage, same-cache repair, Tatoeba/local/alternate provider, or translation fallback exists.
  </behavior>
  <action>Write selector tests first. Implement bounded structured outputs with distinct candidate ordinal/task/prompt/request/cache identities, definition and translation through one telemetry wrapper, complete-bundle gates then deterministic score, and one repair carrying controlled rejection codes/candidate hashes only. Delimit every source/provider string as untrusted data, strictly validate and context-escape selected fields, and poison direct adapter, loose prose/JSON salvage, identity assignment, hidden retry and every fallback path.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_text_generation.py -k 'two_plus_one or repair or selector or fallback or cache or structured or PT_BR' -q</automated></verify>
  <done>One selector exposes exactly two initial candidates and no more than one cache-distinct repair.</done>
</task>

<task id="32-07-02" type="auto" tdd="true">
  <name>Share one persisted selector history across generation and regeneration</name>
  <files>
    - MODIFY: tests/services/test_generate_text_items.py
    - MODIFY: tests/services/test_regenerate_text_item.py
    - MODIFY: src/multilang/services/generate_text_items.py
    - MODIFY: src/multilang/services/regenerate_text_item.py
    - MODIFY: src/multilang/services/lexical_grounding.py
  </files>
  <behavior>
    - Initial and targeted regeneration share immutable candidate/attempt/rejection history across invocations.
    - A failed initial history permits only its one unused repair and cannot reset or create another repair.
    - Once audio requests exist, repaired output is staged and cannot install if either bound NFC spoken-text hash would change or require a new audio request.
    - Non-Korean generation/regeneration remains unchanged.
  </behavior>
  <action>Delegate Korean final generation and regeneration to Task 01 using persisted identity, candidate/attempt hashes, rejection codes and job-frozen policy. Remove the independent Korean repair loop and make resume consume only the original remaining repair budget. When audio requests are already bound, stage the repair result before row installation, recompute NFC word/sentence spoken-text hashes, and compare them with every exact dependent request; any changed hash fails closed with controlled `requires_replan`, installs no repaired candidate, and creates no audio request. Poison history reset, direct adapter, same-cache/second repair, stale dependent audio and fallback while preserving all non-Korean behavior.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_generate_text_items.py tests/services/test_regenerate_text_item.py -k 'korean and (selector or two_plus_one or repair or fallback or cache or resume or history or dependent_audio or spoken_text)' -q</automated></verify>
  <done>Generation/regeneration share one exact 2+1 history, and post-audio repair cannot install stale spoken text or create a new request.</done>
</task>

<task id="32-07-03" type="auto" tdd="true">
  <name>Implement genuine Phase 31-bound staged CAS and exact text commands</name>
  <files>
    - CREATE: tests/cli/test_korean_provider_commands.py
    - MODIFY: tests/test_runtime.py
    - MODIFY: src/multilang/services/lexical_grounding.py
    - MODIFY: src/multilang/runtime.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Prepare/check bind and verify exact Phase 31 pointer/receipt/selected snapshot plus bundle locator/content and every source/policy authority; full also binds distinct provider-review/profile/heard and catalog locator/content.
    - `generate-korean-frequency-text` repeats all genuine verification immediately before every adapter constructor.
    - Exact retry is receipt/row stable and any one-fact drift adds zero attempts.
  </behavior>
  <action>Implement the interface commands over the sole migration and Plan 03 locator helper. Require every Phase 31/source/build/final/policy/execution/catalog/profile argument explicitly; descriptor-rehash the real active pointer, validation receipt and selected snapshot, invoke genuine pathless verify-active, discard raw locators, and compare exact staged CAS. Full stage validates provider-pilot-review, profile-sample and heard-review separately. Before every text adapter constructor rerun the complete verification. Add constructor poison and one-fact pointer/receipt/snapshot/DB/job/root/catalog/authority drift plus exact-retry tests; failures leave candidate and attempt counts unchanged.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_provider_commands.py tests/test_runtime.py -k 'korean and (phase31 or verify_active or binding or locator or receipt or snapshot or exact_retry or drift or pre_constructor or attempt_guard)' -q</automated></verify>
  <done>Exact staged text commands revalidate the genuine authority tuple immediately before all adapter work.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-04 / SC-08 / SC-09 | Bounded validated examples and observable providers | COVERED |
| REQ | KFREQ-03 / KTXT-01 / GTXT-01 / GPRO-01 | Exact 2+1, shared resume and authority-bound calls | COVERED |
| RESEARCH | Two candidates / distinct repair / explicit routes | Strict output and pre-constructor CAS | COVERED |
| CONTEXT | Exactly 2+1, one route/task, no fallback, Phase 31 tuple | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-07-01 | Injection / Misinformation | LLM output | mitigate | Strict bounded schemas, immutable identity, contextual escaping, no approval power. |
| T-32-07-02 | Denial of Service / Cost | candidate flow | mitigate | Exactly two candidates, one repair, job-frozen ceilings and no fallback. |
| T-32-07-03 | Spoofing / Tampering | pre-constructor authority | mitigate | Genuine Phase 31 and explicit locator/content CAS immediately before construction. |
</threat_model>
<high_leverage_review>Second pass: audit exact 2+1 history across commands, cache separation, strict untrusted output, no fallback, and genuine Phase 31/CAS constructor ordering.</high_leverage_review>
<verification>Run all task commands offline; inspect every text entry point for one selector and immediate CAS.</verification>
<success_criteria>
- Exact 2+1 is shared across generation and regeneration.
- Every adapter follows fresh genuine authority verification.
- Drift and retry semantics are zero-attempt and idempotent.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-07-SUMMARY.md` with 2+1 matrix, cache/history proof, command contracts, CAS ordering and bounded self-checked claim.</output>
