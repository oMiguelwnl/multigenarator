---
phase: 32-frequency-portuguese-text-and-audio
plan: "19"
type: execute
wave: 31
runtime: opencode
assurance: self_checked
depends_on: [32-17, 32-18]
files-modified:
  - .multilang/phase32/source-quarantine/한국어 학습용 어휘 목록.txt
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-retrieval-result.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-retrieval-validation.json
autonomous: true
requirements: [KFREQ-01, GLEX-01, GLEX-02]
non_goals: ["No transformation, curation, production asset directory, database mutation, provider call, commit, or approval inference."]
no_ui_proof_rationale: "One fixed public-source retrieval and machine evidence only."
second_pass_required: true
closure_claim_limit: "Proves exact response-derived source bytes and retrieval controls only; terms, transformation, redistribution and content remain unapproved, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "The landing page is contacted only under a valid retrieval-only authority and the attachment target derives from its bounded response."
    - "Exactly one NFC TXT named 한국어 학습용 어휘 목록.txt is installed in owned quarantine after schema/encoding/header/redirect validation."
    - "Independent read-only validation recomputes every response and byte fact while no production bundle/path exists."
  artifacts:
    - {path: .multilang/phase32/source-quarantine/한국어 학습용 어휘 목록.txt, provides: "Quarantined exact NIKL attachment bytes"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-retrieval-result.json, provides: "Content-free retrieval result"}
  key_links:
    - {from: "source-access authority", to: "quarantined TXT", via: "fixed landing request and response-derived attachment resolver"}
---

# Phase 32 Plan 19: Exact NIKL Source Retrieval

<objective>
Retrieve and independently validate exactly one response-derived official TXT under the source-access-only authority.

Purpose: Exact source bytes must be known without guessing a locator or accidentally granting transform/redistribution power.
Output: Quarantined TXT, retrieval result and read-only validation.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-18-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-17-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-19-01" type="auto">
  <name>Retrieve the one response-derived official TXT into owned quarantine</name>
  <files>
    - CREATE: .multilang/phase32/source-quarantine/한국어 학습용 어휘 목록.txt
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-retrieval-result.json
  </files>
  <action>First validate the source-access authority/sidecar and require retrieval-only authorized status. Invoke `retrieve-korean-frequency-source` with the fixed landing URL, expected normalized filename, owned quarantine root, strict byte/redirect/time bounds and result path. Permit no user URL/header/credential override. Require fixed HTTPS host/path/query, public DNS/IP at every hop, normal TLS, no credential forwarding, exactly one response-derived target, TXT encoding/schema, owned temp fsync and atomic install. On denial/drift/failure, create no source file and remove only owned temporary files.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import hashlib,json,pathlib,unicodedata; f=pathlib.Path('.multilang/phase32/source-quarantine/한국어 학습용 어휘 목록.txt'); r=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-retrieval-result.json').read_text()); assert f.is_file() and unicodedata.normalize('NFC',f.name)==f.name and f.name=='한국어 학습용 어휘 목록.txt' and hashlib.sha256(f.read_bytes()).hexdigest()==r['attachment_sha256'] and r['status']=='retrieved'"</automated></verify>
  <done>Only the exact response-derived official TXT exists in quarantine with matching content-free retrieval evidence.</done>
</task>

<task id="32-19-02" type="auto">
  <name>Independently revalidate retrieval and prove pre-transform invariance</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-retrieval-validation.json
  </files>
  <action>Invoke `validate-korean-source-retrieval-result` read-only over the authority/sidecar, retrieval result and exact quarantined TXT. Recompute landing/attachment locator hashes, redirects, headers, normalized filename, byte length/hash, encoding and expected lexical TXT shape; assert the retrieval result grants no transform power and that `assets/frequency/ko`, an active bundle pointer and every database remain absent/unchanged. Write only safe hashes/counts/status.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync multilang validate-korean-source-retrieval-result --authority .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md --authority-sidecar .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md.sha256 --retrieval-result .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-retrieval-result.json --source-file '.multilang/phase32/source-quarantine/한국어 학습용 어휘 목록.txt' --output .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-retrieval-validation.json</automated></verify>
  <done>Exact retrieval facts are independently recomputed and no downstream authority or side effect exists.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 | Exact source artifact follows decision | COVERED |
| REQ | KFREQ-01 / GLEX-01 / GLEX-02 | Auditable fixed source bytes | COVERED |
| RESEARCH | Response-derived resolver / SSRF / pre-ingestion evidence | Exact bounded TXT quarantine | COVERED |
| CONTEXT | NIKL page and named TXT; no spreadsheet/guessed URL | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-19-01 | Spoofing / SSRF | source retrieval | mitigate | Fixed host/path/query, public-IP checks, no redirects outside allowlist, response-derived target. |
| T-32-19-02 | Tampering / Race Condition | quarantine install | mitigate | Bounded no-follow temp, exact hashes, fsync and atomic replacement. |
| T-32-19-03 | Elevation | retrieval result | mitigate | Read-only validator confirms no transform/redistribution power. |
</threat_model>
<high_leverage_review>Second pass: inspect exact authority-before-network ordering, redirect/DNS/header limits, one attachment identity, atomic cleanup and absence of production paths.</high_leverage_review>
<verification>Run both automated checks; no provider/database/build operation is permitted.</verification>
<success_criteria>
- Retrieval is authority-gated and response-derived.
- Exact TXT bytes are quarantined atomically.
- Independent validation grants no downstream power.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-19-SUMMARY.md` with source locator/content hashes, network-control matrix, validation result and bounded claim.</output>
