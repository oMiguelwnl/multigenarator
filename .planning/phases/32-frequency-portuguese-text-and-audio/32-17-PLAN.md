---
phase: 32-frequency-portuguese-text-and-audio
plan: "17"
type: execute
wave: 29
runtime: opencode
assurance: self_checked
depends_on: [32-16]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-preflight.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md.sha256
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authority-validation.json
autonomous: false
requirements: [KFREQ-01, GLEX-01, GLEX-02]
non_goals: ["No source/network access, transformation, database mutation, bundle build, provider work, commit, or publication."]
no_ui_proof_rationale: "Legal/source authority checkpoint over hashes and fixed source metadata only."
second_pass_required: true
closure_claim_limit: "Authorizes only one bounded retrieval from the selected official page; it does not approve bytes, transformation, redistribution, or content, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 2, safe_parallelism: [32-18]}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Offline command/dependency readiness and exact source-decision evidence pass before any source authority is considered; Phase 31 joins only before production generation."
    - "The human decision names the fixed NIKL page, response-derived exact TXT rule, intended use, storage and redistribution posture without guessing attachment bytes or URL."
    - "The resulting authority has source-access power only and a detached whole-file SHA-256 sidecar."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-preflight.json, provides: "Read-only offline dependency/source-decision readiness"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md, provides: "Human source-access-only authority"}
  key_links:
    - {from: "source decision and offline dependency tuple", to: "source-access authority", via: "read-only preflight plus fixed-power project-owner decision"}
---

# Phase 32 Plan 17: Source-Access Authorization Checkpoint

<objective>
Revalidate offline dependencies and obtain an exact least-power project-owner decision before opening the selected NIKL landing page.

Purpose: Planning-source selection is not permission to access, transform, redistribute, or commit exact bytes.
Output: Read-only preflight and sidecar-bound source-access authority.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-16-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/32-FREQUENCY-SOURCE-DECISION.md
@.planning/AI-LINGUISTIC-REVIEW-POLICY.md
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-17-01" type="auto">
  <name>Produce read-only dependency and source-decision preflight</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-preflight.json
  </files>
  <action>Run the Plan 09 harness in dependency-only mode with the fixed Phase 32 command inventory, `pyproject.toml`, `uv.lock`, global AI policy, and exact source-decision bytes. Assert that neither the landing page nor any provider is contacted, no production/source directory is created, and every protected input hash is unchanged. Write scanner-safe JSON only. Do not require the Phase 31 active pointer at this source-access boundary.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; p=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-preflight.json'); d=json.loads(p.read_text()); assert d['status']=='passed' and d['network_attempt_count']==0 and d['provider_attempt_count']==0 and d['protected_inputs_unchanged'] is True and d['shared_venv_unchanged'] is True"</automated></verify>
  <done>Fixed offline dependency/command readiness is proven without source access or mutation; the Phase 31 production join remains later.</done>
</task>

<task id="32-17-02" type="checkpoint:user" gate="blocking">
  <name>Decide and materialize exact source-access-only authority</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md.sha256
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authority-validation.json
  </files>
  <action>Review only the Task 01 preflight and cited source-decision facts, select one option, and provide the required bounded decision fields. After selection, materialize one prose artifact containing exactly one fenced canonical JSON section with kind=`source-access`, fixed landing URL, response-derived filename rule, source-decision/preflight hashes, decision fields and either retrieval-only power or denied status; create the detached lowercase SHA-256 sidecar and validate fixed kind `source-access` in the isolated environment. Do not access the source, run a parser, create source/bundle paths, provide secrets/private paths, or add transformation/build/review/commit/publication power.</action>
  <decision>Authorize or deny one bounded retrieval from `https://www.korean.go.kr/front/etcData/etcDataView.do?mn_id=46&amp;etc_seq=70`, resolving only the response-derived NFC filename `한국어 학습용 어휘 목록.txt`.</decision>
  <context>The decision must separately record intended use, local/private storage, transformed-data distribution, public Git history, commercial downstream use, attribution/change-notice expectations, and the fact that exact bytes/terms remain unreviewed. Approval grants retrieval only; denial ends the external path with the technical implementation still complete.</context>
  <options>
    <option id="authorize-bounded-retrieval"><name>Authorize bounded retrieval only</name><pros>Allows exact bytes and terms evidence to be captured under the fixed resolver.</pros><cons>Does not authorize transformation, redistribution, bundle creation, commit, provider use, or publication.</cons></option>
    <option id="deny-source-access"><name>Deny source access</name><pros>Guarantees zero source-network action.</pros><cons>Production frequency content remains explicitly blocked.</cons></option>
  </options>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync multilang validate-korean-checkpoint-authority --authority .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md --sidecar .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authorization.md.sha256 --expected-kind source-access --output .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/source-access-authority-validation.json</automated></verify>
  <done>The explicit retrieval-only authorization or denial is machine-valid, sidecar-bound, non-widenable, and contains every named use/storage/redistribution field.</done>
  <resume-signal>Select `authorize-bounded-retrieval` or `deny-source-access` and provide the named use/storage/redistribution decisions; no secrets or private paths.</resume-signal>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 | Source decision precedes exact bytes | COVERED |
| REQ | KFREQ-01 / GLEX-01 / GLEX-02 | Exact source and identity boundary | COVERED |
| RESEARCH | Checkpoints 1-2 / source-license pitfall | Genuine dependency and retrieval-only authority | COVERED |
| CONTEXT | Selected NIKL source; exact bytes/use/redistribution remain gated | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-17-01 | Elevation | source authority | mitigate | Fixed kind/power and sidecar validation; CLI cannot widen it. |
| T-32-17-02 | Information Disclosure | checkpoint artifacts | mitigate | Hashes/control IDs only; no paths, content, terms prose or secrets. |
</threat_model>
<high_leverage_review>Second pass: confirm zero network/mutation in preflight and that source-access approval cannot imply transformation, redistribution, asset commit or publication.</high_leverage_review>
<verification>Run the two automated checks; inspect network/provider counters and the validated fixed power.</verification>
<success_criteria>
- Genuine dependency readiness is revalidated read-only.
- The user explicitly authorizes or denies exact bounded source access.
- Any approval remains retrieval-only and sidecar-bound.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-17-SUMMARY.md` with preflight hashes, decision ID/status, validated power and bounded claim.</output>
