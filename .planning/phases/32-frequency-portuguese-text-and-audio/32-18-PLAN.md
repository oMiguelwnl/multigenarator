---
phase: 32-frequency-portuguese-text-and-audio
plan: "18"
type: execute
wave: 30
runtime: opencode
assurance: self_checked
depends_on: [32-17]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-suite-preflight.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-offline-suite.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-dependency-evidence.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-readiness.md
autonomous: true
requirements: [KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, KAUD-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, GPRO-01, GAUD-01]
non_goals: ["No source/network/provider access, source transformation, production DB mutation, bundle/audio/release creation, Git action, or dependency synchronization."]
hard_boundaries: ["Run only from the dedicated .planning/.local/phase32-py312 environment created by Plan 01.", "Treat shared .venv, dependency files, Phase 31 authority, source/bundle/audio/release roots, databases, and user credentials as protected read-only inputs."]
escalation_triggers: ["Stop before source retrieval if the isolated environment, Python 3.12, frozen lock, poison controls, protected hashes, or complete unfiltered suite cannot be proven."]
approval_gates: ["A passing pre-source suite does not grant source, provider, production DB, release, Git, or publication power."]
anti_regression_targets: ["The shared .venv and every protected repository/runtime root remain byte-identical; tests observe no credential, network, or provider access."]
known_unknowns: ["External source/provider/DB evidence remains unknown and must stay untouched until later exact authorities."]
no_ui_proof_rationale: "Pre-external repository regression and environment-isolation evidence only."
high_leverage_surfaces: ["Frozen dependency environment, full-suite isolation, and protected-state invariance before the first external action."]
second_pass_required: true
closure_claim_limit: "Proves only that the complete repository suite passed before external work in a credential-empty poisoned isolated environment; planner self-check only and fresh external recheck pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
leverage: {lost: "No shared-environment convenience or selective-suite shortcut.", kept: "Repository-native full test scope and genuine Phase 31 dependencies.", gained: "A replayable pre-external baseline proving dependency and protected-root invariance."}
must_haves:
  truths:
    - "The dedicated Python 3.12 environment and frozen lock are revalidated without syncing or touching shared .venv."
    - "The complete unfiltered tests tree passes with credentials absent and network/provider constructors poisoned."
    - "Phase 31, dependency files, shared .venv, source/bundle/audio/release roots, and databases are unchanged before retrieval."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-suite-preflight.json, provides: "Exact isolated-environment and protected-root baseline"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-offline-suite.json, provides: "Complete credential-empty pre-source suite evidence"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-readiness.md, provides: "Human-readable readiness projection from the same canonical evidence"}
  key_links:
    - {from: ".planning/.local/phase32-py312 and protected pre-hashes", to: "pre-source suite result", via: "frozen no-sync interpreter plus fixed-argv poisoned harness and post-hash comparison"}
---

# Phase 32 Plan 18: Pre-External Isolated Full-Suite Readiness

## Objective

<objective>
Run the complete repository suite under the dedicated frozen Phase 32 environment before the first source request, proving credential/network/provider isolation and protected-state invariance.

Purpose: No external source work may begin on an unverified dependency surface or after silently mutating shared state.
Output: Preflight, full-suite/dependency evidence, and a canonical readiness projection.
</objective>

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>

## Context

<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-17-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/python-environment-receipt.json
@scripts/run_phase32_isolated_suite.py
</context>

## Requirements Covered
- All Phase 32 requirements are protected by the pre-external regression gate.

## Must-Haves
- The frontmatter truths, artifacts, and key link are normative.

## Anti-Goals
- Do not contact NIKL or any provider and do not sync, install, migrate, generate, or release.

## Hard Boundaries
- Use only `.planning/.local/phase32-py312/bin/python` or `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync`.

## Evidence Contract
- Canonical JSON records Python/lock/project/shared-venv/protected-root hashes, full `tests` scope, zero credentials/network/providers, and pre/post equality; Markdown derives from that object.

## Common Pitfalls
- A focused suite, inherited dotenv, shared `.venv`, hidden dependency sync, or writable production root is not acceptable evidence.

## Stop-And-Challenge
- Stop before Plan 19 on any isolation, suite, lock, Python, shared-environment, or protected-root failure.

## Approval Gates
- This plan grants readiness only and cannot authorize the subsequent source request.

## Checks
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

## Tasks

<tasks>
<task id="32-18-01" type="auto">
  <name>Revalidate the frozen isolated environment and snapshot protected state</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-suite-preflight.json
  </files>
  <action>Descriptor-safely validate Plan 01's environment receipt, reject symlink/reparse traversal, require Python 3.12, rerun `UV_OFFLINE=1 uv lock --check`, and compare current `pyproject.toml`, `uv.lock`, interpreter, environment descriptor, and deterministic shared `.venv` tree hash to the receipt. Snapshot genuine Phase 31 files plus source quarantine, frequency assets, `.multilang/phase32`, release roots, configured database files, and all credential-name absence without reading secret values. Do not create any protected root that is absent.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-suite-preflight.json').read_text()); assert d['status']=='ready' and d['python_minor']=='3.12' and d['lock_check_passed'] is True and d['shared_venv_unchanged'] is True and d['credential_name_count']==0"</automated></verify>
  <done>The exact frozen interpreter/dependency identity and protected prestate are known without a sync or protected write.</done>
</task>

<task id="32-18-02" type="auto">
  <name>Run the complete pre-source repository suite under hard isolation</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-offline-suite.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-dependency-evidence.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/pre-source-readiness.md
  </files>
  <action>Invoke the Plan 09 harness in pre-source full mode with fixed argv and a bounded outer timeout. The child must use the dedicated interpreter with frozen/no-sync semantics, an allowlisted credential-empty environment, no repository dotenv, poisoned DNS/socket and every real LLM/DeepL/Azure/other provider constructor, disposable CWD/HOME/XDG/DB/assets/audio/export/cache/staging roots, and the complete unfiltered `tests` tree. Compare every Task 01 protected hash afterward, including shared `.venv`; write JSON suite/dependency evidence and derive Markdown from the same in-memory evidence. A skipped/filtered suite, network/provider attempt, credential name, protected drift, or nested recursive suite is failure.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; r=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox'); s=json.loads((r/'pre-source-offline-suite.json').read_text()); d=json.loads((r/'pre-source-dependency-evidence.json').read_text()); assert s['status']=='passed' and s['pytest_scope']=='tests' and s['network_attempt_count']==s['provider_attempt_count']==s['credential_name_count']==0 and s['protected_roots_unchanged'] is True and s['shared_venv_unchanged'] is True and d['dependencies_unchanged'] is True"</automated></verify>
  <done>The complete pre-source suite passes in hard isolation and every protected/dependency byte remains unchanged.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01..SC-10 | Regression-safe readiness before external work | COVERED |
| REQ | All 11 Phase 32 IDs | Complete pre-source repository gate | COVERED |
| RESEARCH | Isolation / phase gate | Frozen environment, poison controls and full suite | COVERED |
| CONTEXT | No source/provider/DB action before readiness | Locked decisions preserved | COVERED |
</source_audit>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-18-01 | Supply chain / Tampering | Python dependencies | mitigate | Python 3.12, frozen lock, no-sync environment and shared `.venv` pre/post hash. |
| T-32-18-02 | Information Disclosure / SSRF | full suite | mitigate | Credential-empty allowlist plus network/provider poison and disposable roots. |
</threat_model>

## High-Leverage Review
<high_leverage_review>Second pass: inspect environment identity, full unfiltered scope, poison controls, shared `.venv` and every protected pre/post hash.</high_leverage_review>

## Verification
<verification>Run both isolated commands and require no source, provider, production DB, release or Git side effect.</verification>

## Success Criteria
<success_criteria>
- Frozen Python 3.12 readiness passes without dependency or shared-environment mutation.
- The complete repository suite passes credential-empty and network/provider-poisoned.
- All protected roots remain exact before Plan 19.
</success_criteria>

## Leverage Review
- Lost: Shared-environment convenience and selective-suite shortcuts.
- Kept: Repository-native tests and genuine dependency authority.
- Gained: A replayable pre-external regression baseline.

## Notes
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-18-SUMMARY.md` with environment/dependency hashes, complete-suite result, protected-root comparison and bounded claim.</output>
