---
phase: 32-frequency-portuguese-text-and-audio
plan: "52"
type: execute
wave: 64
runtime: opencode
assurance: self_checked
depends_on: [32-51]
files-modified:
  - .planning/.state-fingerprint.json
  - .planning/SPEC.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-52-PLAN.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-52-SUMMARY.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-env-load-authority.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-target-classification.json
autonomous: true
requirements:
  - KFREQ-01
  - KFREQ-02
  - KFREQ-03
  - KTXT-01
  - KAUD-01
  - GLEX-01
  - GLEX-02
  - GMOR-01
  - GTXT-01
  - GPRO-01
  - GAUD-01
non_goals:
  - Do not promote the prior disposable/test `.env` target into production readiness or production database authority.
  - Do not connect to a database, run Alembic, bind a job, ingest candidates, call providers, query Azure, synthesize audio, import or apply review, export, release, publish, deliver, commit, PR, or close Phase 32.
  - Do not print or persist a raw database URL, credential, host, username, database name, private path, SQL row content, provider payload, prompt, completion, generated Korean text, Portuguese text, or audio bytes.
hard_boundaries:
  - The current-session `.env` exception allows reading only `MULTILANG_DATABASE_URL` for sanitized locator classification, not provider credentials or other secrets.
  - Compare the current `.env` locator only by the same non-secret hash algorithm used by Plan 32-47 disposable preflight evidence.
  - If the current locator hash equals the prior disposable/test target hash, stop before DB connection and record `blocked_matches_disposable_test_target`.
  - A real production DB preflight/migration/job-binding plan requires a locator not previously classified as disposable/test, or explicit user reclassification of the target with its consequences.
escalation_triggers:
  - Stop if `.env` lacks `MULTILANG_DATABASE_URL`, the value is malformed, or it is not PostgreSQL.
  - Stop if the current locator matches the Plan 32-47/32-48 disposable/test target.
  - Stop if proving target classification would require printing a URL or connecting to the database.
  - Stop if the user asks to treat the disposable/wiped target as production without explicitly acknowledging prior disposable classification and reset history.
approval_gates:
  - The only new approval consumed is the current-session `aprovo carregar .env sem imprimir` answer, limited to secret-safe `.env` parsing.
  - Production migration, job binding, full-run provider/model/budget use, Azure synthesis, review application, export, release, publication, delivery, commit, PR, and Phase 32 closure remain separately gated.
anti_regression_targets:
  - Prior disposable/test evidence remains non-production and cannot grant production readiness.
  - The Plan 32-48 reset result remains a disposable/test rehearsal only, even if the same locator is currently in `.env`.
  - Korean remains absent from the static `audio_voice_registry`; `select_voice(SupportedLanguage.KO)` must still fail.
  - Missing or unsafe production target classification must fail closed before connection, migration, job binding, providers, Azure, review, export, release, publication, delivery, or closure.
known_unknowns:
  - Whether the user intended the current `.env` target to remain disposable/test or become the production target is unresolved if the locator matches prior disposable evidence.
  - A distinct production database target, migration state, schema state, row counts, and job state remain unknown because this plan does not connect to any database.
ui_proof_slots: []
no_ui_proof_rationale: Backend/planning/secret-handling gate only; no UI behavior, visual rendering, or Anki import/playback claim is made.
high_leverage_surfaces:
  - Secret-safe `.env` parsing and production/disposable target classification.
  - Production database authority boundary before live DB mutation.
second_pass_required: true
closure_claim_limit: This plan can prove only that `.env` loading was approved, that the current locator was classified by hash, and that execution stopped because it matches prior disposable/test target evidence. It must not claim production DB readiness, migration, job binding, provider execution, audio, review, export, release, publication, delivery, or Phase 32 completion.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: No production DB progress occurs until the target classification is resolved.
  kept: Secret hygiene, prior disposable/test evidence boundaries, and the production mutation gate.
  gained: Prevents accidental promotion of a wiped disposable target into production evidence.
must_haves:
  truths:
    - The user's `.env` load approval is recorded as a narrow exception with no raw secret disclosure.
    - The current `.env` `MULTILANG_DATABASE_URL` locator is hash-compared to prior disposable/test evidence without connecting to a DB.
    - If the hashes match, a blocked evidence artifact records that no production DB preflight, migration, or job binding may proceed on that target.
  artifacts:
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-env-load-authority.md
      provides: Sanitized current-session authority for parsing only `MULTILANG_DATABASE_URL` from `.env` without printing or persisting the value.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-target-classification.json
      provides: Sanitized target-classification result and blocker when the current locator matches prior disposable/test evidence.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/32-52-SUMMARY.md
      provides: Non-closing blocked summary and next required user/operator choice.
  key_links:
    - from: current-session `.env` load approval
      to: production-database-env-load-authority.md
      via: narrow approval capture for secret-safe `MULTILANG_DATABASE_URL` parsing only.
    - from: .env `MULTILANG_DATABASE_URL`
      to: production-database-target-classification.json
      via: local parser and non-secret locator hash comparison, without DB connection.
    - from: disposable-database-preflight.json
      to: production-database-target-classification.json
      via: prior disposable/test locator hash equality check.
---

# Phase 32 Plan 52: Production Target Classification Gate

## Objective
Capture the user's explicit permission to load `.env` without printing secrets, then classify the current `MULTILANG_DATABASE_URL` by hash before any database connection. If the locator matches the target already classified and wiped as disposable/test in Plans 32-47/32-48, record a production target blocker and stop.

## Context
- Plan 32-51 captured production DB gate approval but stopped because the process environment lacked `MULTILANG_DATABASE_URL`.
- The user then approved `aprovo carregar .env sem imprimir`, which permits a narrow `.env` parsing exception for the DB locator only.
- Plans 32-47 and 32-48 classified the existing `.env` target as disposable/test, blocked on non-Alembic schema, then wiped and migrated it as a disposable rehearsal; those artifacts explicitly deny production authority.
- A hash-only comparison during planning found the current `.env` locator matches Plan 32-47 `database_locator_sha256`.
- `.planning/phases/32-frequency-portuguese-text-and-audio/32-APPROACH.md` requires production database mutation to remain a project-owner power and forbids promoting fixture/disposable evidence into production readiness.

<assurance_check>
source_artifact: .planning/phases/32-frequency-portuguese-text-and-audio/32-51-SUMMARY.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: self_checked
status: matched_after_checker
warning: None; gsdd-plan-checker passed in the same runtime family, so assurance remains self_checked rather than cross_runtime_checked.
</assurance_check>

## Requirements Covered
- KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, KAUD-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, GPRO-01, GAUD-01

## Must-Haves
1. Record the `.env` load approval as a narrow exception, not a general secret-reading or mutation approval.
2. Parse only `MULTILANG_DATABASE_URL` from `.env` without shell execution, printing, or persistence of the raw value.
3. Compare the current locator hash to prior disposable/test evidence and block if it matches.
4. Keep Phase 32 in progress and make the next choice explicit.

## Anti-Goals
- No production DB connection, Alembic migration, job binding, ingestion, provider/Azure call, audio synthesis, review application, export, release, publication, delivery, commit, PR, or Phase 32 closure.
- No promotion of Plan 32-47/32-48 disposable/test evidence into production readiness.
- No raw locator, credentials, DB components, provider secrets, private paths, generated text, prompt/completion payloads, SQL row content, or audio bytes in artifacts.

## Hard Boundaries
- Use a local parser for `.env`; do not `source .env` or execute shell content from the file.
- Do not inspect provider keys or other `.env` entries.
- Do not connect to the database; this is a pre-connection target classification gate only.
- If locator hash equals the prior disposable/test hash, stop and require user/operator resolution before any production DB plan.

## Evidence Contract
- `production-database-env-load-authority.md` records the user approval, allowed parsing scope, and forbidden powers.
- `production-database-target-classification.json` records `status=blocked_matches_disposable_test_target`, current/prior locator hash equality, `database_connection_attempted=false`, `alembic_migration_attempted=false`, `job_binding_attempted=false`, and zero external side effects.
- `32-52-SUMMARY.md` records the blocked result and next required choice without claiming production readiness.
- Verification scans outputs for raw URL strings, URL-like Postgres locators, non-generic URL components, private absolute paths, and generated/provider/audio payload markers.

## Common Pitfalls
- Treating the same `.env` target as production merely because the current task is a production gate.
- Shell-sourcing `.env` and accidentally executing content or exposing unrelated secrets.
- Recording URL components while trying to prove secret hygiene.
- Overclaiming DB readiness when no connection, migration, or job binding occurred.

## Stop-And-Challenge
- Stop if `.env` parsing cannot locate a valid PostgreSQL `MULTILANG_DATABASE_URL` without printing it.
- Stop if the current locator matches the prior disposable/test target hash.
- Stop if the next action would connect, migrate, bind, ingest, run providers/Azure, review, export, release, publish, deliver, commit, PR, or close Phase 32.

## Approval Gates
- `.env` parsing only: approved by the user's current-session answer `aprovo carregar .env sem imprimir`.
- Treating the current disposable/wiped target as production: not approved.
- Any distinct production DB preflight/migration/job binding: requires a separate plan after target classification is safe.
- Provider, Azure, review, export, release, publication, delivery, Git, and closure gates remain separate.

<checks>
<plan_check>
checker: gsdd-plan-checker
checker_task_id: ses_f7e898e74ffeOWnmW4v5lVa0Uq
checker_runtime: opencode subagent
status: passed
blocking: false
notes: Independent checker confirmed Plan 32-52 has runnable verification in principle and preserves the fail-closed production target classification boundary, including prior hash comparison, post-fingerprint leak scan, and next-choice summary language.
</plan_check>
</checks>

## Tasks

<task id="32-52-01" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-env-load-authority.md
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-target-classification.json
  </files>
  <action>
    Write the sanitized `.env` load authority, then parse only `MULTILANG_DATABASE_URL` from `.env` without shell execution. Compute the non-secret locator hash using the Plan 32-47 algorithm and compare it to `disposable-database-preflight.json`. Because the current hash matches the prior disposable/test target, write blocked target-classification evidence and stop before any database connection or mutation.
  </action>
  <verify>
    - Run:
      ```bash
      UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python - <<'PY'
      import hashlib
      import json
      import pathlib
      import re

      from sqlalchemy.engine import make_url

      def load_database_url() -> str:
          for raw_line in pathlib.Path('.env').read_text(encoding='utf-8').splitlines():
              line = raw_line.strip()
              if not line or line.startswith('#') or '=' not in line:
                  continue
              key, value = line.split('=', 1)
              if key.strip().removeprefix('export ').strip() == 'MULTILANG_DATABASE_URL':
                  return value.strip().strip('"').strip("'")
          raise AssertionError('missing database URL')

      root = pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox')
      auth = (root / 'production-database-env-load-authority.md').read_text(encoding='utf-8')
      gate = json.loads((root / 'production-database-target-classification.json').read_text(encoding='utf-8'))
      database_url = load_database_url()
      parsed = make_url(database_url)
      locator = '|'.join(str(part or '') for part in (parsed.get_backend_name(), parsed.host, parsed.port, parsed.database))
      assert hashlib.sha256(locator.encode('utf-8')).hexdigest() == gate['current_database_locator_sha256']
      assert gate['status'] == 'blocked_matches_disposable_test_target'
      assert gate['current_matches_prior_disposable_locator'] is True
      assert gate['database_connection_attempted'] is False
      assert gate['alembic_migration_attempted'] is False
      assert gate['job_binding_attempted'] is False
      assert gate['raw_secret_persisted'] is False
      assert 'env-file-load-approved' in auth
      body = auth + '\n' + json.dumps(gate, sort_keys=True)
      assert database_url not in body
      assert not re.search(r'postgres(?:ql)?(?:\+[a-z0-9_]+)?://', body, re.I)
      PY
      ```
    - Run:
      ```bash
      UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python - <<'PY'
      import hashlib
      import json
      import pathlib

      from sqlalchemy.engine import make_url

      def load_database_url() -> str:
          for raw_line in pathlib.Path('.env').read_text(encoding='utf-8').splitlines():
              line = raw_line.strip()
              if not line or line.startswith('#') or '=' not in line:
                  continue
              key, value = line.split('=', 1)
              if key.strip().removeprefix('export ').strip() == 'MULTILANG_DATABASE_URL':
                  return value.strip().strip('"').strip("'")
          raise AssertionError('missing database URL')

      root = pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox')
      gate = json.loads((root / 'production-database-target-classification.json').read_text(encoding='utf-8'))
      prior = json.loads((root / 'disposable-database-preflight.json').read_text(encoding='utf-8'))
      parsed = make_url(load_database_url())
      locator = '|'.join(str(part or '') for part in (parsed.get_backend_name(), parsed.host, parsed.port, parsed.database))
      current = hashlib.sha256(locator.encode('utf-8')).hexdigest()
      expected = '19871fdb496eed265952e0358b94fa795d1b74c37d89a674b0189ee1381f32b6'
      assert prior['database_locator_sha256'] == expected
      assert current == expected
      assert gate['current_database_locator_sha256'] == expected
      assert gate['prior_disposable_database_locator_sha256'] == expected
      assert gate['current_matches_prior_disposable_locator'] is True
      assert gate['prior_disposable_status'] in {'blocked_non_alembic_application_schema_present', 'ready_for_disposable_migration_rehearsal'}
      PY
      ```
  </verify>
  <done>
    The `.env` load exception is recorded, the current DB locator is classified by hash as the prior disposable/test target, and no DB/provider/audio/review/export side effects occurred.
  </done>
</task>

<task id="32-52-02" type="auto">
  <files>
    - MODIFY: .planning/SPEC.md
    - MODIFY: .planning/.state-fingerprint.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/32-52-SUMMARY.md
  </files>
  <action>
    Update `.planning/SPEC.md` Current State to record Plan 32-52 as blocked on target classification, keep Phase 32 in progress, refresh the session fingerprint, and write a non-closing summary. The summary must state that the current `.env` locator matches the previously disposable/test target and that no DB connection, migration, job binding, provider/Azure call, audio synthesis, review application, export, release, publication, delivery, Git action, or phase closure occurred.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from pathlib import Path; import json; phase=Path('.planning/phases/32-frequency-portuguese-text-and-audio'); summary=(phase/'32-52-SUMMARY.md').read_text(encoding='utf-8'); gate=json.loads((phase/'evidence-inbox/production-database-target-classification.json').read_text(encoding='utf-8')); spec=Path('.planning/SPEC.md').read_text(encoding='utf-8'); assert gate['status']=='blocked_matches_disposable_test_target'; assert 'Plan 32-52' in spec; assert 'blocked_matches_disposable_test_target' in spec; assert 'Phase 32 remains in progress' in summary; assert 'No database connection' in summary; assert 'No Alembic migration' in summary; assert 'No job binding' in summary; assert 'No provider call' in summary; assert 'No Azure call' in summary; assert 'No audio synthesis' in summary; assert 'No review application' in summary; assert 'No export' in summary; assert 'No release, publication, or delivery' in summary; assert 'distinct production DB locator' in summary; assert 'explicit target reclassification' in summary; assert 'prior disposable/test classification and reset history' in summary"`
    - Run:
      ```bash
      UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python - <<'PY'
      from multilang.domain.jobs import SupportedLanguage
      from multilang.services.audio_voice_registry import get_voice_registry, select_voice, VoiceSelectionError

      assert SupportedLanguage.KO not in get_voice_registry()
      try:
          select_voice(SupportedLanguage.KO)
      except VoiceSelectionError:
          pass
      else:
          raise AssertionError('Korean voice unexpectedly selected')
      PY
      ```
    - Run `node .planning/bin/gsdd.mjs phase-status 32 in_progress`
    - Run `node .planning/bin/gsdd.mjs session-fingerprint write`
    - Run:
      ```bash
      UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python - <<'PY'
      import pathlib
      import re

      from sqlalchemy.engine import make_url

      def load_database_url() -> str:
          for raw_line in pathlib.Path('.env').read_text(encoding='utf-8').splitlines():
              line = raw_line.strip()
              if not line or line.startswith('#') or '=' not in line:
                  continue
              key, value = line.split('=', 1)
              if key.strip().removeprefix('export ').strip() == 'MULTILANG_DATABASE_URL':
                  return value.strip().strip('"').strip("'")
          raise AssertionError('missing database URL')

      phase = pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio')
      root = phase / 'evidence-inbox'
      paths = [
          pathlib.Path('.planning/.state-fingerprint.json'),
          pathlib.Path('.planning/SPEC.md'),
          phase / '32-52-PLAN.md',
          phase / '32-52-SUMMARY.md',
          root / 'production-database-env-load-authority.md',
          root / 'production-database-target-classification.json',
      ]
      database_url = load_database_url()
      parsed = make_url(database_url)
      rendered = parsed.render_as_string(hide_password=False)
      components = {parsed.username, parsed.password, parsed.host, parsed.database, str(pathlib.Path.home())}
      components.update(str(v) for values in parsed.query.values() for v in (values if isinstance(values, (tuple, list)) else (values,)))
      generic = {'test', 'postgres', 'postgresql', 'localhost', 'default', 'public', 'admin', 'user', 'root', 'db', 'data', 'require'}
      components = {str(v) for v in components if v and len(str(v)) >= 8 and str(v).casefold() not in generic}
      body = '\n'.join(path.read_text(encoding='utf-8') for path in paths)
      whole = lambda value: re.search(r'(?<![A-Za-z0-9_.-])' + re.escape(value) + r'(?![A-Za-z0-9_.-])', body) is not None
      markers = ['s' + 'k-', 'BEGIN' + ' PRIVATE', 'provider' + '_' + 'payload', 'audio' + '_' + 'bytes']
      assert database_url not in body
      assert rendered not in body
      assert not re.search(r'postgres(?:ql)?(?:\+[a-z0-9_]+)?://', body, re.I)
      assert not any(whole(value) for value in components)
      assert not any(marker in body for marker in markers)
      PY
      ```
    - Run `git diff --check -- .planning/.state-fingerprint.json .planning/SPEC.md .planning/phases/32-frequency-portuguese-text-and-audio/32-52-PLAN.md .planning/phases/32-frequency-portuguese-text-and-audio/32-52-SUMMARY.md .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-env-load-authority.md .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-target-classification.json`
  </verify>
  <done>
    SPEC, phase status, fingerprint, and summary all report the same target-classification blocker without claiming Phase 32 completion.
  </done>
</task>

## Verification
- Plan checker confirms this target-classification plan fails closed and preserves production boundaries.
- Authority/classification assertions prove `.env` parsing is narrow and the current locator matches the prior disposable/test target.
- Summary/SPEC assertions prove Phase 32 remains open and all side effects were avoided.
- Korean static voice registry refusal remains intact.
- `phase-status 32 in_progress`, `session-fingerprint write`, and `git diff --check` pass.

## Success Criteria
- `production-database-env-load-authority.md` exists and captures only secret-safe `.env` parsing approval.
- `production-database-target-classification.json` exists and records `blocked_matches_disposable_test_target` with no DB connection or secret persistence.
- `32-52-SUMMARY.md` exists and clearly asks for a distinct production DB locator or explicit target reclassification before the next DB plan.
- Phase 32 remains in progress.

## High-Leverage Review
- Second pass required because this plan protects a production database boundary and secret-handling exception.
- The checker must confirm no disposable/test target can be promoted into production evidence by this plan.

## Leverage Review
- Lost: No production DB migration or job binding runs until target classification is resolved.
- Kept: Secret hygiene, previous reset/disposable boundaries, and Phase 32 non-closing posture.
- Gained: A precise blocker that prevents accidental mutation of the wrong target.

## Notes
- Approach exploration was not rerun; the existing user-confirmed `32-APPROACH.md` already covers production database authority and the no-promotion rule for disposable/fixture evidence.
- Research was not rerun; this plan uses existing SQLAlchemy URL parsing and prior Phase 32 DB evidence conventions.
- Planning-time hash-only comparison result: the current `.env` locator matches `disposable-database-preflight.json` `database_locator_sha256=19871fdb496eed265952e0358b94fa795d1b74c37d89a674b0189ee1381f32b6`.
- Worktree control-map warnings remain: canonical dirty worktree and invalid/detached `/tmp/multilang-phase31-*` sibling worktrees. Execution must touch only declared files and preserve unrelated changes.
