---
phase: 32-frequency-portuguese-text-and-audio
plan: "55"
type: execute
wave: 67
runtime: opencode
assurance: self_checked
depends_on: [32-54]
files-modified:
  - .planning/.state-fingerprint.json
  - .planning/SPEC.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-55-PLAN.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-55-SUMMARY.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py
  - tests/planning/test_phase32_production_text_smoke_recovery.py
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-authorization.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-preflight.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-invocation.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-result.json
autonomous: false
terminal_checkpoint: wait_for_user_before_3000_item_execution
requirements:
  - KFREQ-01
  - KFREQ-02
  - KFREQ-03
  - KTXT-01
  - GLEX-01
  - GLEX-02
  - GMOR-01
  - GTXT-01
  - GPRO-01
requirements_progress: partial_evidence_only
non_goals:
  - Do not create another candidate denominator, another pilot job, an intermediate pilot, or replacement candidates.
  - Do not alter or delete the Plan 32-54 authorization/preflight/result evidence or the original smoke job authority binding.
  - Do not call Azure, synthesize audio, apply review, promote, export, release, publish, deliver, commit, PR, close Phase 32, plan/execute an intermediate pilot, or execute 3000 items.
  - Do not expose secrets, database locator components, learner text, prompts/completions, provider payloads, SQL rows, or private paths.
hard_boundaries:
  - Consume the user's explicit `Autorizar retry` answer only as sidecar recovery authority for the existing `phase32-prod-text-smoke-20` job and exact original ranks 1-20.
  - Keep the original bound `KoreanFrequencyJobAuthority` unchanged. Bind recovery authority by validating and hashing the new sidecar together with the immutable Plan 32-54 result; do not overwrite the job's `pilot_authority_sha256`.
  - Protect Plan 32-54 bytes against the hashes anchored in this plan: authorization `af26bd4e93a11020de01093403f04aa9f66c9804e8aac45662d548f0c32fbdab`, preflight `3d4e1392289ba0228ae27354fcb6701e3362e095de6b1c837a53f98b468038b4`, result `d512dd36cf528eb7d6904a0e1459b44d7b4916e5895ac7cd48f5f1433be914e8`, and summary `5bad3bf992c0cd788b77334d9174bd5831e71792cdcee5d9ac27cf613a500125`.
  - Require the starting DB state to be exactly 20 candidates, `total_items=20`, zero text/audio/export rows, and exactly two external failed attempts classified `rate_limited` plus one outer `ProviderRetryError` log. Any other state blocks.
  - Wait at least the configured 60-second provider cooldown from the latest failed attempt before recovery; this is a temporal readiness gate, not proof of provider availability.
  - Retain `gpt-4.1-mini`, DeepL PT-BR, no fallback, concurrency 1, and at most 2 attempts per provider operation. The fresh approval authorizes a new bounded invocation after the prior failed invocation; it does not increase any per-operation retry loop above 2.
  - Before provider construction, atomically create one immutable recovery-invocation marker bound to the sidecar, prior-evidence hashes, historical telemetry root, and current DB-state root. Existing marker or result blocks every later execution attempt, including after process failure.
  - Resume Unit 1 with `missing_only=true`, `max_items=10`, `synthesize_audio=false`, `concurrency=1`. Unit 2 may run only after exactly 10 text records exist and Unit 1 reports exactly 10 processed items.
  - Stop on any recovered-unit failure without selecting replacements or automatic replay. Stop at the manual full-run checkpoint whether recovery passes or fails.
  - Public recovery execution must use the Plan 32-54 contained-subprocess log capture, contextual leak scan, mode-0600 temporary files, and guaranteed removal before generic output.
approval_gates:
  - Approved now: one newly checked same-job recovery invocation, at most 2 attempts per provider operation, Unit 1 first, and Unit 2 only after exactly 10 Unit-1 text records.
  - Not approved: any additional recovery invocation after this one, altered model/provider/budget, a 21st candidate, Azure/audio, review/export/release, or 3000-item execution.
  - If all 20 items complete, notify the user and stop before 3000-item execution; the only next plan may be the direct full run with no intermediate pilot.
known_unknowns:
  - Provider rate-limit readiness cannot be proven without a live call; the cooldown merely avoids immediate replay.
  - The recovery may fail again, in which case no further retry is authorized.
  - Acceptance/review distribution and content quality remain unknown until successful persistence and later human review.
second_pass_required: true
closure_claim_limit: This plan can prove only recovery or bounded re-failure of the original 20-item text smoke. It cannot satisfy a full requirement, human review, audio, export, release, delivery, full-run authority, or Phase 32 closure.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
must_haves:
  truths:
    - Recovery starts only from the exact immutable Plan 32-54 failure state and preserves its files and bound authority.
    - No provider operation receives more than two attempts in the recovery invocation, and no fallback/audio path is available.
    - Unit 2 cannot execute until Unit 1 has exactly 10 persisted text records.
    - Passing recovery produces exactly 20 text records and then stops for the user before any 3000-item execution.
  artifacts:
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-authorization.md
      provides: Fresh sidecar authority bound to the user's retry answer and immutable Plan 32-54 failure hash.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-preflight.json
      provides: Content-free exact-state and cooldown proof before recovery calls.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-invocation.json
      provides: Immutable one-shot lease proving no second recovery invocation can start.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-result.json
      provides: Sanitized recovered or re-failed unit/aggregate results without overwriting Plan 32-54 evidence.
---

# Phase 32 Plan 55: Recover The 20-Item Production Text Smoke

## Objective
Use the user's fresh retry approval to resume the same 20-item smoke after its rate-limited Unit 1 failure. Preserve all prior evidence and authority, permit no more than two attempts per provider operation, run Unit 2 only after Unit 1 reaches exactly 10 text records, and stop before any full run.

## Context
- Plan 32-54 passed offline and DB setup, then made two external attempts for the first operation; both were classified `rate_limited`. One outer `ProviderRetryError` log brought the telemetry-row count to three. No text record exists.
- The user selected `Autorizar retry`, explicitly allowing a newly checked same-job recovery and retaining the two-attempt ceiling and Unit-2 gate.
- The existing job authority must remain immutable because rebinding a new `pilot_authority_sha256` would conflict. Recovery therefore uses a separately validated sidecar authorization linked to the prior result hash.

<assurance_check>
source_artifact: .planning/phases/32-frequency-portuguese-text-and-audio/32-54-SUMMARY.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: self_checked
status: matched_after_checker
warning: None; independent gsdd-plan-checker passed before recovery calls.
</assurance_check>

## Requirements Covered
- Partial evidence only for KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, and GPRO-01. No requirement or ROADMAP criterion is completed by this recovery alone.

## Evidence Contract
- Recovery authorization records exact job, 20-item denominator, immutable prior result raw-file SHA-256, two attempts per operation, one recovery invocation, Unit-1-first gating, no fallback/audio/full-run, and the terminal checkpoint.
- Recovery authorization and every helper mode compare all four protected Plan 32-54 artifacts to the exact raw-file hashes anchored in this plan, not hashes discovered at execution time.
- Recovery preflight records hashes/counts/control classifications only: exact original authority and identity roots, 20 candidates, zero text/audio/export, prior external-attempt count 2, prior aggregate-failure count 1, cooldown satisfied, credentials-present booleans, and provider/model/policy identifiers.
- Recovery result is a new file. On pass it records two units of 10 and 20 aggregate records; on failure it records the completed-unit boundary and controlled classifications/hashes. It never copies learner/provider values.

## Stop-And-Challenge
- Do not infer success from HTTP/provider availability alone; require persisted text records and complete required telemetry hashes.
- Existing provider log rows are historical and remain counted separately from recovery invocation rows.
- Define one invocation-relative operation identity as `(item_key_hash, operation, provider, model, route_policy_sha256, budget_snapshot_sha256, cache_key_sha256, response_schema_sha256)`. Snapshot the exact historical provider-row-ID root before execution. For each new identity, external rows are retry failure rows plus at most one success row. A lone outer non-retryable failure (`quota_exceeded` or controlled permanent provider failure) with no retry row is counted explicitly as one external-attempt surrogate because current runtime persistence has no inner retry row for it. `ProviderRetryError` aggregate rows remain non-external. Reject an outer non-retryable surrogate combined with retry/success rows for the same call identity, any ambiguous aggregate combination, duplicate ordinals, more than two external attempts, missing policy hashes, unexplained rows, or a second invocation marker. Retry/success external ordinals must form one unique prefix of `[1, 2]`.
- If Unit 1 partially persists before a later item fails, preserve those rows and stop. Do not automatically resume again under this authority.
- Any need for another retry after this invocation requires another explicit user decision.

<checks>
<plan_check>
checker: gsdd-plan-checker
checker_task_id: ses_f79e381c0ffedMDkgPq06KuUOy
checker_runtime: opencode subagent
status: passed
blocking: false
notes: Independent checker passed after revisions anchoring protected hashes, exact attempt accounting, one-shot invocation state, success/refailure verification, pre-call tests, and final verifier/artifact checks.
</plan_check>
</checks>

## Tasks

<task id="32-55-01" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-authorization.md
    - MODIFY: .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py
    - CREATE: tests/planning/test_phase32_production_text_smoke_recovery.py
  </files>
  <action>
    Test-first, extend the phase-local helper with recovery self-check/preflight/execute/verify/artifact-scan modes. Validate the fresh sidecar and all four anchored Plan 32-54 raw-file hashes without rebinding DB authority. Add exact historical telemetry-shape checks, a >=60-second cooldown gate, an immutable one-shot invocation marker, exact historical provider-row-ID root, invocation-relative operation identities, retry/success/non-retryable-surrogate/aggregate row classification, unique-prefix `[1,2]` attempt validation, Unit-1/Unit-2 hard gates, valid success and bounded-refailure evidence, and the existing contained log scanner. Keep provider retry at 2 per operation and forbid audio.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --self-check-recovery`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -m py_compile .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest -q tests/planning/test_phase32_production_text_smoke_recovery.py tests/domain/test_korean_provider_policy.py tests/cli/test_korean_provider_commands.py tests/services/test_korean_provider_pilot_evidence.py` before any recovery execution.
    - Tests/self-check must reject changed bytes for each anchored Plan 32-54 artifact, changed DB authority/candidates, nonzero starting text/audio/export, wrong historical telemetry shape, cooldown under 60 seconds, duplicate/non-prefix/more-than-two external attempts per operation identity, unexplained/aggregate-row confusion, second invocation marker, Unit 2 before 10 records, evidence overwrite, and log leaks. They must accept a completely recovered result, retryable bounded refailure, and a lone non-retryable `quota_exceeded`/permanent outer failure as exactly one external-attempt surrogate; they must reject ambiguous surrogate-plus-retry/success combinations.
  </verify>
  <done>The recovery helper is offline-verified and cannot widen or erase the original smoke authority/evidence.</done>
</task>

<task id="32-55-02" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-preflight.json
  </files>
  <action>
    Run the recovery preflight without provider construction/calls. Verify the four anchored Plan 32-54 file hashes, DB job/authority/candidate/text/telemetry state, policy/settings/credentials, cooldown, and all no-audio/export/full-run boundaries. Persist only sanitized ready/blocker evidence. The preflight must prove the invocation marker and recovery result do not yet exist; only `--execute-recovery` may atomically create the marker immediately before provider construction.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --preflight-recovery`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-recovery-preflight` and require exit 0, `status=ready`, exact historical state, cooldown satisfied, and zero preflight deltas.
  </verify>
  <done>Recovery is proven ready from the exact authorized failed state without a new external call.</done>
</task>

<task id="32-55-03" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-invocation.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-result.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/32-55-SUMMARY.md
    - MODIFY: .planning/SPEC.md
    - MODIFY: .planning/.state-fingerprint.json
  </files>
  <action>
    Run the contained recovery exactly once. Atomically create the one-shot invocation marker, resume Unit 1 under the original deterministic selection and policy, and gate Unit 2 on exactly 10 Unit-1 records/result counts. Persist separate sanitized result evidence whether passed or failed. `--verify-recovery-result` must return exit 0 for either a valid `recovered` outcome or valid `bounded_refailure` outcome, while encoding the operational status in evidence. For bounded refailure require unchanged protected hashes/DB authority, exactly one invocation marker, valid per-operation attempt deltas, truthful partial text count, Unit 2 absent unless Unit 1 reached exactly 10, zero forbidden deltas, contained logs scanned/removed, and terminal checkpoint. Update planning state honestly and never auto-retry.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --execute-recovery` once.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-recovery-result` and require exit 0 for a machine-valid `recovered` or `bounded_refailure` evidence envelope.
    - After summary/SPEC/fingerprint writes, run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-recovery-artifacts` and require exit 0. This mode must rescan every Plan 32-55 artifact plus invocation log scan/removal evidence against exact secrets, locator components, contextual provider/generated-value patterns, Hangul, and unapproved private paths.
    - For either recovered or bounded-refailure outcome, launch one `gsd-verifier` subagent. Require a passing evidence-integrity/boundary verdict while allowing operational status to remain blocked, record its task ID plus `status: passed` in the summary, and machine-check the summary block before returning to the user.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "import re; from pathlib import Path; body=Path('.planning/phases/32-frequency-portuguese-text-and-audio/32-55-SUMMARY.md').read_text(encoding='utf-8'); block=body.split('<verification>',1)[1].split('</verification>',1)[0]; match=re.search(r'(?m)^task_id: (ses_[A-Za-z0-9]{12,})$', block); assert 'verifier: gsd-verifier' in block and match is not None and match.group(1) not in {'none','pending'} and 'status: passed' in block"` and require exit 0 before the terminal user report.
    - After recording and machine-checking the verifier block, rerun `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-recovery-artifacts` and require exit 0 so the final summary bytes are included in the last leak/staleness scan.
  </verify>
  <done>The one authorized recovery either proves exactly 20 bounded text records or preserves a truthful terminal blocker; no audio/export/full-run action occurs, and control returns to the user.</done>
</task>
