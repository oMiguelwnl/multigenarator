---
phase: 32-frequency-portuguese-text-and-audio
plan: "51"
type: execute
wave: 63
runtime: opencode
assurance: self_checked
depends_on: [32-50]
files-modified:
  - .planning/.state-fingerprint.json
  - .planning/SPEC.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-51-PLAN.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-51-SUMMARY.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-authority.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-locator-gate.json
autonomous: true
requirements:
  - KFREQ-01
  - KFREQ-02
  - KFREQ-03
  - KTXT-01
  - KAUD-01
  - GLEX-01
  - GLEX-02
  - GMOR-01
  - GTXT-01
  - GPRO-01
  - GAUD-01
non_goals:
  - Do not read `.env`, print or persist a raw database URL, connect to a database, run Alembic, bind a job, ingest candidates, call providers, query Azure, synthesize audio, import or apply review, export, release, publish, deliver, commit, PR, or close Phase 32.
  - Do not infer a production target from the prior disposable/test DB rehearsal or from absent environment configuration.
  - Do not create fake production DB preflight, migration, job binding, provider, audio, review, export, release, publication, or delivery evidence.
hard_boundaries:
  - The only user authority consumed is the current-session `i approve` reply, interpreted narrowly as approval to proceed through the production DB gate if a process environment `MULTILANG_DATABASE_URL` exists.
  - If `MULTILANG_DATABASE_URL` is missing from the process environment, write a blocked locator-gate artifact and stop before any DB side effect.
  - Evidence may record the variable name, status, allowed powers, forbidden powers, and sanitized hashes of authority text; it must not contain URL components, credentials, private paths, prompts, completions, provider payloads, generated Korean/Portuguese text bodies, or audio bytes.
  - Any later run that has a DB locator must still run a separate production DB preflight/migration/job-binding plan before provider, Azure, review, export, release, publication, delivery, or closure work.
escalation_triggers:
  - Stop if `MULTILANG_DATABASE_URL` is absent, blank, malformed, or only available by reading `.env`.
  - Stop if continuing would require printing a URL, reading secrets, connecting to a database, running Alembic, binding a job, or mutating any data without a visible process environment locator.
  - Stop if the approval would need to be broadened to destructive reset, provider spend, Azure synthesis, review application, export, release, publication, delivery, Git actions, or Phase 32 closure.
approval_gates:
  - The user approved production DB gate scope in this session, but no DB locator exists in the process environment; a new continuation can consume this authority only after `MULTILANG_DATABASE_URL` is present without reading `.env`.
  - Separate later approvals remain required for provider/model/budget/retry authority, Azure synthesis, text/audio review application, content promotion, APKG/CSV/TSV export, release, publication, delivery, commit, PR, and Phase 32 closure.
anti_regression_targets:
  - Phase 32 remains `[-] In progress` and must not be marked complete.
  - The prior disposable/test DB evidence remains non-production and must not be promoted into production readiness.
  - Korean remains absent from the static `audio_voice_registry`; `select_voice(SupportedLanguage.KO)` must continue to fail until a separate activation plan.
  - Missing environment configuration must fail closed before connection, migration, job binding, provider calls, Azure calls, review mutation, export, release, publication, delivery, or closure.
known_unknowns:
  - The actual production database target is unknown because `MULTILANG_DATABASE_URL` is not present in the process environment.
  - Migration state, schema state, row counts, job state, and target identity are unknown because no DB connection is authorized without the locator.
  - Full-run provider/budget, Azure synthesis, text/audio review, export, release, publication, delivery, and Phase 32 closure remain unproven.
ui_proof_slots: []
no_ui_proof_rationale: Backend/planning authority and environment gate only; no UI behavior, visual rendering, or observed Anki import/playback claim is made.
high_leverage_surfaces:
  - Production DB authority and secret-handling boundary for the next Phase 32 side-effect lane.
second_pass_required: true
closure_claim_limit: This plan can prove only that the user's production DB gate approval was captured and that execution failed closed because no process environment DB locator was available. It must not claim production DB readiness, migration, job binding, provider execution, audio, review, export, release, publication, delivery, or Phase 32 completion.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: No DB progress occurs until the database locator is available in the process environment.
  kept: Secret hygiene, fail-closed authority boundaries, and prior Phase 32 evidence remain intact.
  gained: The user's approval is captured without weakening the requirement that real DB work needs an explicit safe locator.
must_haves:
  truths:
    - The user's production DB gate approval is recorded with narrow allowed and forbidden powers.
    - The process environment locator check is recorded as blocked because `MULTILANG_DATABASE_URL` is unavailable.
    - No DB connection, migration, job binding, provider/Azure call, review application, export, release, publication, delivery, or Phase 32 closure occurs.
  artifacts:
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-authority.md
      provides: Sanitized authority record for the current-session approval and its no-secret/no-destructive boundaries.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-locator-gate.json
      provides: Sanitized fail-closed evidence that the process environment lacks `MULTILANG_DATABASE_URL`.
    - path: .planning/phases/32-frequency-portuguese-text-and-audio/32-51-SUMMARY.md
      provides: Non-closing blocked summary and next required action.
  key_links:
    - from: current-session user approval
      to: production-database-authority.md
      via: narrow approval capture for DB locator/preflight/migration/job-binding scope only.
    - from: process environment
      to: production-database-locator-gate.json
      via: presence-only `MULTILANG_DATABASE_URL` check with no value printing.
    - from: production-database-locator-gate.json
      to: next Phase 32 DB plan
      via: blocked status requiring an exported environment locator before DB connection or mutation.
---

# Phase 32 Plan 51: Production DB Approval Capture And Locator Gate

## Objective
Record the user's approval for the production database gate, then fail closed before database connection or mutation because `MULTILANG_DATABASE_URL` is not available in the process environment. This preserves forward progress without reading `.env` or fabricating production DB evidence.

## Context
- Plan 32-50 identified `production_database_authority` as the first blocking gate for the remaining Phase 32 side-effect sequence.
- The user replied `i approve`, which is sufficient to capture narrow DB-gate intent but does not itself reveal or authorize persistence of a raw database locator.
- A presence-only environment check found no `MULTILANG_DATABASE_URL` in the process environment.
- Previous disposable/test DB evidence remains non-production and cannot substitute for a production locator or production DB preflight.

<assurance_check>
source_artifact: .planning/phases/32-frequency-portuguese-text-and-audio/32-50-SUMMARY.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: self_checked
status: matched_after_checker
warning: None; gsdd-plan-checker passed in the same runtime family, so assurance remains self_checked rather than cross_runtime_checked.
</assurance_check>

## Requirements Covered
- KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, KAUD-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, GPRO-01, GAUD-01

## Must-Haves
1. Capture the user's production DB gate approval with narrow allowed powers and explicit forbidden powers.
2. Check only for presence of `MULTILANG_DATABASE_URL` in the process environment without printing or persisting the value.
3. Write blocked evidence if the locator is absent, then stop before DB connection, migration, job binding, provider/Azure calls, review, export, release, publication, delivery, or closure.
4. Keep Phase 32 in progress and make the next required action explicit.

## Anti-Goals
- No `.env` read or secret lookup outside the current process environment.
- No raw DB URL, hostname, database name, username, password, provider credential, private path, prompt, completion, provider payload, generated text, or audio bytes in artifacts.
- No DB connection, Alembic migration, job row creation, candidate ingestion, provider call, Azure call, synthesis, review application, export, release, publication, delivery, commit, PR, or Phase 32 closure.

## Hard Boundaries
- The authority artifact must not include a DB URL or any URL component.
- The locator gate must be presence-only and fail closed on missing env var.
- Existing disposable DB artifacts remain separate and non-production.
- If a locator appears after this plan starts, do not continue directly; create or execute the next DB preflight/migration plan with the locator available.

## Evidence Contract
- `production-database-authority.md` contains a JSON block with `authority_kind=production-database-gate`, `approval_captured=true`, `target_source=process_env:MULTILANG_DATABASE_URL`, allowed powers for locator presence check plus future DB preflight/migration/job-binding planning, `destructive_operations_allowed=false`, and all provider/Azure/review/export/release/publication/delivery/closure grants false.
- `production-database-locator-gate.json` contains `status=blocked_missing_database_url`, `database_url_env_present=false`, `raw_secret_read=false`, `raw_secret_persisted=false`, `database_connection_attempted=false`, `alembic_migration_attempted=false`, `job_binding_attempted=false`, and all external side-effect counts zero.
- `32-51-SUMMARY.md` records that the user approval was captured, that the DB locator is missing, that Phase 32 remains in progress, and that no external side effects occurred.

## Common Pitfalls
- Treating `i approve` as permission to read `.env` or invent a target.
- Treating a missing env var as a reason to reuse the disposable/test target.
- Proceeding to provider/Azure spend before a production DB locator, migration state, and job binding exist.
- Recording secret material while trying to prove that a secret exists.

## Stop-And-Challenge
- Stop if `MULTILANG_DATABASE_URL` is missing from the process environment.
- Stop if the only way to get the locator is reading `.env` or printing/persisting a secret.
- Stop if any command would connect to DB, run Alembic, bind a job, call providers/Azure, mutate review, export, release, publish, deliver, commit, or close Phase 32.

## Approval Gates
- The current approval is consumed only for the production DB gate intent and presence check.
- A later continuation may proceed to DB preflight/migration/job binding only after `MULTILANG_DATABASE_URL` is available in the process environment and a plan covers the exact non-secret evidence to record.
- Provider, Azure, review, export, release, publication, delivery, Git, and closure gates remain separate.

<checks>
<plan_check>
checker: gsdd-plan-checker
checker_task_id: ses_f7ebc7460ffeyY5t8uP4jCcNhK
checker_runtime: opencode subagent
status: passed
blocking: false
notes: Independent checker confirmed Plan 32-51 is a narrow, runnable, fail-closed DB locator gate that preserves Phase 32 boundaries without overclaiming production readiness or closure.
</plan_check>
</checks>

## Tasks

<task id="32-51-01" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-authority.md
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-locator-gate.json
  </files>
  <action>
    Write the sanitized production DB gate authority from the current-session user approval, then write the blocked locator-gate JSON because `MULTILANG_DATABASE_URL` is absent from the process environment. Do not read `.env`, connect to a database, run Alembic, bind a job, or create any provider/audio/review/export/release/delivery authority.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "import json,os,pathlib,re; root=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox'); auth=(root/'production-database-authority.md').read_text(encoding='utf-8'); gate=json.loads((root/'production-database-locator-gate.json').read_text(encoding='utf-8')); assert gate['status']=='blocked_missing_database_url'; assert gate['database_url_env_present'] is False; assert os.environ.get('MULTILANG_DATABASE_URL') is None; assert gate['raw_secret_read'] is False; assert gate['raw_secret_persisted'] is False; assert gate['database_connection_attempted'] is False; assert gate['alembic_migration_attempted'] is False; assert gate['job_binding_attempted'] is False; assert 'production-database-gate' in auth; assert 'process_env:MULTILANG_DATABASE_URL' in auth; assert 'destructive_operations_allowed' in auth and 'false' in auth.lower(); banned=['postgresql://','postgres://','sk-','BEGIN PRIVATE','audio_bytes','provider_payload']; body=auth+'\n'+json.dumps(gate,sort_keys=True); assert not any(token in body for token in banned); assert not re.search(r'(?i)password|api[_-]?key|secret\s*[:=]', body)"`
  </verify>
  <done>
    The user's approval is recorded, the missing environment locator is documented, and execution has stopped before all DB/provider/audio/review/export side effects.
  </done>
</task>

<task id="32-51-02" type="auto">
  <files>
    - MODIFY: .planning/SPEC.md
    - MODIFY: .planning/.state-fingerprint.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/32-51-SUMMARY.md
  </files>
  <action>
    Update `.planning/SPEC.md` Current State to record Plan 32-51 as a blocked locator-gate result, keep Phase 32 in progress, refresh the session fingerprint, and write a non-closing summary. The summary must state that no DB connection, migration, job binding, provider/Azure call, audio synthesis, review application, export, release, publication, delivery, Git action, or phase closure occurred.
  </action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -c "from pathlib import Path; import json; phase=Path('.planning/phases/32-frequency-portuguese-text-and-audio'); summary=(phase/'32-51-SUMMARY.md').read_text(encoding='utf-8'); gate=json.loads((phase/'evidence-inbox/production-database-locator-gate.json').read_text(encoding='utf-8')); spec=Path('.planning/SPEC.md').read_text(encoding='utf-8'); assert gate['status']=='blocked_missing_database_url'; assert 'Plan 32-51' in spec; assert 'blocked_missing_database_url' in spec; assert 'Phase 32 remains in progress' in summary; assert 'No database connection' in summary; assert 'No Alembic migration' in summary; assert 'No job binding' in summary; assert 'No provider call' in summary; assert 'No Azure call' in summary; assert 'No audio synthesis' in summary; assert 'No review application' in summary; assert 'No export' in summary; assert 'No release, publication, or delivery' in summary"`
    - Run `node .planning/bin/gsdd.mjs phase-status 32 in_progress`
    - Run `node .planning/bin/gsdd.mjs session-fingerprint write`
    - Run `git diff --check -- .planning/.state-fingerprint.json .planning/SPEC.md .planning/phases/32-frequency-portuguese-text-and-audio/32-51-PLAN.md .planning/phases/32-frequency-portuguese-text-and-audio/32-51-SUMMARY.md .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-authority.md .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-database-locator-gate.json`
  </verify>
  <done>
    SPEC, phase status, fingerprint, and summary all report the same fail-closed locator blocker without claiming Phase 32 completion.
  </done>
</task>

## Verification
- Authority/gate assertions prove the approval was captured and the environment locator is missing without reading or persisting a secret.
- Summary/SPEC assertions prove Phase 32 remains open and all side effects were avoided.
- `phase-status 32 in_progress` keeps ROADMAP unchanged/open.
- `session-fingerprint write` records the new planning state.
- `git diff --check` guards whitespace on all Plan 32-51 outputs.

## Success Criteria
- `production-database-authority.md` exists and captures narrow production DB gate approval.
- `production-database-locator-gate.json` exists and records `blocked_missing_database_url` with no secret or DB side effects.
- `32-51-SUMMARY.md` exists and clearly asks for `MULTILANG_DATABASE_URL` to be exported in the process environment before the next DB plan.
- Phase 32 remains in progress.

## High-Leverage Review
- Second pass required because this plan sits at the production DB authority boundary. The checker must confirm it fails closed and does not convert approval into secret reads or database mutation.

## Leverage Review
- Lost: No DB migration or job binding can run in this environment until the locator is present.
- Kept: The approved DB-gate intent, prior Phase 32 evidence, and strict no-secret/no-side-effect boundaries.
- Gained: A reusable blocked authority record that makes the next continuation precise.

## Notes
- Approach exploration is skipped because `32-APPROACH.md` already has `alignment_status: user_confirmed` and this plan only captures a narrow authority continuation plus fail-closed environment gate.
- Research is skipped because this plan uses existing DB authority boundaries and does not introduce a new DB implementation pattern.
- Control-map warnings remain: canonical worktree is dirty with current Phase 32 outputs and pre-existing Phase 33 `33-12-PLAN.md`; invalid `/tmp/multilang-phase31-*` worktrees are not used.
