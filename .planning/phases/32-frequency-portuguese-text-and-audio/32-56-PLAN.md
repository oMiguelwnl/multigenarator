---
phase: 32-frequency-portuguese-text-and-audio
plan: "56"
type: execute
wave: 68
runtime: opencode
assurance: self_checked
depends_on: [32-55]
files-modified:
  - .planning/SPEC.md
  - .planning/.state-fingerprint.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-56-PLAN.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/32-56-SUMMARY.md
  - .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py
  - tests/planning/test_phase32_production_text_smoke_recovery.py
autonomous: true
requirements_progress: partial_evidence_only
non_goals:
  - Do not call providers, query or mutate the production DB, remove/reuse the one-shot recovery marker, change candidates, call Azure/audio, export, review, release, commit, or execute any retry/full-run.
  - Do not alter protected Plan 32-54 evidence, Plan 32-55 recovery evidence, or the original job authority.
hard_boundaries:
  - This is an offline helper regression fix only: `build_runtime()` must return the constructed runtime service and must preserve the forbidden audio adapter path.
  - Prove the bug with a failing test before editing the helper.
  - Do not convert the consumed Plan 32-55 marker into retry authorization; future paid execution remains separately gated.
closure_claim_limit: This plan can only repair and verify the local helper return-path bug. It cannot recover the smoke, satisfy requirements, or authorize another provider call.
---

# Phase 32 Plan 56: Offline Fix For Recovery Runtime Construction

## Objective
Fix the local helper bug found by independent verification: `build_runtime()` currently returns `None`, so recovery consumes its one-shot marker before provider construction. Add a regression test first, make the minimal return-path fix, and verify offline only.

## Tasks

<task id="32-56-01" type="auto">
  <files>
    - MODIFY: tests/planning/test_phase32_production_text_smoke_recovery.py
  </files>
  <action>Add a regression test that monkeypatches `build_korean_frequency_text_runtime_service`, calls `build_runtime()`, and asserts the sentinel runtime service is returned with the expected job authority and forbidden audio adapter runtime builder.</action>
  <verify>Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest -q tests/planning/test_phase32_production_text_smoke_recovery.py::test_build_runtime_returns_constructed_service_with_forbidden_audio_adapter` and require it to fail because `build_runtime()` returns `None`.</verify>
  <done>The new test exists, fails for the expected missing-return reason before helper edits, and no helper/evidence/DB/provider state is changed in this task.</done>
</task>

<task id="32-56-02" type="auto">
  <files>
    - MODIFY: .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py
  </files>
  <action>Move the existing `build_korean_frequency_text_runtime_service(...)` return into `build_runtime()` and remove the unreachable block after `classify_recovery_telemetry()`. Do not change recovery marker, provider, DB, evidence, or retry behavior.</action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest -q tests/planning/test_phase32_production_text_smoke_recovery.py::test_build_runtime_returns_constructed_service_with_forbidden_audio_adapter`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest -q tests/planning/test_phase32_production_text_smoke_recovery.py`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --self-check-recovery`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python -m py_compile .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-recovery-artifacts`.
    - Do not run `--execute`, `--execute-worker`, `--execute-recovery`, `--execute-recovery-worker`, provider calls, DB mutation, retry, or full-run modes.
  </verify>
  <done>The focused and full recovery tests pass, self-check/artifact verification/compile pass, `build_runtime()` returns the constructed service, and no marker/evidence/provider/DB/retry/full-run side effect occurs.</done>
</task>

<task id="32-56-03" type="auto">
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/32-56-SUMMARY.md
    - MODIFY: .planning/SPEC.md
    - MODIFY: .planning/.state-fingerprint.json
  </files>
  <action>Record the offline fix and keep all paid retry/full-run gates blocked. Update SPEC truthfully and refresh fingerprint without touching unrelated Phase 33 changes.</action>
  <verify>
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python - <<'PY'
from hashlib import sha256
from pathlib import Path
expected = {
    '.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-authorization.md': '44b338c71decfab25ec4bf46374a6d28fdc188f85d49730eb1f963dacdcdf330',
    '.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-preflight.json': '01e2fc361dd979b6c1b714f9059613fb8821ca1fa5cec0fab22e08e85b44e09b',
    '.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-invocation.json': '5f528c474a131d1d68b2947fd12390a7a84f9e68f0ca6cb3abcbfd608726924f',
    '.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-recovery-result.json': '19492546b469d783f891bd3fffea1dc204dc412e554937aabe44f80d43ff8ba8',
    '.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-authorization.md': 'af26bd4e93a11020de01093403f04aa9f66c9804e8aac45662d548f0c32fbdab',
    '.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-preflight.json': '3d4e1392289ba0228ae27354fcb6701e3362e095de6b1c837a53f98b468038b4',
    '.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/production-text-smoke-20-result.json': 'd512dd36cf528eb7d6904a0e1459b44d7b4916e5895ac7cd48f5f1433be914e8',
    '.planning/phases/32-frequency-portuguese-text-and-audio/32-54-SUMMARY.md': '5bad3bf992c0cd788b77334d9174bd5831e71792cdcee5d9ac27cf613a500125',
}
for path, digest in expected.items():
    assert sha256(Path(path).read_bytes()).hexdigest() == digest, path
PY`.
    - Run `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync python .planning/phases/32-frequency-portuguese-text-and-audio/tools/production_text_smoke_20.py --verify-recovery-artifacts` after summary/SPEC/fingerprint writes.
    - Run `git diff --check`.
  </verify>
  <done>Summary/SPEC/fingerprint record only the offline helper fix; evidence/marker hashes match the pre-plan constants; artifact scan and whitespace checks pass; no provider calls, DB mutations, marker changes, or evidence overwrites occurred.</done>
</task>
