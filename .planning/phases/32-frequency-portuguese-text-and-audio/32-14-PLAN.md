---
phase: 32-frequency-portuguese-text-and-audio
plan: "14"
type: execute
wave: 26
runtime: opencode
assurance: self_checked
depends_on: [32-13]
files-modified:
  - tests/services/test_korean_production_evidence.py
  - tests/cli/test_korean_production_evidence_commands.py
  - src/multilang/services/korean_production_evidence.py
  - src/multilang/cli.py
autonomous: true
requirements: [KFREQ-01, KFREQ-02, KFREQ-03, KTXT-01, KAUD-01, GLEX-01, GLEX-02, GMOR-01, GTXT-01, GPRO-01, GAUD-01]
non_goals: ["No generation, synthesis, review import/application, DB mutation, APKG build, release, delivery, or live call."]
no_ui_proof_rationale: "Read-only production evidence contracts only."
second_pass_required: true
closure_claim_limit: "Proves disposable fake-data run/final reconciliation and mutation refusal only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Pre-review validation recomputes full CAS, 3000 exact text histories, 6000 pending assets, budgets/denominators, frozen prefixes and zero fallback."
    - "Final validation additionally requires exact review aggregates/applications, content-promotion authority and APKG/report structure/media."
    - "Every required authority/path/count is explicit and every one-fact false closure fails read-only with content-free errors."
  artifacts:
    - {path: src/multilang/services/korean_production_evidence.py, provides: "Run and final production reconcilers"}
    - {path: tests/services/test_korean_production_evidence.py, provides: "Complete fake 3000/6000 mutation suite"}
  key_links:
    - {from: "actual database and exact files", to: "production evidence", via: "full row/file/authority recomputation"}
---

# Phase 32 Plan 14: Read-Only Production Run and Final Evidence

<objective>
Implement strict pre-review and final production reconcilers, exact CLI contracts and exhaustive one-fact mutation coverage.

Purpose: Production closure must come from actual DB/files rather than report labels, with no import or mutation ownership in this tooling.
Output: Run/final validator service and commands.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-13-PLAN.md
@src/multilang/repositories/job_repository.py
@src/multilang/repositories/provider_call_log_repository.py
@src/multilang/repositories/audio_repository.py
<interfaces>
Create `validate-korean-production-run-result` and `validate-korean-production-evidence`. Both require explicit DB and genuine Phase 31/source/build/final-bundle/provider-policy/provider-review/full-run/catalog/profile/heard/full-binding inputs; final mode additionally requires content-promotion authority, review aggregate/application receipts, APKG, exact counts and JSON/Markdown audit outputs.
</interfaces>
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-14-01" type="auto" tdd="true">
  <name>Implement row-derived production run reconciliation before review</name>
  <files>
    - CREATE: tests/services/test_korean_production_evidence.py
    - CREATE: src/multilang/services/korean_production_evidence.py
  </files>
  <behavior>
    - Full CAS over genuine Phase 31, bundle/catalog locators, source/build/final and distinct provider/profile/heard authorities precedes every attempt.
    - Exactly 3000 histories contain two initial candidates and no more than one distinct repair; exactly 3000 word and 3000 sentence assets are current pending-review.
    - Prefixes, routes, retries, cache, requests/responses, token/cost/latency/failure denominators and zero fallback all recompute.
  </behavior>
  <action>Create strict safe result models and read-only service. Descriptor-rehash every exact authority/file, run genuine verify-active and reload all rows. Prove pre-constructor ordering, 1000/1000/1000 identities, all histories/hard-gate/adaptive evidence, provider operations/budgets/denominators, 6000 request/byte/profile records, pending-review state, zero fallback and before/after invariance. It cannot import/apply reviews or trust report totals.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_production_evidence.py -k 'run_result or histories or prefixes or attempts or denominators or pending_audio' -q</automated></verify>
  <done>Strict read-only pre-review evidence proves or rejects exact machine production.</done>
</task>

<task id="32-14-02" type="auto" tdd="true">
  <name>Expose complete run and final production validator commands</name>
  <files>
    - CREATE: tests/cli/test_korean_production_evidence_commands.py
    - MODIFY: src/multilang/services/korean_production_evidence.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Run mode requires explicit DB plus text/audio results; final mode requires DB plus generation report.
    - Every authority, sidecar, root, hash, review/application, APKG, expected count and output argument is explicit; no settings/path defaults or invented Phase 31 authority are accepted.
    - Final JSON evidence and JSON/Markdown audits derive from one recomputed object, not report labels.
  </behavior>
  <action>Implement exact command interfaces with mandatory genuine Phase 31 tuple, source-access/retrieval/transformation/build/final-bundle chain, bundle/manifest hashes, policy/provider review/full-run/catalog/profile/heard/full binding; final mode additionally requires content-promotion authority/sidecar, fixed final review aggregate, text/audio application receipts and APKG. Open all inputs read-only, atomically replace only declared outputs, and reject omitted/defaulted paths.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_production_evidence_commands.py -k 'required_flags or run_result or final_result or no_defaults or read_only' -q</automated></verify>
  <done>External plans can invoke exact non-guessed read-only run/final validators.</done>
</task>

<task id="32-14-03" type="auto" tdd="true">
  <name>Complete production and APKG one-fact mutation coverage</name>
  <files>
    - MODIFY: tests/services/test_korean_production_evidence.py
    - MODIFY: tests/cli/test_korean_production_evidence_commands.py
    - MODIFY: src/multilang/services/korean_production_evidence.py
  </files>
  <behavior>
    - Final mode requires 3000 reviewed text records, 6000 current reviewed assets and parent plus three exact 1000-note children with all media/schema/GUID evidence.
    - Any changed/missing/equalized authority, locator, rank, history, denominator, prefix, review, asset, APKG ID/GUID/media/count or report field fails.
    - DB, inputs and prior outputs remain unchanged under every mutation.
  </behavior>
  <action>Build a disposable 3000-row/6000-asset fixture and mutate one fact at a time across Phase 31, source/build/final authority, root/catalog locator, CAS, candidate/cache/fallback/budget/denominator/prefix, review aggregate/application, audio request/bytes/profile/review, APKG hierarchy/count/ID/GUID/field/media and report totals. Snapshot DB/input trees/files/output prestate and require content-free controlled errors with no mutation.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_production_evidence.py -k 'final_evidence or mutation or apkg or review or read_only' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_production_evidence_commands.py -k 'mutation or content_free' -q</automated></verify>
  <done>Every one-fact false production claim fails read-only and preserves prior bytes.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01..SC-10 | Complete row-derived run/final reconciliation | COVERED |
| REQ | All 11 Phase 32 IDs | Every final evidence family | COVERED |
| RESEARCH | Closure validator | Recompute DB/authority/review/APKG rather than trust labels | COVERED |
| CONTEXT | Exact counts, genuine Phase 31, no fallback, Phase 34 claim boundary | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-14-01 | Spoofing / Repudiation | reports | mitigate | Reload DB/files and recompute all roots/counts. |
| T-32-14-02 | Tampering | authority/APKG | mitigate | Exact rehash plus one-fact mutation suite. |
| T-32-14-03 | Information Disclosure | outputs/errors | mitigate | Content-free hashes/counts/codes only. |
</threat_model>
<high_leverage_review>Second pass: prove every aggregate is recomputed, all authorities remain distinct, no review import/mutation path exists, and all input bytes stay unchanged.</high_leverage_review>
<verification>Run all three focused commands; inspect services for no DB mutation, import, package, release or delivery implementation.</verification>
<success_criteria>
- Run validation exists before review.
- Final validation requires every exact authority/review/APKG fact.
- False closure mutations fail read-only and privately.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-14-SUMMARY.md` with command contracts, row/APKG matrix, mutations, invariance proof and bounded self-checked claim.</output>
