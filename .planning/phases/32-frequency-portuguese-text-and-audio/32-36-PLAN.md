---
phase: 32-frequency-portuguese-text-and-audio
plan: "36"
type: execute
wave: 48
runtime: opencode
assurance: self_checked
depends_on: [32-35]
files-modified:
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-preflight.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-role-registry.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-role-registry.json.sha256
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-progress.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-text-review-input/manifest.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-text-review-input/
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-audio-review-input/manifest.json
  - .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-audio-review-input/
autonomous: false
requirements: [KFREQ-03, KTXT-01, KAUD-01, GTXT-01, GPRO-01, GAUD-01]
non_goals: ["No review import/application, DB mutation, generation, repair, synthesis, promotion authority, APKG build, release, delivery, or publication."]
no_ui_proof_rationale: "Qualified final review of changed exact outputs or no-change continuity only."
second_pass_required: true
closure_claim_limit: "Collects final decisions for changed identities and continuity for unchanged evidence; no aggregate/promotion authority exists, and fresh external recheck remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Preflight proves the exact changed text/audio sets and unchanged review continuity from immutable hashes."
    - "Every remediated text and newly synthesized audio byte receives qualified exact review; unchanged identities are not silently reinterpreted."
    - "No-change path records exact continuity without fabricating new reviewer decisions."
    - "Changed-output review uses a private pseudonymous qualified role registry and CAS progress, with at-most-100 sessions resumable only through continue-review."
  artifacts:
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-preflight.json, provides: "Exact changed/unchanged review populations"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-text-review-input/manifest.json, provides: "Changed-text or no-change review index"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-audio-review-input/manifest.json, provides: "Changed-audio or no-change review index"}
    - {path: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-progress.json, provides: "CAS final-review resume cursor and completion proof"}
  key_links:
    - {from: "post-remediation exact changed sets", to: "final human decisions", via: "hash-bound bounded batches and unchanged-ledger continuity"}
---

# Phase 32 Plan 36: Final Changed-Output Review Checkpoint

<objective>
Require qualified final review for every remediated text/audio identity while preserving exact continuity for unchanged approved evidence.

Purpose: Changed content cannot inherit old approval, and unchanged evidence should not be duplicated or fabricated.
Output: Changed/unchanged preflight and private final review inputs.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-35-PLAN.md
@.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/post-remediation-run-evidence.json
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-36-01" type="auto">
  <name>Produce exact read-only changed/unchanged final-review preflight</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-preflight.json
  </files>
  <action>Rerun post-remediation and initial review-ledger validation read-only. Compute exact changed text candidate hashes and changed audio request/artifact hashes; prove every unchanged current identity still equals its effective initial ledger binding. Derive required final text/audio review populations and heard requirements for every newly synthesized byte. Emit only hashes/counts/controlled classes and an exact initial-aggregate continuity root; preserve DB/files.</action>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-preflight.json').read_text()); assert d['status']=='ready_for_final_review' and d['changed_text_count']+d['unchanged_text_count']==3000 and d['changed_word_count']+d['unchanged_word_count']==3000 and d['changed_sentence_count']+d['unchanged_sentence_count']==3000 and d['unchanged_receipts_still_exact'] is True and d['inputs_unchanged'] is True"</automated></verify>
  <done>Final reviewers receive exact changed sets and proven unchanged continuity without mutation.</done>
</task>

<task id="32-36-02" type="checkpoint:review" gate="blocking">
  <name>Review every changed output or confirm exact no-change continuity</name>
  <files>
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-role-registry.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-role-registry.json.sha256
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-review-progress.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-text-review-input/manifest.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-text-review-input/
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-audio-review-input/manifest.json
    - CREATE: .planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox/final-audio-review-input/
  </files>
  <action>Perform only the private changed-item review or exact no-change continuity artifact creation below. Create a private PII-free pseudonymous registry for qualified Korean-linguist, pt-BR-editor and audio-listener roles with qualification/conflict hashes and detached sidecar. Every final text/audio manifest must bind that registry, the exact remediation-result SHA-256 and initial-aggregate continuity root from preflight. Initialize and CAS-update `final-review-progress.json` after every at-most-100 decision session. `continue-review` must rehash registry/manifests/progress/current outputs, present only the next incomplete disjoint batch, reject drift and make exact replay no-write. Do not import/apply decisions, mutate DB/content/audio, run another repair/provider/synthesis call, grant promotion, or expose private content/notes/PII.</action>
  <what-built>The bounded remediation path produced exact changed sets, or a canonical no-remediation result; no changed output has final approval.</what-built>
  <how-to-verify>
    1. If text changed, qualified Korean/pt-BR reviewers inspect every exact new candidate hash against source sense/register/adaptive/context/translation policy and record a new decision. Never reuse the old decision for changed text.
    2. If audio changed, verify 100% integrity and have qualified reviewers play every newly synthesized word/sentence byte, recording decisions bound to exact request/artifact/profile hashes. Never reuse old heard evidence for changed bytes.
     3. Store changed decisions in disjoint private batches and reviewer sessions of at most 100, bind pseudonymous qualified role IDs/registry hash, and CAS-update progress. If no identities changed, require remediation status `not_required` and create zero-batch manifests/progress binding its exact result hash plus the exact initial aggregate continuity root; do not invent decisions.
     4. Do not edit content, audio, policy, routes, profile, DB or prior ledger entries. Any rejection remains a rejection and blocks final promotion rather than opening another remediation cycle.
  </how-to-verify>
  <verify><automated>.planning/.local/phase32-py312/bin/python -c "import hashlib,json,pathlib; r=pathlib.Path('.planning/phases/32-frequency-portuguese-text-and-audio/evidence-inbox'); p=json.loads((r/'final-review-preflight.json').read_text()); m=json.loads((r/'remediation-result.json').read_text()); t=json.loads((r/'final-text-review-input/manifest.json').read_text()); a=json.loads((r/'final-audio-review-input/manifest.json').read_text()); q=json.loads((r/'final-review-role-registry.json').read_text()); z=json.loads((r/'final-review-progress.json').read_text()); assert hashlib.sha256((r/'final-review-role-registry.json').read_bytes()).hexdigest()==(r/'final-review-role-registry.json.sha256').read_text().split()[0] and q['contains_pii'] is False and {'korean_linguist','pt_br_editor','audio_listener'}&lt;=set(q['qualified_role_classes']); assert t['item_count']==p['changed_text_count'] and a['word_item_count']==p['changed_word_count'] and a['sentence_item_count']==p['changed_sentence_count']; assert t['remediation_result_sha256']==a['remediation_result_sha256']==p['remediation_result_sha256'] and t['initial_aggregate_continuity_root']==a['initial_aggregate_continuity_root']==p['initial_aggregate_continuity_root'] and t['role_registry_sha256']==a['role_registry_sha256']==z['role_registry_sha256']; assert all(1&lt;=x['item_count']&lt;=100 for x in t['batches']+a['batches']) and z['max_session_decisions']==100 and z['status']=='complete'; assert (m['status']=='not_required' and t['batch_count']==a['batch_count']==0 and z['completed_decision_count']==0) if p['changed_text_count']+p['changed_word_count']+p['changed_sentence_count']==0 else True"</automated></verify>
  <done>Every changed current hash receives CAS-resumable qualified pseudonymous review, or exact no-change continuity is recorded without fabricated decisions.</done>
  <resume-signal>Type `continue-review` to resume the next CAS-bound session, `final-review-complete` only after progress is complete, or report a blocker without content, notes or PII.</resume-signal>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-04 / SC-05 / SC-08 / SC-10 | Final exact review after optional remediation | COVERED |
| REQ | KFREQ-03 / KTXT-01 / KAUD-01 / GTXT-01 / GPRO-01 / GAUD-01 | Changed-output reapproval and continuity | COVERED |
| RESEARCH | Review hashes invalidate on drift | New decisions for changed content/bytes only | COVERED |
| CONTEXT | One remediation cycle; exact current review before promotion | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-36-01 | Spoofing / Repudiation | review reuse | mitigate | Changed hashes require new decisions; unchanged hashes prove continuity. |
| T-32-36-02 | Information Disclosure | final private review | mitigate | Bounded private batches; public preflight hash/count-only. |
</threat_model>
<high_leverage_review>Second pass: prove changed-set completeness, no stale review reuse, every new byte heard, exact unchanged continuity and no second remediation.</high_leverage_review>
<verification>Run both checks only; no importer/application/generation command is allowed.</verification>
<success_criteria>
- Every changed text/audio identity receives new exact review.
- Unchanged evidence continuity is mechanically proven.
- Rejections block instead of opening another repair cycle.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-36-SUMMARY.md` with safe changed/unchanged counts/roots, review status and bounded claim.</output>
