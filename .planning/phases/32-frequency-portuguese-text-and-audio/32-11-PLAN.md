---
phase: 32-frequency-portuguese-text-and-audio
plan: "11"
type: execute
wave: 23
runtime: opencode
assurance: self_checked
depends_on: [32-09, 32-10]
files-modified:
  - tests/services/test_anki_id_registry.py
  - tests/services/test_japanese_kana_deck.py
  - tests/services/test_japanese_kana_generated_deck.py
  - tests/services/test_japanese_frequency_deck.py
  - tests/services/test_korean_foundation_export.py
  - tests/cli/test_export_command.py
  - src/multilang/services/japanese_kana_deck.py
  - src/multilang/services/japanese_kana_generated_deck.py
  - src/multilang/services/japanese_frequency_deck.py
  - src/multilang/services/korean_foundation_export.py
  - src/multilang/services/anki_id_registry.py
  - src/multilang/runtime.py
  - src/multilang/cli.py
autonomous: true
requirements: [KFREQ-02]
non_goals: ["No Korean frequency packaging, production output, changed numeric ID/GUID, or generalized export behavior."]
no_ui_proof_rationale: "Remaining ID migrations and fail-before-write registry guard only."
second_pass_required: true
closure_claim_limit: "Proves remaining registry migration and prewrite refusal only; execution evidence and fresh external recheck remain pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "Japanese frequency/kana and Korean foundation families use typed registry keys with unchanged identities."
    - "Korean foundation and Korean frequency registrations remain distinct and collision-free."
    - "Repository-wide source/config/data scanning and runtime preflight reject hidden numeric declarations/dynamics before any package/temp/destination write."
  artifacts:
    - {path: src/multilang/services/anki_id_registry.py, provides: "Complete scanner and prewrite guard"}
  key_links:
    - {from: "production roots", to: "package constructors", via: "scanner plus runtime registry guard before writes"}
---

# Phase 32 Plan 11: Remaining ID Families and Repository Prewrite Guard

<objective>
Migrate Japanese and Korean foundation ID consumers, then enforce the canonical registry across all production roots before output creation.

Purpose: Hidden literals or dynamic IDs must not bypass the atomic registry contract or collide with Korean frequency decks.
Output: Remaining family migrations, scanner CLI and prewrite runtime guard.
</objective>
<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-10-PLAN.md
@src/multilang/services/korean_foundation_export.py
</context>
<checks><plan_check>
checker: self
checker_runtime: opencode
status: passed
blocking: false
notes: planner self-check only; fresh external recheck pending
</plan_check></checks>

<tasks>
<task id="32-11-01" type="auto" tdd="true">
  <name>Migrate Japanese frequency and kana consumers without identity drift</name>
  <files>
    - MODIFY: tests/services/test_anki_id_registry.py
    - MODIFY: tests/services/test_japanese_kana_deck.py
    - MODIFY: tests/services/test_japanese_kana_generated_deck.py
    - MODIFY: tests/services/test_japanese_frequency_deck.py
    - MODIFY: src/multilang/services/japanese_kana_deck.py
    - MODIFY: src/multilang/services/japanese_kana_generated_deck.py
    - MODIFY: src/multilang/services/japanese_frequency_deck.py
  </files>
  <behavior>
    - Japanese frequency and both kana paths resolve registered keys.
    - Current IDs, names, hierarchy, fields, tags, media and GUID snapshots remain unchanged.
    - Raw integer parameters and local numeric declarations are rejected.
  </behavior>
  <action>Migrate Japanese constructors and helpers to typed registry resolution while preserving every golden. Split by family inside the task's test-first cycle and run registry collision checks after each conversion; do not alter topology or current values.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_japanese_kana_deck.py tests/services/test_japanese_kana_generated_deck.py tests/services/test_japanese_frequency_deck.py -k 'id or registry or guid' -q</automated></verify>
  <done>All Japanese deck families use the canonical registry with unchanged identity.</done>
</task>

<task id="32-11-02" type="auto" tdd="true">
  <name>Migrate Korean foundation consumers and preserve Phase 31 identity</name>
  <files>
    - MODIFY: tests/services/test_anki_id_registry.py
    - MODIFY: tests/services/test_korean_foundation_export.py
    - MODIFY: src/multilang/services/korean_foundation_export.py
  </files>
  <behavior>
    - Korean foundation uses typed registry keys with exact current IDs/GUIDs/fields/tags/media/hierarchy.
    - Foundation and frequency registrations remain distinct.
    - No foundation media or approval semantics change.
  </behavior>
  <action>Migrate the Phase 31 foundation exporter/helpers to registry resolution, preserve every existing golden, reject raw integer input and prove the new Korean frequency reservations do not alter or collide with foundation IDs.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_foundation_export.py -k 'id or registry or guid or collision' -q</automated></verify>
  <done>Korean foundation identity and media behavior remain unchanged under the shared registry.</done>
</task>

<task id="32-11-03" type="auto" tdd="true">
  <name>Enforce repository-wide registry scanning and prewrite runtime refusal</name>
  <files>
    - MODIFY: tests/services/test_anki_id_registry.py
    - MODIFY: tests/cli/test_export_command.py
    - MODIFY: src/multilang/services/anki_id_registry.py
    - MODIFY: src/multilang/runtime.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Scanner covers src/multilang, scripts, data and assets while excluding tests/planning/build/private roots.
    - Direct literals, unknown keys, aliases, unchecked dynamics, duplicates, cross-kind conflicts and unused unreserved registrations fail.
    - Failure precedes package construction, temp files and destination mutation.
  </behavior>
  <action>Implement deterministic Python AST and JSON/YAML/TOML/CSV key scanning plus `check-anki-id-registry --production-roots`. Inspect assignments, annotations, exports, aliases, mappings, constructor arguments, wrappers and dynamic flows. Wire runtime/export preflight before any write. Inject each violation and assert sentinel destination bytes and temp directory remain unchanged.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_anki_id_registry.py -k 'scanner or literal or config or dynamic or collision or reserved or prewrite' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_export_command.py -k 'registry or collision or output' -q</automated></verify>
  <done>Every production ID declaration/use resolves through the registry and violations fail before writes.</done>
</task>
</tasks>
<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-02 | Stable collision-free child deck IDs | COVERED |
| REQ | KFREQ-02 | Complete registry migration and prewrite guard | COVERED |
| RESEARCH | ID/GUID collision safety | Family split plus repository scanner | COVERED |
| CONTEXT | Preserve Phase 31/current IDs and stop on conflict | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-11-01 | Tampering | family migration | mitigate | Golden IDs/GUIDs and intermediate family tests. |
| T-32-11-02 | Spoofing / Integrity | hidden IDs | mitigate | AST/config/data scanner and fail-before-write runtime guard. |
</threat_model>
<high_leverage_review>Second pass: compare Japanese/foundation goldens, scan dynamic aliases/config data, and prove guard ordering before every package write.</high_leverage_review>
<verification>Run all three task commands and `UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync multilang check-anki-id-registry --production-roots`.</verification>
<success_criteria>
- All remaining production families use the registry.
- Phase 31 identity remains intact.
- Hidden/conflicting IDs stop before writes.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-11-SUMMARY.md` with migration goldens, scanner inventory, injected violations and bounded self-checked claim.</output>
