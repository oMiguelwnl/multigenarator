---
phase: 32-frequency-portuguese-text-and-audio
plan: "02"
type: execute
wave: 14
runtime: opencode
assurance: self_checked
depends_on: [32-01]
files-modified:
  - tests/services/test_korean_frequency.py
  - tests/cli/test_korean_frequency_build_commands.py
  - tests/scripts/test_build_frequency_assets.py
  - tests/services/test_korean_source_review.py
  - tests/cli/test_korean_source_review_commands.py
  - tests/test_migration_schema_parity.py
  - src/multilang/services/korean_frequency.py
  - src/multilang/services/korean_source_review.py
  - scripts/build_frequency_assets.py
  - src/multilang/cli.py
  - alembic/versions/20260821_18_frequency_text_audio_evidence.py
  - src/multilang/db/models.py
  - src/multilang/domain/korean.py
autonomous: true
requirements: [KFREQ-01, GLEX-01, GLEX-02, GPRO-01, KAUD-01, GAUD-01]
non_goals:
  - "No real source transformation, production migration, pointer activation, provider call, or reviewer/legal inference."
no_ui_proof_rationale: "Offline immutable builder, review receipts, and persistence schema only."
second_pass_required: true
closure_claim_limit: "Proves synthetic interruption, bounded review, CLI wiring, and disposable migration behavior only; execution evidence remains pending."
parallelism_budget: {max_concurrent_plans: 1, safe_parallelism: []}
hard_boundaries: ["Touch only files-modified outputs; canonical planning docs and undeclared paths are out of bounds.", "No side effect may outrun its exact authority, rights scope, DB target, or immutable input hash."]
escalation_triggers: ["Stop on missing, stale, contradictory, or wider authority; do not infer or repair external evidence."]
approval_gates: ["Network, provider, production DB, release, Git, and publication effects require the exact preceding checkpoint authority."]
anti_regression_targets: ["Preserve Phase 30/31 identity and authority, frozen dependencies, no fallback, local-use/redistribution separation, stable IDs/GUIDs, and Phase 34 boundaries."]
known_unknowns: ["Execution-time source, human, provider, DB, and release evidence remains unknown until its designated checkpoint."]
high_leverage_surfaces: ["Authority/evidence chain and fail-closed transitions named in must_haves."]
leverage: {lost: "No implicit convenience fallback or shared-environment mutation.", kept: "Verified project contracts and serial authority ordering.", gained: "Replayable exact evidence with bounded claims and explicit stop conditions."}
must_haves:
  truths:
    - "A candidate appears only after sibling staging, per-member fsync, manifest-last construction, reopen validation, absent-target rename, and parent fsync."
    - "Review batches are disjoint, at most 100 entries, content-free, exact-replay stable, and aggregate all 5965 dispositions."
    - "Exactly one additive reversible Phase 32 migration persists the genuine Phase 31 pointer/receipt/snapshot tuple and all evidence families without raw paths."
  artifacts:
    - {path: scripts/build_frequency_assets.py, provides: "Durable inactive immutable bundle builder"}
    - {path: tests/cli/test_korean_frequency_build_commands.py, provides: "CLI wiring/privacy proof for build-result validation"}
    - {path: src/multilang/services/korean_source_review.py, provides: "Bounded source-review importer/aggregate"}
    - {path: tests/cli/test_korean_source_review_commands.py, provides: "CLI wiring/privacy proof for source-review import and aggregation"}
    - {path: alembic/versions/20260821_18_frequency_text_audio_evidence.py, provides: "Sole Phase 32 migration"}
  key_links:
    - {from: "retrieval result plus transform authority", to: "inactive immutable target", via: "owned sibling staging and atomic absent-target promotion"}
    - {from: "build/review service validators", to: "operator workflow", via: "Typer CLI commands with safe aggregate output only"}
    - {from: "typed evidence fields", to: "SQLAlchemy schema", via: "one reversible sole-head migration"}
---

# Phase 32 Plan 02: Atomic Builder, Source Review, and Sole Migration

<objective>
Implement crash-safe inactive bundle construction, bounded source-review receipts, and the sole additive persistence schema required by every later authority stage.

Purpose: Partial trees, unreviewed source dispositions, and ad hoc schema changes must never become production authority.
Output: Builder/build validator, source-review importer/aggregate, CLI wiring/privacy proof, migration/ORM parity.
</objective>

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>
<context>
@.planning/phases/32-frequency-portuguese-text-and-audio/32-01-PLAN.md
@alembic/versions/20260804_17_korean_lexical_identity.py
@scripts/build_frequency_assets.py
<interfaces>
Create `validate-korean-source-build-result`, `import-korean-bundle-review-batch`, and `validate-korean-bundle-review-batches`. The builder requires exact retrieval/source and separate transformation authority, writes only an inactive version, and never changes a current pointer. Revision `20260821_18` is the sole Phase 32 child of verified head `20260804_17`.
</interfaces>
</context>
<checks><plan_check>
checker: gsd-plan-checker
checker_runtime: opencode
status: passed
blocking: false
notes: Fresh-context same-runtime checker passed on cycle 2 after adding CLI-level tests and verification for every new operator command declared by this plan.
</plan_check></checks>

<tasks>
<task id="32-02-01" type="auto" tdd="true">
  <name>Implement durable staged atomic bundle construction and strict build-result validation</name>
  <files>
    - MODIFY: tests/services/test_korean_frequency.py
    - CREATE: tests/cli/test_korean_frequency_build_commands.py
    - CREATE: tests/scripts/test_build_frequency_assets.py
    - MODIFY: src/multilang/services/korean_frequency.py
    - MODIFY: scripts/build_frequency_assets.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Build starts only from exact retrieval/source bytes and a separate authority binding parser, curation, analyzer, version, target/staging, counts, and disposition.
    - Members flush/fsync before manifest/root; the closed tree reopens and validates before absent-target atomic rename and parent fsync.
    - Links, traversal, undeclared files, collision, interruption, partial/non-identical target, or pointer mutation fail while preserving prior bytes; exact existing target is zero-write.
  </behavior>
  <action>Write interruption and mutation tests first. Implement the builder and strict validator using one authorization-hash-named descriptor-safe sibling staging tree. Canonicalize/write attribution, curated inventory, rejection ledger, and curation report; flush/fsync each file, write manifest/root last, fsync directories, close/reopen and fully revalidate, then rename only into an absent immutable inactive target and fsync the parent. Never overwrite or update a current pointer. Inject failures at every open/write/flush/fsync/manifest/reopen/rename/parent-fsync boundary and prove cleanup removes only owned staging; an exact complete existing target returns with no write or timestamp drift.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/scripts/test_build_frequency_assets.py -k 'staging or fsync or atomic_rename or interruption or rollback or collision or idempotent' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_frequency.py -k 'build_validator or manifest or root or inactive or exact_existing' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_frequency_build_commands.py -k 'build_result or cli or privacy or safe_output' -q</automated></verify>
  <done>The candidate is a complete durable inactive immutable tree or no target/pointer/prior byte changes occur.</done>
</task>

<task id="32-02-02" type="auto" tdd="true">
  <name>Implement bounded immutable source-review import and complete aggregation</name>
  <files>
    - CREATE: tests/services/test_korean_source_review.py
    - CREATE: tests/cli/test_korean_source_review_commands.py
    - CREATE: src/multilang/services/korean_source_review.py
    - MODIFY: src/multilang/cli.py
  </files>
  <behavior>
    - Each private batch covers at most 100 disjoint build-bound identities/dispositions with exact hashes, role, class, and decision.
    - Aggregate requires 5965 dispositions, 3000 accepted, 2965 rejected, 1000 per level, and every linguistic/modernity/analyzer/license/attribution risk exactly once.
    - Replay is no-write; overlap, gap, stale bytes, changed identity, bad role, oversize, or content/path/note leakage fails.
  </behavior>
  <action>Implement only the bounded import and aggregate commands. Validate each private payload against the immutable build tree/result, atomically emit one canonical content-free receipt by batch ID, and never alter or approve source/bundle bytes. Aggregate the fixed receipt directory with complete disjoint coverage and qualified roles into one safe root/count/status result. Mutation-test every binding, role, size, overlap, omission, risk, replay, and privacy boundary.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/services/test_korean_source_review.py -k 'batch or aggregate or bounded or disjoint or complete or role or privacy' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/cli/test_korean_source_review_commands.py -k 'import or aggregate or cli or privacy or safe_output' -q</automated></verify>
  <done>Qualified source review is resumable and machine-aggregated without source mutation or final approval.</done>
</task>

<task id="32-02-03" type="auto" tdd="true">
  <name>Create the sole additive migration and complete ORM/domain parity</name>
  <files>
    - MODIFY: tests/test_migration_schema_parity.py
    - CREATE: alembic/versions/20260821_18_frequency_text_audio_evidence.py
    - MODIFY: src/multilang/db/models.py
    - MODIFY: src/multilang/domain/korean.py
  </files>
  <behavior>
    - The sole head is reversible revision 20260821_18 over verified 20260804_17.
    - Base/full authority stores genuine Phase 31 active-pointer locator/content, validation receipt, pointer-selected snapshot manifest/root, and bundle locator/content; audio/full stages add catalog/profile/review authorities.
    - Legacy rows remain readable and no raw/private locator, prompt, payload, credential, note, or traceback field exists.
  </behavior>
  <action>Write migration lifecycle/parity tests first, verify the unique live head, and create one additive PostgreSQL-compatible revision plus matching ORM/domain fields for source, lexical, candidate/adaptive/review, audio, export, provider telemetry, and staged execution authority. Use safe historical nullability and stage constraints. Include strict 64-hex locator/content hashes and distinct provider-review/profile-sample/heard-review fields; reject invented `phase31_authority`, raw paths, destructive conversion, GUID changes, or any second Phase 32 revision. Prove upgrade, downgrade, re-upgrade, sole-head, legacy-null, and ORM parity on disposable databases only.</action>
  <verify><automated>UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync pytest tests/test_migration_schema_parity.py -k 'frequency_text_audio or locator or sole_linear_head or migrations_include_every_orm_column' -q &amp;&amp; UV_PROJECT_ENVIRONMENT=.planning/.local/phase32-py312 UV_OFFLINE=1 uv run --frozen --no-sync alembic heads</automated></verify>
  <done>One reversible migration owns every Phase 32 evidence field with legacy compatibility and no sensitive path persistence.</done>
</task>
</tasks>

<source_audit>
| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | SC-01 / SC-02 / SC-03 / SC-06 | Durable exact inactive inventory and auditable persistence | COVERED |
| REQ | KFREQ-01 / GLEX-01 / GLEX-02 / GPRO-01 / KAUD-01 / GAUD-01 | Bundle/review/schema authority | COVERED |
| RESEARCH | Atomic bundle / bounded review / additive migration | Fsync/reopen/rename, disjoint receipts, sole-head parity | COVERED |
| CONTEXT | 5965→3000+2965, three levels, separate powers, no pointer activation | Locked decisions preserved | COVERED |
</source_audit>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-32-02-01 | Tampering / Race Condition | bundle install | mitigate | Owned sibling staging, no-follow reads, fsync/reopen, absent-target rename, parent fsync. |
| T-32-02-02 | Information Disclosure / DoS | review receipts | mitigate | At-most-100 private payloads and content-free complete aggregates. |
| T-32-02-03 | Tampering / Injection | migration | mitigate | Sole-head parity, reversible lifecycle, strict SQLAlchemy mappings. |
</threat_model>
<high_leverage_review>Second pass: inspect every interruption/collision boundary, complete review accounting/privacy, and migration downgrade/legacy parity.</high_leverage_review>
<verification>Run all task commands offline against synthetic source and disposable databases only, including CLI-level command tests for every new operator entry point declared in this plan.</verification>
<success_criteria>
- Inactive installation is durable, atomic, collision-safe, and idempotent.
- Source review is bounded, disjoint, complete, and content-free.
- One reversible migration contains every required evidence family.
</success_criteria>
<output>After completion, create `.planning/phases/32-frequency-portuguese-text-and-audio/32-02-SUMMARY.md` with interruption matrix, review accounting, migration parity, command results, and bounded self-checked claim.</output>
