---
phase: 02-input-decks-lexical-grounding
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/multilang/runtime.py
  - src/multilang/cli.py
  - src/multilang/services/ingest_lexical_items.py
  - tests/cli/test_generate_command.py
  - tests/integration/test_lexical_job_flow.py
autonomous: true
requirements:
  - DECK-02
  - DECK-03
  - LEX-01
  - LEX-02
  - LEX-03
must_haves:
  truths:
    - `multilang generate` can output grounded candidates from either a built-in 3-level frequency deck or a custom word list.
    - Frequency runs finish with 1000 grounded candidates in each of the three levels.
    - Custom word-list runs preserve requested items, including pending ones, across reruns and resume flows.
  artifacts:
    - path: src/multilang/services/ingest_lexical_items.py
      provides: Batch coordinator for frequency/custom lexical ingestion
      contains: class IngestLexicalItemsService
    - path: src/multilang/cli.py
      provides: Shipped CLI wiring for Phase 2 lexical ingestion
      contains: multilang generate
    - path: tests/integration/test_lexical_job_flow.py
      provides: End-to-end lexical ingestion coverage on shipped runtime path
      contains: def test_generate_frequency_deck_persists_three_grounded_levels
  key_links:
    - from: src/multilang/runtime.py
      to: src/multilang/services/ingest_lexical_items.py
      via: runtime service construction for shipped CLI path
      pattern: IngestLexicalItemsService
    - from: src/multilang/cli.py
      to: tests/integration/test_lexical_job_flow.py
      via: shipped-path generate command behavior
      pattern: generate|pending_groundings|backfilled_candidates
---

<objective>
Wire the Phase 2 lexical services into the shipped CLI and runtime path.

Purpose: Turn the contracts and services from the earlier plans into the actual user-visible `multilang generate` behavior for both frequency and custom-list inputs.
Output: Runtime coordinator, CLI wiring, and end-to-end tests for lexical candidate generation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-input-decks-lexical-grounding/02-CONTEXT.md
@.planning/phases/02-input-decks-lexical-grounding/02-RESEARCH.md
@src/multilang/cli.py
@src/multilang/runtime.py
@src/multilang/services/generate_job.py
@src/multilang/repositories/job_repository.py
@src/multilang/repositories/lexical_repository.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the lexical ingestion coordinator for the shipped runtime path</name>
  <read_first>
    .planning/STATE.md
    src/multilang/runtime.py
    src/multilang/services/generate_job.py
    src/multilang/repositories/job_repository.py
    src/multilang/repositories/lexical_repository.py
    src/multilang/services/frequency_decks.py
    src/multilang/services/lexical_grounding.py
  </read_first>
  <files>src/multilang/runtime.py, src/multilang/services/ingest_lexical_items.py, tests/integration/test_lexical_job_flow.py</files>
  <behavior>
    - Test 1: a default frequency run persists three level buckets with exactly 1000 grounded lexical candidates each even when some ranked tokens must be backfilled per D-08
    - Test 2: a custom-list run persists every requested item, marking ungrounded items pending instead of replacing them
    - Test 3: rerun/resume behavior remains duplicate-safe because candidate rows update by `(job_id, item_key)` and job rows keep Phase 1 semantics
  </behavior>
  <action>Create `src/multilang/services/ingest_lexical_items.py` with a coordinator such as `IngestLexicalItemsService` that composes `GenerateJobService`, `LexicalRepository`, the frequency builder, the word-list parser, and the lexical grounding service. For `source_type="frequency"`, call `build_frequency_deck(...)`, ground candidates, and keep backfilling until all three levels persist exactly 1000 grounded rows each per DECK-02 and D-08; allow `--level` as an optional narrow-scope mode for targeted reruns, but the default frequency path must build the full 3-level deck. For `source_type="word-list"`, parse the file, ground each requested item, and persist pending rows for misses rather than swapping them away per DECK-03 and D-08. Update `src/multilang/runtime.py` so the shipped runtime path constructs this coordinator with the repository-backed session and configured lexical cache path. Add `tests/integration/test_lexical_job_flow.py` first, then implement the coordinator/runtime wiring so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/services/ingest_lexical_items.py` contains `class IngestLexicalItemsService`
    - `src/multilang/runtime.py` contains `IngestLexicalItemsService`
    - `tests/integration/test_lexical_job_flow.py` contains `def test_generate_frequency_deck_persists_three_grounded_levels`
    - `tests/integration/test_lexical_job_flow.py` contains `def test_custom_word_list_preserves_pending_items`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_lexical_job_flow.py -q</automated>
  </verify>
  <done>The shipped runtime can coordinate Phase 2 lexical ingestion for both source types while preserving Phase 1 resume/rerun guarantees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `multilang generate` to the lexical coordinator and surface diagnostics</name>
  <read_first>
    src/multilang/cli.py
    src/multilang/runtime.py
    src/multilang/services/ingest_lexical_items.py
    tests/cli/test_generate_command.py
    tests/integration/test_lexical_job_flow.py
  </read_first>
  <files>src/multilang/cli.py, tests/cli/test_generate_command.py, tests/integration/test_lexical_job_flow.py</files>
  <behavior>
    - Test 1: `multilang generate --source frequency` reports grounded/backfilled counts for all three levels and exits 0 when 3000 candidates persist across the deck
    - Test 2: `multilang generate --source word-list --input-file ...` reports rejected rows, pending groundings, and preserved originals
    - Test 3: rerunning the same input does not silently duplicate lexical candidates or lose pending rows
  </behavior>
  <action>Replace the placeholder Phase 1 ingest behavior in `src/multilang/cli.py` with the new lexical coordinator. Keep the existing single-command surface from Phase 1 and STATE.md: `multilang generate` remains the only operator entry point. For frequency runs, `--source frequency` with no `--level` must build the full 3-level deck, while `--level {1|2|3}` remains available only for targeted reruns/testing. Print counters including `grounded_candidates`, `backfilled_candidates`, `level_1_candidates`, `level_2_candidates`, `level_3_candidates`, and `pending_groundings=0` for the full-deck path. For custom word-list runs, print `grounded_candidates`, `pending_groundings`, and `rejected_rows`, while still printing the existing lifecycle summary fields so Phase 1 operator expectations remain intact. Update `tests/cli/test_generate_command.py` and `tests/integration/test_lexical_job_flow.py` first, then wire the CLI so those diagnostics and persistence behaviors are covered on the shipped-app path.</action>
  <acceptance_criteria>
    - `src/multilang/cli.py` contains `grounded_candidates=`
    - `src/multilang/cli.py` contains `pending_groundings=`
    - `src/multilang/cli.py` contains `rejected_rows=`
    - `src/multilang/cli.py` contains `level_1_candidates=`
    - `tests/cli/test_generate_command.py` contains `def test_generate_frequency_command_reports_grounded_candidate_counts`
    - `tests/cli/test_generate_command.py` contains `def test_generate_word_list_command_reports_pending_groundings`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cli/test_generate_command.py tests/integration/test_lexical_job_flow.py -q</automated>
  </verify>
  <done>Users can invoke one CLI command for either source type and receive persisted grounded candidates plus explicit diagnostics for backfill, pending items, and rejected rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI command → ingestion coordinator | User requests trigger persistence and external lexical lookups |
| coordinator → repositories | Runtime writes must preserve Phase 1 duplicate-safe semantics while adding lexical rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | T | `src/multilang/services/ingest_lexical_items.py` | mitigate | Reuse Phase 1 job orchestration and lexical repository upserts so reruns update existing work instead of duplicating candidate rows. |
| T-02-13 | R | `src/multilang/cli.py` | mitigate | Print explicit grounded/backfilled/pending/rejected counters so operators can audit what happened during lexical ingestion. |
| T-02-14 | D | `tests/integration/test_lexical_job_flow.py` | mitigate | Add shipped-path integration tests for frequency and custom-list flows so runtime wiring failures are caught before execution completes. |
</threat_model>

<verification>
- `uv run pytest tests/cli/test_generate_command.py tests/integration/test_lexical_job_flow.py -q`
- `uv run pytest tests -q`
</verification>

<success_criteria>
    - The shipped CLI can generate lexical candidates from either a full 3-level frequency deck or a custom word list.
    - Default frequency runs persist 1000 grounded candidates in each of levels 1, 2, and 3, even with backfill.
    - Custom word-list runs preserve requested items, including pending ones, across reruns and resume flows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-decks-lexical-grounding/02-04-SUMMARY.md`
</output>
