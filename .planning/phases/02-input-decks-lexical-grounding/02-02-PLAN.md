---
phase: 02-input-decks-lexical-grounding
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pyproject.toml
  - src/multilang/services/frequency_decks.py
  - tests/services/test_frequency_decks.py
autonomous: true
requirements:
  - DECK-02
  - LEX-01
must_haves:
  truths:
    - A selected language can produce one deterministic 3-level frequency deck with explicit level boundaries.
    - Noise tokens are filtered out before they become study candidates.
    - Frequency runs can continue past rejected candidates to preserve 1000 grounded cards per level.
  artifacts:
    - path: pyproject.toml
      provides: Runtime dependency on `wordfreq`
      contains: wordfreq
    - path: src/multilang/services/frequency_decks.py
      provides: Curated frequency deck builder and level selector
      contains: build_frequency_deck
    - path: tests/services/test_frequency_decks.py
      provides: Deterministic frequency filter and level-window coverage
      contains: def test_build_frequency_level_returns_1000_ranked_items
  key_links:
    - from: src/multilang/services/frequency_decks.py
      to: pyproject.toml
      via: `wordfreq` runtime dependency
      pattern: wordfreq
    - from: src/multilang/services/frequency_decks.py
      to: tests/services/test_frequency_decks.py
      via: level slicing and backfill tests
      pattern: build_frequency_deck|build_frequency_level|iter_curated_frequency_candidates
---

<objective>
Implement the deterministic frequency-deck builder for all supported languages.

Purpose: Deliver the built-in 3-level deck path with explicit curation rules, rank windows, and backfill support before CLI wiring happens.
Output: `wordfreq` dependency, curated frequency service, and unit tests proving full-deck rank/count/filter behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-input-decks-lexical-grounding/02-CONTEXT.md
@.planning/phases/02-input-decks-lexical-grounding/02-RESEARCH.md
@src/multilang/domain/lexicon.py
@src/multilang/services/input_fingerprint.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the curated frequency candidate iterator</name>
  <read_first>
    .planning/phases/02-input-decks-lexical-grounding/02-CONTEXT.md
    .planning/phases/02-input-decks-lexical-grounding/02-RESEARCH.md
    pyproject.toml
    src/multilang/domain/lexicon.py
    src/multilang/services/input_fingerprint.py
  </read_first>
  <files>pyproject.toml, src/multilang/services/frequency_decks.py, tests/services/test_frequency_decks.py</files>
  <behavior>
    - Test 1: the iterator rejects digits, punctuation-only tokens, obvious web noise, and malformed abbreviations per D-03 and D-04
    - Test 2: the iterator keeps valid lower-case function words instead of filtering them just because they are short or closed-class
    - Test 3: iteration order remains deterministic for a fixed language and mock `wordfreq` output
  </behavior>
  <action>Add `wordfreq>=3.1,<4.0` to `pyproject.toml`, then create `src/multilang/services/frequency_decks.py`. Implement an iterator such as `iter_curated_frequency_candidates(language, scan_limit=6000)` that calls `wordfreq.top_n_list()` or `iter_wordlist()` and applies the Phase 2 mandatory filter rules exactly: reject any token containing digits, reject punctuation-only/symbol-heavy tokens, reject `http`, `https`, `www`, and `nbsp`, reject broken abbreviations with internal periods, reject title-cased proper-name candidates when the lower-cased form differs from the source token, and accept alphabetic tokens plus internal apostrophes/hyphens when the surrounding characters are letters. Do not reject valid function words only because they are short. Write tests first in `tests/services/test_frequency_decks.py`, then implement the iterator so the tests pass.</action>
  <acceptance_criteria>
    - `pyproject.toml` contains `wordfreq`
    - `src/multilang/services/frequency_decks.py` contains `def iter_curated_frequency_candidates`
    - `src/multilang/services/frequency_decks.py` contains `scan_limit=6000` or equivalent explicit scan budget
    - `tests/services/test_frequency_decks.py` contains `def test_iterator_rejects_noise_tokens`
    - `tests/services/test_frequency_decks.py` contains `def test_iterator_keeps_function_words`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_frequency_decks.py -q</automated>
  </verify>
  <done>The project can derive deterministic, filtered ranked candidates from `wordfreq` for every supported language without leaking obvious corpus noise into study decks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the 3-level selector with backfill support</name>
  <read_first>
    src/multilang/domain/lexicon.py
    src/multilang/services/frequency_decks.py
    tests/services/test_frequency_decks.py
  </read_first>
  <files>src/multilang/services/frequency_decks.py, tests/services/test_frequency_decks.py</files>
  <behavior>
    - Test 1: `build_frequency_deck(language)` returns three level buckets with exactly 1000 accepted items each
    - Test 2: each level uses the explicit rank windows 1-1000, 1001-2000, and 2001-3000 for its primary slice
    - Test 3: the selector can keep scanning beyond rank 3000 so rejected/failed candidates are backfilled until every level still yields 1000 items per D-08
    - Test 4: returned records preserve both `frequency_rank` and `frequency_level` for later persistence per LEX-01
  </behavior>
  <action>Extend `src/multilang/services/frequency_decks.py` with `build_frequency_deck(language, required_count_per_level=1000, rejected_lemmas_by_level=None)` plus `build_frequency_level(...)`. Use explicit rank windows `1..1000`, `1001..2000`, and `2001..3000`, but keep reading curated candidates past the initial 3000 so callers can exclude failed groundings and still receive exactly 1000 accepted items in every level per D-08. Return a structure keyed by level (`1`, `2`, `3`) whose entries are `LexicalCardCandidate`-compatible seed records containing `submitted_form`, `display_form`, `frequency_rank`, and `frequency_level`. Update `tests/services/test_frequency_decks.py` first to lock the full-deck counts, rank windows, and backfill behavior, then implement the selector.</action>
  <acceptance_criteria>
    - `src/multilang/services/frequency_decks.py` contains `def build_frequency_deck`
    - `src/multilang/services/frequency_decks.py` contains `def build_frequency_level`
    - `src/multilang/services/frequency_decks.py` contains the explicit ranges `1000`, `2000`, and `3000`
    - `tests/services/test_frequency_decks.py` contains `def test_build_frequency_deck_returns_three_full_levels`
    - `tests/services/test_frequency_decks.py` contains `def test_build_frequency_level_backfills_rejected_candidates`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_frequency_decks.py -q</automated>
  </verify>
  <done>Each supported language has a deterministic 1000-card level builder that preserves rank metadata and can backfill past rejected candidates instead of shrinking the deck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `wordfreq` output → study candidates | External ranked tokens become learner-facing deck inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | `src/multilang/services/frequency_decks.py` | mitigate | Apply explicit token filters for digits, symbols, web noise, and malformed abbreviations before any candidate can enter a deck. |
| T-02-07 | D | `src/multilang/services/frequency_decks.py` | mitigate | Keep a bounded `scan_limit` and deterministic iteration path so backfill does not turn into unbounded scanning. |
| T-02-08 | R | `tests/services/test_frequency_decks.py` | mitigate | Add unit tests proving exact rank windows and 1000-item counts so later refactors cannot silently change deck size. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_frequency_decks.py -q`
</verification>

<success_criteria>
    - `wordfreq` is wired into the project as the deterministic frequency bootstrap source.
    - Rank windows for levels 1-3 are explicit and tested against one full 3-level deck shape.
    - Filter and backfill rules are encoded in one service instead of ad-hoc CLI logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-decks-lexical-grounding/02-02-SUMMARY.md`
</output>
