---
phase: 12-highlight-generation-audio-and-qa
plan: 04
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - src/multilang/services/text_review.py
  - src/multilang/repositories/text_repository.py
  - src/multilang/runtime.py
  - tests/services/test_text_review.py
  - tests/integration/test_v12_existing_mode_regression_boundary.py
  - .planning/phases/12-highlight-generation-audio-and-qa/12-GENERATION-QA-EVIDENCE.md
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03]
must_haves:
  truths:
    - "Highlight generation QA outcomes are distinguishable from frequency/custom QA outcomes in review evidence."
    - "Review evidence does not leak private highlight text, source file paths, book metadata, or WebDAV-like secrets."
    - "Existing frequency/custom regression evidence remains green after highlight generation/audio changes."
  artifacts:
    - path: "src/multilang/services/text_review.py"
      provides: "Source-aware review report items"
      exports: ["ReviewReport", "ReviewReportItem", "TextReviewService"]
    - path: "tests/integration/test_v12_existing_mode_regression_boundary.py"
      provides: "Existing-mode plus highlight QA regression boundary"
    - path: ".planning/phases/12-highlight-generation-audio-and-qa/12-GENERATION-QA-EVIDENCE.md"
      provides: "Scanner-readable phase evidence commands and outcomes"
  key_links:
    - from: "src/multilang/runtime.py"
      to: "src/multilang/services/text_review.py"
      via: "TextReviewService receives enough repository context to include source_type safely"
      pattern: "TextReviewService"
    - from: "src/multilang/services/text_review.py"
      to: "src/multilang/security/redaction.py"
      via: "redact report details before serialization"
      pattern: "redact_sensitive_text|redact_mapping"
---

<objective>
Close Phase 12 with source-aware QA and privacy evidence.

Purpose: The user needs proof that highlight generation/audio QA is distinct, privacy-safe, and does not regress existing generation modes.
Output: Source-aware review reporting, regression tests, and a phase evidence artifact.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/RETROSPECTIVE.md
@.planning/phases/09-source-profiles-privacy-regression-boundary/09-04-SUMMARY.md
@.planning/phases/11-highlight-pipeline-integration/11-04-SUMMARY.md
@src/multilang/services/text_review.py
@src/multilang/repositories/text_repository.py
@src/multilang/runtime.py
@tests/services/test_text_review.py
@tests/integration/test_v12_existing_mode_regression_boundary.py

<interfaces>
From `src/multilang/services/text_review.py`:
```python
class ReviewReportItem(BaseModel):
    confidence_label: ConfidenceLabel
    example_sentence: str | None = None
    item_key: str
    job_id: str
    review_reason: str | None = None
    translation_text: str | None = None
    validation_flags: list[str]

class TextReviewService:
    def build_review_report(self, *, job_id: str, output_path: Path | str) -> ReviewReport: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-aware privacy-safe review report items</name>
  <files>src/multilang/services/text_review.py, src/multilang/repositories/text_repository.py, src/multilang/runtime.py, tests/services/test_text_review.py</files>
  <behavior>
    - Test 1: review report items include `source_type` so `kindle-highlights` QA is distinguishable from frequency/custom QA.
    - Test 2: highlight report items include validation flags, confidence, and item keys but no private highlight text, file path, or book metadata.
    - Test 3: frequency/custom review reports keep their previous fields and remain valid JSON.
  </behavior>
  <action>Extend `ReviewReportItem` with `source_type` and, if useful, `translation_required` from `SourceProfile`. Give `TextReviewService` access to lexical/source metadata through an injected repository or a text repository query that returns safe source fields only. Redact serialized report text fields with `multilang.security.redaction` before writing. Do not include raw highlight records or source paths in the report.</action>
  <verify>
    <automated>uv run pytest tests/services/test_text_review.py tests/security/test_redaction.py -q</automated>
  </verify>
  <done>Review reports clearly identify highlight QA rows and remain free of private reading text and paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record Phase 12 QA and regression evidence</name>
  <files>tests/integration/test_v12_existing_mode_regression_boundary.py, .planning/phases/12-highlight-generation-audio-and-qa/12-GENERATION-QA-EVIDENCE.md</files>
  <behavior>
    - Test 1: existing frequency generation/audio/export regression boundary still passes.
    - Test 2: existing custom word-list generation/audio/export regression boundary still passes.
    - Test 3: highlight generation/audio QA evidence includes source-aware review output and does not include private fixture text.
  </behavior>
  <action>Extend the v1.2 regression boundary with a focused highlight generation/audio QA assertion that reuses the integration evidence from Plan 03 and the source-aware report from Task 1. Create `12-GENERATION-QA-EVIDENCE.md` with the exact commands run, expected pass signals, requirement IDs GEN-01/GEN-02/GEN-03, and privacy checks. The evidence artifact must contain counts, file names, and safe source keys only; do not copy raw highlight fixture text.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_v12_existing_mode_regression_boundary.py tests/integration/test_highlight_generation_audio_flow.py tests/services/test_text_review.py -q</automated>
  </verify>
  <done>Phase 12 has scanner-readable evidence that highlight QA is distinct and privacy-safe while existing modes remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted QA records -> report artifact | Review evidence may be committed or shared and must not leak private input. |
| source metadata -> user-facing QA | Safe source labels distinguish mode behavior without exposing raw source content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | `TextReviewService._write_report` | mitigate | Redact report fields and include only source_type/item_key/flags/counts; test private text/path absence. |
| T-12-08 | Repudiation | `12-GENERATION-QA-EVIDENCE.md` | mitigate | Record exact automated commands and requirement IDs so Phase 12 QA outcomes are auditable. |
</threat_model>

<verification>
Run `uv run pytest tests/integration/test_v12_existing_mode_regression_boundary.py tests/integration/test_highlight_generation_audio_flow.py tests/services/test_text_review.py tests/security/test_redaction.py -q`.
</verification>

<success_criteria>
- Source-aware review reports distinguish `kindle-highlights` from frequency/custom QA.
- Evidence artifact maps GEN-01, GEN-02, and GEN-03 to passing commands.
- Privacy checks prove raw highlight text, paths, and metadata are absent from reports/evidence.
- Existing-mode regression boundary remains passing.
</success_criteria>

<output>
After completion, create `.planning/phases/12-highlight-generation-audio-and-qa/12-04-SUMMARY.md`.
</output>
