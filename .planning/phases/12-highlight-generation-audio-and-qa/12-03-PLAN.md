---
phase: 12-highlight-generation-audio-and-qa
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - src/multilang/services/generate_audio_items.py
  - src/multilang/services/assemble_export_cards.py
  - tests/services/test_generate_audio_items.py
  - tests/services/test_assemble_export_cards.py
  - tests/integration/test_highlight_generation_audio_flow.py
autonomous: true
requirements: [GEN-01]
must_haves:
  truths:
    - "Accepted highlight records receive both word audio and sentence audio through the existing Azure-first audio boundary."
    - "Highlight export rows contain word/headword, IPA plus spoken form, definition, example sentence, audio tags, and blank Image."
    - "Highlight rows do not require or populate Translation as learner-facing content."
  artifacts:
    - path: "src/multilang/services/generate_audio_items.py"
      provides: "Accepted highlight text audio generation and reuse"
      exports: ["GenerateAudioItemsService"]
    - path: "src/multilang/services/assemble_export_cards.py"
      provides: "Frozen highlight card rows from accepted text and audio"
      exports: ["AssembleExportCardsService"]
    - path: "tests/integration/test_highlight_generation_audio_flow.py"
      provides: "End-to-end highlight generation/audio/card evidence"
  key_links:
    - from: "src/multilang/services/generate_audio_items.py"
      to: "src/multilang/domain/audio.py"
      via: "AudioAssetKind.WORD and AudioAssetKind.SENTENCE for highlight item keys"
      pattern: "AudioAssetKind\.WORD|AudioAssetKind\.SENTENCE"
    - from: "src/multilang/services/assemble_export_cards.py"
      to: "src/multilang/domain/source_profiles.py"
      via: "source profile controls Translation omission/blanking for highlights"
      pattern: "exports_translation_field|get_source_profile"
---

<objective>
Prove highlight cards carry complete learner-facing content and playable audio references.

Purpose: GEN-01 is only true when accepted highlight text can flow into word audio, sentence audio, and frozen card rows without relying on the normal Translation field.
Output: Source-aware card assembly/audio tests and a focused integration proof for highlight mode.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-audio-synthesis/04-03-SUMMARY.md
@.planning/phases/05-anki-safe-export-contract/05-03-SUMMARY.md
@.planning/phases/11-highlight-pipeline-integration/11-04-SUMMARY.md
@src/multilang/services/generate_audio_items.py
@src/multilang/services/assemble_export_cards.py
@src/multilang/domain/exporting.py
@tests/services/test_generate_audio_items.py
@tests/services/test_assemble_export_cards.py

<interfaces>
From `src/multilang/services/generate_audio_items.py`:
```python
class GenerateAudioItemsService:
    def execute(self, *, job_id: str, deck_language: SupportedLanguage,
                item_keys: set[str] | None = None) -> GenerateAudioItemsResult: ...
```

From `src/multilang/services/assemble_export_cards.py`:
```python
class AssembleExportCardsService:
    def execute(self, *, job_id: str, deck_language: SupportedLanguage) -> AssembleExportCardsResult: ...
```

From `src/multilang/domain/exporting.py`:
```python
HIGHLIGHT_EXPORT_CARD_FIELD_NAMES = ("SortIndex", "Word", "IPA", "word_audio", "Example Sentence", "sentence_audio", "Definition", "Image")
class ExportCardRow(BaseModel):
    translation: str = Field(default="", alias="Translation")
    image: str = Field(default="", alias="Image")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble highlight card rows with blank Translation and Image</name>
  <files>src/multilang/services/assemble_export_cards.py, tests/services/test_assemble_export_cards.py</files>
  <behavior>
    - Test 1: a `kindle-highlights` lexical candidate assembles into an `ExportCardRow` whose ordered highlight mapping has `Word`, `Definition`, audio fields, and blank `Image`.
    - Test 2: highlight rows store `translation == ""` even when no Translation field will be exported.
    - Test 3: IPA requires spoken form exactly as normal cards do, so highlight cards keep the Phase 08 pronunciation behavior.
  </behavior>
  <action>Update `AssembleExportCardsService` to resolve the source profile for each lexical candidate. For `exports_translation_field=False`, set the row's `translation` to an empty string while still requiring definitions, IPA, spoken form, word audio, sentence audio, and blank `Image`. Do not change frequency/custom assembly behavior.</action>
  <verify>
    <automated>uv run pytest tests/services/test_assemble_export_cards.py -q</automated>
  </verify>
  <done>Highlight export rows contain the requested learner fields, playable sound tags, blank `Image`, and no learner-facing Translation content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add highlight generation/audio integration evidence</name>
  <files>src/multilang/services/generate_audio_items.py, tests/services/test_generate_audio_items.py, tests/integration/test_highlight_generation_audio_flow.py</files>
  <behavior>
    - Test 1: accepted highlight text rows generate exactly two audio assets per accepted item: word and sentence.
    - Test 2: review-required highlight text rows do not generate audio.
    - Test 3: an integrated highlight fixture produces accepted text, synthesized/reused audio, and an assembled card row with `source_type="kindle-highlights"`.
  </behavior>
  <action>Add focused tests proving `GenerateAudioItemsService` handles `kindle-highlights` item keys and source types through the existing audio path. If any code currently assumes frequency/custom keys only, replace that assumption with source-agnostic item-key handling. Create `tests/integration/test_highlight_generation_audio_flow.py` using deterministic fake/local adapters and fake audio; do not call live Azure or provider services.</action>
  <verify>
    <automated>uv run pytest tests/services/test_generate_audio_items.py tests/services/test_assemble_export_cards.py tests/integration/test_highlight_generation_audio_flow.py -q</automated>
  </verify>
  <done>Automated evidence proves accepted highlight cards have word audio, sentence audio, IPA/spoken form, definitions, example sentence, and blank Image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted text -> audio provider | Learner-facing text becomes synthesized media input. |
| persisted text/audio -> export row | Generated artifacts become Anki-ready card data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | `AssembleExportCardsService` | mitigate | Require source-profile lookup, synthesized audio status, IPA, spoken form, definitions, and blank Image before card row persistence. |
| T-12-06 | Denial of Service | `GenerateAudioItemsService` | accept | Existing deterministic reuse and failure counters already prevent silent provider loops; this plan adds highlight coverage without changing provider retry policy. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_generate_audio_items.py tests/services/test_assemble_export_cards.py tests/integration/test_highlight_generation_audio_flow.py -q`.
</verification>

<success_criteria>
- Highlight accepted rows generate word and sentence audio assets.
- Highlight rows assemble with word/headword, IPA/spoken form, definition, example sentence, audio tags, and blank Image.
- Highlight rows leave Translation empty and remain compatible with Phase 13 dedicated export/template work.
</success_criteria>

<output>
After completion, create `.planning/phases/12-highlight-generation-audio-and-qa/12-03-SUMMARY.md`.
</output>
