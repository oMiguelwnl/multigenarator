---
phase: 12-highlight-generation-audio-and-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/domain/source_profiles.py
  - src/multilang/services/text_validation.py
  - src/multilang/services/text_generation.py
  - src/multilang/services/generate_text_items.py
  - tests/services/test_text_validation.py
  - tests/services/test_generate_text_items.py
autonomous: true
requirements: [GEN-01, GEN-02]
must_haves:
  truths:
    - "Highlight text generation uses highlight source-profile rules instead of frequency/custom defaults."
    - "Accepted highlight examples are concise, richer than generic support templates, include the target word or lemma, and pass a 6-16 token gate."
    - "Highlight cards can be accepted without sentence translation validation because the highlight deck does not use Translation."
  artifacts:
    - path: "src/multilang/domain/source_profiles.py"
      provides: "Highlight min/max sentence and translation-required policy"
      contains: "kindle-highlights"
    - path: "src/multilang/services/text_validation.py"
      provides: "Configurable sentence length and translation validation"
      exports: ["TextValidationService"]
    - path: "src/multilang/services/generate_text_items.py"
      provides: "Source-profile-aware text validation routing"
      exports: ["GenerateTextItemsService"]
  key_links:
    - from: "src/multilang/services/generate_text_items.py"
      to: "src/multilang/domain/source_profiles.py"
      via: "get_source_profile(source_type) before validation"
      pattern: "get_source_profile"
    - from: "src/multilang/services/generate_text_items.py"
      to: "src/multilang/services/text_validation.py"
      via: "validate(... min_sentence_tokens, max_sentence_tokens, require_translation)"
      pattern: "min_sentence_tokens|max_sentence_tokens|require_translation"
---

<objective>
Make the text-generation validation boundary honor highlight deck rules.

Purpose: Highlight-mode cards need richer examples and no Translation-field dependency while existing frequency/custom behavior remains unchanged.
Output: Source-profile-aware validation contracts and tests proving highlight examples pass the requested rules.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-highlight-pipeline-integration/11-03-SUMMARY.md
@src/multilang/domain/source_profiles.py
@src/multilang/services/text_validation.py
@src/multilang/services/text_generation.py
@src/multilang/services/generate_text_items.py
@tests/services/test_text_validation.py
@tests/services/test_generate_text_items.py

<interfaces>
From `src/multilang/domain/source_profiles.py`:
```python
class SourceProfile:
    source_type: SourceType
    requires_translation_validation: bool
    exports_translation_field: bool
    min_sentence_tokens: int
    max_sentence_tokens: int
    note_type_name: str
    template_name: str

def get_source_profile(source_type: str) -> SourceProfile
```

From `src/multilang/services/text_validation.py`:
```python
class TextValidationService:
    def validate(self, *, sentence, translation, display_form, lemma, definitions_html,
                 uncertainty_notes=None, disallowed_sentence_texts=None,
                 require_translation=True) -> TextValidationResult: ...
```

From `src/multilang/services/generate_text_items.py`:
```python
class GenerateTextItemsService:
    def execute(self, *, job_id: str, deck_language: SupportedLanguage) -> GenerateTextItemsResult: ...
```
</interfaces>

<source_audit>
GOAL Phase 12: covered by Plans 01-04.
REQ GEN-01: covered by Plans 03-04 and partially by Plan 01 validation prerequisites.
REQ GEN-02: covered by Plans 01-02.
REQ GEN-03: covered by Plans 02 and 04.
RESEARCH/STACK constraints: Azure audio, typed Pydantic contracts, deterministic tests, and provider boundaries are covered by Plans 01-04.
CONTEXT decisions: no phase CONTEXT.md exists; carry-forward STATE.md decisions on highlight privacy, Azure audio, blank Image, and existing-mode preservation are implemented across Plans 01-04.
Deferred/out-of-scope exclusions: no translations for highlight cards, no image sourcing, no private raw highlight commits.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add configurable validation limits for highlight examples</name>
  <files>src/multilang/services/text_validation.py, tests/services/test_text_validation.py</files>
  <behavior>
    - Test 1: a 5-token highlight sentence fails when min_sentence_tokens=6.
    - Test 2: a 13-token frequency sentence still passes the existing default only if the default max remains 12; do not change frequency/custom defaults.
    - Test 3: a 15-token highlight sentence passes when max_sentence_tokens=16 and includes the target lemma.
  </behavior>
  <action>Extend `TextValidationService.validate` with optional `min_sentence_tokens` and `max_sentence_tokens` keyword-only parameters. Use those values inside `_check_sentence_length` without changing the class defaults for callers that omit them. Keep target-form, duplicate, banned-pattern, and translation checks unchanged.</action>
  <verify>
    <automated>uv run pytest tests/services/test_text_validation.py -q</automated>
  </verify>
  <done>Validation accepts caller-supplied highlight sentence bounds while existing default bounds remain 4-12 tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route highlight generation through source-profile validation</name>
  <files>src/multilang/domain/source_profiles.py, src/multilang/services/text_generation.py, src/multilang/services/generate_text_items.py, tests/services/test_generate_text_items.py</files>
  <behavior>
    - Test 1: a `kindle-highlights` candidate with an empty translation passes validation when the example satisfies target and length rules.
    - Test 2: a `kindle-highlights` candidate uses 6-16 token bounds from `SourceProfile`.
    - Test 3: frequency and word-list candidates still use their existing translation and length behavior.
  </behavior>
  <action>In `GenerateTextItemsService._validate_bundle`, resolve `get_source_profile(source_type or inferred default)` and pass `requires_translation_validation`, `min_sentence_tokens`, and `max_sentence_tokens` into `TextValidationService.validate`. Use the existing `kindle-highlights` profile exactly; do not add Translation validation for highlights. If `source_type` is absent, preserve current behavior by defaulting to `frequency` for ranked candidates and `word-list` otherwise.</action>
  <verify>
    <automated>uv run pytest tests/services/test_generate_text_items.py tests/services/test_text_validation.py -q</automated>
  </verify>
  <done>Highlight candidates can be accepted with no translation validation and with the profile's 6-16 token sentence bounds; existing modes stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted lexical row -> text validation | Source type controls validation rules and must not be spoofed into unsafe defaults. |
| generated text -> persisted QA record | Provider/local output becomes learner-facing content only after deterministic validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `GenerateTextItemsService._validate_bundle` | mitigate | Resolve source behavior through `get_source_profile` instead of ad hoc string comparisons, and test `kindle-highlights`, `frequency`, and `word-list`. |
| T-12-02 | Information Disclosure | highlight text validation records | accept | This plan only validates generated examples and does not read raw highlight text; private context handling is in Plan 02. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_generate_text_items.py tests/services/test_text_validation.py -q`.
</verification>

<success_criteria>
- Highlight examples use 6-16 token bounds.
- Highlight examples require target lemma/form inclusion.
- Highlight validation does not require Translation.
- Existing frequency/custom text tests remain passing.
</success_criteria>

<output>
After completion, create `.planning/phases/12-highlight-generation-audio-and-qa/12-01-SUMMARY.md`.
</output>
