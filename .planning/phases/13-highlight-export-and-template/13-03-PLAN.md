---
phase: 13-highlight-export-and-template
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - tests/services/test_export_tabular_bundle.py
  - tests/integration/test_highlight_export_artifacts.py
  - .planning/phases/13-highlight-export-and-template/13-EXPORT-EVIDENCE.md
autonomous: true
requirements: [EXPORT-01, EXPORT-03]
must_haves:
  truths:
    - "User can export highlight CSV and TSV files with strict Anki headers and exact highlight columns."
    - "User receives evidence that highlight exports contain no Translation field and no dangling template references."
    - "User receives regression evidence that existing frequency and word-list export behavior remains stable."
  artifacts:
    - path: "tests/services/test_export_tabular_bundle.py"
      provides: "Strict highlight CSV/TSV header tests"
    - path: "tests/integration/test_highlight_export_artifacts.py"
      provides: "APKG/CSV/TSV highlight export integration evidence"
    - path: ".planning/phases/13-highlight-export-and-template/13-EXPORT-EVIDENCE.md"
      provides: "Scanner-readable Phase 13 evidence"
  key_links:
    - from: "src/multilang/services/export_tabular_bundle.py"
      to: "src/multilang/domain/exporting.py"
      via: "export_field_names_for_rows(sorted_rows)"
      pattern: "HIGHLIGHT_EXPORT_CARD_FIELD_NAMES"
    - from: "src/multilang/runtime.py"
      to: "src/multilang/domain/source_profiles.py"
      via: "_note_type_name_for_rows(rows)"
      pattern: "get_source_profile"
---

<objective>
Prove APKG/CSV/TSV highlight export artifacts satisfy strict Anki import contracts and preserve existing source-mode boundaries.

Purpose: Phase 13 is complete only when all export formats have deterministic evidence, not only APKG template wiring.
Output: Highlight tabular/export integration tests and a Phase 13 evidence artifact.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-highlight-export-and-template/13-CONTEXT.md
@.planning/phases/13-highlight-export-and-template/13-RESEARCH.md
@.planning/phases/13-highlight-export-and-template/13-01-SUMMARY.md
@src/multilang/services/export_tabular_bundle.py
@src/multilang/runtime.py
@tests/services/test_export_tabular_bundle.py
@tests/integration/test_export_job_flow.py

<interfaces>
From `src/multilang/services/export_tabular_bundle.py`:
```python
def write_export_tabular_bundle(*, rows: list[ExportCardRow], export_format: ExportArtifactFormat, output_dir: Path, deck_name: str, note_type_name: str) -> ExportTabularBundleResult: ...
```

From `src/multilang/runtime.py`:
```python
def _note_type_name_for_rows(rows: list[object]) -> str:
    source_types = {row.identity.source_type for row in rows}
    if len(source_types) > 1:
        raise ValueError("cannot export mixed source types in one note model")
    return get_source_profile(source_type).note_type_name
```

Anki text import research:
```text
#separator:Comma|Tab
#html:true
#notetype:<note type>
#deck:<deck>
#columns:<separator-joined field names>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict highlight CSV/TSV header tests</name>
  <files>tests/services/test_export_tabular_bundle.py</files>
  <behavior>
    - Test 1: Highlight TSV starts with `#separator:Tab`, `#html:true`, `#notetype:Multilang::Highlight Card`, `#deck:<deck>`, and exact `#columns:SortIndex\tWord\tIPA\tword_audio\tExample Sentence\tsentence_audio\tDefinition\tImage` per D-16.
    - Test 2: Highlight CSV starts with `#separator:Comma` and exact comma-separated highlight columns with no `Translation`, `word`, or `Definitions` per D-14/D-16.
    - Test 3: Highlight rows serialize `Word`, `Definition`, audio sound tags, and blank `Image`; mixed highlight/frequency rows fail closed per D-13/D-17.
    - Test 4: Existing `frequency` and `word-list` tabular tests still preserve `Translation` per D-18.
  </behavior>
  <action>Extend `tests/services/test_export_tabular_bundle.py` with `kindle-highlights` rows and the exact Anki headers from research. Use `note_type_name="Multilang::Highlight Card"` and assert the strict highlight field order from `HIGHLIGHT_EXPORT_CARD_FIELD_NAMES`. Do not add `Translation` to highlight CSV/TSV. If the existing implementation does not produce these headers or fail closed for mixed rows, update production code in `src/multilang/services/export_tabular_bundle.py` only as needed to make the source-profile contract explicit.</action>
  <verify>
    <automated>python -m pytest tests/services/test_export_tabular_bundle.py tests/domain/test_exporting.py -q</automated>
  </verify>
  <done>CSV and TSV highlight exports have strict Anki headers, exact fields, no `Translation`, safe sound-tag fields, blank `Image`, and mixed-source rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integrated highlight export artifact evidence</name>
  <files>tests/integration/test_highlight_export_artifacts.py, .planning/phases/13-highlight-export-and-template/13-EXPORT-EVIDENCE.md</files>
  <behavior>
    - Test 1: A synthetic accepted highlight row exports APKG, CSV, and TSV using the dedicated highlight note type and fields per EXPORT-01.
    - Test 2: The APKG model/template has no dangling field references and no `Translation`; CSV/TSV headers match strict Anki import metadata per EXPORT-03.
    - Test 3: Regression command includes existing frequency/manual export tests so D-18 is protected.
  </behavior>
  <action>Create `tests/integration/test_highlight_export_artifacts.py` using synthetic fixtures and local audio files only. Exercise the exported `export_anki_package()` and `write_export_tabular_bundle()` services directly or through runtime if the fixture cost stays bounded. Assert APKG zip exists and packaged media entries are present; assert model/template content through `build_multilang_model(source_type="kindle-highlights")`; assert CSV/TSV headers and rows have exact highlight fields and no `Translation`. Create `13-EXPORT-EVIDENCE.md` with requirement IDs `EXPORT-01`, `EXPORT-02`, `EXPORT-03`, commands run, pass counts, and privacy note that no raw private highlight text, book metadata, source path, or credentials are included per D-14. Include the regression command `python -m pytest tests/services/test_export_anki_package.py tests/services/test_export_tabular_bundle.py tests/integration/test_highlight_export_artifacts.py tests/domain/test_exporting.py -q`.</action>
  <verify>
    <automated>python -m pytest tests/services/test_export_anki_package.py tests/services/test_export_tabular_bundle.py tests/integration/test_highlight_export_artifacts.py tests/domain/test_exporting.py -q</automated>
  </verify>
  <done>Phase 13 has automated evidence for highlight APKG/CSV/TSV exports, no dangling/Translation template references, strict headers, safe packaged media, and existing-mode export regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export rows → text import files | Rows become user-imported CSV/TSV artifacts with Anki metadata headers. |
| source-mode selection → note type | Source rows decide note type and field columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | T | `write_export_tabular_bundle` | mitigate | Assert exact `#columns` and note type for highlight rows; reject mixed source rows through `export_field_names_for_rows`. |
| T-13-07 | I | `13-EXPORT-EVIDENCE.md` | mitigate | Evidence uses synthetic data and records only safe commands/results, not raw private highlight text or paths. |
| T-13-08 | R | export regression evidence | mitigate | Record scanner-readable commands and pass counts mapping to EXPORT-01/02/03. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_export_anki_package.py tests/services/test_export_tabular_bundle.py tests/integration/test_highlight_export_artifacts.py tests/domain/test_exporting.py -q`.
</verification>

<success_criteria>
Highlight APKG, CSV, and TSV artifact behavior is proven with strict note type/field/header checks, no `Translation`, no dangling references, safe media, and existing source-mode regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/13-highlight-export-and-template/13-03-SUMMARY.md`.
</output>
