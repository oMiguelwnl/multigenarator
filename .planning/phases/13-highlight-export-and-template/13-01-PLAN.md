---
phase: 13-highlight-export-and-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HIGHLIGHT_CARD_TEMPLATE.md
  - src/multilang/services/card_template_loader.py
  - tests/services/test_card_template_loader.py
autonomous: true
requirements: [EXPORT-02, EXPORT-03]
must_haves:
  truths:
    - "User sees highlight card fronts with word, IPA, word audio, example sentence, and sentence audio."
    - "User sees highlight card backs reuse FrontSide and reveal only a Definition answer area after a divider."
    - "User sees centered, responsive Multilang-blue highlight styling without horizontal scroll."
  artifacts:
    - path: "HIGHLIGHT_CARD_TEMPLATE.md"
      provides: "Dedicated highlight front/back/CSS template"
      contains: "Definition"
    - path: "src/multilang/services/card_template_loader.py"
      provides: "Source-profile-aware template loading and field-reference validation"
      exports: ["CardTemplate", "load_card_template", "validate_template_references"]
    - path: "tests/services/test_card_template_loader.py"
      provides: "Automated template contract tests"
  key_links:
    - from: "HIGHLIGHT_CARD_TEMPLATE.md"
      to: "src/multilang/domain/exporting.py"
      via: "field references match HIGHLIGHT_EXPORT_CARD_FIELD_NAMES"
      pattern: "Word.*Example Sentence.*Definition"
    - from: "src/multilang/services/card_template_loader.py"
      to: "src/multilang/domain/source_profiles.py"
      via: "get_source_profile(source_type).template_name"
      pattern: "template_name"
---

<objective>
Create the dedicated highlight card template and a reusable loader/validator that guarantees the template references only fields exported by the highlight note type.

Purpose: Phase 13 must ship highlight-only study cards without changing normal frequency or manual word-list templates.
Output: `HIGHLIGHT_CARD_TEMPLATE.md`, `card_template_loader.py`, and template contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-highlight-export-and-template/13-CONTEXT.md
@.planning/phases/13-highlight-export-and-template/13-RESEARCH.md
@CARD_TEMPLATE.md
@src/multilang/domain/source_profiles.py
@src/multilang/domain/exporting.py

<interfaces>
From `src/multilang/domain/source_profiles.py`:
```python
SourceType = Literal["frequency", "word-list", "kindle-highlights"]
@dataclass(frozen=True, slots=True)
class SourceProfile:
    source_type: SourceType
    exports_translation_field: bool
    note_type_name: str
    template_name: str
def get_source_profile(source_type: str) -> SourceProfile: ...
```

From `src/multilang/domain/exporting.py`:
```python
HIGHLIGHT_EXPORT_CARD_FIELD_NAMES = (
    "SortIndex", "Word", "IPA", "word_audio", "Example Sentence",
    "sentence_audio", "Definition", "Image",
)
def export_field_names_for_source_type(source_type: str) -> tuple[str, ...]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define source-aware template loader contracts</name>
  <files>src/multilang/services/card_template_loader.py, tests/services/test_card_template_loader.py</files>
  <behavior>
    - Test 1: `load_card_template(source_type="frequency")` returns the current `CARD_TEMPLATE.md` sections and accepts normal fields including `Translation`.
    - Test 2: `load_card_template(source_type="kindle-highlights")` loads the highlight template and rejects any `{{Translation}}`, raw highlight, book metadata, or unknown exported-field reference per D-14.
    - Test 3: `validate_template_references(template, field_names=HIGHLIGHT_EXPORT_CARD_FIELD_NAMES)` passes for allowed Anki helpers (`{{FrontSide}}`, conditionals like `{{#Image}}`) and fails with a clear `ExportAnkiPackageError`-compatible `ValueError` for dangling fields.
  </behavior>
  <action>Create `CardTemplate(front: str, back: str, css: str, source_template_name: str)` plus `load_card_template(source_type: str) -> CardTemplate` and `validate_template_references(template: CardTemplate, field_names: tuple[str, ...]) -> None`. Move the existing markdown section parser logic into this module, preserve the existing `CARD_TEMPLATE.md` parser for `frequency` and `word-list` per D-18, and route `kindle-highlights` to `HIGHLIGHT_CARD_TEMPLATE.md` via `get_source_profile(source_type).template_name`. Validation must parse `{{Field}}`, `{{#Field}}`, and `{{/Field}}`; allow only `FrontSide` as a non-field Anki helper; and fail if a template references `Translation`, raw highlight text, book metadata, source paths, private import records, or any field not in the resolved field tuple per D-14.</action>
  <verify>
    <automated>python -m pytest tests/services/test_card_template_loader.py -q</automated>
  </verify>
  <done>Template loading is source-profile-driven, normal templates still parse, highlight validation fails closed for dangling/private/Translation references, and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author dedicated highlight front/back/CSS template</name>
  <files>HIGHLIGHT_CARD_TEMPLATE.md, tests/services/test_card_template_loader.py</files>
  <behavior>
    - Test 1: Front contains `{{Word}}`, conditional `{{#IPA}}{{IPA}}{{/IPA}}`, `{{word_audio}}`, `{{Example Sentence}}`, `{{sentence_audio}}`, and conditional `{{#Image}}...{{Image}}...{{/Image}}` per D-01 through D-03.
    - Test 2: Back contains `{{FrontSide}}`, one answer divider, a visible `Definition` label, and `{{Definition}}`, with no new `word_audio`, `sentence_audio`, autoplay, or `Translation` references per D-05 through D-08.
    - Test 3: CSS centers the shell, uses Multilang blue variables, keeps comfortable spacing, sets max-width/responsive width, allows vertical overflow, and prevents horizontal overflow per D-09 through D-12.
  </behavior>
  <action>Create `HIGHLIGHT_CARD_TEMPLATE.md` with the same markdown section headings as `CARD_TEMPLATE.md`: `## Front Template`, `## Back Template`, and `## Styling (CSS)`. The front must use prompt-side content only: `Word`, `IPA`, `word_audio`, `Example Sentence`, `sentence_audio`, and optional `Image` per D-01/D-02/D-03. Keep labels minimal per D-04. The back must be `{{FrontSide}}`, then an answer divider, then a `Definition` answer block with a clear `Definition` label per D-05/D-06. Render multiple definitions as a bullet list when the exported field contains `<br>` separators by using a small inline script or CSS-compatible structure that does not introduce private fields; do not repeat audio or add autoplay beyond the `FrontSide` content per D-07/D-08. Use centered, responsive, comfortable Multilang-blue CSS with `max-width`, `margin: 0 auto`, `overflow-y: auto`, and `overflow-x: hidden` per D-09 through D-12. Do not alter `CARD_TEMPLATE.md` or phonetics files.</action>
  <verify>
    <automated>python -m pytest tests/services/test_card_template_loader.py -q</automated>
  </verify>
  <done>Highlight template validates against exact highlight fields, omits `Translation` and private/raw highlight references, preserves blank-image conditional rendering, and leaves normal template behavior untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template file → APKG model | Template references become executable Anki card markup. |
| private highlight provenance → template | Raw reading text/book/path metadata must not be exposed in cards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | I | `HIGHLIGHT_CARD_TEMPLATE.md` | mitigate | Validate template references against `HIGHLIGHT_EXPORT_CARD_FIELD_NAMES` and assert no `Translation`, raw highlight, book metadata, paths, or private import fields. |
| T-13-02 | T | `card_template_loader.py` | mitigate | Fail closed on dangling fields instead of silently packaging invalid templates. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_card_template_loader.py -q`.
</verification>

<success_criteria>
The dedicated highlight template exists, validates against exact highlight fields, implements D-01 through D-12 and D-14/D-18, and all loader/template tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/13-highlight-export-and-template/13-01-SUMMARY.md`.
</output>
