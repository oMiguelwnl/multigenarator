---
phase: 13-highlight-export-and-template
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/multilang/services/export_anki_package.py
  - tests/services/test_export_anki_package.py
autonomous: true
requirements: [EXPORT-01, EXPORT-03]
must_haves:
  truths:
    - "User can export highlight rows to APKG with the dedicated highlight note type and exact highlight fields."
    - "User receives a clear failure before APKG writing when highlight media is missing or mismatched."
    - "Existing frequency and word-list APKG templates remain unchanged."
  artifacts:
    - path: "src/multilang/services/export_anki_package.py"
      provides: "APKG model selection, validated template loading, and media packaging"
      exports: ["build_multilang_model", "export_anki_package"]
    - path: "tests/services/test_export_anki_package.py"
      provides: "APKG highlight template/media/regression tests"
  key_links:
    - from: "src/multilang/services/export_anki_package.py"
      to: "src/multilang/services/card_template_loader.py"
      via: "load_card_template(source_type=source_type)"
      pattern: "load_card_template"
    - from: "src/multilang/services/export_anki_package.py"
      to: "genanki.Package.media_files"
      via: "validated word_audio and sentence_audio references"
      pattern: "package.media_files"
---

<objective>
Wire the dedicated highlight template into APKG model creation while preserving existing normal/manual model behavior and fail-closed media safety.

Purpose: Highlight APKG exports must be importable and use the requested study template without leaking into other source modes.
Output: Updated APKG template loading and package tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-highlight-export-and-template/13-CONTEXT.md
@.planning/phases/13-highlight-export-and-template/13-RESEARCH.md
@.planning/phases/13-highlight-export-and-template/13-01-SUMMARY.md
@HIGHLIGHT_CARD_TEMPLATE.md
@src/multilang/services/card_template_loader.py
@src/multilang/services/export_anki_package.py
@tests/services/test_export_anki_package.py

<interfaces>
From `src/multilang/services/export_anki_package.py`:
```python
def build_multilang_model(*, source_type: str = "frequency") -> genanki.Model: ...
def build_multilang_note(row: ExportCardRow, *, model: genanki.Model | None = None) -> genanki.Note: ...
def export_anki_package(*, rows: list[ExportCardRow], media_index: dict[str, Path], output_path: Path, deck_name: str) -> ExportAnkiPackageResult: ...
```

Research-backed genanki shape:
```python
genanki.Model(model_id, name, fields=[{"name": "Field"}], templates=[{"qfmt": "...", "afmt": "..."}], css="...")
package = genanki.Package(deck)
package.media_files = ["file.mp3"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Use validated source-profile templates for model creation</name>
  <files>src/multilang/services/export_anki_package.py, tests/services/test_export_anki_package.py</files>
  <behavior>
    - Test 1: `build_multilang_model(source_type="kindle-highlights")` uses `HIGHLIGHT_MODEL_ID`, `Multilang::Highlight Card`, exact highlight fields, highlight qfmt/afmt/css, and no `Translation` reference per D-13/D-14/D-18.
    - Test 2: `build_multilang_model()` and `build_multilang_model(source_type="word-list")` still use normal `CARD_TEMPLATE.md` content and retain `Translation` behavior per D-17/D-18.
    - Test 3: A malformed highlight template with a dangling field causes a clear export/template error before a model is returned.
  </behavior>
  <action>Replace the local `_SECTION_RE`/`_load_project_card_template()` implementation with the Plan 13-01 `load_card_template(source_type=source_type)` result. Preserve the public `build_multilang_model()` signature. Convert `CardTemplate` to the existing `{"front", "back", "css"}` usage or update call sites directly. Ensure highlight source type gets `HIGHLIGHT_MODEL_ID`, `Multilang::Highlight Card`, and `HIGHLIGHT_EXPORT_CARD_FIELD_NAMES`; normal and manual modes must keep their existing note type names, fields, and normal template per D-17/D-18. Do not apply the highlight template to `word-list` decks; that idea is deferred.</action>
  <verify>
    <automated>python -m pytest tests/services/test_export_anki_package.py tests/services/test_card_template_loader.py -q</automated>
  </verify>
  <done>APKG model creation is source-profile-aware, highlight models use the dedicated validated template, normal/manual models are unchanged, and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove highlight APKG media and mixed-source failures</name>
  <files>tests/services/test_export_anki_package.py, src/multilang/services/export_anki_package.py</files>
  <behavior>
    - Test 1: `export_anki_package()` with a `kindle-highlights` row packages both `word_audio` and `sentence_audio` files and writes an APKG using the highlight model per D-15.
    - Test 2: Missing word audio, missing sentence audio, invalid `[sound:...]`, or media basename mismatch fails before `output_path` exists per D-15.
    - Test 3: Mixed highlight/frequency or highlight/word-list rows fail with the existing clear mixed-source note-model error per D-13/D-17.
  </behavior>
  <action>Extend APKG tests with highlight rows using `source_type="kindle-highlights"`. Keep the existing `_resolve_media_files()` fail-closed behavior and tighten it only if tests show the output file can be written after media validation fails. Assert the output path is absent after missing/mismatched media failures. Keep mixed-source rejection before model/package writing per D-13. Do not weaken current media basename checks.</action>
  <verify>
    <automated>python -m pytest tests/services/test_export_anki_package.py -q</automated>
  </verify>
  <done>Highlight APKG export packages safe word/sentence audio references, fails before writing broken packages, rejects mixed-source rows, and preserves existing source modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export rows → APKG package | Source-specific rows become importable Anki notes and media. |
| filesystem media paths → packaged media | Local audio files are included in the APKG. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | T | `build_multilang_model` | mitigate | Resolve model/template through `SourceProfile`; reject dangling/private fields via loader validation. |
| T-13-04 | I | `export_anki_package` | mitigate | Reject mixed source rows before packaging to prevent note-model collisions or highlight/private contract leakage. |
| T-13-05 | D | `_resolve_media_files` | mitigate | Validate both word and sentence audio references before `Package.write_to_file()` so broken APKGs are not written. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_export_anki_package.py tests/services/test_card_template_loader.py -q`.
</verification>

<success_criteria>
Highlight APKG exports use the dedicated note type/template/fields, safe media is packaged, broken media or mixed-source rows fail closed, and frequency/manual APKG behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/13-highlight-export-and-template/13-02-SUMMARY.md`.
</output>
