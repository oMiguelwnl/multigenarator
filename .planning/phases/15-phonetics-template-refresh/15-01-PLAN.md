---
phase: 15-phonetics-template-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/russian_phoneme_card.md
  - src/multilang/services/russian_phoneme_deck.py
  - tests/services/test_russian_phoneme_deck.py
autonomous: true
requirements: [PHON-01, PHON-02, PHON-03]
must_haves:
  truths:
    - "User sees phonetics fronts with Spellings, Sound, letter audio, Example Word, word audio, Word Translation, Example Sentence, and sentence audio in the provided layout."
    - "User sees Sentence Translation revealed on the phonetics back using Multilang colors."
    - "User receives phonetics note fields with no Notes, is_priming, is_sentence, Definitions, image, typo-only, or other unused template fields."
    - "Existing Russian phonetics letter, word, and sentence audio references remain renderable after the template refresh."
  artifacts:
    - path: "templates/russian_phoneme_card.md"
      provides: "Refreshed phonetics front/back/CSS template"
      contains: "Sentence Translation"
    - path: "src/multilang/services/russian_phoneme_deck.py"
      provides: "Phonetics model field contract and card field mapping"
      exports: ["PHONEME_FIELD_NAMES", "RussianPhonemeCard", "build_russian_phoneme_model", "build_russian_phoneme_note"]
    - path: "tests/services/test_russian_phoneme_deck.py"
      provides: "Focused phonetics template and field contract tests"
  key_links:
    - from: "src/multilang/services/russian_phoneme_deck.py"
      to: "templates/russian_phoneme_card.md"
      via: "_load_russian_phoneme_template markdown parser"
      pattern: "russian_phoneme_card\.md"
    - from: "PHONEME_FIELD_NAMES"
      to: "templates/russian_phoneme_card.md"
      via: "Anki field references in qfmt/afmt"
      pattern: "{{(Spellings|Sound|letter_audio|Example Word|word_audio|Word Translation|Example Sentence|sentence_audio|Sentence Translation)}}"
---

<objective>
Refresh the Russian phonetics note contract and template so exported phonetics cards match the supplied layout and reveal sentence translation on the back.

Purpose: Phase 15 is isolated from normal and highlight deck templates, but the phonetics deck must now use the requested field set, Multilang colors, and preserved audio slots.
Output: Updated phonetics markdown template, model field tuple, card field mapping, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@alter_organizado.md
@templates/russian_phoneme_card.md
@src/multilang/services/russian_phoneme_deck.py
@tests/services/test_russian_phoneme_deck.py
@.planning/phases/08-card-quality-refresh/08-04-SUMMARY.md

<interfaces>
From src/multilang/services/russian_phoneme_deck.py:
```python
PHONEME_FIELD_NAMES = (
    "SortIndex", "Spellings", "IPA", "letter_audio", "Example Word", "word_audio",
    "Word Translation", "Definitions", "Exemple Sentence", "sentence_audio", "Translation", "image",
)

@dataclass(frozen=True)
class RussianPhonemeCard:
    sort_index: int
    letters: str
    ipa: str
    example_word: str
    example_word_translation: str
    example_sentence: str
    example_sentence_translation: str
    definitions: str | None = None
```

Required replacement contract for this plan:
```python
PHONEME_FIELD_NAMES = (
    "Spellings",
    "Sound",
    "letter_audio",
    "Example Word",
    "word_audio",
    "Word Translation",
    "Example Sentence",
    "sentence_audio",
    "Sentence Translation",
)
```
Keep `sort_index` on `RussianPhonemeCard` for deterministic ordering and GUID generation, but do not export it as an Anki note field because it is not part of the refreshed user-facing phonetics template.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the refreshed phonetics field and reference contract</name>
  <files>tests/services/test_russian_phoneme_deck.py</files>
  <behavior>
    - Test 1: `build_russian_phoneme_model()` exposes exactly `Spellings`, `Sound`, `letter_audio`, `Example Word`, `word_audio`, `Word Translation`, `Example Sentence`, `sentence_audio`, `Sentence Translation` in that order for PHON-03.
    - Test 2: model front references the PHON-01 fields from the supplied layout and does not reference `Notes`, `is_priming`, `is_sentence`, `Definitions`, `image`, `IPA`, `Exemple Sentence`, or `Translation`.
    - Test 3: model back uses `{{FrontSide}}` and reveals `Sentence Translation` with a deterministic element/script or equivalent back-only reveal behavior for PHON-02.
    - Test 4: a synthetic `RussianPhonemeCard` carrying `[sound:letter.mp3]`, `[sound:word.mp3]`, and `[sound:sentence.mp3]` preserves those exact audio values in the exported `letter_audio`, `word_audio`, and `sentence_audio` fields for PHON-03.
  </behavior>
  <action>Update the existing focused test file before production changes. Replace assertions tied to the old `IPA`, `Definitions`, misspelled `Exemple Sentence`, `Translation`, and `image` fields with the refreshed PHON-01/PHON-02/PHON-03 contract. Add a helper card with explicit audio refs so the test proves the refreshed template keeps the existing Russian phonetics audio field names renderable instead of only preserving empty strings.</action>
  <verify>
    <automated>uv run pytest tests/services/test_russian_phoneme_deck.py::test_build_russian_phoneme_model_uses_intro_template -q</automated>
  </verify>
  <done>The focused test fails before implementation for the refreshed exact field tuple, template references, back reveal, and audio ref preservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the refreshed phonetics model and template</name>
  <files>templates/russian_phoneme_card.md, src/multilang/services/russian_phoneme_deck.py, tests/services/test_russian_phoneme_deck.py</files>
  <behavior>
    - Test 1: old unused fields are absent from the note model and all generated note field values align with the refreshed PHONEME_FIELD_NAMES tuple.
    - Test 2: front template follows the supplied `customCard cardBack` structure from `alter_organizado.md` for Spellings/Sound/audio/example sections while replacing the old `{{hint:Sentence Translation}}` front reveal with back-only reveal behavior required by PHON-02.
    - Test 3: CSS uses Multilang color variables/palette instead of the old Russian-only palette while keeping mobile-safe max width, padding, divider, header, target sound, example word, sentence translation, and audio button styling.
  </behavior>
  <action>Update `PHONEME_FIELD_NAMES` to the exact refreshed tuple from the context block. Rename the dataclass sound attribute or map existing `ipa` into the exported `Sound` field without leaving an exported `IPA` field. Add optional `letter_audio`, `word_audio`, and `sentence_audio` dataclass fields defaulting to empty strings and map them into the same-named exported fields. Rename exported sentence fields to `Example Sentence` and `Sentence Translation`. Rewrite `templates/russian_phoneme_card.md` front using the provided phonetics layout from `alter_organizado.md` lines 197-254, remove the `{{#Notes}}` block entirely, remove `Definitions` and `image` sections, and make `Sentence Translation` hidden on front and revealed on back via `{{FrontSide}}` plus a small script or equivalent Anki-compatible reveal. Refresh CSS to use the Multilang card colors established for deck templates while preserving existing audio button class names so Russian phonetics audio remains playable.</action>
  <verify>
    <automated>uv run pytest tests/services/test_russian_phoneme_deck.py -q</automated>
  </verify>
  <done>All Russian phoneme deck tests pass; generated notes use only refreshed fields; front/back/CSS satisfy PHON-01, PHON-02, and PHON-03 without touching `CARD_TEMPLATE.md` or `HIGHLIGHT_CARD_TEMPLATE.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template markdown→genanki model | Local markdown template content becomes Anki HTML/CSS in exported packages. |
| card data→template fields | Static Russian phoneme content and audio refs become rendered Anki field values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `templates/russian_phoneme_card.md` | mitigate | Focused tests assert forbidden field references are absent and required PHON-01/PHON-02 references are present. |
| T-15-02 | Denial of Service | `_load_russian_phoneme_template` | mitigate | Keep the existing parser failure behavior so malformed template markdown raises before package generation. |
| T-15-03 | Information Disclosure | phonetics card content | accept | Static Russian phoneme examples contain no user secrets or private reading text; no external input crosses this boundary. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_russian_phoneme_deck.py -q` and confirm no normal/highlight template files changed.
</verification>

<success_criteria>
PHON-01/PHON-02/PHON-03 are implemented in the phonetics-only model/template path: supplied front layout, back-only Sentence Translation reveal, Multilang colors, no unused fields, and preserved letter/word/sentence audio slots.
</success_criteria>

<output>
After completion, create `.planning/phases/15-phonetics-template-refresh/15-01-SUMMARY.md`.
</output>
