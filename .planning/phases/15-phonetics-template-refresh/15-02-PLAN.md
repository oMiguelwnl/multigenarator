---
phase: 15-phonetics-template-refresh
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - tests/integration/test_russian_phoneme_template_refresh_flow.py
  - tests/services/test_russian_phoneme_deck.py
  - src/multilang/services/russian_phoneme_deck.py
  - templates/russian_phoneme_card.md
  - .planning/phases/15-phonetics-template-refresh/15-PHONETICS-TEMPLATE-EVIDENCE.md
autonomous: false
requirements: [PHON-01, PHON-02, PHON-03]
must_haves:
  truths:
    - "User has automated export evidence that refreshed phonetics APKG generation still works."
    - "User has evidence that the refreshed phonetics note model contains no unused or forbidden fields/references."
    - "User can visually verify the supplied front layout, back Sentence Translation reveal, Multilang colors, and audio controls in Anki."
  artifacts:
    - path: "tests/integration/test_russian_phoneme_template_refresh_flow.py"
      provides: "Integration regression coverage for refreshed phonetics export"
    - path: ".planning/phases/15-phonetics-template-refresh/15-PHONETICS-TEMPLATE-EVIDENCE.md"
      provides: "Phase 15 phonetics evidence for Phase 16 audit input"
    - path: "templates/russian_phoneme_card.md"
      provides: "Final visual template checked by automated tests and human verification"
  key_links:
    - from: "export_russian_phoneme_deck"
      to: "templates/russian_phoneme_card.md"
      via: "genanki model/package creation"
      pattern: "build_russian_phoneme_model\(\)"
    - from: "tests/integration/test_russian_phoneme_template_refresh_flow.py"
      to: "PHONEME_FIELD_NAMES"
      via: "exact model field and forbidden-reference assertions"
      pattern: "PHONEME_FIELD_NAMES"
---

<objective>
Add export-level regression evidence and a focused human verification gate for the refreshed phonetics template.

Purpose: The template refresh is visual and Anki-facing, so automated APKG/field/reference evidence must be paired with a user presentation check before Phase 16 audit work depends on it.
Output: Integration test coverage, evidence markdown, and human verification instructions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@alter_organizado.md
@templates/russian_phoneme_card.md
@src/multilang/services/russian_phoneme_deck.py
@tests/services/test_russian_phoneme_deck.py
@.planning/phases/15-phonetics-template-refresh/15-01-SUMMARY.md

<interfaces>
This plan consumes the refreshed contracts created by Plan 15-01:
```python
PHONEME_FIELD_NAMES = (
    "Spellings", "Sound", "letter_audio", "Example Word", "word_audio",
    "Word Translation", "Example Sentence", "sentence_audio", "Sentence Translation",
)

def build_russian_phoneme_model() -> genanki.Model: ...
def build_russian_phoneme_note(card: RussianPhonemeCard, *, model: genanki.Model | None = None) -> genanki.Note: ...
def export_russian_phoneme_deck(*, output_path: Path, deck_name: str = DEFAULT_RUSSIAN_PHONEME_DECK_NAME) -> RussianPhonemeDeckExportResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phonetics export refresh integration coverage</name>
  <files>tests/integration/test_russian_phoneme_template_refresh_flow.py, tests/services/test_russian_phoneme_deck.py, src/multilang/services/russian_phoneme_deck.py, templates/russian_phoneme_card.md</files>
  <behavior>
    - Test 1: `export_russian_phoneme_deck()` writes an APKG and reports the full static Russian phoneme card count after the refreshed field/schema change.
    - Test 2: the model field names equal `PHONEME_FIELD_NAMES` and the rendered template references are a subset of `PHONEME_FIELD_NAMES + {FrontSide}`.
    - Test 3: forbidden references `Notes`, `is_priming`, `is_sentence`, `Definitions`, `image`, `IPA`, `Exemple Sentence`, and `Translation` do not occur in qfmt or afmt.
    - Test 4: a synthetic note with all three `[sound:...]` values proves letter, word, and sentence audio refs occupy the correct fields and appear in the visible front template.
  </behavior>
  <action>Create a new integration test that exercises the APKG boundary and template reference contract after Plan 15-01. Reuse or add a local regex helper to extract Anki `{{...}}` references from qfmt/afmt; treat `FrontSide` as the only allowed non-field helper. If this test exposes missed production/template adjustments, fix only the phonetics-specific files listed here and keep normal/highlight deck template files untouched.</action>
  <verify>
    <automated>uv run pytest tests/services/test_russian_phoneme_deck.py tests/integration/test_russian_phoneme_template_refresh_flow.py -q</automated>
  </verify>
  <done>Focused service and integration tests prove refreshed phonetics export, field references, forbidden-reference removal, and audio ref preservation.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 15 phonetics template evidence</name>
  <files>.planning/phases/15-phonetics-template-refresh/15-PHONETICS-TEMPLATE-EVIDENCE.md</files>
  <action>Create an evidence markdown file containing: the exact command `uv run pytest tests/services/test_russian_phoneme_deck.py tests/integration/test_russian_phoneme_template_refresh_flow.py -q`, the pass/fail result, the final `PHONEME_FIELD_NAMES` tuple, the forbidden field/reference list checked, the generated APKG smoke result from the integration test, and a note that `CARD_TEMPLATE.md` and `HIGHLIGHT_CARD_TEMPLATE.md` were intentionally not modified. Do not include real audio files, private paths, WebDAV data, raw highlights, or secrets.</action>
  <verify>
    <automated>uv run pytest tests/services/test_russian_phoneme_deck.py tests/integration/test_russian_phoneme_template_refresh_flow.py -q</automated>
  </verify>
  <done>Evidence file exists and gives Phase 16 a safe, reproducible summary for PHON-01, PHON-02, and PHON-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The agent refreshed the phonetics-only Russian Anki template, preserved letter/word/sentence audio fields, removed unused/forbidden fields, generated automated APKG evidence, and wrote Phase 15 evidence.</what-built>
  <how-to-verify>
    1. Have the agent provide the temporary APKG path it generated during execution, or ask it to run `uv run python -c "from pathlib import Path; from multilang.services.russian_phoneme_deck import export_russian_phoneme_deck; print(export_russian_phoneme_deck(output_path=Path('.multilang/tmp/russian-phonemes-refresh-check.apkg')).output_path)"`.
    2. Import the APKG into Anki in a disposable profile.
    3. Open one Russian phonetics card front and confirm the supplied layout sections are visible: letter(s), sound, letter audio control, example word, word audio control, word translation, example sentence, and sentence audio control.
    4. Flip the card and confirm `Sentence Translation` appears on the back with Multilang colors and no `Notes`, `is_priming`, `is_sentence`, `Definitions`, or image-only section is visible.
    5. Confirm existing letter, word, and sentence audio controls are still displayed/clickable where media is present.
  </how-to-verify>
  <resume-signal>Type "approved" if the refreshed phonetics template looks correct, or describe the visual/audio issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| genanki package→Anki import | Generated APKG is opened by Anki and must not contain dangling template references. |
| evidence file→repository | Planning evidence is committed or shared and must avoid private paths, secrets, and live media. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | APKG template references | mitigate | Integration test extracts Anki references and fails if any reference is outside the refreshed field tuple plus FrontSide. |
| T-15-05 | Denial of Service | Anki import/rendering | mitigate | APKG smoke test verifies package creation with the full static card set after schema changes. |
| T-15-06 | Information Disclosure | `15-PHONETICS-TEMPLATE-EVIDENCE.md` | mitigate | Evidence action explicitly excludes real audio files, private paths, WebDAV data, raw highlights, and secrets. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_russian_phoneme_deck.py tests/integration/test_russian_phoneme_template_refresh_flow.py -q`, inspect `.planning/phases/15-phonetics-template-refresh/15-PHONETICS-TEMPLATE-EVIDENCE.md`, then complete the human Anki presentation check.
</verification>

<success_criteria>
Phase 15 has automated export/template evidence and a human-approved Anki presentation for the refreshed phonetics layout, Sentence Translation back reveal, unused-field removal, and preserved audio controls.
</success_criteria>

<output>
After completion, create `.planning/phases/15-phonetics-template-refresh/15-02-SUMMARY.md`.
</output>
