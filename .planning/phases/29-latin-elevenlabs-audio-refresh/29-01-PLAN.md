---
phase: 29-latin-elevenlabs-audio-refresh
plan: 01
type: execute
wave: 1
runtime: opencode
assurance: self_checked
depends_on:
  - 28-latin-export-and-milestone-evidence
files-modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - src/multilang/services/latin_audio.py
  - src/multilang/services/latin_audio_samples.py
  - src/multilang/services/latin_audio_generation.py
  - src/multilang/services/latin_audio_refresh.py
  - tests/services/test_latin_audio.py
  - tests/services/test_latin_audio_samples.py
  - tests/services/test_latin_audio_generation.py
  - tests/services/test_latin_audio_refresh.py
  - tests/services/test_latin_mvp.py
  - tests/cli/test_generate_latin_mvp_command.py
  - tests/integration/test_v20_latin_audio_asset.py
  - tests/integration/test_v20_latin_audio_evidence.py
  - tests/integration/test_v21_latin_google_tts_final_audio.py
  - tests/services/test_latin_export.py
  - tests/integration/test_v20_latin_export_evidence.py
  - tests/integration/test_v20_final_milestone_evidence.py
  - .planning/phases/29-latin-elevenlabs-audio-refresh/29-GOOGLE-TTS-FINAL-REVIEW.md
  - .planning/phases/29-latin-elevenlabs-audio-refresh/29-SECOND-PASS-REVIEW.md
  - .planning/STATE.md
  - LATIN-STRUCTURE.md
autonomous: true
requirements:
  - AUDR-01
  - AUDR-02
  - AUDR-03
  - AUDR-04
  - AUDR-05
non_goals:
  - Do not activate ElevenLabs as the final Latin provider in this phase; all configured ElevenLabs keys returned HTTP 402 Payment Required during sample generation.
  - Do not add FineVoice as an active provider or adapter in this phase.
  - Do not regenerate the frozen Latin source pack, translations, grammar fields, curation identities, or Anki field order.
  - Do not generate Latin scale beyond the approved 50-card MVP.
  - Do not uninstall eSpeak NG from the user's system environment.
hard_boundaries:
  - Preserve the committed Google Translate TTS (`la`) Latin MVP manifest/media unless validation proves a concrete mismatch.
  - Treat shipped Phase 22-28 planning artifacts as historical records; do not rewrite them to hide earlier eSpeak or ElevenLabs exploration.
  - Keep provider secrets and raw provider errors out of committed artifacts, CLI output, tests, and evidence.
  - Keep FineVoice research-only and ElevenLabs deferred unless a separate future plan proves billing, sample quality, and approval.
escalation_triggers:
  - Stop if making Google Translate TTS final requires changing Latin source text, Portuguese translations, grammar, card count, or Anki field order.
  - Stop if tests show the committed Google TTS media are missing, unplayable, unapproved, source-pack mismatched, or path-unsafe.
  - Stop if any task would make live provider calls or spend quota; this replan is an offline finalization path.
  - Stop before destructive cleanup outside repository files or any system-level eSpeak NG uninstall instruction.
approval_gates:
  - Ask before replacing committed audio media; this plan should validate the existing Google TTS media, not regenerate it by default.
  - Ask before removing an adapter used by non-Latin or future-provider flows.
  - Ask before changing roadmap requirements beyond this plan artifact.
anti_regression_targets:
  - Latin export stays approved-only and fails closed for missing, unapproved, stale, unsafe, or source-pack-mismatched audio.
  - Latin APKG packages 100 Google TTS MP3 media references and CSV/TSV field order remains stable.
  - Existing modern-language frequency, manual, highlight, phonetics, and provider adapter tests remain isolated from Latin final-provider work.
  - FineVoice remains research-only and ElevenLabs remains fallback/deferred, not the final provider for Phase 29.
known_unknowns:
  - Whether future paid ElevenLabs quota would sound better remains unvalidated and deferred.
  - Whether Google Translate TTS quality is sufficient beyond the current 50-card MVP remains outside this phase.
ui_proof_slots: []
no_ui_proof_rationale: Backend/data/export phase only; no visible UI route or component is claimed.
high_leverage_surfaces:
  - data/latin_mvp/latin-mvp-50-v1-audio.json
  - data/latin_mvp/audio/latin-mvp-50-v1/
  - src/multilang/services/latin_audio.py
  - src/multilang/services/latin_export.py
  - tests/integration/test_v20_latin_audio_evidence.py
  - tests/integration/test_v20_latin_export_evidence.py
second_pass_required: true
closure_claim_limit: Do not claim Phase 29 complete until Google TTS final-provider evidence, manifest/media readiness, export evidence, existing-mode regression, and active-provider cleanup checks all pass.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: The higher-quality ElevenLabs replacement goal is deferred because all configured keys returned HTTP 402 and cannot produce reviewable samples now.
  kept: Existing approved Google TTS manifest/media, Latin source/translation/grammar/export contracts, fail-closed audio validators, and provider fallback metadata.
  gained: A phase plan that matches the user's current provider decision and can complete offline without provider billing risk.
must_haves:
  truths:
    - Google Translate TTS (`la`) is explicitly recorded as the final approved provider for the current 50-card Latin MVP audio pack.
    - The committed Latin MVP manifest and 100 media files are approved, source-aligned, path-safe MP3 artifacts that do not depend on eSpeak NG.
    - FineVoice is research-only and ElevenLabs is fallback/deferred after HTTP 402, not an active final-provider requirement in this phase.
    - Latin audio and export tests fail closed against missing, unapproved, stale, unsafe, or source-pack-mismatched audio while passing with the Google TTS manifest.
    - Active source, tests, and current docs no longer require eSpeak NG or live ElevenLabs to complete current Latin export; historical archives may still mention them.
  artifacts:
    - path: .planning/phases/29-latin-elevenlabs-audio-refresh/29-GOOGLE-TTS-FINAL-REVIEW.md
      provides: Provider decision evidence for Google TTS finalization and ElevenLabs billing deferral.
    - path: tests/integration/test_v21_latin_google_tts_final_audio.py
      provides: Scanner-readable evidence for AUDR-01 through AUDR-05 under the Google TTS final-provider decision.
    - path: data/latin_mvp/latin-mvp-50-v1-audio.json
      provides: Approved 50-card Latin word/sentence audio manifest using Google TTS metadata.
    - path: data/latin_mvp/audio/latin-mvp-50-v1/
      provides: 100 committed playable Latin word/sentence MP3 media files.
    - path: .planning/phases/29-latin-elevenlabs-audio-refresh/29-SECOND-PASS-REVIEW.md
      provides: High-leverage final review over manifest, media, export, active planning contract, privacy, and regression evidence.
  key_links:
    - from: data/latin_mvp/latin-mvp-50-v1-audio.json
      to: src/multilang/services/latin_audio.py
      via: LatinAudioManifest validation and export-readiness checks.
    - from: src/multilang/services/latin_export.py
      to: data/latin_mvp/latin-mvp-50-v1-audio.json
      via: build_latin_export_rows loads and validates audio before export.
    - from: .planning/phases/29-latin-elevenlabs-audio-refresh/29-GOOGLE-TTS-FINAL-REVIEW.md
      to: tests/integration/test_v21_latin_google_tts_final_audio.py
      via: evidence asserts the provider decision and billing deferral are scanner-readable.
---

# Phase 29: Google TTS Final Latin Audio Provider - Plan 01

## Objective

Finalize Google Translate TTS (`la`) as the approved provider for the current 50-card Classical Latin MVP audio pack, record ElevenLabs as deferred because all configured keys returned `HTTP 402 Payment Required`, keep FineVoice research-only, and prove the existing Google TTS manifest/media/export path is fail-closed and learner-ready.

## Context

- User decision in this planning run: alter Phase 29 to use Google Translate TTS as the final Latin audio provider instead of continuing ElevenLabs promotion.
- `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` were updated by this planning pass so the active Phase 29 contract now matches Google TTS finalization instead of ElevenLabs promotion.
- `.planning/phases/27-latin-audio-policy-and-integrity/27-AUDIO-PLAYBACK-REVIEW.md` already records `selected_provider=google-translate-tts`, `selected_voice=la`, `pronunciation_policy=google_translate_latin`, and `playback_review_status=approved`.
- `data/latin_mvp/latin-mvp-50-v1-audio.json` already records 50 word and 50 sentence MP3 artifacts with `provider=google-translate-tts`.
- `docs/latin-real-data-provider-handoff.md` records Google Translate TTS (`la`) as audio primary, ElevenLabs Italian as fallback 1, Azure Italian as fallback 2, and FineVoice as research-only.
- A prior execution attempt against the old ElevenLabs-first plan failed at the live provider checkpoint because key1, key2, and key3 all returned `HTTP 402 Payment Required`.
- Current worktree contains dirty quick-task and partial Phase 29 execution changes; executors must preserve unrelated quick-task artifacts and reconcile any old ElevenLabs-first edits rather than reverting blindly.

<assurance_check>
source_artifact: .planning/phases/29-latin-elevenlabs-audio-refresh/29-01-PLAN.md
source_runtime: opencode
source_assurance: self_checked
current_runtime: opencode
current_assurance: unreviewed
status: lower_assurance_draft
warning: This draft intentionally replaces the previous ElevenLabs-first plan because the user selected Google TTS as final provider after live ElevenLabs billing failure.
</assurance_check>

## Requirements Covered

- AUDR-01
- AUDR-02
- AUDR-03
- AUDR-04
- AUDR-05

## Must-Haves

1. User can inspect scanner-readable provider decision evidence that finalizes Google Translate TTS and records ElevenLabs as deferred due billing/quota failure.
2. The committed 50-card Latin audio manifest and 100 media files are approved Google TTS MP3 artifacts that no longer depend on eSpeak NG.
3. FineVoice remains research-only and is not wired as an active provider.
4. Audio readiness and export evidence fail closed for stale eSpeak dependence, unapproved audio, text mismatch, unsafe paths, missing media, and source-pack drift.
5. Active project code/tests/current docs do not require eSpeak NG or live ElevenLabs for the current Latin MVP export, and no system uninstall instruction is introduced.

## Anti-Goals

- Do not make live ElevenLabs calls in execution for this plan.
- Do not promote ElevenLabs to final provider unless a future plan proves billing, samples, and approval.
- Do not activate FineVoice.
- Do not change Latin source-pack entries, Portuguese translations, grammar labels, card count, or export field order.
- Do not rewrite historical Phase 22-28 records that accurately describe earlier decisions.

## Hard Boundaries

- Keep the existing Google TTS manifest/media as the default source of truth unless validation finds a concrete issue.
- Keep provider credentials and raw HTTP responses out of committed artifacts.
- Keep existing modern-language providers and exports outside this phase except for focused regression checks.
- Keep system-level eSpeak NG installation untouched.

## Evidence Contract

- Provider review artifact records Google TTS final status, ElevenLabs HTTP 402 deferral, Azure fallback status, FineVoice research-only status, and non-uninstall posture.
- Integration evidence loads committed real manifest/media and proves all 50 word plus 50 sentence artifacts are approved, source-aligned, path-safe MP3 files using Google TTS metadata.
- Export evidence builds Latin APKG/CSV/TSV from committed assets and verifies 50 cards plus 100 packaged media references without field-order regression.
- Active dependency grep or targeted tests prove current source/tests/docs no longer require eSpeak NG or live ElevenLabs for Latin MVP export; historical archives may still mention them.
- Existing-mode regression tests remain green for non-Latin export modes.

## Common Pitfalls

- Leaving `elevenlabs-italian` as the final/current provider after the user chose Google TTS.
- Treating Google TTS finalization as permission to weaken export readiness checks.
- Rewriting historical eSpeak or ElevenLabs planning archives instead of current active docs/tests.
- Removing provider adapters used elsewhere just because Latin no longer depends on them.
- Claiming native/human Latin pronunciation quality beyond the approved 50-card MVP claim.

## Stop-And-Challenge

- Stop if committed Google TTS media are missing, empty, not MP3/ID3, source-mismatched, or unapproved.
- Stop if preserving Google TTS requires changing learner-facing card content or export schema.
- Stop if execution would call live providers, spend quota, or require new secrets.
- Stop if cleanup would delete adapters or files outside the active Phase 29 write set.

## Approval Gates

- Ask before regenerating or deleting committed audio media.
- Ask before removing any non-Latin provider adapter.
- Ask before editing roadmap/requirement text beyond the plan/evidence artifacts.

<checks>
<plan_check>
checker: native
checker_runtime: opencode
status: passed
blocking: false
notes: Native OpenCode gsdd-plan-checker passed on cycle 2 after active ROADMAP/REQUIREMENTS wording was aligned to Google TTS finalization and final second-pass review was moved to the closing task.
</plan_check>
</checks>

## Tasks

<task id="29-01" type="auto">
  <files>
    - MODIFY: .planning/ROADMAP.md
    - MODIFY: .planning/REQUIREMENTS.md
    - MODIFY: src/multilang/services/latin_audio.py
    - MODIFY: src/multilang/services/latin_audio_generation.py
    - MODIFY: src/multilang/services/latin_audio_refresh.py
    - MODIFY: tests/services/test_latin_audio.py
    - MODIFY: tests/services/test_latin_audio_generation.py
    - MODIFY: tests/services/test_latin_audio_refresh.py
  </files>
  <action>
    Reconcile the active planning and code contracts around Google Translate TTS as the final current provider. Ensure `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` use Google TTS finalization wording for Phase 29, then remove or downgrade any partial ElevenLabs-first current-provider behavior from the prior aborted execution so `elevenlabs-italian` is fallback/deferred, `azure-italian` remains fallback, and `finevoice` remains research-only. Keep any existing ElevenLabs helper code only if it is clearly optional/future-facing and cannot be mistaken for the active Phase 29 provider path.
  </action>
  <verify>
    - Run `uv run pytest tests/services/test_latin_audio.py tests/services/test_latin_audio_generation.py tests/services/test_latin_audio_refresh.py -q`
  </verify>
  <done>
    Active planning text and tests prove the Latin provider policy is Google TTS final, ElevenLabs is not required for current export, FineVoice is not active, and partial ElevenLabs-first drift from the aborted execution is removed or made explicitly deferred.
  </done>
</task>

<task id="29-02" type="auto">
  <files>
    - CREATE: .planning/phases/29-latin-elevenlabs-audio-refresh/29-GOOGLE-TTS-FINAL-REVIEW.md
    - MODIFY: src/multilang/services/latin_audio_samples.py
    - MODIFY: tests/services/test_latin_audio_samples.py
    - CREATE: tests/integration/test_v21_latin_google_tts_final_audio.py
    - MODIFY: tests/integration/test_v20_latin_audio_asset.py
    - MODIFY: tests/integration/test_v20_latin_audio_evidence.py
  </files>
  <action>
    Add scanner-readable provider decision evidence for Phase 29 that records Google Translate TTS (`la`) as final for the current 50-card MVP, records all three configured ElevenLabs keys as blocked by `HTTP 402 Payment Required` without leaking key values, keeps Azure Italian as fallback, and keeps FineVoice research-only. Add integration evidence that the committed manifest/media are approved Google TTS MP3 artifacts aligned to the source pack and that stale eSpeak or unapproved replacement states fail readiness.
  </action>
  <verify>
    - Run `uv run pytest tests/services/test_latin_audio_samples.py tests/integration/test_v21_latin_google_tts_final_audio.py tests/integration/test_v20_latin_audio_asset.py tests/integration/test_v20_latin_audio_evidence.py -q`
  </verify>
  <done>
    The review artifact exists, no provider secrets leak, Google TTS final status is executable evidence, ElevenLabs is deferred by billing evidence, and committed audio readiness remains fail-closed.
  </done>
</task>

<task id="29-03" type="auto">
  <files>
    - MODIFY: tests/services/test_latin_mvp.py
    - MODIFY: tests/cli/test_generate_latin_mvp_command.py
    - MODIFY: tests/services/test_latin_export.py
    - MODIFY: tests/integration/test_v20_latin_export_evidence.py
    - MODIFY: tests/integration/test_v20_final_milestone_evidence.py
  </files>
  <action>
    Update service, CLI, and export evidence so public audio summaries and Latin export checks assert the Google TTS final provider counts, 100 packaged MP3 media references, stable Latin field order, privacy safeguards, and existing-mode isolation.
  </action>
  <verify>
    - Run `uv run pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py tests/services/test_latin_export.py tests/integration/test_v20_latin_export_evidence.py tests/integration/test_v20_final_milestone_evidence.py -q`
    - Run `uv run multilang export-latin-mvp --format apkg --output-dir exports/latin_mvp`
    - Run `uv run multilang export-latin-mvp --format csv --output-dir exports/latin_mvp`
    - Run `uv run multilang export-latin-mvp --format tsv --output-dir exports/latin_mvp`
  </verify>
  <done>
    Latin export produces 50 notes with 100 Google TTS media references, CSV/TSV remain importable with stable field order, and public summaries show Google TTS provider counts.
  </done>
</task>

<task id="29-04" type="auto">
  <files>
    - MODIFY: .planning/STATE.md
    - MODIFY: LATIN-STRUCTURE.md
    - MODIFY: tests/integration/test_v21_latin_google_tts_final_audio.py
    - CREATE: .planning/phases/29-latin-elevenlabs-audio-refresh/29-SECOND-PASS-REVIEW.md
  </files>
  <action>
    Update active current-state documentation to say Latin MVP audio is finalized on Google Translate TTS for the 50-card deck, ElevenLabs is deferred due `HTTP 402 Payment Required`, FineVoice remains research-only, and eSpeak NG is historical/non-current only. Add or update evidence that active source, tests, and current docs do not require eSpeak NG or live ElevenLabs for the current Latin export path while preserving historical archive mentions. Write the final second-pass review artifact after all planning, source/test, export, grep, and regression checks are in their final Phase 29 state.
  </action>
  <verify>
    - Run `uv run pytest tests/integration/test_v21_latin_google_tts_final_audio.py tests/integration/test_v20_existing_modes_regression_evidence.py tests/integration/test_v13_existing_modes_regression_evidence.py -q`
    - Run `rg "espeak|eSpeak|espeak-ng|ElevenLabs" src tests LATIN-STRUCTURE.md .planning/STATE.md`
  </verify>
  <done>
    Current docs and active evidence identify Google TTS as final provider, any remaining ElevenLabs/eSpeak mentions are fallback/deferred or historical context only, existing-mode regression tests pass, and final second-pass evidence is up to date.
  </done>
</task>

## Verification

- Run `uv run pytest tests/services/test_latin_audio.py tests/services/test_latin_audio_generation.py tests/services/test_latin_audio_refresh.py tests/services/test_latin_audio_samples.py -q`.
- Run `uv run pytest tests/integration/test_v21_latin_google_tts_final_audio.py tests/integration/test_v20_latin_audio_asset.py tests/integration/test_v20_latin_audio_evidence.py -q`.
- Run `uv run pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py tests/services/test_latin_export.py tests/integration/test_v20_latin_export_evidence.py tests/integration/test_v20_final_milestone_evidence.py -q`.
- Run `uv run pytest tests/integration/test_v20_existing_modes_regression_evidence.py tests/integration/test_v13_existing_modes_regression_evidence.py -q`.
- Run `uv run multilang export-latin-mvp --format apkg --output-dir exports/latin_mvp` and confirm 50 cards plus 100 Google TTS media references.
- Run `rg "espeak|eSpeak|espeak-ng|ElevenLabs" src tests LATIN-STRUCTURE.md .planning/STATE.md` and confirm remaining active hits are fallback/deferred/historical context only.

## Success Criteria

- AUDR-01: Provider review artifact records Google TTS final status and ElevenLabs billing deferral; no live provider call is required.
- AUDR-02: Committed Latin audio manifest and 100 media files use approved Google TTS metadata and pass readiness without eSpeak NG dependence.
- AUDR-03: FineVoice has no active runtime/provider wiring.
- AUDR-04: Audio readiness and export evidence fail closed for stale, unsafe, missing, unapproved, or mismatched audio states and pass with the Google TTS assets.
- AUDR-05: Active project state no longer requires eSpeak NG or live ElevenLabs for current Latin export, historical/optional mentions are explicitly bounded, and no system uninstall instructions are introduced.

## High-Leverage Review

- Latin audio manifest/media and Latin export evidence are high-leverage surfaces because bad changes can silently corrupt learner-ready APKG media.
- The plan requires a second-pass review artifact after all export, active-doc cleanup, grep, and regression checks before claiming Phase 29 complete.

## Leverage Review

- Lost: The plan defers a potentially higher-quality ElevenLabs replacement because billing/quota prevents sample generation now.
- Kept: Existing approved Google TTS manifest/media, fail-closed audio readiness, Latin export shape, and existing-mode regression patterns.
- Gained: Provider decision honesty and an offline completion path that matches the user's current instruction.

## Notes

- Approach exploration skipped with `reduced_alignment` because `.planning/config.json` lacks `workflow.discuss: true`; the user supplied the locked provider decision directly.
- Native plan checking is required because `.planning/config.json` uses `workflow.plan_check: true`.
- The active roadmap and requirement prose now say Google TTS finalization; historical shipped artifacts may still mention earlier eSpeak or ElevenLabs exploration.
- Current worktree dirty state includes quick-task artifacts and partial old Phase 29 execution edits; executors must preserve unrelated quick-task files and reconcile overlapping source/test files deliberately.
