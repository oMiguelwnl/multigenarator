---
phase: 30-korean-contracts-and-morphology
plan: "01"
type: execute
wave: 1
runtime: opencode
assurance: self_checked
depends_on: []
files-modified:
  - pyproject.toml
  - uv.lock
  - src/multilang/domain/jobs.py
  - src/multilang/settings.py
  - scripts/build_frequency_assets.py
  - tests/domain/test_jobs.py
  - tests/test_settings.py
  - tests/services/test_korean_language_support.py
  - tests/services/test_frequency_decks.py
autonomous: true
requirements: [KMODE-01, KMODE-02, KNLP-01]
non_goals:
  - Do not create, seed, download, or commit a Korean frequency asset.
  - Do not implement morphology, persistence, provider, audio, template, or export behavior in this plan.
  - Do not add a Korean-only source type or isolated pipeline.
hard_boundaries:
  - The sole request/settings identity introduced here is `ko`; `ko-KR` is never accepted as a language value.
  - Selectable languages and approved committed frequency-asset languages are separate explicit capabilities.
  - An explicit Korean asset operation fails before creating directories or files.
escalation_triggers:
  - Stop before Task 30-01-01 if the Mandarin quick 027 execution-head preflight cannot prove commit ancestry, required technical artifacts, and a clean shared write set; escalate the baseline conflict instead of editing, stashing, resetting, or absorbing it.
  - Stop if the exact Kiwi pins cannot resolve on Python 3.12 instead of substituting an analyzer or broad version range.
  - Stop if registering `ko` requires a Korean data asset or changes an existing language/source contract.
approval_gates:
  - Explicit developer approval is required before committing any redistributed Korean lexical/frequency data.
anti_regression_targets:
  - Every existing language/default remains present exactly once.
  - Existing frequency build/check behavior remains deterministic for the approved committed asset set.
  - Non-Korean startup does not import or construct Kiwi eagerly.
known_unknowns:
  - Korean source, attribution, and redistribution terms are unresolved and assigned to Phase 32.
ui_proof_slots: []
no_ui_proof_rationale: Dependency, registry, and licensing-gate work only; no visible UI outcome is claimed.
high_leverage_surfaces:
  - pyproject.toml
  - uv.lock
  - src/multilang/domain/jobs.py
  - src/multilang/settings.py
  - scripts/build_frequency_assets.py
second_pass_required: true
high_leverage_second_pass: Recheck locks, enum/default exhaustiveness, approved-asset iteration, and no-write Korean failure after focused tests pass.
closure_claim_limit: This plan establishes exact dependencies, canonical selectability, and the frequency license gate only; it does not claim morphology or a working Korean generation flow.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Enum exhaustiveness can no longer stand in for downstream asset capability.
  kept: Shared modern requests, typed settings, source modes, and deterministic asset tooling.
  gained: Reproducible analyzer installation and an explicit legal boundary around selectable Korean.
must_haves:
  truths:
    - `GenerationRequest` accepts exactly `ko` for frequency, word-list, and kindle-highlight source types and rejects `ko-KR`.
    - Settings expose `ko` once without treating it as an approved committed frequency asset.
    - The environment locks exactly `kiwipiepy==0.23.2` and `kiwipiepy-model==0.23.0`.
    - Build-all excludes Korean and an explicit Korean asset operation fails before filesystem mutation.
  artifacts:
    - path: pyproject.toml
      provides: Exact direct Kiwi analyzer/model requirements.
    - path: src/multilang/domain/jobs.py
      provides: `SupportedLanguage.KO = "ko"`.
    - path: src/multilang/settings.py
      provides: Separate selectable and approved-frequency language tuples.
    - path: scripts/build_frequency_assets.py
      provides: Actionable Korean license gate before writes.
  key_links:
    - from: src/multilang/settings.py
      to: scripts/build_frequency_assets.py
      via: Builder iterates the approved committed asset tuple, which excludes `ko`.
    - from: src/multilang/domain/jobs.py
      to: tests/domain/test_jobs.py
      via: Three-mode acceptance and locale rejection contract tests.
---

# Phase 30: Korean Contracts and Morphology - Plan 01

## Objective

<objective>
Install the locked Kiwi pair and register canonical Korean selectability while retaining the explicit redistribution block on Korean frequency data.

Purpose: Every later plan depends on one stable `ko` identity and reproducible local analyzer packages, but technical routing must not imply corpus approval.

Output: Exact dependency lock, canonical request/settings registration, and approved-frequency capability separation.
</objective>

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
</execution_context>

## Context

<context>
@AGENTS.md
@.planning/SPEC.md
@.planning/ROADMAP.md
@.planning/phases/30-korean-contracts-and-morphology/30-APPROACH.md
@.planning/phases/30-korean-contracts-and-morphology/30-RESEARCH.md
@.planning/phases/30-korean-contracts-and-morphology/30-PATTERNS.md
@pyproject.toml
@src/multilang/domain/jobs.py
@src/multilang/settings.py
@scripts/build_frequency_assets.py

<interfaces>
```python
class SupportedLanguage(str, Enum):
    # existing values remain
    JA = "ja"
    ZH = "zh"

class GenerationRequest(BaseModel):
    language: SupportedLanguage
    source_type: Literal["frequency", "word-list", "kindle-highlights", "latin-mvp"]

DEFAULT_SUPPORTED_LANGUAGES: tuple[SupportedLanguageCode, ...]
```
</interfaces>
</context>

<assurance_check>
source_artifact: .planning/phases/30-korean-contracts-and-morphology/30-APPROACH.md
source_runtime: unknown
source_assurance: unknown
current_runtime: opencode
current_assurance: self_checked
status: passed
warning: none — cycle 3 passed in a fresh context within the same OpenCode runtime, so assurance is capped at `self_checked` rather than `cross_runtime_checked`.
</assurance_check>

## Requirements Covered

- **KMODE-01:** canonical `ko` request/settings identity across all three source types.
- **KMODE-02:** existing language/source/default and committed-asset behavior remains intact.
- **KNLP-01:** exact analyzer and model distributions are direct locked dependencies.

## Must-Haves

1. `ko` is selectable and `ko-KR` is not an internal alias.
2. Both Kiwi distributions are exact and reproducible.
3. Korean selectability cannot create or require an unapproved asset.

## Anti-Goals

- No Korean rows, corpus files, morphology code, voice, provider, template, or export changes.

## Hard Boundaries

- Use the exact approved pins.
- Keep three existing modern source types.
- Fail before asset filesystem side effects.

## Evidence Contract

- Dependency metadata and lock checks prove exact installed versions.
- Request/settings tests prove three-mode `ko` acceptance and `ko-KR` rejection.
- Asset tests prove approved-only build-all and direct Korean no-write failure.

## Common Pitfalls

- Appending `ko` to defaults while continuing to use defaults as the asset list.
- Pinning only analyzer code and allowing the model package to drift.
- Silently skipping an explicit Korean asset request instead of explaining the license gate.

## Stop-And-Challenge

- Stop on resolver/Python baseline mismatch or any need for Korean production data.

## Approval Gates

- No asset redistribution or publication is approved.

<checks>
<plan_check>
checker: fresh-context same-runtime
checker_runtime: opencode
status: passed
blocking: false
cycle_count: 3
notes: "Cycle 3 fresh-context same-runtime check passed; all prior blockers and warnings were closed. Assurance is `self_checked`, not `cross_runtime_checked`."
</plan_check>
</checks>

## Execution Preflight

<preflight id="30-01-PF-MANDARIN-027" gate="blocking">
  <name>Prove Mandarin quick 027 is reconciled on the current execution head</name>
  <files>
    - READ ONLY: .planning/quick/027-adicionar-mandarim-integrado/027-VERIFICATION.md
    - READ ONLY: src/multilang/services/mandarin_orthography.py
    - READ ONLY: alembic/versions/20260720_15_mandarin_export_fields.py
    - READ ONLY: tests/integration/test_mandarin_modern_flow.py
    - READ ONLY: Phase 30 / quick 027 shared write set listed in the commands below
  </files>
  <action>
    Run every automated command below before creating or modifying any Task 30-01-01 file. The ancestry check proves the technical Mandarin integration commit is contained by the exact execution `HEAD`; the `git cat-file` checks prove its required technical artifacts still exist on that head; the unstaged and staged diff checks prove there is no live conflicting change in the files both quick 027 and Phase 30 write. This gate deliberately does not read, require, or wait for quick 027's unrelated Anki Desktop/mobile human UI review. Do not mutate, stash, reset, clean, commit, or auto-merge another actor's work to make this gate pass.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor dc63333 HEAD</automated>
    <automated>git cat-file -e HEAD:.planning/quick/027-adicionar-mandarim-integrado/027-VERIFICATION.md</automated>
    <automated>git cat-file -e HEAD:src/multilang/services/mandarin_orthography.py</automated>
    <automated>git cat-file -e HEAD:alembic/versions/20260720_15_mandarin_export_fields.py</automated>
    <automated>git cat-file -e HEAD:tests/integration/test_mandarin_modern_flow.py</automated>
    <automated>git diff --quiet HEAD -- pyproject.toml uv.lock scripts/build_frequency_assets.py src/multilang/db/models.py src/multilang/domain/jobs.py src/multilang/runtime.py src/multilang/services/audio_voice_registry.py src/multilang/services/lexical_grounding.py src/multilang/services/provider_text_adapters.py src/multilang/services/tatoeba_sentence_source.py src/multilang/services/text_validation.py src/multilang/settings.py tests/domain/test_jobs.py tests/services/test_audio_voice_registry.py tests/services/test_frequency_decks.py tests/services/test_text_validation.py tests/test_migration_schema_parity.py tests/test_settings.py</automated>
    <automated>git diff --cached --quiet HEAD -- pyproject.toml uv.lock scripts/build_frequency_assets.py src/multilang/db/models.py src/multilang/domain/jobs.py src/multilang/runtime.py src/multilang/services/audio_voice_registry.py src/multilang/services/lexical_grounding.py src/multilang/services/provider_text_adapters.py src/multilang/services/tatoeba_sentence_source.py src/multilang/services/text_validation.py src/multilang/settings.py tests/domain/test_jobs.py tests/services/test_audio_voice_registry.py tests/services/test_frequency_decks.py tests/services/test_text_validation.py tests/test_migration_schema_parity.py tests/test_settings.py</automated>
  </verify>
  <done>`dc63333` is an ancestor of the current execution `HEAD`, all four technical Mandarin artifacts exist at `HEAD`, and the complete shared write set has neither staged nor unstaged changes; no human UI status is part of this prerequisite.</done>
  <on_failure>STOP before Task 30-01-01. Record `git rev-parse HEAD`, the failed command, and `git status --short` for the named shared write set, then escalate to the developer to reconcile or explicitly approve a new execution baseline. Do not continue Phase 30 and do not alter the conflicting work.</on_failure>
</preflight>

## Tasks

<tasks>

<task id="30-01-01" type="auto" tdd="true">
  <name>Lock the exact Kiwi analyzer and model distributions</name>
  <files>
    - CREATE: tests/services/test_korean_language_support.py
    - MODIFY: pyproject.toml
    - MODIFY: uv.lock
  </files>
  <behavior>
    - RED: package metadata requires analyzer `0.23.2` and model `0.23.0` before they are present.
    - GREEN: Python 3.12 imports both distributions at those exact versions from the locked environment.
    - REGRESSION: the executed interpreter proves `sys.version_info[:2] == (3, 12)`, project Python remains `>=3.12`, and the lock is consistent.
  </behavior>
  <action>
    Create dependency assertions first and run them to record RED under `uv run --python 3.12`. Add direct runtime requirements `kiwipiepy==0.23.2` and `kiwipiepy-model==0.23.0`, then regenerate `uv.lock` with uv. Verify the actual Python 3.12 interpreter, import both `kiwipiepy` and `kiwipiepy_model`, and assert both distribution/module versions rather than inferring compatibility from wheel metadata. Do not use broad ranges, optional extras, or transitive-only model installation because both versions are persisted analyzer evidence under the user-confirmed Kiwi decision. This task installs packages only; no project module may eagerly construct Kiwi.
  </action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/services/test_korean_language_support.py -q</automated>
    <automated>uv lock --check</automated>
    <automated>uv run --python 3.12 python -c "import sys, kiwipiepy, kiwipiepy_model; from importlib.metadata import version; assert sys.version_info[:2] == (3, 12), sys.version; assert version('kiwipiepy') == '0.23.2'; assert version('kiwipiepy-model') == '0.23.0'; assert kiwipiepy.__version__ == '0.23.2'; assert kiwipiepy_model.__version__ == '0.23.0'"</automated>
  </verify>
  <done>The exact code/model pair is declared, locked, and imported at the expected module/distribution versions by an asserted Python 3.12 interpreter, with a failing-then-passing contract test.</done>
</task>

<task id="30-01-02" type="auto" tdd="true">
  <name>Register canonical `ko` in requests and selectable settings</name>
  <files>
    - MODIFY: tests/domain/test_jobs.py
    - MODIFY: tests/test_settings.py
    - MODIFY: tests/services/test_korean_language_support.py
    - MODIFY: src/multilang/domain/jobs.py
    - MODIFY: src/multilang/settings.py
  </files>
  <behavior>
    - RED: frequency, word-list, and kindle-highlight requests accept `SupportedLanguage.KO` with value `ko`.
    - RED: `ko-KR` fails request and settings validation.
    - RED: defaults contain all prior values unchanged and `ko` exactly once.
  </behavior>
  <action>
    Write request/settings tests first and record RED. Add exactly `KO = "ko"`, one `"ko"` literal/default entry, and no locale alias or Korean-only source mode. Introduce an explicitly named tuple for approved committed frequency assets containing the shipped asset languages and excluding Korean. This implements `Canonical Korean Identity and Routing` and separates selectability from redistribution approval.
  </action>
  <verify>
    <automated>uv run pytest tests/domain/test_jobs.py tests/test_settings.py tests/services/test_korean_language_support.py -q</automated>
  </verify>
  <done>`ko` is the only accepted Korean request/settings identity for all three modern modes, existing values remain intact, and asset capability is separate.</done>
</task>

<task id="30-01-03" type="auto" tdd="true">
  <name>Enforce the Korean frequency redistribution gate</name>
  <files>
    - MODIFY: tests/services/test_korean_language_support.py
    - MODIFY: tests/services/test_frequency_decks.py
    - MODIFY: src/multilang/settings.py
    - MODIFY: scripts/build_frequency_assets.py
  </files>
  <behavior>
    - RED: build/check-all iterates approved committed assets and excludes `ko`.
    - RED: direct `ko` build/check raises an actionable license error before `mkdir`, wordfreq iteration, or CSV write.
    - REGRESSION: every approved existing asset still validates with its current rows and metadata.
  </behavior>
  <action>
    Add capability/no-write tests before implementation. Make build-all consume the approved tuple from settings. Guard a direct Korean request before `_build_language_asset` and any filesystem or `iter_wordlist` call; raise a deterministic content-free error explaining that source, attribution, and redistribution approval are required. Update the exhaustive asset test to iterate approved asset capability, not every selectable language. Do not create a silent skip or any `assets/frequency/ko` content.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_korean_language_support.py tests/services/test_frequency_decks.py -q</automated>
  </verify>
  <done>Build-all remains deterministic for approved assets and direct Korean asset operations fail actionably before writes.</done>
</task>

</tasks>

<source_audit>
GOAL: Covers canonical identity and license-safe selectability foundations for Roadmap criteria 1, 2, and 5.

REQ: KMODE-01 is covered by three-mode `ko` acceptance; KMODE-02 by exact existing contract/asset regressions; KNLP-01 by exact pins.

RESEARCH: Covers exact dependency versions, canonical-code registration, and the approved-frequency capability split.

CONTEXT: Implements `Canonical Korean Identity and Routing` and `Frequency License and Downstream Capability Gates`, including the retained 3000-entry redistribution block.

Excluded without gap: unsupported voice/Tatoeba boundaries and morphology are Plan 30-02; persistence, modes, validation, runtime, and closure are Plans 30-03 through 30-08.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Manifest -> installed analyzer | Third-party code/model enters trusted morphology evidence. |
| Request string -> enum/settings | Aliases could create split identity. |
| Asset language -> filesystem | A selection can trigger redistributable output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-30-01-01 | Tampering | dependencies | mitigate | Exact pins, installed metadata assertion, and lock check. |
| T-30-01-02 | Spoofing | language contracts | mitigate | Accept only `ko`; reject `ko-KR` in requests/settings. |
| T-30-01-03 | Tampering / Repudiation | asset builder | mitigate | Approved capability tuple and pre-write actionable license gate. |
</threat_model>

## Verification

- Pass the Mandarin quick 027 execution-head preflight before Task 30-01-01, run all task commands, and confirm no Korean asset directory/file exists.
- Preserve the successful `uv run --python 3.12` interpreter/import/version evidence with the plan summary.

## Success Criteria

- Mandarin quick 027 is technically reconciled on the execution head without requiring its unrelated human UI review; exact dependencies import under Python 3.12; `ko` alone is selectable; approved-only asset workflows remain green; direct Korean asset work fails before writes.

## High-Leverage Review

- Inspect lock, enum/default tuple, approved asset tuple, and no-write gate after tests pass.

## Leverage Review

- Lost: One shared tuple no longer represents both selectability and asset approval.
- Kept: Existing request and frequency tooling.
- Gained: Legally safe Korean routing foundation.

## Notes

- Research is complete in `30-RESEARCH.md`; stop only on factual package-resolution drift.

<output>
After completion, create `.planning/phases/30-korean-contracts-and-morphology/30-01-SUMMARY.md`.
</output>
