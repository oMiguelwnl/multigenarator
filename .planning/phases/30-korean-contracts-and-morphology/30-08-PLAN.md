---
phase: 30-korean-contracts-and-morphology
plan: "08"
type: execute
wave: 8
runtime: opencode
assurance: self_checked
depends_on: [30-07]
files-modified:
  - tests/integration/test_korean_modern_flow.py
  - tests/services/test_korean_language_support.py
autonomous: true
requirements: [KMODE-01, KMODE-02, KNLP-01, KNLP-02]
non_goals:
  - Do not modify production code merely to make closure tests pass; return to the owning plan or stop on a scope-boundary conflict.
  - Do not claim production frequency data, Korean audio, final templates, final APKG/import readiness, visual acceptance, or milestone completion.
  - Do not write a Korean production asset or make live provider calls.
hard_boundaries:
  - Integration uses disposable SQLite, reviewed/synthetic lexical fixtures, temporary frequency candidates, local files, real Kiwi where linguistic behavior matters, and offline provider/audio fakes.
  - Generic export field/tag behavior is verified without modifying exporter/template source and proves compatibility only.
  - Every production `ko-KR` occurrence must be an explicit locale constant/boundary; internal enum/job/run-key/path/cache/tag identity is `ko`.
escalation_triggers:
  - Stop if integration reveals missing production wiring outside the prior plans; revise the owning plan rather than hiding the gap in a test fake.
  - Stop if export proof requires Korean note types/templates or the separate Japanese snapshot repair.
  - Stop on a caused existing-mode regression; do not weaken or skip its assertion.
approval_gates:
  - Ask before any live call, quota spend, data publication, asset/voice approval, template/export change, or visual claim.
anti_regression_targets:
  - Existing modern frequency, custom-list, highlight, Japanese, Mandarin, Latin, phoneme, template, audio, persistence, and export tests remain green.
  - Dependency lock and migration parity remain green.
known_unknowns:
  - Production content/audio/export quality remains assigned to Phases 31-34 and cannot be inferred from these contract tests.
ui_proof_slots: []
no_ui_proof_rationale: Offline backend/integration and static artifact-contract evidence only; no rendered UI or Anki visual result is claimed.
high_leverage_surfaces:
  - tests/integration/test_korean_modern_flow.py
  - tests/services/test_korean_language_support.py
second_pass_required: true
high_leverage_second_pass: Trace one homograph and one private highlight end-to-end, review canonical scan allowlist, confirm exporter/template no-touch, then rerun focused and full suites.
closure_claim_limit: Phase closure may claim canonical routing, NFC/morphology contracts, durable source-backed identity, three-mode grounding, and fail-closed matching only; no later-phase learner-content, audio, template, export-readiness, visual, or milestone claim is permitted.
parallelism_budget:
  max_concurrent_plans: 1
  safe_parallelism: []
leverage:
  lost: Closure requires a broad regression run despite only two evidence files changing.
  kept: Existing production surfaces are tested without closure-only rewrites.
  gained: One scanner-readable offline integration and canonical-code gate covering every Phase 30 requirement and boundary.
must_haves:
  truths:
    - Offline frequency-fixture, word-list, and highlight flows use canonical `ko`, persist one identity shape, and survive commit/expire/reload.
    - Strict positive/negative matching and review status consume the persisted identity after reload.
    - Private highlights retain only hashes/indexes/canonical identity in public persistence.
    - Existing generic field sets, blank Image, reloaded language `ko`, and Anki tag `ko` work for representative source rows without Korean-specific export code.
    - Canonical scan, exact lock, migration/focused tests, named existing-mode matrix, and full suite all pass.
  artifacts:
    - path: tests/integration/test_korean_modern_flow.py
      provides: Offline three-mode routing, persistence, matching, privacy, and generic export/tag contract evidence.
    - path: tests/services/test_korean_language_support.py
      provides: Canonical `ko`/locale-only `ko-KR` scan and cross-boundary contract assertions.
  key_links:
    - from: three source-mode fixtures
      to: lexical_candidates.korean_identity
      via: Real shared runtime/ingestion services followed by session expiration and reload.
    - from: persisted Korean identity
      to: strict text validation
      via: Reloaded candidate drives match/review assertions.
    - from: representative generic rows
      to: existing exporter/tag implementation
      via: Static/offline artifact inspection proves `ko`, unchanged fields, and blank Image without source modification.
---

# Phase 30: Korean Contracts and Morphology - Plan 08

## Objective

<objective>
Close Phase 30 with offline end-to-end evidence for all three modes, canonical identity, persistence, strict matching, privacy, generic export compatibility, and complete existing-mode regression.

Purpose: Unit contracts are insufficient unless real wiring survives reload and adding Korean leaves established modes operational.

Output: Korean modern-flow integration test, canonical scan, focused/existing/full-suite evidence, and bounded closure claim.
</objective>

<execution_context>
@.agents/skills/gsdd-execute/SKILL.md
@.agents/skills/test-driven-development/SKILL.md
@.agents/skills/code-security/SKILL.md
@.agents/skills/llm-security/SKILL.md
</execution_context>

## Context

<context>
@AGENTS.md
@.planning/SPEC.md
@.planning/ROADMAP.md
@KOREAN-STRUCTURE.md
@.planning/phases/30-korean-contracts-and-morphology/30-APPROACH.md
@.planning/phases/30-korean-contracts-and-morphology/30-RESEARCH.md
@.planning/phases/30-korean-contracts-and-morphology/30-PATTERNS.md
@.planning/phases/30-korean-contracts-and-morphology/30-07-SUMMARY.md
@tests/integration/test_mandarin_modern_flow.py
@tests/integration/test_frequency_e2e_export_flow.py
@tests/integration/test_custom_word_list_e2e_export_flow.py
@tests/integration/test_highlight_generation_audio_flow.py
</context>

## Requirements Covered

- **KMODE-01:** complete three-mode and canonical-code proof.
- **KMODE-02:** focused/named/full existing-mode regression proof.
- **KNLP-01:** real analyzer, NFC, fingerprint, and persistence proof after reload.
- **KNLP-02:** inflection/homograph/fail-closed matching in integrated flow.

## Must-Haves

1. Three real orchestration slices share one durable identity.
2. Reloaded evidence controls acceptance.
3. Privacy survives public persistence/provider boundaries.
4. Generic export identity remains compatible without overclaim.
5. Every regression gate is green.

## Anti-Goals

- No production source, audio, template, UI, final export, or milestone evidence work.

## Hard Boundaries

- Fakes replace external/network/audio boundaries, not internal persistence or matcher wiring.
- Real Kiwi remains the positive linguistic authority.
- Temporary frequency candidates never enter `assets/frequency/ko`.
- Static export proof cannot be described as final Korean deck readiness.

## Evidence Contract

- Integration creates disposable DB/files, executes all three modes through project services, expires/reloads, and inspects stored identity/review/privacy results.
- Generic artifact inspection verifies existing field resolver, blank Image, and exact `ko` tag without modifying production exporters/templates.
- Canonical scan has a narrow explicit locale allowlist.
- Exact lock, focused Phase 30 tests, named existing-mode matrix, and full suite pass.

## Common Pitfalls

- Testing in-memory candidates without expiring the session.
- Mocking positive morphology instead of using real Kiwi.
- Treating failed Korean audio as a phase failure or registering a voice to avoid it.
- Allowlisting arbitrary `ko-KR` scan hits.
- Claiming final APKG/template readiness from generic tag/schema inspection.

## Stop-And-Challenge

- Stop and return to the owning plan for missing wiring; do not hide gaps in closure fixtures.
- Stop on any exporter/template/Japanese snapshot change request.
- Stop on caused regressions and diagnose them explicitly.

## Approval Gates

- No external call, asset, voice, delivery, or visual claim is approved.

<checks>
<plan_check>
checker: fresh-context same-runtime
checker_runtime: opencode
status: passed
blocking: false
cycle_count: 3
notes: "Cycle 3 fresh-context same-runtime check passed; all prior blockers and warnings were closed. Assurance is `self_checked`, not `cross_runtime_checked`."
</plan_check>
</checks>

## Tasks

<tasks>

<task id="30-08-01" type="auto" tdd="true">
  <name>Prove offline Korean three-mode identity, persistence, privacy, and generic tags</name>
  <files>
    - CREATE: tests/integration/test_korean_modern_flow.py
  </files>
  <behavior>
    - RED: temporary frequency candidates, NFD/inflected word-list input, and private highlights all route as `ko` and persist one typed identity shape.
    - RED: commit/`session.expire_all()`/reload preserves canonical lemma/POS/sense/signature/fingerprint and strict match/review outcomes.
    - RED: highlights retain one-syllable/attached morphology but public rows omit raw excerpt/path/context/token dump.
    - RED: representative generic source rows preserve existing fields, blank Image, reloaded `ko`, and exact Anki tag `ko` without Korean note-type assumptions.
  </behavior>
  <action>
    Create the integration test before any closure adjustment and run RED. Follow the disposable SQLite/offline fake structure of the Mandarin integration, but use real Kiwi-backed positive morphology and reviewed/synthetic lexical records with explicit senses. Exercise frequency by monkeypatching temporary candidates only, word-list with submitted NFD/inflected form and order, and highlights with one-syllable/particle/compound/private provenance. Use the actual runtime/ingestion/repository/validation wiring, then expire/reload before assertions. Use offline provider/audio fakes or the expected safe unapproved-audio state; never make a live call or register a Korean voice. Build representative existing-schema rows/artifacts to inspect source field sets, blank Image, and tag `ko`; do not modify or claim final exporters/templates.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_korean_modern_flow.py -q</automated>
  </verify>
  <done>All three modes prove canonical durable identity, strict post-reload matching, private-highlight safety, and generic `ko` traceability offline.</done>
</task>

<task id="30-08-02" type="auto" tdd="true">
  <name>Enforce canonical scan and complete anti-regression closure</name>
  <files>
    - MODIFY: tests/services/test_korean_language_support.py
  </files>
  <behavior>
    - RED: production scan rejects `ko-KR` outside the explicit Korean locale constant/provider boundary and rejects internal enum/job/run-key/path/cache/tag use.
    - RED: cross-contract assertions cover all three modes, Portuguese policy, no asset/voice/Tatoeba, exact dependencies, and runtime/tag `ko`.
    - REGRESSION: the named real-Kiwi compound projection/match smoke created in Plan 30-02 passes under an explicitly selected Python 3.12 interpreter.
    - REGRESSION: focused Phase 30, named existing-mode, lock, migration, and full-suite commands all pass.
  </behavior>
  <action>
    Add a scanner-readable canonical-code test before the final run. Scan production source/scripts, allow `ko-KR` only in the explicit locale constant or a verified provider-locale boundary, and fail on aliases, DB/job identity, run keys, source directories, lexical/cache keys, or tags. Keep tests' own rejection literals outside the production allowlist. Run the named real-Kiwi smoke from Plan 30-02 with `uv run --python 3.12` only after its test file and implementation exist through the dependency chain. Then run all Phase 30 focused files, exact lock/version checks, migration parity, the existing modern/Japanese/Mandarin/Latin/phoneme matrix, and the full suite. Fix only Phase 30-caused failures in the owning implementation; do not skip tests, weaken assertions, modify excluded export/template surfaces, or broaden closure claims. Perform the required second-pass traces after all tests are final.
  </action>
  <verify>
    <automated>uv run pytest tests/domain/test_jobs.py tests/test_settings.py tests/services/test_frequency_decks.py tests/services/test_audio_voice_registry.py tests/services/test_tatoeba_sentence_source.py tests/domain/test_korean.py tests/services/test_korean_morphology.py tests/services/test_korean_language_support.py tests/domain/test_lexicon.py tests/services/test_lexical_lookup.py tests/services/test_word_list_parser.py tests/services/test_input_fingerprint.py tests/repositories/test_lexical_repository.py tests/test_migration_schema_parity.py tests/services/test_lexical_grounding.py tests/services/test_highlight_candidate_extraction.py tests/services/test_highlight_import_preview.py tests/services/test_highlight_ingest_lexical_items.py tests/services/test_text_generation.py tests/services/test_provider_text_adapters.py tests/services/test_text_validation.py tests/services/test_generate_text_items.py tests/services/test_regenerate_text_item.py tests/test_runtime.py tests/cli/test_kindle_highlight_preview_command.py tests/cli/test_webdav_highlight_commands.py tests/integration/test_korean_modern_flow.py -q</automated>
    <automated>uv run pytest tests/integration/test_frequency_e2e_export_flow.py tests/integration/test_custom_word_list_e2e_export_flow.py tests/integration/test_highlight_generation_audio_flow.py tests/integration/test_v13_existing_modes_regression_evidence.py tests/services/test_japanese_furigana.py tests/services/test_assemble_export_cards.py::test_assemble_export_cards_builds_japanese_row_without_ipa tests/integration/test_mandarin_modern_flow.py tests/integration/test_v21_latin_google_tts_final_audio.py tests/services/test_russian_phoneme_deck.py -q</automated>
    <automated>uv run --python 3.12 pytest tests/services/test_korean_morphology.py -q -k test_real_kiwi_python312_smoke_projects_and_matches_compound_predicate</automated>
    <automated>uv lock --check</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>Canonical scan, exact lock, the focused real-Kiwi Python 3.12 smoke, all Phase 30 evidence, named existing-mode matrix, and full suite pass with closure limited to Phase 30 contracts/morphology.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Select Korean throughout modern pipeline | Plans 30-01, 30-04, 30-07, and Task 30-08-01 cover request/settings, all three modes, runtime, persistence, and tag identity. |
| GOAL | Deterministic NFC morphology | Plans 30-02 through 30-06 cover Unicode, exact analyzer, source identity, provider/cache canonicalization, and strict matching. |
| GOAL | No existing-mode regression | Plans 30-01/02/03/04/06/07 capture local targets; Task 30-08-02 runs named and full suites. |
| REQ | KMODE-01 | Canonical `ko`, locale-only `ko-KR`, three modes, durable/runtime/provider/cache identity, and tag scan. |
| REQ | KMODE-02 | Additive branches, lazy isolation, nullable schema, no-touch exports/templates, named matrix, full suite. |
| REQ | KNLP-01 | NFC/script contracts, exact pins/options, real goldens, source identity, persistence, cache boundary. |
| REQ | KNLP-02 | Ordered same-eojeol signatures, top-two consensus, inflection, homograph/negative boundaries, all fail-closed states. |
| RESEARCH | Stack/configuration | Plans 30-01/02 pin and fingerprint the verified stack/options. |
| RESEARCH | Identity/durability | Plans 30-03/04 implement source sense, nullable JSON, reload, and three modes. |
| RESEARCH | Provider/validation/runtime | Plans 30-05/06/07 implement cache, strict gate, and one adapter. |
| CONTEXT | Sole `ko`; boundary `ko-KR` | Canonical scan plus Plans 30-01/02 enforce it. |
| CONTEXT | Pinned Kiwi, NFC, source-backed identity | Plans 30-01 through 30-04 implement without alternatives. |
| CONTEXT | Both top-two analyses agree | Plans 30-02/04/06 enforce across analysis, grounding, highlights, and validation. |
| CONTEXT | Frequency redistribution blocked | Plan 30-01 plus temporary-only integration fixtures. |
| CONTEXT | Anti-regression and bounded claims | Every plan declares boundaries; this plan runs closure evidence and claim limit. |

No source item is unplanned. Exclusions are approved: Korean production frequency data, Hangul/pronunciation and grammar curricula, live Korean text/audio, final note/template/deck topology, final APKG/import/visual acceptance, milestone evidence, and the separate Japanese snapshot repair belong outside Phase 30.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local fixtures -> real orchestration/database | Evidence must exercise production wiring without becoming production data. |
| Private highlights -> public rows/artifacts | Raw reading data must remain private. |
| Static artifact test -> closure claim | Test evidence must not be promoted into final visual/export readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-30-08-01 | Tampering | integration fakes | mitigate | Fake only external boundaries; use real services, DB lifecycle, and Kiwi positives. |
| T-30-08-02 | Information Disclosure | highlight evidence | mitigate | Assert public rows/artifacts omit raw excerpt/path/context/token dumps. |
| T-30-08-03 | Spoofing | canonical scan | mitigate | Narrow production allowlist and exact DB/run/cache/tag assertions. |
| T-30-08-04 | Repudiation | closure evidence | mitigate | Scanner-readable tests plus explicit command matrix and bounded claim limit. |
</threat_model>

## Verification

- Run both task command sets in order and preserve actual failures for diagnosis.

## Success Criteria

- All five Roadmap success criteria and all four requirements have offline executable evidence, including the focused real-Kiwi Python 3.12 smoke; every existing-mode/full-suite gate passes; closure stays within Phase 30.

## High-Leverage Review

- Trace a POS homograph and private highlight from source through DB reload, request/cache, match/review, and generic identity.
- Confirm no production asset/voice/provider/export/template/UI was added and scan allowlist is narrow.

## Leverage Review

- Lost: Broad final regression cost.
- Kept: Production code remains owned by focused prior plans.
- Gained: Honest scanner-readable Phase 30 closure.

## Notes

- If a prior plan's implementation is missing, revise that plan/implementation; do not turn the integration test into a substitute architecture.

<output>
After completion, create `.planning/phases/30-korean-contracts-and-morphology/30-08-SUMMARY.md`.
</output>
