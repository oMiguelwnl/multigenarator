---
phase: 14-webdav-highlight-fetch-adapter
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - src/multilang/cli.py
  - tests/cli/test_generate_webdav_highlights_command.py
  - tests/integration/test_webdav_highlight_fetch_flow.py
  - .planning/phases/14-webdav-highlight-fetch-adapter/14-WEBDAV-EVIDENCE.md
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
must_haves:
  truths:
    - "User can run highlight generation from an explicit WebDAV remote path without first copying secrets into source code or CLI flags."
    - "Unchanged WebDAV content reuses content-hash import identity and reports reused/new counts through the existing highlight ingestion summary."
    - "Phase evidence proves WebDAV fetch, local parser handoff, idempotency, and privacy boundaries without real credentials or private highlights."
  artifacts:
    - path: "src/multilang/cli.py"
      provides: "Generate command WebDAV remote-path handoff"
      contains: "--webdav-remote-path"
    - path: "tests/cli/test_generate_webdav_highlights_command.py"
      provides: "Generate CLI WebDAV handoff tests"
    - path: "tests/integration/test_webdav_highlight_fetch_flow.py"
      provides: "Synthetic end-to-end WebDAV fetch-to-ingest proof"
    - path: ".planning/phases/14-webdav-highlight-fetch-adapter/14-WEBDAV-EVIDENCE.md"
      provides: "Scanner-readable Phase 14 evidence commands and pass signals"
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/webdav_highlight_fetch.py"
      via: "generate resolves webdav remote path into cached input_file"
      pattern: "webdav_remote_path.*fetch_export"
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/ingest_lexical_items.py"
      via: "GenerationRequest.input_file uses fetched cached_path for `kindle-highlights`"
      pattern: "input_file=.*cached_path"
---

<objective>
Wire WebDAV-fetched highlight exports into the shipped `generate --source highlights` path and record evidence.

Purpose: Fetching is useful only if cached content enters the same idempotent ingestion/generation path as local Kindle imports.
Output: Generate-command WebDAV handoff, integration tests, and Phase 14 evidence artifact.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-webdav-highlight-fetch-adapter/14-CONTEXT.md
@.planning/phases/14-webdav-highlight-fetch-adapter/14-RESEARCH.md
@src/multilang/cli.py
@src/multilang/services/ingest_lexical_items.py
@tests/services/test_highlight_ingest_lexical_items.py
@tests/integration/test_kindle_local_normalization_flow.py

<interfaces>
Existing generate request construction:
```python
request = GenerationRequest(
    language=language,
    source_type=internal_source,
    input_file=input_file,
)
```

Required new option behavior:
```python
webdav_remote_path: Path | None via typer.Option("--webdav-remote-path", help="Explicit WebDAV remote Kindle export path for --source highlights.")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generate WebDAV remote-path handoff</name>
  <files>src/multilang/cli.py, tests/cli/test_generate_webdav_highlights_command.py</files>
  <read_first>src/multilang/cli.py, src/multilang/services/webdav_highlight_fetch.py, tests/cli/test_generate_command.py, .planning/phases/14-webdav-highlight-fetch-adapter/14-CONTEXT.md</read_first>
  <behavior>
    - `generate --language es --source highlights --webdav-remote-path /dav/private/export.html` fetches explicit remote content, sets `GenerationRequest.input_file` to the private cached path, and continues through existing `kindle-highlights` ingestion, per D-12.
    - `--webdav-remote-path` is valid only with `--source highlights`, and is mutually exclusive with `--input-file`, per D-03 and D-05.
    - Missing WebDAV configuration fails before generation starts with `webdav_error=missing_config`, per D-04 and D-13.
    - No raw remote path, configured full URL, username, secret, or highlight text appears in generate output, per D-16.
  </behavior>
  <action>Add optional `webdav_remote_path: str | None = typer.Option(None, "--webdav-remote-path", help="Explicit WebDAV remote Kindle export path for --source highlights.")` to the existing `generate` command. Validate: if `webdav_remote_path is not None and source != "highlights"`, raise `typer.BadParameter("--webdav-remote-path is only valid when --source highlights")`; if both `input_file` and `webdav_remote_path` are set, raise `typer.BadParameter("--input-file and --webdav-remote-path are mutually exclusive")`; if `source == "highlights"` and `webdav_remote_path` is set, call `WebDAVHighlightFetchService.from_settings(Settings()).fetch_export(webdav_remote_path)` before constructing `GenerationRequest`, assign `input_file = fetch_result.cached_path`, and print `webdav_content_hash={fetch_result.content_hash}` plus `webdav_size_bytes={fetch_result.size_bytes}` before existing ingestion counts. Catch `WebDAVError` as in Plan 03 and exit 1. This implements D-03, D-04, D-05, D-11, D-12, D-13, D-15, and D-16.</action>
  <acceptance_criteria>
    - `src/multilang/cli.py` contains `--webdav-remote-path` and mutual-exclusion validation with `--input-file`.
    - `tests/cli/test_generate_webdav_highlights_command.py` asserts a fake WebDAV fetch sets a cached file path on the `GenerationRequest` received by the injected service.
    - `tests/cli/test_generate_webdav_highlights_command.py` asserts `--webdav-remote-path` with `--source word-list` fails and both `--input-file` plus `--webdav-remote-path` fail.
    - Generate command test output excludes sample `reader-secret`, `https://example.invalid`, `/dav/private/export.html`, and private sentence text.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cli/test_generate_webdav_highlights_command.py tests/cli/test_generate_command.py -q</automated>
  </verify>
  <done>`generate --source highlights` can fetch explicit WebDAV content into the existing local ingest path while preserving secret-free CLI behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove idempotent WebDAV fetch-to-ingest evidence</name>
  <files>tests/integration/test_webdav_highlight_fetch_flow.py, .planning/phases/14-webdav-highlight-fetch-adapter/14-WEBDAV-EVIDENCE.md</files>
  <read_first>tests/services/test_highlight_ingest_lexical_items.py, tests/integration/test_kindle_local_normalization_flow.py, src/multilang/services/ingest_lexical_items.py, .planning/phases/14-webdav-highlight-fetch-adapter/14-VALIDATION.md</read_first>
  <behavior>
    - Synthetic WebDAV bytes become a private cached file, parse through `parse_kindle_highlight_export`, and enter `IngestLexicalItemsService` with the same counts as a local `--input-file` import, per D-12.
    - Running the same WebDAV body twice yields the same content hash and reports `reused_existing_candidates > 0` and `newly_planned_candidates == 0` on the second run, per D-11 and D-15.
    - Evidence artifact lists commands and pass signals only; it contains no real credentials, raw remote paths with private metadata, raw response bodies, or raw highlight text, per D-10 and D-16.
  </behavior>
  <action>Create `tests/integration/test_webdav_highlight_fetch_flow.py` using synthetic Spanish Kindle text similar to `tests/services/test_highlight_ingest_lexical_items.py`, fake WebDAV transport from Plan 02, and in-memory SQLite service fixtures from existing highlight ingestion tests. Assert content hash stability, imported/rejected/extracted/duplicate/planned counts, and second-run reuse/new counts. Create `.planning/phases/14-webdav-highlight-fetch-adapter/14-WEBDAV-EVIDENCE.md` with frontmatter mapping `requirements: [INGEST-01, INGEST-02]`, commands to run, expected pass signals, and a privacy checklist. Do not include raw synthetic highlight sentence in the evidence file; use placeholder `[synthetic highlight text redacted]`. This implements D-10, D-11, D-12, D-15, and D-16.</action>
  <acceptance_criteria>
    - `tests/integration/test_webdav_highlight_fetch_flow.py` contains assertions for `reused_existing_items` or `reused_existing_candidates`, `newly_planned_candidates == 0`, and matching `content_hash` values across two fetches.
    - `.planning/phases/14-webdav-highlight-fetch-adapter/14-WEBDAV-EVIDENCE.md` contains `INGEST-01`, `INGEST-02`, `uv run pytest tests/integration/test_webdav_highlight_fetch_flow.py`, and `[synthetic highlight text redacted]`.
    - `.planning/phases/14-webdav-highlight-fetch-adapter/14-WEBDAV-EVIDENCE.md` does not contain `reader-secret`, `https://example.invalid/dav/private`, or raw Kindle highlight prose.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_webdav_highlight_fetch_flow.py tests/services/test_highlight_ingest_lexical_items.py -q</automated>
  </verify>
  <done>Phase 14 has automated evidence that WebDAV-fetched content follows the existing parser/ingest path and reruns are content-hash idempotent without leaking private data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI remote path→generation request | Explicit remote selection changes the input file used by generation. |
| test/evidence artifacts→repository | Synthetic fixture outputs and planning evidence become committable artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | T | `generate --webdav-remote-path` | mitigate | Enforce source-only and mutual-exclusion validation before request creation. |
| T-14-12 | R | Idempotent import summaries | mitigate | Assert second run reports reused candidates and zero newly planned candidates. |
| T-14-13 | I | `14-WEBDAV-EVIDENCE.md` | mitigate | Evidence uses command names, hashes/counts, and `[synthetic highlight text redacted]` only. |
</threat_model>

<verification>
Run `uv run pytest tests/cli/test_generate_webdav_highlights_command.py tests/integration/test_webdav_highlight_fetch_flow.py tests/services/test_highlight_ingest_lexical_items.py -q`.
</verification>

<success_criteria>
`generate --source highlights --webdav-remote-path ...` fetches explicit WebDAV content into the existing local highlight ingestion path, second runs reuse content-derived identity, and evidence proves privacy-safe behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/14-webdav-highlight-fetch-adapter/14-04-SUMMARY.md`.
</output>
