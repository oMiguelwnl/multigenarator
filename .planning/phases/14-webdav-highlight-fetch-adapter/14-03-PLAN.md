---
phase: 14-webdav-highlight-fetch-adapter
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - src/multilang/cli.py
  - tests/cli/test_webdav_highlight_commands.py
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
must_haves:
  truths:
    - "User can run a listing command that prints supported remote candidates as safe key=value lines."
    - "User can run a fetch command with an explicit remote path and receive safe hash/count summaries from the existing local preview flow."
    - "CLI errors for missing config, auth, path, network, malformed, unsupported, and empty source are distinct and redacted."
  artifacts:
    - path: "src/multilang/cli.py"
      provides: "Typer commands for WebDAV list and fetch"
      contains: "list-webdav-highlights"
    - path: "tests/cli/test_webdav_highlight_commands.py"
      provides: "CLI tests for WebDAV list/fetch output and privacy"
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/webdav_highlight_fetch.py"
      via: "WebDAVHighlightFetchService.from_settings(Settings())"
      pattern: "WebDAVHighlightFetchService"
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/highlight_import_preview.py"
      via: "fetch command calls build_highlight_import_preview on cached_path"
      pattern: "build_highlight_import_preview\(.*cached_path"
---

<objective>
Expose safe WebDAV listing and explicit fetch/preview through the CLI.

Purpose: Users need operator commands that avoid secret flags, print safe summaries, and reuse existing local preview semantics.
Output: `list-webdav-highlights` and `fetch-webdav-highlights` commands with CLI tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-webdav-highlight-fetch-adapter/14-CONTEXT.md
@.planning/phases/14-webdav-highlight-fetch-adapter/14-RESEARCH.md
@src/multilang/cli.py
@src/multilang/services/webdav_highlight_fetch.py
@src/multilang/services/highlight_import_preview.py
@tests/cli/test_kindle_highlight_preview_command.py

<interfaces>
Existing CLI pattern:
```python
@cli.command("preview-kindle-highlights")
def preview_kindle_highlights(...):
    typer.echo(f"imported_highlights={preview.imported_highlights}")
```

Service calls from Plan 02:
```python
service = WebDAVHighlightFetchService.from_settings(Settings())
candidates = service.list_exports()
fetch_result = service.fetch_export(remote_path)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add safe WebDAV listing CLI command</name>
  <files>src/multilang/cli.py, tests/cli/test_webdav_highlight_commands.py</files>
  <read_first>src/multilang/cli.py, src/multilang/services/webdav_highlight_fetch.py, tests/cli/test_kindle_highlight_preview_command.py, .planning/phases/14-webdav-highlight-fetch-adapter/14-CONTEXT.md</read_first>
  <behavior>
    - `list-webdav-highlights` prints one candidate per line using `candidate=<safe_name> suffix=<suffix> size_bytes=<n-or-empty> modified_at=<value-or-empty>`.
    - Listing output never contains configured full WebDAV URL, username, secret, raw remote href, query strings, or private book metadata, per D-06 and D-16.
    - No `--webdav-username` or `--webdav-secret` options exist, per D-02 and D-03.
    - `WebDAVError` codes print as `webdav_error=<code>` and exit 1.
  </behavior>
  <action>In `create_app`, import `WebDAVHighlightFetchService`, `WebDAVError`, and `Settings`. Add command `list-webdav-highlights` with no secret options. It should instantiate `WebDAVHighlightFetchService.from_settings(Settings())`, call `list_exports()`, and print stable key=value candidate lines. Add a test seam that monkeypatches the service factory or allows `create_app` injection without disrupting existing tests; keep default behavior unchanged. Catch `WebDAVError` and print exactly `webdav_error={exc.code.value}` plus optional `webdav_error_detail={safe message}` if helpful, then `raise typer.Exit(code=1)`. This implements D-02, D-03, D-06, D-08, D-13, and D-16.</action>
  <acceptance_criteria>
    - `src/multilang/cli.py` contains `@cli.command("list-webdav-highlights")`.
    - `tests/cli/test_webdav_highlight_commands.py` asserts candidate output includes `candidate=`, `suffix=.html`, and excludes `reader-secret`, `https://example.invalid`, and raw `/dav/private/` paths.
    - `src/multilang/cli.py` does not contain `--webdav-secret` or `--webdav-username`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cli/test_webdav_highlight_commands.py tests/test_settings.py -q</automated>
  </verify>
  <done>Users can safely list supported WebDAV Kindle exports without exposing secrets or raw remote paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit WebDAV fetch and preview CLI command</name>
  <files>src/multilang/cli.py, tests/cli/test_webdav_highlight_commands.py</files>
  <read_first>src/multilang/cli.py, src/multilang/services/highlight_import_preview.py, src/multilang/services/webdav_highlight_fetch.py, tests/cli/test_kindle_highlight_preview_command.py</read_first>
  <behavior>
    - `fetch-webdav-highlights --language es --remote-path <path>` fetches only the explicit remote path, per D-05 and D-08.
    - Successful output includes `webdav_content_hash=`, `webdav_cached_file=`, `imported_highlights=`, `extracted_candidates=`, `rejected_highlights=`, `duplicate_candidates=`, and `planned_cards=`, per D-12 and D-15.
    - Fetch output never contains raw remote path, full URL, credentials, raw response body, or raw highlight text, per D-16.
    - Empty source and unsupported format produce distinct `webdav_error=empty_source` and `webdav_error=unsupported_format` lines, per D-13.
  </behavior>
  <action>Add command `fetch-webdav-highlights` with options `--language`, `--remote-path`, and optional `--planned-card-limit`. Do not add username/secret options. The command calls `service.fetch_export(remote_path)`, then passes `fetch_result.cached_path` to `build_highlight_import_preview(language=language, planned_card_limit=planned_card_limit)`. Print `webdav_content_hash={fetch_result.content_hash}`, `webdav_cached_file={fetch_result.cached_path}`, `webdav_size_bytes={fetch_result.size_bytes}`, followed by the same preview count lines as `preview-kindle-highlights`. Catch `WebDAVError` and preview `ValueError` as safe failures; for preview parser errors, print `webdav_error=malformed_response` unless the message contains `empty`, in which case print `webdav_error=empty_source`. This implements D-05, D-08, D-09, D-12, D-13, D-15, and D-16.</action>
  <acceptance_criteria>
    - `src/multilang/cli.py` contains `@cli.command("fetch-webdav-highlights")`, `--remote-path`, and `build_highlight_import_preview` in the command body.
    - `tests/cli/test_webdav_highlight_commands.py` asserts success output includes `webdav_content_hash=`, `imported_highlights=`, and `planned_cards=`.
    - `tests/cli/test_webdav_highlight_commands.py` asserts errors for empty source and unsupported format use distinct `webdav_error=` codes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cli/test_webdav_highlight_commands.py tests/cli/test_kindle_highlight_preview_command.py -q</automated>
  </verify>
  <done>Users can explicitly fetch one WebDAV export, cache it privately, and see the same count-only preview semantics as local Kindle imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args→fetch service | User-provided non-secret remote path enters URL resolution and fetch. |
| service/cache→terminal | Cached path, content hash, and preview counts are displayed to user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | I | `list-webdav-highlights` | mitigate | Print safe candidate name/metadata only; tests assert raw URL/secret/path absence. |
| T-14-09 | I | `fetch-webdav-highlights` | mitigate | Print content hash and `.multilang` cache path only; no raw remote path or body. |
| T-14-10 | R | CLI failures | mitigate | Emit stable `webdav_error=<code>` for each failure category. |
</threat_model>

<verification>
Run `uv run pytest tests/cli/test_webdav_highlight_commands.py tests/cli/test_kindle_highlight_preview_command.py -q`.
</verification>

<success_criteria>
Separate list and fetch commands exist, require no secret flags, use explicit remote selection, and print only safe key=value outputs plus local preview counts.
</success_criteria>

<output>
After completion, create `.planning/phases/14-webdav-highlight-fetch-adapter/14-03-SUMMARY.md`.
</output>
