---
phase: 25-latin-review-gates-and-curated-records
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - src/multilang/services/latin_review.py
  - data/latin_mvp/latin-mvp-50-v1-curation.json
  - tests/integration/test_v20_latin_review_curation_asset.py
autonomous: true
requirements: [REV-01, REV-02, REV-03]
must_haves:
  truths:
    - "User can inspect one curated review record for each of the 50 Latin MVP source-pack entries."
    - "User can see source, translation, grammar, and audio readiness gates on every curated record."
    - "User can inspect rejection, replacement, and uncertainty reasons while original source and frequency provenance remain present."
  artifacts:
    - path: "data/latin_mvp/latin-mvp-50-v1-curation.json"
      provides: "Committed 50-record Latin curation asset keyed to the frozen source pack"
      contains: "latin-mvp-0050"
    - path: "tests/integration/test_v20_latin_review_curation_asset.py"
      provides: "Integration evidence that curation asset covers 50 records and preserves provenance/gates"
    - path: "src/multilang/services/latin_review.py"
      provides: "Loader for curated Latin review records"
      exports: ["load_latin_curated_records"]
  key_links:
    - from: "data/latin_mvp/latin-mvp-50-v1-curation.json"
      to: "data/latin_mvp/latin-mvp-50-v1.json"
      via: "same ordered item_key sequence and source_pack_version"
      pattern: "latin-mvp-0001.*latin-mvp-0050"
---

<objective>
Commit the first reviewable Latin MVP curation asset and validate it against the Phase 23 source pack and Phase 25 review contract.

Purpose: Review states must exist on concrete records, not only in abstract models, before CLI inspection and approval workflows can be useful.
Output: 50-record curation JSON plus loader-backed integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-02-SUMMARY.md
@.planning/phases/25-latin-review-gates-and-curated-records/25-01-SUMMARY.md
@src/multilang/services/latin_review.py
@src/multilang/services/latin_source_pack.py
@data/latin_mvp/latin-mvp-50-v1.json

<interfaces>
From Plan 25-01 expected exports:
```python
class LatinCuratedRecord(BaseModel): ...
def summarize_latin_review_records(records: list[LatinCuratedRecord]) -> LatinReviewSummary: ...
def assert_latin_records_export_ready(records: list[LatinCuratedRecord]) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add curated-record loader and source-pack cross-checks</name>
  <files>src/multilang/services/latin_review.py, tests/integration/test_v20_latin_review_curation_asset.py</files>
  <behavior>
    - Test 1: `load_latin_curated_records()` returns 50 `LatinCuratedRecord` models from the default curation path.
    - Test 2: loader raises `ValueError` for malformed JSON or invalid review states.
    - Test 3: curation records match source-pack item keys, sequence, source_pack_version, lemma, target_form, frequency rank, source type, citation, and license note.
  </behavior>
  <action>Add `DEFAULT_LATIN_MVP_CURATION_PATH = Path("data") / "latin_mvp" / "latin-mvp-50-v1-curation.json"` and `load_latin_curated_records(path: Path | None = None, source_pack_path: Path | None = None)`. The loader must parse JSON, validate a list of `LatinCuratedRecord`, load the Phase 23 source pack with `load_latin_mvp_source_pack`, and fail if count, order, identity, or provenance fields differ. Do not silently fill missing provenance from the source pack; missing/mismatched curation data must fail so the committed asset stays auditable.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_review_curation_asset.py -q</automated>
  </verify>
  <done>Loader validates the curation JSON against the source pack and fails closed on malformed data or provenance drift.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the 50-record Latin MVP curation asset</name>
  <files>data/latin_mvp/latin-mvp-50-v1-curation.json, tests/integration/test_v20_latin_review_curation_asset.py</files>
  <action>Create `data/latin_mvp/latin-mvp-50-v1-curation.json` as a JSON array of exactly 50 records in `latin-mvp-0001` through `latin-mvp-0050` order. For each record, copy source/frequency/provenance fields exactly from `data/latin_mvp/latin-mvp-50-v1.json`. Set `source_gate.status` to `approved` because Phase 23 license/source validation is complete; set `grammar_gate.status` according to the Phase 24 grammar handoff once Phase 24 exists, and if Phase 24 is not present when executing, stop and report the phase dependency instead of inventing grammar approvals. Set `translation_gate.status` and `audio_gate.status` to `needs_review` with concrete reasons `translation_pending_phase_26` and `audio_pending_phase_27`; these gates must block export now. Include `replacement_reason` and `uncertainty_reason` fields as `null` unless the source-pack rationale identifies a replacement/uncertainty reason. Do not remove or compress original source/frequency provenance.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_review_curation_asset.py -q</automated>
  </verify>
  <done>The curation asset loads as 50 records, preserves source/frequency provenance, exposes four gates per record, and is not export-ready while translation/audio gates remain unapproved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON→loader | Hand-authored curation JSON can drift from the source pack. |
| Phase 24 handoff→curation asset | Grammar approvals must come from actual resolved morphology, not assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Tampering | load_latin_curated_records | mitigate | Cross-check every curated record against Phase 23 source-pack identity and provenance. |
| T-25-05 | Repudiation | curation reasons | mitigate | Require reasons for non-approved gates and keep replacement/uncertainty fields explicit. |
| T-25-06 | Information Disclosure | curation JSON | mitigate | Store only public source-pack provenance and review reasons; do not add private paths or provider secrets. |
</threat_model>

<verification>
Run `python -m pytest tests/integration/test_v20_latin_review_curation_asset.py -q`.
</verification>

<success_criteria>
Concrete curated records exist for all 50 Latin MVP items and remain tied to source-pack provenance while blocking export until all gates are approved.
</success_criteria>

<output>
After completion, create `.planning/phases/25-latin-review-gates-and-curated-records/25-02-SUMMARY.md`.
</output>
