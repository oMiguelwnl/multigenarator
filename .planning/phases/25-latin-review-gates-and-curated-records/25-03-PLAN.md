---
phase: 25-latin-review-gates-and-curated-records
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - src/multilang/services/latin_review.py
  - src/multilang/cli.py
  - tests/cli/test_generate_latin_mvp_command.py
autonomous: true
requirements: [REV-01, REV-03]
must_haves:
  truths:
    - "User can inspect Latin MVP review gate counts from the CLI."
    - "User can update a specific source, translation, grammar, or audio gate to needs_review, approved, or rejected with a reason when required."
    - "User cannot accidentally overwrite already approved curated fields without an explicit force flag."
  artifacts:
    - path: "src/multilang/cli.py"
      provides: "Latin review CLI commands"
      contains: "review-latin-mvp"
    - path: "src/multilang/services/latin_review.py"
      provides: "Review update/write helpers with approval overwrite protection"
      exports: ["update_latin_review_gate", "write_latin_curated_records"]
    - path: "tests/cli/test_generate_latin_mvp_command.py"
      provides: "CLI regression coverage for Latin review commands and existing generate-latin-mvp output"
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/latin_review.py"
      via: "review-latin-mvp command loads, summarizes, updates, and writes curated records"
      pattern: "load_latin_curated_records|update_latin_review_gate"
---

<objective>
Expose user-facing Latin MVP review inspection and gate-update commands while protecting approved curated fields from accidental overwrites.

Purpose: The phase goal says users can manage review states and inspect reasons before export; this plan makes that possible through the existing CLI-first workflow.
Output: CLI command and service update helpers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-latin-review-gates-and-curated-records/25-01-SUMMARY.md
@.planning/phases/25-latin-review-gates-and-curated-records/25-02-SUMMARY.md
@src/multilang/cli.py
@src/multilang/services/latin_review.py
@tests/cli/test_generate_latin_mvp_command.py

<interfaces>
From `src/multilang/cli.py`:
```python
def create_app(..., latin_mvp_service: LatinMvpGenerationService | None = None) -> typer.Typer: ...
```

Expected Plan 25-02 exports:
```python
def load_latin_curated_records(path: Path | None = None, source_pack_path: Path | None = None) -> list[LatinCuratedRecord]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add update and write helpers with approved-field protection</name>
  <files>src/multilang/services/latin_review.py, tests/cli/test_generate_latin_mvp_command.py</files>
  <behavior>
    - Test 1: updating `latin-mvp-0001` grammar gate from `needs_review` to `approved` changes only that gate.
    - Test 2: updating an approved gate without `force=True` raises a `ValueError` and preserves the existing approved gate.
    - Test 3: rejected/needs_review updates require a reason and approved updates may include reviewer metadata.
  </behavior>
  <action>Add `write_latin_curated_records(records, path)` and `update_latin_review_gate(records, *, item_key, gate, status, reason=None, reviewed_by=None, reviewed_at=None, force=False)`. `gate` must be one of `source`, `translation`, `grammar`, `audio`. If the current gate is `approved` and the requested status or reason differs, raise `ValueError` unless `force=True`; this implements the roadmap success criterion that approved curated fields are protected from accidental provider/regeneration overwrites. Return a new list or updated model consistently; avoid hidden mutation surprises in tests.</action>
  <verify>
    <automated>python -m pytest tests/cli/test_generate_latin_mvp_command.py -q</automated>
  </verify>
  <done>Service helpers can update review gates deterministically and fail closed when an approved gate would be overwritten without force.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add review-latin-mvp CLI inspection and update command</name>
  <files>src/multilang/cli.py, tests/cli/test_generate_latin_mvp_command.py</files>
  <behavior>
    - Test 1: `review-latin-mvp --summary` prints `total_records=50`, `learner_ready_records=0`, and per-gate status counts.
    - Test 2: `review-latin-mvp --item-key latin-mvp-0001 --gate grammar --status approved --reason resolved_by_phase_24 --curation-file <tmp>` updates the JSON file and prints the updated item/gate/status.
    - Test 3: attempting to change an approved gate without `--force` exits non-zero and prints the overwrite-protection message.
  </behavior>
  <action>Add a `review-latin-mvp` Typer command inside `create_app`. Options: `--curation-file` defaulting to `DEFAULT_LATIN_MVP_CURATION_PATH`, `--summary` boolean, `--item-key`, `--gate`, `--status`, `--reason`, `--reviewed-by`, `--reviewed-at`, and `--force`. With `--summary`, load curated records and print stable key=value lines: `total_records`, `learner_ready_records`, `blocked_records`, and `gate_counts=<json sorted keys>`. With update options, require `--item-key`, `--gate`, and `--status`, call `update_latin_review_gate`, write the file, and print `updated_item_key`, `updated_gate`, `updated_status`. Do not alter existing `generate-latin-mvp` behavior or modern `generate` validation.</action>
  <verify>
    <automated>python -m pytest tests/cli/test_generate_latin_mvp_command.py -q</automated>
  </verify>
  <done>User can inspect and update Latin review gates from the CLI, and approved gates cannot be overwritten accidentally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input→curation JSON | User-provided item keys, gate names, statuses, and reasons modify committed/review files. |
| approved curation→regeneration/update | Approved review state must not be overwritten accidentally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Tampering | review-latin-mvp update | mitigate | Validate gate/status enums and item_key existence before writing. |
| T-25-08 | Repudiation | update_latin_review_gate | mitigate | Preserve reason/reviewer/timestamp metadata supplied by the command. |
| T-25-09 | Tampering | approved gates | mitigate | Require explicit `--force` to alter an already approved gate. |
</threat_model>

<verification>
Run `python -m pytest tests/cli/test_generate_latin_mvp_command.py -q`.
</verification>

<success_criteria>
The CLI can inspect and manage Latin review gates while preserving existing Latin MVP command behavior and blocking accidental approved-field overwrites.
</success_criteria>

<output>
After completion, create `.planning/phases/25-latin-review-gates-and-curated-records/25-03-SUMMARY.md`.
</output>
