---
phase: 25-latin-review-gates-and-curated-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/latin_review.py
  - tests/services/test_latin_review.py
autonomous: true
requirements: [REV-01, REV-02, REV-03]
must_haves:
  truths:
    - "User can see four independent Latin MVP review gates: source, translation, grammar, and audio readiness."
    - "User can tell whether a Latin MVP record is learner-ready from approved/rejected/needs_review gate states."
    - "User can inspect rejection, replacement, and uncertainty reasons without losing source/frequency provenance."
  artifacts:
    - path: "src/multilang/services/latin_review.py"
      provides: "Latin review gate domain contracts and export-readiness validator"
      exports: ["LatinReviewStatus", "LatinReviewGateName", "LatinReviewGate", "LatinCuratedRecord", "LatinReviewSummary", "summarize_latin_review_records", "assert_latin_records_export_ready"]
    - path: "tests/services/test_latin_review.py"
      provides: "TDD coverage for gate states, reason preservation, provenance preservation, and export readiness blocking"
  key_links:
    - from: "src/multilang/services/latin_review.py"
      to: "src/multilang/services/latin_source_pack.py"
      via: "LatinCuratedRecord stores source_pack_version/item_key/lemma/frequency/source provenance copied from LatinMvpSourcePackEntry"
      pattern: "source_pack_version.*frequency_rank.*source_type"
---

<objective>
Create the core Latin MVP review contracts that later plans can use to manage curated records and block learner-ready export until all required gates are approved.

Purpose: Phase 25 must make review state explicit before translation, audio, and export phases can safely consume Latin records.
Output: `latin_review.py` service module plus focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-01-SUMMARY.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-02-SUMMARY.md
@src/multilang/services/latin_source_pack.py
@src/multilang/domain/latin.py

<interfaces>
From `src/multilang/services/latin_source_pack.py`:
```python
class LatinMvpSourcePackEntry(LatinSourceProvenance, LatinDidacticRationale):
    item_key: str
    sequence: int
    lemma: str
    target_form: str
    latin_sentence: str
    frequency_rank: int
    frequency_source: str
    frequency_source_url: HttpUrl
    source_pack_version: Literal["latin-mvp-50-v1"]
    target_match_mode: LatinTargetMatchMode
```

From `src/multilang/domain/latin.py`:
```python
LATIN_LANGUAGE_CODE = "la"
LATIN_MVP_CARD_COUNT = 50
DEFAULT_LATIN_SOURCE_PACK_VERSION = "latin-mvp-50-v1"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define review gate and curated-record contracts</name>
  <files>src/multilang/services/latin_review.py, tests/services/test_latin_review.py</files>
  <behavior>
    - Test 1: `LatinReviewGate(status="needs_review", reason="grammar ambiguous")` is valid and exposes the reason.
    - Test 2: valid statuses are exactly `needs_review`, `approved`, and `rejected` for REV-01.
    - Test 3: `LatinCuratedRecord` requires source, translation, grammar, and audio gates and stores source/frequency provenance fields copied from the source pack for REV-03.
  </behavior>
  <action>Create `src/multilang/services/latin_review.py` with Pydantic v2 models. Add `LatinReviewStatus` (`needs_review`, `approved`, `rejected`), `LatinReviewGateName` (`source`, `translation`, `grammar`, `audio`), `LatinReviewGate` with `status`, optional `reason`, optional `reviewed_by`, optional `reviewed_at`, and `LatinCuratedRecord` with identity/provenance fields: `item_key`, `sequence`, `lemma`, `target_form`, `latin_sentence`, `source_pack_version`, `frequency_rank`, `frequency_source`, `source_type`, `citation`, `work_reference`, `source_url_or_id`, `license_note`, plus `replacement_reason`, `uncertainty_reason`, and four gate fields named `source_gate`, `translation_gate`, `grammar_gate`, `audio_gate`. Implement validators that reject blank required text and require a non-blank reason when a gate status is `rejected` or `needs_review`. Do not add DB persistence in this plan; keep this an offline deterministic contract consistent with Phase 23 patterns.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_review.py -q</automated>
  </verify>
  <done>Tests prove all four gate names and all three statuses exist, reasons are required for blocking states, and curated records preserve source/frequency provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add summary and export-readiness validation</name>
  <files>src/multilang/services/latin_review.py, tests/services/test_latin_review.py</files>
  <behavior>
    - Test 1: a record with all four gates approved is counted as learner-ready.
    - Test 2: a record with any gate `needs_review` or `rejected` is not learner-ready and reports the blocking gate names.
    - Test 3: `assert_latin_records_export_ready` raises `ValueError` listing item keys and gate names until every required gate is approved for REV-02.
  </behavior>
  <action>Add `LatinReviewSummary`, `summarize_latin_review_records(records)`, and `assert_latin_records_export_ready(records)`. Summary must include `total_records`, `learner_ready_records`, `blocked_records`, and per-gate counts by status. Readiness is true only when `source_gate`, `translation_gate`, `grammar_gate`, and `audio_gate` are all `approved`; any other status blocks export. Error text must be scanner-readable enough for tests, for example `latin_export_blocked item_key=latin-mvp-0001 gates=grammar,audio`.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_review.py -q</automated>
  </verify>
  <done>Export readiness is centralized in `latin_review.py` and fails closed for every non-approved source, translation, grammar, or audio gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON/filesystem→review models | Curated-review data may be edited by hand before validation. |
| review models→future export | Incorrect readiness logic could permit unapproved learner-facing cards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | LatinCuratedRecord | mitigate | Validate required fields and require reasons for `needs_review`/`rejected` gates with Pydantic validators. |
| T-25-02 | Information Disclosure | LatinCuratedRecord provenance | accept | Phase 23 provenance is already license-gated public metadata; no secrets should be added to this contract. |
| T-25-03 | Elevation of Privilege | assert_latin_records_export_ready | mitigate | Fail closed unless all four required gates are exactly `approved`. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_review.py -q`.
</verification>

<success_criteria>
REV-01/REV-02/REV-03 have a typed service foundation: gate states exist, export readiness fails closed, and reason/provenance fields are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/25-latin-review-gates-and-curated-records/25-01-SUMMARY.md`.
</output>
