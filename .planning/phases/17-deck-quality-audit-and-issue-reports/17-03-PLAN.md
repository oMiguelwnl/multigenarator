---
phase: 17-deck-quality-audit-and-issue-reports
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - src/multilang/cli.py
  - tests/cli/test_audit_deck_command.py
autonomous: false
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
user_setup:
  - service: local_apkg_input
    why: "Deck-specific evidence for dbda4eb2-f0ec-402b-864f-48cdcf982b09.apkg if it is not already present in the workspace"
    env_vars: []
    dashboard_config:
      - task: "Provide the APKG file path when prompted by the executor if automatic search cannot find it."
        location: "Local filesystem"
must_haves:
  truths:
    - "User can run a CLI audit against any generated APKG path, including dbda4eb2-f0ec-402b-864f-48cdcf982b09.apkg when supplied."
    - "User sees JSON and Markdown report paths, card count, issue count, and input hash in CLI output."
    - "User can rerun the CLI and receive reproducible output without mutating the APKG."
  artifacts:
    - path: "src/multilang/cli.py"
      provides: "`audit-deck` Typer command"
    - path: "tests/cli/test_audit_deck_command.py"
      provides: "CLI integration tests for audit output and non-mutation behavior"
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/deck_audit_reader.py"
      via: "audit-deck reads APKG into AuditCard rows"
      pattern: "read_apkg_cards"
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/deck_audit_reports.py"
      via: "audit-deck writes Markdown and JSON outputs"
      pattern: "write_deck_audit_reports"
---

<objective>
Wire the APKG audit into the operator CLI and prove it works end-to-end with synthetic and supplied deck inputs.

Purpose: The user needs a concrete command to audit generated APKGs non-destructively and collect issue reports.
Output: `audit-deck` command, CLI tests, and an optional deck-specific verification checkpoint for the known APKG.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@src/multilang/cli.py
@src/multilang/services/deck_audit_reader.py
@src/multilang/services/deck_audit_reports.py
@tests/cli/test_export_command.py

<interfaces>
Existing CLI pattern from `src/multilang/cli.py`:
```python
@cli.command("export")
def export(job_id: Annotated[str, typer.Option("--job-id", ...)], ...) -> None:
    try:
        result = resolved_service.export_job(...)
    except ValueError as exc:
        typer.echo(str(exc))
        raise typer.Exit(code=1) from exc
    typer.echo(f"artifact_path={result.output_path}")
```

Plan 17-01/17-02 contracts:
```python
def read_apkg_cards(path: Path) -> DeckAuditReadResult: ...
def detect_card_issues(card: AuditCard) -> list[AuditIssue]: ...
def write_deck_audit_reports(read_result, issues, output_dir) -> DeckAuditReportResult: ...
```
</interfaces>

<source_audit>
GOAL Phase 17: COVERED by executable CLI audit flow.
REQ AUDIT-01: COVERED by `audit-deck --input-apkg ... --output-dir ...` report generation for generated APKGs, including the named APKG when supplied.
REQ AUDIT-02: COVERED by CLI invoking Definition issue detection from Plan 17-01.
REQ AUDIT-03: COVERED by CLI output hash, deterministic report writer, and non-mutation tests.
RESEARCH/PROJECT: Follows Typer CLI patterns and existing synthetic APKG tests; no live services or new dependencies.
CONTEXT Decisions: Honors v1.3 audit-first remediation sequence and does not implement Phase 18 corrections, Phase 19 template changes, Phase 20 audio gates, or future interactive repair.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audit-deck CLI command</name>
  <files>src/multilang/cli.py, tests/cli/test_audit_deck_command.py</files>
  <behavior>
    - Test 1: `audit-deck --input-apkg deck.apkg --output-dir reports` exits 0, prints `json_report=`, `markdown_report=`, `card_count=`, `issue_count=`, and `input_sha256=`.
    - Test 2: a synthetic APKG containing a grammar-metadata Definition exits 0 and writes both reports with one issue.
    - Test 3: a missing APKG path exits 1 with a clear diagnostic and writes no reports.
  </behavior>
  <action>Add `@cli.command("audit-deck")` in `create_app`. Options: `--input-apkg` (`Path`, file path, exists=False for custom diagnostic), and `--output-dir` (`Path`, dir path, default `Path(".multilang/audits")`). The command must call `read_apkg_cards`, run `detect_card_issues` for every card, call `write_deck_audit_reports`, and print stable key/value lines. Catch `ValueError` and `OSError`, print the diagnostic, and exit code 1. Do not call generation/export services and do not modify the APKG.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_audit_deck_command.py -q</automated>
  </verify>
  <done>`audit-deck` provides the user-facing Phase 17 command and reports deterministic audit metadata and output paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove CLI reproducibility and non-mutation end-to-end</name>
  <files>tests/cli/test_audit_deck_command.py</files>
  <behavior>
    - Test 1: running `audit-deck` twice against the same synthetic APKG produces identical JSON bytes.
    - Test 2: APKG SHA-256 is unchanged after CLI execution.
    - Test 3: Markdown report groups the issue by card identifier and Definition/Definitions field.
  </behavior>
  <action>Extend the CLI tests with a synthetic APKG built from existing `ExportCardRow` and `export_anki_package` helpers, including at least one Definition issue from `card_issues_normalized.md`. Assert rerun reproducibility by reading `deck-audit.json` bytes before and after the second run. Assert APKG non-mutation by hashing the APKG before and after the command. Keep fixture text synthetic and do not commit real user deck content.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_audit_deck_command.py tests/services/test_deck_audit_reader.py tests/services/test_deck_audit_reports.py tests/domain/test_deck_audit.py -q</automated>
  </verify>
  <done>End-to-end CLI evidence proves reproducible report output and unchanged APKG bytes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Supply known APKG for deck-specific evidence if absent</name>
  <files>dbda4eb2-f0ec-402b-864f-48cdcf982b09.apkg</files>
  <action>Run the automated search and audit command before asking the user for anything. Only pause if the named APKG is not present in the workspace or the user-provided path is needed to satisfy deck-specific evidence for AUDIT-01.</action>
  <what-built>The `audit-deck` command and synthetic APKG regression tests are complete.</what-built>
  <how-to-verify>
    1. Executor first searches the workspace for `dbda4eb2-f0ec-402b-864f-48cdcf982b09.apkg`.
    2. If found, run: `uv run python -m multilang.cli audit-deck --input-apkg <found-path> --output-dir .multilang/audits/dbda4eb2-f0ec-402b-864f-48cdcf982b09`.
    3. If not found, ask the user to provide the local APKG path, then run the same command with that path.
    4. Expected output includes `json_report=`, `markdown_report=`, `card_count=`, `issue_count=`, and `input_sha256=`; the report files exist and the original APKG hash is unchanged.
  </how-to-verify>
  <verify>
    <automated>uv run pytest tests/cli/test_audit_deck_command.py -q</automated>
  </verify>
  <resume-signal>Type "approved" after the known APKG audit command succeeds, or provide the APKG path/error output.</resume-signal>
  <done>Known APKG deck-specific evidence is produced when the file is available; otherwise implementation remains verified by synthetic APKG tests and waits for the user-supplied deck path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI path→APKG reader | User-supplied local path may be missing, malformed, or private. |
| audit command→reports directory | CLI writes files into a user-selected output directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Spoofing | `audit-deck --input-apkg` | mitigate | Print input SHA-256 and source basename so report identity can be verified. |
| T-17-08 | Tampering | `audit-deck` | mitigate | Reader performs non-mutation hash check; CLI tests assert unchanged APKG bytes. |
| T-17-09 | Information Disclosure | CLI diagnostics | mitigate | Diagnostics use provided path only in user-facing command context; reports serialize basename-only metadata and bounded evidence. |
</threat_model>

<verification>
Run `uv run pytest tests/cli/test_audit_deck_command.py tests/services/test_deck_audit_reader.py tests/services/test_deck_audit_reports.py tests/domain/test_deck_audit.py -q`.
</verification>

<success_criteria>
The user can run `audit-deck` against generated APKGs, receive stable JSON/Markdown issue reports, and verify the original APKG was not mutated.
</success_criteria>

<output>
After completion, create `.planning/phases/17-deck-quality-audit-and-issue-reports/17-03-SUMMARY.md`.
</output>
