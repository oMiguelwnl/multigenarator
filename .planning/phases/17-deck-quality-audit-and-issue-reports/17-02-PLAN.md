---
phase: 17-deck-quality-audit-and-issue-reports
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/multilang/services/deck_audit_reports.py
  - tests/services/test_deck_audit_reports.py
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
must_haves:
  truths:
    - "User receives a human-readable issue report grouped by note/card identifier and field."
    - "User receives a machine-readable issue report with deterministic ordering across reruns."
    - "User can compare reruns because input hash, card count, issue count, and issue ordering are stable."
  artifacts:
    - path: "src/multilang/services/deck_audit_reports.py"
      provides: "Markdown and JSON audit report writers"
      exports: ["DeckAuditReportResult", "write_deck_audit_reports"]
    - path: "tests/services/test_deck_audit_reports.py"
      provides: "Stable report ordering and grouped output tests"
  key_links:
    - from: "src/multilang/services/deck_audit_reports.py"
      to: "src/multilang/domain/deck_audit.py"
      via: "serializes AuditIssue values generated from AuditCard rows"
      pattern: "AuditIssue"
---

<objective>
Create reproducible human-readable and machine-readable audit outputs from the deck-audit foundation.

Purpose: AUDIT-01 and AUDIT-03 require actionable reports, not just in-memory issue detection.
Output: Markdown and JSON report writer with stable sorting and tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@src/multilang/domain/deck_audit.py
@src/multilang/services/deck_audit_reader.py

<interfaces>
From Plan 17-01 contracts to consume directly:
```python
@dataclass(frozen=True)
class DeckAuditReadResult:
    source_path_name: str
    input_sha256: str
    cards: list[AuditCard]
    card_count: int

def detect_card_issues(card: AuditCard) -> list[AuditIssue]: ...
```
</interfaces>

<source_audit>
GOAL Phase 17: COVERED by report outputs the user can inspect.
REQ AUDIT-01: COVERED by grouped Markdown/JSON issues by note/card identifier and field.
REQ AUDIT-02: COVERED by carrying Definition issue types and messages into reports.
REQ AUDIT-03: COVERED by deterministic sorting, input hash, and reproducible JSON formatting.
RESEARCH/PROJECT: Uses stdlib JSON/Markdown text output; no provider calls or deck mutation.
CONTEXT Decisions: Implements normalized defect catalog reporting only; does not plan deferred interactive review/repair workflow.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize deterministic JSON audit reports</name>
  <files>src/multilang/services/deck_audit_reports.py, tests/services/test_deck_audit_reports.py</files>
  <behavior>
    - Test 1: two identical inputs write byte-identical `deck-audit.json` files.
    - Test 2: JSON contains `source_path_name`, `input_sha256`, `card_count`, `issue_count`, and sorted `issues`.
    - Test 3: issues are sorted by `card_identifier`, `field_name`, `issue_type`, then `note_id`.
  </behavior>
  <action>Create `DeckAuditReportResult` and `write_deck_audit_reports(read_result, issues, output_dir)`. The JSON writer must use `ensure_ascii=False`, `indent=2`, sorted deterministic issue order, and a trailing newline. Include only bounded issue evidence already provided by `AuditIssue`; do not serialize absolute APKG paths or raw SQLite internals. Output path names must be stable: `deck-audit.json` and `deck-audit.md`.</action>
  <verify>
    <automated>uv run pytest tests/services/test_deck_audit_reports.py -q</automated>
  </verify>
  <done>Machine-readable report is deterministic and includes all metadata needed to reproduce and compare audit runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write grouped Markdown issue reports</name>
  <files>src/multilang/services/deck_audit_reports.py, tests/services/test_deck_audit_reports.py</files>
  <behavior>
    - Test 1: Markdown groups issues under note/card identifiers and field names.
    - Test 2: empty issue lists produce an explicit passing report with `issue_count=0`.
    - Test 3: Definition issue messages include remediation hints from the normalized catalog without promising automatic repair.
  </behavior>
  <action>Extend the report writer so `deck-audit.md` starts with deck metadata, then groups issues as `## Card <card_identifier>` and `### Field: <field_name>`. Include issue type, severity, message, evidence, and recommended next action. For no issues, write a clear `No normalized Definition audit issues found.` section. Do not include repair/edit workflow language; Phase 17 reports issues only and FUTURE-01 interactive repair is out of scope.</action>
  <verify>
    <automated>uv run pytest tests/services/test_deck_audit_reports.py -q</automated>
  </verify>
  <done>Human report is actionable, grouped, reproducible, and scoped to audit findings without mutating cards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit issues→report files | Untrusted deck field excerpts are written to local reports. |
| output directory→filesystem | User-controlled output path receives generated files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Information Disclosure | `deck_audit_reports.py` | mitigate | Serialize basename-only source metadata and bounded issue evidence; no absolute input paths. |
| T-17-05 | Tampering | report writer | mitigate | Create output directory explicitly and write only fixed filenames `deck-audit.json` and `deck-audit.md`. |
| T-17-06 | Repudiation | report metadata | mitigate | Include input SHA-256, card count, issue count, and deterministic ordering in both outputs. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_deck_audit_reports.py tests/domain/test_deck_audit.py -q`.
</verification>

<success_criteria>
Audit results can be consumed by humans and tools, with stable report bytes across identical reruns and no mutation/repair side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/17-deck-quality-audit-and-issue-reports/17-02-SUMMARY.md`.
</output>
