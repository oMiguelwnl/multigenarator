---
phase: 09-source-profiles-privacy-regression-boundary
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/security/__init__.py
  - src/multilang/security/redaction.py
  - tests/security/test_redaction.py
  - .gitignore
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "Sensitive WebDAV credentials, raw highlight paths, book metadata, and reading text have one reusable redaction boundary before later ingestion phases."
    - "Raw highlight artifacts and local secrets are excluded from commit candidates by default."
  artifacts:
    - path: "src/multilang/security/redaction.py"
      provides: "Reusable privacy redaction helpers for logs, errors, reports, and future prompt construction"
      exports: ["SENSITIVE_VALUE_REDACTION", "redact_sensitive_text", "redact_mapping", "redact_exception"]
    - path: "tests/security/test_redaction.py"
      provides: "Privacy regression coverage for credentials, URLs, book metadata, raw highlight text, and exception messages"
    - path: ".gitignore"
      provides: "Repository exclusion for local secrets and raw highlight artifacts"
  key_links:
    - from: "future WebDAV/highlight adapters"
      to: "src/multilang/security/redaction.py"
      via: "shared redaction helper used before logging/reporting"
      pattern: "redact_sensitive_text"
---

<objective>
Create the reusable privacy/redaction boundary required before WebDAV and raw highlight handling are implemented.

Purpose: SEC-01 is a milestone requirement, not a later cleanup; later phases must have safe utilities and ignored local artifact paths ready to use.
Output: Security package with deterministic redaction helpers, tests, and gitignore protections.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.gitignore

<interfaces>
Create these functions for later phases:
```python
SENSITIVE_VALUE_REDACTION = "[REDACTED]"
def redact_sensitive_text(text: str, *, extra_terms: Iterable[str] = ()) -> str: ...
def redact_mapping(values: Mapping[str, object], *, extra_terms: Iterable[str] = ()) -> dict[str, object]: ...
def redact_exception(exc: BaseException, *, extra_terms: Iterable[str] = ()) -> str: ...
```
The helpers must redact Authorization headers/tokens, username/password-like keys, WebDAV URLs, raw highlight export paths, obvious book metadata labels, and caller-provided private terms.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic redaction helpers</name>
  <files>src/multilang/security/__init__.py, src/multilang/security/redaction.py, tests/security/test_redaction.py</files>
  <behavior>
    - Test 1: authorization headers, bearer/basic tokens, passwords, usernames, and API keys are replaced with `[REDACTED]`.
    - Test 2: WebDAV URLs and local raw highlight paths under `.multilang/highlights/raw/` are redacted.
    - Test 3: book metadata labels such as `Title:`, `Author:`, and `Book:` redact their values.
    - Test 4: caller-supplied `extra_terms` redact private highlight snippets exactly, including non-ASCII text.
    - Test 5: `redact_mapping()` preserves non-sensitive keys/values while redacting sensitive nested scalar values.
  </behavior>
  <action>Create `src/multilang/security/redaction.py` and package `__init__.py`. Implement pure deterministic helpers only; do not add logging side effects or external dependencies. Use conservative regex patterns for `Authorization:`, `Bearer`, `Basic`, `password=`, `username=`, `secret=`, `token=`, `api_key=`, `https?://.../dav...`, `.multilang/highlights/raw/...`, and metadata labels (`Title`, `Author`, `Book`, `Location`) per SEC-01. Preserve enough structure for debugging, but never return raw sensitive values. Include tests in `tests/security/test_redaction.py` with synthetic data only; do not use real WebDAV URLs, real book titles, or real highlight text.</action>
  <verify>
    <automated>uv run pytest tests/security/test_redaction.py -q</automated>
  </verify>
  <done>Redaction helpers remove known sensitive shapes and caller-provided private terms while preserving safe diagnostic context.</done>
</task>

<task type="auto">
  <name>Task 2: Exclude local highlight artifacts and secrets from git</name>
  <files>.gitignore, tests/security/test_redaction.py</files>
  <action>Update `.gitignore` to keep the existing `.env` and `.multilang/` exclusions and add explicit future-proof lines for `.env.*`, `!.env.example`, `.multilang/highlights/raw/`, `.multilang/highlights/cache/`, `.multilang/highlights/sync/`, `kindle-highlights*.html`, `kindle-highlights*.txt`, and `webdav-secrets*.json`. Add a test in `tests/security/test_redaction.py` that reads `.gitignore` and asserts these exact patterns exist so SEC-01 remains enforceable before Phase 10/14 create highlight artifacts.</action>
  <verify>
    <automated>uv run pytest tests/security/test_redaction.py -q</automated>
  </verify>
  <done>Repository ignore rules explicitly cover raw Kindle/WebDAV artifacts and local secrets before any ingestion phase can produce them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private local files → logs/errors | Raw highlights and local paths may enter diagnostics. |
| WebDAV config → process output | Credentials and remote URLs may enter exceptions, reports, or debug output. |
| working tree → commit candidates | Local secrets/raw exports may be present during development. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure | `redact_sensitive_text` | mitigate | Redact credentials, URLs, private terms, and metadata values before logs/errors/reports. |
| T-09-08 | Information Disclosure | `.gitignore` | mitigate | Ignore raw highlight caches and local secret files explicitly. |
| T-09-09 | Repudiation | redacted diagnostics | accept | Redaction reduces raw forensic detail by design; preserve labels and structure enough to diagnose failures without private content. |
</threat_model>

<verification>
Run `uv run pytest tests/security/test_redaction.py -q`.
</verification>

<success_criteria>
SEC-01 has a concrete redaction utility and repository-level artifact exclusions available for all later v1.2 phases.
</success_criteria>

<output>
After completion, create `.planning/phases/09-source-profiles-privacy-regression-boundary/09-03-SUMMARY.md`.
</output>
