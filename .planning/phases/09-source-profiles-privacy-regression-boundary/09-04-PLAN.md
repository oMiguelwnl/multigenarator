---
phase: 09-source-profiles-privacy-regression-boundary
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - tests/integration/test_v12_existing_mode_regression_boundary.py
  - .planning/phases/09-source-profiles-privacy-regression-boundary/09-REGRESSION-EVIDENCE.md
autonomous: true
requirements: [MODE-02, SEC-01, SEC-02]
must_haves:
  truths:
    - "User receives regression test evidence that frequency generation, audio, and export still work after source-profile/privacy boundaries."
    - "User receives regression test evidence that custom word-list generation, audio, and export still work after source-profile/privacy boundaries."
    - "Regression evidence includes privacy redaction coverage so later highlight phases do not proceed without SEC-01 proof."
  artifacts:
    - path: "tests/integration/test_v12_existing_mode_regression_boundary.py"
      provides: "Phase 09 regression boundary suite tying MODE-02, SEC-01, and SEC-02 to runnable evidence"
    - path: ".planning/phases/09-source-profiles-privacy-regression-boundary/09-REGRESSION-EVIDENCE.md"
      provides: "Evidence commands and expected scope for phase verification"
  key_links:
    - from: "tests/integration/test_v12_existing_mode_regression_boundary.py"
      to: "tests/integration/test_frequency_e2e_export_flow.py"
      via: "same shipped CLI/fake Azure pattern proves frequency flow"
      pattern: "frequency"
    - from: "tests/integration/test_v12_existing_mode_regression_boundary.py"
      to: "tests/integration/test_custom_word_list_e2e_export_flow.py"
      via: "same shipped CLI/fake Azure pattern proves custom flow"
      pattern: "word-list"
---

<objective>
Create the phase-level regression evidence boundary that must pass before Phase 10+ highlight work proceeds.

Purpose: SEC-02 asks for evidence, not just unit tests; this plan makes the existing-mode safety net explicit and records the exact commands future phases should keep green.
Output: A focused v1.2 regression test file and evidence artifact.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/RETROSPECTIVE.md
@.planning/phases/06-end-to-end-text-acceptance-pipeline/06-03-SUMMARY.md
@.planning/phases/06-end-to-end-text-acceptance-pipeline/06-04-SUMMARY.md
@tests/integration/test_custom_word_list_e2e_export_flow.py
@tests/integration/test_frequency_e2e_export_flow.py
@tests/security/test_redaction.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add v1.2 existing-mode regression boundary tests</name>
  <files>tests/integration/test_v12_existing_mode_regression_boundary.py</files>
  <behavior>
    - Test 1: the shipped frequency E2E regression still reaches accepted text, fake Azure audio, APKG/CSV/TSV export, and the 3×1000 default contract.
    - Test 2: the shipped custom word-list E2E regression still reaches accepted text, fake Azure audio, APKG/CSV/TSV export, and no highlight note type/field names appear in exported existing-mode field lists.
    - Test 3: CLI still rejects `--source kindle-highlights` until Phase 11 intentionally wires the user-facing highlights mode.
  </behavior>
  <action>Create `tests/integration/test_v12_existing_mode_regression_boundary.py`. Reuse the existing fake Azure/shipped CLI patterns from `tests/integration/test_frequency_e2e_export_flow.py` and `tests/integration/test_custom_word_list_e2e_export_flow.py`; do not make live Azure, WebDAV, LLM, or DeepL calls. Assert existing exports use `NOTE_TYPE_NAME`/`MANUAL_NOTE_TYPE_NAME` and `FREQUENCY_EXPORT_CARD_FIELD_NAMES`/`MANUAL_EXPORT_CARD_FIELD_NAMES`, not `HIGHLIGHT_NOTE_TYPE_NAME` or `HIGHLIGHT_EXPORT_CARD_FIELD_NAMES`. Add a CLI validation test that `kindle-highlights` is not accepted by `multilang generate` in Phase 09, because MODE-01 belongs to Phase 11.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_v12_existing_mode_regression_boundary.py -q</automated>
  </verify>
  <done>One focused regression suite proves existing generation modes remain stable and highlight mode is not accidentally user-enabled early.</done>
</task>

<task type="auto">
  <name>Task 2: Record phase regression evidence commands</name>
  <files>.planning/phases/09-source-profiles-privacy-regression-boundary/09-REGRESSION-EVIDENCE.md</files>
  <action>Create `.planning/phases/09-source-profiles-privacy-regression-boundary/09-REGRESSION-EVIDENCE.md` with the exact evidence commands and what each command proves. Include: source profile/domain tests, export isolation tests, redaction tests, the new v1.2 regression boundary test, existing frequency/custom E2E tests, and `uv run pytest --collect-only -q` to catch broad collection drift before future phases rely on the suite. This is an evidence artifact, not a substitute for tests.</action>
  <verify>
    <automated>uv run pytest tests/domain/test_source_profiles.py tests/domain/test_exporting.py tests/services/test_export_anki_package.py tests/security/test_redaction.py tests/integration/test_v12_existing_mode_regression_boundary.py tests/integration/test_custom_word_list_e2e_export_flow.py tests/integration/test_frequency_e2e_export_flow.py -q</automated>
  </verify>
  <done>The phase has a documented, runnable regression boundary covering MODE-02, SEC-01, and SEC-02 before highlight ingestion work starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → regression proof | Synthetic fixtures stand in for private inputs and must not include secrets. |
| future highlight work → existing modes | New source behavior must not mutate shipped frequency/custom flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | regression suite | mitigate | Assert exact field constants/note types and CLI source gating for shipped modes. |
| T-09-11 | Information Disclosure | test fixtures/evidence | mitigate | Use synthetic fixture words and redaction tests only; do not store real highlights/WebDAV data. |
| T-09-12 | Denial of Service | regression command size | accept | Focused evidence commands are bounded and fake-provider based; full collection is collect-only for drift detection. |
</threat_model>

<verification>
Run the command in Task 2 plus `uv run pytest --collect-only -q`.
</verification>

<success_criteria>
Phase 09 produces runnable evidence that existing generation/export modes and privacy boundaries are stable before Phase 10 starts.
</success_criteria>

<output>
After completion, create `.planning/phases/09-source-profiles-privacy-regression-boundary/09-04-SUMMARY.md`.
</output>
