---
phase: 09-source-profiles-privacy-regression-boundary
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/multilang/domain/exporting.py
  - src/multilang/services/export_anki_package.py
  - src/multilang/runtime.py
  - tests/domain/test_exporting.py
  - tests/services/test_export_anki_package.py
autonomous: true
requirements: [MODE-02, SEC-02]
must_haves:
  truths:
    - "Frequency and custom word-list exports keep the exact shipped ten-field contract."
    - "Highlight export behavior cannot silently reuse frequency/manual note models or leak a Translation field."
    - "Mixed-source exports fail fast instead of producing an invalid Anki package."
  artifacts:
    - path: "src/multilang/domain/exporting.py"
      provides: "Source-profile-aware field-name resolution while preserving shipped field constants"
      exports: ["HIGHLIGHT_EXPORT_CARD_FIELD_NAMES", "export_field_names_for_source_type", "export_field_names_for_rows"]
    - path: "src/multilang/services/export_anki_package.py"
      provides: "Exact source-type model selection and mixed-source guard"
      exports: ["HIGHLIGHT_MODEL_ID", "HIGHLIGHT_NOTE_TYPE_NAME"]
    - path: "src/multilang/runtime.py"
      provides: "Tabular note-type selection uses exact source-type resolution"
  key_links:
    - from: "src/multilang/domain/source_profiles.py"
      to: "src/multilang/domain/exporting.py"
      via: "source profile exports_translation_field drives field list"
      pattern: "get_source_profile"
    - from: "src/multilang/domain/exporting.py"
      to: "src/multilang/services/export_anki_package.py"
      via: "model fields use export_field_names_for_source_type(source_type)"
      pattern: "export_field_names_for_source_type"
---

<objective>
Apply the source-profile boundary to export contracts and Anki model selection while preserving existing frequency/custom outputs.

Purpose: MODE-02 requires highlight behavior to be isolated before later phases add a highlight template or ingestion path.
Output: Source-aware export field resolution, exact note-type selection, mixed-source rejection, and export regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/05-anki-safe-export-contract/05-04-SUMMARY.md
@.planning/phases/08-card-quality-refresh/08-04-SUMMARY.md
@src/multilang/domain/exporting.py
@src/multilang/services/export_anki_package.py
@src/multilang/runtime.py

<interfaces>
Preserve these current exports:
```python
FREQUENCY_EXPORT_CARD_FIELD_NAMES = (..., "Translation", ...)
MANUAL_EXPORT_CARD_FIELD_NAMES = FREQUENCY_EXPORT_CARD_FIELD_NAMES
def export_field_names_for_source_type(source_type: str) -> tuple[str, ...]:
    return FREQUENCY_EXPORT_CARD_FIELD_NAMES
```

Add highlight-only exported fields from research:
```python
HIGHLIGHT_EXPORT_CARD_FIELD_NAMES = (
    "SortIndex", "Word", "IPA", "word_audio", "Example Sentence",
    "sentence_audio", "Definition", "Image",
)
```
Do not rename internal `ExportCardRow` fields or DB columns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make export field mapping source-profile-aware</name>
  <files>src/multilang/domain/exporting.py, tests/domain/test_exporting.py</files>
  <behavior>
    - Test 1: `export_field_names_for_source_type("frequency")` equals the existing ten-field tuple, including `Translation`.
    - Test 2: `export_field_names_for_source_type("word-list")` equals the existing ten-field tuple, including `Translation`.
    - Test 3: `export_field_names_for_source_type("kindle-highlights")` returns the highlight tuple with `Word` and `Definition`, no `word`/`Definitions` aliases, and no `Translation`.
    - Test 4: `ExportCardRow.ordered_field_mapping()` maps highlight `Word` from internal `word`, `Definition` from internal `definitions`, and omits `Translation`.
  </behavior>
  <action>Update `src/multilang/domain/exporting.py` to import `get_source_profile`. Add `HIGHLIGHT_EXPORT_CARD_FIELD_NAMES` exactly as listed above. Update `ExportCardRow.ordered_field_mapping()` so it can synthesize aliases for highlight fields from the existing internal fields without changing `ExportCardRow` storage shape: `Word` → `word`, `Definition` → `Definitions`. Update `export_field_names_for_source_type()` and `export_field_names_for_rows()` to use exact source types. Preserve `EXPORT_CARD_FIELD_NAMES`, `FREQUENCY_EXPORT_CARD_FIELD_NAMES`, and `MANUAL_EXPORT_CARD_FIELD_NAMES` for backward compatibility per MODE-02.</action>
  <verify>
    <automated>uv run pytest tests/domain/test_exporting.py -q</automated>
  </verify>
  <done>Frequency/manual rows keep the shipped field order, highlight rows have a distinct no-Translation field order, and unknown/mixed source cases fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard Anki and tabular note-type selection by exact source type</name>
  <files>src/multilang/services/export_anki_package.py, src/multilang/runtime.py, tests/services/test_export_anki_package.py</files>
  <behavior>
    - Test 1: frequency model ID/name/fields are unchanged.
    - Test 2: word-list model ID/name/fields are unchanged.
    - Test 3: kindle-highlights model uses new `HIGHLIGHT_MODEL_ID`, `HIGHLIGHT_NOTE_TYPE_NAME`, and highlight fields, but template loading may still use the normal template until Phase 13 creates `templates/highlight_card.md`.
    - Test 4: `export_anki_package()` raises `ExportAnkiPackageError` for rows containing more than one source type.
  </behavior>
  <action>Replace the current `source_type = "word-list" if rows ... else "frequency"` heuristic in `export_anki_package()` with exact-source resolution: collect `{row.identity.source_type for row in rows}`, raise `ExportAnkiPackageError("cannot export mixed source types in one note model")` when the set has more than one source type, otherwise use that source type or `frequency` for an empty row list. Add `HIGHLIGHT_MODEL_ID` and `HIGHLIGHT_NOTE_TYPE_NAME = "Multilang::Highlight Card"`. Update `build_multilang_model()` and `runtime._note_type_name_for_rows()` to use source profiles and exact source type instead of non-word-list-as-frequency logic. Do not create the final highlight template in this phase; Phase 13 owns highlight template implementation, but this phase must prevent highlight note/model collisions.</action>
  <verify>
    <automated>uv run pytest tests/services/test_export_anki_package.py tests/domain/test_exporting.py -q</automated>
  </verify>
  <done>Export code cannot silently misclassify highlight rows as frequency rows, and shipped model names/fields for existing modes remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted rows → export renderer | Persisted source_type decides note model and visible fields. |
| mixed source rows → Anki package | Invalid mixed source data could create field/template collisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | `export_anki_package()` | mitigate | Reject mixed-source rows before writing an `.apkg`. |
| T-09-05 | Information Disclosure | highlight field mapping | mitigate | Omit `Translation` from highlight exported field names and avoid frequency/manual fallback. |
| T-09-06 | Repudiation | note type selection | mitigate | Use explicit source profiles so exported note model choice is auditable from source_type. |
</threat_model>

<verification>
Run `uv run pytest tests/domain/test_exporting.py tests/services/test_export_anki_package.py -q`.
</verification>

<success_criteria>
Existing export contracts remain byte/field-order compatible at the source-contract level, and highlight export cannot accidentally use the normal deck model or Translation field.
</success_criteria>

<output>
After completion, create `.planning/phases/09-source-profiles-privacy-regression-boundary/09-02-SUMMARY.md`.
</output>
