---
phase: 09-source-profiles-privacy-regression-boundary
plan: 05
type: execute
wave: 5
depends_on: [09-01, 09-03, 09-04]
files_modified:
  - src/multilang/domain/source_profiles.py
  - tests/domain/test_source_profiles.py
  - .planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md
autonomous: true
requirements: [SEC-01]
gap_closure: true
must_haves:
  truths:
    - "Unsupported source-profile lookup errors do not disclose arbitrary user-supplied source strings, local paths, credentials, raw highlight text, or book metadata."
    - "Phase 09 security status has threats_open: 0 after T-09-02 remediation evidence is recorded."
  artifacts:
    - path: "src/multilang/domain/source_profiles.py"
      provides: "Privacy-safe fail-closed source profile lookup"
      exports: ["get_source_profile"]
    - path: "tests/domain/test_source_profiles.py"
      provides: "Regression proof that unsupported source errors omit private/path-bearing input"
      contains: "test_unknown_source_profile_error_omits_private_input"
    - path: ".planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md"
      provides: "Closed security gate record for T-09-02"
      contains: "threats_open: 0"
  key_links:
    - from: "src/multilang/domain/source_profiles.py"
      to: "tests/domain/test_source_profiles.py"
      via: "ValueError message contract for unsupported source_type"
      pattern: "unsupported source_type"
---

<objective>
Close the Phase 09 security gap T-09-02 by preventing unsupported source-profile errors from reflecting arbitrary private input.

Purpose: Phase 09 is otherwise complete, but security audit status is blocked because `get_source_profile()` currently includes the raw unknown `source_type` in its `ValueError`. That can disclose local Kindle paths, WebDAV credentials, book metadata, or raw highlight text before Phase 10/14 begin using these boundaries.
Output: Privacy-safe source-profile error handling, updated domain regression tests, and an updated Phase 09 security record showing T-09-02 closed.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md
@.planning/phases/09-source-profiles-privacy-regression-boundary/09-01-SUMMARY.md
@.planning/phases/09-source-profiles-privacy-regression-boundary/09-03-SUMMARY.md
@src/multilang/domain/source_profiles.py
@tests/domain/test_source_profiles.py

<interfaces>
Existing source-profile contract:
```python
SourceType = Literal["frequency", "word-list", "kindle-highlights"]
SUPPORTED_SOURCE_TYPES: tuple[SourceType, ...] = tuple(SOURCE_PROFILES.keys())

def get_source_profile(source_type: str) -> SourceProfile:
    try:
        return SOURCE_PROFILES[source_type]  # type: ignore[index]
    except KeyError as exc:
        raise ValueError(f"unsupported source_type: {source_type}") from exc
```

Required replacement error contract for T-09-02:
```python
raise ValueError(
    "unsupported source_type; expected one of: "
    + ", ".join(SUPPORTED_SOURCE_TYPES)
) from exc
```
The error may list safe supported source-type keys. It must not include the rejected `source_type` value.
</interfaces>

<gap_source>
From `.planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md`:
- Threat `T-09-02` is open.
- Gap: `Unknown source_type is reflected verbatim in the exception message, so a path/secret-bearing value would be disclosed.`
- Required mitigation: `Error messages include only source-type keys and never file contents, raw highlights, paths, or credentials.`
</gap_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make unsupported source-profile errors privacy-safe</name>
  <files>src/multilang/domain/source_profiles.py, tests/domain/test_source_profiles.py</files>
  <read_first>src/multilang/domain/source_profiles.py, tests/domain/test_source_profiles.py, .planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md</read_first>
  <behavior>
    - Test 1: `get_source_profile("missing-source /home/user/.multilang/highlights/raw/private.html secret text")` raises `ValueError` whose message does not contain `missing-source`, `/home/user`, `.multilang`, `private.html`, `secret`, or `text`.
    - Test 2: the same error message contains `unsupported source_type` and lists safe supported keys `frequency`, `word-list`, and `kindle-highlights`.
    - Test 3: existing profile tests for `frequency`, `word-list`, `kindle-highlights`, and `SUPPORTED_SOURCE_TYPES` still pass unchanged.
  </behavior>
  <action>Update `tests/domain/test_source_profiles.py` first: replace `test_unknown_source_profile_errors_include_only_unknown_value` with `test_unknown_source_profile_error_omits_private_input`. The test must use the exact private input string `missing-source /home/user/.multilang/highlights/raw/private.html secret text`, call `get_source_profile(unknown)`, and assert the resulting message does not contain the substrings `missing-source`, `/home/user`, `.multilang`, `private.html`, `secret`, or `text`. It must also assert the message contains `unsupported source_type`, `frequency`, `word-list`, and `kindle-highlights`. Then update `src/multilang/domain/source_profiles.py` so the `except KeyError` branch raises exactly `ValueError("unsupported source_type; expected one of: " + ", ".join(SUPPORTED_SOURCE_TYPES))` from the caught exception. Do not import redaction helpers here; omitting the unsafe value is stricter and avoids turning source-profile lookup into a security-package dependency.</action>
  <verify>
    <automated>uv run pytest tests/domain/test_source_profiles.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/domain/test_source_profiles.py` contains `def test_unknown_source_profile_error_omits_private_input`.
    - `tests/domain/test_source_profiles.py` contains assertions for all forbidden substrings: `missing-source`, `/home/user`, `.multilang`, `private.html`, `secret`, and `text`.
    - `src/multilang/domain/source_profiles.py` contains `unsupported source_type; expected one of:`.
    - `src/multilang/domain/source_profiles.py` no longer contains `unsupported source_type: {source_type}`.
    - `uv run pytest tests/domain/test_source_profiles.py -q` exits 0.
  </acceptance_criteria>
  <done>Unsupported source lookup still fails closed but no longer reflects arbitrary unknown input; safe supported source keys remain visible for diagnosis.</done>
</task>

<task type="auto">
  <name>Task 2: Close T-09-02 in the Phase 09 security record</name>
  <files>.planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md</files>
  <read_first>.planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md, src/multilang/domain/source_profiles.py, tests/domain/test_source_profiles.py</read_first>
  <action>After Task 1 passes, update `.planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md`: set frontmatter `status: verified`, set `threats_open: 0`, add an `updated:` timestamp for the execution date, change the T-09-02 row status from `open` to `closed`, replace the `## Open Threats` table with the exact sentence `No open threats remain after T-09-02 remediation.`, append an audit trail row for this gap-closure run with `Threats Total` 12, `Closed` 12, `Open` 0, and `Run By` `gsd-gap-closure`, update Notes to state that `get_source_profile()` omits the unsafe unknown source value and `tests/domain/test_source_profiles.py` asserts private/path-bearing values are absent, and check both sign-off boxes `threats_open: 0 confirmed` and `status: verified set in frontmatter`. Preserve accepted risks AR-09-01 and AR-09-02 unchanged.</action>
  <verify>
    <automated>uv run pytest tests/domain/test_source_profiles.py tests/domain/test_jobs.py tests/security/test_redaction.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `09-SECURITY.md` frontmatter contains `status: verified`.
    - `09-SECURITY.md` frontmatter contains `threats_open: 0`.
    - `09-SECURITY.md` contains `| T-09-02 | Information Disclosure | \`get_source_profile\` errors | mitigate |` and the same row ends with `| closed |`.
    - `09-SECURITY.md` contains `No open threats remain after T-09-02 remediation.`.
    - `09-SECURITY.md` contains `| 2026-05-` and `| 12 | 12 | 0 | gsd-gap-closure |` in the audit trail.
    - `uv run pytest tests/domain/test_source_profiles.py tests/domain/test_jobs.py tests/security/test_redaction.py -q` exits 0.
  </acceptance_criteria>
  <done>The security artifact records T-09-02 as remediated with automated regression evidence and Phase 09 no longer has open security threats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → source profile lookup | Arbitrary source strings may include local file paths, credentials, or private highlight text before domain validation rejects them. |
| source profile errors → CLI/log/report output | Exception text can cross into user-visible diagnostics or future reports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Information Disclosure | `get_source_profile` errors | mitigate | Do not interpolate the rejected `source_type`; raise a static message with only safe supported source keys and test that private/path-bearing input is absent. |
</threat_model>

<verification>
Run `uv run pytest tests/domain/test_source_profiles.py tests/domain/test_jobs.py tests/security/test_redaction.py -q` and confirm `.planning/phases/09-source-profiles-privacy-regression-boundary/09-SECURITY.md` has `status: verified` and `threats_open: 0`.
</verification>

<success_criteria>
T-09-02 is closed: unsupported source-profile errors are useful but privacy-safe, tests prove private input is omitted, and the Phase 09 security gate records zero open threats.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
-------|----|---------------------|------|--------|------
GOAL | — | Existing generation modes remain stable while highlight-specific behavior and privacy rules become explicit boundaries | 09-05 | COVERED | Closes the remaining privacy-boundary gap without changing existing source behavior.
REQ | SEC-01 | WebDAV credentials, raw highlight files, book metadata, and private reading text are redacted from logs/errors/artifacts | 09-05 | COVERED | Unsupported source errors stop reflecting private/path-bearing values.
RESEARCH | T-09-02 security audit | Source-profile errors must not disclose file contents, raw highlights, paths, or credentials | 09-05 | COVERED | Error contract lists safe keys only.
CONTEXT | Phase 09 carry-forward decision | Future highlight/WebDAV diagnostics should use privacy boundaries before private data appears in output | 09-05 | COVERED | Source-profile validation no longer emits arbitrary rejected input.
</source_audit>

<output>
After completion, create `.planning/phases/09-source-profiles-privacy-regression-boundary/09-05-SUMMARY.md`.
</output>
