---
phase: 23-frozen-50-card-source-pack-and-sentence-sequence
plan: 2
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - data/latin_mvp/latin-mvp-50-v1.json
  - tests/integration/test_v20_latin_source_pack_asset.py
autonomous: true
requirements: [FREQ-01, FREQ-02, FREQ-03, SRC-01, SRC-02, SENT-01, SENT-02]
must_haves:
  truths:
    - "User can inspect an actual frozen 50-card Latin MVP manifest, not a generated placeholder."
    - "Every row explains frequency rank, source provenance, license status, source type, target-form match, and didactic sequencing rationale."
  artifacts:
    - path: "data/latin_mvp/latin-mvp-50-v1.json"
      provides: "Frozen first-50 Classical Latin MVP source pack"
      contains: "50 entries"
    - path: "tests/integration/test_v20_latin_source_pack_asset.py"
      provides: "Executable validation that the committed asset satisfies Phase 23 requirements"
  key_links:
    - from: "tests/integration/test_v20_latin_source_pack_asset.py"
      to: "src/multilang/services/latin_source_pack.py"
      via: "load_latin_mvp_source_pack"
      pattern: "load_latin_mvp_source_pack"
---

<objective>
Commit the frozen `latin-mvp-50-v1` source pack with 50 real, traceable, license-gated Latin MVP entries.

Purpose: Users need a reproducible first-50 manifest ordered by lemma frequency and didactic suitability before grammar, translation, audio, or export phases build on it.
Output: A committed JSON asset and tests proving it is complete, licensed, sequenced, and target-containing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-DISCOVERY.md
@LATIN-STRUCTURE.md
@src/multilang/services/latin_source_pack.py
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-01-SUMMARY.md

<interfaces>
Plan 23-01 creates:
```python
class LatinMvpSourcePack(BaseModel): ...
class LatinMvpSourcePackEntry(BaseModel): ...
def load_latin_mvp_source_pack(path: Path | None = None) -> LatinMvpSourcePack: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the frozen 50-entry manifest asset</name>
  <files>data/latin_mvp/latin-mvp-50-v1.json</files>
  <action>Create `data/latin_mvp/latin-mvp-50-v1.json` with pack-level metadata (`language_code=la`, `variant=classical`, `source_pack_version=latin-mvp-50-v1`, `card_count=50`, DCC attribution/license text) and exactly 50 entry objects. Each entry must include: `item_key`, `sequence`, `lemma`, `target_form`, `latin_sentence`, `frequency_rank`, `frequency_source`, `frequency_source_url`, `source_pack_version`, `inclusion_status`, `inclusion_rationale`, `didactic_order_rationale`, `source_type`, `citation`, `work_reference`, `source_url_or_id`, `license_note`, `license_gate`, and `target_match_mode`. Use DCC Latin Core Vocabulary frequency ranks/lemma metadata with CC BY-SA attribution for FREQ-02; use original Classical Latin, adapted didactic Latin, or reference examples only when SRC-02 marks the type truthfully. Adapted didactic rows must say they are project-authored didactic text and must not be presented as original citations. Do not include Tatoeba, private local paths, provider output, Portuguese translations, grammar analysis, or audio fields in this Phase 23 asset.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_source_pack_asset.py -q</automated>
  </verify>
  <done>The JSON file exists with exactly 50 concrete entries and no placeholder/TBD/future/deferred language inside required fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate manifest content and ordering evidence</name>
  <files>tests/integration/test_v20_latin_source_pack_asset.py</files>
  <behavior>
    - The committed asset loads with `load_latin_mvp_source_pack()` and contains exactly 50 entries in item-key/sequence order.
    - Every entry has approved license metadata, DCC frequency metadata, non-empty inclusion rationale, non-empty didactic ordering rationale, and source type in the approved set.
    - The sequence is not raw frequency order only: at least one entry records a didactic reorder/defer/replacement rationale while still preserving frequency rank metadata.
  </behavior>
  <action>Add integration tests that import the loader from Plan 23-01 and validate the committed asset. Tests must assert no entry contains `TBD`, `placeholder`, `future`, `later`, `hardcoded`, raw private path markers, or empty strings in required fields. Per FREQ-03 and SENT-02, assert sequence metadata explains Rafael Falcon-style didactic suitability (clear early context, simpler forms early, complex/poetic ambiguity deferred or replaced) without deleting frequency ranks.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_source_pack_asset.py -q</automated>
  </verify>
  <done>Focused asset tests prove all 50 entries are inspectable, licensed, ordered, and target-containing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated source data -> repository | Human-authored curation becomes committed product data. |
| third-party/license metadata -> learner-visible evidence | External source claims are displayed to users and scanners. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering | `data/latin_mvp/latin-mvp-50-v1.json` | mitigate | Validate committed JSON through the Plan 23-01 loader in integration tests. |
| T-23-02-02 | Repudiation | curation rationale fields | mitigate | Require inclusion and didactic rationale per entry, including defer/replacement/reorder explanations. |
| T-23-02-03 | Information Disclosure | source URL/path fields | mitigate | Test that source identifiers are public URLs or stable local IDs, not private absolute paths or secrets. |
| T-23-02-04 | Spoofing | adapted didactic sentences | mitigate | Require `source_type=adapted_didactic` and a didactic license note so adapted text is never spoofed as original Classical citation. |
</threat_model>

<verification>
Run `python -m pytest tests/integration/test_v20_latin_source_pack_asset.py -q`.
</verification>

<success_criteria>
- `latin-mvp-50-v1.json` contains exactly 50 real entries and passes the loader.
- All entries carry frequency source/rank, provenance/license metadata, source type, and target-form validation mode.
- Didactic sequence rationale exists for the first-50 order and does not imply a full 3000-card deck.
</success_criteria>

<output>
After completion, create `.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-02-SUMMARY.md`
</output>
