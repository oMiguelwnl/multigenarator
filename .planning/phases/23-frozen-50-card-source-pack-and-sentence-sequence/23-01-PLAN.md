---
phase: 23-frozen-50-card-source-pack-and-sentence-sequence
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/latin_source_pack.py
  - tests/services/test_latin_source_pack.py
autonomous: true
requirements: [FREQ-01, FREQ-02, FREQ-03, SRC-01, SRC-02, SENT-01, SENT-02]
must_haves:
  truths:
    - "User can validate a frozen 50-card Latin source-pack manifest before generation uses it."
    - "Every manifest row must carry lemma frequency metadata, source provenance, license gate metadata, target-form validation metadata, and didactic sequencing rationale."
  artifacts:
    - path: "src/multilang/services/latin_source_pack.py"
      provides: "Pydantic models plus loader/validator for the frozen Latin MVP manifest"
      exports: ["LatinMvpSourcePack", "LatinMvpSourcePackEntry", "load_latin_mvp_source_pack", "validate_latin_target_presence"]
    - path: "tests/services/test_latin_source_pack.py"
      provides: "Contract tests for pack shape, license gates, target-form checks, and 50-card invariants"
  key_links:
    - from: "src/multilang/services/latin_source_pack.py"
      to: "data/latin_mvp/latin-mvp-50-v1.json"
      via: "loader path parameter/default path"
      pattern: "load_latin_mvp_source_pack"
---

<objective>
Create the source-pack contract that makes Phase 23 enforceable before any 50-card asset is accepted.

Purpose: Phase 23 cannot rely on narrative curation; the frozen pack needs executable invariants for frequency metadata, licensing, provenance, source type, target-form presence, and didactic ordering.
Output: A typed loader/validator and tests that downstream plans use for the actual `latin-mvp-50-v1` asset.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-DISCOVERY.md
@LATIN-STRUCTURE.md
@src/multilang/domain/latin.py
@.planning/phases/22-latin-mode-contracts-and-isolation/22-01-SUMMARY.md
@.planning/phases/22-latin-mode-contracts-and-isolation/22-02-SUMMARY.md

<interfaces>
From `src/multilang/domain/latin.py`:
```python
LATIN_LANGUAGE_CODE = "la"
LATIN_MVP_CARD_COUNT = 50
DEFAULT_LATIN_SOURCE_PACK_VERSION = "latin-mvp-50-v1"
LATIN_MVP_SOURCE_TYPE = "latin-mvp"
class LatinDeckMetadata(BaseModel): ...
class LatinGenerationRequest(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Latin source-pack models and invariants</name>
  <files>src/multilang/services/latin_source_pack.py, tests/services/test_latin_source_pack.py</files>
  <behavior>
    - Constructing a pack with 50 ordered entries succeeds only when `source_pack_version` is `latin-mvp-50-v1` and `card_count` is 50.
    - Entries reject missing lemma, missing target form, blank Latin sentence, missing frequency rank/source, missing inclusion/defer/replacement rationale, missing provenance, or unapproved license-gate status.
    - Source type accepts exactly `original_classical`, `adapted_didactic`, or `reference_example`; adapted didactic entries must not carry an `original_citation_claim=true` style marker.
  </behavior>
  <action>Create `latin_source_pack.py` with Pydantic v2 models for `LatinSourceProvenance`, `LatinDidacticRationale`, `LatinMvpSourcePackEntry`, and `LatinMvpSourcePack`. Include enums/literals for source type, license gate (`approved`, `blocked`), inclusion status (`included`, `deferred`, `replacement`), and target match mode (`exact`, `orthographic_normalization`, `enclitic_normalization`). Validate the pack has exactly 50 entries, sequence numbers 1-50, item keys `latin-mvp-0001` through `latin-mvp-0050`, unique included lemmas, `language_code="la"`, and every entry's `source_pack_version` matching the pack version. Per FREQ-01/FREQ-02/FREQ-03/SRC-01/SRC-02/SENT-01/SENT-02, this is the enforcement contract; do not create placeholders or optional TODO fields.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_source_pack.py -q</automated>
  </verify>
  <done>The model tests prove the exact 50-card shape, frequency metadata, source/provenance fields, license gate, source-type distinction, and rationale fields are required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement target-form presence and normalization checks</name>
  <files>src/multilang/services/latin_source_pack.py, tests/services/test_latin_source_pack.py</files>
  <behavior>
    - `validate_latin_target_presence("virum", "Arma virumque cano.")` accepts enclitic normalization for `virumque` when the entry declares `enclitic_normalization`.
    - Exact mode accepts whole-token exact matches and rejects absent targets.
    - Orthographic normalization accepts macron-insensitive matches and rejects unrelated forms.
  </behavior>
  <action>Add `validate_latin_target_presence(target_form, latin_sentence, match_mode)` and call it from entry validation. Implement whole-token matching with case folding, punctuation stripping, macron-insensitive normalization, and accepted Latin enclitic suffixes `que`, `ve`, `ne`. Per SENT-01, missing target form must be a validation error; never silently accept a sentence because the lemma appears elsewhere.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_source_pack.py -q</automated>
  </verify>
  <done>Tests cover exact, macron-insensitive, enclitic, and rejection cases for displayed target-form presence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic manifest loader</name>
  <files>src/multilang/services/latin_source_pack.py, tests/services/test_latin_source_pack.py</files>
  <behavior>
    - Loading a valid JSON fixture returns a `LatinMvpSourcePack` with 50 entries.
    - Loading missing, malformed, blocked-license, or wrong-version JSON raises a clear `ValueError` without leaking private local paths.
  </behavior>
  <action>Implement `load_latin_mvp_source_pack(path: Path | None = None) -> LatinMvpSourcePack` with a default path of `data/latin_mvp/latin-mvp-50-v1.json`. Return typed models and convert JSON/Pydantic errors into concise `ValueError` messages. Keep the function offline and deterministic; do not fetch web pages at runtime.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_source_pack.py -q</automated>
  </verify>
  <done>The loader has focused tests and is ready for the frozen asset in Plan 23-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local JSON asset -> Pydantic loader | Committed data is parsed into trusted generation metadata. |
| source metadata -> CLI/evidence output | Provenance and license text become user-visible scanner evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering | `load_latin_mvp_source_pack` | mitigate | Validate exact count, sequence, keys, version, approved license gate, and target-form presence before returning a pack. |
| T-23-01-02 | Information Disclosure | loader error handling | mitigate | Convert parse/path errors into concise messages that name the asset contract, not absolute private filesystem paths. |
| T-23-01-03 | Repudiation | source provenance fields | mitigate | Require citation/source/license/rationale fields for every entry so curation decisions are auditable. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_source_pack.py -q`.
</verification>

<success_criteria>
- The source-pack contract enforces all Phase 23 data required by FREQ-01/FREQ-02/FREQ-03/SRC-01/SRC-02/SENT-01/SENT-02.
- No runtime web calls or provider credentials are needed.
- A downstream executor can author the 50-row JSON asset against a precise schema.
</success_criteria>

<output>
After completion, create `.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-01-SUMMARY.md`
</output>
