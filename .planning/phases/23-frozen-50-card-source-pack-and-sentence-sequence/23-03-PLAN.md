---
phase: 23-frozen-50-card-source-pack-and-sentence-sequence
plan: 3
type: execute
wave: 3
depends_on: [23-01, 23-02]
files_modified:
  - src/multilang/services/latin_mvp.py
  - src/multilang/cli.py
  - tests/services/test_latin_mvp.py
  - tests/cli/test_generate_latin_mvp_command.py
autonomous: true
requirements: [FREQ-01, FREQ-02, FREQ-03, SRC-01, SRC-02, SENT-01, SENT-02]
must_haves:
  truths:
    - "User can start or inspect the Latin MVP path and see that it uses the frozen 50-card source pack."
    - "CLI output exposes source-pack version, first/last item keys, source-type counts, and license-gate status without using the modern frequency generator."
  artifacts:
    - path: "src/multilang/services/latin_mvp.py"
      provides: "Latin MVP service backed by the frozen source pack"
    - path: "src/multilang/cli.py"
      provides: "User-facing source-pack inspection through existing isolated Latin CLI path"
    - path: "tests/services/test_latin_mvp.py"
      provides: "Service tests for manifest-backed item keys and summary metadata"
    - path: "tests/cli/test_generate_latin_mvp_command.py"
      provides: "CLI tests for source-pack metadata output and existing-mode isolation"
  key_links:
    - from: "src/multilang/services/latin_mvp.py"
      to: "src/multilang/services/latin_source_pack.py"
      via: "load_latin_mvp_source_pack"
      pattern: "load_latin_mvp_source_pack"
    - from: "src/multilang/cli.py"
      to: "LatinMvpGenerationService.start"
      via: "generate-latin-mvp command"
      pattern: "generate_latin_mvp"
---

<objective>
Wire the isolated Latin MVP service/CLI to the frozen source pack so users can inspect the actual 50-card manifest metadata.

Purpose: Phase 22 created a deterministic Latin start path; Phase 23 must replace synthetic item-key-only output with source-pack-backed metadata while preserving modern-mode isolation.
Output: Service and CLI output that prove the Latin path is driven by `latin-mvp-50-v1.json`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/multilang/services/latin_mvp.py
@src/multilang/cli.py
@tests/services/test_latin_mvp.py
@tests/cli/test_generate_latin_mvp_command.py
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-01-SUMMARY.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-02-SUMMARY.md

<interfaces>
Existing service:
```python
class LatinMvpStartResult(BaseModel):
    metadata: LatinDeckMetadata
    source_type: str = "latin-mvp"
    item_keys: list[str]
class LatinMvpGenerationService:
    def start(self, request: LatinGenerationRequest) -> LatinMvpStartResult: ...
```
Plan 23-01 loader:
```python
def load_latin_mvp_source_pack(path: Path | None = None) -> LatinMvpSourcePack: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Latin MVP service manifest-backed</name>
  <files>src/multilang/services/latin_mvp.py, tests/services/test_latin_mvp.py</files>
  <behavior>
    - `LatinMvpGenerationService().start(LatinGenerationRequest())` loads the frozen source pack and returns item keys from manifest order.
    - The result exposes summary metadata: `manifest_path`, `first_item_key`, `last_item_key`, `license_gate_status`, `source_type_counts`, `frequency_source_count`, and `didactic_sequence_summary`.
    - A source-pack version mismatch between request and manifest raises `ValueError` instead of silently using the wrong asset.
  </behavior>
  <action>Extend `LatinMvpStartResult` to include manifest-backed source-pack summary fields while preserving existing `metadata`, `source_type`, and `item_keys` fields. Inject an optional source-pack loader/path into `LatinMvpGenerationService` for deterministic tests. Per FREQ-01/FREQ-02/FREQ-03/SRC-01/SRC-02/SENT-01/SENT-02, item keys must come from the committed manifest, not a synthetic range.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_mvp.py -q</automated>
  </verify>
  <done>Service tests prove manifest-backed item order, version checks, license-gate summary, and source-type counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose source-pack inspection in the Latin CLI</name>
  <files>src/multilang/cli.py, tests/cli/test_generate_latin_mvp_command.py</files>
  <behavior>
    - `generate-latin-mvp` output includes `source_pack_version=latin-mvp-50-v1`, `item_count=50`, `first_item_key=latin-mvp-0001`, `last_item_key=latin-mvp-0050`, `license_gate_status=approved`, and source-type counts.
    - A new `--manifest-json` flag prints the validated manifest JSON summary without private paths and exits 0.
    - Existing `generate --language la` and `generate --source latin-mvp` remain rejected.
  </behavior>
  <action>Update the existing isolated `generate-latin-mvp` command to print manifest-backed summary lines and add `--manifest-json` for source-pack inspection. Keep this on the separate Latin command per Phase 22 decisions; do not add `la` to `SupportedLanguage` and do not add `latin-mvp` to the modern `generate --source` options.</action>
  <verify>
    <automated>python -m pytest tests/cli/test_generate_latin_mvp_command.py tests/cli/test_generate_command.py::test_generate_command_rejects_unsupported_language tests/cli/test_generate_command.py::test_public_kindle_highlights_source_remains_rejected -q</automated>
  </verify>
  <done>CLI tests prove users can inspect the frozen pack through the Latin path and existing modes remain isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest loader -> Latin service | Validated source-pack entries become generation start metadata. |
| Latin service -> CLI output | Source/provenance summary becomes terminal-visible user evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering | `LatinMvpGenerationService.start` | mitigate | Reject request/manifest version mismatch and derive item keys only from validated pack entries. |
| T-23-03-02 | Information Disclosure | `--manifest-json` output | mitigate | Emit public source identifiers and relative asset metadata only; tests reject private path markers. |
| T-23-03-03 | Elevation of Privilege | `generate` command boundaries | mitigate | Preserve tests that reject `la` and `latin-mvp` in the modern generation path. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py -q` plus the focused existing-generate rejection tests listed in Task 2.
</verification>

<success_criteria>
- Latin start output is backed by the frozen manifest and exposes inspectable source-pack summary fields.
- Users can inspect manifest metadata from the isolated Latin CLI path.
- Existing frequency/custom/highlight command boundaries remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-03-SUMMARY.md`
</output>
