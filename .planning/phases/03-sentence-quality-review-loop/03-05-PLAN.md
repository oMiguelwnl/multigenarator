---
phase: 03-sentence-quality-review-loop
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/multilang/services/ingest_lexical_items.py
  - src/multilang/services/regenerate_text_item.py
  - src/multilang/runtime.py
  - src/multilang/cli.py
  - tests/services/test_regenerate_text_item.py
  - tests/cli/test_generate_command.py
  - tests/integration/test_text_job_flow.py
autonomous: true
requirements:
  - TEXT-05
  - TEXT-01
  - TEXT-02
  - TEXT-03
  - TEXT-04
must_haves:
  truths:
    - The shipped `multilang generate` path composes lexical ingestion and Phase 3 text generation in one repository-backed flow.
    - Operators can regenerate one flagged item without rerunning the full batch.
    - Runtime and integration coverage prove Phase 3 works on top of stable Phase 2 lexical candidates.
  artifacts:
    - path: src/multilang/services/regenerate_text_item.py
      provides: Item-level text regeneration service
      contains: class RegenerateTextItemService
    - path: src/multilang/runtime.py
      provides: Runtime bootstrap for Phase 3 repositories and services
      contains: build_runtime_service
    - path: tests/integration/test_text_job_flow.py
      provides: Shipped-path text-stage and regeneration verification
      contains: def test_generate_command_regenerates_one_flagged_item_without_full_rerun
  key_links:
    - from: src/multilang/runtime.py
      to: src/multilang/services/generate_text_items.py
      via: repository-backed runtime composition sequences lexical ingestion, text generation, and review services
      pattern: GenerateTextItemsService|TextRepository|TextReviewService
    - from: src/multilang/cli.py
      to: src/multilang/services/regenerate_text_item.py
      via: item-level regeneration on the existing generate command
      pattern: regenerate_item_key|resume
---

<objective>
Finish Phase 3 by wiring item-level regeneration and end-to-end runtime verification.

Purpose: Deliver TEXT-05 on the shipped CLI path while keeping D-01, D-09, and D-10 intact: one `multilang generate` surface, item-level regeneration only, and runtime work that assumes Phase 2 re-verification has been closed before execution starts.
Output: Regeneration service, runtime/CLI integration, and shipped-path tests for normal text generation plus flagged-item regeneration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-sentence-quality-review-loop/03-CONTEXT.md
@.planning/phases/03-sentence-quality-review-loop/03-RESEARCH.md
@src/multilang/runtime.py
@src/multilang/cli.py
@src/multilang/services/ingest_lexical_items.py
@src/multilang/domain/jobs.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement item-level text regeneration against stable item identity</name>
  <read_first>
    .planning/STATE.md
    .planning/phases/03-sentence-quality-review-loop/03-CONTEXT.md
    src/multilang/domain/jobs.py
    src/multilang/repositories/text_repository.py
    src/multilang/services/generate_text_items.py
  </read_first>
  <files>src/multilang/services/regenerate_text_item.py, tests/services/test_regenerate_text_item.py</files>
  <behavior>
    - Test 1: regeneration targets exactly one `(job_id, item_key)` text row and leaves other rows untouched per D-09 and TEXT-05
    - Test 2: regeneration reuses the existing grounded lexical candidate and reruns `generate -> validate -> repair` without redoing lexical ingestion per D-03 and D-10
    - Test 3: a failed regenerated item stays flagged with updated provenance/review reasons instead of creating a second text row
  </behavior>
  <action>Create `src/multilang/services/regenerate_text_item.py` with `RegenerateTextItemService` that takes `job_id` plus `item_key`, reloads the existing grounded lexical candidate, clears only the targeted text row state that must be recomputed, and reruns the Phase 3 text pipeline for that one item. Keep the scope item-level only in v1 per D-09; do not add field-level regeneration. Write `tests/services/test_regenerate_text_item.py` first, then implement the service so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/services/regenerate_text_item.py` contains `class RegenerateTextItemService`
    - `src/multilang/services/regenerate_text_item.py` contains `item_key`
    - `tests/services/test_regenerate_text_item.py` contains `def test_regenerate_text_item_updates_only_requested_row`
    - `tests/services/test_regenerate_text_item.py` contains `def test_regenerate_text_item_reuses_grounded_lexical_candidate`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_regenerate_text_item.py -q</automated>
  </verify>
  <done>Phase 3 can regenerate one flagged card's text using stable job/item identity without rerunning the full batch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Phase 3 runtime execution and shipped-path regeneration coverage</name>
  <read_first>
    src/multilang/runtime.py
    src/multilang/cli.py
    src/multilang/services/ingest_lexical_items.py
    src/multilang/services/generate_text_items.py
    src/multilang/services/text_review.py
    tests/cli/test_generate_command.py
    tests/integration/test_text_job_flow.py
  </read_first>
  <files>src/multilang/services/ingest_lexical_items.py, src/multilang/runtime.py, src/multilang/cli.py, tests/cli/test_generate_command.py, tests/integration/test_text_job_flow.py</files>
  <behavior>
    - Test 1: the default shipped `multilang generate` path persists grounded lexical candidates, then generates sentence+translation text, validation state, and review-report diagnostics in one run per D-01 and D-02
    - Test 2: `multilang generate --resume <job_id> --regenerate-item-key <item_key>` reruns only the targeted flagged item without creating a new job or reprocessing all other items per D-09 and TEXT-05
    - Test 3: integration coverage uses the stable Phase 2 lexical candidate contract and assumes Phase 2 re-verification is closed before execution per D-10
  </behavior>
  <action>Extend the repository-backed runtime to compose Phase 2 lexical ingestion plus Phase 3 text services on the shipped path. Update `src/multilang/runtime.py` to construct `TextRepository`, `TextGenerationService`, `TextValidationService`, `TextReviewService`, and `RegenerateTextItemService`, and sequence lexical ingestion plus text generation at runtime without turning `src/multilang/services/ingest_lexical_items.py` into the owner of the full Phase 3 pipeline. If `ingest_lexical_items.py` needs changes, keep them minimal and lexical-stage-scoped, such as exposing the completed job/report data required for the next runtime step. In `src/multilang/cli.py`, keep the single `multilang generate` surface from D-01 and add only the minimal item-level regeneration flags needed for v1, such as `--regenerate-item-key` used with `--resume <job_id>`. Update `tests/cli/test_generate_command.py` and add shipped-path `tests/integration/test_text_job_flow.py` coverage proving normal runs persist accepted/flagged text rows plus review-report output, and proving item-level regeneration fixes or re-flags one card without a full rerun.</action>
  <acceptance_criteria>
    - `src/multilang/runtime.py` constructs `TextRepository`
    - `src/multilang/runtime.py` constructs `RegenerateTextItemService` or equivalent runtime wiring
    - `src/multilang/cli.py` contains `--regenerate-item-key`
    - `src/multilang/services/ingest_lexical_items.py` advances work to `JobStage.GENERATE_TEXT`
    - `tests/cli/test_generate_command.py` contains `def test_generate_command_regenerates_single_flagged_item`
    - `tests/integration/test_text_job_flow.py` contains `def test_generate_command_regenerates_one_flagged_item_without_full_rerun`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_generate_text_items.py tests/services/test_text_review.py tests/services/test_regenerate_text_item.py tests/cli/test_generate_command.py tests/integration/test_text_job_flow.py -q</automated>
  </verify>
  <done>The shipped runtime can generate reviewable text in one run and later regenerate one flagged item on the same `multilang generate` surface without rerunning the full batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped CLI request → runtime text pipeline | Operator requests can trigger full-job text generation or single-item regeneration |
| regeneration target → persisted text row | A targeted repair must update only the intended item and preserve auditability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | T | `src/multilang/services/regenerate_text_item.py` | mitigate | Require explicit `job_id` plus `item_key` targeting and update the existing text row in place so regeneration cannot spill into unrelated cards per D-09. |
| T-03-16 | R | `src/multilang/cli.py` | mitigate | Keep regeneration on the existing `multilang generate` surface with explicit flags and review-report output so operators can audit when a single card was rerun. |
| T-03-17 | D | `tests/integration/test_text_job_flow.py` | mitigate | Add shipped-path integration coverage for full text generation and single-item regeneration so runtime wiring regressions are caught before Phase 3 verification. |
| T-03-18 | T | `src/multilang/services/ingest_lexical_items.py` | mitigate | Reuse the stable Phase 2 lexical candidate contract and only execute after Phase 2 re-verification closes per D-10, preventing Phase 3 runtime work from papering over unstable upstream data. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_generate_text_items.py tests/services/test_text_review.py tests/services/test_regenerate_text_item.py tests/cli/test_generate_command.py tests/integration/test_text_job_flow.py -q`
- `uv run pytest tests/services/test_text_generation.py tests/services/test_text_validation.py tests/services/test_generate_text_items.py tests/services/test_text_review.py tests/integration/test_text_job_flow.py tests/cli/test_generate_command.py -q`
</verification>

<success_criteria>
- The shipped `multilang generate` path runs Phase 3 text generation on top of grounded lexical candidates.
- One flagged card can be regenerated by item identity without a full batch rerun.
- Integration coverage proves review/report and regeneration behavior on the repository-backed runtime path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sentence-quality-review-loop/03-05-SUMMARY.md`
</output>
