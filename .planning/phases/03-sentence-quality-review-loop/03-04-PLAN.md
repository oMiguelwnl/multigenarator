---
phase: 03-sentence-quality-review-loop
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/multilang/services/text_review.py
  - src/multilang/cli.py
  - tests/services/test_text_review.py
  - tests/cli/test_generate_command.py
autonomous: true
requirements:
  - TEXT-04
  - TEXT-05
must_haves:
  truths:
    - Low-confidence or failed text rows can be reviewed from the CLI before export work begins.
    - Review output is backed by a persisted report/queue artifact rather than transient console text only.
    - The saved review artifact preserves stable item identity required for later regeneration targeting.
    - The shipped operator surface remains centered on `multilang generate`.
  artifacts:
    - path: src/multilang/services/text_review.py
      provides: Review queue and report builder for flagged text rows
      contains: class TextReviewService
    - path: src/multilang/cli.py
      provides: CLI-first review/report output on `multilang generate`
      contains: review_report
    - path: tests/services/test_text_review.py
      provides: Deterministic review sorting and report formatting coverage
      contains: def test_build_review_report_orders_highest_risk_items_first
  key_links:
    - from: src/multilang/services/text_review.py
      to: src/multilang/repositories/text_repository.py
      via: flagged-row queries and report serialization
      pattern: list_flagged_records|review_reason
    - from: src/multilang/cli.py
      to: tests/cli/test_generate_command.py
      via: shipped-path report output for flagged items
      pattern: flagged_cards=|review_report=
---

<objective>
Add the CLI-first review queue and report flow for Phase 3.

Purpose: Make weak cards reviewable on the shipped operator path per D-08 without introducing a second top-level workflow or waiting for export-phase tooling.
Output: Review-report service plus `multilang generate` output that surfaces flagged counts, reasons, and a saved report path for later item-level regeneration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-sentence-quality-review-loop/03-CONTEXT.md
@.planning/phases/03-sentence-quality-review-loop/03-RESEARCH.md
@src/multilang/domain/text_quality.py
@src/multilang/repositories/text_repository.py
@src/multilang/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the flagged-text review queue and report serializer</name>
  <read_first>
    .planning/phases/03-sentence-quality-review-loop/03-CONTEXT.md
    .planning/phases/03-sentence-quality-review-loop/03-RESEARCH.md
    src/multilang/domain/text_quality.py
    src/multilang/repositories/text_repository.py
  </read_first>
  <files>src/multilang/services/text_review.py, tests/services/test_text_review.py</files>
  <behavior>
    - Test 1: review queries return only low-confidence or review-required rows with their reasons, current text payload, and confidence metadata per D-08
    - Test 2: report output sorts highest-risk rows first by review status, confidence, and flag count so operators can triage the worst cards first
    - Test 3: the saved report format contains stable identifiers (`job_id`, `item_key`) needed for later item-level regeneration per D-09
  </behavior>
  <action>Create `src/multilang/services/text_review.py` with `TextReviewService` and a serializable review-report model. Per D-08 and D-09, the service must query `TextRepository.list_flagged_records(...)`, map each row into a CLI/report payload containing `job_id`, `item_key`, `example_sentence`, `translation_text`, `confidence_label`, `validation_flags`, and `review_reason`, and serialize the report to a deterministic JSON file or equivalent plain report artifact that later regeneration can target by stable identity. Write `tests/services/test_text_review.py` first, then implement the service so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/services/text_review.py` contains `class TextReviewService`
    - `src/multilang/services/text_review.py` contains `build_review_report`
    - `tests/services/test_text_review.py` contains `def test_build_review_report_orders_highest_risk_items_first`
    - `tests/services/test_text_review.py` contains `def test_review_report_keeps_job_and_item_identity`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_text_review.py -q</automated>
  </verify>
  <done>Phase 3 has a deterministic review queue and saved report artifact for flagged text rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface flagged-card review output on `multilang generate`</name>
  <read_first>
    src/multilang/cli.py
    src/multilang/services/text_review.py
    tests/cli/test_generate_command.py
  </read_first>
  <files>src/multilang/cli.py, tests/cli/test_generate_command.py</files>
  <behavior>
    - Test 1: `multilang generate` prints `flagged_cards` and `review_report` when a run leaves review-required rows per D-08
    - Test 2: the CLI keeps one shipped top-level command and uses optional flags/summary output instead of adding a new command tree per D-01
    - Test 3: accepted runs print `flagged_cards=0` without writing an empty review artifact
  </behavior>
  <action>Extend `src/multilang/cli.py` so the shipped `multilang generate` path can surface review/report diagnostics after Phase 3 processing. Keep D-01 intact: do not add a second top-level review command. Add only the minimal output needed on the existing command surface, such as an optional `--review-report-file` path override plus summary lines `flagged_cards=` and `review_report=` when flagged rows exist. Update `tests/cli/test_generate_command.py` first using injected fake review output, then implement the CLI behavior so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/cli.py` contains `flagged_cards=`
    - `src/multilang/cli.py` contains `review_report=`
    - `src/multilang/cli.py` contains `--review-report-file`
    - `tests/cli/test_generate_command.py` contains `def test_generate_command_prints_review_report_for_flagged_text_rows`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_text_review.py tests/cli/test_generate_command.py -q</automated>
  </verify>
  <done>Operators can review flagged cards from the shipped `multilang generate` path through explicit counters and a saved review report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted flagged rows → review report artifact | Sensitive learner-facing text and diagnostics are exposed to operators |
| CLI summary → operator action | Review output drives which cards get regenerated or held back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | R | `src/multilang/services/text_review.py` | mitigate | Include `job_id`, `item_key`, validation flags, and review reason in each report row so review decisions remain auditable and actionable. |
| T-03-13 | I | `src/multilang/cli.py` | mitigate | Print explicit `flagged_cards` and `review_report` lines so operators do not mistake flagged output for fully accepted text. |
| T-03-14 | T | `src/multilang/cli.py` | mitigate | Keep review/report behavior on the existing `multilang generate` surface, avoiding a divergent command path that could bypass Phase 3 diagnostics per D-01 and D-08. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_text_review.py tests/cli/test_generate_command.py -q`
</verification>

<success_criteria>
- Flagged text rows are reviewable from the CLI before export work begins.
- Review output is backed by a persisted report artifact containing stable item identity.
- The operator surface stays centered on `multilang generate`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sentence-quality-review-loop/03-04-SUMMARY.md`
</output>
