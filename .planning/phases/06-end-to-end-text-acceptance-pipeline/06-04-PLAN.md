---
phase: 06-end-to-end-text-acceptance-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/integration/test_frequency_e2e_export_flow.py
autonomous: true
requirements: [DECK-02, TEXT-01, TEXT-02, TEXT-03, AUDI-01, AUDI-02, EXPT-01, EXPT-02, EXPT-03]
must_haves:
  truths:
    - "Frequency-deck inputs can move through accepted text, audio, and export."
    - "The frequency path exercises all three levels rather than only a custom list."
    - "The 3-level/1000-card contract remains protected while the E2E test uses a bounded sample."
  artifacts:
    - path: "tests/integration/test_frequency_e2e_export_flow.py"
      provides: "Frequency-deck accepted text/audio/export E2E proof"
      contains: "--source frequency"
  key_links:
    - from: "frequency request"
      to: "lexical grounding"
      via: "cards_per_level and level_counts"
      pattern: "level_1_candidates|level_2_candidates|level_3_candidates"
    - from: "accepted frequency rows"
      to: "exports"
      via: "same job id exported to apkg/csv/tsv"
      pattern: "ExportArtifactFormat"
---

<objective>
Add frequency-deck E2E evidence that accepted text, audio, and export work for the built-in deck path.

Purpose: DECK-02 is reopened because lexical 3x1000 generation is proven, but accepted text/audio/export was not proven for frequency inputs.
Output: A bounded, all-three-level frequency integration test plus a contract assertion preserving the 1000-cards-per-level default.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/02-input-decks-lexical-grounding/02-02-SUMMARY.md
@.planning/phases/06-end-to-end-text-acceptance-pipeline/06-01-SUMMARY.md
@src/multilang/services/frequency_decks.py
@src/multilang/cli.py
@tests/integration/test_export_job_flow.py

<interfaces>
From `src/multilang/services/frequency_decks.py`:
```python
LEVEL_WINDOWS = {1: (1, 1000), 2: (1001, 2000), 3: (2001, 3000)}
def build_frequency_deck(language: SupportedLanguage, *, required_count_per_level: int = 1000, ...) -> dict[int, list[LexicalCardCandidate]]: ...
```

From `src/multilang/cli.py`:
```python
# generate --source frequency --cards-per-level <n> prints level_1_candidates, level_2_candidates, level_3_candidates.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the frequency generate path across all three levels</name>
  <files>tests/integration/test_frequency_e2e_export_flow.py</files>
  <behavior>
    - `generate --language en --source frequency --cards-per-level 1` exits 0 with one candidate in each level.
    - The Kaikki fixture grounds the selected frequency items so `accepted_text_items=3` and `review_required_text_items=0`.
    - Audio is generated for all accepted rows with six persisted audio assets.
  </behavior>
  <action>Create `tests/integration/test_frequency_e2e_export_flow.py`. Build a local Kaikki index for the first curated English frequency tokens used by the request, or monkeypatch `wordfreq.iter_wordlist`/frequency candidate iteration in the test so the selected tokens are deterministic and have matching fixture entries. Use `--cards-per-level 1` to exercise levels 1, 2, and 3 within the <60s verification budget while preserving the production 1000-card default in Task 2. Use the same fake Azure adapter pattern as export/audio integration tests.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_frequency_e2e_export_flow.py -q</automated>
  </verify>
  <done>Frequency generation produces accepted text and audio for all three levels on the shipped CLI path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export the frequency sample and protect the 3x1000 contract</name>
  <files>tests/integration/test_frequency_e2e_export_flow.py</files>
  <behavior>
    - The frequency job exports successfully as `.apkg`, CSV, and TSV.
    - Exported artifact count matches the three accepted frequency rows.
    - A direct contract assertion verifies `build_frequency_deck` defaults to `required_count_per_level=1000` and `LEVEL_WINDOWS` remain 1-1000, 1001-2000, 2001-3000.
  </behavior>
  <action>Extend the frequency E2E test to export the generated job in all three formats via the shipped CLI. Add a focused contract test in the same file that inspects the `build_frequency_deck` signature/default and `LEVEL_WINDOWS` constants so the bounded E2E sample cannot be mistaken for a product-level reduction of the 3x1000 deck decision. Do not change production defaults to satisfy the sample test.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_frequency_e2e_export_flow.py tests/services/test_frequency_decks.py -q</automated>
  </verify>
  <done>Frequency path has all-three-level accepted text/audio/export proof, and the full 3-level/1000-card contract remains explicitly protected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| wordfreq/frequency candidates→lexical grounding | External corpus-derived tokens become user-facing deck candidates. |
| frequency sample evidence→milestone audit | Bounded E2E proof must not obscure the production 3x1000 requirement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-04-01 | T | frequency fixtures | mitigate | Use deterministic fixtures and assert level counts so all three levels are exercised. |
| T-06-04-02 | R | DECK-02 evidence | mitigate | Pair bounded E2E with explicit assertions for `LEVEL_WINDOWS` and the 1000 default. |
| T-06-04-03 | D | frequency E2E test | mitigate | Keep automated verification bounded with `--cards-per-level 1`; production defaults remain unchanged. |
</threat_model>

<verification>
Run `uv run pytest tests/integration/test_frequency_e2e_export_flow.py tests/services/test_frequency_decks.py -q`.
</verification>

<success_criteria>
- Frequency source no longer has an unproven accepted-text/audio/export path.
- All three levels are represented in the E2E evidence.
- The product's 3 levels × 1000 cards per level contract remains intact and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-text-acceptance-pipeline/06-04-SUMMARY.md`.
</output>
