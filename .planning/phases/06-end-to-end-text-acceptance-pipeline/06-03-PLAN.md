---
phase: 06-end-to-end-text-acceptance-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/integration/test_custom_word_list_e2e_export_flow.py
autonomous: true
requirements: [DECK-03, TEXT-01, TEXT-02, TEXT-03, AUDI-01, AUDI-02, EXPT-01, EXPT-02, EXPT-03]
must_haves:
  truths:
    - "A custom word list can reach accepted text, audio, and all export formats on the shipped path."
    - "Generated word and sentence audio exist before export."
    - "`.apkg`, CSV, and TSV artifacts are written from the same completed job."
  artifacts:
    - path: "tests/integration/test_custom_word_list_e2e_export_flow.py"
      provides: "Custom word-list accepted text/audio/export E2E proof"
      contains: "apkg"
  key_links:
    - from: "multilang generate"
      to: "multilang export"
      via: "persisted job_id from SQLite GenerationJob"
      pattern: "job.id"
    - from: "text rows"
      to: "audio/export"
      via: "accepted rows consumed by GenerateAudioItemsService and AssembleExportCardsService"
      pattern: "accepted_text_items=2"
---

<objective>
Add the audit-required shipped-path E2E proof for custom word list → accepted text → audio → `.apkg`/CSV/TSV export.

Purpose: DECK-03 and downstream audio/export requirements remain reopened until arbitrary custom vocabulary is proven beyond text acceptance.
Output: A deterministic integration test using a fake Azure adapter and local SQLite job data.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-audio-synthesis/04-05-SUMMARY.md
@.planning/phases/05-anki-safe-export-contract/05-04-SUMMARY.md
@.planning/phases/05-anki-safe-export-contract/05-05-SUMMARY.md
@.planning/phases/06-end-to-end-text-acceptance-pipeline/06-01-SUMMARY.md
@tests/integration/test_export_job_flow.py
@tests/integration/test_audio_job_flow.py
@src/multilang/cli.py

<interfaces>
From `tests/integration/test_export_job_flow.py`:
```python
class FakeAzureSpeechAdapter(AudioSynthesisAdapter):
    def available_voice_ids(self) -> set[str] | None: ...
    def synthesize(..., output_path: Path, ...) -> AudioSynthesisResponse: ...
```

From `src/multilang/cli.py`:
```python
# export command: multilang export --job-id <id> --format apkg|csv|tsv --output-dir <dir>
# success prints artifact_path=<path> and card_count=<n>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create custom-list E2E fixture and generate assertions</name>
  <files>tests/integration/test_custom_word_list_e2e_export_flow.py</files>
  <behavior>
    - A two-word custom list with grounded Kaikki fixture rows exits 0 on `generate`.
    - CLI output includes `grounded_candidates=2`, `accepted_text_items=2`, `review_required_text_items=0`, and `audio_processed_items=4`.
    - The database contains one job, two text rows with accepted review status, and four audio assets whose files start with fake MP3 bytes.
  </behavior>
  <action>Create `tests/integration/test_custom_word_list_e2e_export_flow.py` by reusing the fake Azure adapter pattern from `test_export_job_flow.py`. Use custom words that Plan 06-01 accepts without curated special-casing, for example `alpha` and `bravo`, with local Kaikki index fixture definitions and IPA. Monkeypatch `multilang.runtime.AzureSpeechAdapter` to the fake adapter, build `build_runtime_service(Settings(..., tatoeba_enabled=False))`, invoke the real Typer app, and assert persisted text/audio state.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_custom_word_list_e2e_export_flow.py -q</automated>
  </verify>
  <done>Custom input reaches accepted text and audio in one shipped generate invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export custom-list job to `.apkg`, CSV, and TSV</name>
  <files>tests/integration/test_custom_word_list_e2e_export_flow.py</files>
  <behavior>
    - The job produced in Task 1 exports successfully as `apkg`, `csv`, and `tsv`.
    - All three artifact files exist in the requested output directory.
    - `DeckExportModel` has three completed rows and `CardExportModel` has two card snapshots.
  </behavior>
  <action>Extend the same E2E test to fetch the persisted `GenerationJob.id`, invoke the shipped `export` command three times, and assert success outputs plus artifact files. Verify media-backed export by checking the same fake audio files remain present and by relying on runtime media validation before every export format. Do not bypass the CLI or call export services directly.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_custom_word_list_e2e_export_flow.py tests/integration/test_export_job_flow.py -q</automated>
  </verify>
  <done>One custom-list shipped-path job produces accepted text, word/sentence audio, `.apkg`, CSV, and TSV artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| custom word list→CLI | User-controlled word input enters job generation. |
| generated media→export artifacts | File paths and sound tags become importable Anki media references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-03-01 | T | custom fixture input | mitigate | Use existing lexical parser/Kaikki grounding and assert accepted text is persisted, not constructed in test. |
| T-06-03-02 | I | audio media paths | mitigate | Runtime export validates sound tags and media files before writing all formats. |
| T-06-03-03 | R | E2E evidence | mitigate | Assert CLI output, DB rows, and artifact files so evidence can be audited from multiple sources. |
</threat_model>

<verification>
Run `uv run pytest tests/integration/test_custom_word_list_e2e_export_flow.py tests/integration/test_export_job_flow.py -q`.
</verification>

<success_criteria>
- Custom word-list E2E no longer breaks at text acceptance.
- Downstream audio and all export formats are proven on the shipped CLI path.
- The test is deterministic and does not require live Azure credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-text-acceptance-pipeline/06-03-SUMMARY.md`.
</output>
