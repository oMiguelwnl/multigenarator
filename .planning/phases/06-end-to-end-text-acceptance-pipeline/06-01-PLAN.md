---
phase: 06-end-to-end-text-acceptance-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/runtime.py
  - src/multilang/services/local_text_adapter.py
  - tests/services/test_local_text_adapter.py
autonomous: true
requirements: [TEXT-01, TEXT-02, TEXT-03, DECK-03]
must_haves:
  truths:
    - "Realistic grounded vocabulary produces accepted text rows instead of review-only records."
    - "Accepted example sentences contain the target word and avoid weak meta templates."
    - "Translations describe the displayed example sentence, not only the isolated headword."
  artifacts:
    - path: "src/multilang/services/local_text_adapter.py"
      provides: "Runtime sentence and translation adapters for deterministic accepted local text"
      exports: ["LocalSentenceAdapter", "LocalTranslationAdapter"]
    - path: "src/multilang/runtime.py"
      provides: "Shipped runtime wiring for local text adapters"
      contains: "LocalSentenceAdapter"
    - path: "tests/services/test_local_text_adapter.py"
      provides: "Acceptance coverage for realistic local text generation"
  key_links:
    - from: "src/multilang/runtime.py"
      to: "src/multilang/services/local_text_adapter.py"
      via: "TextGenerationService adapter injection"
      pattern: "LocalSentenceAdapter|LocalTranslationAdapter"
    - from: "src/multilang/services/local_text_adapter.py"
      to: "src/multilang/services/text_validation.py"
      via: "sentences that pass TextValidationService"
      pattern: "TextValidationService"
---

<objective>
Make the shipped local text path strong enough for grounded custom/frequency vocabulary to produce accepted sentence/translation rows.

Purpose: Phase 6 closes the milestone audit GAP-1 where runtime templates route realistic vocabulary into review-only text and block downstream audio/export.
Output: A dedicated local text adapter module plus tests proving non-meta accepted examples and sentence-faithful translations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03-sentence-quality-review-loop/03-05-SUMMARY.md
@src/multilang/runtime.py
@src/multilang/services/text_generation.py
@src/multilang/services/text_validation.py

<interfaces>
From `src/multilang/services/text_generation.py`:
```python
class SentenceGenerationAdapter(Protocol):
    def generate_sentence(self, request: SentenceGenerationRequest) -> SentenceGenerationResult: ...

class SentenceTranslationAdapter(Protocol):
    def translate_sentence(self, request: SentenceTranslationRequest) -> SentenceTranslationResult: ...

class TextGenerationService:
    def generate_bundle(self, *, candidate: LexicalCardCandidate, deck_language: SupportedLanguage) -> GeneratedTextBundle: ...
```

From `src/multilang/services/text_validation.py`:
```python
class TextValidationService:
    min_sentence_tokens = 4
    max_sentence_tokens = 12
    def validate(..., display_form: str, lemma: str, definitions_html: str | None, ...) -> TextValidationResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract runtime text adapters behind tested contracts</name>
  <files>src/multilang/services/local_text_adapter.py, src/multilang/runtime.py, tests/services/test_local_text_adapter.py</files>
  <behavior>
    - For `alpha` with definition `definition for alpha`, generated English text contains `alpha`, has 4-12 tokens, and does not begin with `The word`.
    - For `harbor`, `lantern`, and `meadow`, existing curated English sentences and Portuguese translations remain available for smoke artifacts.
    - Translation output is not identical to the source sentence and is not identical to the lexical definition.
  </behavior>
  <action>Create `LocalSentenceAdapter` and `LocalTranslationAdapter` in `src/multilang/services/local_text_adapter.py`, moving the runtime-local text behavior out of `runtime.py`. Keep the curated `harbor`/`lantern`/`meadow` cases because Phase 5 review artifacts depend on them. Replace generic meta sentences like `The word {term} is useful...` with natural deterministic templates that include the display form as an object or subject without triggering `_looks_like_meta_sentence` or `_looks_like_short_command`. Wire `build_runtime_service()` to instantiate the new adapters. Do not weaken `TextValidationService`; fix generation to satisfy existing quality gates.</action>
  <verify>
    <automated>uv run pytest tests/services/test_local_text_adapter.py -q</automated>
  </verify>
  <done>Local adapters are importable, runtime uses them, curated smoke terms are preserved, and realistic generic terms produce validator-acceptable sentence/translation bundles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove accepted text through the existing generation service</name>
  <files>tests/services/test_local_text_adapter.py</files>
  <behavior>
    - `TextGenerationService(LocalSentenceAdapter, LocalTranslationAdapter)` plus `TextValidationService` accepts representative grounded candidates.
    - A verb-like candidate such as `usar` still produces a target-language sentence containing the requested form and a non-empty English translation.
  </behavior>
  <action>Extend `tests/services/test_local_text_adapter.py` with service-level tests that build `LexicalCardCandidate` fixtures and validate generated bundles with the real `TextValidationService`. Cover an English generic noun, a Spanish verb-like item, and one curated smoke term. Keep assertions focused on observable contract: accepted validation, target form present, no meta prefix, translation not copied from source/definition.</action>
  <verify>
    <automated>uv run pytest tests/services/test_local_text_adapter.py tests/services/test_text_validation.py tests/services/test_generate_text_items.py -q</automated>
  </verify>
  <done>Generated text bundles pass the real validator for representative grounded vocabulary without review-only routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| lexical candidate→text adapter | User-provided or frequency-derived text becomes generated study content. |
| generated text→validation | Generated content decides whether downstream audio/export are allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-01-01 | T | `LocalSentenceAdapter` | mitigate | Preserve target-form inclusion and banned-pattern tests through `TextValidationService`; do not bypass validation. |
| T-06-01-02 | I | `LocalTranslationAdapter` | mitigate | Ensure translations are not copied source/definition text so exported cards do not present misleading duplicate fields. |
| T-06-01-03 | D | runtime text generation | accept | Local deterministic templates are bounded CPU/string operations; no network or unbounded loops are introduced. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_local_text_adapter.py tests/services/test_text_validation.py tests/services/test_generate_text_items.py -q`.
</verification>

<success_criteria>
- The runtime no longer owns large inline template adapter classes.
- Generic grounded vocabulary can produce accepted text without weakening quality validators.
- The milestone audit's text-acceptance break point has a deterministic local generation fix ready for E2E tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-text-acceptance-pipeline/06-01-SUMMARY.md`.
</output>
