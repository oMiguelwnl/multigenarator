---
phase: 06-end-to-end-text-acceptance-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/integration/test_text_job_flow.py
autonomous: true
requirements: [TEXT-01, TEXT-02, TEXT-03, DECK-03]
must_haves:
  truths:
    - "The existing shipped text integration test reflects intended runtime behavior."
    - "Custom word-list terms generate accepted text rows when lexical grounding succeeds."
    - "Flagged placeholder cases still route to review for human repair."
  artifacts:
    - path: "tests/integration/test_text_job_flow.py"
      provides: "Current shipped-path text acceptance integration coverage"
      contains: "accepted_text_items"
  key_links:
    - from: "tests/integration/test_text_job_flow.py"
      to: "src/multilang/runtime.py"
      via: "build_runtime_service(Settings(...))"
      pattern: "build_runtime_service"
---

<objective>
Refresh the stale text integration test so it proves the intended accepted-text runtime behavior after Plan 06-01.

Purpose: The audit explicitly identified `tests/integration/test_text_job_flow.py` failing 3/3 and stale against current runtime behavior.
Output: Updated integration tests that pass and protect custom-list accepted text plus review routing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03-sentence-quality-review-loop/03-05-SUMMARY.md
@.planning/phases/06-end-to-end-text-acceptance-pipeline/06-01-SUMMARY.md
@tests/integration/test_text_job_flow.py
@src/multilang/cli.py

<interfaces>
From `src/multilang/cli.py`:
```python
def create_app(..., service: GenerateJobService | IngestLexicalItemsService | None = None, ...) -> typer.Typer: ...
# generate command prints grounded_candidates, pending_groundings, text_processed_items,
# accepted_text_items, review_required_text_items, audio_processed_items, and review_report.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh custom-list accepted text integration assertions</name>
  <files>tests/integration/test_text_job_flow.py</files>
  <behavior>
    - The custom word-list test with `alpha` and `flag-beta` exits 0.
    - `alpha` is accepted and `flag-beta` remains review-required.
    - Regenerating `flag-beta` updates only that item and keeps review-required status.
  </behavior>
  <action>Update stale expected strings and sentence assumptions in `tests/integration/test_text_job_flow.py` to match the intended Plan 06-01 local adapter behavior. Keep the review-only `flag-beta` path; it is a deliberate quality gate. Do not remove the assertions that prove accepted/review counts, stable row IDs, or review report creation.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_text_job_flow.py::test_generate_command_regenerates_one_flagged_item_without_full_rerun -q</automated>
  </verify>
  <done>The custom-list integration test passes and proves realistic grounded vocabulary can be accepted while bad placeholders stay review-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh language-specific text integration assertions</name>
  <files>tests/integration/test_text_job_flow.py</files>
  <behavior>
    - Spanish `usar` generation exits 0 and prints `accepted_text_items=1`.
    - Persisted Spanish sentence contains `usar` and is not a banned hollow support/meta template.
    - Persisted translation is non-empty and differs from the Spanish source sentence.
  </behavior>
  <action>Replace the stale exact Spanish sentence/translation literals with assertions tied to the product contract: correct language path, accepted status, target-form inclusion, and sentence-faithful non-copy translation. Keep the pending-groundings test intact and update only expectations that were stale per the milestone audit.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_text_job_flow.py -q</automated>
  </verify>
  <done>`tests/integration/test_text_job_flow.py` passes as the authoritative shipped-path text acceptance proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test fixture→runtime CLI | Integration fixtures simulate user-provided custom words entering shipped code. |
| runtime output→audit evidence | Test assertions become milestone evidence for reopened text requirements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-02-01 | R | `tests/integration/test_text_job_flow.py` | mitigate | Assert persisted DB rows and CLI counters so audit evidence is attributable and reproducible. |
| T-06-02-02 | T | text integration tests | mitigate | Do not weaken validator expectations; assert accepted/review status from persisted records. |
| T-06-02-03 | I | review report path | accept | Test data is synthetic and local; no secrets or PII are written. |
</threat_model>

<verification>
Run `uv run pytest tests/integration/test_text_job_flow.py -q`.
</verification>

<success_criteria>
- The audit-cited failing text integration file is green.
- Tests prove custom grounded vocabulary can produce accepted text rows.
- Review-required behavior remains intact for intentionally weak items.
</success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-text-acceptance-pipeline/06-02-SUMMARY.md`.
</output>
