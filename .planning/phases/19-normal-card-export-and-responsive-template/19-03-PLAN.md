---
phase: 19-normal-card-export-and-responsive-template
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - tests/integration/test_v13_normal_template_export_contract.py
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-03]
must_haves:
  truths:
    - "User can verify normal APKG/CSV/TSV exports use the revised field contract end-to-end."
    - "User can verify highlight APKG/CSV/TSV behavior remains unchanged after normal-card changes."
    - "User can verify phonetics template behavior remains unchanged after normal-card changes."
  artifacts:
    - path: "tests/integration/test_v13_normal_template_export_contract.py"
      provides: "End-to-end v1.3 regression evidence for normal contract/layout and isolated modes"
  key_links:
    - from: "tests/integration/test_v13_normal_template_export_contract.py"
      to: "src/multilang/services/export_anki_package.py"
      via: "build_multilang_model/export_anki_package"
      pattern: "Front of Card"
    - from: "tests/integration/test_v13_normal_template_export_contract.py"
      to: "src/multilang/services/export_tabular_bundle.py"
      via: "write_export_tabular_bundle"
      pattern: "#columns"
---

<objective>
Add integrated evidence that Phase 19 normal-card changes work end-to-end and do not regress isolated templates.

Purpose: TMPL-03 requires confidence that normal schema/CSS changes did not mutate highlight or phonetics behavior; TMPL-01 and TMPL-02 need artifact-level proof beyond unit assertions.
Output: A focused v1.3 integration test file that inspects normal APKG, CSV, TSV, template CSS, highlight fields, and phonetics template references.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-normal-card-export-and-responsive-template/19-01-SUMMARY.md
@.planning/phases/19-normal-card-export-and-responsive-template/19-02-SUMMARY.md
@src/multilang/domain/exporting.py
@src/multilang/services/export_anki_package.py
@src/multilang/services/export_tabular_bundle.py
@src/multilang/services/russian_phoneme_deck.py
@tests/integration/test_highlight_export_artifacts.py
@tests/integration/test_russian_phoneme_template_refresh_flow.py

<interfaces>
Use existing integration-test patterns:
```python
build_multilang_model(source_type="frequency")
export_anki_package(rows=[...], media_index={...}, output_path=..., deck_name=...)
write_export_tabular_bundle(rows=[...], export_format=ExportArtifactFormat.CSV|TSV, ...)
build_russian_phoneme_model()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove normal APKG/CSV/TSV revised contract and layout evidence per TMPL-01/TMPL-02</name>
  <files>tests/integration/test_v13_normal_template_export_contract.py</files>
  <behavior>
    - A synthetic normal/frequency APKG model has no `Front of Card` field and no `{{Front of Card}}` template reference.
    - Synthetic normal CSV and TSV headers omit `Front of Card` and include `word`, `IPA`, `Definitions`, `Example Sentence`, `Translation`, `word_audio`, `sentence_audio`, `Image` in the revised order.
    - Normal model CSS contains the sentence/audio flex selectors from 19-02.
  </behavior>
  <action>Create `tests/integration/test_v13_normal_template_export_contract.py` with synthetic media files and a normal `ExportCardRow`. Inspect APKG `collection.anki2` model JSON like existing export integration tests, then write CSV and TSV bundles and assert exact `#columns:` metadata. Assert template markup/CSS contains the new example sentence/audio row and never contains `Front of Card`. Keep fixtures synthetic only; do not read private APKG audit reports.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v13_normal_template_export_contract.py -q</automated>
  </verify>
  <done>Integrated normal export evidence proves APKG/CSV/TSV contract removal and responsive sentence-audio layout from exported artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove highlight and phonetics isolation per TMPL-03</name>
  <files>tests/integration/test_v13_normal_template_export_contract.py</files>
  <behavior>
    - Highlight model fields still equal `HIGHLIGHT_EXPORT_CARD_FIELD_NAMES`, omit `Translation`, and use `highlight_card` markup.
    - Word-list/manual model still uses the highlight/manual field contract and does not gain normal `word_audio`/`Translation` behavior.
    - Russian phoneme model fields and references remain `PHONEME_FIELD_NAMES` and do not include normal fields such as `word`, `Definitions`, `Translation`, or `Front of Card`.
  </behavior>
  <action>Extend the new v1.3 integration file with source-profile isolation checks. Reuse assertions/patterns from `test_highlight_export_artifacts.py` and `test_russian_phoneme_template_refresh_flow.py`, but keep this file focused on the Phase 19 regression boundary: normal schema/CSS changes must not mutate highlight or phonetics templates. Do not edit `highlight_card.md`, `russian_phoneme_card.md`, or phonetics production code.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v13_normal_template_export_contract.py tests/integration/test_highlight_export_artifacts.py tests/integration/test_russian_phoneme_template_refresh_flow.py -q</automated>
  </verify>
  <done>TMPL-03 regression evidence proves highlight and phonetics behavior remains isolated after the normal-card export/template changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration fixtures -> generated artifacts | Synthetic rows/media become APKG/CSV/TSV evidence files during tests. |
| normal template changes -> existing modes | Shared export loader/model code routes multiple source profiles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | R | v1.3 integration evidence | mitigate | Tests inspect actual generated artifact metadata rather than trusting in-memory constants only. |
| T-19-07 | I | test fixtures | mitigate | Use synthetic rows/media under `tmp_path`; do not read private `.multilang/audits` or deck excerpts. |
| T-19-08 | T | source-profile isolation | mitigate | Regression tests assert highlight/manual/phonetics fields and forbidden normal references after normal changes. |
</threat_model>

<verification>
Run `python -m pytest tests/integration/test_v13_normal_template_export_contract.py tests/integration/test_highlight_export_artifacts.py tests/integration/test_russian_phoneme_template_refresh_flow.py -q`.
</verification>

<success_criteria>
TMPL-01, TMPL-02, and TMPL-03 have integrated evidence: normal artifacts omit `Front of Card`, normal sentence audio sits beside the example sentence, and highlight/phonetics behavior remains isolated.
</success_criteria>

<output>
After completion, create `.planning/phases/19-normal-card-export-and-responsive-template/19-03-SUMMARY.md`.
</output>
