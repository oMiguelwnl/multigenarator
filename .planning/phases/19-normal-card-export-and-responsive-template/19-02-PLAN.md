---
phase: 19-normal-card-export-and-responsive-template
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/multilang/templates/normal_card.md
  - tests/services/test_card_template_loader.py
  - tests/services/test_export_anki_package.py
autonomous: true
requirements: [TMPL-02]
must_haves:
  truths:
    - "User sees `sentence_audio` beside `Example Sentence` on normal cards at desktop widths."
    - "User sees `sentence_audio` beside `Example Sentence` on normal cards at mobile card widths."
    - "Normal card translation reveal still works after layout changes."
  artifacts:
    - path: "src/multilang/templates/normal_card.md"
      provides: "Responsive normal example sentence/audio layout"
      contains: "exampleSentenceLine"
    - path: "tests/services/test_card_template_loader.py"
      provides: "Template assertions for normal responsive sentence audio CSS"
  key_links:
    - from: "src/multilang/templates/normal_card.md"
      to: "src/multilang/services/card_template_loader.py"
      via: "load_card_template(source_type='frequency')"
      pattern: "exampleSentenceLine"
---

<objective>
Make the normal-card example sentence and `sentence_audio` render as one responsive line.

Purpose: TMPL-02 requires the sentence audio control to sit visually beside `Example Sentence` at desktop and mobile card widths instead of dropping below on larger cards.
Output: Normal template HTML/CSS with a dedicated sentence/audio flex row and regression assertions for responsive-safe markup.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@.planning/phases/19-normal-card-export-and-responsive-template/19-01-SUMMARY.md
@src/multilang/templates/normal_card.md
@src/multilang/services/card_template_loader.py
@tests/services/test_card_template_loader.py
@tests/services/test_export_anki_package.py

<interfaces>
Normal template loading remains markdown-section based:
```python
template = load_card_template(source_type="frequency")
template.front  # normal qfmt
template.back   # normal afmt, includes translation reveal
template.css    # normal styling passed to genanki model
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normal example sentence/audio flex markup per TMPL-02</name>
  <files>src/multilang/templates/normal_card.md, tests/services/test_card_template_loader.py</files>
  <behavior>
    - Normal front template wraps `{{Example Sentence}}` and `{{sentence_audio}}` in a single dedicated row, e.g. `.exampleSentenceLine`.
    - The sentence text and sentence audio are siblings in that row, not separate block lines.
    - `{{Translation}}` remains hidden in the front template and revealed by the back template script.
  </behavior>
  <action>Update only `normal_card.md` front markup around the example section so `Example Sentence` and `sentence_audio` are sibling elements inside a named row such as `<div class="exampleSentenceLine">`. Keep the translation container id `translation`, hidden front behavior, and back `{{FrontSide}}` reveal script intact. Add loader tests that assert the row contains both template references and that the translation reveal markup/script remains unchanged.</action>
  <verify>
    <automated>python -m pytest tests/services/test_card_template_loader.py -q</automated>
  </verify>
  <done>Normal template markup makes sentence text and sentence audio a single layout unit while preserving translation reveal behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add responsive CSS for beside-sentence audio per TMPL-02</name>
  <files>src/multilang/templates/normal_card.md, tests/services/test_card_template_loader.py, tests/services/test_export_anki_package.py</files>
  <behavior>
    - `.exampleSentenceLine` uses flexbox with `align-items: center` and a gap between text and audio.
    - The sentence text can wrap within available width without forcing `sentence_audio` onto a separate block below at normal desktop width.
    - Mobile card width remains bounded by the existing normal card max-width and does not require horizontal scrolling.
  </behavior>
  <action>Add CSS for the new normal example row using flexbox. Use a text child class such as `.exampleSentenceText { flex: 1 1 auto; min-width: 0; }` and an audio child class such as `.sentenceAudioButton { flex: 0 0 auto; margin-left: 8px; }`. Do not modify `highlight_card.md` or `russian_phoneme_card.md`; those templates have separate layout contracts. Update APKG/template tests to assert these CSS selectors are present in the normal model CSS.</action>
  <verify>
    <automated>python -m pytest tests/services/test_card_template_loader.py tests/services/test_export_anki_package.py -q</automated>
  </verify>
  <done>Normal model CSS includes responsive flex rules that keep `sentence_audio` visually beside `Example Sentence` across card widths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template CSS -> Anki renderer | CSS controls presentation in desktop/mobile Anki card webviews. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | D | `normal_card.md` example row | mitigate | CSS uses bounded flex and `min-width: 0` to avoid mobile overflow/horizontal scrolling. |
| T-19-05 | T | translation reveal markup | mitigate | Tests assert the `translation` id and back reveal script remain unchanged while layout changes. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_card_template_loader.py tests/services/test_export_anki_package.py -q`.
</verification>

<success_criteria>
TMPL-02 is satisfied: normal cards place `sentence_audio` beside `Example Sentence` using responsive markup/CSS, without breaking the normal back-side translation reveal.
</success_criteria>

<output>
After completion, create `.planning/phases/19-normal-card-export-and-responsive-template/19-02-SUMMARY.md`.
</output>
