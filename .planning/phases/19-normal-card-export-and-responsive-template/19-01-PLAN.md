---
phase: 19-normal-card-export-and-responsive-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/domain/exporting.py
  - src/multilang/templates/normal_card.md
  - tests/domain/test_exporting.py
  - tests/services/test_export_anki_package.py
  - tests/services/test_export_tabular_bundle.py
  - tests/services/test_card_template_loader.py
autonomous: true
requirements: [TMPL-01, TMPL-03]
must_haves:
  truths:
    - "User receives normal APKG exports whose note fields omit `Front of Card`."
    - "User receives normal CSV and TSV exports whose import headers omit `Front of Card`."
    - "Normal card templates reference `word`, not the removed `Front of Card` field."
    - "Highlight and word-list field contracts remain source-profile-isolated while normal fields change."
  artifacts:
    - path: "src/multilang/domain/exporting.py"
      provides: "Normal export field tuple without `Front of Card`"
      contains: "FREQUENCY_EXPORT_CARD_FIELD_NAMES"
    - path: "src/multilang/templates/normal_card.md"
      provides: "Normal template without dangling `Front of Card` references"
      contains: "{{word}}"
    - path: "tests/domain/test_exporting.py"
      provides: "Domain regression for revised normal field order"
  key_links:
    - from: "src/multilang/domain/exporting.py"
      to: "src/multilang/services/export_anki_package.py"
      via: "export_field_names_for_source_type('frequency')"
      pattern: "FREQUENCY_EXPORT_CARD_FIELD_NAMES"
    - from: "src/multilang/templates/normal_card.md"
      to: "src/multilang/services/card_template_loader.py"
      via: "validate_template_references"
      pattern: "{{word}}"
---

<objective>
Remove the redundant `Front of Card` field from the normal generated-card export contract.

Purpose: TMPL-01 requires APKG, CSV, TSV, and template references to stop exporting or referencing the duplicated normal-card front field while keeping non-normal source profiles isolated per TMPL-03.
Output: Revised normal export field tuple, normal template field reference, and focused regression tests for APKG/tabular/template loading.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@.planning/phases/18-text-field-remediation/18-01-PLAN.md
@.planning/phases/18-text-field-remediation/18-02-PLAN.md
@.planning/phases/18-text-field-remediation/18-03-PLAN.md
@src/multilang/domain/exporting.py
@src/multilang/services/export_anki_package.py
@src/multilang/services/export_tabular_bundle.py
@src/multilang/services/card_template_loader.py
@src/multilang/templates/normal_card.md
@src/multilang/templates/highlight_card.md
@tests/domain/test_exporting.py
@tests/services/test_export_anki_package.py
@tests/services/test_export_tabular_bundle.py
@tests/services/test_card_template_loader.py

<interfaces>
Existing contracts to update directly:

From `src/multilang/domain/exporting.py`:
```python
FREQUENCY_EXPORT_CARD_FIELD_NAMES = (...)
class ExportCardRow(BaseModel):
    word: str = Field(min_length=1, alias="word")
    front_of_card: str = Field(min_length=1, alias="Front of Card")
    def ordered_field_mapping(self, *, field_names: tuple[str, ...] | None = None) -> dict[str, object]: ...
def export_field_names_for_source_type(source_type: str) -> tuple[str, ...]: ...
```

From `src/multilang/services/card_template_loader.py`:
```python
def load_card_template(source_type: str) -> CardTemplate: ...
def validate_template_references(template: CardTemplate, field_names: tuple[str, ...]) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revise the normal export field contract per TMPL-01</name>
  <files>src/multilang/domain/exporting.py, tests/domain/test_exporting.py</files>
  <behavior>
    - `FREQUENCY_EXPORT_CARD_FIELD_NAMES` is exactly `SortIndex, word, IPA, Definitions, Example Sentence, Translation, word_audio, sentence_audio, Image`.
    - `ExportCardRow.ordered_field_mapping(source_type='frequency')` returns no `Front of Card` key.
    - `HIGHLIGHT_EXPORT_CARD_FIELD_NAMES` and `MANUAL_EXPORT_CARD_FIELD_NAMES` remain unchanged.
  </behavior>
  <action>Update the normal generated-card field tuple to remove `Front of Card` per TMPL-01. Keep `ExportCardRow.front_of_card` only if needed as an internal/backward-compatible construction attribute for existing assembly code, but do not include it in normal APKG/CSV/TSV ordered mappings. Preserve the existing alias synthesis for `Word` and `Definition` used by highlight/word-list rows; do not change `HIGHLIGHT_EXPORT_CARD_FIELD_NAMES` or `MANUAL_EXPORT_CARD_FIELD_NAMES` because TMPL-03 requires isolation.</action>
  <verify>
    <automated>python -m pytest tests/domain/test_exporting.py -q</automated>
  </verify>
  <done>Normal export field ordering omits `Front of Card`, and source-profile-aware highlight/manual contracts are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove normal template and artifact references to `Front of Card` per TMPL-01</name>
  <files>src/multilang/templates/normal_card.md, tests/services/test_export_anki_package.py, tests/services/test_export_tabular_bundle.py, tests/services/test_card_template_loader.py</files>
  <behavior>
    - `build_multilang_model(source_type='frequency')` creates normal model fields without `Front of Card`.
    - Normal template markup contains `{{word}}` for the target word and contains no `{{Front of Card}}` reference.
    - Normal CSV and TSV `#columns:` headers omit `Front of Card`; row values still display the target word through `word`.
    - Template validation rejects a normal template that still references `Front of Card` against the revised normal field tuple.
  </behavior>
  <action>Change `src/multilang/templates/normal_card.md` so the target word renders from `{{word}}` and no normal template section references `Front of Card`. Update APKG and CSV/TSV tests to assert the revised normal field list and headers. Add/adjust template-loader tests so dangling `Front of Card` references fail after the normal field removal. Keep `Translation`, `word_audio`, `sentence_audio`, `Image`, and all highlight-template assertions unchanged.</action>
  <verify>
    <automated>python -m pytest tests/services/test_export_anki_package.py tests/services/test_export_tabular_bundle.py tests/services/test_card_template_loader.py tests/domain/test_exporting.py -q</automated>
  </verify>
  <done>Normal APKG, CSV, TSV, and template references no longer expose `Front of Card`, with no highlight/manual regressions in focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export row -> APKG/CSV/TSV | Frozen internal card rows become importable Anki artifacts. |
| markdown template -> genanki model | Template references are compiled into a note model field contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | T | `FREQUENCY_EXPORT_CARD_FIELD_NAMES` | mitigate | Tests assert the exact revised normal field tuple and reject the removed field in APKG/tabular outputs. |
| T-19-02 | D | `validate_template_references` | mitigate | Template validation must fail if `normal_card.md` still references `Front of Card` after the field removal. |
| T-19-03 | T | source-profile export routing | mitigate | Preserve tests proving highlight/manual field tuples remain unchanged. |
</threat_model>

<verification>
Run `python -m pytest tests/domain/test_exporting.py tests/services/test_export_anki_package.py tests/services/test_export_tabular_bundle.py tests/services/test_card_template_loader.py -q`.
</verification>

<success_criteria>
TMPL-01 is satisfied for normal APKG, CSV, TSV, and template references; TMPL-03 has focused source-profile regression coverage for unchanged highlight/manual contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/19-normal-card-export-and-responsive-template/19-01-SUMMARY.md`.
</output>
