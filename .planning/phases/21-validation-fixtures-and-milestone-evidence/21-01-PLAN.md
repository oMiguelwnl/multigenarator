---
phase: 21-validation-fixtures-and-milestone-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/v13_validation.py
  - tests/services/test_v13_validation.py
autonomous: true
requirements: [VAL-01]
must_haves:
  truths:
    - "User can run one deterministic validation surface for IPA word repetition, banned Definition patterns, Translation/example mismatch, word_audio/Word mismatch, and dangling template fields."
    - "User receives normalized issue types and field-specific diagnostics without reading private deck excerpts or absolute paths."
  artifacts:
    - path: "src/multilang/services/v13_validation.py"
      provides: "Reusable v1.3 validator facade over text, template, and audio integrity checks"
      exports: ["V13ValidationIssue", "V13ValidationIssueType", "validate_v13_card", "validate_v13_template_contract"]
    - path: "tests/services/test_v13_validation.py"
      provides: "Focused coverage for every VAL-01 validator branch"
  key_links:
    - from: "src/multilang/services/v13_validation.py"
      to: "src/multilang/services/text_field_remediation.py"
      via: "validate_definition_html for banned Definition patterns"
      pattern: "validate_definition_html"
    - from: "src/multilang/services/v13_validation.py"
      to: "src/multilang/services/text_validation.py"
      via: "TextValidationService for Translation/example mismatch"
      pattern: "TextValidationService"
    - from: "src/multilang/services/v13_validation.py"
      to: "src/multilang/services/audio_integrity.py"
      via: "assert_word_audio_matches_word for Phase 20 word_audio/Word checks"
      pattern: "assert_word_audio_matches_word"
---

<objective>
Create the shared v1.3 validation surface for all normalized issue categories.

Purpose: VAL-01 requires validators that can be run repeatably for the defects cataloged in `card_issues_normalized.md`, including the Phase 20 audio-integrity gate.
Output: A tested `v13_validation` service that normalizes card-quality findings for downstream fixtures and milestone evidence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@.planning/phases/17-deck-quality-audit-and-issue-reports/17-03-SUMMARY.md
@.planning/phases/18-text-field-remediation/18-02-SUMMARY.md
@.planning/phases/18-text-field-remediation/18-03-SUMMARY.md
@.planning/phases/19-normal-card-export-and-responsive-template/19-03-SUMMARY.md
@.planning/phases/20-word-audio-integrity-gate/20-03-SUMMARY.md
@src/multilang/domain/exporting.py
@src/multilang/services/text_field_remediation.py
@src/multilang/services/text_validation.py
@src/multilang/services/card_template_loader.py
@src/multilang/services/audio_integrity.py

<interfaces>
Use these existing contracts directly:
```python
class ExportCardRow(BaseModel):
    word: str
    ipa: str | None
    definitions: str
    example_sentence: str
    translation: str
    word_audio: str
    sentence_audio: str

def validate_definition_html(*, lemma_key: str, definitions_html: str | None) -> None: ...
class TextValidationService:
    def validate(..., sentence: GeneratedSentence, translation: GeneratedTranslation, display_form: str, lemma: str, definitions_html: str | None, require_translation: bool = True, ...) -> TextValidationResult: ...
def validate_template_references(template: CardTemplate, field_names: tuple[str, ...]) -> None: ...
def assert_word_audio_matches_word(asset: AudioAssetRecord, expected_word: str, item_key: str | None = None) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize text-field validators for VAL-01</name>
  <files>tests/services/test_v13_validation.py, src/multilang/services/v13_validation.py</files>
  <behavior>
    - A card with IPA `[ˈɡromkə] (гро́мко)` for word `громко` returns an `ipa_word_repetition` issue for field `IPA`.
    - A card with Definition `noun: inflection of заболева́ние` or `adjective: masculine animate accusative singular` returns a `definition_banned_pattern` issue for field `Definitions`.
    - A card whose Translation is `to achieve` while Example Sentence is `Он хочет достичь цели завтра.` returns a `translation_example_mismatch` issue for field `Translation`.
    - Learner-safe IPA, Definition, and full-sentence Translation return no text-field issues.
  </behavior>
  <action>Create `tests/services/test_v13_validation.py` first. Then create `src/multilang/services/v13_validation.py` with `V13ValidationIssueType` enum values `ipa_word_repetition`, `definition_banned_pattern`, `translation_example_mismatch`, `word_audio_word_mismatch`, and `dangling_template_field`; an immutable `V13ValidationIssue` dataclass with `field_name`, `issue_type`, `severity`, and bounded `message`; and `validate_v13_card(row, *, word_audio_asset=None) -> list[V13ValidationIssue]`. Implement IPA repetition by rejecting parenthetical or bracket-adjacent repetitions of `row.word` after accent-insensitive/casefold normalization, while allowing the exact word as the entire IPA fallback when no phonetic transcription exists. Delegate Definition checks to `validate_definition_html`. Delegate Translation checks to `TextValidationService` with `GeneratedSentence` and `GeneratedTranslation` wrappers, `display_form=row.word`, `lemma=row.word`, and `definitions_html=row.definitions`. Keep messages field-specific and omit absolute paths/private source text.</action>
  <verify>
    <automated>python -m pytest tests/services/test_v13_validation.py -q</automated>
  </verify>
  <done>VAL-01 text validators produce normalized issue objects for IPA, Definition, and Translation failures and pass clean card rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add audio and template validators for VAL-01</name>
  <files>tests/services/test_v13_validation.py, src/multilang/services/v13_validation.py</files>
  <behavior>
    - A row whose `word_audio` asset metadata says another word returns a `word_audio_word_mismatch` issue for field `word_audio` using the Phase 20 helper.
    - A valid Phase 20 WORD asset for the same `row.word` produces no audio issue.
    - A template contract containing `{{Front of Card}}` against the normal v1.3 field tuple returns a `dangling_template_field` issue.
    - The normal v1.3 template and field tuple produce no dangling-template issue.
  </behavior>
  <action>Extend the validator service with `validate_v13_template_contract(template, field_names) -> list[V13ValidationIssue]` that wraps `validate_template_references` and maps failures to `dangling_template_field`. In `validate_v13_card`, when `word_audio_asset` is supplied and `row.word_audio` is non-empty, call `assert_word_audio_matches_word(word_audio_asset, row.word, item_key=row.identity.item_key)` and map `AudioIntegrityError` to `word_audio_word_mismatch`. Do not synthesize audio, read media files, or parse APKGs in this service; it is a deterministic validator facade over existing boundaries.</action>
  <verify>
    <automated>python -m pytest tests/services/test_v13_validation.py tests/services/test_text_field_remediation.py tests/services/test_text_validation.py tests/services/test_audio_integrity.py tests/services/test_card_template_loader.py -q</automated>
  </verify>
  <done>VAL-01 has one reusable service covering all required validator categories and preserving existing underlying validators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export row -> validation service | Learner-facing card data enters deterministic validation before evidence/export confidence is claimed. |
| audio metadata -> validation service | Stored Phase 20 audio metadata may be stale or tampered before validation. |
| template text -> field contract | Template references can point to fields not exported in the active source profile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | `validate_v13_card` | mitigate | Delegate Definition, Translation, and word-audio checks to the shipped validators and normalize failures into issue objects. |
| T-21-02 | Information Disclosure | `V13ValidationIssue.message` | mitigate | Bound messages and omit absolute paths, private source text, and raw deck excerpts. |
| T-21-03 | Repudiation | validator issue taxonomy | mitigate | Emit stable issue type enum values for each normalized defect class. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_v13_validation.py tests/services/test_text_field_remediation.py tests/services/test_text_validation.py tests/services/test_audio_integrity.py tests/services/test_card_template_loader.py -q` and confirm all tests pass.
</verification>

<success_criteria>
- VAL-01 validators exist for all five required issue categories.
- Validator results use stable issue type names that fixture and evidence tests can assert directly.
- Existing text, audio, and template validator behavior is reused rather than duplicated inconsistently.
</success_criteria>

<output>
After completion, create `.planning/phases/21-validation-fixtures-and-milestone-evidence/21-01-SUMMARY.md`.
</output>
