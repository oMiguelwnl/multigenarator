---
phase: 21-validation-fixtures-and-milestone-evidence
plan: 04
type: execute
wave: 4
depends_on: [21-01, 21-02]
files_modified:
  - src/multilang/services/v13_validation.py
  - tests/fixtures/v13/card_issues_normalized_cases.json
  - tests/integration/test_v13_normalized_issue_fixtures.py
autonomous: true
gap_closure: true
requirements: [VAL-01, VAL-02]
must_haves:
  truths:
    - "Sentence-audio layout validation cannot be bypassed by valid Anki template references with whitespace."
    - "User gets regression coverage for the normalized sentence_audio layout issue across valid Anki field-reference formatting."
    - "Existing literal `{{sentence_audio}}` and corrected selector coverage continue to pass through the same VAL-02 fixture runner."
  artifacts:
    - path: "src/multilang/services/v13_validation.py"
      provides: "Whitespace-tolerant sentence_audio template-reference detection before responsive layout enforcement"
      exports: ["validate_v13_template_contract"]
    - path: "tests/fixtures/v13/card_issues_normalized_cases.json"
      provides: "VAL-02 fixture row for `{{ sentence_audio }}` missing required layout selectors"
    - path: "tests/integration/test_v13_normalized_issue_fixtures.py"
      provides: "Executable fixture runner proving whitespace-formatted sentence_audio references emit sentence_audio_layout"
  key_links:
    - from: "src/multilang/services/v13_validation.py"
      to: "tests/fixtures/v13/card_issues_normalized_cases.json"
      via: "validate_v13_template_contract detects whitespace-formatted sentence_audio references"
      pattern: "\\{\\{\\s*sentence_audio\\s*\\}\\}"
    - from: "tests/integration/test_v13_normalized_issue_fixtures.py"
      to: "src/multilang/services/v13_validation.py"
      via: "fixture cases call validate_v13_template_contract for template validators"
      pattern: "validate_v13_template_contract"
---

<objective>
Close the Phase 21 verification gap in sentence_audio layout validation.

Purpose: Verification found that `validate_v13_template_contract` only detects the literal `{{sentence_audio}}` substring, so valid Anki references with whitespace such as `{{ sentence_audio }}` bypass the VAL-02 layout fixture and leave normalized issue coverage incomplete.
Output: A whitespace-tolerant detector plus an executable regression fixture proving malformed whitespace-formatted sentence_audio templates emit `sentence_audio_layout`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-validation-fixtures-and-milestone-evidence/21-VERIFICATION.md
@.planning/phases/21-validation-fixtures-and-milestone-evidence/21-REVIEW.md
@.planning/phases/21-validation-fixtures-and-milestone-evidence/21-01-SUMMARY.md
@.planning/phases/21-validation-fixtures-and-milestone-evidence/21-02-SUMMARY.md
@src/multilang/services/v13_validation.py
@tests/fixtures/v13/card_issues_normalized_cases.json
@tests/integration/test_v13_normalized_issue_fixtures.py

<interfaces>
Use these existing contracts directly:
```python
def validate_v13_template_contract(
    template: CardTemplate,
    field_names: tuple[str, ...],
) -> list[V13ValidationIssue]: ...

class V13ValidationIssueType(StrEnum):
    SENTENCE_AUDIO_LAYOUT = "sentence_audio_layout"

class CardTemplate:
    front: str
    back: str
    css: str
    source_template_name: str
```

Current failing edge from verification:
```python
CardTemplate(
    front='<div>{{Example Sentence}}</div><div>{{ sentence_audio }}</div>',
    back='{{FrontSide}}<div>{{Definitions}}</div>',
    css='.card { color: #222; }',
    source_template_name='sentence_audio_whitespace_missing_layout',
)
```
This template must produce `V13ValidationIssueType.SENTENCE_AUDIO_LAYOUT` because `sentence_audio` is present but `exampleSentenceLine`, `exampleSentenceText`, and `sentenceAudioButton` are absent.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Repeatable validation/evidence that normalized issue catalog is covered | 21-04 | COVERED | Closes verifier truth #9 and the partial VAL-02 layout fixture coverage.
REQ | VAL-01 | User can run validators for template issues including dangling fields and sentence_audio layout | 21-04 | COVERED | Tightens the existing validator facade without changing the issue taxonomy.
REQ | VAL-02 | Regression fixtures cover normalized issue examples from `card_issues_normalized.md` | 21-04 | COVERED | Adds the missing valid-whitespace Anki reference fixture.
RESEARCH | pytest deterministic validation | 21-04 | COVERED | Uses focused pytest and JSON fixture validation; no live providers.
CONTEXT | Phase 21 privacy-safe synthetic fixtures | 21-04 | COVERED | Adds synthetic template markup only; no private APKG excerpts, paths, or text.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add whitespace-formatted sentence_audio layout regression fixture</name>
  <files>tests/fixtures/v13/card_issues_normalized_cases.json, tests/integration/test_v13_normalized_issue_fixtures.py</files>
  <behavior>
    - Fixture case `sentence_audio_layout_bad_whitespace_reference` uses `{{ sentence_audio }}` in `front`, omits `exampleSentenceLine`, `exampleSentenceText`, and `sentenceAudioButton`, and expects exactly `["sentence_audio_layout"]` with `expected_pass: false`.
    - Existing `sentence_audio_layout_bad_missing_selectors` and `sentence_audio_layout_corrected_selectors` cases continue to assert the literal bad and corrected selector behaviors.
    - Fixture catalog remains valid JSON and keeps `sentence_audio_layout` coverage metadata tied to source lines 92-105 / summary action line 129.
  </behavior>
  <action>Add the missing VAL-02 fixture row for the verifier gap. Place it in the existing `sentence_audio_layout` group beside the literal-reference bad case. Use synthetic template strings only. Do not add private deck text or absolute paths. If the integration fixture runner already executes every JSON case, keep code changes to the JSON file; only modify `tests/integration/test_v13_normalized_issue_fixtures.py` if an explicit assertion is needed to make the whitespace-reference edge unmistakable in failure output.</action>
  <verify>
    <automated>python -m json.tool tests/fixtures/v13/card_issues_normalized_cases.json</automated>
    <automated>python -m pytest tests/integration/test_v13_normalized_issue_fixtures.py -q</automated>
  </verify>
  <done>The fixture suite fails before the production detector change because `sentence_audio_layout_bad_whitespace_reference` returns no issue instead of `sentence_audio_layout`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make sentence_audio reference detection whitespace-tolerant</name>
  <files>src/multilang/services/v13_validation.py, tests/services/test_v13_validation.py</files>
  <behavior>
    - `validate_v13_template_contract` treats `{{sentence_audio}}`, `{{ sentence_audio }}`, and other valid whitespace around `sentence_audio` as the same field reference for deciding whether layout selectors are required.
    - A template with whitespace-formatted `sentence_audio` and missing required selectors emits one `V13ValidationIssueType.SENTENCE_AUDIO_LAYOUT` issue.
    - A template with whitespace-formatted `sentence_audio` and all three selectors returns no layout issue.
    - Dangling-field detection remains first: invalid references still return `DANGLING_TEMPLATE_FIELD` instead of layout issues.
  </behavior>
  <action>Replace the literal `"{{sentence_audio}}" in template.front + template.back` check with a whitespace-tolerant Anki field reference detector such as `re.search(r"{{\s*sentence_audio\s*}}", template_markup)`. Keep the detector scoped to `template.front + template.back` so CSS-only text does not trigger layout enforcement. Preserve the existing required selector set (`exampleSentenceLine`, `exampleSentenceText`, `sentenceAudioButton`) and bounded issue message. Add focused service-level tests only if the fixture regression does not already make each behavior clear; do not change public issue enum values or field names.</action>
  <verify>
    <automated>python -m pytest tests/services/test_v13_validation.py tests/integration/test_v13_normalized_issue_fixtures.py -q</automated>
    <automated>python -m pytest tests/integration/test_v13_final_milestone_evidence.py tests/integration/test_v13_existing_modes_regression_evidence.py -q</automated>
  </verify>
  <done>All Phase 21 focused validation/evidence suites pass, and the whitespace-formatted sentence_audio fixture emits `sentence_audio_layout`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template markup → validator facade | Template strings may originate from project templates or future generated/imported template artifacts and must be parsed deterministically before export evidence trusts them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | T | `validate_v13_template_contract` | mitigate | Use explicit whitespace-tolerant Anki reference detection before layout enforcement so formatting cannot tamper with validation outcome. |
| T-21-04-02 | I | Fixture/evidence artifacts | mitigate | Keep new fixture data synthetic and avoid private APKG excerpts, absolute paths, secrets, or raw reading text. |
| T-21-04-03 | D | Template validator regex | accept | The regex is bounded to small template strings already loaded in memory; focused pytest covers expected reference formats. |
</threat_model>

<verification>
Run focused Phase 21 gap-closure checks:

```bash
python -m json.tool tests/fixtures/v13/card_issues_normalized_cases.json
python -m pytest tests/services/test_v13_validation.py tests/integration/test_v13_normalized_issue_fixtures.py tests/integration/test_v13_final_milestone_evidence.py tests/integration/test_v13_existing_modes_regression_evidence.py -q
```
</verification>

<success_criteria>
- `{{ sentence_audio }}` with missing responsive selectors produces `sentence_audio_layout`.
- Corrected sentence_audio templates with all required selectors still pass.
- Dangling template field behavior is unchanged.
- VAL-02 fixture catalog remains scanner-readable and synthetic.
- Phase 21 verification gap truth #9 is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/21-validation-fixtures-and-milestone-evidence/21-04-SUMMARY.md`.
</output>
