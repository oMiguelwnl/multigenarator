---
phase: 21-validation-fixtures-and-milestone-evidence
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - tests/fixtures/v13/card_issues_normalized_cases.json
  - tests/integration/test_v13_normalized_issue_fixtures.py
autonomous: true
requirements: [VAL-02]
must_haves:
  truths:
    - "User gets regression fixtures covering every normalized issue example from `card_issues_normalized.md`."
    - "User can rerun one fixture suite and see each known bad example fail with the expected normalized validator issue."
  artifacts:
    - path: "tests/fixtures/v13/card_issues_normalized_cases.json"
      provides: "Scanner-readable fixture catalog for v1.3 normalized issue examples"
    - path: "tests/integration/test_v13_normalized_issue_fixtures.py"
      provides: "Integration coverage connecting fixture rows to the VAL-01 validator facade"
  key_links:
    - from: "tests/integration/test_v13_normalized_issue_fixtures.py"
      to: "src/multilang/services/v13_validation.py"
      via: "validate_v13_card and validate_v13_template_contract"
      pattern: "validate_v13_card|validate_v13_template_contract"
    - from: "tests/fixtures/v13/card_issues_normalized_cases.json"
      to: "card_issues_normalized.md"
      via: "case ids matching each documented issue section"
      pattern: "ipa_word_repetition|definition_banned_pattern|translation_example_mismatch|word_audio_word_mismatch|dangling_template_field"
---

<objective>
Turn the normalized defect catalog into executable regression fixtures.

Purpose: VAL-02 requires fixture coverage for the examples and rules in `card_issues_normalized.md`, so regressions are caught by a repeatable suite instead of manual rereading.
Output: A JSON fixture catalog plus an integration test that runs every fixture through the VAL-01 validators.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@.planning/phases/21-validation-fixtures-and-milestone-evidence/21-01-SUMMARY.md
@src/multilang/domain/exporting.py
@src/multilang/services/v13_validation.py
@tests/integration/test_v13_normal_template_export_contract.py

<interfaces>
Use the Phase 21 Plan 01 contracts:
```python
def validate_v13_card(row: ExportCardRow, *, word_audio_asset: AudioAssetRecord | None = None) -> list[V13ValidationIssue]: ...
def validate_v13_template_contract(template: CardTemplate, field_names: tuple[str, ...]) -> list[V13ValidationIssue]: ...
class V13ValidationIssueType(str, Enum): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode normalized bad and corrected examples for VAL-02</name>
  <files>tests/fixtures/v13/card_issues_normalized_cases.json</files>
  <action>Create `tests/fixtures/v13/card_issues_normalized_cases.json` as UTF-8 JSON with a top-level object containing `source_document: "card_issues_normalized.md"`, `cases`, and `coverage`. Include one bad case and one corrected/pass case for each source catalog group: `ipa_word_repetition` using `громко` and `[ˈɡromkə] (гро́мко)` vs `[ˈɡromkə]`; `definition_banned_pattern` using `дальнего`, `местности`, `поги́бли`, and `заболевания`; `definition_wrong_sense` using `дости́чь` with the wrong and corrected meanings; `translation_example_mismatch` using a multi-token Russian example whose bad Translation is only the study word/gloss and whose corrected Translation is full-sentence English; `normal_front_of_card_dangling_template` using `{{Front of Card}}` as the bad template reference and `{{word}}` as corrected; `sentence_audio_layout` asserting the corrected normal template selectors `exampleSentenceLine`, `exampleSentenceText`, and `sentenceAudioButton`; and `word_audio_word_mismatch` using a bad asset whose manifest says a different word and a corrected asset whose metadata exactly matches `Word`. Each case must include `id`, `source_lines`, `field`, `input`, `expected_issue_types`, and `expected_pass`.</action>
  <verify>
    <automated>python -m json.tool tests/fixtures/v13/card_issues_normalized_cases.json</automated>
  </verify>
  <done>The fixture file covers every numbered issue group and correction rule from `card_issues_normalized.md` with explicit expected issue types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run normalized fixtures through validators per VAL-02</name>
  <files>tests/integration/test_v13_normalized_issue_fixtures.py</files>
  <behavior>
    - Every fixture with `expected_pass=false` emits exactly the expected v1.3 issue type or issue type set.
    - Every corrected fixture with `expected_pass=true` emits no v1.3 validation issue.
    - The fixture suite fails if any `card_issues_normalized.md` source group is absent from fixture coverage.
  </behavior>
  <action>Create `tests/integration/test_v13_normalized_issue_fixtures.py`. Load the JSON fixture file, construct `ExportCardRow` values with synthetic `ExportCardIdentity` records, construct Phase 20 `AudioAssetRecord` fixtures for audio cases, and construct `CardTemplate` fixtures for template cases. Call `validate_v13_card` and `validate_v13_template_contract` as appropriate. Assert the fixture `coverage` list includes the eight summary action groups from `card_issues_normalized.md` lines 121-130: IPA correction, grammar Definition correction, wrong-sense Definition correction, Definition analysis/validation, Translation correction, `Front of Card` removal, `sentence_audio` layout, and `word_audio` correction.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v13_normalized_issue_fixtures.py tests/services/test_v13_validation.py -q</automated>
  </verify>
  <done>VAL-02 fixtures are executable and prove both bad normalized examples and corrected forms against the shared validators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized catalog -> test fixtures | Human-readable defect examples become executable test data. |
| fixture data -> validator facade | Synthetic issue examples drive validation logic and can mask gaps if coverage metadata is incomplete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | `card_issues_normalized_cases.json` | mitigate | Assert required coverage groups and expected issue types in the integration test. |
| T-21-05 | Repudiation | fixture-to-source traceability | mitigate | Include `source_document` and `source_lines` for every case. |
| T-21-06 | Information Disclosure | fixture content | mitigate | Use only synthetic examples already present in `card_issues_normalized.md`; do not include private APKG excerpts. |
</threat_model>

<verification>
Run `python -m json.tool tests/fixtures/v13/card_issues_normalized_cases.json` and `python -m pytest tests/integration/test_v13_normalized_issue_fixtures.py tests/services/test_v13_validation.py -q`.
</verification>

<success_criteria>
- VAL-02 fixture coverage maps to all normalized issue groups in `card_issues_normalized.md`.
- Bad fixtures fail with stable issue types; corrected fixtures pass.
- Fixture tests use synthetic data and do not depend on private APKG contents.
</success_criteria>

<output>
After completion, create `.planning/phases/21-validation-fixtures-and-milestone-evidence/21-02-SUMMARY.md`.
</output>
