---
phase: 10-local-kindle-normalization-and-candidate-extraction
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/multilang/domain/highlights.py
  - src/multilang/services/highlight_candidate_extraction.py
  - tests/services/test_highlight_candidate_extraction.py
autonomous: true
requirements: [NORM-02, CAND-01, CAND-02]
must_haves:
  truths:
    - "User receives deterministic vocabulary candidates from normalized highlights for every supported language."
    - "User does not get every token as a card because noise, duplicates, and unusable fragments are filtered."
    - "Candidate ordering follows first occurrence in source highlights."
  artifacts:
    - path: "src/multilang/services/highlight_candidate_extraction.py"
      provides: "Candidate extraction and duplicate filtering from normalized highlights"
      exports: ["extract_highlight_candidates"]
    - path: "src/multilang/domain/highlights.py"
      provides: "HighlightCandidate and extraction summary contracts"
      exports: ["HighlightCandidate", "HighlightCandidateExtractionResult"]
    - path: "tests/services/test_highlight_candidate_extraction.py"
      provides: "All-language deterministic extraction coverage"
  key_links:
    - from: "src/multilang/services/highlight_candidate_extraction.py"
      to: "src/multilang/domain/highlights.py"
      via: "accepts NormalizedHighlight list and returns HighlightCandidateExtractionResult"
      pattern: "NormalizedHighlight"
---

<objective>
Turn normalized highlight text into reviewable target-language vocabulary candidates.

Purpose: The user needs a deterministic pre-generation candidate list, not a raw token dump.
Output: Candidate contracts, extraction service, and all-supported-language tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-local-kindle-normalization-and-candidate-extraction/10-01-SUMMARY.md
@src/multilang/domain/jobs.py
@src/multilang/domain/highlights.py
@src/multilang/services/frequency_decks.py

<interfaces>
```python
# Existing supported languages
class SupportedLanguage(str, Enum):
    PT = "pt"; ES = "es"; EN = "en"; FR = "fr"; DE = "de"; IT = "it"; PL = "pl"; TR = "tr"; RO = "ro"; RU = "ru"; NL = "nl"

# Existing frequency-token curation pattern to mirror where appropriate
def iter_curated_frequency_candidates(language: SupportedLanguage, scan_limit: int = 6000) -> Iterator[tuple[int, str]]: ...
```

Contracts this plan must add:
```python
class HighlightCandidate(BaseModel):
    item_key: str
    display_form: str
    lemma_key: str
    first_highlight_id: str
    first_source_index: int
    occurrence_count: int

class HighlightCandidateExtractionResult(BaseModel):
    candidates: list[HighlightCandidate]
    duplicate_count: int
    rejected_token_count: int
```
</interfaces>

Do not add interactive review UI here; `FUTURE-03` is deferred. Do not generate cards or call LLM/translation/audio providers in this plan.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add candidate extraction contracts and RED behavior tests</name>
  <files>src/multilang/domain/highlights.py, tests/services/test_highlight_candidate_extraction.py</files>
  <behavior>
    - For each SupportedLanguage, synthetic highlight text yields at least one candidate without raising.
    - Duplicate words across highlights collapse to one candidate with first-seen highlight provenance and occurrence count.
    - URLs, digits, punctuation-only chunks, one-character tokens, and configured stopwords/noise do not become candidates.
  </behavior>
  <action>Extend `domain/highlights.py` with the candidate contracts shown in context. Create RED tests for `extract_highlight_candidates(highlights, language=SupportedLanguage.X)`. Tests must assert first-seen ordering, duplicate counts, rejected token counts, and preservation of accented/Cyrillic display forms.</action>
  <verify>
    <automated>uv run pytest tests/services/test_highlight_candidate_extraction.py -q</automated>
  </verify>
  <done>Candidate contract tests describe deterministic extraction before implementation is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic candidate extraction and filtering</name>
  <files>src/multilang/services/highlight_candidate_extraction.py, tests/services/test_highlight_candidate_extraction.py</files>
  <behavior>
    - Extraction is pure and deterministic: same normalized highlights plus language returns the same ordered candidate list.
    - Tokenization supports Unicode letters plus internal apostrophe/hyphen, and rejects web/noise tokens similarly to `frequency_decks`.
    - Language-specific stopword/noise tables cover all 11 supported languages enough to avoid turning articles/conjunctions into candidates in fixtures.
  </behavior>
  <action>Implement `extract_highlight_candidates(highlights: Sequence[NormalizedHighlight], *, language: SupportedLanguage) -> HighlightCandidateExtractionResult`. Use stdlib `re`/`unicodedata` and small in-module stopword sets for pt/es/en/fr/de/it/pl/tr/ro/ru/nl. Normalize `lemma_key` with casefolded whitespace normalization, preserve first display form, increment occurrences for duplicates, and report rejected token count and duplicate count. Keep this service provider-free and DB-free.</action>
  <verify>
    <automated>uv run pytest tests/services/test_highlight_candidate_extraction.py -q</automated>
  </verify>
  <done>All supported languages produce deterministic filtered candidates with duplicate-first-seen behavior and no provider/network dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized highlights → extractor | Private reading text is transformed into candidate metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | I | highlight_candidate_extraction | mitigate | Candidate result stores candidate forms and highlight IDs only, not full raw highlight text. |
| T-10-05 | T | token filtering | mitigate | Reject URLs/digits/control-like tokens and normalize duplicate keys deterministically. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_highlight_candidate_extraction.py -q`.
</verification>

<success_criteria>
NORM-02 and CAND-01/02 are satisfied for normalized records across all supported languages.
</success_criteria>

<output>
After completion, create `.planning/phases/10-local-kindle-normalization-and-candidate-extraction/10-02-SUMMARY.md`.
</output>
