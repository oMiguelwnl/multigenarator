---
phase: 10-local-kindle-normalization-and-candidate-extraction
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/multilang/cli.py
  - src/multilang/services/highlight_import_preview.py
  - tests/cli/test_kindle_highlight_preview_command.py
  - tests/services/test_highlight_import_preview.py
autonomous: true
requirements: [INGEST-03, NORM-03, CAND-03]
must_haves:
  truths:
    - "User can run a local preview command for a Kindle export without starting expensive generation."
    - "User can review imported, candidate, rejected, duplicate, and planned-card counts."
    - "Preview errors and summaries remain privacy-safe."
  artifacts:
    - path: "src/multilang/services/highlight_import_preview.py"
      provides: "Parser + extraction preview summary service"
      exports: ["build_highlight_import_preview"]
    - path: "src/multilang/cli.py"
      provides: "Operator-visible preview command"
      contains: "preview-kindle-highlights"
    - path: "tests/cli/test_kindle_highlight_preview_command.py"
      provides: "CLI preview output and privacy tests"
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/highlight_import_preview.py"
      via: "preview-kindle-highlights command calls build_highlight_import_preview"
      pattern: "build_highlight_import_preview"
---

<objective>
Expose a safe local Kindle import preview before generation integration exists.

Purpose: The user needs reviewable counts before expensive generation while Phase 11 remains responsible for full highlight deck mode integration.
Output: Preview service, non-generation CLI command, and service/CLI tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-source-profiles-privacy-regression-boundary/09-04-SUMMARY.md
@.planning/phases/10-local-kindle-normalization-and-candidate-extraction/10-01-SUMMARY.md
@.planning/phases/10-local-kindle-normalization-and-candidate-extraction/10-02-SUMMARY.md
@src/multilang/cli.py
@src/multilang/security/redaction.py

<interfaces>
```python
# Add a pure preview function rather than integrating with GenerateJobService in Phase 10.
def build_highlight_import_preview(path: str | Path, *, language: SupportedLanguage, planned_card_limit: int | None = None) -> HighlightImportPreview: ...

class HighlightImportPreview(BaseModel):
    imported_highlights: int
    extracted_candidates: int
    rejected_highlights: int
    duplicate_candidates: int
    planned_cards: int
```
</interfaces>

Respect Phase 09 decision: `kindle-highlights` stays blocked from the existing `multilang generate --source` flow until Phase 11. This plan may add a preview-only command that never creates jobs, text, audio, or exports.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build privacy-safe import preview service</name>
  <files>src/multilang/services/highlight_import_preview.py, tests/services/test_highlight_import_preview.py</files>
  <behavior>
    - Preview combines parser and candidate extraction counts exactly: imported highlights, extracted candidates, rejected highlights, duplicate candidates, planned cards.
    - `planned_card_limit` caps planned cards when provided; otherwise planned cards equals extracted candidates.
    - Exceptions and rejected details do not include raw highlight text or private paths.
  </behavior>
  <action>Create `HighlightImportPreview` in `domain/highlights.py` if not already placed there, then implement `build_highlight_import_preview`. Use parser/extractor outputs from Plans 10-01 and 10-02. Redact or omit source path/highlight content in raised errors following Phase 09 T-09-02.</action>
  <verify>
    <automated>uv run pytest tests/services/test_highlight_import_preview.py -q</automated>
  </verify>
  <done>Service returns deterministic review counts and privacy-safe failures without touching job/runtime generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add preview-only Kindle CLI command</name>
  <files>src/multilang/cli.py, tests/cli/test_kindle_highlight_preview_command.py</files>
  <behavior>
    - `multilang preview-kindle-highlights --language es --input-file <fixture>` prints count lines for imported_highlights, extracted_candidates, rejected_highlights, duplicate_candidates, planned_cards.
    - CLI preview supports `--planned-card-limit` and rejects invalid language via existing Typer enum handling.
    - `multilang generate --source kindle-highlights` remains unavailable/blocked until Phase 11.
  </behavior>
  <action>Add a Typer command named `preview-kindle-highlights` to `create_app()`. It must call `build_highlight_import_preview`, print one stable `key=value` line per count, and not call `GenerationRequest`, `GenerateJobService`, text generation, audio, or export code. Preserve existing generate validation that keeps `kindle-highlights` out of user-facing generation.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_kindle_highlight_preview_command.py tests/services/test_highlight_import_preview.py -q</automated>
  </verify>
  <done>User can review local Kindle import counts from CLI while full highlight generation remains gated for Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → preview service | User-provided local file path and private highlight export enter command handling. |
| preview service → terminal | Summary output could leak private text if not count-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | I | preview-kindle-highlights CLI | mitigate | Print count-only summary lines and privacy-safe errors; no raw highlight text or metadata. |
| T-10-07 | E | preview-kindle-highlights CLI | mitigate | Command is preview-only and must not invoke generation/export side effects. |
</threat_model>

<verification>
Run `uv run pytest tests/cli/test_kindle_highlight_preview_command.py tests/services/test_highlight_import_preview.py -q`.
</verification>

<success_criteria>
INGEST-03, NORM-03, and CAND-03 are visible to the user through a safe local preview command.
</success_criteria>

<output>
After completion, create `.planning/phases/10-local-kindle-normalization-and-candidate-extraction/10-03-SUMMARY.md`.
</output>
