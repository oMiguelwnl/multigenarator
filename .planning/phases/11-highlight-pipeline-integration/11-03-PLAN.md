---
phase: 11-highlight-pipeline-integration
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - src/multilang/services/lexical_grounding.py
  - src/multilang/services/ingest_lexical_items.py
  - src/multilang/runtime.py
  - tests/services/test_highlight_ingest_lexical_items.py
autonomous: true
requirements: [MODE-01, INGEST-04]
must_haves:
  truths:
    - "Highlight candidates enter the existing job/resume pipeline."
    - "Ungrounded highlight candidates are blocked and counted, not backfilled."
    - "Grounded highlight candidates are persisted with safe provenance for downstream generation."
  artifacts:
    - path: "src/multilang/services/ingest_lexical_items.py"
      provides: "highlight-mode ingestion branch"
      contains: "_ingest_highlights"
    - path: "src/multilang/services/lexical_grounding.py"
      provides: "highlight candidate grounding without frequency backfill"
      contains: "ground_highlight_candidate"
    - path: "src/multilang/runtime.py"
      provides: "runtime repository composition for highlight imports"
      contains: "HighlightImportRepository"
  key_links:
    - from: "IngestLexicalItemsService._ingest_highlights"
      to: "HighlightImportRepository"
      via: "private record and manifest persistence"
      pattern: "upsert_import_manifest"
    - from: "IngestLexicalItemsService._ingest_highlights"
      to: "GenerateJobService.orchestrate"
      via: "stable highlight candidate item keys"
      pattern: "requested_item_keys=\[candidate.item_key"
---

<objective>
Wire highlight candidates into the existing lexical ingestion, job resume, duplicate-prevention, and persistence flow.

Purpose: Users need `highlights` to behave like a real deck mode inside the existing pipeline while preserving Phase 11's grounding-only boundary.
Output: Highlight ingestion service branch, runtime repository wiring, and service tests for duplicate/reuse/blocking counts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md
@.planning/phases/11-highlight-pipeline-integration/11-RESEARCH.md
@.planning/phases/10-local-kindle-normalization-and-candidate-extraction/10-03-SUMMARY.md
@src/multilang/services/ingest_lexical_items.py
@src/multilang/services/lexical_grounding.py
@src/multilang/services/kindle_highlight_parser.py
@src/multilang/services/highlight_candidate_extraction.py
@src/multilang/repositories/highlight_import_repository.py

<interfaces>
From src/multilang/services/ingest_lexical_items.py:
```python
@dataclass(slots=True)
class IngestLexicalItemsResult:
    report: JobExecutionReport
    grounded_candidates: int
    pending_groundings: int
    rejected_rows: int
    backfilled_candidates: int
    level_counts: dict[int, int]

class IngestLexicalItemsService:
    def execute(self, request: GenerationRequest) -> IngestLexicalItemsResult: ...
```

From src/multilang/services/lexical_grounding.py:
```python
class LexicalGroundingService:
    def ground_word_list_item(...)
    def ground_frequency_candidate(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add highlight grounding and ingestion result counters</name>
  <files>src/multilang/services/lexical_grounding.py, src/multilang/services/ingest_lexical_items.py, tests/services/test_highlight_ingest_lexical_items.py</files>
  <read_first>src/multilang/services/lexical_grounding.py; src/multilang/services/ingest_lexical_items.py; src/multilang/domain/lexicon.py; src/multilang/domain/highlights.py; .planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md</read_first>
  <behavior>
    - A grounded highlight candidate becomes a `LexicalCardCandidate` with `source_type="kindle-highlights"` persistence path.
    - An ungrounded highlight candidate returns `GroundingStatus.INSUFFICIENT` or `PENDING` and is counted as blocked, not backfilled.
    - `IngestLexicalItemsResult` exposes counts for imported, rejected, extracted, duplicate, reused, newly planned, blocked, and planned cards.
  </behavior>
  <action>Per D-12, D-13, D-14, and D-16, add fields to `IngestLexicalItemsResult`: `imported_highlights: int = 0`, `rejected_highlights: int = 0`, `extracted_candidates: int = 0`, `duplicate_candidates: int = 0`, `reused_existing_items: int = 0`, `newly_planned_candidates: int = 0`, `blocked_candidates: int = 0`, and `planned_cards: int = 0`. Add `LexicalGroundingService.ground_highlight_candidate(language: SupportedLanguage, candidate: HighlightCandidate) -> LexicalCardCandidate`. It should look up `candidate.lemma_key`, return grounded candidate using `_grounded_candidate()` when found, and return an insufficient candidate with `warning_code="highlight_grounding_missing"`, `warning_detail="no authoritative lexical match for highlight candidate"`, `provenance.source="kindle_highlight"`, and no raw highlight text when missing. Do not call `_backfill_required_candidate()` for highlights.</action>
  <verify>
    <automated>python -m pytest tests/services/test_highlight_ingest_lexical_items.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/multilang/services/lexical_grounding.py` contains `def ground_highlight_candidate(`.
    - `src/multilang/services/lexical_grounding.py` contains `highlight_grounding_missing`.
    - `src/multilang/services/ingest_lexical_items.py` contains `blocked_candidates` and `planned_cards` fields.
    - Tests assert missing highlight grounding does not produce `GroundingStatus.BACKFILL_REQUIRED`.
  </acceptance_criteria>
  <done>Highlight grounding has an explicit no-backfill contract and result counters can represent the complete import lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement highlight ingestion through existing job pipeline</name>
  <files>src/multilang/services/ingest_lexical_items.py, src/multilang/runtime.py, tests/services/test_highlight_ingest_lexical_items.py</files>
  <read_first>src/multilang/services/ingest_lexical_items.py; src/multilang/services/kindle_highlight_parser.py; src/multilang/services/highlight_candidate_extraction.py; src/multilang/repositories/highlight_import_repository.py; src/multilang/runtime.py; tests/services/test_generate_job.py</read_first>
  <behavior>
    - `request.source_type == "kindle-highlights"` requires `request.input_file` and uses parser -> extractor -> job orchestration.
    - Rerun of the same highlight import skips already completed grounded item keys and reports reused counts.
    - Private normalized highlights are persisted through `HighlightImportRepository`; lexical candidates store safe hash/provenance only.
  </behavior>
  <action>Add optional `highlight_import_repo: HighlightImportRepository | None = None` to `IngestLexicalItemsService.__init__`; require it only for highlight ingestion. Add `if request.source_type == "kindle-highlights": return self._ingest_highlights(request)` before unsupported-source failure. Implement `_ingest_highlights()` to: require `request.input_file`; parse with `parse_kindle_highlight_export`; extract with `extract_highlight_candidates(language=request.language)`; build `import_content_hash = sha256("\n".join(sorted(h.provenance.content_hash for h in parsed.highlights)).encode("utf-8")).hexdigest()`; orchestrate with `requested_item_keys=[candidate.item_key for candidate in extraction.candidates]`; persist private records and `HighlightImportManifest(import_content_hash=..., candidate_keys=..., counts={...})`; ground only `orchestration.pending_item_keys`; upsert grounded lexical candidates with `source_type="kindle-highlights"`, `normalized_source=candidate.source_content_hash`, and provenance notes containing source hash/first index/occurrence count but no raw highlight text; call `record_item_success` only for grounded candidates; set `blocked_candidates` for missing/insufficient groundings; set `planned_cards` equal to grounded persisted candidates; advance job to `JobStage.GENERATE_TEXT` only when at least one grounded candidate is planned/reused. Do not generate text, audio, export, or WebDAV behavior in this phase. In `runtime.py`, instantiate `HighlightImportRepository(session)` and pass it to `RuntimeGenerateService`.</action>
  <verify>
    <automated>python -m pytest tests/services/test_highlight_ingest_lexical_items.py tests/repositories/test_highlight_import_repository.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/multilang/services/ingest_lexical_items.py` contains `_ingest_highlights`.
    - `src/multilang/services/ingest_lexical_items.py` contains `parse_kindle_highlight_export` and `extract_highlight_candidates`.
    - `src/multilang/services/ingest_lexical_items.py` contains `source_type="kindle-highlights"`.
    - Tests assert rerun reports reused/skipped highlight item keys without duplicate lexical rows.
    - Tests assert a sample private highlight sentence is present in private records and absent from lexical candidate provenance.
  </acceptance_criteria>
  <done>Highlight imports use the existing orchestration/resume/repository path with duplicate-safe counts and private provenance handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local parser/extractor -> runtime service | Private highlight records and candidate hashes enter service orchestration. |
| grounding service -> lexical repository | Candidate provenance becomes downstream-visible persisted data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | I | lexical candidate provenance | mitigate | Store source content hashes, indices, and occurrence counts only; tests assert raw highlight text is absent from candidate provenance. |
| T-11-06 | D | highlight ingestion with no usable candidates | mitigate | Fail/stop only when no usable candidates exist; otherwise continue with grounded candidates and count blocked candidates per D-11/D-13. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_highlight_ingest_lexical_items.py tests/repositories/test_highlight_import_repository.py -q`.
</verification>

<success_criteria>
- Highlight mode reaches existing job orchestration and lexical candidate persistence.
- Duplicate reruns reuse completed items instead of creating duplicate cards.
- Ungrounded candidates are blocked/reported without frequency backfill.
</success_criteria>

<output>
After completion, create `.planning/phases/11-highlight-pipeline-integration/11-03-SUMMARY.md`.
</output>
