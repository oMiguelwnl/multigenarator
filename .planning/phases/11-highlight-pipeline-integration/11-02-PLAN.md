---
phase: 11-highlight-pipeline-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/db/models.py
  - src/multilang/repositories/highlight_import_repository.py
  - alembic/versions/20260505_11_highlight_import_tables.py
  - tests/repositories/test_highlight_import_repository.py
autonomous: true
requirements: [INGEST-04]
must_haves:
  truths:
    - "Private normalized highlight text is stored internally for later audit/context."
    - "Safe import manifests contain hashes, candidate keys, and counts only."
  artifacts:
    - path: "src/multilang/db/models.py"
      provides: "highlight import and safe manifest ORM tables"
      contains: "HighlightImportModel"
    - path: "src/multilang/repositories/highlight_import_repository.py"
      provides: "repository boundary for private highlights and safe manifests"
      contains: "upsert_import_manifest"
    - path: "alembic/versions/20260505_11_highlight_import_tables.py"
      provides: "database migration for highlight import tables"
      contains: "highlight_import_manifests"
  key_links:
    - from: "HighlightImportRepository"
      to: "GenerationJob"
      via: "job_id foreign key"
      pattern: "ForeignKey\(\"generation_jobs.id"
---

<objective>
Add the persistence boundary that separates private normalized highlight text from count/hash-only import manifests.

Purpose: Phase 12 needs private highlight text for context/audit, while Phase 11 summaries/manifests must remain safe to print, inspect, and commit.
Output: ORM models, migration, repository, and tests for private highlight import storage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md
@.planning/phases/11-highlight-pipeline-integration/11-RESEARCH.md
@src/multilang/db/models.py
@src/multilang/repositories/job_repository.py
@src/multilang/repositories/lexical_repository.py
@alembic/versions/20260419_02_lexical_grounding_tables.py

<interfaces>
From src/multilang/db/models.py:
```python
class GenerationJob(Base):
    __tablename__ = "generation_jobs"
    id: Mapped[str]
    run_key: Mapped[str]
    source_type: Mapped[str]

class LexicalCandidate(Base):
    __tablename__ = "lexical_candidates"
    __table_args__ = (UniqueConstraint("job_id", "item_key", ...),)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add private highlight import ORM and migration</name>
  <files>src/multilang/db/models.py, alembic/versions/20260505_11_highlight_import_tables.py, tests/repositories/test_highlight_import_repository.py</files>
  <read_first>src/multilang/db/models.py; alembic/versions/20260419_02_lexical_grounding_tables.py; tests/repositories/test_job_repository.py; .planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md</read_first>
  <behavior>
    - Database metadata includes a `highlight_import_records` table for private normalized text.
    - Database metadata includes a `highlight_import_manifests` table for safe hash/count manifests.
    - Safe manifest rows do not require raw highlight text, source paths, credentials, or book metadata.
  </behavior>
  <action>Per D-08, D-15, and D-16, add `HighlightImportRecordModel` with table name `highlight_import_records`, columns `id`, `job_id` FK to `generation_jobs.id`, `import_content_hash` (64-char string/indexed), `highlight_id`, `source_content_hash` (64-char string/indexed), `source_index` integer, `normalized_text` Text, `created_at`, `updated_at`, and unique constraint `(job_id, highlight_id)`. Add `HighlightImportManifestModel` with table name `highlight_import_manifests`, columns `id`, `job_id` FK unique/indexed, `import_content_hash` indexed, `candidate_keys` JSON default list, `counts` JSON default dict, `created_at`, `updated_at`. Add relationships from `GenerationJob` named `highlight_import_records` and `highlight_import_manifest`. Create Alembic revision `20260505_11_highlight_import_tables.py` with matching `upgrade()` and `downgrade()` definitions. Do not add raw source path/book/author/location fields to manifest table.</action>
  <verify>
    <automated>python -m pytest tests/repositories/test_highlight_import_repository.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/multilang/db/models.py` contains `class HighlightImportRecordModel`.
    - `src/multilang/db/models.py` contains `class HighlightImportManifestModel`.
    - Migration file contains `op.create_table("highlight_import_records"` and `op.create_table("highlight_import_manifests"`.
    - No `book_title`, `author`, `source_path`, or `raw_location` column appears in `HighlightImportManifestModel`.
  </acceptance_criteria>
  <done>Schema can store private highlight text separately from safe import manifests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement highlight import repository</name>
  <files>src/multilang/repositories/highlight_import_repository.py, tests/repositories/test_highlight_import_repository.py</files>
  <read_first>src/multilang/repositories/lexical_repository.py; src/multilang/repositories/job_repository.py; src/multilang/domain/highlights.py; tests/repositories/test_lexical_repository.py</read_first>
  <behavior>
    - `upsert_import_records()` persists normalized text by job/highlight id and is idempotent on rerun.
    - `upsert_import_manifest()` stores only import hash, candidate keys, and counts.
    - `get_manifest(job_id)` returns a manifest without private text.
  </behavior>
  <action>Create `HighlightImportRepository` with `__init__(self, session: Session)`. Implement `upsert_import_records(job_id: str, import_content_hash: str, highlights: Iterable[NormalizedHighlight]) -> int` that inserts or updates rows keyed by `(job_id, highlight.highlight_id)` and stores `normalized_text=highlight.text`, `source_content_hash=highlight.provenance.content_hash`, and `source_index=highlight.provenance.source_index`; return number of records processed. Implement `upsert_import_manifest(job_id: str, manifest: HighlightImportManifest) -> HighlightImportManifest` that upserts one row per job and stores `candidate_keys` and `counts` JSON. Implement `get_manifest(job_id: str) -> HighlightImportManifest | None` and `list_private_records(job_id: str) -> list[HighlightImportRecordModel]` for tests/internal use. Commit session changes consistently with existing repositories.</action>
  <verify>
    <automated>python -m pytest tests/repositories/test_highlight_import_repository.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/multilang/repositories/highlight_import_repository.py` contains `class HighlightImportRepository`.
    - `src/multilang/repositories/highlight_import_repository.py` contains `def upsert_import_records(`.
    - `src/multilang/repositories/highlight_import_repository.py` contains `def upsert_import_manifest(`.
    - Tests assert private records include a sample normalized highlight string.
    - Tests assert manifest model dump does not include that sample normalized highlight string.
  </acceptance_criteria>
  <done>Services can persist private highlight records while exposing only safe manifest data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parser output -> database | Private normalized highlight text enters persistent storage. |
| database -> manifest/report output | Safe metadata may be printed or committed as evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | I | highlight_import_manifests | mitigate | Manifest schema contains only `import_content_hash`, `candidate_keys`, and integer `counts`; tests assert private snippets and path-like values are absent. |
| T-11-04 | R | private highlight records | mitigate | Store `job_id`, `highlight_id`, `source_content_hash`, and `source_index` so later audit can trace records without printing raw text. |
</threat_model>

<verification>
Run `python -m pytest tests/repositories/test_highlight_import_repository.py -q`.
</verification>

<success_criteria>
- Private normalized highlight text persists only in the private import records table.
- Safe manifests are idempotent and omit raw text/path/book metadata.
- Alembic migration matches ORM table names and constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/11-highlight-pipeline-integration/11-02-SUMMARY.md`.
</output>
