---
phase: 11-highlight-pipeline-integration
plan: 04
type: execute
wave: 3
depends_on: [11-03]
files_modified:
  - src/multilang/cli.py
  - tests/cli/test_generate_command.py
  - tests/cli/test_kindle_highlight_preview_command.py
  - tests/integration/test_v12_existing_mode_regression_boundary.py
  - tests/integration/test_kindle_local_normalization_flow.py
autonomous: true
requirements: [MODE-01, INGEST-04]
must_haves:
  truths:
    - "User can choose `generate --source highlights` with `--input-file`."
    - "Preview remains side-effect-free after generation mode is enabled."
    - "CLI summary visibly distinguishes imported, rejected, duplicate, reused, newly planned, blocked, and planned counts."
  artifacts:
    - path: "src/multilang/cli.py"
      provides: "public highlights CLI alias and count-only output"
      contains: "--source must be one of: frequency, word-list, highlights"
    - path: "tests/integration/test_v12_existing_mode_regression_boundary.py"
      provides: "updated v1.2 regression boundary after public highlights mode is wired"
      contains: "highlights"
  key_links:
    - from: "src/multilang/cli.py"
      to: "GenerationRequest.source_type"
      via: "public highlights alias maps to internal kindle-highlights"
      pattern: "source_type=\"kindle-highlights\""
---

<objective>
Expose highlight generation as the public `highlights` deck mode and print the Phase 11 duplicate-safe import summary.

Purpose: This is the user-facing completion point for MODE-01 and the visible import-summary requirement in INGEST-04.
Output: CLI validation/mapping, count-only summary output, and focused regression updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md
@.planning/phases/11-highlight-pipeline-integration/11-RESEARCH.md
@.planning/phases/09-source-profiles-privacy-regression-boundary/09-05-SUMMARY.md
@.planning/phases/10-local-kindle-normalization-and-candidate-extraction/10-04-SUMMARY.md
@src/multilang/cli.py
@tests/cli/test_generate_command.py
@tests/integration/test_v12_existing_mode_regression_boundary.py

<interfaces>
From src/multilang/cli.py:
```python
def _validate_request(request: GenerationRequest, *, test_mode: bool = False) -> None: ...
def create_app(...):
    @cli.command("preview-kindle-highlights")
    @cli.command("generate")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map public highlights source to internal profile</name>
  <files>src/multilang/cli.py, tests/cli/test_generate_command.py, tests/integration/test_v12_existing_mode_regression_boundary.py</files>
  <read_first>src/multilang/cli.py; src/multilang/domain/source_profiles.py; tests/cli/test_generate_command.py; tests/integration/test_v12_existing_mode_regression_boundary.py; .planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md</read_first>
  <behavior>
    - `generate --source highlights --input-file <path>` creates a `GenerationRequest` whose internal `source_type` is `kindle-highlights`.
    - `generate --source kindle-highlights` remains rejected at the public CLI.
    - Frequency-only flags remain rejected for highlights.
  </behavior>
  <action>Per D-01, D-02, D-03, and D-04, update `generate` source validation to accept public sources `frequency`, `word-list`, and `highlights` only. Add helper logic near CLI request creation: `internal_source = "kindle-highlights" if source == "highlights" else source`. Pass `source_type=internal_source` into `GenerationRequest`. Update `_validate_request()` so `--input-file` is required for `word-list` and `kindle-highlights`, allowed for those two internal source types, and rejected for `frequency`; `--level`, `--test-mode`, and `--cards-per-level` remain frequency-only. Keep `preview-kindle-highlights` unchanged and side-effect-free. Do not expose public `kindle-highlights` as an accepted source string.</action>
  <verify>
    <automated>python -m pytest tests/cli/test_generate_command.py tests/integration/test_v12_existing_mode_regression_boundary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/multilang/cli.py` contains `"highlights"` in the accepted public source set.
    - `src/multilang/cli.py` contains `source_type="kindle-highlights"` or equivalent alias assignment.
    - Tests assert public `--source highlights` passes a request with internal `kindle-highlights`.
    - Tests assert public `--source kindle-highlights` exits non-zero.
    - Tests assert `--input-file` is required for highlights.
  </acceptance_criteria>
  <done>User can select `highlights` while internal source-profile isolation remains `kindle-highlights`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Print count-only highlight lifecycle summary and update regressions</name>
  <files>src/multilang/cli.py, tests/cli/test_generate_command.py, tests/cli/test_kindle_highlight_preview_command.py, tests/integration/test_v12_existing_mode_regression_boundary.py, tests/integration/test_kindle_local_normalization_flow.py</files>
  <read_first>src/multilang/cli.py; src/multilang/services/ingest_lexical_items.py; tests/cli/test_kindle_highlight_preview_command.py; tests/integration/test_kindle_local_normalization_flow.py; .planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md</read_first>
  <behavior>
    - `generate --source highlights` output includes all D-12 lifecycle counters as stable `key=value` lines.
    - Default summary does not print candidate word lists, highlight snippets, source paths, or book metadata.
    - Phase 10 preview tests still prove `preview-kindle-highlights` is count-only and side-effect-free.
  </behavior>
  <action>Per D-09, D-10, D-11, and D-12, after `lexical_result = resolved_service.execute(request)` in the `IngestLexicalItemsService` CLI path, print these extra lines only when `request.source_type == "kindle-highlights"`: `imported_highlights={lexical_result.imported_highlights}`, `rejected_highlights={lexical_result.rejected_highlights}`, `extracted_candidates={lexical_result.extracted_candidates}`, `duplicate_candidates={lexical_result.duplicate_candidates}`, `reused_existing_candidates={lexical_result.reused_existing_items}`, `newly_planned_candidates={lexical_result.newly_planned_candidates}`, `blocked_candidates={lexical_result.blocked_candidates}`, and `planned_cards={lexical_result.planned_cards}`. Keep existing grounded/pending/rejected/lifecycle counters. Update regression tests so Phase 11 proves public highlights mode is enabled while existing frequency/custom behavior remains unchanged. Keep assertions that preview output has no generation side effects and no private snippets.</action>
  <verify>
    <automated>python -m pytest tests/cli/test_generate_command.py tests/cli/test_kindle_highlight_preview_command.py tests/integration/test_v12_existing_mode_regression_boundary.py tests/integration/test_kindle_local_normalization_flow.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/multilang/cli.py` contains `imported_highlights=`.
    - `src/multilang/cli.py` contains `reused_existing_candidates=`.
    - `src/multilang/cli.py` contains `blocked_candidates=`.
    - Tests assert output contains every D-12 counter name.
    - Tests assert output does not contain a sample private highlight sentence, a source path, or book metadata string.
    - `preview-kindle-highlights` tests still assert count-only output and no generation executor call.
  </acceptance_criteria>
  <done>Highlight generation CLI exposes a complete count-only import lifecycle summary without leaking private reading data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI args -> GenerationRequest | Untrusted source/input flags are mapped to internal source profiles. |
| service result -> terminal output | Counts become user-visible output while private highlight data must stay hidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | S | CLI source alias | mitigate | Public accepted set is `frequency`, `word-list`, `highlights`; internal `kindle-highlights` remains a profile key, not public user input. |
| T-11-08 | I | CLI summary output | mitigate | Print only explicit numeric counters; tests assert private snippet/path/book metadata are absent. |
</threat_model>

<verification>
Run `python -m pytest tests/cli/test_generate_command.py tests/cli/test_kindle_highlight_preview_command.py tests/integration/test_v12_existing_mode_regression_boundary.py tests/integration/test_kindle_local_normalization_flow.py -q`.
</verification>

<success_criteria>
- Public `generate --source highlights --input-file <path>` is accepted and maps to internal `kindle-highlights`.
- Public `generate --source kindle-highlights` remains rejected.
- Highlight generation output includes count-only D-12 lifecycle counters and omits private data.
- Existing frequency/custom regression tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-highlight-pipeline-integration/11-04-SUMMARY.md`.
</output>
