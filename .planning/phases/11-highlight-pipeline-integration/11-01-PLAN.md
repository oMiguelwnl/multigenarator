---
phase: 11-highlight-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/domain/highlights.py
  - src/multilang/services/highlight_candidate_extraction.py
  - src/multilang/services/input_fingerprint.py
  - tests/services/test_highlight_candidate_extraction.py
  - tests/services/test_generate_job.py
autonomous: true
requirements: [INGEST-04]
must_haves:
  truths:
    - "Rerunning the same normalized highlight content produces the same import identity even when file names or order change."
    - "The same word in genuinely different source content can remain a distinct highlight candidate."
  artifacts:
    - path: "src/multilang/domain/highlights.py"
      provides: "stable highlight import and candidate identity contracts"
      contains: "source_content_hash"
    - path: "src/multilang/services/highlight_candidate_extraction.py"
      provides: "reorder-safe candidate item keys"
      contains: "highlight-{language.value}-{source_hash}-{lemma_hash}"
    - path: "src/multilang/services/input_fingerprint.py"
      provides: "highlight-aware run fingerprint support"
      contains: "kindle-highlights"
  key_links:
    - from: "highlight_candidate_extraction.extract_highlight_candidates"
      to: "GenerateJobService.orchestrate"
      via: "stable candidate item_key list"
      pattern: "source_content_hash"
---

<objective>
Create the stable identity layer that makes highlight imports duplicate-safe before any DB or CLI wiring depends on it.

Purpose: Phase 11 cannot satisfy INGEST-04 unless import and candidate identity are based on normalized content hashes rather than file paths or first-seen sequence numbers.
Output: Updated highlight contracts, reorder-safe candidate keys, and highlight-aware job fingerprints.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md
@.planning/phases/11-highlight-pipeline-integration/11-RESEARCH.md
@src/multilang/domain/highlights.py
@src/multilang/services/highlight_candidate_extraction.py
@src/multilang/services/input_fingerprint.py
@src/multilang/services/generate_job.py

<interfaces>
From src/multilang/domain/highlights.py:
```python
class NormalizedHighlight(BaseModel):
    highlight_id: str
    text: str
    provenance: HighlightProvenance  # includes content_hash

class HighlightCandidate(BaseModel):
    item_key: str
    display_form: str
    lemma_key: str
    first_highlight_id: str
    first_source_index: int
    occurrence_count: int
```

From src/multilang/services/input_fingerprint.py:
```python
def normalize_requested_item_keys(requested_item_keys: Iterable[str]) -> list[str]
def build_input_fingerprint(request: GenerationRequest, *, requested_item_keys: Iterable[str] = ()) -> str
def build_run_key(request: GenerationRequest, *, requested_item_keys: Iterable[str] = ()) -> str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define stable highlight identity contracts</name>
  <files>src/multilang/domain/highlights.py, tests/services/test_highlight_candidate_extraction.py</files>
  <read_first>src/multilang/domain/highlights.py; src/multilang/services/highlight_candidate_extraction.py; tests/services/test_highlight_candidate_extraction.py; .planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md</read_first>
  <behavior>
    - Same normalized highlight content hash produces the same `HighlightCandidate.source_content_hash` regardless of source file name.
    - `HighlightCandidate.item_key` is valid without a first-seen sequence number.
    - Candidate provenance exposes `source_content_hash`, `first_source_index`, `first_highlight_id`, and `occurrence_count` without storing raw highlight text.
  </behavior>
  <action>Per D-05, D-06, D-07, D-08, and D-16, extend `HighlightCandidate` with `source_content_hash: str = Field(min_length=64, max_length=64)` and validate lowercase SHA-256 hex using the same rule as `HighlightProvenance.content_hash`. Add a safe manifest domain model named `HighlightImportManifest` with fields `import_content_hash: str`, `candidate_keys: list[str]`, and `counts: dict[str, int]`; do not include `text`, `source_path`, `raw_location`, `book`, `author`, or any path-bearing field. Update tests to assert that model dumps contain `source_content_hash` and do not contain raw highlight text.</action>
  <verify>
    <automated>python -m pytest tests/services/test_highlight_candidate_extraction.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/multilang/domain/highlights.py` contains `class HighlightImportManifest`.
    - `src/multilang/domain/highlights.py` contains `source_content_hash: str`.
    - `tests/services/test_highlight_candidate_extraction.py` asserts `source_content_hash` equals a 64-character lowercase hash.
    - `tests/services/test_highlight_candidate_extraction.py` asserts manifest serialization omits a sample private highlight sentence.
  </acceptance_criteria>
  <done>Highlight candidate and manifest contracts can represent stable, count/hash-only identity without raw private text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate reorder-safe candidate keys and highlight fingerprints</name>
  <files>src/multilang/services/highlight_candidate_extraction.py, src/multilang/services/input_fingerprint.py, tests/services/test_highlight_candidate_extraction.py, tests/services/test_generate_job.py</files>
  <read_first>src/multilang/services/highlight_candidate_extraction.py; src/multilang/services/input_fingerprint.py; src/multilang/services/generate_job.py; tests/services/test_generate_job.py; .planning/phases/11-highlight-pipeline-integration/11-CONTEXT.md</read_first>
  <behavior>
    - Two inputs containing the same normalized highlight records in different order return the same set of candidate `item_key` values.
    - Same lemma from two different highlight content hashes returns two distinct item keys.
    - `build_run_key()` for `source_type="kindle-highlights"` depends on normalized candidate keys and not on `input_file` path text.
  </behavior>
  <action>Replace sequence-based keys `highlight-{language.value}-{len(ordered_keys):04d}-{lemma_key}` with content-derived keys. For each candidate, compute `lemma_hash = sha256(lemma_key.encode("utf-8")).hexdigest()[:16]` and `source_hash = highlight.provenance.content_hash[:16]`; set `item_key = f"highlight-{language.value}-{source_hash}-{lemma_hash}"` and `source_content_hash = highlight.provenance.content_hash`. Deduplicate within an import by `(source_content_hash, lemma_key)` so the same word in different source content remains distinct per D-07. Keep result ordering deterministic by sorting final candidates by `(first_source_index, lemma_key, item_key)`. In `input_fingerprint.py`, keep existing behavior for `frequency` and `word-list`, but document and test that `kindle-highlights` uses normalized requested item keys exactly like other item-list sources, yielding `items:{sha256}` and run key `{language}:kindle-highlights:items:{hash}`. Do not add file path, source file name, or raw text to fingerprints.</action>
  <verify>
    <automated>python -m pytest tests/services/test_highlight_candidate_extraction.py tests/services/test_generate_job.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/multilang/services/highlight_candidate_extraction.py` contains `sha256`.
    - `src/multilang/services/highlight_candidate_extraction.py` no longer contains `len(ordered_keys):04d`.
    - Tests prove reordered highlights produce equal sorted item keys.
    - Tests prove different `content_hash` values for the same lemma produce different item keys.
    - `tests/services/test_generate_job.py` contains a kindle-highlights run-key assertion that does not include a local path string.
  </acceptance_criteria>
  <done>Highlight candidate keys and run fingerprints are stable across reruns and safe against path/order drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local highlight file -> parser/extractor | Private reading text enters deterministic normalization and candidate extraction. |
| extracted candidate keys -> job identity | Hash/candidate identifiers become persisted orchestration state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | I | highlight identity/fingerprint | mitigate | Derive candidate keys and fingerprints from SHA-256 hashes and normalized item keys only; tests assert no path/raw text in run key or manifest dumps. |
| T-11-02 | T | duplicate-prevention identity | mitigate | Remove sequence-only candidate keys so reordering cannot create false-new planned cards. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_highlight_candidate_extraction.py tests/services/test_generate_job.py -q`.
</verification>

<success_criteria>
- Reordered same-content highlights produce stable candidate keys.
- Same word from different content hashes remains distinct.
- Highlight run key construction contains no raw text or file path data.
</success_criteria>

<output>
After completion, create `.planning/phases/11-highlight-pipeline-integration/11-01-SUMMARY.md`.
</output>
