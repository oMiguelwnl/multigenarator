---
phase: 26-portuguese-translation-quality
plan: 3
type: execute
wave: 3
depends_on: [26-02]
files_modified:
  - src/multilang/services/latin_mvp.py
  - src/multilang/cli.py
  - tests/services/test_latin_mvp.py
  - tests/cli/test_generate_latin_mvp_command.py
  - tests/integration/test_v20_latin_portuguese_translation_evidence.py
autonomous: true
requirements: [PT-01, PT-02, PT-03]
must_haves:
  truths:
    - "User can inspect Portuguese translation QA evidence before Latin cards are treated as learner-ready."
    - "The Latin MVP CLI can expose a scanner-friendly Portuguese translation summary without live provider credentials."
    - "Phase 26 evidence proves PT-01/PT-02/PT-03 coverage and keeps existing mode boundaries intact."
  artifacts:
    - path: "src/multilang/services/latin_mvp.py"
      provides: "Latin MVP start result with optional Portuguese translation QA summary"
    - path: "src/multilang/cli.py"
      provides: "CLI flag for printing Portuguese translation QA summary"
    - path: "tests/integration/test_v20_latin_portuguese_translation_evidence.py"
      provides: "Scanner-readable Phase 26 evidence"
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/latin_translation_quality.py"
      via: "translation QA summary command path"
      pattern: "portuguese|translation"
    - from: "tests/integration/test_v20_latin_portuguese_translation_evidence.py"
      to: "data/latin_mvp/latin-mvp-50-v1-pt.json"
      via: "loaded translation asset validated against source pack"
      pattern: "PHASE_26_REQUIREMENTS"
---

<objective>
Wire Portuguese translation QA into the Latin MVP inspection path and add scanner-readable Phase 26 evidence.

Purpose: Users need to see translation QA before approval/export, and milestone scanners need executable proof that PT-01/PT-02/PT-03 are covered.
Output: Service/CLI summary wiring plus focused evidence tests for Portuguese translation quality and existing-mode isolation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-latin-mode-contracts-and-isolation/22-02-SUMMARY.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-03-SUMMARY.md
@.planning/phases/26-portuguese-translation-quality/26-01-SUMMARY.md
@.planning/phases/26-portuguese-translation-quality/26-02-SUMMARY.md
@src/multilang/services/latin_mvp.py
@src/multilang/cli.py
@src/multilang/services/latin_translation_quality.py

<interfaces>
Current `LatinMvpStartResult` from `src/multilang/services/latin_mvp.py`:
```python
class LatinMvpStartResult(BaseModel):
    metadata: LatinDeckMetadata
    source_type: str = "latin-mvp"
    item_keys: list[str] = Field(default_factory=list)
    manifest_path: str
    first_item_key: str
    last_item_key: str
    license_gate_status: str
    source_type_counts: dict[str, int] = Field(default_factory=dict)
    frequency_source_count: int
    didactic_sequence_summary: str

    def manifest_summary(self) -> dict[str, object]: ...
```

Current CLI pattern from `src/multilang/cli.py`: `generate-latin-mvp` prints stable `key=value` metadata and supports `--manifest-json` for scanner-friendly summary output.
</interfaces>
</context>

<source_coverage_audit>
GOAL Phase 26 Portuguese Translation Quality: COVERED by user/scanner-visible QA summary.
REQ PT-01: COVERED by summary evidence that every entry has a context-matched short translation.
REQ PT-02: COVERED by summary evidence that every entry has a context-matched sentence translation.
REQ PT-03: COVERED by QA evidence for no English leakage, no dictionary-only mismatches, no contradictions, and review status visibility.
RESEARCH/STACK: Follows CLI scanner output and deterministic pytest evidence patterns from Phases 22-23.
CONTEXT decisions: No Phase 26 CONTEXT.md exists; isolated `generate-latin-mvp` command remains separate from modern generation.
Deferred/out-of-scope exclusions: No export, audio, images, 3000-card scale, Greek, or live provider calls are added.
</source_coverage_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Portuguese QA summary to Latin MVP service and CLI inspection</name>
  <files>src/multilang/services/latin_mvp.py, src/multilang/cli.py, tests/services/test_latin_mvp.py, tests/cli/test_generate_latin_mvp_command.py</files>
  <behavior>
    - Test: `LatinMvpGenerationService().start(...).manifest_summary()` can include `portuguese_translation_summary` with `entry_count=50`, `passed_count=50`, and `failed_count=0` when requested.
    - Test: `generate-latin-mvp --portuguese-json` prints valid JSON containing the Portuguese QA summary.
    - Test: existing `generate-latin-mvp --manifest-json` behavior still works and existing `generate --language la` rejection remains unchanged.
  </behavior>
  <action>Extend `LatinMvpGenerationService` with optional translation-pack loading through the Plan 26-01/26-02 service. Keep default startup behavior backward-compatible: no live providers, no mandatory translation loading unless the CLI requests the Portuguese QA summary. Add a `--portuguese-json` option to `generate-latin-mvp` that loads `data/latin_mvp/latin-mvp-50-v1-pt.json`, validates it against `load_latin_mvp_source_pack()`, and prints a public JSON summary. Preserve existing stable `key=value` lines and the separate Latin command boundary.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py -q</automated>
  </verify>
  <done>Users can inspect Portuguese translation QA from the Latin MVP CLI without affecting modern generation modes or requiring external credentials.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scanner-readable Phase 26 Portuguese evidence</name>
  <files>tests/integration/test_v20_latin_portuguese_translation_evidence.py</files>
  <behavior>
    - Test: `PHASE_26_REQUIREMENTS == ("PT-01", "PT-02", "PT-03")`.
    - Test PT-01/PT-02: all 50 entries have validated short and sentence translations aligned to source-pack item keys.
    - Test PT-03: QA summary has no English leakage, no Latin-copy sentence translations, no dictionary-only one-word sentence translations, and exposes review-status counts.
    - Test isolation: existing frequency/word-list/highlight source profiles and Latin source-pack loader remain importable.
  </behavior>
  <action>Create the evidence file following Phase 22/23 scanner-readable patterns. Import the translation validator, source-pack loader, `get_source_profile`, and CLI/service symbols only; do not call live translation providers. Include assertions that the Portuguese asset does not contain raw provider secrets, private absolute paths, or provider error text.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_portuguese_translation_evidence.py -q</automated>
  </verify>
  <done>Executable evidence proves PT-01/PT-02/PT-03 coverage and mode isolation for Phase 26.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user→summary output | User requests scanner-readable translation QA; output must not leak secrets or private paths. |
| translation asset→Latin MVP service | Translation data is loaded into public CLI summaries and must be validated first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | I | `generate-latin-mvp --portuguese-json` | mitigate | Print only public QA counts/statuses and test for absence of secrets, private paths, and provider error text. |
| T-26-08 | T | `LatinMvpGenerationService` optional translation summary | mitigate | Validate translation pack against source pack before adding summary output. |
| T-26-09 | D | CLI summary path | accept | Offline JSON validation is bounded to 50 entries and does not call providers; low DoS risk. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py tests/integration/test_v20_latin_portuguese_translation_evidence.py -q`.
</verification>

<success_criteria>
Phase 26 has user-visible and scanner-readable Portuguese QA evidence covering PT-01/PT-02/PT-03, with no live credentials and no existing-mode regression.
</success_criteria>

<output>
After completion, create `.planning/phases/26-portuguese-translation-quality/26-03-SUMMARY.md`.
</output>
