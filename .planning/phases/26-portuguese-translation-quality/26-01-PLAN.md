---
phase: 26-portuguese-translation-quality
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/latin_translation_quality.py
  - tests/services/test_latin_translation_quality.py
autonomous: true
requirements: [PT-01, PT-02, PT-03]
must_haves:
  truths:
    - "Every Latin MVP item can be validated for a Portuguese short translation matching the selected target-form context."
    - "Every Latin MVP item can be validated for a Portuguese sentence translation matching the displayed Latin sentence."
    - "English leakage, Latin-copy translations, dictionary-only glosses, blank fields, and context contradictions are surfaced as QA failures."
  artifacts:
    - path: "src/multilang/services/latin_translation_quality.py"
      provides: "Portuguese translation contracts and deterministic QA validator"
      exports: ["LatinPortugueseTranslationEntry", "LatinPortugueseTranslationPack", "LatinPortugueseTranslationQaService", "LatinPortugueseTranslationQaStatus"]
    - path: "tests/services/test_latin_translation_quality.py"
      provides: "Focused behavior tests for PT-01/PT-02/PT-03 validator rules"
  key_links:
    - from: "src/multilang/services/latin_translation_quality.py"
      to: "src/multilang/services/latin_source_pack.py"
      via: "validated item_key and source_pack_version alignment"
      pattern: "LatinMvpSourcePack|LatinMvpSourcePackEntry"
---

<objective>
Create the Portuguese translation quality contract and deterministic validator for Classical Latin MVP cards.

Purpose: Phase 26 needs Portuguese learner-facing text that is reviewable and cannot silently accept English leakage, Latin copies, blank fields, or dictionary-only mismatches.
Output: A service module with typed translation entries, pack-level validation, QA result objects, and focused tests covering PT-01, PT-02, and PT-03.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-01-SUMMARY.md
@src/multilang/services/latin_source_pack.py
@src/multilang/domain/text_quality.py

<interfaces>
From `src/multilang/services/latin_source_pack.py`:
```python
class LatinMvpSourcePackEntry(LatinSourceProvenance, LatinDidacticRationale):
    item_key: str
    sequence: int
    lemma: str
    target_form: str
    latin_sentence: str
    source_pack_version: Literal["latin-mvp-50-v1"]

class LatinMvpSourcePack(BaseModel):
    source_pack_version: Literal["latin-mvp-50-v1"]
    card_count: Literal[50]
    entries: list[LatinMvpSourcePackEntry]
```

From `src/multilang/domain/text_quality.py`:
```python
class ValidationStatus(str, Enum):
    PENDING = "pending"
    PASSED = "passed"
    FAILED = "failed"

class ValidationFlagCode(str, Enum):
    TRANSLATION_MISMATCH = "translation_mismatch"
    LANGUAGE_MISMATCH = "language_mismatch"
    LOW_CONFIDENCE = "low_confidence"
```
</interfaces>
</context>

<source_coverage_audit>
GOAL Phase 26 Portuguese Translation Quality: COVERED by validator contract and later asset/wiring plans.
REQ PT-01: COVERED by short-translation validation rules in this plan.
REQ PT-02: COVERED by sentence-translation validation rules in this plan.
REQ PT-03: COVERED by QA flags for English leakage, dictionary-only glosses, and contradictions in this plan.
RESEARCH/STACK: Uses Python/Pydantic/pytest patterns and avoids live provider responses as permanent truth.
CONTEXT decisions: No Phase 26 CONTEXT.md exists; project decisions require Classical Latin-only v2.0 and 50-card scope, both preserved.
Deferred/out-of-scope exclusions: Latin scale beyond 50 cards, Greek, auto image generation, and unreviewed bulk generation are not planned.
</source_coverage_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Portuguese translation QA contracts with failing tests first</name>
  <files>tests/services/test_latin_translation_quality.py, src/multilang/services/latin_translation_quality.py</files>
  <behavior>
    - Test PT-01: an entry with `short_translation_pt="menino"` for lemma `puer` and matching context passes short-translation checks.
    - Test PT-02: an entry with `sentence_translation_pt="E o menino lê."` for `Et puer legit.` passes sentence-translation checks.
    - Test PT-03: blank Portuguese fields, English words such as `the/boy/reads`, Latin-copy sentence text, and a one-word sentence translation for a multi-token Latin sentence fail with explicit QA flags.
  </behavior>
  <action>Create `src/multilang/services/latin_translation_quality.py` with Pydantic models `LatinPortugueseTranslationEntry`, `LatinPortugueseTranslationPack`, `LatinPortugueseTranslationQaIssue`, `LatinPortugueseTranslationQaResult`, enum `LatinPortugueseTranslationQaStatus`, and service `LatinPortugueseTranslationQaService`. Write tests first, then implement. Use deterministic string/token checks only; do not call DeepL, LLMs, or live providers in the validator because Phase 26 needs reproducible review evidence. Each translation entry must include `item_key`, `source_pack_version`, `lemma`, `target_form`, `latin_sentence`, `short_translation_pt`, `sentence_translation_pt`, `translation_notes`, and `review_status` with values `needs_review`, `approved`, or `rejected` for future Phase 25/26 gate compatibility.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_translation_quality.py -q</automated>
  </verify>
  <done>Validator contracts exist, tests prove valid Portuguese entries pass, and invalid English/Latin-copy/blank/dictionary-only examples fail with explicit issue codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce source-pack alignment and QA summaries</name>
  <files>tests/services/test_latin_translation_quality.py, src/multilang/services/latin_translation_quality.py</files>
  <behavior>
    - Test: a translation pack must contain exactly the same ordered item keys as `LatinMvpSourcePack`.
    - Test: mismatched `source_pack_version`, `lemma`, `target_form`, or `latin_sentence` fails validation so translations cannot drift from the selected Latin context.
    - Test: QA summary reports total entries, passed entries, failed entries, issue counts, and review-status counts.
  </behavior>
  <action>Extend the service with a `validate_pack(translations, source_pack)` method that compares each translation entry to the corresponding `LatinMvpSourcePackEntry` by item key and context fields. Return a scanner-friendly summary dictionary from `qa_summary()` containing `source_pack_version`, `translation_pack_version`, `entry_count`, `passed_count`, `failed_count`, `issue_counts`, and `review_status_counts`. Use Pydantic v2 validators for nonblank strings and service-level checks for source-pack alignment.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_translation_quality.py -q</automated>
  </verify>
  <done>Pack-level QA fails closed on source-pack drift and emits deterministic summary metadata that downstream asset and evidence plans can consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed translation asset→validator | Translation strings are human/provider-produced content and must be treated as untrusted learner-facing text. |
| source pack→translation pack | A translation entry can drift from the Latin sentence or target form unless alignment is checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | T | `LatinPortugueseTranslationQaService.validate_pack` | mitigate | Compare item key, source-pack version, lemma, target form, and Latin sentence against `LatinMvpSourcePackEntry`. |
| T-26-02 | I | `LatinPortugueseTranslationEntry` | mitigate | Require nonblank fields and reject raw provider-error/English leakage patterns before learner-facing approval. |
| T-26-03 | R | QA summary | mitigate | Emit deterministic issue counts and review-status counts for scanner-readable evidence. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_translation_quality.py -q` and confirm all tests pass without live provider credentials.
</verification>

<success_criteria>
PT-01/PT-02/PT-03 have executable validator coverage, and downstream plans can validate a committed 50-entry Portuguese translation pack against the frozen Latin source pack.
</success_criteria>

<output>
After completion, create `.planning/phases/26-portuguese-translation-quality/26-01-SUMMARY.md`.
</output>
