---
phase: 01-job-orchestration-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/multilang/__init__.py
  - src/multilang/settings.py
  - src/multilang/domain/jobs.py
  - tests/conftest.py
  - tests/test_settings.py
  - tests/domain/test_jobs.py
autonomous: true
requirements:
  - DECK-01
  - JOB-01
must_haves:
  truths:
    - User can only start a job with one of the 7 supported target languages.
    - The codebase has explicit job lifecycle contracts for status, stage progress, and resume diagnostics.
    - Automated tests exist before orchestration logic starts changing persisted state.
  artifacts:
    - path: pyproject.toml
      provides: Python 3.12 project metadata, runtime dependencies, and pytest configuration
      contains: '[project]'
    - path: src/multilang/settings.py
      provides: Typed runtime settings for the CLI and persistence layer
      contains: 'class Settings'
    - path: src/multilang/domain/jobs.py
      provides: Supported language enum, job stage/status enums, request/progress/diagnostic models
      contains: 'class SupportedLanguage'
    - path: tests/domain/test_jobs.py
      provides: Contract coverage for supported languages and resume diagnostics
      contains: 'def test_supported_languages'
  key_links:
    - from: src/multilang/settings.py
      to: pyproject.toml
      via: project packaging and test configuration
      pattern: 'tool.pytest'
    - from: src/multilang/domain/jobs.py
      to: tests/domain/test_jobs.py
      via: enum and model contract tests
      pattern: 'SupportedLanguage|ResumeDiagnostic'
---

<objective>
Establish the Python project shell and typed job orchestration contracts the rest of Phase 1 will build on.

Purpose: Make Phase 1 executable without ad-hoc discovery by defining runtime conventions, supported-language validation, and resume-safe domain models first.
Output: Bootstrapped Python package, runtime settings module, job contract module, and Wave 0 tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
@.planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
@.planning/phases/01-job-orchestration-recovery/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Python package and runtime settings</name>
  <read_first>
    AGENTS.md
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
    .planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
  </read_first>
  <files>pyproject.toml, src/multilang/__init__.py, src/multilang/settings.py, tests/conftest.py, tests/test_settings.py</files>
  <action>Create the initial Python 3.12 package per research and project constraints. In `pyproject.toml`, define the `multilang` package under `src/`, add runtime dependencies `typer`, `rich`, `pydantic`, `pydantic-settings`, `sqlalchemy`, `alembic`, and `psycopg[binary]`, and add dev dependencies `pytest` and `pytest-asyncio`. Add pytest configuration so `uv run pytest` works from repo root. Create `src/multilang/settings.py` with a `Settings` class exposing at minimum `database_url`, `default_retry_attempts`, and `supported_languages`, with `supported_languages` defaulting to exactly `['pt','es','en','fr','de','ru','nl']` per D-01 and DECK-01. Keep `src/multilang/__init__.py` minimal; do not add CLI execution logic here. Add `tests/conftest.py` for shared settings fixtures and `tests/test_settings.py` proving the 7-language default and retry default load without environment overrides.</action>
  <acceptance_criteria>
    - `pyproject.toml` contains `requires-python = ">=3.12"`
    - `pyproject.toml` contains dependency names `typer`, `rich`, `pydantic-settings`, `sqlalchemy`, and `alembic`
    - `src/multilang/settings.py` contains `class Settings`
    - `src/multilang/settings.py` contains the exact codes `pt`, `es`, `en`, `fr`, `de`, `ru`, `nl`
    - `tests/test_settings.py` contains `def test_default_supported_languages`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_settings.py -q</automated>
  </verify>
  <done>Project bootstrap is runnable with `uv`, settings are typed, and automated tests prove the phase starts from a constrained language set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define job lifecycle contracts and diagnostics</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
    .planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
    src/multilang/settings.py
  </read_first>
  <files>src/multilang/domain/jobs.py, tests/domain/test_jobs.py</files>
  <behavior>
    - Test 1: supported-language enum accepts only `pt`, `es`, `en`, `fr`, `de`, `ru`, and `nl`
    - Test 2: resume diagnostics preserve a machine-readable `reason`, `job_id`, and mismatch details for D-06
    - Test 3: progress snapshots track `stage`, `completed_items`, `failed_items`, `retrying_items`, and `skipped_duplicates`
  </behavior>
  <action>Create `src/multilang/domain/jobs.py` with explicit contracts for the rest of the phase: `SupportedLanguage`, `JobStage`, `JobStatus`, `GenerationRequest`, `JobProgressSnapshot`, `RetryPolicy`, and `ResumeDiagnostic`. Use stage names `ingest`, `enrich`, `generate_text`, `synthesize_audio`, and `export` so later phases can reuse them without renaming. `GenerationRequest` must support `language`, `source_type`, `level`, `input_file`, `resume_job_id`, `overwrite`, and `yes_overwrite`. `ResumeDiagnostic` must carry a non-empty `reason`, the `job_id`, and a `details` mapping so inconsistent persisted state stops safely per D-06 instead of auto-repairing. Write tests first in `tests/domain/test_jobs.py`, then implement the models to make those tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/domain/jobs.py` contains `class SupportedLanguage`
    - `src/multilang/domain/jobs.py` contains `class ResumeDiagnostic`
    - `src/multilang/domain/jobs.py` contains `skipped_duplicates`
    - `tests/domain/test_jobs.py` contains `def test_supported_languages`
    - `tests/domain/test_jobs.py` contains `def test_resume_diagnostic_requires_reason`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_settings.py tests/domain/test_jobs.py -q</automated>
  </verify>
  <done>Typed contracts exist for supported languages, job stages, progress counters, and corrupted-resume diagnostics, with passing tests to lock the behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → domain models | Untrusted operator input becomes validated job requests |
| environment variables → settings | Runtime configuration may be missing or malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-job-01 | T | `src/multilang/domain/jobs.py` | mitigate | Use strict enums and Pydantic validation so unsupported languages or malformed request shapes are rejected before job creation. |
| T-01-job-02 | D | `src/multilang/domain/jobs.py` | mitigate | Model `ResumeDiagnostic` explicitly so corrupted resume state stops with a clear diagnostic instead of looping or continuing unsafely per D-06. |
| T-01-job-03 | R | `tests/domain/test_jobs.py` | mitigate | Add contract tests for supported languages and diagnostics so later changes cannot silently weaken Phase 1 safety rules. |
| T-01-job-04 | I | `src/multilang/settings.py` | accept | No secrets are introduced in Phase 1 planning scope; settings exposure risk is low because only local runtime defaults are present. |
</threat_model>

<verification>
- `uv run pytest tests/test_settings.py tests/domain/test_jobs.py -q`
- Confirm `src/multilang/domain/jobs.py` exports the enums and models later plans depend on.
</verification>

<success_criteria>
- Python project bootstrap exists and is testable with `uv run pytest`.
- Supported-language validation is encoded in one shared contract, not duplicated ad hoc.
- Resume diagnostics and progress counters exist as typed models before persistence and CLI wiring begins.
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-orchestration-recovery/01-01-SUMMARY.md`
</output>
