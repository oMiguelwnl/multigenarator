---
phase: 01-job-orchestration-recovery
plan: 06
type: execute
wave: 6
depends_on:
  - 01-05
files_modified:
  - src/multilang/cli.py
  - src/multilang/services/job_summary.py
  - tests/cli/test_generate_command.py
  - tests/integration/test_job_flow.py
autonomous: true
requirements:
  - JOB-01
  - JOB-02
  - JOB-03
gap_closure: true
must_haves:
  truths:
    - The shipped `multilang generate` command prints stage-level progress while it runs per D-03.
    - The shipped app can resume a persisted job safely and abort with a clear diagnostic if persisted state is inconsistent per D-05 and D-06.
    - The shipped app reaches duplicate-skip and explicit-overwrite behavior on real reruns per D-07 and D-08.
  artifacts:
    - path: src/multilang/cli.py
      provides: default runtime execution path with progress, summary, and resume-diagnostic output
      contains: "JobSummaryBuilder"
    - path: tests/integration/test_job_flow.py
      provides: shipped-app lifecycle coverage using the real bootstrap path
      contains: "def test_shipped_app_supports_resume_and_duplicate_safe_rerun"
    - path: tests/cli/test_generate_command.py
      provides: CLI-level assertions for resume-diagnostic and summary output
      contains: "persisted resume state is inconsistent"
  key_links:
    - from: src/multilang/cli.py
      to: src/multilang/progress.py
      via: default runtime executor emits rendered progress lines during shipped app execution
      pattern: "ProgressRenderer"
    - from: src/multilang/cli.py
      to: src/multilang/services/job_summary.py
      via: runtime CLI prints lifecycle summary after repository-backed execution
      pattern: "JobSummaryBuilder"
    - from: tests/integration/test_job_flow.py
      to: src/multilang/cli.py
      via: tests invoke the shipped bootstrap path rather than only injected `create_app(service=service)` wiring
      pattern: "app = create_app\(\)"
---

<objective>
Finish Phase 1 gap closure by making the shipped CLI visibly exercise progress, safe resume, and duplicate-safe rerun behavior end to end.

Purpose: The remaining verification failures are behavioral, not structural—the default user path must now display progress, print lifecycle outcomes, and prove resume/rerun semantics through real shipped-app coverage.
Output: Runtime summary/diagnostic output in the CLI plus integration coverage against the shipped bootstrap path.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
@.planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
@src/multilang/runtime.py
@src/multilang/services/execution_report.py

<interfaces>
Plan 05 creates these contracts for this plan to consume:

```python
def build_runtime_service(settings: Settings | None = None) -> GenerateJobService: ...

class JobExecutionReport:
    orchestration: GenerateJobResult
    progress_updates: list[str]
    retried_item_keys: list[str]
    failed_item_keys: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Print runtime progress, lifecycle summary, and resume diagnostics on the shipped path</name>
  <read_first>
    src/multilang/cli.py
    src/multilang/progress.py
    src/multilang/services/job_summary.py
    src/multilang/runtime.py
    tests/cli/test_generate_command.py
    .planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
    .planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
  </read_first>
  <files>src/multilang/cli.py, src/multilang/services/job_summary.py, tests/cli/test_generate_command.py</files>
  <behavior>
    - Test 1: A repository-backed shipped run prints counter-based progress lines and a final lifecycle summary instead of exiting silently.
    - Test 2: Resume diagnostics surface `persisted resume state is inconsistent` and exit non-zero per D-06.
    - Test 3: Explicit overwrite confirmation and duplicate-skip behavior still work after the default app switches to runtime bootstrap.
  </behavior>
  <action>Per D-03, D-05, D-06, D-07, and D-08, update `src/multilang/cli.py` so the default `generate` command handles a `JobExecutionReport` result from the runtime executor: emit progress via `ProgressRenderer`, build a final lifecycle summary with `JobSummaryBuilder`, print summary lines containing `completed_items`, `retried_items`, `failed_items`, `skipped_duplicates`, `resumed_from_job`, and `overwritten_items`, and if `report.orchestration.diagnostic` is not `None` print the diagnostic reason/details then exit with code 1 instead of continuing unsafely. Update `tests/cli/test_generate_command.py` first to assert these observable strings and non-zero exit behavior on corrupted resume state. Do not reintroduce verbose per-item default logs.</action>
  <acceptance_criteria>
    - `src/multilang/cli.py` contains `JobSummaryBuilder`
    - `src/multilang/cli.py` contains `persisted resume state is inconsistent` handling or equivalent diagnostic print path
    - `tests/cli/test_generate_command.py` contains assertions for `completed_items` and `skipped_duplicates`
    - `tests/cli/test_generate_command.py` contains `persisted resume state is inconsistent`
  </acceptance_criteria>
  <verify>
    <automated>"/home/miguel/.local/bin/uv" run pytest tests/cli/test_generate_command.py tests/cli/test_runtime_bootstrap.py -q</automated>
  </verify>
  <done>The shipped CLI visibly reports progress and lifecycle outcomes, and corrupted resume state aborts with a clear diagnostic instead of silent success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace injected-only smoke coverage with shipped-app bootstrap coverage</name>
  <read_first>
    src/multilang/cli.py
    src/multilang/runtime.py
    tests/integration/test_job_flow.py
    tests/cli/test_runtime_bootstrap.py
    .planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
  </read_first>
  <files>tests/integration/test_job_flow.py, src/multilang/cli.py</files>
  <behavior>
    - Test 1: The integration suite invokes the shipped bootstrap path and observes stage progress output for a real run.
    - Test 2: The shipped bootstrap path supports resume of a persisted partially completed job.
    - Test 3: The shipped bootstrap path skips duplicates on rerun by default and only overwrites with explicit confirmation.
  </behavior>
  <action>Rewrite `tests/integration/test_job_flow.py` so the primary smoke test covers the verifier's missing path directly. Per D-03, D-05, D-07, and D-08, use a temp SQLite database file via `MULTILANG_DATABASE_URL`, invoke the shipped `app = create_app()` path with `CliRunner`, and assert observable output plus persisted state for three flows: (1) fresh word-list run emits `stage=ingest` progress and creates a job, (2) resume against a pre-seeded partial job processes only the remaining items and reports `resumed_from_job`, and (3) rerunning the same input reports `skipped_duplicates`, while `--overwrite --yes-overwrite` reports `overwritten_items`. Keep any helper setup repository-backed; do not fall back to `create_app(service=service)` as the main proof path.</action>
  <acceptance_criteria>
    - `tests/integration/test_job_flow.py` contains `def test_shipped_app_supports_resume_and_duplicate_safe_rerun`
    - `tests/integration/test_job_flow.py` contains `MULTILANG_DATABASE_URL`
    - `tests/integration/test_job_flow.py` asserts `stage=ingest`
    - `tests/integration/test_job_flow.py` asserts `skipped_duplicates` and `overwritten_items`
  </acceptance_criteria>
  <verify>
    <automated>"/home/miguel/.local/bin/uv" run pytest tests/integration/test_job_flow.py tests/cli/test_generate_command.py tests/cli/test_runtime_bootstrap.py -q</automated>
  </verify>
  <done>The real shipped app path is now the integration-tested source of truth for progress, resume, and duplicate-safe rerun behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted resume state → CLI output | Stored job state drives whether the app resumes, skips, or aborts |
| repository-backed execution → operator summary | Users rely on printed lifecycle data to trust reruns and failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-gap-08 | R | `src/multilang/cli.py` final output | mitigate | Print explicit lifecycle summary fields so operators can audit completed, failed, skipped, resumed, and overwritten work after each shipped run. |
| T-01-gap-09 | T | resume flow in `src/multilang/cli.py` | mitigate | Abort with the repository's `ResumeDiagnostic` when persisted state is inconsistent rather than attempting an unsafe automatic restart. |
| T-01-gap-10 | S | `tests/integration/test_job_flow.py` shipped-app coverage | mitigate | Exercise the real `app = create_app()` bootstrap path so regressions in visible progress, resume, or rerun semantics cannot hide behind injected-only tests. |
</threat_model>

<verification>
- `"/home/miguel/.local/bin/uv" run pytest tests/integration/test_job_flow.py tests/cli/test_generate_command.py tests/cli/test_runtime_bootstrap.py -q`
- `"/home/miguel/.local/bin/uv" run python -c "from typer.testing import CliRunner; from multilang.cli import app; import tempfile, pathlib, os; tmp=tempfile.TemporaryDirectory(); db=pathlib.Path(tmp.name)/'job.db'; words=pathlib.Path(tmp.name)/'words.txt'; words.write_text('alpha\nbeta\n', encoding='utf-8'); os.environ['MULTILANG_DATABASE_URL']=f'sqlite+pysqlite:///{db}'; r=CliRunner().invoke(app,['generate','--language','en','--source','word-list','--input-file',str(words)]); print(r.exit_code); print(r.output)"`
</verification>

<success_criteria>
- The shipped app prints progress during execution instead of returning empty output.
- The shipped app can resume persisted jobs safely and surfaces corrupted-state diagnostics clearly.
- Duplicate-safe reruns and explicit overwrite confirmation are validated on the real bootstrap path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-orchestration-recovery/01-06-SUMMARY.md`
</output>
