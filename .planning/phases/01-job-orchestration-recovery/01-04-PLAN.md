---
phase: 01-job-orchestration-recovery
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/multilang/progress.py
  - src/multilang/services/job_summary.py
  - src/multilang/cli.py
  - tests/test_progress.py
  - tests/test_job_summary.py
  - tests/integration/test_job_flow.py
autonomous: true
requirements:
  - JOB-01
  - JOB-02
  - JOB-03
must_haves:
  truths:
    - User sees stage-level progress and failures while the job runs.
    - Failed items retry automatically, then remain visible in the final summary if they still fail.
    - Start, resume, rerun-skip, and overwrite-confirm flows all work together in one smoke-tested job lifecycle.
  artifacts:
    - path: src/multilang/progress.py
      provides: default stage-level progress renderer with counters
      contains: 'class ProgressRenderer'
    - path: src/multilang/services/job_summary.py
      provides: final summary containing failed, retried, skipped, and resumed counts
      contains: 'class JobSummaryBuilder'
    - path: tests/integration/test_job_flow.py
      provides: end-to-end job lifecycle verification
      contains: 'def test_job_flow_supports_resume_and_duplicate_safe_rerun'
  key_links:
    - from: src/multilang/cli.py
      to: src/multilang/progress.py
      via: CLI uses progress renderer during execution
      pattern: 'ProgressRenderer'
    - from: src/multilang/services/job_summary.py
      to: tests/integration/test_job_flow.py
      via: lifecycle summary assertions in smoke coverage
      pattern: 'failed_items|skipped_duplicates|retried_items'
---

<objective>
Finish Phase 1 with the progress, retry, and lifecycle-summary behavior users need to trust long-running runs.

Purpose: Resumability and duplicate safety are not enough unless users can see progress, understand failures, and verify the whole lifecycle in tests.
Output: Rich progress renderer, bounded retry-and-continue behavior, final summary builder, and job-flow smoke tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
@.planning/phases/01-job-orchestration-recovery/01-RESEARCH.md

<interfaces>
The preceding plans establish:

```python
class JobProgressSnapshot(BaseModel):
    stage: JobStage
    completed_items: int
    failed_items: int
    retrying_items: int
    skipped_duplicates: int

class GenerateJobService:
    def start(...): ...
    def resume(...): ...
    def rerun(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render stage-level progress and bounded retry behavior</name>
  <read_first>
    .planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
    .planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
    src/multilang/domain/jobs.py
    src/multilang/services/generate_job.py
    src/multilang/cli.py
  </read_first>
  <files>src/multilang/progress.py, src/multilang/cli.py, tests/test_progress.py</files>
  <behavior>
    - Test 1: default progress rendering shows stage name plus completed, failed, retrying, and skipped counters
    - Test 2: item failures retry once more, then move to failed state while the job continues per D-04
    - Test 3: default execution path uses progress counters instead of verbose per-item logs per D-03
  </behavior>
  <action>Implement `ProgressRenderer` in `src/multilang/progress.py` using Rich. The default rendered line must include the active stage name plus `completed/total`, `retrying`, `failed`, and `skipped_duplicates`. Update `src/multilang/cli.py` to route job execution through this renderer by default. In the orchestration path, use exactly `default_retry_attempts = 2 total attempts` from `Settings`: first failure triggers one retry, second failure records the item as failed and continues processing the remaining queue per D-04. Do not switch the default UX to verbose logs in this plan. Write tests first in `tests/test_progress.py`, then implement the renderer and retry behavior to satisfy them.</action>
  <acceptance_criteria>
    - `src/multilang/progress.py` contains `class ProgressRenderer`
    - `src/multilang/progress.py` contains the counter names `retrying` and `skipped_duplicates`
    - `tests/test_progress.py` contains `def test_progress_renderer_shows_stage_counters`
    - `src/multilang/cli.py` references `ProgressRenderer`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_progress.py -q</automated>
  </verify>
  <done>Running jobs show counter-based progress by default, and item failures follow bounded retry then continue behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build final lifecycle summaries and smoke-test the full job flow</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
    src/multilang/cli.py
    src/multilang/progress.py
    src/multilang/services/generate_job.py
  </read_first>
  <files>src/multilang/services/job_summary.py, tests/test_job_summary.py, tests/integration/test_job_flow.py</files>
  <behavior>
    - Test 1: final summary lists failed items after retries are exhausted
    - Test 2: final summary includes resumed, skipped duplicate, and overwritten counts distinctly
    - Test 3: integration flow covers fresh start, interrupted run, resume, rerun-skip, and overwrite-confirm paths
  </behavior>
  <action>Create `JobSummaryBuilder` in `src/multilang/services/job_summary.py` that emits a final summary with these fields visible: `completed_items`, `failed_items`, `retried_items`, `skipped_duplicates`, `resumed_from_job`, and `overwritten_items`. Add an integration test in `tests/integration/test_job_flow.py` that simulates a partial run, resumes from the persisted state, reruns the same input with duplicate skipping, and then verifies overwrite only occurs when explicit confirmation is supplied. Keep the smoke test repository-backed; do not replace it with a pure unit test. Write tests first in `tests/test_job_summary.py` and `tests/integration/test_job_flow.py`, then implement the summary builder.</action>
  <acceptance_criteria>
    - `src/multilang/services/job_summary.py` contains `class JobSummaryBuilder`
    - `src/multilang/services/job_summary.py` contains `skipped_duplicates` and `overwritten_items`
    - `tests/test_job_summary.py` contains `def test_summary_lists_failed_items_after_retries`
    - `tests/integration/test_job_flow.py` contains `def test_job_flow_supports_resume_and_duplicate_safe_rerun`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_progress.py tests/test_job_summary.py tests/integration/test_job_flow.py -q</automated>
  </verify>
  <done>Phase 1 has an end-to-end verified lifecycle: visible progress, bounded retries, safe resume, duplicate-safe reruns, and explicit overwrite handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestration events → terminal output | Users rely on rendered progress and summaries to decide whether a run is trustworthy |
| failure/retry loop → repository state | Retry handling changes final persisted job outcomes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-job-05 | D | `src/multilang/progress.py` | mitigate | Render counters from `JobProgressSnapshot` so the default UI remains concise and informative instead of flooding logs or hiding failures. |
| T-01-job-08 | R | `src/multilang/services/job_summary.py` | mitigate | Emit final summary fields for failed, retried, skipped, resumed, and overwritten counts so operators can audit what happened in a run. |
| T-01-job-09 | T | retry loop in `src/multilang/cli.py` / orchestration path | mitigate | Cap retries at two total attempts and persist failures after exhaustion so repeated errors cannot mutate job state indefinitely. |
| T-01-job-10 | S | `tests/integration/test_job_flow.py` | mitigate | Add end-to-end smoke coverage for start/resume/rerun/overwrite paths so regressions in lifecycle behavior are caught automatically before shipping. |
</threat_model>

<verification>
- `uv run pytest tests/test_progress.py tests/test_job_summary.py tests/integration/test_job_flow.py -q`
- Manually run one stubbed `multilang generate` execution and confirm the default output is counter-based rather than verbose-log-first.
</verification>

<success_criteria>
- Users can observe job progress and failures while a run is active.
- Failed items retry automatically, then remain visible in the final summary if still unsuccessful.
- The complete job lifecycle is smoke-tested for start, resume, rerun-skip, and overwrite-confirm flows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-orchestration-recovery/01-04-SUMMARY.md`
</output>
