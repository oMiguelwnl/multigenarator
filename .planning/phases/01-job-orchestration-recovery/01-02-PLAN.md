---
phase: 01-job-orchestration-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - alembic.ini
  - alembic/env.py
  - alembic/versions/20260418_01_job_tables.py
  - src/multilang/db/base.py
  - src/multilang/db/models.py
  - src/multilang/repositories/job_repository.py
  - tests/repositories/test_job_repository.py
autonomous: true
requirements:
  - JOB-01
  - JOB-03
must_haves:
  truths:
    - Job state survives process interruption because job and item state are persisted.
    - Reruns can identify already completed items for the same logical input and skip them safely.
    - Corrupted persisted state produces an explicit diagnostic path instead of an unsafe resume.
  artifacts:
    - path: src/multilang/db/models.py
      provides: SQLAlchemy job and item persistence schema
      contains: 'generation_jobs'
    - path: src/multilang/repositories/job_repository.py
      provides: persistence API for create/load/resume/duplicate-skip operations
      contains: 'class JobRepository'
    - path: tests/repositories/test_job_repository.py
      provides: automated verification for uniqueness and resume validation
      contains: 'def test_completed_items_are_reused_on_rerun'
  key_links:
    - from: src/multilang/db/models.py
      to: alembic/versions/20260418_01_job_tables.py
      via: migration reflecting the SQLAlchemy schema
      pattern: 'generation_jobs|generation_items'
    - from: src/multilang/repositories/job_repository.py
      to: src/multilang/domain/jobs.py
      via: repository returns typed diagnostics and state snapshots
      pattern: 'ResumeDiagnostic|JobProgressSnapshot'
---

<objective>
Add persisted job/item storage and repository rules for resumability and duplicate-safe reruns.

Purpose: Phase 1 cannot deliver safe resume or duplicate skipping without durable state and uniqueness enforcement.
Output: Alembic migration, SQLAlchemy models, and a repository that persists jobs, item progress, and corrupted-state diagnostics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
@.planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
@.planning/phases/01-job-orchestration-recovery/01-PATTERNS.md

<interfaces>
From `src/multilang/domain/jobs.py` (created by Plan 01), downstream code should use these contracts directly:

```python
class SupportedLanguage(str, Enum): ...
class JobStage(str, Enum): ...
class JobStatus(str, Enum): ...
class GenerationRequest(BaseModel): ...
class JobProgressSnapshot(BaseModel): ...
class ResumeDiagnostic(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the persisted job schema and migration</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
    .planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
    src/multilang/domain/jobs.py
    pyproject.toml
  </read_first>
  <files>alembic.ini, alembic/env.py, alembic/versions/20260418_01_job_tables.py, src/multilang/db/base.py, src/multilang/db/models.py</files>
  <action>Create the SQLAlchemy base and initial Alembic migration for Phase 1 persistence. Define `generation_jobs` with columns `id`, `run_key`, `language`, `source_type`, `source_fingerprint`, `status`, `current_stage`, `last_completed_stage`, `total_items`, `completed_items`, `failed_items`, `retrying_items`, `skipped_duplicates`, `resume_state`, `created_at`, and `updated_at`. Define `generation_items` with columns `id`, `job_id`, `run_key`, `item_key`, `status`, `last_completed_stage`, `retry_count`, `last_error`, `created_at`, and `updated_at`. Add a unique constraint on `(run_key, item_key)` per D-07 so duplicate records cannot be inserted silently. Keep the migration and SQLAlchemy models aligned exactly; do not invent extra tables in this plan.</action>
  <acceptance_criteria>
    - `src/multilang/db/models.py` contains `generation_jobs`
    - `src/multilang/db/models.py` contains `generation_items`
    - `src/multilang/db/models.py` contains `UniqueConstraint(`
    - `alembic/versions/20260418_01_job_tables.py` contains both table names
    - `generation_items` unique constraint names both `run_key` and `item_key`
  </acceptance_criteria>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
models = Path('src/multilang/db/models.py').read_text()
migration = Path('alembic/versions/20260418_01_job_tables.py').read_text()
assert 'generation_jobs' in models
assert 'generation_items' in models
assert 'run_key' in models and 'item_key' in models
assert 'generation_jobs' in migration and 'generation_items' in migration
print('schema-artifacts-ok')
PY</automated>
  </verify>
  <done>Durable schema exists for jobs and items, and uniqueness rules prevent silent duplicate persistence across reruns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement repository behavior for resume-safe reads and duplicate reuse</name>
  <read_first>
    .planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
    .planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
    src/multilang/domain/jobs.py
    src/multilang/db/models.py
  </read_first>
  <files>src/multilang/repositories/job_repository.py, tests/repositories/test_job_repository.py</files>
  <behavior>
    - Test 1: loading a rerun by `run_key` returns completed item keys so missing work can be scheduled without duplicating completed rows
    - Test 2: a persisted stage mismatch returns `ResumeDiagnostic` instead of attempting recovery
    - Test 3: recording the same `(run_key, item_key)` twice is blocked or surfaced as duplicate reuse logic, not a silent second success row
  </behavior>
  <action>Write tests first, then implement `JobRepository` with at minimum `create_job`, `get_job`, `list_completed_item_keys`, `record_item_success`, `record_item_failure`, and `validate_resume_state`. `validate_resume_state` must compare the stored `current_stage` / `last_completed_stage` with actual item records and return a `ResumeDiagnostic` containing mismatch details when state is inconsistent per D-06. `list_completed_item_keys` must power D-05 and D-07 by returning only already completed item keys for the requested `run_key`, so later plans can skip them during resume or rerun. Keep repository return values aligned with the domain models from Plan 01.</action>
  <acceptance_criteria>
    - `src/multilang/repositories/job_repository.py` contains `class JobRepository`
    - `src/multilang/repositories/job_repository.py` contains `def list_completed_item_keys`
    - `src/multilang/repositories/job_repository.py` contains `def validate_resume_state`
    - `tests/repositories/test_job_repository.py` contains `def test_completed_items_are_reused_on_rerun`
    - `tests/repositories/test_job_repository.py` contains `def test_corrupted_resume_state_returns_diagnostic`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/repositories/test_job_repository.py -q</automated>
  </verify>
  <done>Repository behavior is proven by tests for resume diagnostics and duplicate-safe completed-item reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository methods → database | Persisted state becomes trusted input for future resumes |
| migration/schema → runtime services | Uniqueness and counters depend on live schema matching code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-job-03 | T | `src/multilang/db/models.py` | mitigate | Enforce `(run_key, item_key)` uniqueness so reruns cannot create silent duplicate item rows. |
| T-01-job-04 | R | `src/multilang/repositories/job_repository.py` | mitigate | Return explicit completed-item lists and persisted state reads so rerun/resume decisions are auditable through repository methods. |
| T-01-job-05 | D | `src/multilang/repositories/job_repository.py` | mitigate | `validate_resume_state` must abort with `ResumeDiagnostic` when counters and stage pointers disagree, preventing unsafe resume loops. |
| T-01-job-06 | E | `alembic/versions/20260418_01_job_tables.py` | accept | No privilege boundary change occurs in this local schema setup; migration risk is limited to developer environment initialization. |
</threat_model>

<verification>
- `uv run pytest tests/repositories/test_job_repository.py -q`
- Confirm the migration and SQLAlchemy models both include `generation_jobs` and `generation_items` with the same columns.
</verification>

<success_criteria>
- Persisted schema exists for job-level and item-level state.
- Repository code can distinguish safe resume from corrupted state.
- Duplicate-safe rerun behavior is implemented through durable uniqueness and completed-item lookup, not only in memory.
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-orchestration-recovery/01-02-SUMMARY.md`
</output>
