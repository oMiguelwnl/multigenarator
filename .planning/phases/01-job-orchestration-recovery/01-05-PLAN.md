---
phase: 01-job-orchestration-recovery
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - src/multilang/cli.py
  - src/multilang/runtime.py
  - src/multilang/services/execution_report.py
  - src/multilang/services/job_summary.py
  - tests/cli/test_runtime_bootstrap.py
autonomous: true
requirements:
  - JOB-01
  - JOB-02
  - JOB-03
gap_closure: true
must_haves:
  truths:
    - The shipped `multilang generate` path constructs a repository-backed orchestration service instead of returning the request unchanged.
    - Resume and rerun commands issued through the default app now reach persisted job state.
    - CLI runtime summary wiring can import the execution report contract without circular imports.
  artifacts:
    - path: src/multilang/runtime.py
      provides: lazy runtime bootstrap for engine, session, repository, and service construction
      contains: "build_runtime_service"
    - path: src/multilang/services/execution_report.py
      provides: shared execution-report contract used by CLI and summary code
      contains: "class JobExecutionReport"
    - path: tests/cli/test_runtime_bootstrap.py
      provides: automated proof that the default CLI bootstrap resolves a real service
      contains: "def test_default_app_bootstraps_runtime_service"
  key_links:
    - from: src/multilang/cli.py
      to: src/multilang/runtime.py
      via: default app resolves a runtime service factory lazily at command execution
      pattern: "build_runtime_service"
    - from: src/multilang/services/job_summary.py
      to: src/multilang/services/execution_report.py
      via: summary builder imports the shared report dataclass instead of importing from cli.py
      pattern: "JobExecutionReport"
---

<objective>
Close the core Phase 1 wiring gap by making the shipped CLI bootstrap the real persistence-backed orchestration stack.

Purpose: Verification proved the user-facing app still exits through a no-op path, which blocks visible progress, real resume, and duplicate-safe reruns even though the underlying repository/service code exists.
Output: Shared execution-report contract, runtime bootstrap module, and default CLI bootstrap coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
@.planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
@.planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md

<interfaces>
From src/multilang/domain/jobs.py:
```python
class GenerationRequest(BaseModel):
    language: SupportedLanguage
    source_type: Literal["frequency", "word-list"]
    level: int | None = None
    input_file: Path | None = None
    resume_job_id: str | None = None
    overwrite: bool = False
    yes_overwrite: bool = False

class ResumeDiagnostic(BaseModel):
    job_id: str
    reason: str
    details: dict[str, Any]
```

From src/multilang/services/generate_job.py:
```python
class GenerateJobService:
    def __init__(self, repository: JobRepository) -> None: ...
    def orchestrate(self, request: GenerationRequest, *, requested_item_keys: Iterable[str]) -> GenerateJobResult: ...
```

From src/multilang/settings.py:
```python
class Settings(BaseSettings):
    database_url: str = "postgresql+psycopg://postgres:postgres@localhost:5432/multilang"
    default_retry_attempts: int = 2
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract a shared execution-report contract and runtime bootstrap</name>
  <read_first>
    .planning/phases/01-job-orchestration-recovery/01-CONTEXT.md
    .planning/phases/01-job-orchestration-recovery/01-RESEARCH.md
    .planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
    src/multilang/cli.py
    src/multilang/services/generate_job.py
    src/multilang/services/job_summary.py
    src/multilang/settings.py
    src/multilang/db/base.py
    src/multilang/db/models.py
  </read_first>
  <files>src/multilang/services/execution_report.py, src/multilang/runtime.py, src/multilang/cli.py, src/multilang/services/job_summary.py</files>
  <behavior>
    - Test 1: CLI and summary code import the same `JobExecutionReport` contract without a circular import.
    - Test 2: The default app resolves a real `GenerateJobService` from runtime settings instead of using `default_generate_executor`, closing the verification blocker.
    - Test 3: Runtime bootstrap uses `Settings.database_url` to construct engine/session/repository/service objects, satisfying the missing runtime wiring from the gap report.
  </behavior>
  <action>Per D-05, D-06, and the first two verification gaps, move `JobExecutionReport` out of `src/multilang/cli.py` into `src/multilang/services/execution_report.py` and update all imports so `src/multilang/services/job_summary.py` no longer imports from `cli.py`. Create `src/multilang/runtime.py` that exposes a lazy runtime bootstrap function named `build_runtime_service(settings: Settings | None = None) -> GenerateJobService`. That function must use SQLAlchemy `create_engine(settings.database_url)`, open a session, wrap it in `JobRepository`, and return `GenerateJobService(repository)`. Update `src/multilang/cli.py` so `create_app()` defaults to a service factory that calls `build_runtime_service` at command execution time rather than binding the current no-op `default_generate_executor`. Keep explicit dependency injection for tests (`service=` and `generate_executor=`) working; do not remove the injectable hooks.</action>
  <acceptance_criteria>
    - `src/multilang/services/execution_report.py` contains `class JobExecutionReport`
    - `src/multilang/runtime.py` contains `def build_runtime_service`
    - `src/multilang/runtime.py` contains `create_engine`
    - `src/multilang/services/job_summary.py` imports `JobExecutionReport` from `multilang.services.execution_report`
    - `src/multilang/cli.py` references `build_runtime_service`
    - `src/multilang/cli.py` no longer contains `def default_generate_executor(request: GenerationRequest) -> GenerationRequest`
  </acceptance_criteria>
  <verify>
    <automated>"/home/miguel/.local/bin/uv" run pytest tests/test_job_summary.py tests/cli/test_generate_command.py -q</automated>
  </verify>
  <done>The shipped CLI has a real runtime service bootstrap and the summary layer can consume execution reports without import cycles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add default-app bootstrap coverage for repository-backed execution</name>
  <read_first>
    src/multilang/cli.py
    src/multilang/runtime.py
    tests/cli/test_generate_command.py
    tests/integration/test_job_flow.py
    .planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
  </read_first>
  <files>tests/cli/test_runtime_bootstrap.py, src/multilang/cli.py</files>
  <behavior>
    - Test 1: The default `create_app()` path calls the runtime service factory when no explicit service is injected.
    - Test 2: The default CLI path accepts a temp SQLite `MULTILANG_DATABASE_URL` and reaches repository-backed orchestration.
  </behavior>
  <action>Write `tests/cli/test_runtime_bootstrap.py` first. Per D-05 and D-07, cover the exact shipped path the verifier said was missing: instantiate the default app (no injected `service=`), configure `MULTILANG_DATABASE_URL` to a temp SQLite file, invoke `generate`, and assert that the runtime bootstrap path constructs a real service instead of returning the request unchanged. In `src/multilang/cli.py`, keep service resolution lazy so environment overrides set inside tests are honored before command execution. The default shipped app must now be able to create or resume persisted jobs through the repository-backed stack, not only through `create_app(service=service)` test wiring.</action>
  <acceptance_criteria>
    - `tests/cli/test_runtime_bootstrap.py` contains `def test_default_app_bootstraps_runtime_service`
    - `tests/cli/test_runtime_bootstrap.py` contains `MULTILANG_DATABASE_URL`
    - `src/multilang/cli.py` contains a lazy service-resolution branch for the default app path
  </acceptance_criteria>
  <verify>
    <automated>"/home/miguel/.local/bin/uv" run pytest tests/cli/test_runtime_bootstrap.py tests/cli/test_generate_command.py tests/test_job_summary.py -q</automated>
  </verify>
  <done>Automated coverage proves the shipped CLI bootstrap now resolves a real persistence-backed orchestration service.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI command invocation → runtime bootstrap | Untrusted operator input triggers DB/session construction and orchestration wiring |
| runtime bootstrap → persistence layer | Default app now mutates persisted job state instead of using a no-op path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-gap-05 | T | `src/multilang/cli.py` default executor path | mitigate | Remove the no-op default execution path and route the shipped CLI through `build_runtime_service` so job state cannot be silently discarded. |
| T-01-gap-06 | D | `src/multilang/runtime.py` DB bootstrap | mitigate | Build the service lazily from `Settings.database_url` at command execution so runtime config is explicit and testable rather than hidden at import time. |
| T-01-gap-07 | R | `src/multilang/services/job_summary.py` import path | mitigate | Move `JobExecutionReport` into a shared module so runtime summary wiring is explicit and cannot fail from a circular import when the default app starts using it. |
</threat_model>

<verification>
- `"/home/miguel/.local/bin/uv" run pytest tests/cli/test_runtime_bootstrap.py tests/cli/test_generate_command.py tests/test_job_summary.py -q`
</verification>

<success_criteria>
- Running the shipped app no longer exits through a no-op executor.
- Default CLI execution can create or resume persisted jobs through runtime bootstrap code.
- Shared execution-report wiring is in place for later summary and diagnostic output.
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-orchestration-recovery/01-05-SUMMARY.md`
</output>
