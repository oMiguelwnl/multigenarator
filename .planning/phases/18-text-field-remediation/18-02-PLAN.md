---
phase: 18-text-field-remediation
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/multilang/services/text_field_remediation.py
  - src/multilang/services/lexical_grounding.py
  - src/multilang/services/assemble_export_cards.py
  - tests/services/test_text_field_remediation.py
  - tests/services/test_lexical_grounding.py
  - tests/services/test_assemble_export_cards.py
autonomous: true
requirements: [DEF-01, DEF-02]
must_haves:
  truths:
    - "User receives semantic English definitions instead of grammar-only case or inflection metadata."
    - "User receives the known corrected sense for `дости́чь`: `verb: to achieve, to attain, to reach`."
  artifacts:
    - path: "src/multilang/services/text_field_remediation.py"
      provides: "Definition remediation and validation helpers"
      exports: ["remediate_definition_html", "validate_definition_html"]
    - path: "src/multilang/services/lexical_grounding.py"
      provides: "Definition remediation at grounding time"
    - path: "src/multilang/services/assemble_export_cards.py"
      provides: "Export-time block for unresolved banned Definition patterns"
  key_links:
    - from: "src/multilang/services/lexical_grounding.py"
      to: "src/multilang/services/text_field_remediation.py"
      via: "remediate_definition_html before LexicalCardCandidate persistence"
      pattern: "remediate_definition_html"
    - from: "src/multilang/services/assemble_export_cards.py"
      to: "src/multilang/services/text_field_remediation.py"
      via: "validate_definition_html before export row creation"
      pattern: "validate_definition_html"
---

<objective>
Correct Definition values so normal generated cards use learner-safe semantic meanings rather than grammar metadata or known wrong senses.

Purpose: DEF-01 and DEF-02 require corrections before export, not just reports from Phase 17.
Output: A reusable remediation helper, grounding-time correction, and export-time validation block.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@.planning/phases/17-deck-quality-audit-and-issue-reports/17-01-SUMMARY.md
@src/multilang/services/lexical_grounding.py
@src/multilang/services/assemble_export_cards.py
@tests/services/test_lexical_grounding.py
@tests/services/test_assemble_export_cards.py

<interfaces>
Create this interface in `src/multilang/services/text_field_remediation.py`:
```python
def remediate_definition_html(
    *,
    display_form: str,
    lemma: str,
    part_of_speech: str | None,
    generated_html: str | None,
    source_definitions: list[str],
) -> str | None: ...

def validate_definition_html(*, lemma_key: str, definitions_html: str | None) -> None: ...
```
`validate_definition_html` should raise `ValueError` with a message containing `learner-safe semantic definition` when unresolved banned patterns remain.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable Definition remediation rules per DEF-01 and DEF-02</name>
  <files>src/multilang/services/text_field_remediation.py, tests/services/test_text_field_remediation.py</files>
  <behavior>
    - Grammar-only values such as `adjective: masculine animate accusative singular` are rejected as learner-facing definitions.
    - `inflection of`, `genitive of`, `accusative of`, and similar relation-only patterns are rejected.
    - `дости́чь` and its normalized variants remediate to `verb: to achieve, to attain, to reach`.
    - When generated output is banned but source definitions include a substantive meaning, the helper formats that meaning as `[part of speech]: [meaning]`.
  </behavior>
  <action>Create `text_field_remediation.py` with deterministic helpers for Definition cleanup. Reuse the existing lexical definition filtering concepts from `lexical_grounding.py` instead of adding provider calls. Implement a known-sense correction map for `дости́чь` per DEF-02. Keep the helper pure and unit-tested; do not add interactive repair or human review workflow, which is explicitly future scope.</action>
  <verify>
    <automated>python -m pytest tests/services/test_text_field_remediation.py -q</automated>
  </verify>
  <done>Definition remediation can distinguish semantic definitions from morphology-only metadata and correct the known wrong-sense example.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply Definition remediation before persistence and block unresolved bad definitions</name>
  <files>src/multilang/services/lexical_grounding.py, src/multilang/services/assemble_export_cards.py, tests/services/test_lexical_grounding.py, tests/services/test_assemble_export_cards.py</files>
  <behavior>
    - Grounded candidates persist remediated semantic `definitions_html` instead of generated grammar-only metadata.
    - Export assembly raises before writing a card if a Definition still contains banned morphology-only patterns.
    - Existing valid `[part of speech]: [meaning]` definitions continue to export unchanged.
  </behavior>
  <action>Call `remediate_definition_html` inside `LexicalGroundingService._grounded_candidate` after the definition generator returns and before constructing `LexicalCardCandidate`. Pass `record.definitions` and `record.part_of_speech` so inflected forms can fall back to substantive source meanings. Call `validate_definition_html` from `AssembleExportCardsService._render_definitions` before escaping each definition segment, converting `ValueError` to `AssembleExportCardsError` with the item key. This implements DEF-01/DEF-02 without weakening export fail-fast behavior.</action>
  <verify>
    <automated>python -m pytest tests/services/test_text_field_remediation.py tests/services/test_lexical_grounding.py tests/services/test_assemble_export_cards.py -q</automated>
  </verify>
  <done>Bad Definition patterns are corrected when possible and blocked before export when unresolved; `дости́чь` has the required corrected sense.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lexical/provider text -> persisted candidate | Generated or cached definition content becomes persistent learner-facing data. |
| persisted candidate -> export row | Definitions are serialized into Anki-ready cards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | T | `remediate_definition_html` | mitigate | Deterministically reject morphology-only patterns and apply known correction map before persistence. |
| T-18-04 | I | `AssembleExportCardsError` | mitigate | Error messages include item keys and safe pattern names only, not raw private deck excerpts. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_text_field_remediation.py tests/services/test_lexical_grounding.py tests/services/test_assemble_export_cards.py -q`.
</verification>

<success_criteria>
DEF-01 and DEF-02 are satisfied: learner-facing Definition fields are semantic, not morphology-only, and known wrong senses are corrected or blocked before export.
</success_criteria>

<output>
After completion, create `.planning/phases/18-text-field-remediation/18-02-SUMMARY.md`.
</output>
