---
phase: 18-text-field-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/lexical_grounding.py
  - src/multilang/services/assemble_export_cards.py
  - tests/services/test_lexical_grounding.py
  - tests/services/test_assemble_export_cards.py
autonomous: true
requirements: [IPA-01]
must_haves:
  truths:
    - "User sees IPA values containing only phonetic transcription, without appended original/accented word text."
    - "User receives the study word itself as the IPA fallback when no confident pronunciation exists."
  artifacts:
    - path: "src/multilang/services/lexical_grounding.py"
      provides: "IPA fallback selection before export"
      contains: "ipa=learner_display_form"
    - path: "src/multilang/services/assemble_export_cards.py"
      provides: "IPA-only export rendering"
      contains: "_render_ipa"
  key_links:
    - from: "src/multilang/services/lexical_grounding.py"
      to: "src/multilang/services/assemble_export_cards.py"
      via: "LexicalCardCandidate.ipa"
      pattern: "ipa=.*learner_display_form|_render_ipa"
---

<objective>
Correct the IPA path so exported normal cards never append the word/spoken form to phonetic transcription.

Purpose: Phase 18 must eliminate the normalized IPA defect from `card_issues_normalized.md` §1 and satisfy IPA-01 before later export/template work.
Output: Grounded candidates have an IPA fallback when needed, and export rows render only the IPA value.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@.planning/phases/17-deck-quality-audit-and-issue-reports/17-03-SUMMARY.md
@src/multilang/services/lexical_grounding.py
@src/multilang/services/assemble_export_cards.py
@tests/services/test_lexical_grounding.py
@tests/services/test_assemble_export_cards.py

<interfaces>
Existing contracts the executor must use directly:

From `src/multilang/domain/lexicon.py`:
```python
class LexicalCardCandidate(BaseModel):
    display_form: str
    lemma: str
    definitions_html: str | None = None
    ipa: str | None = None
    spoken_form: str | None = None
```

From `src/multilang/services/assemble_export_cards.py`:
```python
def _render_ipa(self, ipa: str | None, spoken_form: str | None) -> str:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve IPA-only export rendering per IPA-01</name>
  <files>src/multilang/services/assemble_export_cards.py, tests/services/test_assemble_export_cards.py</files>
  <behavior>
    - Existing phonetic IPA `/ˈkaza/` exports exactly `/ˈkaza/`, not `/ˈkaza/ (KA-za)`.
    - IPA values already containing a parenthetical word repetition, such as `[ˈɡromkə] (гро́мко)`, export as `[ˈɡromkə]`.
    - Blank/missing IPA still fails fast; do not silently emit an empty field.
  </behavior>
  <action>Update `_render_ipa` so the learner-facing `IPA` field contains only the phonetic transcription per IPA-01 and `card_issues_normalized.md` §1. Keep `spoken_form` available for audio synthesis and provenance, but do not concatenate it into exported IPA. Strip a trailing parenthetical display-word/accent hint when the IPA already contains one; do not remove IPA alternates or bracket/slash notation. Avoid changing highlight/phonetics note contracts outside this normal export rendering path.</action>
  <verify>
    <automated>python -m pytest tests/services/test_assemble_export_cards.py -q</automated>
  </verify>
  <done>Normal export rows render IPA without any appended original/accented word text, and existing export assembly tests pass after expectation updates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use word fallback when confident IPA is unavailable per IPA-01</name>
  <files>src/multilang/services/lexical_grounding.py, tests/services/test_lexical_grounding.py</files>
  <behavior>
    - When a lexical record has no IPA and no pronunciation generator is configured, grounded candidates set `ipa` to the learner display form.
    - Provenance notes state that the word fallback was used because authoritative IPA was missing.
    - When a pronunciation generator is configured and succeeds, its IPA remains preferred over the word fallback.
  </behavior>
  <action>Change `LexicalGroundingService._grounded_candidate` so missing authoritative IPA falls back to `learner_display_form` only after the optional pronunciation generator path is unavailable. Keep `spoken_form` suitable for audio when possible, but ensure export no longer depends on `spoken_form` for IPA rendering. Record provenance notes that distinguish authoritative IPA, provider IPA, and word fallback per IPA-01.</action>
  <verify>
    <automated>python -m pytest tests/services/test_lexical_grounding.py tests/services/test_assemble_export_cards.py -q</automated>
  </verify>
  <done>Cards with no confident IPA can still export with the word itself in `IPA`, while cards with authoritative/provider IPA keep the phonetic transcription only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lexical cache/provider -> export row | Lexical pronunciation text crosses from data/provider adapters into learner-facing Anki fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | T | `_render_ipa` | mitigate | Normalize and strip only the redundant parenthetical word hint; keep missing/blank IPA fail-fast behavior. |
| T-18-02 | I | `LexicalProvenance.notes` | mitigate | Record fallback reason without private source paths or raw deck excerpts. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_lexical_grounding.py tests/services/test_assemble_export_cards.py -q`.
</verification>

<success_criteria>
IPA-01 is satisfied for normal generated-card rows: phonetic transcription is not followed by the word, and fallback uses the word only when pronunciation is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/18-text-field-remediation/18-01-SUMMARY.md`.
</output>
