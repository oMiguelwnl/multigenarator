---
phase: 18-text-field-remediation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/text_validation.py
  - tests/services/test_text_validation.py
  - tests/services/test_generate_text_items.py
autonomous: true
requirements: [TRNS-01]
must_haves:
  truths:
    - "User sees Translation values that translate the full Example Sentence."
    - "Cards whose Translation is only the isolated Word are rejected for repair/review instead of accepted."
  artifacts:
    - path: "src/multilang/services/text_validation.py"
      provides: "Translation/example mismatch gate"
      contains: "TRANSLATION_MISMATCH"
    - path: "tests/services/test_text_validation.py"
      provides: "Regression cases for isolated-word translations"
    - path: "tests/services/test_generate_text_items.py"
      provides: "Pipeline proof that failed translations route to repair/review"
  key_links:
    - from: "src/multilang/services/generate_text_items.py"
      to: "src/multilang/services/text_validation.py"
      via: "TextValidationService.validate"
      pattern: "TRANSLATION_MISMATCH"
---

<objective>
Harden deterministic validation so Translation must correspond to the Example Sentence, not the isolated study word.

Purpose: TRNS-01 requires bad translations to fail before cards become accepted export rows.
Output: Translation mismatch heuristics and pipeline tests proving bad translations trigger repair/review.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@card_issues_normalized.md
@src/multilang/services/text_validation.py
@src/multilang/services/generate_text_items.py
@tests/services/test_text_validation.py
@tests/services/test_generate_text_items.py

<interfaces>
Existing validation contract:
```python
class TextValidationService:
    def validate(
        self,
        *,
        sentence: GeneratedSentence,
        translation: GeneratedTranslation,
        display_form: str,
        lemma: str,
        definitions_html: str | None,
        require_translation: bool = True,
        ...
    ) -> TextValidationResult: ...
```
Use `ValidationFlagCode.TRANSLATION_MISMATCH` for all new translation/example mismatch failures.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject isolated-word translations per TRNS-01</name>
  <files>src/multilang/services/text_validation.py, tests/services/test_text_validation.py</files>
  <behavior>
    - If `display_form="дости́чь"`, `lemma="достичь"`, sentence is multi-token, and translation is only `to achieve`, validation fails with `TRANSLATION_MISMATCH`.
    - If translation is a short isolated lemma/display-form equivalent while the sentence has multiple content words, validation fails.
    - Full sentence translations such as `He wants to achieve the goal tomorrow.` continue to pass when other checks pass.
  </behavior>
  <action>Extend `TextValidationService._check_translation` with deterministic heuristics that compare translation length/content against the sentence and target term context. Flag translations that are empty, copied from definition, copied from source sentence, or look like an isolated word/gloss rather than a sentence translation. Keep the rule conservative to avoid rejecting valid short sentence translations: only apply the isolated-word/gloss check when the source sentence has multiple tokens and the translation is very short or equals normalized display/lemma/primary definition gloss.</action>
  <verify>
    <automated>python -m pytest tests/services/test_text_validation.py -q</automated>
  </verify>
  <done>Translation validation catches the normalized issue where Translation translates only Word, while valid sentence translations still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove bad translations route through repair/review instead of acceptance</name>
  <files>tests/services/test_generate_text_items.py</files>
  <behavior>
    - A generated bundle with isolated-word Translation fails validation and triggers the existing repair chain.
    - If repair still fails, the persisted `TextQualityRecord` has `review_status=REVIEW_REQUIRED` and `review_reason=translation_mismatch`.
    - If repair returns a full sentence translation, the item is accepted.
  </behavior>
  <action>Add focused service tests around `GenerateTextItemsService` using existing fake repositories/adapters in `tests/services/test_generate_text_items.py`. Do not add provider calls. Assert the existing repair/review machinery consumes the new `TRANSLATION_MISMATCH` flag, satisfying TRNS-01 at the pre-export acceptance gate.</action>
  <verify>
    <automated>python -m pytest tests/services/test_text_validation.py tests/services/test_generate_text_items.py -q</automated>
  </verify>
  <done>Pipeline tests prove isolated-word translations cannot become accepted card text without successful repair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translation provider/local adapter -> validation | Provider text becomes candidate learner-facing Translation content. |
| validation -> persistence | Validation status controls whether text can become accepted export data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | T | `TextValidationService._check_translation` | mitigate | Deterministically flag isolated-word/gloss translations using existing `TRANSLATION_MISMATCH`. |
| T-18-06 | R | `GenerateTextItemsService` tests | mitigate | Persist review reason `translation_mismatch` so failed translation acceptance is auditable. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_text_validation.py tests/services/test_generate_text_items.py -q`.
</verification>

<success_criteria>
TRNS-01 is satisfied: accepted generated text records only contain Translation values that correspond to the Example Sentence, or they are sent to repair/review.
</success_criteria>

<output>
After completion, create `.planning/phases/18-text-field-remediation/18-03-SUMMARY.md`.
</output>
