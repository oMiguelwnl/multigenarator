---
phase: 07-milestone-evidence-audit-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
autonomous: true
requirements: [JOB-01, JOB-02, JOB-03]
must_haves:
  truths:
    - "Phase 1 verification status reflects current shipped CLI lifecycle behavior instead of the stale 2026-04-19 blocker."
    - "JOB-01, JOB-02, and JOB-03 have current verification evidence from shipped-path tests."
  artifacts:
    - path: ".planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md"
      provides: "Current re-verification report for Phase 1 lifecycle requirements"
      contains: "status: verified"
  key_links:
    - from: ".planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md"
      to: "tests/integration/test_job_flow.py"
      via: "Behavioral spot-check command"
      pattern: "test_job_flow.py"
---

<objective>
Refresh the stale Phase 1 verification artifact so it reflects the current shipped CLI/runtime evidence for resume, progress, and duplicate-safe reruns.

Purpose: The v1.0 audit currently blocks JOB-01, JOB-02, and JOB-03 because `01-VERIFICATION.md` still reports old gaps that Plans 01-05 and 01-06 closed.
Output: `.planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md` updated as a current re-verification report with passing evidence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-milestone-evidence-audit-hygiene/07-RESEARCH.md
@.planning/phases/07-milestone-evidence-audit-hygiene/07-VALIDATION.md
@.planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
@.planning/phases/01-job-orchestration-recovery/01-05-SUMMARY.md
@.planning/phases/01-job-orchestration-recovery/01-06-SUMMARY.md
@tests/integration/test_job_flow.py
@tests/cli/test_generate_command.py

<interfaces>
Existing verification evidence to preserve and supersede:

From `01-06-SUMMARY.md` frontmatter:
```yaml
provides:
  - Operator-visible lifecycle summary output on the shipped CLI path
  - Safe resume aborts with persisted-state diagnostics
  - Shipped-app integration coverage for fresh runs, resume, duplicate skips, and explicit overwrites
requirements-completed: [JOB-01, JOB-02, JOB-03]
```

From `tests/integration/test_job_flow.py`:
```python
def test_shipped_app_supports_resume_and_duplicate_safe_rerun(monkeypatch, tmp_path: Path) -> None:
    assert "resumed_from_job=" in resume_result.output
    assert "skipped_duplicates=2" in rerun_result.output
    assert "overwritten_items=2" in overwrite_result.output
```

From `tests/cli/test_generate_command.py`:
```python
assert "stage=ingest" in first_result.output
assert "completed_items=2" in first_result.output
assert "persisted resume state is inconsistent" in result.output
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run current shipped lifecycle evidence</name>
  <files>.planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
    - .planning/phases/01-job-orchestration-recovery/01-05-SUMMARY.md
    - .planning/phases/01-job-orchestration-recovery/01-06-SUMMARY.md
    - tests/integration/test_job_flow.py
    - tests/cli/test_generate_command.py
  </read_first>
  <action>Run the focused Phase 1 lifecycle command `uv run pytest tests/integration/test_job_flow.py tests/cli/test_generate_command.py -q`. Capture the pass count and any runtime duration from pytest output. Do not edit product code in this task; if the command fails, stop and report the failing test names because Phase 7 must not mark stale evidence as verified without current behavior.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_job_flow.py tests/cli/test_generate_command.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Command exits 0.
    - Pytest output contains `passed`.
    - No product source files under `src/multilang/` are modified by this task.
  </acceptance_criteria>
  <done>The current shipped CLI lifecycle test evidence is known before the verification artifact is changed.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale Phase 1 blocker with current re-verification report</name>
  <files>.planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md
    - .planning/phases/01-job-orchestration-recovery/01-05-SUMMARY.md
    - .planning/phases/01-job-orchestration-recovery/01-06-SUMMARY.md
    - .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <action>Rewrite `01-VERIFICATION.md` as a current Phase 1 re-verification report. Set frontmatter `status: verified`, update `verified:` to the execution date, set `score: 7/7 must-haves verified`, and add a `re_verification:` block with `previous_status: gaps_found`, `previous_score: 3/7`, and `gaps_closed` entries for visible progress, resume, duplicate-safe rerun, and failed-item lifecycle summary. In the body, keep the Phase 1 goal and truth table, but mark JOB-01, JOB-02, and JOB-03 evidence as satisfied using exact current evidence from `01-05-SUMMARY.md`, `01-06-SUMMARY.md`, `tests/integration/test_job_flow.py`, and `tests/cli/test_generate_command.py`. Explicitly state that the old shipped-app no-op blocker is superseded by `src/multilang/runtime.py` bootstrap and the default `create_app()` shipped-path tests.</action>
  <verify>
    <automated>python -c "from pathlib import Path; text=Path('.planning/phases/01-job-orchestration-recovery/01-VERIFICATION.md').read_text(); assert 'status: verified' in text; assert 'score: 7/7 must-haves verified' in text; [(_ for _ in ()).throw(AssertionError(n)) for n in ['JOB-01','JOB-02','JOB-03','resumed_from_job=','skipped_duplicates=2','overwritten_items=2','stage=ingest'] if n not in text]; assert 'status: gaps_found' not in text.split('---',2)[1]"</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter contains `status: verified`.
    - Frontmatter contains `score: 7/7 must-haves verified`.
    - Body contains `JOB-01`, `JOB-02`, and `JOB-03` rows marked satisfied/verified.
    - Body contains exact evidence strings `resumed_from_job=`, `skipped_duplicates=2`, `overwritten_items=2`, and `stage=ingest`.
  </acceptance_criteria>
  <done>Phase 1 verification no longer blocks JOB-01, JOB-02, or JOB-03 with stale shipped-path findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test output→verification artifact | Current automated behavior is converted into milestone-closing evidence. |
| stale audit→updated verification | Old blocker data is superseded without losing traceability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `01-VERIFICATION.md` | mitigate | Require `uv run pytest tests/integration/test_job_flow.py tests/cli/test_generate_command.py -q` to pass before changing status. |
| T-07-01-02 | Repudiation | Phase 1 audit evidence | mitigate | Include exact test names and output strings in the report so future audits can trace why JOB-01 through JOB-03 closed. |
| T-07-01-03 | Information Disclosure | Verification docs | accept | No secrets or user data are introduced; report references test paths and counters only. |
</threat_model>

<verification>
Run `uv run pytest tests/integration/test_job_flow.py tests/cli/test_generate_command.py -q`, then run the Python metadata assertion from Task 2.
</verification>

<success_criteria>
`01-VERIFICATION.md` is a current verified report, and the v1.0 audit can no longer cite stale Phase 1 verification status as the reason JOB-01, JOB-02, or JOB-03 are unsatisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-milestone-evidence-audit-hygiene/07-01-SUMMARY.md`.
</output>
