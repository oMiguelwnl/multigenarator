---
phase: 08-card-quality-refresh
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CARD_TEMPLATE.md
  - tests/services/test_export_anki_package.py
autonomous: true
requirements:
  - QUAL-THEME-01
must_haves:
  truths:
    - "Normal generated decks use the CSS supplied in total.md."
    - "The phonetics deck template is not changed by this styling update."
  artifacts:
    - path: "CARD_TEMPLATE.md"
      provides: "Normal Anki deck template CSS"
      contains: "--color-nightMode-card-background: #0a1628;"
    - path: "tests/services/test_export_anki_package.py"
      provides: "Regression tests for normal deck CSS"
      contains: "space-between"
  key_links:
    - from: "src/multilang/services/export_anki_package.py"
      to: "CARD_TEMPLATE.md"
      via: "_load_project_card_template"
      pattern: "CARD_TEMPLATE.md"
---

<objective>
Apply the user-provided normal deck CSS per D-04.

Purpose: Refresh generated non-phonetics Anki card styling while preserving the dedicated phonetics deck template.
Output: Updated `CARD_TEMPLATE.md` styling block and package tests proving the normal model consumes it.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-card-quality-refresh/08-CONTEXT.md
@total.md
@CARD_TEMPLATE.md
@src/multilang/services/export_anki_package.py
@src/multilang/services/russian_phoneme_deck.py
@tests/services/test_export_anki_package.py

<interfaces>
`export_anki_package.py` reads `CARD_TEMPLATE.md` via `_load_project_card_template()` and applies the `## Styling (CSS)` fenced block to `genanki.Model.css`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock normal-deck CSS expectations</name>
  <files>tests/services/test_export_anki_package.py</files>
  <read_first>tests/services/test_export_anki_package.py, CARD_TEMPLATE.md, total.md, src/multilang/services/russian_phoneme_deck.py</read_first>
  <behavior>
    - `build_multilang_model().css` contains `--max-width-card: 400px;`, `--color-nightMode-card-background: #0a1628;`, `.targetWordContainer`, `justify-content: space-between;`, `.wordAudioButtonBack`, and `margin-left: 8px;` per D-04.
    - Test reads `src/multilang/services/russian_phoneme_deck.py` and asserts it does not contain `--max-width-card: 400px;` so D-04 does not alter the phonetics deck.
  </behavior>
  <action>Update package/template tests to check the CSS values from `total.md` that differ from the current template: `.targetWordContainer` must use `justify-content: space-between;`, `.wordAudioButtonBack` must use `margin-left: 8px;`, and the normal model CSS must include `--color-nightMode-card-background: #0a1628;`. Include an explicit guard that `src/multilang/services/russian_phoneme_deck.py` is not updated with this CSS per D-04.</action>
  <verify><automated>uv run pytest tests/services/test_export_anki_package.py -x</automated></verify>
  <acceptance_criteria>`tests/services/test_export_anki_package.py` contains `space-between`, `margin-left: 8px`, and `russian_phoneme_deck.py`; pytest fails before Task 2 and passes after Task 2.</acceptance_criteria>
  <done>Tests lock the user-provided normal-deck styling and phonetics exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Replace normal deck CSS from total.md</name>
  <files>CARD_TEMPLATE.md</files>
  <read_first>CARD_TEMPLATE.md, total.md</read_first>
  <action>In `CARD_TEMPLATE.md`, replace only the contents of the `## Styling (CSS)` fenced `css` block with the CSS supplied in `total.md` lines 10-473 per D-04. Do not edit the Front Template, Back Template, Notes section, or `src/multilang/services/russian_phoneme_deck.py`. Preserve the user-provided CSS values exactly, including `.targetWordContainer { justify-content: space-between; }`, `.wordAudioButtonBack { margin-left: 8px; }`, `.ipa { color: var(--color-divader); }`, and the Chrome Base Styles comments.</action>
  <verify><automated>uv run pytest tests/services/test_export_anki_package.py -x</automated></verify>
  <acceptance_criteria>`CARD_TEMPLATE.md` contains `/* Chrome Base Styles */`, `justify-content: space-between;`, `margin-left: 8px;`, and `var(--color-divader)`; `src/multilang/services/russian_phoneme_deck.py` does not contain `--max-width-card: 400px;`; pytest exits 0.</acceptance_criteria>
  <done>Normal deck CSS matches the user-supplied CSS and the phonetics deck remains untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template CSS -> Anki renderer | Project CSS is rendered by Anki clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | `CARD_TEMPLATE.md` | mitigate | Change only the CSS fenced block and test representative required CSS strings. |
| T-08-04-02 | Information Disclosure | Anki template | accept | CSS contains no dynamic data, scripts, or secrets. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_export_anki_package.py -x`.
</verification>

<success_criteria>
Normal deck exports consume the exact supplied CSS; phonetics deck code is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/08-card-quality-refresh/08-04-SUMMARY.md`.
</output>
