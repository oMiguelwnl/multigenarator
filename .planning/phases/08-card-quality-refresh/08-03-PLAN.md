---
phase: 08-card-quality-refresh
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - src/multilang/db/models.py
  - src/multilang/repositories/lexical_repository.py
  - src/multilang/services/assemble_export_cards.py
  - alembic/versions/20260502_08_spoken_form.py
  - tests/repositories/test_lexical_repository.py
  - tests/services/test_assemble_export_cards.py
autonomous: true
requirements:
  - QUAL-PRON-01
  - QUAL-PRON-02
must_haves:
  truths:
    - "AI-generated spoken form survives database persistence."
    - "Every exported generated card renders IPA as `/ipa/ (spoken-form)`."
  artifacts:
    - path: "alembic/versions/20260502_08_spoken_form.py"
      provides: "Database migration for lexical spoken forms"
      contains: "spoken_form"
    - path: "src/multilang/services/assemble_export_cards.py"
      provides: "Export rendering for IPA plus spoken form"
      contains: "spoken_form"
  key_links:
    - from: "LexicalRepository._candidate_payload"
      to: "LexicalCandidate.spoken_form"
      via: "candidate.spoken_form"
      pattern: "spoken_form"
    - from: "AssembleExportCardsService.execute"
      to: "ExportCardRow.ipa"
      via: "_render_ipa(lexical_candidate.ipa, lexical_candidate.spoken_form)"
      pattern: "\(spoken"
---

<objective>
Persist and export the AI-generated spoken form beside IPA per D-02 and D-03.

Purpose: Ensure pronunciation generated during grounding reaches every final Anki card instead of being lost before export.
Output: Migration, repository mapping, and export rendering tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-card-quality-refresh/08-CONTEXT.md
@src/multilang/db/models.py
@src/multilang/repositories/lexical_repository.py
@src/multilang/services/assemble_export_cards.py
@tests/repositories/test_lexical_repository.py
@tests/services/test_assemble_export_cards.py

<interfaces>
Created by Plan 08-02:
```python
class LexicalCardCandidate(BaseModel):
    ipa: str | None = None
    spoken_form: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist spoken_form through lexical repository</name>
  <files>src/multilang/db/models.py, src/multilang/repositories/lexical_repository.py, alembic/versions/20260502_08_spoken_form.py, tests/repositories/test_lexical_repository.py</files>
  <read_first>src/multilang/db/models.py, src/multilang/repositories/lexical_repository.py, tests/repositories/test_lexical_repository.py, alembic/versions/20260419_02_lexical_grounding_tables.py</read_first>
  <behavior>
    - Repository upsert stores `spoken_form="RYT"` on `lexical_candidates.spoken_form`.
    - Repository `list_candidates()` returns `LexicalCardCandidate.spoken_form == "RYT"`.
    - Migration adds nullable string column `spoken_form` to `lexical_candidates` and downgrade removes it.
  </behavior>
  <action>Add `spoken_form: Mapped[str | None] = mapped_column(String(255), nullable=True)` immediately after `ipa` in `LexicalCandidate`. Add Alembic migration `20260502_08_spoken_form.py` with `revision = "20260502_08"`, `down_revision = "20260426_05"`, `op.add_column("lexical_candidates", sa.Column("spoken_form", sa.String(length=255), nullable=True))`, and matching `op.drop_column`. Update `_candidate_payload` and `_to_domain` to round-trip `candidate.spoken_form`.</action>
  <verify><automated>uv run pytest tests/repositories/test_lexical_repository.py -x</automated></verify>
  <acceptance_criteria>`src/multilang/db/models.py` contains `spoken_form`; migration contains `op.add_column("lexical_candidates"`; repository test asserts `stored[0].spoken_form == "RYT"`; pytest exits 0.</acceptance_criteria>
  <done>Spoken form is persisted and restored with lexical candidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export IPA with spoken form for all generated cards</name>
  <files>src/multilang/services/assemble_export_cards.py, tests/services/test_assemble_export_cards.py</files>
  <read_first>src/multilang/services/assemble_export_cards.py, tests/services/test_assemble_export_cards.py, src/multilang/domain/exporting.py</read_first>
  <behavior>
    - Candidate with `ipa="/ˈkaza/"` and `spoken_form="KA-za"` exports `row.ipa == "/ˈkaza/ (KA-za)"`.
    - Candidate with IPA but missing `spoken_form` raises `AssembleExportCardsError` so D-02 cannot silently regress.
    - Candidate with missing IPA raises `AssembleExportCardsError` so D-03 cannot silently regress.
  </behavior>
  <action>Change `_render_ipa` signature to accept both `ipa: str | None` and `spoken_form: str | None`. Remove the inferred `_readable_pronunciation_hint` fallback from the export path. If either value is missing/blank, raise `AssembleExportCardsError` with messages containing `missing IPA` or `missing spoken form`. Return `f"{escaped_ipa} ({escape(cleaned_spoken_form)})"` for valid candidates per D-02.</action>
  <verify><automated>uv run pytest tests/services/test_assemble_export_cards.py tests/repositories/test_lexical_repository.py -x</automated></verify>
  <acceptance_criteria>`src/multilang/services/assemble_export_cards.py` contains `missing spoken form`; test file contains `/ˈkaza/ (KA-za)`; pytest exits 0.</acceptance_criteria>
  <done>Final export cards always show `/ipa/ (spoken-form)` and fail before export when pronunciation data is incomplete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted lexical candidate -> export HTML | Stored IPA/spoken form becomes Anki field content. |
| migration -> database | Schema changes affect persisted lexical rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | `AssembleExportCardsService._render_ipa` | mitigate | Escape both IPA and spoken form before writing the Anki field. |
| T-08-03-02 | Repudiation | migration | mitigate | Add deterministic Alembic revision and repository tests proving round-trip behavior. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_assemble_export_cards.py tests/repositories/test_lexical_repository.py -x`.
</verification>

<success_criteria>
Generated cards cannot export without AI IPA and spoken form, and successful exports render the exact side-by-side pronunciation format.
</success_criteria>

<output>
After completion, create `.planning/phases/08-card-quality-refresh/08-03-SUMMARY.md`.
</output>
