---
phase: 08-card-quality-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/audio_synthesis.py
  - src/multilang/services/azure_speech_adapter.py
  - tests/services/test_audio_synthesis.py
  - tests/services/test_azure_speech_adapter.py
autonomous: true
requirements:
  - QUAL-AUDIO-01
must_haves:
  truths:
    - "Generated word audio is more prominent than sentence audio through Azure SSML."
    - "Sentence audio remains natural and is not wrapped with word-prominence prosody."
  artifacts:
    - path: "src/multilang/services/audio_synthesis.py"
      provides: "Word-only prominent SSML generation"
      contains: "AudioAssetKind.WORD"
    - path: "src/multilang/services/azure_speech_adapter.py"
      provides: "Azure SSML wrapper that preserves prosody markup"
      contains: "prosody"
  key_links:
    - from: "AudioSynthesisService._normalize_input"
      to: "AzureSpeechAdapter.synthesize"
      via: "prepared_asset.normalized_input.ssml_text"
      pattern: "prosody.*rate=\"-10%\""
---

<objective>
Add Azure Speech word-audio prominence per D-01.

Purpose: Make headword audio sound more highlighted while preserving existing sentence-audio behavior.
Output: Word assets carry prominent SSML; Azure adapter preserves the SSML instead of flattening it to text.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-card-quality-refresh/08-CONTEXT.md
@.planning/phases/08-card-quality-refresh/08-RESEARCH.md
@src/multilang/services/audio_synthesis.py
@src/multilang/services/azure_speech_adapter.py
@tests/services/test_audio_synthesis.py
@tests/services/test_azure_speech_adapter.py

<interfaces>
Existing contracts:
```python
class AudioAssetKind(str, Enum):
    WORD = "word"
    SENTENCE = "sentence"

class AudioSynthesisAdapter(Protocol):
    def synthesize(*, ssml_text: str, voice_id: str, locale: str, output_path: Path, audio_format: str) -> AudioSynthesisResponse: ...

def build_azure_ssml(*, text: str, locale: str, voice_id: str) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test word-only SSML prominence</name>
  <files>tests/services/test_audio_synthesis.py, tests/services/test_azure_speech_adapter.py</files>
  <read_first>tests/services/test_audio_synthesis.py, tests/services/test_azure_speech_adapter.py, src/multilang/services/audio_synthesis.py, src/multilang/services/azure_speech_adapter.py</read_first>
  <behavior>
    - Word asset `normalized_input.ssml_text` contains `<prosody rate="-10%" pitch="+8%" volume="+20%">` per D-01.
    - Sentence asset `normalized_input.ssml_text` does not contain `<prosody`.
    - `build_azure_ssml(text='<speak version="1.0"><prosody rate="-10%" pitch="+8%" volume="+20%">run</prosody></speak>', ...)` preserves the `<prosody ...>` element inside `<voice>`.
  </behavior>
  <action>Add failing tests for D-01. Use exact prosody values `rate="-10%"`, `pitch="+8%"`, and `volume="+20%"`. Do not apply the prosody assertion to sentence audio.</action>
  <verify><automated>uv run pytest tests/services/test_audio_synthesis.py tests/services/test_azure_speech_adapter.py -x</automated></verify>
  <acceptance_criteria>`tests/services/test_audio_synthesis.py` contains `rate="-10%"`; `tests/services/test_azure_speech_adapter.py` contains `&lt;prosody` or `<prosody`; pytest command fails before implementation and passes after Task 2.</acceptance_criteria>
  <done>Tests encode D-01 and distinguish word audio from sentence audio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Azure word prominence and SSML preservation</name>
  <files>src/multilang/services/audio_synthesis.py, src/multilang/services/azure_speech_adapter.py</files>
  <read_first>src/multilang/services/audio_synthesis.py, src/multilang/services/azure_speech_adapter.py, tests/services/test_audio_synthesis.py, tests/services/test_azure_speech_adapter.py</read_first>
  <action>In `AudioSynthesisService`, change `_normalize_input` or its call site so `AudioAssetKind.WORD` uses `ssml_text='<speak version="1.0"><prosody rate="-10%" pitch="+8%" volume="+20%">{escaped normalized word}</prosody></speak>'` per D-01, while `AudioAssetKind.SENTENCE` keeps the current plain `<speak version="1.0">...</speak>` shape. In `azure_speech_adapter.py`, update `build_azure_ssml` so legacy `<speak>` input can preserve safe child SSML elements (`prosody`) inside `<voice name="...">` instead of converting all children to plain text. Continue escaping plain-text inputs and malformed XML exactly as before.</action>
  <verify><automated>uv run pytest tests/services/test_audio_synthesis.py tests/services/test_azure_speech_adapter.py -x</automated></verify>
  <acceptance_criteria>`src/multilang/services/audio_synthesis.py` contains `AudioAssetKind.WORD` and `volume="+20%"`; `src/multilang/services/azure_speech_adapter.py` contains `prosody`; pytest command exits 0.</acceptance_criteria>
  <done>Word audio requests include Azure prosody prominence and adapter output preserves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lexical text -> SSML | User/input-derived words become XML sent to Azure Speech. |
| app -> Azure Speech SDK | SSML leaves local process for external TTS generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | `build_azure_ssml` | mitigate | Escape plain text and preserve only parsed SSML children generated internally by `AudioSynthesisService`. |
| T-08-01-02 | Denial of Service | Azure synthesis | accept | Existing media validation and exception-to-failed-asset behavior remains in `synthesize_prepared_asset`. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_audio_synthesis.py tests/services/test_azure_speech_adapter.py -x`.
</verification>

<success_criteria>
`word_audio` assets include the exact Azure prosody settings from this plan; `sentence_audio` assets do not; existing Azure adapter tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-card-quality-refresh/08-01-SUMMARY.md`.
</output>
