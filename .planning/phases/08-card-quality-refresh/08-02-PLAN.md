---
phase: 08-card-quality-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/domain/lexicon.py
  - src/multilang/services/provider_pronunciation_adapters.py
  - src/multilang/services/lexical_grounding.py
  - tests/services/test_provider_pronunciation_adapters.py
  - tests/services/test_lexical_grounding.py
autonomous: true
requirements:
  - QUAL-PRON-01
  - QUAL-PRON-02
must_haves:
  truths:
    - "Every newly grounded card candidate receives AI-generated IPA."
    - "Every newly grounded card candidate receives a readable spoken form."
    - "Kaikki IPA is not used as the final card IPA."
  artifacts:
    - path: "src/multilang/services/provider_pronunciation_adapters.py"
      provides: "LiteLLM pronunciation generation adapter"
      exports: ["LiteLLMPronunciationAdapter", "PronunciationGenerationRequest", "PronunciationGenerationResult"]
    - path: "src/multilang/domain/lexicon.py"
      provides: "Candidate spoken_form contract"
      contains: "spoken_form"
  key_links:
    - from: "LexicalGroundingService"
      to: "LiteLLMPronunciationAdapter"
      via: "pronunciation_generator.generate_pronunciation"
      pattern: "generate_pronunciation"
---

<objective>
Add AI pronunciation generation for all generated card candidates per D-02 and D-03.

Purpose: Stop exporting Kaikki IPA as final truth and create explicit spoken-form data for every generated card.
Output: Pronunciation adapter, candidate contract, and lexical grounding integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-card-quality-refresh/08-CONTEXT.md
@src/multilang/domain/lexicon.py
@src/multilang/services/provider_text_adapters.py
@src/multilang/services/lexical_grounding.py
@tests/services/test_lexical_grounding.py

<interfaces>
Follow existing provider adapter style from `provider_text_adapters.py`:
```python
class LiteLLMSentenceAdapter:
    def generate_sentence(self, request: SentenceGenerationRequest) -> SentenceGenerationResult: ...
```
Required new result shape:
```python
class PronunciationGenerationResult(BaseModel):
    ipa: str
    spoken_form: str
    uncertainty_notes: list[str] = []
    provenance: dict[str, object]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pronunciation adapter contract and tests</name>
  <files>src/multilang/services/provider_pronunciation_adapters.py, tests/services/test_provider_pronunciation_adapters.py</files>
  <read_first>src/multilang/services/provider_text_adapters.py, tests/services/test_provider_text_adapters.py</read_first>
  <behavior>
    - Given a fake LiteLLM JSON response `{"ipa":"/ˈkaza/","spoken_form":"KA-za","uncertainty_notes":[]}`, adapter returns those exact values.
    - Empty `ipa` or empty `spoken_form` raises `ValueError`.
    - Prompt includes target language, display form, lemma, definitions, and the rule to return JSON keys `ipa`, `spoken_form`, `uncertainty_notes`.
  </behavior>
  <action>Create `PronunciationGenerationRequest`, `PronunciationGenerationResult`, and `LiteLLMPronunciationAdapter`. Use the same completion parsing helpers/patterns as `LiteLLMSentenceAdapter`: `temperature=0.1`, `response_format={"type":"json_object"}`, provider provenance `source="provider-pronunciation-generator"`, `provider="litellm"`, `model=settings.text_generation_model` after existing model resolution. The system prompt must say: `You generate pronunciation data for an Anki vocabulary card. Return only JSON with keys: ipa, spoken_form, uncertainty_notes.`</action>
  <verify><automated>uv run pytest tests/services/test_provider_pronunciation_adapters.py -x</automated></verify>
  <acceptance_criteria>`src/multilang/services/provider_pronunciation_adapters.py` contains `PronunciationGenerationRequest`, `PronunciationGenerationResult`, and `LiteLLMPronunciationAdapter`; test command exits 0.</acceptance_criteria>
  <done>AI pronunciation adapter is deterministic under fake completion tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace Kaikki IPA in lexical grounding</name>
  <files>src/multilang/domain/lexicon.py, src/multilang/services/lexical_grounding.py, tests/services/test_lexical_grounding.py</files>
  <read_first>src/multilang/domain/lexicon.py, src/multilang/services/lexical_grounding.py, tests/services/test_lexical_grounding.py, src/multilang/services/provider_pronunciation_adapters.py</read_first>
  <behavior>
    - If Kaikki record has `ipa="/wrong/"` and fake AI returns `ipa="/right/", spoken_form="RYT"`, grounded candidate has `ipa == "/right/"` and `spoken_form == "RYT"` per D-02/D-03.
    - Grounded frequency candidates and grounded custom word-list candidates both call the pronunciation generator.
    - Provenance pronunciation source is `provider-pronunciation-generator`, not `kaikki`, when AI pronunciation succeeds.
  </behavior>
  <action>Add `spoken_form: str | None = None` to `LexicalCardCandidate`. Update `LexicalGroundingService.__init__` to accept `pronunciation_generator: object | None = None`. In `_grounded_candidate`, call `pronunciation_generator.generate_pronunciation(...)` when provided; set `ipa` and `spoken_form` from its result for every grounded candidate. Do not use `record.ipa` as final output when the generator is present per D-03. If no generator is provided, keep existing behavior so current tests can construct the service without external credentials.</action>
  <verify><automated>uv run pytest tests/services/test_lexical_grounding.py tests/services/test_provider_pronunciation_adapters.py -x</automated></verify>
  <acceptance_criteria>`src/multilang/domain/lexicon.py` contains `spoken_form`; `src/multilang/services/lexical_grounding.py` contains `provider-pronunciation-generator`; pytest command exits 0.</acceptance_criteria>
  <done>Lexical grounding can produce AI IPA and spoken form for generated cards without trusting Kaikki IPA as final output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lexical data -> LLM | Grounded word data is sent to configured text-generation provider. |
| LLM response -> persisted card data | Provider JSON becomes candidate IPA/spoken form. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | `LiteLLMPronunciationAdapter` | mitigate | Require JSON object, reject empty `ipa` and `spoken_form`, preserve provenance. |
| T-08-02-02 | Information Disclosure | pronunciation prompt | accept | Prompt sends only lexical card data already used for generation; no secrets or user credentials. |
</threat_model>

<verification>
Run `uv run pytest tests/services/test_lexical_grounding.py tests/services/test_provider_pronunciation_adapters.py -x`.
</verification>

<success_criteria>
AI pronunciation can override wrong Kaikki IPA and supplies explicit `spoken_form` for generated grounded candidates.
</success_criteria>

<output>
After completion, create `.planning/phases/08-card-quality-refresh/08-02-SUMMARY.md`.
</output>
