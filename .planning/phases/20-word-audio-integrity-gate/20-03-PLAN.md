---
phase: 20-word-audio-integrity-gate
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - src/multilang/services/assemble_export_cards.py
  - src/multilang/runtime.py
  - tests/services/test_assemble_export_cards.py
  - tests/integration/test_export_job_flow.py
autonomous: true
requirements: [AUD-01, AUD-02]
must_haves:
  truths:
    - "User receives a clear validation block when `word_audio` cannot be repaired before export."
    - "User only receives APKG, CSV, or TSV artifacts when each normal card's `word_audio` metadata exactly matches its card `Word`."
    - "User keeps highlight exports unaffected because highlight cards do not export `word_audio`."
  artifacts:
    - path: "src/multilang/services/assemble_export_cards.py"
      provides: "Assembly-time word-audio mismatch block before card snapshots are persisted"
    - path: "src/multilang/runtime.py"
      provides: "Export-time word-audio mismatch block for already-persisted card snapshots"
    - path: "tests/services/test_assemble_export_cards.py"
      provides: "Focused assembly validation coverage"
    - path: "tests/integration/test_export_job_flow.py"
      provides: "Runtime APKG/CSV/TSV validation-block evidence"
  key_links:
    - from: "src/multilang/services/assemble_export_cards.py"
      to: "src/multilang/services/audio_integrity.py"
      via: "assert word asset matches lexical candidate lemma before `ExportCardRow` creation"
      pattern: "assert_word_audio_matches_word"
    - from: "src/multilang/runtime.py"
      to: "src/multilang/services/audio_integrity.py"
      via: "assert persisted word asset matches existing row.word before media index construction"
      pattern: "assert_word_audio_matches_word"
---

<objective>
Block exports when a remaining word-audio mismatch cannot be repaired by generation-time reuse validation.

Purpose: AUD-02 requires cards to be exported only when `word_audio` matches `Word`, with clear validation errors when not fixable at export time.
Output: Assembly and runtime export gates with focused service tests and end-to-end CLI export failure evidence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-word-audio-integrity-gate/20-01-SUMMARY.md
@.planning/phases/20-word-audio-integrity-gate/20-02-SUMMARY.md
@.planning/phases/19-normal-card-export-and-responsive-template/19-01-SUMMARY.md
@src/multilang/services/audio_integrity.py
@src/multilang/services/assemble_export_cards.py
@src/multilang/runtime.py
@tests/services/test_assemble_export_cards.py
@tests/integration/test_export_job_flow.py

<interfaces>
Use these existing contracts directly:
```python
class AssembleExportCardsService:
    def execute(self, *, job_id: str, deck_language: SupportedLanguage) -> AssembleExportCardsResult: ...
    def _require_audio(..., asset_kind: AudioAssetKind, ...) -> AudioAssetRecord: ...

class RuntimeGenerateService:
    def export_job(self, *, job_id: str, export_format: ExportArtifactFormat, output_dir: Path, deck_name: str | None = None) -> RuntimeExportResult: ...
    def _build_media_index(self, rows: list[object]) -> dict[str, Path]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Block mismatched word audio during export-card assembly per AUD-01/AUD-02</name>
  <files>tests/services/test_assemble_export_cards.py, src/multilang/services/assemble_export_cards.py</files>
  <behavior>
    - A normal/frequency row whose lexical candidate `lemma` is `run` and WORD audio manifest says `jump` raises `AssembleExportCardsError` before any card snapshot is persisted.
    - The error message includes `word_audio`, `Word`, and the item key.
    - Highlight rows remain unaffected because their source profile field tuple does not include `word_audio`.
  </behavior>
  <action>Add focused tests to `tests/services/test_assemble_export_cards.py` using existing `make_candidate`, `make_asset`, and fake repositories. Then update `src/multilang/services/assemble_export_cards.py` so after `_require_audio(... WORD ...)` returns an asset, the service calls `assert_word_audio_matches_word(word_audio, lexical_candidate.lemma, item_key=text_record.item_key)` before creating `ExportCardRow`. Wrap `AudioIntegrityError` as `AssembleExportCardsError` to keep the existing service error boundary. Do not compare against `display_form`; the exported normal `Word` comes from `lexical_candidate.lemma`.</action>
  <verify>
    <automated>python -m pytest tests/services/test_assemble_export_cards.py tests/services/test_audio_integrity.py -q</automated>
  </verify>
  <done>Assembly blocks unrepairable word-audio mismatches before snapshots can enter export repositories, while highlight behavior remains isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Block mismatched persisted snapshots during APKG/CSV/TSV export per AUD-02</name>
  <files>tests/integration/test_export_job_flow.py, src/multilang/runtime.py</files>
  <behavior>
    - If card snapshots already exist and the stored WORD audio asset later says another word, `export --format apkg` exits non-zero with a `word_audio`/`Word` mismatch diagnostic.
    - The same runtime gate applies before CSV and TSV export writes artifacts.
    - Missing media-file checks still behave as before for assets whose text metadata matches.
  </behavior>
  <action>Add a parametrized integration test in `tests/integration/test_export_job_flow.py` for `apkg`, `csv`, and `tsv`: generate a one-word job, force card snapshot creation with an initial export or by invoking runtime export once, corrupt the persisted WORD audio row's `display_text`/`tts_text`/`text_hash` to another word while leaving media files present, then assert the next export exits with code 1 and includes `word_audio` and `Word`. Update `src/multilang/runtime.py` so `_build_media_index` validates the retrieved WORD asset against `row.word` before `_validate_media_reference`; use `html.unescape(row.word)` if needed because row values are escaped for export. Keep sentence-audio media basename validation unchanged.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_export_job_flow.py tests/services/test_assemble_export_cards.py tests/services/test_generate_audio_items.py tests/services/test_audio_integrity.py -q</automated>
  </verify>
  <done>Runtime export blocks APKG/CSV/TSV artifact creation when persisted word audio no longer matches `Word`, with clear CLI diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted generation records -> export snapshots | Accepted text, lexical, and audio records are frozen into learner-facing card rows. |
| persisted snapshots/audio metadata -> artifact writers | Existing database snapshots may be exported without reassembly unless runtime validates them. |
| CLI export caller -> filesystem artifacts | User-visible APKG/CSV/TSV files are written only after validation succeeds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | `AssembleExportCardsService.execute` | mitigate | Validate WORD audio metadata against `lexical_candidate.lemma` before card snapshots are persisted. |
| T-20-08 | Tampering | `RuntimeGenerateService._build_media_index` | mitigate | Revalidate persisted WORD audio metadata against `row.word` before APKG/CSV/TSV export proceeds. |
| T-20-09 | Repudiation | CLI export diagnostics | mitigate | Surface clear non-zero export errors containing `word_audio`, `Word`, and item key context. |
| T-20-10 | Information Disclosure | export error text | mitigate | Avoid absolute paths and private sentence/deck excerpts in mismatch diagnostics. |
</threat_model>

<verification>
Run `python -m pytest tests/integration/test_export_job_flow.py tests/services/test_assemble_export_cards.py tests/services/test_generate_audio_items.py tests/services/test_audio_integrity.py -q` and confirm all tests pass.
</verification>

<success_criteria>
- AUD-01 detection is enforced during card assembly and runtime export.
- AUD-02 blocks APKG/CSV/TSV exports with clear errors when mismatches cannot be repaired.
- Highlight exports remain unaffected because no `word_audio` field is required for highlight source profiles.
</success_criteria>

<output>
After completion, create `.planning/phases/20-word-audio-integrity-gate/20-03-SUMMARY.md`.
</output>
