---
phase: 20-word-audio-integrity-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/audio_integrity.py
  - tests/services/test_audio_integrity.py
autonomous: true
requirements: [AUD-01, AUD-02]
must_haves:
  truths:
    - "User can detect when a `word_audio` asset's stored display text, normalized TTS text, or provenance text hash does not exactly match the exported card `Word`."
    - "User receives deterministic, item-specific diagnostics for word-audio mismatches before export artifacts are written."
  artifacts:
    - path: "src/multilang/services/audio_integrity.py"
      provides: "Reusable word-audio integrity checks for generation, assembly, and export gates"
      exports: ["AudioIntegrityError", "assert_word_audio_matches_word", "word_audio_matches_word"]
    - path: "tests/services/test_audio_integrity.py"
      provides: "Focused mismatch and success coverage for exact word-audio checks"
  key_links:
    - from: "src/multilang/services/audio_integrity.py"
      to: "src/multilang/domain/audio.py"
      via: "AudioAssetRecord normalized_input/provenance fields"
      pattern: "AudioAssetRecord"
    - from: "tests/services/test_audio_integrity.py"
      to: "src/multilang/services/audio_integrity.py"
      via: "assert_word_audio_matches_word"
      pattern: "word_audio.*Word"
---

<objective>
Create the reusable integrity contract that defines exactly when `word_audio` matches the exported card `Word`.

Purpose: AUD-01 requires deterministic detection of mismatches in synthesis text and stored manifests; AUD-02 requires a shared gate that later generation and export code can use consistently.
Output: A tested `audio_integrity` service with exact-match helpers and clear validation errors.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/multilang/domain/audio.py
@tests/domain/test_audio.py

<interfaces>
Use these existing contracts directly:
```python
class AudioAssetKind(str, Enum):
    WORD = "word"
    SENTENCE = "sentence"

class NormalizedTtsInput(BaseModel):
    display_text: str
    tts_text: str
    ssml_text: str | None = None
    text_hash: str | None = None
    ssml_hash: str | None = None

class AudioAssetRecord(BaseModel):
    job_id: str
    item_key: str
    asset_kind: AudioAssetKind
    display_text: str
    normalized_input: NormalizedTtsInput
    provenance: AudioProvenance
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exact word-audio match behavior per AUD-01</name>
  <files>tests/services/test_audio_integrity.py, src/multilang/services/audio_integrity.py</files>
  <behavior>
    - A synthesized `AudioAssetKind.WORD` record passes when `asset.display_text`, `asset.normalized_input.display_text`, and `asset.normalized_input.tts_text` exactly equal the expected card `Word` after only trimming surrounding whitespace from the expected word.
    - A word asset fails when the stored display text is another word, even if file names or item keys look plausible.
    - A word asset fails when normalized TTS text is another word, because that is the actual synthesis text.
    - A word asset fails when `provenance.text_hash` does not equal the hash derived by `NormalizedTtsInput` from its stored `tts_text`.
  </behavior>
  <action>Create `tests/services/test_audio_integrity.py` first with deterministic `AudioAssetRecord` fixtures modeled after `tests/domain/test_audio.py`. Then create `src/multilang/services/audio_integrity.py` exporting `AudioIntegrityError`, `word_audio_matches_word(asset, expected_word) -> bool`, and `assert_word_audio_matches_word(asset, expected_word, item_key=None) -> None`. Reject non-word assets, empty expected words, mismatched `display_text`, mismatched `normalized_input.display_text`, mismatched `normalized_input.tts_text`, and mismatched provenance `text_hash`. Error messages must include `word_audio`, `Word`, the item key when supplied, and the mismatching metadata field name; do not include file system absolute paths.</action>
  <verify>
    <automated>python -m pytest tests/services/test_audio_integrity.py -q</automated>
  </verify>
  <done>Exact AUD-01 mismatch detection exists as a reusable service and all mismatch branches produce clear deterministic diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve legitimate word normalization boundaries per AUD-01</name>
  <files>tests/services/test_audio_integrity.py, src/multilang/services/audio_integrity.py</files>
  <behavior>
    - A word like `ação` fails when the TTS text is `acao`, because Phase 20 requires exact `word_audio`/`Word` matching rather than accent-stripped synthesis.
    - A sentence asset fails the word-audio gate instead of being treated as a matching word asset.
    - Surrounding whitespace in the expected `Word` is stripped before comparison so row assembly can pass values safely.
  </behavior>
  <action>Extend the same test file and helper implementation to document exact-match behavior for accented words and sentence assets. Keep the helper pure: no repository reads, no synthesis calls, no media file IO. This ensures downstream repair code can call the helper before deciding whether to reuse, regenerate, or block.</action>
  <verify>
    <automated>python -m pytest tests/services/test_audio_integrity.py -q</automated>
  </verify>
  <done>The integrity contract is strict enough to catch accent-stripped or sentence-audio substitutions while staying deterministic and side-effect free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database audio metadata -> export gate | Stored audio metadata may be stale or corrupted before cards are exported. |
| generated lexical Word -> audio integrity helper | Learner-facing card data becomes the authority for allowed word audio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `audio_integrity.assert_word_audio_matches_word` | mitigate | Compare `display_text`, `normalized_input.display_text`, `normalized_input.tts_text`, and `provenance.text_hash` against the expected Word before export. |
| T-20-02 | Information Disclosure | `AudioIntegrityError` messages | mitigate | Include item keys and field names, but omit absolute paths and raw private deck excerpts. |
| T-20-03 | Denial of Service | strict exact matching | accept | Exact matching can block exports, which is the intended safety behavior for AUD-02. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_audio_integrity.py -q` and confirm all tests pass.
</verification>

<success_criteria>
- AUD-01 has a reusable exact-match detector for synthesis text and stored manifest mismatches.
- AUD-02 has a deterministic error type that later export gates can raise when repair is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/20-word-audio-integrity-gate/20-01-SUMMARY.md`.
</output>
