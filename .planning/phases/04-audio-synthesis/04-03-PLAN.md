---
phase: 04-audio-synthesis
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/multilang/services/audio_synthesis.py
  - tests/services/test_audio_synthesis.py
autonomous: true
requirements:
  - AUDI-01
  - AUDI-02
user_setup:
  - service: azure-speech
    why: "Azure-first word and sentence TTS synthesis"
    env_vars:
      - name: MULTILANG_AZURE_SPEECH_KEY
        source: "Azure portal -> Speech resource -> Keys and Endpoint"
      - name: MULTILANG_AZURE_SPEECH_REGION
        source: "Azure portal -> Speech resource -> Overview / Keys and Endpoint"
must_haves:
  truths:
    - Accepted cards can produce separate playable word and sentence audio assets.
    - Voice fallback follows the locked Azure registry order and is persisted.
    - Media integrity checks reject missing or empty files before assets are accepted.
  artifacts:
    - path: src/multilang/services/audio_synthesis.py
      provides: Azure-backed synthesis, normalization, and integrity validation
      contains: class AudioSynthesisService
    - path: tests/services/test_audio_synthesis.py
      provides: Coverage for normalization, fallback, integrity, and reuse inputs
      contains: test_audio_synthesis_service_persists_fallback_used
  key_links:
    - from: src/multilang/services/audio_synthesis.py
      to: src/multilang/services/audio_voice_registry.py
      via: deterministic voice selection and fallback ordering
      pattern: select_voice|VOICE_REGISTRY_VERSION
    - from: src/multilang/services/audio_synthesis.py
      to: src/multilang/domain/audio.py
      via: normalized TTS input, integrity metadata, and persisted provenance
      pattern: NormalizedTtsInput|AudioAssetRecord|duration_ms
---

<objective>
Implement the Azure-first synthesis service, TTS normalization, and media-integrity validation.

Purpose: Deliver the real Phase 4 behavior behind AUDI-01 and AUDI-02 by enforcing D-04, D-06, D-07, D-08, D-09, and D-10 in one tested service boundary.
Output: Audio synthesis service with fakeable Azure adapter coverage and deterministic asset-building rules.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audio-synthesis/04-CONTEXT.md
@.planning/phases/04-audio-synthesis/04-RESEARCH.md
@src/multilang/domain/audio.py
@src/multilang/services/audio_voice_registry.py
@src/multilang/repositories/audio_repository.py
@src/multilang/domain/text_quality.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Azure-first audio synthesis and integrity service</name>
  <read_first>
    src/multilang/domain/audio.py
    src/multilang/services/audio_voice_registry.py
    src/multilang/domain/text_quality.py
    .planning/phases/04-audio-synthesis/04-RESEARCH.md
  </read_first>
  <files>src/multilang/services/audio_synthesis.py, tests/services/test_audio_synthesis.py</files>
  <behavior>
    - Test 1: the service only synthesizes assets for accepted Phase 3 text rows and refuses review-required rows per D-04
    - Test 2: the service derives normalized `tts_text` and SSML-safe input separately from learner-facing text per D-06, then builds deterministic storage keys from normalized hashes plus chosen voice per D-09
    - Test 3: the service applies the locked fallback order, records `fallback_used`, and rejects zero-byte or missing files before returning success per D-08 and D-10
    - Test 4: both `word` and `sentence` assets are synthesized separately with separate provenance and storage paths per D-01 and D-03
  </behavior>
  <action>Create `src/multilang/services/audio_synthesis.py` with an `AudioSynthesisService` that accepts an accepted lexical/text combination and returns separate `word` and `sentence` audio asset records. Keep Azure Speech behind an injectable adapter or protocol so tests can use fake responses instead of live SDK calls. Implement D-06 by deriving normalized `tts_text` / SSML-safe input without mutating visible card text. Implement D-08 by resolving voices strictly through the versioned registry: preferred voice, same-locale alternate, approved alternate locale, then fail the asset visibly. Implement D-09 by hashing normalized synthesis input plus voice configuration to produce deterministic filenames or storage paths. Implement D-10 by verifying file existence, non-zero bytes, stable storage path, and duration metadata before an asset is returned as successful. Write `tests/services/test_audio_synthesis.py` first, then implement the service so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/services/audio_synthesis.py` contains `class AudioSynthesisService`
    - `src/multilang/services/audio_synthesis.py` contains `fallback_used`
    - `src/multilang/services/audio_synthesis.py` contains logic for both `word` and `sentence` assets
    - `tests/services/test_audio_synthesis.py` contains `def test_audio_synthesis_service_rejects_review_required_text`
    - `tests/services/test_audio_synthesis.py` contains `def test_audio_synthesis_service_persists_fallback_used`
    - `tests/services/test_audio_synthesis.py` contains `def test_audio_synthesis_service_reuses_deterministic_storage_keys`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_audio_synthesis.py -q</automated>
  </verify>
  <done>Phase 4 has a tested Azure-first synthesis service that normalizes TTS input safely, enforces deterministic fallback, and validates media integrity before success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted text row → Azure synthesis request | only trusted accepted text should cross into external TTS calls |
| Azure synthesis result → local storage | provider output must be validated before becoming reusable project state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | T | `src/multilang/services/audio_synthesis.py` | mitigate | Gate synthesis on accepted text rows only per D-04 so review-required content cannot silently acquire audio. |
| T-04-10 | T | `src/multilang/services/audio_synthesis.py` | mitigate | Derive normalized `tts_text` separately from learner-facing text per D-06 and hash that normalized input plus voice config per D-09. |
| T-04-11 | R | `src/multilang/services/audio_synthesis.py` | mitigate | Persist chosen voice, locale, format, hashes, and `fallback_used` per D-03 and D-08 so every successful or failed synthesis attempt is auditable. |
| T-04-12 | D | `src/multilang/services/audio_synthesis.py` | mitigate | Reject missing or zero-byte files and require stable storage paths plus duration metadata where available per D-10 before returning success. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_audio_synthesis.py -q`
</verification>

<success_criteria>
- The synthesis service produces separate word and sentence assets only for accepted text rows.
- Deterministic fallback and storage-key rules are enforced in one tested service boundary.
- Media-integrity failures are rejected before runtime integration begins.
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-synthesis/04-03-SUMMARY.md`
</output>
