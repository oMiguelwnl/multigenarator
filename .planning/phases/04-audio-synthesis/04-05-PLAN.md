---
phase: 04-audio-synthesis
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - pyproject.toml
  - src/multilang/runtime.py
  - src/multilang/services/azure_speech_adapter.py
  - tests/services/test_azure_speech_adapter.py
  - tests/cli/test_generate_command.py
  - tests/integration/test_audio_job_flow.py
autonomous: true
requirements:
  - AUDI-01
  - AUDI-02
user_setup:
  - service: azure-speech
    why: "Live Azure-backed synthesis on the shipped `multilang generate` path"
    env_vars:
      - name: MULTILANG_AZURE_SPEECH_KEY
        source: "Azure portal -> Speech resource -> Keys and Endpoint"
      - name: MULTILANG_AZURE_SPEECH_REGION
        source: "Azure portal -> Speech resource -> Overview / Keys and Endpoint"
must_haves:
  truths:
    - "The default shipped runtime uses Azure Speech to create playable `word_audio`, or records a visible failed asset when no approved Azure fallback voice exists."
    - "The default shipped runtime uses Azure Speech to create playable `sentence_audio`, or records a visible failed asset when no approved Azure fallback voice exists."
    - "Resume and rerun continue reusing successful audio assets, while fallback and failure remain operator-visible instead of being hidden behind fake media bytes."
  artifacts:
    - path: src/multilang/services/azure_speech_adapter.py
      provides: Azure Speech adapter with real synthesis and voice inventory lookup
      contains: class AzureSpeechAdapter
    - path: src/multilang/runtime.py
      provides: Default runtime wiring to the Azure-backed adapter
      contains: build_runtime_service
    - path: tests/services/test_azure_speech_adapter.py
      provides: Provider-boundary tests for voice inventory and file synthesis
      contains: test_azure_speech_adapter_synthesizes_ssml_to_expected_file
    - path: tests/integration/test_audio_job_flow.py
      provides: Shipped-path proof that default runtime audio is non-zero media and remains reusable
      contains: test_generate_command_default_runtime_uses_azure_audio_adapter
  key_links:
    - from: src/multilang/runtime.py
      to: src/multilang/services/azure_speech_adapter.py
      via: default adapter construction when no explicit test adapter is injected
      pattern: AzureSpeechAdapter\(
    - from: src/multilang/services/azure_speech_adapter.py
      to: src/multilang/services/audio_synthesis.py
      via: AudioSynthesisAdapter protocol and AudioSynthesisResponse
      pattern: "available_voice_ids|def synthesize"
    - from: tests/integration/test_audio_job_flow.py
      to: src/multilang/runtime.py
      via: shipped-path generate command without the stub adapter
      pattern: build_runtime_service|audio_reused_items|fallback_audio_items
gap_closure: true
---

<objective>
Close the Phase 4 shipped-runtime gap so default audio generation uses Azure Speech instead of fake byte payloads.

Purpose: Finish AUDI-01 and AUDI-02 by replacing the `_RuntimeAudioAdapter` hollow provider path with a real Azure-backed adapter that honors D-07, D-08, and D-10 while preserving reuse from D-02 and D-09.
Output: Azure Speech runtime adapter, default runtime wiring, and shipped-path tests proving playable media plus deterministic Azure fallback handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audio-synthesis/04-CONTEXT.md
@.planning/phases/04-audio-synthesis/04-RESEARCH.md
@.planning/phases/04-audio-synthesis/04-VERIFICATION.md
@.planning/phases/04-audio-synthesis/04-04-SUMMARY.md
@pyproject.toml
@src/multilang/runtime.py
@src/multilang/services/audio_synthesis.py
@src/multilang/services/audio_voice_registry.py
@src/multilang/settings.py

<interfaces>
From `src/multilang/services/audio_synthesis.py`:
```python
class AudioSynthesisAdapter(Protocol):
    def available_voice_ids(self) -> set[str] | None: ...
    def synthesize(
        self,
        *,
        ssml_text: str,
        voice_id: str,
        locale: str,
        output_path: Path,
        audio_format: str,
    ) -> AudioSynthesisResponse: ...
```

From `src/multilang/runtime.py`:
```python
def build_runtime_service(
    settings: Settings | None = None,
    *,
    audio_adapter: AudioSynthesisAdapter | None = None,
) -> IngestLexicalItemsService:
```

From `src/multilang/services/audio_voice_registry.py`:
```python
def select_voice(
    language: SupportedLanguage,
    *,
    available_voice_ids: set[str] | None = None,
) -> VoiceSelection:
```

From `src/multilang/settings.py`:
```python
azure_speech_key: str | None = None
azure_speech_region: str | None = None
azure_speech_output_format: AudioOutputFormat = "audio-24khz-48kbitrate-mono-mp3"
audio_storage_dir: Path = Path(".multilang/audio")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the real Azure Speech adapter and deterministic voice inventory lookup</name>
  <files>pyproject.toml, src/multilang/services/azure_speech_adapter.py, tests/services/test_azure_speech_adapter.py</files>
  <behavior>
    - Test 1: `available_voice_ids()` returns Azure `ShortName` values from the voices-list response so `select_voice(...)` can enforce D-08's preferred → same-locale alternate → approved alternate-locale order.
    - Test 2: `synthesize(...)` writes provider-generated bytes to the requested output path and returns `AudioSynthesisResponse` with the same path, non-zero bytes, and duration metadata when exposed by Azure per D-10.
    - Test 3: Missing credentials, canceled synthesis, or unapproved voice availability fail visibly; the adapter must not introduce a local fake-audio fallback or secondary TTS provider per D-07.
  </behavior>
  <action>Add `azure-cognitiveservices-speech` to `pyproject.toml`, then create `src/multilang/services/azure_speech_adapter.py` with `class AzureSpeechAdapter` implementing the existing `AudioSynthesisAdapter` protocol. Use the Azure Speech SDK to synthesize SSML/text to the exact `output_path`, set `speech_synthesis_voice_name` explicitly, honor the configured output format, and return `AudioSynthesisResponse` from real provider results instead of writing UTF-8 payload bytes. Implement `available_voice_ids()` using Azure's voices-list endpoint keyed by `MULTILANG_AZURE_SPEECH_KEY` and `MULTILANG_AZURE_SPEECH_REGION`, caching returned `ShortName` values so fallback selection in `select_voice(...)` can obey D-08 at runtime. Write the tests first with fakes/monkeypatches for the SDK and HTTP call so no live Azure access is needed.</action>
  <verify>
    <automated>uv run pytest tests/services/test_azure_speech_adapter.py -q</automated>
  </verify>
  <done>`AzureSpeechAdapter` exists, is dependency-backed, returns real provider metadata, and no shipped-path code writes fake text payload bytes as audio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the shipped runtime stub and prove default-path playable media behavior</name>
  <files>src/multilang/runtime.py, tests/cli/test_generate_command.py, tests/integration/test_audio_job_flow.py</files>
  <behavior>
    - Test 1: `build_runtime_service()` constructs `AzureSpeechAdapter` by default when no explicit `audio_adapter` is injected, so shipped `multilang generate` closes the verification gap for both AUDI-01 and AUDI-02.
    - Test 2: When the preferred voice is absent from `available_voice_ids()`, the shipped path uses the approved Azure fallback order from D-08 and reports fallback usage instead of silently omitting audio.
    - Test 3: The default shipped path produces non-zero audio files and still reuses successful assets on resume/rerun per D-02, D-09, and D-10.
  </behavior>
  <action>Update `src/multilang/runtime.py` so the default branch of `build_runtime_service()` instantiates `AzureSpeechAdapter` and deletes the `_RuntimeAudioAdapter` shipped-path stub. Keep the explicit `audio_adapter=` injection hook for test-controlled paths, but make the no-override runtime use the real provider boundary per D-07 and D-08. Extend `tests/cli/test_generate_command.py` and `tests/integration/test_audio_job_flow.py` to stop treating `FileWritingAudioAdapter` as evidence for the default runtime path; instead monkeypatch `AzureSpeechAdapter` construction or its internal SDK/voice-list seams so the tests prove the shipped default path writes non-zero media files, increments fallback counters when alternates are chosen, and preserves reuse of previously synthesized assets. Also add a failure-path assertion that no approved voice results in a visible failed audio outcome rather than silent fake media generation per D-08 and D-10.</action>
  <verify>
    <automated>uv run pytest tests/services/test_azure_speech_adapter.py tests/cli/test_generate_command.py tests/integration/test_audio_job_flow.py -q</automated>
  </verify>
  <done>The default shipped runtime uses Azure-backed synthesis for both asset kinds, fallback behavior is explicit and test-covered, and replayed runs still reuse successful media instead of regenerating duplicates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime settings → Azure Speech | secrets and provider selection cross from local configuration into a third-party API boundary |
| Azure voice inventory → fallback selection | untrusted provider availability data determines whether the approved fallback chain is used |
| synthesized file output → persisted audio record | audio bytes must be validated before they become reusable truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | T | `src/multilang/runtime.py` | mitigate | Remove `_RuntimeAudioAdapter` from the default shipped path so runtime cannot tamper with audio truth by writing text payload bytes instead of provider output. |
| T-04-18 | S | `src/multilang/services/azure_speech_adapter.py` | mitigate | Require explicit Azure credentials and explicit voice IDs per D-07; do not fall back to implicit provider defaults or a second provider. |
| T-04-19 | T | `src/multilang/services/azure_speech_adapter.py` | mitigate | Query actual voice inventory and pass the resulting `ShortName` set into `select_voice(...)` so D-08 fallback ordering stays deterministic and approved-only. |
| T-04-20 | D | `tests/integration/test_audio_job_flow.py` | mitigate | Verify the default shipped path writes non-zero files and preserves reuse, preventing regressions back to hollow media generation. |
| T-04-21 | R | `tests/cli/test_generate_command.py` | mitigate | Assert fallback and failure counters remain visible so operators can distinguish reused, degraded, and failed audio outcomes. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_azure_speech_adapter.py tests/cli/test_generate_command.py tests/integration/test_audio_job_flow.py -q`
</verification>

<success_criteria>
- The default `multilang generate` path no longer writes fake UTF-8 payload bytes to `.mp3` files.
- `word_audio` and `sentence_audio` are synthesized through Azure Speech with approved Azure fallback selection or explicit failure records.
- Resume/rerun reuse still works with the real provider boundary, and CLI output keeps fallback/failure visible.
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-synthesis/04-05-SUMMARY.md`
</output>
