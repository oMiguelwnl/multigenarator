---
phase: 04-audio-synthesis
plan: 04
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/multilang/services/generate_audio_items.py
  - src/multilang/runtime.py
  - src/multilang/cli.py
  - tests/services/test_generate_audio_items.py
  - tests/cli/test_generate_command.py
  - tests/integration/test_audio_job_flow.py
autonomous: true
requirements:
  - AUDI-01
  - AUDI-02
user_setup:
  - service: azure-speech
    why: "Live shipped-path synthesis on the default generate command"
    env_vars:
      - name: MULTILANG_AZURE_SPEECH_KEY
        source: "Azure portal -> Speech resource -> Keys and Endpoint"
      - name: MULTILANG_AZURE_SPEECH_REGION
        source: "Azure portal -> Speech resource -> Overview / Keys and Endpoint"
must_haves:
  truths:
    - The shipped `multilang generate` path now produces reusable word and sentence audio after accepted text.
    - Interrupted or repeated runs reuse existing audio assets instead of silently duplicating them.
    - Operators can see audio counters for processed, reused, fallback, and failed assets on the shipped CLI path.
  artifacts:
    - path: src/multilang/services/generate_audio_items.py
      provides: Runtime orchestration for accepted-text audio generation and reuse
      contains: class GenerateAudioItemsService
    - path: tests/integration/test_audio_job_flow.py
      provides: End-to-end shipped-path reuse verification for Phase 4
      contains: test_generate_command_reuses_existing_audio_assets_on_resume
    - path: src/multilang/cli.py
      provides: Audio counters and diagnostics on the default command
      contains: word_audio_processed
  key_links:
    - from: src/multilang/runtime.py
      to: src/multilang/services/generate_audio_items.py
      via: repository-backed runtime composition after accepted text exists
      pattern: GenerateAudioItemsService|AudioRepository|AudioSynthesisService
    - from: src/multilang/cli.py
      to: tests/integration/test_audio_job_flow.py
      via: shipped-path counters and resume/rerun reuse behavior
      pattern: word_audio_|sentence_audio_|fallback_audio_|
---

<objective>
Wire Phase 4 into the shipped runtime and verify resume/rerun-safe audio reuse.

Purpose: Close AUDI-01 and AUDI-02 on the real `multilang generate` path while preserving D-04, D-05, D-09, and D-10 under repository-backed integration tests.
Output: Audio generation orchestrator, runtime/CLI wiring, and shipped-path tests for normal runs plus reuse on resume/rerun.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audio-synthesis/04-CONTEXT.md
@.planning/phases/04-audio-synthesis/04-RESEARCH.md
@src/multilang/runtime.py
@src/multilang/cli.py
@src/multilang/domain/jobs.py
@src/multilang/repositories/audio_repository.py
@src/multilang/services/audio_synthesis.py
@src/multilang/domain/text_quality.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add accepted-text audio orchestration with reusable asset lookup</name>
  <read_first>
    src/multilang/domain/jobs.py
    src/multilang/repositories/audio_repository.py
    src/multilang/repositories/text_repository.py
    src/multilang/services/audio_synthesis.py
  </read_first>
  <files>src/multilang/services/generate_audio_items.py, tests/services/test_generate_audio_items.py</files>
  <behavior>
    - Test 1: the orchestrator loads accepted text rows only, then creates both `word` and `sentence` assets per item per D-04 and AUDI-01/AUDI-02
    - Test 2: if deterministic hashes and voice selection match an existing successful asset, the orchestrator reuses that row/path instead of regenerating duplicate audio per D-02 and D-09
    - Test 3: fallback and failure counters are tracked explicitly, and a failed audio record remains visible instead of being silently skipped per D-08 and D-10
  </behavior>
  <action>Create `src/multilang/services/generate_audio_items.py` with `GenerateAudioItemsService` that queries accepted Phase 3 text rows only per D-04, uses `AudioRepository` to look up reusable assets by deterministic hashes and selected voice per D-09, synthesizes missing `word` and `sentence` assets through `AudioSynthesisService`, and records item progress under the existing `JobStage.SYNTHESIZE_AUDIO` stage per D-05. Keep the asset accounting explicit so later CLI output can report processed, reused, fallback, and failed counts. Write `tests/services/test_generate_audio_items.py` first, then implement the service so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/services/generate_audio_items.py` contains `class GenerateAudioItemsService`
    - `src/multilang/services/generate_audio_items.py` contains `JobStage.SYNTHESIZE_AUDIO`
    - `src/multilang/services/generate_audio_items.py` contains reuse logic keyed by deterministic hashes or voice selection
    - `tests/services/test_generate_audio_items.py` contains `def test_generate_audio_items_service_reuses_existing_assets`
    - `tests/services/test_generate_audio_items.py` contains `def test_generate_audio_items_service_skips_review_required_text_rows`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_generate_audio_items.py -q</automated>
  </verify>
  <done>The runtime has one orchestrator that turns accepted text rows into reusable word and sentence audio assets without duplicating existing media.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire shipped-path audio generation, counters, and end-to-end reuse tests</name>
  <read_first>
    src/multilang/runtime.py
    src/multilang/cli.py
    src/multilang/services/generate_audio_items.py
    tests/cli/test_generate_command.py
    tests/integration/test_text_job_flow.py
  </read_first>
  <files>src/multilang/runtime.py, src/multilang/cli.py, tests/cli/test_generate_command.py, tests/integration/test_audio_job_flow.py</files>
  <behavior>
    - Test 1: the default shipped `multilang generate` path runs lexical ingestion, text generation, then audio synthesis for accepted text on one repository-backed job per D-05
    - Test 2: rerunning or resuming the same job reuses previously generated audio rows and storage paths instead of creating duplicates per D-02 and D-09
    - Test 3: CLI output reports separate audio counters for processed, reused, fallback, and failed assets so omissions stay visible per D-08 and D-10
  </behavior>
  <action>Update `src/multilang/runtime.py` to construct `AudioRepository`, `AudioSynthesisService`, and `GenerateAudioItemsService`, then compose them after the existing accepted-text flow on the shipped runtime path. Update `src/multilang/cli.py` to print explicit counters for audio work on `multilang generate` without adding a new command or extra job stages, honoring D-05. Extend `tests/cli/test_generate_command.py` and add `tests/integration/test_audio_job_flow.py` to prove one shipped-path run persists both audio asset kinds for accepted text and that resume/rerun reuse returns the same rows/paths instead of duplicating media.</action>
  <acceptance_criteria>
    - `src/multilang/runtime.py` constructs `AudioRepository`
    - `src/multilang/runtime.py` constructs `GenerateAudioItemsService`
    - `src/multilang/cli.py` contains `word_audio_processed` or equivalent processed counter
    - `src/multilang/cli.py` contains `word_audio_reused` or equivalent reuse counter
    - `tests/cli/test_generate_command.py` contains `def test_generate_command_reports_audio_counters`
    - `tests/integration/test_audio_job_flow.py` contains `def test_generate_command_reuses_existing_audio_assets_on_resume`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_generate_audio_items.py tests/cli/test_generate_command.py tests/integration/test_audio_job_flow.py -q</automated>
  </verify>
  <done>The shipped runtime generates reusable audio on the default command path and proves resume/rerun-safe asset reuse under integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted persisted text → runtime audio stage | only accepted content should be allowed to trigger audio generation |
| shipped CLI output → operator decisions | counters must expose reuse/failure accurately so missing audio is not hidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | T | `src/multilang/services/generate_audio_items.py` | mitigate | Query accepted text rows only and keep all work inside `JobStage.SYNTHESIZE_AUDIO` per D-04 and D-05. |
| T-04-14 | R | `src/multilang/cli.py` | mitigate | Print explicit processed, reused, fallback, and failed audio counters so operators can audit missing audio instead of trusting silent success. |
| T-04-15 | T | `src/multilang/services/generate_audio_items.py` | mitigate | Reuse assets only when deterministic hashes and selected voice configuration match per D-09; otherwise synthesize a new row. |
| T-04-16 | D | `tests/integration/test_audio_job_flow.py` | mitigate | Add shipped-path resume/rerun reuse tests so runtime regressions cannot silently reintroduce duplicate or missing media. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_generate_audio_items.py tests/cli/test_generate_command.py tests/integration/test_audio_job_flow.py -q`
</verification>

<success_criteria>
- The shipped `multilang generate` path now produces `word_audio` and `sentence_audio` after accepted text.
- Resume and rerun behavior reuse existing assets instead of silently generating duplicates.
- CLI output makes fallback and failure behavior operator-visible.
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-synthesis/04-04-SUMMARY.md`
</output>
