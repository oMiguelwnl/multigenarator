---
phase: 04-audio-synthesis
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/multilang/db/models.py
  - src/multilang/repositories/audio_repository.py
  - alembic/versions/20260424_04_audio_synthesis_tables.py
  - tests/repositories/test_audio_repository.py
autonomous: true
requirements:
  - AUDI-01
  - AUDI-02
must_haves:
  truths:
    - Every job item can persist separate reusable word and sentence audio rows.
    - Resume and rerun work can reuse audio rows instead of silently duplicating them.
    - The live schema exposes the Phase 4 audio table before runtime wiring begins.
  artifacts:
    - path: src/multilang/repositories/audio_repository.py
      provides: Persistence boundary for audio assets
      contains: class AudioRepository
    - path: alembic/versions/20260424_04_audio_synthesis_tables.py
      provides: Migration for persisted audio records
      contains: audio_assets
    - path: src/multilang/db/models.py
      provides: ORM relationship wiring for audio records
      contains: class AudioAssetModel
  key_links:
    - from: src/multilang/repositories/audio_repository.py
      to: src/multilang/db/models.py
      via: unique upsert on `(job_id, item_key, asset_kind)`
      pattern: uq_audio_assets_job_id_item_key_asset_kind|AudioAssetModel
    - from: alembic/versions/20260424_04_audio_synthesis_tables.py
      to: src/multilang/db/models.py
      via: live schema verification for Phase 4 tables
      pattern: audio_assets
---

<objective>
Add the Phase 4 audio persistence boundary and verified schema migration.

Purpose: Implement D-01, D-02, D-03, D-09, and D-10 as durable storage rules before Azure synthesis and shipped-path orchestration are wired.
Output: ORM model, repository, migration, and persistence verification.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audio-synthesis/04-CONTEXT.md
@.planning/phases/04-audio-synthesis/04-RESEARCH.md
@src/multilang/domain/audio.py
@src/multilang/db/models.py
@src/multilang/repositories/text_repository.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audio ORM storage and repository queries</name>
  <read_first>
    src/multilang/domain/audio.py
    src/multilang/db/models.py
    src/multilang/repositories/text_repository.py
  </read_first>
  <files>src/multilang/db/models.py, src/multilang/repositories/audio_repository.py, tests/repositories/test_audio_repository.py</files>
  <behavior>
    - Test 1: one audio row exists per `(job_id, item_key, asset_kind)` even after reruns or fallback updates per D-02 and D-09
    - Test 2: repository queries can return reusable accepted assets, failed assets, and per-job ordered rows without collapsing word and sentence assets together per D-01 and D-10
    - Test 3: repository round-trips provenance, hash fields, byte size, duration, and fallback state as structured data per D-03
  </behavior>
  <action>Extend `src/multilang/db/models.py` with a new `AudioAssetModel` plus relationships from `GenerationJob` and `TextQualityRecordModel` if needed for lookup convenience. Create `src/multilang/repositories/audio_repository.py` with an upsert path keyed by `(job_id, item_key, asset_kind)` per D-02, plus methods to fetch one asset, list assets for a job, and find an existing reusable asset by normalized hashes/voice selection per D-09. Persist the provenance and integrity fields required by D-03 and D-10 exactly, including storage path, byte size, duration, format, hashes, status, and fallback state. Write `tests/repositories/test_audio_repository.py` first, then implement the ORM and repository so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/db/models.py` contains `class AudioAssetModel`
    - `src/multilang/repositories/audio_repository.py` contains `class AudioRepository`
    - `src/multilang/repositories/audio_repository.py` contains `get_reusable_asset`
    - `src/multilang/repositories/audio_repository.py` contains `list_assets_for_job`
    - `tests/repositories/test_audio_repository.py` contains `def test_upsert_audio_asset_updates_existing_row`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/repositories/test_audio_repository.py -q</automated>
  </verify>
  <done>Phase 4 has a repository-backed persistence boundary for separate word and sentence assets with stable identity and reusable lookup rules.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply and verify the Phase 4 schema migration</name>
  <read_first>
    src/multilang/db/models.py
    src/multilang/repositories/audio_repository.py
    alembic.ini
  </read_first>
  <files>alembic/versions/20260424_04_audio_synthesis_tables.py</files>
  <action>Create `alembic/versions/20260424_04_audio_synthesis_tables.py` to add the persisted audio table and any required indexes or unique constraints, especially the unique `(job_id, item_key, asset_kind)` identity from D-02. After the migration exists, run it against a disposable SQLite database and confirm the resulting schema contains `audio_assets` alongside the existing `generation_jobs`, `generation_items`, `lexical_candidates`, and `text_quality_records` tables. Do not skip this live-schema check; later runtime work depends on the real table existing, not only the ORM metadata.</action>
  <acceptance_criteria>
    - `alembic/versions/20260424_04_audio_synthesis_tables.py` contains `audio_assets`
    - migration verification output contains `audio_assets`
    - migration verification output still contains `text_quality_records`
    - migration verification output still contains `lexical_candidates`
  </acceptance_criteria>
  <verify>
    <automated>MULTILANG_DATABASE_URL=sqlite+pysqlite:////tmp/multilang-phase4-schema.db uv run alembic upgrade head && MULTILANG_DATABASE_URL=sqlite+pysqlite:////tmp/multilang-phase4-schema.db uv run python -c "from sqlalchemy import create_engine, inspect; from multilang.settings import Settings; engine=create_engine(Settings().database_url); print(sorted(inspect(engine).get_table_names()))"</automated>
  </verify>
  <done>The live database exposes the Phase 4 audio table required by synthesis, reuse, and shipped-path runtime wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime synthesis result → persisted audio row | incomplete or duplicate media must not be accepted as reusable truth |
| repository writes → live database | word and sentence assets must not overwrite each other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | T | `src/multilang/repositories/audio_repository.py` | mitigate | Enforce the unique `(job_id, item_key, asset_kind)` identity from D-02 so reruns update one row instead of creating conflicting duplicates. |
| T-04-06 | I | `src/multilang/repositories/audio_repository.py` | mitigate | Persist structured provenance, hash, and integrity metadata per D-03 and D-10 so operators can inspect why an asset was reused, replaced, or failed. |
| T-04-07 | D | live schema | mitigate | Run the Phase 4 Alembic migration against a disposable database before runtime wiring to prevent synthesis code from running against a stale schema. |
| T-04-08 | T | `src/multilang/db/models.py` | mitigate | Keep word and sentence assets separated by `asset_kind` so sentence synthesis cannot overwrite headword pronunciation records. |
</threat_model>

<verification>
- `uv run pytest tests/repositories/test_audio_repository.py -q`
- `MULTILANG_DATABASE_URL=sqlite+pysqlite:////tmp/multilang-phase4-schema.db uv run alembic upgrade head && MULTILANG_DATABASE_URL=sqlite+pysqlite:////tmp/multilang-phase4-schema.db uv run python -c "from sqlalchemy import create_engine, inspect; from multilang.settings import Settings; engine=create_engine(Settings().database_url); print(sorted(inspect(engine).get_table_names()))"`
</verification>

<success_criteria>
- Phase 4 persists separate word and sentence audio rows with stable item identity.
- Repository lookups can safely find reusable assets on resume/rerun.
- The audio schema is verified live before synthesis services are wired.
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-synthesis/04-02-SUMMARY.md`
</output>
