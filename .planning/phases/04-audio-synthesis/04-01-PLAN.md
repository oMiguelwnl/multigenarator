---
phase: 04-audio-synthesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/domain/audio.py
  - src/multilang/services/audio_voice_registry.py
  - src/multilang/settings.py
  - tests/domain/test_audio.py
  - tests/services/test_audio_voice_registry.py
autonomous: true
requirements:
  - AUDI-01
  - AUDI-02
must_haves:
  truths:
    - Phase 4 has one typed contract for separate word and sentence audio assets.
    - Azure voice selection is deterministic for all seven supported languages.
    - TTS-specific input and learner-facing text are modeled separately before persistence or runtime wiring begins.
  artifacts:
    - path: src/multilang/domain/audio.py
      provides: Typed audio contracts and provenance models
      contains: class AudioAssetRecord
    - path: src/multilang/services/audio_voice_registry.py
      provides: Versioned Azure voice registry and fallback selection
      contains: VOICE_REGISTRY_VERSION
    - path: tests/services/test_audio_voice_registry.py
      provides: Coverage for deterministic voice resolution
      contains: test_voice_registry_resolves_supported_languages
  key_links:
    - from: src/multilang/services/audio_voice_registry.py
      to: src/multilang/domain/jobs.py
      via: language-to-voice resolution keyed by supported language codes
      pattern: SupportedLanguage|select_voice
    - from: src/multilang/domain/audio.py
      to: tests/domain/test_audio.py
      via: contract tests for asset kind, provenance, and normalized TTS input
      pattern: AudioAssetKind|AudioAssetRecord|tts_text
---

<objective>
Define the Phase 4 audio contracts, Azure voice registry, and runtime settings.

Purpose: Lock the typed boundaries for D-01, D-03, D-06, D-07, and D-08 before persistence and Azure synthesis work begins.
Output: Audio domain models, deterministic voice-selection registry, Azure settings, and contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audio-synthesis/04-CONTEXT.md
@.planning/phases/04-audio-synthesis/04-RESEARCH.md
@src/multilang/domain/jobs.py
@src/multilang/settings.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed audio contracts for separate word and sentence assets</name>
  <read_first>
    .planning/phases/04-audio-synthesis/04-CONTEXT.md
    .planning/phases/04-audio-synthesis/04-RESEARCH.md
    src/multilang/domain/jobs.py
  </read_first>
  <files>src/multilang/domain/audio.py, tests/domain/test_audio.py</files>
  <behavior>
    - Test 1: `AudioAssetRecord` keeps one asset per `(job_id, item_key, asset_kind)` with explicit `word` and `sentence` kinds per D-01 and D-02
    - Test 2: provenance stores `provider`, `voice_id`, `locale`, `format`, `text_hash`, `ssml_hash`, `storage_path`, `byte_size`, `duration_ms`, `status`, and `fallback_used` per D-03 and D-10
    - Test 3: learner-facing text stays separate from normalized `tts_text` / SSML-safe input per D-06
  </behavior>
  <action>Create `src/multilang/domain/audio.py` as the Phase 4 source-of-truth contract layer. Include explicit enums/models for `AudioAssetKind`, `AudioSynthesisStatus`, `AudioProvider`, `AudioFormat`, `NormalizedTtsInput`, `AudioProvenance`, and `AudioAssetRecord`. The record must encode D-01, D-02, D-03, D-06, D-09, and D-10 exactly: stable job/item identity, separate `word`/`sentence` assets, separate normalized TTS input from display text, deterministic hash fields, provenance, integrity metadata, and fallback state. Write `tests/domain/test_audio.py` first, then implement the models so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/domain/audio.py` contains `class AudioAssetRecord`
    - `src/multilang/domain/audio.py` contains `tts_text`
    - `src/multilang/domain/audio.py` contains `fallback_used`
    - `tests/domain/test_audio.py` contains `def test_audio_asset_record_keeps_separate_word_and_sentence_identity`
    - `tests/domain/test_audio.py` contains `def test_audio_asset_record_separates_tts_text_from_display_text`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/domain/test_audio.py -q</automated>
  </verify>
  <done>Phase 4 has one typed contract for persisted audio identity, provenance, normalized TTS input, and media-integrity metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the versioned Azure voice registry and speech settings</name>
  <read_first>
    src/multilang/domain/audio.py
    src/multilang/settings.py
    .planning/phases/04-audio-synthesis/04-RESEARCH.md
  </read_first>
  <files>src/multilang/services/audio_voice_registry.py, src/multilang/settings.py, tests/services/test_audio_voice_registry.py</files>
  <behavior>
    - Test 1: the registry resolves preferred and approved fallback Azure voices for `pt`, `es`, `en`, `fr`, `de`, `ru`, and `nl` per D-07 and D-08
    - Test 2: fallback order is deterministic: preferred same-locale voice, same-locale alternate, approved alternate locale, then failure per D-08
    - Test 3: typed runtime settings expose Azure Speech credentials and output/storage defaults without hiding the selected registry version per D-07 and D-09
  </behavior>
  <action>Create `src/multilang/services/audio_voice_registry.py` with a versioned in-repo registry that hard-codes the approved Azure voice matrix for the seven supported languages per D-07, including the Dutch fallback path called out in STATE.md. Expose a deterministic selection function that returns both the chosen voice and whether fallback was used per D-08. Update `src/multilang/settings.py` to add typed Azure Speech configuration (`MULTILANG_AZURE_SPEECH_KEY`, `MULTILANG_AZURE_SPEECH_REGION`, output format, local audio storage directory, and optional registry override/version pin if needed). Write `tests/services/test_audio_voice_registry.py` first, then implement the registry and settings so the tests pass.</action>
  <acceptance_criteria>
    - `src/multilang/services/audio_voice_registry.py` contains `VOICE_REGISTRY_VERSION`
    - `src/multilang/services/audio_voice_registry.py` contains supported entries for `pt`, `es`, `en`, `fr`, `de`, `ru`, and `nl`
    - `src/multilang/settings.py` contains `azure_speech_key`
    - `src/multilang/settings.py` contains `azure_speech_region`
    - `tests/services/test_audio_voice_registry.py` contains `def test_voice_registry_resolves_supported_languages`
    - `tests/services/test_audio_voice_registry.py` contains `def test_voice_registry_uses_deterministic_fallback_order`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/domain/test_audio.py tests/services/test_audio_voice_registry.py -q</automated>
  </verify>
  <done>The codebase has a shipping voice contract for all supported languages plus typed Azure settings required by later persistence and synthesis work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-visible card text → TTS input | synthesis normalization must not rewrite learner-facing content |
| supported language code → Azure voice choice | implicit provider defaults would cause inconsistent pronunciation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `src/multilang/domain/audio.py` | mitigate | Encode separate learner-facing text and normalized `tts_text` fields so D-06 is enforced in the contract rather than left to runtime convention. |
| T-04-02 | T | `src/multilang/services/audio_voice_registry.py` | mitigate | Version and hard-code the approved Azure voice matrix per D-07 so runtime code cannot drift to undocumented provider defaults. |
| T-04-03 | R | `src/multilang/domain/audio.py` | mitigate | Persist explicit fallback and provenance fields per D-03 and D-08 so later failures can be audited deterministically. |
| T-04-04 | D | `src/multilang/settings.py` | mitigate | Add typed Azure settings early so later runtime wiring fails fast on missing credentials instead of failing deep inside synthesis. |
</threat_model>

<verification>
- `uv run pytest tests/domain/test_audio.py tests/services/test_audio_voice_registry.py -q`
</verification>

<success_criteria>
- Phase 4 has a typed audio contract separate from lexical and text records.
- Azure voice selection is deterministic for all seven supported languages.
- Runtime settings expose the Azure credentials and local storage defaults needed by later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/04-audio-synthesis/04-01-SUMMARY.md`
</output>
