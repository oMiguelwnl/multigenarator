---
phase: 22-latin-mode-contracts-and-isolation
plan: 3
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - tests/integration/test_v20_latin_mode_isolation_evidence.py
autonomous: true
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
must_haves:
  truths:
    - "Focused regression evidence proves the Latin profile does not mutate frequency, custom word-list, highlight, phonetics, review, audio, or export contracts."
    - "Evidence remains scanner-readable and tied to MODE-01, MODE-02, and MODE-03."
    - "Existing export and phonetics contracts remain distinct from the Latin profile."
  artifacts:
    - path: "tests/integration/test_v20_latin_mode_isolation_evidence.py"
      provides: "Phase 22 scanner-readable mode-isolation regression evidence"
      contains: "MODE-01"
  key_links:
    - from: "tests/integration/test_v20_latin_mode_isolation_evidence.py"
      to: "src/multilang/domain/source_profiles.py"
      via: "assert source profiles for Latin and existing modes remain distinct"
      pattern: "get_source_profile"
    - from: "tests/integration/test_v20_latin_mode_isolation_evidence.py"
      to: "src/multilang/domain/exporting.py"
      via: "assert existing export field names remain unchanged"
      pattern: "export_field_names_for_source_type"
---

<objective>
Create focused Phase 22 evidence that the new Latin mode is isolated from shipped modes.

Purpose: MODE-03 requires existing frequency, custom word-list, highlight, phonetics, review, audio, and export flows to remain operational after Latin mode is added.
Output: A scanner-readable integration evidence test for Latin mode isolation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/multilang/domain/source_profiles.py
@src/multilang/domain/exporting.py
@src/multilang/services/russian_phoneme_deck.py
@tests/integration/test_v13_existing_modes_regression_evidence.py

<interfaces>
Existing evidence pattern to follow:
```python
def test_frequency_mode_keeps_revised_normal_contract_with_word_audio_and_translation() -> None: ...
def test_custom_word_list_mode_keeps_manual_highlight_style_contract_without_normal_audio() -> None: ...
def test_highlight_mode_keeps_dedicated_note_type_and_source_profile_fields() -> None: ...
def test_russian_phonetics_mode_keeps_phoneme_contract_without_normal_or_highlight_leakage() -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scanner-readable Latin mode contract evidence</name>
  <files>tests/integration/test_v20_latin_mode_isolation_evidence.py</files>
  <behavior>
    - Test 1: a module-level `PHASE_22_REQUIREMENTS = ("MODE-01", "MODE-02", "MODE-03")` exists for scanner-readable traceability.
    - Test 2: Latin metadata from `LatinMvpGenerationService().start(...)` reports `language_code=la`, Classical Latin, source pack version, 50 cards, and `latin-mvp` source type.
    - Test 3: `latin-mvp` profile is not equal to the `frequency`, `word-list`, or `kindle-highlights` profiles.
  </behavior>
  <action>Create the integration evidence file with focused assertions over the Latin contracts and service from Plans 22-01/22-02. Include requirement IDs as constants and in assertion names/docstrings so later milestone evidence scanners can locate MODE-01/MODE-02/MODE-03 without parsing prose.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_mode_isolation_evidence.py -q</automated>
  </verify>
  <done>Phase 22 has scanner-readable evidence for Latin path selection, Latin metadata, and profile isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove existing export and phonetics contracts remain unchanged</name>
  <files>tests/integration/test_v20_latin_mode_isolation_evidence.py</files>
  <behavior>
    - Test 1: `export_field_names_for_source_type("frequency")` still returns the normal field tuple including `Translation`, `word_audio`, `sentence_audio`, and blank `Image` support.
    - Test 2: `export_field_names_for_source_type("word-list")` and `export_field_names_for_source_type("kindle-highlights")` still return highlight/manual field names without `Translation` or `word_audio`.
    - Test 3: `PHONEME_FIELD_NAMES` still contains phonetics-specific fields and remains disjoint from normal/highlight learner field contracts as in v1.3 evidence.
  </behavior>
  <action>Add regression assertions modeled on `tests/integration/test_v13_existing_modes_regression_evidence.py`. Do not modify production export or phonetics code in this plan; if this evidence fails, repair the specific contract that regressed rather than weakening the assertions.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_mode_isolation_evidence.py tests/integration/test_v13_existing_modes_regression_evidence.py -q</automated>
  </verify>
  <done>Focused evidence proves Latin additions did not alter existing export or phonetics contracts for MODE-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove existing review and audio modules remain importable with Latin contracts present</name>
  <files>tests/integration/test_v20_latin_mode_isolation_evidence.py</files>
  <behavior>
    - Test 1: `TextReviewService`, `ReviewReport`, and `ReviewReportItem` remain importable after Latin contracts are added.
    - Test 2: `AudioSynthesisRequest`, `AudioAssetStatus`, and voice-registry symbols used by focused audio tests remain importable after Latin contracts are added.
    - Test 3: Import checks do not instantiate external providers or require Azure credentials.
  </behavior>
  <action>Add lightweight import-level isolation assertions for review and audio modules because MODE-03 explicitly names review and audio flows. Keep the test deterministic and offline; avoid provider calls or filesystem writes.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_mode_isolation_evidence.py tests/services/test_text_review.py tests/services/test_audio_synthesis.py -q</automated>
  </verify>
  <done>Focused regression evidence covers review and audio import boundaries without requiring live services.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test evidence → milestone scanner | Scanner-readable constants and assertions become compliance evidence. |
| Latin contracts → existing modules | New imports and profile values can accidentally change existing mode behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Repudiation | `test_v20_latin_mode_isolation_evidence.py` | mitigate | Include explicit MODE-01/MODE-02/MODE-03 constants and test names tied to each requirement. |
| T-22-08 | Tampering | Export and phonetics contracts | mitigate | Assert exact existing field tuples and phonetics disjointness against the v1.3 evidence pattern. |
| T-22-09 | Denial of Service | Review/audio evidence | mitigate | Keep checks import-only/offline; no external provider calls or credentials. |
</threat_model>

<verification>
Run `python -m pytest tests/integration/test_v20_latin_mode_isolation_evidence.py tests/integration/test_v13_existing_modes_regression_evidence.py tests/services/test_text_review.py tests/services/test_audio_synthesis.py -q`.
</verification>

<success_criteria>
Phase 22 has focused evidence for Latin mode selection, metadata, profile isolation, unchanged export/phonetics contracts, and review/audio import safety.
</success_criteria>

<output>
After completion, create `.planning/phases/22-latin-mode-contracts-and-isolation/22-03-SUMMARY.md`.
</output>
