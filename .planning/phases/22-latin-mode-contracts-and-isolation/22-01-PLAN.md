---
phase: 22-latin-mode-contracts-and-isolation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/domain/latin.py
  - src/multilang/domain/source_profiles.py
  - src/multilang/domain/jobs.py
  - tests/domain/test_latin_contracts.py
  - tests/domain/test_source_profiles.py
  - tests/domain/test_jobs.py
autonomous: true
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
must_haves:
  truths:
    - "Latin mode has explicit contracts for `la`, Classical Latin, source pack version, and 50-card scope."
    - "Latin mode is represented as a separate source/profile contract and not as the existing modern-language frequency profile."
    - "Existing frequency, word-list, kindle-highlight, and SupportedLanguage contracts continue to match their shipped values."
  artifacts:
    - path: "src/multilang/domain/latin.py"
      provides: "Classical Latin contract types and metadata factory"
      exports: ["LatinVariant", "LATIN_LANGUAGE_CODE", "LATIN_MVP_CARD_COUNT", "DEFAULT_LATIN_SOURCE_PACK_VERSION", "LatinDeckMetadata", "LatinGenerationRequest"]
    - path: "src/multilang/domain/source_profiles.py"
      provides: "Latin source profile isolated from modern frequency, manual, and highlight profiles"
      contains: "latin-mvp"
    - path: "tests/domain/test_latin_contracts.py"
      provides: "Executable MODE-01/MODE-02 contract evidence"
  key_links:
    - from: "src/multilang/domain/latin.py"
      to: "src/multilang/domain/source_profiles.py"
      via: "LatinGenerationRequest.source_type defaults to the Latin profile key"
      pattern: "latin-mvp"
    - from: "tests/domain/test_source_profiles.py"
      to: "src/multilang/domain/source_profiles.py"
      via: "tests assert existing profile fields and Latin profile isolation"
      pattern: "get_source_profile"
---

<objective>
Create the domain contracts that let later work address Classical Latin as its own `la` generation mode.

Purpose: Phase 22 must introduce Latin contracts without mutating the shipped normal, manual, highlight, or phonetics contracts.
Output: Latin contract module, Latin source-profile entry, and focused contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/multilang/domain/source_profiles.py
@src/multilang/domain/jobs.py
@tests/domain/test_source_profiles.py
@tests/domain/test_jobs.py

<interfaces>
Existing contracts the executor must preserve:

From `src/multilang/domain/source_profiles.py`:
```python
SourceType = Literal["frequency", "word-list", "kindle-highlights"]
@dataclass(frozen=True, slots=True)
class SourceProfile:
    source_type: SourceType
    requires_translation_validation: bool
    exports_translation_field: bool
    min_sentence_tokens: int
    max_sentence_tokens: int
    note_type_name: str
    template_name: str
```

From `src/multilang/domain/jobs.py`:
```python
class SupportedLanguage(str, Enum):
    PT = "pt"; ES = "es"; EN = "en"; FR = "fr"; DE = "de"; IT = "it"; PL = "pl"; TR = "tr"; RO = "ro"; RU = "ru"; NL = "nl"
class GenerationRequest(BaseModel):
    language: SupportedLanguage
    source_type: SourceType
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Classical Latin domain metadata contracts</name>
  <files>src/multilang/domain/latin.py, tests/domain/test_latin_contracts.py</files>
  <behavior>
    - Test 1: `LatinDeckMetadata()` exposes `language_code == "la"`, `variant == LatinVariant.CLASSICAL`, a non-empty source pack version, and `card_count == 50`.
    - Test 2: `LatinGenerationRequest()` defaults to source type `latin-mvp`, language code `la`, Classical Latin variant, default source pack version, and 50-card scope.
    - Test 3: Pydantic rejects blank source pack versions and any card count other than 50.
  </behavior>
  <action>Create `src/multilang/domain/latin.py` with Pydantic v2 models and constants for the Latin contract: `LATIN_LANGUAGE_CODE = "la"`, `LATIN_MVP_CARD_COUNT = 50`, `DEFAULT_LATIN_SOURCE_PACK_VERSION = "latin-mvp-50-v1"`, `LatinVariant` containing only `CLASSICAL = "classical"`, `LatinDeckMetadata`, and `LatinGenerationRequest`. Keep this separate from `SupportedLanguage` so the modern-language enum remains an existing-mode contract for MODE-03.</action>
  <verify>
    <automated>python -m pytest tests/domain/test_latin_contracts.py -q</automated>
  </verify>
  <done>Latin metadata and request contracts exist, are validated, and encode `language_code=la`, Classical Latin, source pack version, and exact 50-card scope for MODE-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register isolated Latin source profile without changing shipped profiles</name>
  <files>src/multilang/domain/source_profiles.py, tests/domain/test_source_profiles.py</files>
  <behavior>
    - Test 1: `get_source_profile("latin-mvp")` returns `source_type == "latin-mvp"`, `note_type_name == "Multilang::Latin MVP Card"`, `template_name == "latin_mvp_card"`, and sentence token limits appropriate for curated Latin source sentences.
    - Test 2: Existing frequency, word-list, and kindle-highlight profile assertions remain identical.
    - Test 3: Unknown source-profile errors list `latin-mvp` but still redact the unknown input value and private path fragments.
  </behavior>
  <action>Extend `SourceType` and `SOURCE_PROFILES` with a Latin-specific `latin-mvp` profile. Do not change frequency, word-list, or kindle-highlight profile values. Keep `SUPPORTED_SOURCE_TYPES` derived from `SOURCE_PROFILES.keys()` so error messages and tests stay source-of-truth. Use the Latin profile for MODE-01 isolation; do not route it through the `frequency` profile.</action>
  <verify>
    <automated>python -m pytest tests/domain/test_source_profiles.py -q</automated>
  </verify>
  <done>`latin-mvp` is a first-class source profile and all pre-existing source-profile contracts still pass unchanged for MODE-01 and MODE-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve modern-language request contracts while exporting Latin contracts</name>
  <files>src/multilang/domain/jobs.py, tests/domain/test_jobs.py</files>
  <behavior>
    - Test 1: `SupportedLanguage` still contains exactly `pt, es, en, fr, de, it, pl, tr, ro, ru, nl` and does not add `la`.
    - Test 2: `GenerationRequest(language="en", source_type="latin-mvp")` validates if needed for shared infrastructure, while `GenerationRequest(language="la", source_type="frequency")` remains invalid.
    - Test 3: `multilang.domain.latin` symbols are importable through the module where executor patterns expect domain contracts.
  </behavior>
  <action>Update imports/exports in `jobs.py` only as needed after adding `latin-mvp` to `SourceType`. Preserve the modern `SupportedLanguage` enum exactly to prove Latin is not being folded into the modern frequency path. If `GenerationRequest` accepts the new source type via `SourceType`, document with tests that `la` still belongs to `LatinGenerationRequest`, not `SupportedLanguage`.</action>
  <verify>
    <automated>python -m pytest tests/domain/test_jobs.py tests/domain/test_latin_contracts.py tests/domain/test_source_profiles.py -q</automated>
  </verify>
  <done>Modern language validation remains unchanged, Latin-specific contracts are available, and the domain boundary is clear for MODE-01/MODE-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/test input → Pydantic contracts | Untrusted source pack versions or card counts enter Latin request models. |
| Source profile lookup input → error text | User-supplied profile names can contain private paths or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `LatinDeckMetadata` / `LatinGenerationRequest` | mitigate | Validate source pack version is non-blank and card count equals 50. |
| T-22-02 | Information Disclosure | `get_source_profile` errors | mitigate | Preserve redacted unknown-profile error behavior; tests assert private input fragments are omitted. |
| T-22-03 | Tampering | `SupportedLanguage` enum | mitigate | Tests assert `la` is not added to the modern-language enum and existing values remain exact. |
</threat_model>

<verification>
Run `python -m pytest tests/domain/test_latin_contracts.py tests/domain/test_source_profiles.py tests/domain/test_jobs.py -q`.
</verification>

<success_criteria>
Latin contract symbols exist, `latin-mvp` is isolated from the `frequency` profile, and modern-language contracts pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/22-latin-mode-contracts-and-isolation/22-01-SUMMARY.md`.
</output>
