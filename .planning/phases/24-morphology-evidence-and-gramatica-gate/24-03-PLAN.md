---
phase: 24-morphology-evidence-and-gramatica-gate
plan: 3
type: execute
wave: 3
depends_on: [24-02]
files_modified:
  - src/multilang/services/latin_mvp.py
  - src/multilang/cli.py
  - tests/services/test_latin_mvp.py
  - tests/cli/test_generate_latin_mvp_command.py
autonomous: true
requirements: [GRAM-01, GRAM-02, GRAM-03, GRAM-04]
must_haves:
  truths:
    - "Starting Latin MVP generation reports grammar readiness from the manifest."
    - "The CLI exposes scanner-readable grammar summary lines without routing Latin through existing modern-language generation."
    - "A manifest with unresolved grammar cannot be treated as learner-ready by the Latin MVP start service."
  artifacts:
    - path: "src/multilang/services/latin_mvp.py"
      provides: "Grammar readiness summary and fail-closed gate in Latin MVP start result"
      exports: ["LatinMvpStartResult", "LatinMvpGenerationService"]
    - path: "src/multilang/cli.py"
      provides: "CLI output for grammar gate status and grammar evidence counts"
    - path: "tests/services/test_latin_mvp.py"
      provides: "Service tests for grammar summaries and unresolved gate behavior"
    - path: "tests/cli/test_generate_latin_mvp_command.py"
      provides: "CLI tests for scanner-readable grammar output"
  key_links:
    - from: "src/multilang/services/latin_mvp.py"
      to: "data/latin_mvp/latin-mvp-50-v1.json"
      via: "load_latin_mvp_source_pack grammar fields"
      pattern: "grammar_gate_status"
    - from: "src/multilang/cli.py"
      to: "LatinMvpStartResult.manifest_summary"
      via: "generate-latin-mvp summary output"
      pattern: "grammar_"
---

<objective>
Wire grammar readiness into the existing Latin MVP start service and CLI inspection output.

Purpose: Users and scanners need to see that the 50-card MVP has resolved grammar evidence, and later review/export phases need a fail-closed signal when grammar evidence is not approved.
Output: Service result fields, manifest JSON summary fields, CLI key=value lines, and tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-03-SUMMARY.md
@.planning/phases/24-morphology-evidence-and-gramatica-gate/24-02-SUMMARY.md
@src/multilang/services/latin_mvp.py
@src/multilang/cli.py
@tests/services/test_latin_mvp.py
@tests/cli/test_generate_latin_mvp_command.py

<interfaces>
From `src/multilang/services/latin_mvp.py`:
```python
class LatinMvpStartResult(BaseModel):
    metadata: LatinDeckMetadata
    source_type: str = "latin-mvp"
    item_keys: list[str]
    manifest_path: str
    license_gate_status: str
    def manifest_summary(self) -> dict[str, object]: ...

class LatinMvpGenerationService:
    def start(self, request: LatinGenerationRequest) -> LatinMvpStartResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add service tests for grammar readiness and gate status</name>
  <files>tests/services/test_latin_mvp.py</files>
  <behavior>
    - `LatinMvpGenerationService().start(LatinGenerationRequest())` returns `grammar_gate_status == "approved"`, `grammar_evidence_count == 50`, `gramatica_count == 50`, and includes the required case vocabulary in a summary field.
    - `manifest_summary()` includes the same grammar fields for scanner-readable output.
    - A fake loader returning a pack with an unresolved or non-approved grammar entry causes the service to expose a blocked grammar gate or raise a concise `ValueError`; do not allow a learner-ready/approved service result for unresolved grammar.
  </behavior>
  <action>Add tests first. Use the existing service dependency injection pattern from Phase 23. If constructing an invalid Pydantic pack is impossible because Plan 24-01 validation rejects it, assert that the service cannot be created from unresolved data by using the loader failure path and checking for a concise grammar-related error.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_mvp.py -q</automated>
  </verify>
  <done>New service tests fail until grammar summary fields are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement service grammar summary fields</name>
  <files>src/multilang/services/latin_mvp.py, tests/services/test_latin_mvp.py</files>
  <behavior>
    - Summary fields count validated grammar evidence and `Gramatica` values directly from the loaded manifest.
    - `grammar_gate_status` is `approved` only when all 50 entries have approved grammar review status and validated `gramatica`.
    - The manifest JSON summary includes grammar fields without leaking internal notes unless explicitly safe and useful.
  </behavior>
  <action>Extend `LatinMvpStartResult` with `grammar_gate_status`, `grammar_evidence_count`, `gramatica_count`, and `required_case_labels` (the six required labels). Populate them in `LatinMvpGenerationService.start()` from `pack.entries`. Keep license gate behavior unchanged. Do not add translation/audio/export readiness fields in this phase.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_mvp.py -q</automated>
  </verify>
  <done>The service reports approved grammar readiness for the committed 50-entry manifest and cannot report approved readiness for unresolved grammar data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose grammar summary in CLI output</name>
  <files>src/multilang/cli.py, tests/cli/test_generate_latin_mvp_command.py</files>
  <behavior>
    - `generate-latin-mvp` prints stable key=value lines: `grammar_gate_status=approved`, `grammar_evidence_count=50`, and `gramatica_count=50`.
    - `generate-latin-mvp --manifest-json` includes the same grammar fields.
    - Existing modern-language rejection tests still prove Latin remains isolated from `generate`.
  </behavior>
  <action>Update the existing `generate-latin-mvp` command output to print the grammar fields after the existing manifest/source summary lines. Update CLI tests to assert the new key=value lines and JSON keys. Preserve existing output keys so Phase 23 evidence does not regress.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py -q</automated>
  </verify>
  <done>CLI output is scanner-readable for grammar readiness while existing Latin mode isolation tests continue to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated manifest → CLI output | Internal grammar readiness becomes public scanner/user output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | I | `manifest_summary()` | mitigate | Output aggregate counts and labels only; avoid dumping private paths or unnecessary evidence notes. |
| T-24-08 | T | `LatinMvpGenerationService.start` | mitigate | Derive grammar gate from validated entries; never from caller-provided flags. |
| T-24-09 | D | CLI summary | accept | Output is small fixed metadata for 50 entries; no unbounded user input is processed here. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py -q`.
</verification>

<success_criteria>
- Service result and manifest JSON expose grammar evidence counts and approved gate status.
- CLI prints stable grammar summary fields.
- Latin remains isolated from modern-language `generate` paths.
</success_criteria>

<output>
After completion, create `.planning/phases/24-morphology-evidence-and-gramatica-gate/24-03-SUMMARY.md`.
</output>
