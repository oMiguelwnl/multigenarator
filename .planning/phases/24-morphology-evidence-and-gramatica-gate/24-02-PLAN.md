---
phase: 24-morphology-evidence-and-gramatica-gate
plan: 2
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - data/latin_mvp/latin-mvp-50-v1.json
  - tests/integration/test_v20_latin_grammar_asset.py
autonomous: true
requirements: [GRAM-01, GRAM-02, GRAM-03, GRAM-04]
must_haves:
  truths:
    - "All 50 frozen MVP entries load with approved morphology evidence."
    - "All 50 frozen MVP entries expose a concise standardized `Gramatica` field."
    - "The committed asset has no ambiguous or unresolved grammar status."
  artifacts:
    - path: "data/latin_mvp/latin-mvp-50-v1.json"
      provides: "Curated morphology evidence and `Gramatica` values for all 50 MVP entries"
      contains: "morphology_evidence"
    - path: "tests/integration/test_v20_latin_grammar_asset.py"
      provides: "Integration evidence for the committed 50-entry grammar asset"
  key_links:
    - from: "data/latin_mvp/latin-mvp-50-v1.json"
      to: "src/multilang/services/latin_source_pack.py"
      via: "load_latin_mvp_source_pack validates committed asset"
      pattern: "load_latin_mvp_source_pack"
---

<objective>
Curate the frozen 50-card source pack so each actual MVP card has morphology evidence and a standardized `Gramatica` note.

Purpose: Phase 24 is not complete until the real frozen asset, not only synthetic tests, carries the grammar data users will see and later review/export phases will gate.
Output: Updated `latin-mvp-50-v1.json` and integration tests proving every entry has approved grammar evidence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-02-SUMMARY.md
@.planning/phases/24-morphology-evidence-and-gramatica-gate/24-01-SUMMARY.md
@data/latin_mvp/latin-mvp-50-v1.json
@src/multilang/services/latin_source_pack.py

<interfaces>
Use the grammar contract created by Plan 24-01. Each JSON entry must include:
```json
"morphology_evidence": {
  "lemma": "...",
  "part_of_speech": "subst|adj|v|pron|prep|conj|adv",
  "case_label": "Nominativus|Vocativus|Accusativus|Genitivus|Dativus|Ablativus|null",
  "number": "sg|pl|null",
  "verbal_analysis": "... or null",
  "syntactic_function": "Suj|OD|OI|Pred|Circ|Conj|Mod|V",
  "evidence_note": "short note tied to the target form in the Latin sentence",
  "grammar_review_status": "approved"
},
"gramatica": "short note using approved abbreviations and case labels"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add committed-asset grammar evidence tests</name>
  <files>tests/integration/test_v20_latin_grammar_asset.py</files>
  <behavior>
    - Test 1: `load_latin_mvp_source_pack()` loads exactly 50 entries and each entry has `morphology_evidence.grammar_review_status == "approved"`.
    - Test 2: every `morphology_evidence.lemma` matches the entry `lemma`, every nominal target has case+number, every verb target has verbal analysis, and every entry has a syntactic function.
    - Test 3: all `gramatica` values are non-blank, concise, validated by `validate_latin_gramatica()`, and contain no `Genetivus`, English labels, or long Portuguese labels like `substantivo`.
    - Test 4: the six required case labels are represented or accepted by the validator, with `Genitivus` included exactly.
  </behavior>
  <action>Create the integration test file before editing the asset. Import `load_latin_mvp_source_pack` and `validate_latin_gramatica`. Keep assertions scanner-friendly and focused on GRAM-01/GRAM-02/GRAM-03/GRAM-04. This test should fail until the committed JSON asset has grammar fields for all 50 entries.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_grammar_asset.py -q</automated>
  </verify>
  <done>The new integration test fails because the committed asset has not yet been enriched with grammar evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add morphology evidence and `Gramatica` to all 50 entries</name>
  <files>data/latin_mvp/latin-mvp-50-v1.json, tests/integration/test_v20_latin_grammar_asset.py</files>
  <behavior>
    - Every one of the 50 JSON entries has a target-form-specific morphology analysis matching its displayed Latin sentence.
    - The `gramatica` value is concise and uses approved abbreviations such as `subst`, `adj`, `v`, `sg`, `pl`, `Suj`, `OD`, `OI` and required Latin case labels.
    - Ambiguous cards are not left ambiguous; if an entry cannot be resolved, fix the analysis to a reviewed final value rather than weakening validation.
  </behavior>
  <action>Update `data/latin_mvp/latin-mvp-50-v1.json` entry-by-entry. Preserve every Phase 23 field exactly unless the new grammar fields require insertion. For each entry, analyze the target form as used in `latin_sentence`; set `morphology_evidence.lemma` equal to the existing `lemma`; set `grammar_review_status` to `approved`; write a short evidence note that explains the target form in context. Use `Genitivus`, never `Genetivus`. Do not add Portuguese translation, review workflow states beyond grammar approval, audio metadata, export fields, image fields, or scale beyond the frozen 50-card MVP.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_grammar_asset.py tests/services/test_latin_source_pack.py -q</automated>
  </verify>
  <done>The committed manifest loads successfully with approved grammar evidence and validated `Gramatica` for all 50 entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated grammar asset → learner card pipeline | Human/AI-curated grammar text becomes learner-facing card data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | T | `data/latin_mvp/latin-mvp-50-v1.json` | mitigate | Loader-backed integration tests validate all 50 entries before service/export can consume them. |
| T-24-05 | R | grammar curation | mitigate | Require per-entry evidence notes and approved status so grammar decisions are auditable. |
| T-24-06 | I | committed asset | mitigate | Preserve Phase 23 source/privacy constraints; add only grammar metadata, no private paths or provider secrets. |
</threat_model>

<verification>
Run `python -m pytest tests/integration/test_v20_latin_grammar_asset.py tests/services/test_latin_source_pack.py -q`.
</verification>

<success_criteria>
- All 50 source-pack entries contain morphology evidence for the target form.
- All 50 source-pack entries contain validated concise `Gramatica`.
- The committed asset has no unresolved grammar review status.
</success_criteria>

<output>
After completion, create `.planning/phases/24-morphology-evidence-and-gramatica-gate/24-02-SUMMARY.md`.
</output>
