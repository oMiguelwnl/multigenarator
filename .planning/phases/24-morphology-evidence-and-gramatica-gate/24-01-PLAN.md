---
phase: 24-morphology-evidence-and-gramatica-gate
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/latin_source_pack.py
  - tests/services/test_latin_source_pack.py
autonomous: true
requirements: [GRAM-01, GRAM-02, GRAM-03, GRAM-04]
must_haves:
  truths:
    - "Every loaded Latin MVP entry can carry resolved morphology evidence for the target form."
    - "The source-pack loader rejects invalid or unresolved grammar evidence before generation consumes it."
    - "The `Gramatica` note is short, standardized, Portuguese-facing, and uses approved abbreviations and Latin case labels."
  artifacts:
    - path: "src/multilang/services/latin_source_pack.py"
      provides: "Latin grammar/morphology evidence contracts and fail-closed validation"
      exports: ["LatinMorphologyEvidence", "LatinGrammarReviewStatus", "validate_latin_gramatica"]
    - path: "tests/services/test_latin_source_pack.py"
      provides: "Focused tests for grammar evidence and `Gramatica` validation"
  key_links:
    - from: "src/multilang/services/latin_source_pack.py"
      to: "LatinMvpSourcePackEntry"
      via: "required morphology_evidence and gramatica fields"
      pattern: "morphology_evidence.*gramatica"
---

<objective>
Add the typed grammar contract that Phase 24 needs before the 50-card asset and generator summary can consume morphology evidence.

Purpose: GRAM-01 through GRAM-04 require fail-closed grammar data, not loose strings. The loader must reject unresolved ambiguity and non-standard grammar labels before later review/export phases can mark cards learner-ready.
Output: Pydantic models and validation tests for Latin morphology evidence, case vocabulary, `Gramatica`, and grammar review status.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frozen-50-card-source-pack-and-sentence-sequence/23-01-SUMMARY.md
@src/multilang/services/latin_source_pack.py
@tests/services/test_latin_source_pack.py

<interfaces>
From `src/multilang/services/latin_source_pack.py`:
```python
class LatinMvpSourcePackEntry(LatinSourceProvenance, LatinDidacticRationale):
    item_key: str
    sequence: int
    lemma: str
    target_form: str
    latin_sentence: str
    frequency_rank: int
    frequency_source: str
    source_pack_version: Literal["latin-mvp-50-v1"]
    target_match_mode: LatinTargetMatchMode = "exact"

class LatinMvpSourcePack(BaseModel):
    entries: list[LatinMvpSourcePackEntry]

def load_latin_mvp_source_pack(path: Path | None = None) -> LatinMvpSourcePack
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing grammar-contract tests</name>
  <files>tests/services/test_latin_source_pack.py</files>
  <behavior>
    - Test 1: a synthetic 50-entry pack with `morphology_evidence` and `gramatica` validates.
    - Test 2: blank morphology fields, missing syntactic function, and unresolved grammar review status are rejected per GRAM-01 and GRAM-04.
    - Test 3: invalid case labels or spelling `Genetivus` are rejected; valid labels include exactly `Nominativus`, `Vocativus`, `Accusativus`, `Genitivus`, `Dativus`, `Ablativus` per GRAM-03.
    - Test 4: `gramatica` must use approved short abbreviations (`subst`, `adj`, `v`, `sg`, `pl`, `Suj`, `OD`, `OI`) and must reject long labels like `substantivo`, English labels, and unapproved case text per GRAM-02.
  </behavior>
  <action>Update the test helper `_entry()` so generated fixture entries include a realistic `morphology_evidence` object and `gramatica` string. Add parametrized failing tests before implementation. Keep the tests focused on `LatinMvpSourcePack.model_validate()` and `validate_latin_gramatica()` so they run independently of the committed JSON asset.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_source_pack.py -q</automated>
  </verify>
  <done>The new tests fail for missing symbols/validation before production code is changed, and existing source-pack tests remain in the same file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement morphology evidence and `Gramatica` validation</name>
  <files>src/multilang/services/latin_source_pack.py, tests/services/test_latin_source_pack.py</files>
  <behavior>
    - `LatinMorphologyEvidence` carries lemma, part of speech, optional case label, optional verbal analysis, optional number, syntactic function, evidence note, and grammar review status.
    - Nominal entries require a valid case label and number; verbal entries require verbal analysis; every entry requires syntactic function and non-blank evidence.
    - Only `approved` grammar evidence is accepted by the frozen loader in this phase; unresolved/ambiguous values raise `ValueError`/Pydantic validation errors so GRAM-04 fails closed.
    - `validate_latin_gramatica()` accepts concise abbreviations and required case vocabulary, including `Genitivus`, and rejects unapproved spellings or long learner-facing labels.
  </behavior>
  <action>In `latin_source_pack.py`, add literals for case labels, part-of-speech labels, grammar review status, and grammar abbreviations. Add `LatinMorphologyEvidence` and `validate_latin_gramatica()`, then add required `morphology_evidence: LatinMorphologyEvidence` and `gramatica: str` fields to `LatinMvpSourcePackEntry`. Enforce: no blank text, `grammar_review_status == "approved"`, `case_label` only from the six approved Latin labels, `Genitivus` not `Genetivus`, `number` only `sg`/`pl` when present, and `gramatica` contains only concise tokens from the approved vocabulary plus Latin case labels and simple punctuation. Do not add review workflow, Portuguese translation fields, audio fields, or export fields in this plan; those are later phases.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_source_pack.py -q</automated>
  </verify>
  <done>Focused source-pack tests pass and the loader contract now requires scanner-readable grammar evidence for every entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON asset → Pydantic loader | Grammar strings and morphology fields are data input that can corrupt generated learner cards if not validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | `LatinMvpSourcePackEntry` | mitigate | Validate all morphology fields and `gramatica` with Pydantic before service consumption. |
| T-24-02 | I | loader error messages | mitigate | Preserve existing concise `ValueError` handling so local temp paths and private file paths are not leaked. |
| T-24-03 | R | grammar evidence provenance | mitigate | Require non-blank `evidence_note` and approved review status for each entry. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_source_pack.py -q`.
</verification>

<success_criteria>
- GRAM-01 fields exist in the typed source-pack entry contract.
- GRAM-02 `Gramatica` validation rejects long/unapproved labels.
- GRAM-03 case labels are constrained to the required Latin vocabulary with `Genitivus`.
- GRAM-04 unresolved morphology fails closed at loader validation.
</success_criteria>

<output>
After completion, create `.planning/phases/24-morphology-evidence-and-gramatica-gate/24-01-SUMMARY.md`.
</output>
