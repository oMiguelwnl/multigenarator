---
phase: 27-latin-audio-policy-and-integrity
plan: 05
type: execute
wave: 5
depends_on: [27-04]
files_modified:
  - src/multilang/services/latin_mvp.py
  - src/multilang/cli.py
  - tests/services/test_latin_mvp.py
  - tests/cli/test_generate_latin_mvp_command.py
  - tests/integration/test_v20_latin_audio_evidence.py
autonomous: true
requirements: [AUD-01, AUD-02, AUD-03, AUD-04]
must_haves:
  truths:
    - "User can inspect public Latin audio policy and manifest-readiness counts from the CLI."
    - "Scanner-readable Phase 27 evidence maps all AUD requirements to executable assertions."
    - "Existing Latin MVP default output remains provider-free unless audio JSON is requested."
  artifacts:
    - path: "src/multilang/services/latin_mvp.py"
      provides: "Optional audio manifest summary attachment to Latin MVP service result"
    - path: "src/multilang/cli.py"
      provides: "`generate-latin-mvp --audio-json` public inspection output"
    - path: "tests/integration/test_v20_latin_audio_evidence.py"
      provides: "Scanner-readable Phase 27 AUD evidence and no-scope-creep checks"
  key_links:
    - from: "src/multilang/cli.py"
      to: "src/multilang/services/latin_mvp.py"
      via: "--audio-json calls service with include_audio_summary=True"
      pattern: "audio_summary"
    - from: "tests/integration/test_v20_latin_audio_evidence.py"
      to: "data/latin_mvp/latin-mvp-50-v1-audio.json"
      via: "real asset evidence"
      pattern: "PHASE_27_REQUIREMENTS"
---

<objective>
Expose Latin audio readiness through the Latin MVP service/CLI and add scanner-readable Phase 27 evidence.

Purpose: Phase 28 needs a public, safe way to inspect audio status and evidence without reading private paths, raw provider logs, or unreviewed sample details.
Output: Optional `--audio-json` CLI output and integration evidence proving AUD-01 through AUD-04.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-portuguese-translation-quality/26-03-SUMMARY.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-04-SUMMARY.md
@src/multilang/services/latin_mvp.py
@src/multilang/cli.py
@src/multilang/services/latin_audio.py
@tests/cli/test_generate_latin_mvp_command.py

<interfaces>
Existing `LatinMvpGenerationService.start` signature:
```python
def start(self, request: LatinGenerationRequest, *, include_portuguese_translation_summary: bool = False) -> LatinMvpStartResult: ...
```

Extend it to:
```python
def start(self, request: LatinGenerationRequest, *, include_portuguese_translation_summary: bool = False, include_audio_summary: bool = False) -> LatinMvpStartResult: ...
```
Add `audio_summary: dict[str, object] | None = None` to `LatinMvpStartResult` and include it in `manifest_summary()` only when requested.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan coverage |
|---|---|---|
| GOAL | User can inspect approved playable audio metadata and exact-text checks | `--audio-json` prints public summary counts and readiness status. |
| REQ | AUD-01 sample comparison evidence | Integration evidence asserts playback review artifact exists and policy is represented. |
| REQ | AUD-02 approved audio/export blocking | Evidence asserts readiness summary and blocked/approved counts from real manifest. |
| REQ | AUD-03 metadata | Evidence asserts required metadata fields exist for every real artifact. |
| REQ | AUD-04 text mismatch blocks export | Evidence calls readiness mutation tests or direct validator assertions. |
| RESEARCH | Avoid raw provider secrets/local paths | CLI output exposes aggregate counts/statuses only. |
| CONTEXT | Existing Latin MVP default output should remain stable | Default `generate-latin-mvp` remains provider-free; audio summary is opt-in like Portuguese summary. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional audio summary to service and CLI</name>
  <files>src/multilang/services/latin_mvp.py, src/multilang/cli.py, tests/services/test_latin_mvp.py, tests/cli/test_generate_latin_mvp_command.py</files>
  <behavior>
    - Test 1: default `generate-latin-mvp` output is unchanged and does not load audio manifest.
    - Test 2: `generate-latin-mvp --audio-json` returns JSON with `audio_summary` containing manifest version, entry count, word/sentence counts, approved count, blocked count, provider counts, playback status counts, readiness status, and no absolute paths.
    - Test 3: `--audio-json` and `--portuguese-json` can both be requested and include both summaries in one public JSON payload.
  </behavior>
  <action>Extend `LatinMvpGenerationService` with optional audio manifest loading through `load_latin_audio_manifest()` and `summarize_latin_audio_manifest()`. Add a Typer `--audio-json` flag to `generate-latin-mvp`. Print public aggregate summary only; do not print translation text, full generated Latin sentence lists, media local paths, provider raw responses, or secrets.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py -q</automated>
  </verify>
  <done>CLI audio summary is opt-in, scanner-readable, path-safe, and can coexist with the Portuguese QA summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scanner-readable Phase 27 evidence and boundary checks</name>
  <files>tests/integration/test_v20_latin_audio_evidence.py</files>
  <behavior>
    - Test 1: `PHASE_27_REQUIREMENTS == ("AUD-01", "AUD-02", "AUD-03", "AUD-04")`.
    - Test 2: evidence loads the real source pack, curation asset, playback review artifact, and audio manifest.
    - Test 3: evidence proves every card has approved word and sentence audio or explicit blocked status tied to rejected playback review.
    - Test 4: evidence proves Latin remains outside modern `SupportedLanguage` and no Phase 28 export fields/templates are introduced here.
  </behavior>
  <action>Create scanner-readable evidence that maps each AUD requirement to executable assertions over committed assets and public CLI/service summaries. Include no-scope-creep checks: do not create Latin APKG/CSV/TSV export templates, do not approve translation gates unless already approved by Phase 26/curation policy, and do not add `la` to normal frequency generation.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_audio_evidence.py -q</automated>
  </verify>
  <done>Phase 27 evidence passes and proves AUD-01 through AUD-04 without crossing into Phase 28 export implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| manifest/curation assets → CLI JSON | CLI output must be safe and aggregate-only. |
| Phase 27 evidence → Phase 28 export | Evidence must prove audio readiness without implementing export scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-27-05-01 | Information Disclosure | `--audio-json` | mitigate | Tests assert no absolute paths, provider raw responses, or secrets in CLI output. |
| T-27-05-02 | Elevation of Privilege | Latin language routing | mitigate | Evidence asserts `la` is not accepted by existing modern frequency `generate` command. |
| T-27-05-03 | Tampering | summary readiness | mitigate | Service summary derives readiness from validated manifest, not caller-supplied flags. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py tests/integration/test_v20_latin_audio_evidence.py -q`.
</verification>

<success_criteria>
- `generate-latin-mvp --audio-json` exposes safe public audio readiness counts.
- Scanner-readable evidence covers AUD-01/AUD-02/AUD-03/AUD-04.
- Existing Latin MVP default and modern-language generation boundaries remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/27-latin-audio-policy-and-integrity/27-05-SUMMARY.md`.
</output>
