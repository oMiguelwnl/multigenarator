---
phase: 27-latin-audio-policy-and-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/multilang/services/latin_audio.py
  - tests/services/test_latin_audio.py
autonomous: true
requirements: [AUD-02, AUD-03, AUD-04]
must_haves:
  truths:
    - "Latin audio records distinguish word audio from sentence audio."
    - "Latin audio metadata records provider, provider version, voice, pronunciation policy, generated text, text hash, review status, and fallback reason."
    - "Latin audio integrity rejects stale word or sentence audio when persisted text differs from the exported Latin text."
  artifacts:
    - path: "src/multilang/services/latin_audio.py"
      provides: "Latin audio metadata contracts, manifest loader, summary, and integrity validators"
      exports: ["LatinAudioArtifact", "LatinAudioManifest", "assert_latin_audio_manifest_export_ready"]
    - path: "tests/services/test_latin_audio.py"
      provides: "Focused AUD-02/AUD-03/AUD-04 contract tests"
  key_links:
    - from: "src/multilang/services/latin_audio.py"
      to: "src/multilang/services/latin_review.py"
      via: "item_key/source-pack aligned review gate semantics"
      pattern: "audio_review_status.*approved"
    - from: "src/multilang/services/latin_audio.py"
      to: "data/latin_mvp/latin-mvp-50-v1.json"
      via: "manifest validation against source-pack target_form and latin_sentence"
      pattern: "target_form.*latin_sentence"
---

<objective>
Define the Latin audio contract and fail-closed integrity rules that later plans will use to generate, approve, and expose word/sentence audio.

Purpose: Phase 27 cannot approve learner-ready audio unless every audio artifact has auditable metadata and exact-text checks for both the target form and the displayed Latin sentence.
Output: A tested `latin_audio.py` service module with Pydantic contracts, manifest validation, summary counts, and export-readiness assertions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/25-latin-review-gates-and-curated-records/25-01-SUMMARY.md
@.planning/phases/25-latin-review-gates-and-curated-records/25-02-SUMMARY.md
@.planning/phases/26-portuguese-translation-quality/26-03-SUMMARY.md
@src/multilang/services/latin_review.py
@src/multilang/services/latin_source_pack.py
@tests/services/test_audio_integrity.py

<interfaces>
From `src/multilang/services/latin_review.py`:
```python
LatinReviewStatus = Literal["needs_review", "approved", "rejected"]
class LatinReviewGate(BaseModel):
    status: LatinReviewStatus
    reason: str | None = None
    reviewed_by: str | None = None
    reviewed_at: str | None = None
class LatinCuratedRecord(BaseModel):
    item_key: str
    target_form: str
    latin_sentence: str
    audio_gate: LatinReviewGate
def load_latin_curated_records(path: Path | None = None, source_pack_path: Path | None = None) -> list[LatinCuratedRecord]: ...
```

From `src/multilang/services/latin_source_pack.py` executor should inspect existing exports before implementation; the loader exposes 50 entries with `item_key`, `target_form`, `latin_sentence`, and `source_pack_version="latin-mvp-50-v1"`.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan coverage |
|---|---|---|
| GOAL | Approved playable Latin word/sentence audio with provider metadata and exact-text checks | This plan creates metadata and exact-text checks; later plans create samples/assets and evidence. |
| REQ | AUD-02 approved playable word and sentence audio blocks missing/unapproved audio | `assert_latin_audio_manifest_export_ready` fails unless each record has approved word and sentence artifacts. |
| REQ | AUD-03 metadata fields | `LatinAudioArtifact` stores provider, provider version, voice, pronunciation policy, generated text, text hash, audio kind, playback review status, fallback reason. |
| REQ | AUD-04 text mismatch blocks export | Validators compare word artifacts to `target_form` and sentence artifacts to `latin_sentence`. |
| RESEARCH | eSpeak NG is the verified Latin-capable provider; Azure is experimental only | Contract permits `espeak-ng` as approved provider and requires fallback reasons for experimental/blocked providers. |
| CONTEXT | v2.0 Classical Latin only; no scale beyond 50; approved-only export gates | Contract validates exactly the frozen 50-card source-pack shape and approved review statuses. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Latin audio artifact and manifest contracts</name>
  <files>src/multilang/services/latin_audio.py, tests/services/test_latin_audio.py</files>
  <behavior>
    - Test 1: a word artifact requires `audio_kind="word"`, nonblank `generated_text`, matching `text_hash`, provider, provider version, voice, pronunciation policy, playback review status, and storage path.
    - Test 2: a sentence artifact requires `audio_kind="sentence"` with the same metadata fields.
    - Test 3: missing fallback reason is allowed only when no fallback was used; fallback/blocked provider records require a public reason.
  </behavior>
  <action>Create `src/multilang/services/latin_audio.py` with Pydantic models `LatinAudioArtifact`, `LatinAudioPair`, `LatinAudioManifest`, and `LatinAudioSummary`. Use literal statuses `needs_playback_review`, `approved`, `rejected`, and `blocked`; use providers `espeak-ng` and `azure-multilingual-experimental`. Compute and validate SHA-256 hashes over normalized `generated_text`. Do not add Latin to `SupportedLanguage` or the global Azure voice registry because Phase 22 locked Latin as a separate `latin-mvp` path.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_audio.py -q</automated>
  </verify>
  <done>`LatinAudioArtifact` and manifest models reject blank metadata, stale text hashes, unsupported audio kinds/statuses/providers, and fallback records without reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce source-pack aligned export-readiness integrity</name>
  <files>src/multilang/services/latin_audio.py, tests/services/test_latin_audio.py</files>
  <behavior>
    - Test 1: a manifest with approved word and sentence artifacts for every source-pack entry passes readiness.
    - Test 2: missing audio, `needs_playback_review`, `rejected`, or `blocked` artifacts fail with item_key and audio kind diagnostics.
    - Test 3: word generated text must equal `target_form`; sentence generated text must equal `latin_sentence` after whitespace normalization; mismatches fail export readiness.
  </behavior>
  <action>Add `load_latin_audio_manifest()`, `summarize_latin_audio_manifest()`, `assert_latin_audio_manifest_export_ready()`, and text-normalization helpers. Validate manifest item order and source-pack version against `load_latin_mvp_source_pack()`. Preserve public diagnostics only: include item_key, audio kind, and field names, but never local absolute paths or provider secrets.</action>
  <verify>
    <automated>python -m pytest tests/services/test_latin_audio.py -q</automated>
  </verify>
  <done>Readiness fails closed for every AUD-02/AUD-04 violation and passes only when all 50 source-pack entries have approved exact-text word and sentence audio metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed audio manifest → export gate | Manifest data is trusted only after Pydantic validation and source-pack exact-text checks. |
| provider metadata → CLI/evidence output | Provider/version/path metadata may expose local paths or secrets if not redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-27-01-01 | Tampering | `LatinAudioManifest` | mitigate | Recompute SHA-256 text hashes and compare artifact text to source-pack target/sentence before export readiness passes. |
| T-27-01-02 | Information Disclosure | diagnostics/evidence | mitigate | Emit item_key/audio_kind/field diagnostics only; tests assert no `C:\\` or `/Users/` in errors. |
| T-27-01-03 | Spoofing | provider metadata | mitigate | Restrict provider enum to approved labels and require provider version plus voice for every artifact. |
</threat_model>

<verification>
Run `python -m pytest tests/services/test_latin_audio.py -q` and confirm all new tests pass without requiring eSpeak NG or network credentials.
</verification>

<success_criteria>
- `src/multilang/services/latin_audio.py` exists and exports the contract/readiness functions.
- Missing, unapproved, stale, or wrong-kind Latin audio fails readiness.
- Approved exact-text word and sentence artifacts pass readiness against the frozen 50-card source pack.
</success_criteria>

<output>
After completion, create `.planning/phases/27-latin-audio-policy-and-integrity/27-01-SUMMARY.md`.
</output>
