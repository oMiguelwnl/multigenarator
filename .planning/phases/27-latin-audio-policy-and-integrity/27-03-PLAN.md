---
phase: 27-latin-audio-policy-and-integrity
plan: 03
type: execute
wave: 3
depends_on: [27-02]
files_modified:
  - .planning/phases/27-latin-audio-policy-and-integrity/27-AUDIO-PLAYBACK-REVIEW.md
autonomous: false
requirements: [AUD-01]
must_haves:
  truths:
    - "User compares representative Latin audio samples before the final MVP audio policy is locked."
    - "Playback review records the selected provider, pronunciation policy, approval status, and fallback caveats."
    - "Unapproved or rejected sample results prevent later plans from marking full MVP audio as approved."
  artifacts:
    - path: ".planning/phases/27-latin-audio-policy-and-integrity/27-AUDIO-PLAYBACK-REVIEW.md"
      provides: "Human playback review decision for Phase 27 audio policy"
  key_links:
    - from: ".planning/phases/27-latin-audio-policy-and-integrity/27-AUDIO-PLAYBACK-REVIEW.md"
      to: "data/latin_mvp/latin-mvp-50-v1-audio.json"
      via: "policy and review status consumed by full manifest generation"
      pattern: "selected_provider.*espeak-ng"
---

<objective>
Pause for the required human playback evaluation of representative Latin audio samples before approving the MVP audio policy.

Purpose: Research says eSpeak NG supports Latin but quality is uncertain, and Azure has no verified native Latin locale. AUD-01 requires evaluation before policy lock.
Output: A committed playback review artifact that later plans consume when marking full MVP audio approved or blocked.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-02-SUMMARY.md
@src/multilang/services/latin_audio_samples.py
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan coverage |
|---|---|---|
| GOAL | Approved playable Latin audio with honest provider caveats | Human playback review records whether eSpeak NG `la` is approved for the MVP and what fallback caveats remain. |
| REQ | AUD-01 candidate provider comparison before policy lock | Blocking checkpoint requires sample listening and provider selection before full manifest approval. |
| RESEARCH | eSpeak quality uncertain; Azure native Latin not verified | Review template asks about pronunciation acceptability and records Azure fallback as unsupported unless tested. |
| CONTEXT | Final learner-ready export is approved-only | Later full audio approval must reference this review artifact. |
</source_audit>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Review representative Latin audio samples and lock policy</name>
  <files>.planning/phases/27-latin-audio-policy-and-integrity/27-AUDIO-PLAYBACK-REVIEW.md</files>
  <action>Before asking the user, generate or locate the Plan 27-02 sample manifest and sample WAV files. Create `27-AUDIO-PLAYBACK-REVIEW.md` with sample paths, provider candidates, the representative word/sentence list, and empty review fields. Present the user with exact files to play and ask for approval/rejection of `espeak-ng` voice `la` under pronunciation policy `classical_approx`. If samples cannot be generated because eSpeak NG is absent, record `selected_provider=blocked` and the missing binary reason instead of approving audio.</action>
  <what-built>Plan 27-02 created deterministic sample metadata and eSpeak NG sample generation. This checkpoint records the user's playback decision.</what-built>
  <how-to-verify>
    1. Open the generated sample manifest path reported by the executor, usually `.multilang/latin-audio-samples/latin-audio-samples.json`.
    2. Play representative word files for `virum`, `puella`, `caesar`, `cicero`, `veni`, `quae`, `cum`, and `Romae`.
    3. Play representative sentence audio, including `Arma virumque cano.`.
    4. Confirm whether eSpeak NG `la` is acceptable for the 50-card Classical Latin MVP with the caveat `pronunciation_policy=classical_approx`, or reject it with the reason.
  </how-to-verify>
  <verify>
    <automated>python -m pytest tests/services/test_latin_audio_samples.py -q</automated>
    <manual>User types `approved espeak-ng classical_approx` or gives rejection details.</manual>
  </verify>
  <resume-signal>Type `approved espeak-ng classical_approx` to proceed, or describe rejection/blocking reason.</resume-signal>
  <done>`27-AUDIO-PLAYBACK-REVIEW.md` exists and records selected_provider, selected_voice, pronunciation_policy, playback_review_status, reviewer, reviewed_at, representative sample list, and fallback caveats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated samples → human approval | Technical playback does not equal pedagogical acceptability. |
| human review artifact → automated manifest approval | Later automation must not infer approval without explicit review fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-27-03-01 | Repudiation | playback review artifact | mitigate | Record reviewer identifier, reviewed_at, sample list, selected provider/voice, status, and reasons. |
| T-27-03-02 | Tampering | policy handoff | mitigate | Later plans must parse this artifact and fail closed if status is not approved. |
| T-27-03-03 | Information Disclosure | sample paths | mitigate | Store repository-relative or `.multilang` relative paths only; no absolute local paths. |
</threat_model>

<verification>
Automated sample tests pass and the human review artifact explicitly approves or blocks the selected policy.
</verification>

<success_criteria>
- User has evaluated representative samples.
- Audio policy is either approved with `espeak-ng/la/classical_approx` or blocked with a concrete reason.
- Later plans have a concrete artifact to consume; no unreviewed policy can be treated as approved.
</success_criteria>

<output>
After completion, create `.planning/phases/27-latin-audio-policy-and-integrity/27-03-SUMMARY.md`.
</output>
