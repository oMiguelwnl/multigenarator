---
phase: 27-latin-audio-policy-and-integrity
plan: 06
type: execute
wave: 6
depends_on: [27-01, 27-02, 27-04, 27-05]
files_modified:
  - src/multilang/services/latin_audio.py
  - tests/services/test_latin_audio.py
  - tests/services/test_latin_audio_samples.py
autonomous: true
gap_closure: true
requirements: [AUD-01, AUD-02, AUD-03, AUD-04]
must_haves:
  truths:
    - "Approved Latin audio cannot pass export readiness unless word and sentence media paths are repository-relative, path-safe, existing, nonempty, and playable enough to identify as media."
    - "Focused Phase 27 tests collect and run reliably from a clean checkout without importing one test module through the non-package tests namespace."
  artifacts:
    - path: "src/multilang/services/latin_audio.py"
      provides: "Fail-closed storage_path and media-file validation inside Latin audio export readiness"
      exports: ["assert_latin_audio_manifest_export_ready", "summarize_latin_audio_manifest"]
    - path: "tests/services/test_latin_audio.py"
      provides: "Focused blockers for absolute, unsafe, missing, empty, and non-media Latin audio storage paths"
    - path: "tests/services/test_latin_audio_samples.py"
      provides: "Self-contained sample tests that no longer import FakeRunner from tests.services"
  key_links:
    - from: "src/multilang/services/latin_audio.py"
      to: "artifact.storage_path"
      via: "readiness validation before export approval"
      pattern: "storage_path"
    - from: "tests/services/test_latin_audio_samples.py"
      to: "EspeakNgSpeechAdapter(runner=FakeRunner())"
      via: "local fake runner definition in same test file"
      pattern: "class FakeRunner"
---

<objective>
Close Phase 27 verification gaps by making Latin audio export readiness fail closed on unsafe or missing media files and by making the focused Phase 27 pytest suite collect reliably.

Purpose: AUD-02 requires missing audio to block learner-ready export, and AUD-01/AUD-03 evidence must be executable without environment-specific test package imports.
Output: Patched readiness validator, targeted regression tests, and self-contained sample tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-VERIFICATION.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-01-SUMMARY.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-02-SUMMARY.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-04-SUMMARY.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-05-SUMMARY.md
@src/multilang/services/latin_audio.py
@tests/services/test_latin_audio.py
@tests/services/test_latin_audio_samples.py
@tests/services/test_espeak_ng_speech_adapter.py

<interfaces>
From `src/multilang/services/latin_audio.py`:
```python
class LatinAudioArtifact(BaseModel):
    audio_kind: Literal["word", "sentence"]
    provider: Literal["espeak-ng", "azure-multilingual-experimental"]
    provider_version: str
    voice: str
    pronunciation_policy: str
    generated_text: str
    text_hash: str
    playback_review_status: Literal["needs_playback_review", "approved", "rejected", "blocked"]
    storage_path: str
    fallback_reason: str | None = None

def summarize_latin_audio_manifest(manifest: LatinAudioManifest) -> LatinAudioSummary: ...
def assert_latin_audio_manifest_export_ready(manifest: LatinAudioManifest) -> None: ...
```

Current gap: `_readiness_issues()` validates status/text/hash but does not validate `storage_path` safety, file existence, file size, or media marker.

From `tests/services/test_espeak_ng_speech_adapter.py`:
```python
@dataclass(frozen=True)
class FakeCompletedProcess:
    returncode: int = 0
    stdout: str = ""
    stderr: str = ""

class FakeRunner:
    def __call__(self, command: list[str], *, input_text: str | None = None) -> FakeCompletedProcess: ...
```

Current gap: `tests/services/test_latin_audio_samples.py` imports this through `from tests.services...`, but `tests` is not importable as a package in the verification environment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail closed on unsafe or missing Latin audio media paths</name>
  <files>src/multilang/services/latin_audio.py, tests/services/test_latin_audio.py</files>
  <behavior>
    - Absolute `storage_path` values block export readiness and diagnostics expose only `item_key`, `audio_kind`, and `field=storage_path`.
    - Path traversal such as `../outside.wav` blocks export readiness and does not leak resolved local paths.
    - Repository-relative paths that do not exist block export readiness.
    - Existing but empty files block export readiness.
    - Existing nonempty files without a media marker block export readiness.
    - Existing nonempty RIFF/WAV files referenced by approved exact-text artifacts pass readiness.
  </behavior>
  <action>Add tests first in `tests/services/test_latin_audio.py` for absolute, path-traversal, missing, empty, non-media, and valid RIFF WAV storage paths. Then update `src/multilang/services/latin_audio.py` so export readiness validates each approved artifact's `storage_path` before Phase 28 can export it. Treat a valid storage path as repository-relative, not absolute, not containing `..` traversal, resolving under the repository root, existing, regular file, nonempty, and beginning with a known media marker for the committed Phase 27 WAV evidence (`RIFF`). Keep diagnostics public per existing Phase 27 decisions: include `item_key`, `audio_kind`, and `field=storage_path` (or a similarly non-sensitive field token), but never include absolute paths, home directories, provider raw details, or local filesystem locations. If adding optional `repo_root: Path | None = None` parameters to internal helpers or public summary/readiness functions is the cleanest way to make tests deterministic, keep defaults backward-compatible with current callers.</action>
  <verify>
    <automated>uv run pytest tests/services/test_latin_audio.py -q</automated>
  </verify>
  <done>`assert_latin_audio_manifest_export_ready()` rejects unsafe, missing, empty, and non-media paths; accepts committed-style RIFF WAV paths; and all focused Latin audio contract tests pass with public-only error messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make focused Phase 27 sample tests self-contained and green</name>
  <files>tests/services/test_latin_audio_samples.py</files>
  <behavior>
    - `tests/services/test_latin_audio_samples.py` has no `from tests.services...` import.
    - The sample tests still use deterministic fake eSpeak NG version, voices, command capture, and RIFF fake WAV output.
    - The full focused Phase 27 verification command collects all listed test modules and passes.
  </behavior>
  <action>Remove the package-style import of `FakeRunner` from `tests.services.test_espeak_ng_speech_adapter`. Define the small fake completed-process and fake runner needed by the sample tests directly in `tests/services/test_latin_audio_samples.py`, or move them to an importable non-test helper only if that avoids broader churn. Preserve the existing sample assertions for representative words, sample hashes, repository-relative `.multilang/latin-audio-samples/` paths, and Azure blocked metadata. Do not change provider policy decisions: eSpeak NG `la` under `classical_approx` remains the approved local candidate, and Azure remains blocked without a verified native Classical Latin/`la` voice.</action>
  <verify>
    <automated>PATH="/c/Program Files/eSpeak NG:$PATH" uv run pytest tests/services/test_latin_audio.py tests/services/test_espeak_ng_speech_adapter.py tests/services/test_latin_audio_samples.py tests/services/test_latin_mvp.py tests/cli/test_generate_latin_mvp_command.py tests/integration/test_v20_latin_audio_asset.py tests/integration/test_v20_latin_audio_evidence.py -q</automated>
  </verify>
  <done>The focused Phase 27 suite collects without `ModuleNotFoundError` and passes, proving AUD-01 through AUD-04 evidence is executable after the storage-path readiness fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio manifest JSON -> export readiness | Committed manifest fields can be stale, unsafe, or maliciously edited before export. |
| test runner -> native-tool fakes | Test-only fake process output must not hide production provider/network behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Tampering | `LatinAudioArtifact.storage_path` | mitigate | Reject absolute paths, traversal, paths resolving outside repo root, missing files, empty files, and files lacking a RIFF media marker during export readiness. |
| T-27-06-02 | Information Disclosure | readiness diagnostics | mitigate | Preserve Phase 27 public-only diagnostics: report item/key/kind/field tokens only; assert no absolute paths or home directories appear in tests. |
| T-27-06-03 | Repudiation | focused evidence suite | mitigate | Ensure the full focused Phase 27 pytest command collects and passes from a clean checkout so verification failures are reproducible. |
</threat_model>

<verification>
Run the focused Phase 27 command from Task 2. Then re-run `/gsd-verify-phase 27` or the equivalent plan-checker/verification step; expected result is all Phase 27 must-haves verified with no remaining gaps.
</verification>

<success_criteria>
- AUD-02 gap is closed: an approved exact-text artifact with unsafe or missing `storage_path` cannot pass export readiness.
- AUD-01/AUD-03 evidence gap is closed: sample tests no longer depend on `tests` being an importable package.
- Error messages remain scanner-readable and privacy-safe.
- No new external services, network calls, provider changes, or Phase 28 export implementation are introduced.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Approved playable Latin word and sentence audio with export-blocking integrity checks | Task 1 adds media-path/file validation; Task 2 restores executable focused evidence. |
| REQ AUD-01 | Compare/evaluate candidate Latin TTS providers before policy lock | Task 2 preserves sample/provider comparison tests and makes them executable. |
| REQ AUD-02 | Approved playable word/sentence audio; missing/failed/unapproved blocks export | Task 1 directly closes missing/unsafe media readiness blockers. |
| REQ AUD-03 | Store provider/version/voice/policy/text/hash/kind/status/fallback metadata | Task 2 preserves sample metadata coverage; Task 1 keeps artifact contract unchanged. |
| REQ AUD-04 | Block export on persisted audio text mismatch | Task 1 preserves existing text/hash checks while adding storage validation. |
| VERIFICATION gap 1 | `storage_path` safety/existence/nonempty/media validation missing | Task 1 covers all missing items. |
| VERIFICATION gap 2 | Focused suite fails collection due `tests.services` import | Task 2 covers all missing items. |
| CONTEXT decisions | Public-only diagnostics; eSpeak NG `la`/`classical_approx`; Azure blocked | Both tasks explicitly preserve these locked Phase 27 decisions. |
| Deferred | Latin scale, future higher-quality/human-recorded audio, Phase 28 export implementation | Excluded from this gap closure plan. |
</source_audit>

<output>
After completion, create `.planning/phases/27-latin-audio-policy-and-integrity/27-06-SUMMARY.md`
</output>
