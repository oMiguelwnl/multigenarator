---
phase: 27-latin-audio-policy-and-integrity
plan: 04
type: execute
wave: 4
depends_on: [27-03]
files_modified:
  - data/latin_mvp/latin-mvp-50-v1-audio.json
  - data/latin_mvp/audio/latin-mvp-50-v1/
  - data/latin_mvp/latin-mvp-50-v1-curation.json
  - tests/integration/test_v20_latin_audio_asset.py
autonomous: true
requirements: [AUD-02, AUD-03, AUD-04]
user_setup:
  - service: espeak-ng
    why: "Synthesize 100 playable MVP clips: one word and one sentence file for each of the 50 Latin records."
    env_vars: []
    dashboard_config:
      - task: "Install eSpeak NG 1.52+ and ensure `espeak-ng` is on PATH before executing this plan."
        location: "Local workstation package manager or eSpeak NG release installer"
must_haves:
  truths:
    - "Every final Latin MVP card has approved playable word audio and sentence audio."
    - "Every audio artifact has provider, provider version, voice, pronunciation policy, generated text, text hash, audio kind, playback review status, and fallback reason when applicable."
    - "Latin export readiness remains blocked when persisted audio text differs from target form or Latin sentence."
  artifacts:
    - path: "data/latin_mvp/latin-mvp-50-v1-audio.json"
      provides: "50-card word/sentence audio manifest with approved playback metadata"
    - path: "data/latin_mvp/latin-mvp-50-v1-curation.json"
      provides: "Audio review gates updated according to the approved playback policy"
    - path: "tests/integration/test_v20_latin_audio_asset.py"
      provides: "AUD-02/AUD-03/AUD-04 integration evidence over real assets"
  key_links:
    - from: "data/latin_mvp/latin-mvp-50-v1-audio.json"
      to: "data/latin_mvp/latin-mvp-50-v1.json"
      via: "item_key, target_form, latin_sentence, source_pack_version alignment"
      pattern: "latin-mvp-0050"
    - from: "data/latin_mvp/latin-mvp-50-v1-curation.json"
      to: "src/multilang/services/latin_review.py"
      via: "audio_gate approved status participates in export readiness"
      pattern: "audio_gate.*approved"
---

<objective>
Generate and validate the full 50-card Latin MVP audio manifest, then approve audio gates only when the playback policy from Plan 27-03 allows it.

Purpose: Phase 27 must leave Phase 28 with exact-text, approved, playable word and sentence audio for all learner-ready Latin cards.
Output: A full audio manifest asset, updated curation audio gates, and integration tests proving AUD-02/AUD-03/AUD-04.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-01-SUMMARY.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-02-SUMMARY.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-03-SUMMARY.md
@.planning/phases/27-latin-audio-policy-and-integrity/27-AUDIO-PLAYBACK-REVIEW.md
@src/multilang/services/latin_audio.py
@src/multilang/services/espeak_ng_speech_adapter.py
@src/multilang/services/latin_review.py
@data/latin_mvp/latin-mvp-50-v1-curation.json

<interfaces>
From Plan 27-01 `src/multilang/services/latin_audio.py`:
```python
def load_latin_audio_manifest(path: Path | None = None, source_pack_path: Path | None = None) -> LatinAudioManifest: ...
def assert_latin_audio_manifest_export_ready(manifest: LatinAudioManifest, source_pack: LatinMvpSourcePack) -> None: ...
```

From `src/multilang/services/latin_review.py`:
```python
def update_latin_review_gate(records, *, item_key: str, gate: str, status: str, reason: str | None = None, reviewed_by: str | None = None, reviewed_at: str | None = None, force: bool = False) -> list[LatinCuratedRecord]: ...
def write_latin_curated_records(records: list[LatinCuratedRecord], path: Path) -> None: ...
```
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan coverage |
|---|---|---|
| GOAL | Approved playable word and sentence audio for every final Latin MVP card | Creates a 50-record manifest with 100 approved artifacts and updates curation audio gates. |
| REQ | AUD-02 approved playable audio blocks missing/unapproved audio | Integration tests prove readiness fails on missing/unapproved assets and passes for full approved manifest. |
| REQ | AUD-03 metadata | Each artifact stores all required metadata fields in `latin-mvp-50-v1-audio.json`. |
| REQ | AUD-04 text mismatch blocks export | Integration tests mutate word/sentence text and assert readiness failure. |
| RESEARCH | eSpeak NG `la` is default if human playback accepts it; no silent Azure Latin claim | Manifest uses Plan 27-03 approved provider, or remains blocked if Plan 27-03 rejected samples. |
| CONTEXT | Translation/audio gates must remain pending until their phases approve them | This plan updates only `audio_gate` after Phase 27 playback approval; it does not alter source/translation/grammar decisions. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration tests for full Latin audio asset readiness</name>
  <files>tests/integration/test_v20_latin_audio_asset.py</files>
  <behavior>
    - Test 1: `PHASE_27_REQUIREMENTS = ("AUD-01", "AUD-02", "AUD-03", "AUD-04")` is present for scanner-readable evidence handoff.
    - Test 2: full manifest has 50 item pairs, 50 word artifacts, 50 sentence artifacts, approved playback status, exact text hashes, no absolute paths, and source-pack aligned item order.
    - Test 3: manifest readiness fails when an artifact is missing, unapproved, has stale hash, or word/sentence generated text is changed.
    - Test 4: curation audio gates are approved only when the playback review artifact says approved; source/translation/grammar gates are not changed by this plan.
  </behavior>
  <action>Create the failing integration test first. Use the Plan 27-01 loader/readiness API and the existing `load_latin_curated_records()` helper. Do not mock the full source pack; tests must load committed assets so drift is caught.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_audio_asset.py -q</automated>
  </verify>
  <done>Tests initially fail because the full audio manifest and curation audio gate updates do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Generate approved 50-card audio manifest and update audio gates</name>
  <files>data/latin_mvp/latin-mvp-50-v1-audio.json, data/latin_mvp/audio/latin-mvp-50-v1/, data/latin_mvp/latin-mvp-50-v1-curation.json</files>
  <action>Read `27-AUDIO-PLAYBACK-REVIEW.md`. If it records approved `selected_provider=espeak-ng`, synthesize one word clip from each source-pack `target_form` and one sentence clip from each `latin_sentence` using the eSpeak adapter. Use deterministic media paths `data/latin_mvp/audio/latin-mvp-50-v1/{item_key}-word.wav` and `data/latin_mvp/audio/latin-mvp-50-v1/{item_key}-sentence.wav`, write those repository-relative paths into `data/latin_mvp/latin-mvp-50-v1-audio.json`, and mark each artifact `playback_review_status="approved"` with the review metadata. Then update only the `audio_gate` of each curation record to `approved`, preserving source, translation, grammar, provenance, sequence, and reasons. If the review artifact is blocked/rejected, write a blocked manifest with explicit fallback reasons and keep curation audio gates blocked; do not mark any audio approved.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_v20_latin_audio_asset.py -q</automated>
  </verify>
  <done>Full asset tests pass; approved policy yields 100 playable audio files and 50 approved audio gates, while blocked policy keeps export readiness blocked with explicit reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| playback review artifact → curation gate updates | A text artifact controls whether audio gates can be approved. |
| generated media files → committed manifest | Media path/hash metadata must match actual generated files and source-pack text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-27-04-01 | Tampering | `latin-mvp-50-v1-audio.json` | mitigate | Integration tests mutate text/hash/status and assert readiness fails. |
| T-27-04-02 | Repudiation | curation audio approval | mitigate | Copy reviewer/reviewed_at/policy from `27-AUDIO-PLAYBACK-REVIEW.md` into manifest/gates. |
| T-27-04-03 | Information Disclosure | media paths | mitigate | Store relative paths only and test for absence of Windows/user-home absolute paths. |
| T-27-04-04 | Denial of Service | missing native eSpeak binary | mitigate | If missing, write blocked manifest/gates with reason; do not produce false approval. |
</threat_model>

<verification>
Run `python -m pytest tests/integration/test_v20_latin_audio_asset.py -q`. Also run `python -m pytest tests/services/test_latin_audio.py tests/services/test_espeak_ng_speech_adapter.py tests/services/test_latin_audio_samples.py tests/integration/test_v20_latin_audio_asset.py -q` before summary.
</verification>

<success_criteria>
- `data/latin_mvp/latin-mvp-50-v1-audio.json` aligns one-for-one with the frozen source pack.
- Every approved artifact records all AUD-03 metadata and points to a playable nonempty media file.
- Exact-text mismatch for either word or sentence audio blocks export readiness.
- Curation audio gates reflect the reviewed audio policy without changing other gates.
</success_criteria>

<output>
After completion, create `.planning/phases/27-latin-audio-policy-and-integrity/27-04-SUMMARY.md`.
</output>
