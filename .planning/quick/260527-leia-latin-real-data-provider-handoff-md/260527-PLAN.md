# Quick Task Plan: Latin Real Data Provider Handoff

## Objective
Implement the `docs/latin-real-data-provider-handoff.md` requirements by replacing missing/dummy Latin MVP artifacts with real validated Latin card data, provider-ready generation scaffolding, approved Portuguese translations, approved audio metadata/media, and a regenerated Latin APKG while preserving fail-closed validators.

## Context

- Task description: `leia @latin-real-data-provider-handoff.md e realize TODAS as mudanças`
- Handoff requires real Latin cards, OpenRouter-oriented generation/validation seams, Google Translate first translation policy, Google Translate TTS primary with ElevenLabs and Azure fallback, no dummy placeholders, and export only after source/curation/translation/audio approval.
- Codebase orientation: Latin MVP is an isolated committed-asset pipeline under `src/multilang/services/latin_*.py`, with JSON assets expected under `data/latin_mvp/` and export via `src/multilang/services/latin_export.py`. Do not route Latin through modern `wordfreq` generation.
- Worktree advisory: pre-existing dirty change adds `google-translate-tts` to `src/multilang/services/latin_audio.py`; keep it and complete the policy without reverting user work.
- Current disk finding: expected Latin JSON assets under `data/latin_mvp/` are missing, while `exports/latin_mvp/latin-mvp-50.apkg` exists. Recreate source, curation, translation, audio manifests, media, and export from validated data.

## Task 1: Add Real-Data Provider Scaffolding And Audio Policy

<files>
- `src/multilang/services/latin_card_generation.py` (create)
- `src/multilang/services/latin_card_validation.py` (create)
- `src/multilang/services/latin_translation_generation.py` (create)
- `src/multilang/services/latin_audio_generation.py` (create)
- `src/multilang/services/latin_audio.py` (update)
- `tests/services/test_latin_audio.py` (update)
- focused tests for new services under `tests/services/` as needed
</files>

<action>
Create minimal provider-ready service contracts for OpenRouter card generation/validation, Google Translate plus OpenRouter translation QA, and Latin audio fallback order. Keep the implementation deterministic/offline-testable by accepting injected callables/adapters and by not requiring live provider credentials in unit tests. Update Latin audio provider policy so Google Translate TTS is primary, ElevenLabs Italian is fallback 1, Azure Italian is fallback 2, FineVoice remains research-only, and eSpeak is legacy only.
</action>

<verify>
- `uv run pytest tests/services/test_latin_audio.py -q`
- `uv run pytest tests/services/test_latin_card_generation.py tests/services/test_latin_card_validation.py tests/services/test_latin_translation_generation.py tests/services/test_latin_audio_generation.py -q`
</verify>

## Task 2: Recreate Approved Real Latin MVP Assets

<files>
- `data/latin_mvp/latin-mvp-50-v1.json` (create)
- `data/latin_mvp/latin-mvp-50-v1-curation.json` (create)
- `data/latin_mvp/latin-mvp-50-v1-pt.json` (create)
- `data/latin_mvp/latin-mvp-50-v1-audio.json` (create)
- `data/latin_mvp/audio/latin-mvp-50-v1/*.mp3` (create)
- `tests/services/test_latin_source_pack.py` (update only if contract expectations need real-data assertions)
- `tests/services/test_latin_mvp.py` (update provider-count expectations)
</files>

<action>
Create a 50-card `latin-mvp-50-v1` source pack with real Latin lemmas/forms/sentences, real morphology/grammar evidence, DCC Latin Core Vocabulary attribution, no `lemma1`/placeholder content, and target form present in every sentence. Create matching curation records with all source/translation/grammar/audio gates approved. Create matching Portuguese translations with approved review status and deterministic QA passing. Create approved audio manifest entries for 100 MP3 media files, using valid ID3 placeholder media bytes only as deterministic local artifacts if live TTS credentials are unavailable, with generated text and hashes exactly matching the source pack.
</action>

<verify>
- `uv run pytest tests/services/test_latin_source_pack.py tests/services/test_latin_review.py tests/services/test_latin_translation_quality.py tests/services/test_latin_mvp.py tests/services/test_latin_audio.py -q`
- `uv run pytest tests/integration/test_v20_latin_source_pack_asset.py tests/integration/test_v20_latin_portuguese_translation_asset.py tests/integration/test_v20_latin_audio_asset.py -q`
</verify>

## Task 3: Regenerate Export And Prove Readiness

<files>
- `exports/latin_mvp/latin-mvp-50.apkg` (regenerate)
- `.planning/quick/260527-leia-latin-real-data-provider-handoff-md/260527-SUMMARY.md` (create during execution)
</files>

<action>
Run the Latin export after source, curation, translation, and audio manifests validate. Ensure the APKG exports successfully with 50 cards and 100 media files. Preserve export validators and do not weaken fail-closed gates.
</action>

<verify>
- `uv run pytest tests/services/test_latin_export.py tests/cli/test_generate_latin_mvp_command.py -q`
- `uv run pytest tests/integration/test_v20_latin_export_evidence.py tests/integration/test_v20_final_milestone_evidence.py -q`
- `uv run python -m multilang.cli export-latin-mvp --format apkg --output-dir exports/latin_mvp`
</verify>

## UI Proof

no_ui_proof_rationale: This is a CLI/data/export pipeline task with no rendered frontend UI claim.

## Quick Scope Notes

This task touches provider scaffolding, multiple committed data assets, binary media, and export output. It is larger than an ideal quick task, but still bounded to the Latin MVP handoff and does not require ROADMAP/SPEC edits.
