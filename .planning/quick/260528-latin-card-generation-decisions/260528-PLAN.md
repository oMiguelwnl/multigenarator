# Quick Task 260528 Plan: Latin Card Generation Decisions

## Objective

Align the Latin card generation evidence with the already-recorded provider decisions: Latin frequency remains a curated MVP source pack, Portuguese translation uses the existing Latin translation pack/QA path, definition and grammar stay in the Latin card/source contracts, and Latin audio uses Google Translate TTS (`la`) with ElevenLabs Italian then Azure Italian as fallbacks.

## Context

Task description: Consolidate the user's Latin card generation notes around frequency, translation, definition/grammar, and audio provider decisions.

Approach context: No separate approach clarification was required by config; use the existing repository decisions where present instead of introducing new providers or architecture.

Codebase context: Provisional brownfield baseline. The repo is a Python 3.12+ Typer/Pydantic project with Latin-specific modules already present under `src/multilang/services/latin_*`, source assets under `data/latin_mvp/`, and Latin CLI coverage under `tests/cli/test_generate_latin_mvp_command.py`. Existing handoff documentation in `docs/latin-real-data-provider-handoff.md` already selects Google Translate TTS (`la`) as Latin audio primary, ElevenLabs Italian as fallback 1, Azure Italian as fallback 2, and FineVoice as research-only. The committed audio manifest also uses `google-translate-tts`, while `tests/integration/test_v20_latin_audio_evidence.py` still contains stale `espeak-ng` expectations. Safest surface: update the stale evidence test only. Risky zones to avoid: source-pack content, audio manifests, generated MP3 files, export validators, and public CLI contracts. Re-verify with focused Latin CLI/audio tests.

No UI proof required: this is a CLI/data validation task with no rendered UI claim.

## Tasks

### Task 1: Align stale Latin audio evidence expectations

<files>

- `tests/integration/test_v20_latin_audio_evidence.py`

</files>

<action>

Update the stale Phase 27 audio evidence assertions from `espeak-ng`/`classical_approx` to the already approved Google Translate Latin policy: provider `google-translate-tts`, voice `la`, pronunciation policy `google_translate_latin`, and provider summary `{"google-translate-tts": 100}`.

</action>

<verify>

- `uv run pytest tests/integration/test_v20_latin_audio_evidence.py -q`
- `uv run pytest tests/cli/test_generate_latin_mvp_command.py::test_generate_latin_mvp_audio_json_prints_public_audio_summary -q`

</verify>

### Task 2: Confirm Latin decision consistency across handoff, manifest, and CLI summary

<files>

- `tests/integration/test_v20_latin_audio_evidence.py`

</files>

<action>

Keep the verification scoped to existing artifacts and avoid changing provider policy, generated audio files, or source-pack content. Treat Google Translate TTS as the answer for both `word_audio` and `sentence_audio` unless an artifact is unavailable, in which case the existing fallback order applies. Also remove stale assertions that belonged to the earlier pre-translation/pre-export boundary, while preserving the current boundary that Latin is not part of the modern wordfreq generation path.

</action>

<verify>

- `uv run pytest tests/integration/test_v20_latin_audio_evidence.py tests/cli/test_generate_latin_mvp_command.py::test_generate_latin_mvp_audio_json_prints_public_audio_summary -q`

</verify>

## Plan Self-Check

- At least one task has `<action>` and `<verify>` sections: yes.
- Each task has at least one runnable verification command: yes.
- Task count does not exceed 3: yes, 2 tasks.

## Scope Signal

No quick-scope warning. The plan modifies one existing test file and does not create new public APIs, routes, schema, generated assets, or architecture.
