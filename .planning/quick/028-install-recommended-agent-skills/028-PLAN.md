---
mode: quick
task: 028-install-recommended-agent-skills
no_ui_proof_rationale: This task only installs project-scoped agent skill manifests and has no rendered UI behavior to validate.
---

# Quick Task 028 Plan: Install Recommended Agent Skills

## Objective

Install six recommended skills into `.agents/skills/` for OpenCode discovery while preserving all 14 existing GSDD skills and every unrelated worktree change.

## Must-Haves

- Project-scoped manifests exist for `systematic-debugging`, `test-driven-development`, `azure-ai`, `supabase-postgres-best-practices`, `code-security`, and `llm-security`.
- The Skills CLI is used with the authoritative packages and OpenCode/project scope; no existing skill is overwritten.
- All 14 `.agents/skills/gsdd-*/SKILL.md` manifests remain present and unchanged.
- Application code, existing GSDD skills, `danish-test-deck` deletions, and untracked Japanese files are not modified.

## Task 1: Install And Verify Six Project Skills

<name>Install and verify six project-scoped OpenCode skills</name>

<files>
- `.agents/skills/systematic-debugging/SKILL.md`
- `.agents/skills/test-driven-development/SKILL.md`
- `.agents/skills/azure-ai/SKILL.md`
- `.agents/skills/supabase-postgres-best-practices/SKILL.md`
- `.agents/skills/code-security/SKILL.md`
- `.agents/skills/llm-security/SKILL.md`
- `skills-lock.json` (only if generated by the Skills CLI)
</files>

<action>
- Capture `git status --short` before installation. Confirm the 14 GSDD manifests are present and clean; retain the existing `danish-test-deck` deletion and untracked Japanese entries as a before/after boundary.
- From the repository root, run the Skills CLI with `--agent opencode --copy`, without global scope or unattended overwrite flags: install `systematic-debugging` and `test-driven-development` from `obra/superpowers`, `azure-ai` from `microsoft/azure-skills`, `supabase-postgres-best-practices` from `supabase/agent-skills`, and `code-security` plus `llm-security` from `semgrep/skills`. Review each prompt, keep project scope, and abort rather than replace any existing path.
- Inspect each installed `SKILL.md` for non-empty content and the expected skill identity. Inspect `npx -y skills list --json` and any generated `skills-lock.json` to confirm all six installations are project-scoped.
- Compare final status with the captured baseline. Allow only the six new skill trees, CLI-managed lock metadata, and this quick-task artifact; do not touch application code, GSDD skills, or unrelated Danish/Japanese changes.
</action>

<done>
- All six project-scoped manifests exist under `.agents/skills/` and match the specified source packages.
- All 14 existing GSDD manifests remain present and unchanged.
- The pre-existing `danish-test-deck` deletions and untracked Japanese files remain unchanged, with no unrelated worktree modifications introduced.
</done>

<verify>
- `node -e "const fs=require('node:fs');const names=['systematic-debugging','test-driven-development','azure-ai','supabase-postgres-best-practices','code-security','llm-security'];for(const n of names){const p='.agents/skills/'+n+'/SKILL.md';if(!fs.existsSync(p)||!fs.statSync(p).isFile()||!fs.readFileSync(p,'utf8').trim())throw new Error('Missing or empty '+p)}console.log('verified 6 skill manifests')"`
- `node -e "const fs=require('node:fs');const root='.agents/skills';const names=fs.readdirSync(root,{withFileTypes:true}).filter(e=>e.isDirectory()&&e.name.startsWith('gsdd-')&&fs.existsSync(root+'/'+e.name+'/SKILL.md')).map(e=>e.name);if(names.length!==14)throw new Error('Expected 14 GSDD manifests, found '+names.length);console.log('verified 14 GSDD manifests')"`
- `git diff --exit-code HEAD -- .agents/skills/gsdd-*/SKILL.md`
- `npx -y skills list --json`
- `git status --short`
</verify>
